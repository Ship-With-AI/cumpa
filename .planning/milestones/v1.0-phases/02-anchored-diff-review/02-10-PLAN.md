---
phase: 02-anchored-diff-review
plan: 10
type: execute
wave: 8
depends_on:
  - 02-09
files_modified:
  - tests/helpers/git-fixture.ts
  - tests/e2e/anchored-review.spec.ts
autonomous: true
gap_closure: true
requirements:
  - DIFF-07
  - CMT-01
  - CMT-08
  - DRFT-02
must_haves:
  truths:
    - "The production-built CLI/server/UI package supports non-line-1 base and head pointer anchoring with exactly one inline composer (DIFF-07, CMT-01, D-01, D-02)."
    - "A comment added on an exact side/model line is persisted atomically and recovered after relaunching the same immutable comparison on that same anchor (DRFT-02, D-06, D-12)."
    - "The packaged workspace remains usable through wide, 1100–1439px, and narrow layouts, including focus-safe drawers and recorded stale/orphan inspection (CMT-08)."
    - "Focused unit, browser-mock, and packaged acceptance checks jointly close every blocking verification finding without substituting a source-text assertion."
  artifacts:
    - path: "tests/e2e/anchored-review.spec.ts"
      provides: "Production-built anchored gap-closure acceptance scenarios"
      contains: "packaged anchored gap closure"
    - path: "tests/helpers/git-fixture.ts"
      provides: "Deterministic multi-line immutable fixture suitable for unchanged-line base/head activation"
  key_links:
    - from: "tests/e2e/anchored-review.spec.ts"
      to: "dist/bin/diff-review.mjs"
      via: "launch packaged CLI against a real Git fixture and exercise the browser workspace"
      pattern: "diff-review"
    - from: "tests/e2e/anchored-review.spec.ts"
      to: ".diff-review"
      via: "close and relaunch the same comparison, then assert recovered comment anchor and recorded details"
      pattern: "relaunch"
    - from: "tests/e2e/anchored-review.spec.ts"
      to: "src/web/monaco/diff-adapter.ts"
      via: "user-visible hover/click and keyboard behavior exercised through production assets"
      pattern: "anchored gap closure"
---

<objective>
Reverify Phase 02 through the production-built CLI/server/UI boundary and preserve every repaired blocker as packaged browser acceptance evidence.

Purpose: The prior packaged test asserted fixed line-1 controls and tolerated duplicate composer text. The phase goal is not closed until the repaired Monaco pointer, single-composer, exact recovery, degraded-record, and responsive behaviors work through the actual package entrypoint.
Output: Deterministic Git fixture support, packaged anchored acceptance scenarios, and a focused three-layer re-verification command set.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-UI-SPEC.md
@.planning/phases/02-anchored-diff-review/02-VERIFICATION.md
@.planning/phases/02-anchored-diff-review/02-08-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-09-SUMMARY.md
@tests/helpers/git-fixture.ts
@tests/e2e/anchored-review.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace line-1 packaged acceptance with exact anchored gap-closure flows</name>
  <files>tests/helpers/git-fixture.ts, tests/e2e/anchored-review.spec.ts</files>
  <read_first>
    - tests/e2e/anchored-review.spec.ts — retain the production package launch/cleanup and real API security assertions while replacing fixed line-1 and duplicate-composer expectations.
    - tests/helpers/git-fixture.ts — reuse real Git commits/blobs and change only fixture content/helper surface needed for deterministic unchanged non-line-1 targets.
    - .planning/phases/02-anchored-diff-review/02-VERIFICATION.md — cover every blocking finding and listed related interaction regression.
    - .planning/phases/02-anchored-diff-review/02-09-SUMMARY.md — use the actual stable accessible names/data attributes delivered by the repair.
  </read_first>
  <action>
Create packaged Playwright scenarios named `packaged anchored gap closure` that launch the built CLI against the real anchored Git fixture. Ensure the fixture contains stable, visibly identifiable unchanged lines beyond line 1 on both base and head, plus changed hunks/context collapse. In the first scenario, reveal context, hover/focus and activate a non-line-1 unchanged base line and then a non-line-1 head line, assert the accessible affordance and composer header carry the exact side/model line, and assert there is exactly one textarea-backed composer. Cover the deliberate non-empty move confirmation, explicit Add Comment, atomic accepted state, and close/relaunch of the same pinned comparison with the recovered comment on its original exact anchor per D-01, D-02, D-05, D-06, and D-12.

In the second scenario, seed stale and orphaned draft outcomes through the existing supported fixture/API boundary, relaunch, and assert recorded exact facts, copy control, capability-gated recorded-file inspection, rail focus retention, and no inline relocation. Exercise 1440px, 1200px, and 900px viewports, checking Close controls, inert/aria-hidden inactive drawers, focus entry/return, one active file, and continued single-composer anchoring through resize/context/file restoration per D-07, D-09, D-10, and D-11. Use real user actions, web-first assertions, and stable model-line attributes. Do not inspect source text, use fixed screen coordinates as anchor identity, assume line 1, or assert duplicated comment text counts.
  </action>
  <verify>
    <automated>npm run test:package -- tests/e2e/anchored-review.spec.ts --grep "packaged anchored gap closure"</automated>
    <expected>The focused production-package scenarios pass for exact base/head lines, one composer, relaunch recovery, degraded records, and all three viewport bands.</expected>
  </verify>
  <acceptance_criteria>
    - Production assets—not the Vite mock server—prove non-line-1 base and head pointer anchoring.
    - Composer count is exactly one through movement, resize, context changes, file switching, and relaunch.
    - Relaunch proves the same immutable comparison and same exact anchor, not merely the same visible filename.
    - Stale/orphan inspection remains rail-only and capability-gated.
    - The 1200px scenario explicitly proves the repaired comments-drawer Close/focus contract.
  </acceptance_criteria>
  <done>The packaged acceptance suite protects all blocking and related Phase 02 interaction repairs through the actual shipped boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Execute focused cross-layer re-verification</name>
  <files>None — verification-only; this task must not modify production code or tests.</files>
  <read_first>
    - tests/unit/draft-reconciliation.test.ts — confirm exact-byte collision and side-matching contract names from Plan 02-08.
    - tests/unit/workspace-state.test.ts — confirm deliberate movement state tests from Plan 02-09.
    - tests/integration/anchored-workspace.spec.ts — confirm mock-browser arbitrary-line and responsive regression names.
    - tests/e2e/anchored-review.spec.ts — confirm production-package scenario names from Task 1.
  </read_first>
  <action>
Run only the four focused Phase 02 checks below, in order. This task is verification-only: if any command fails, stop, record the failing command and diagnostic evidence in `02-10-SUMMARY.md`, and escalate the regression to the plan that owns the declared file instead of editing any source or test file here. The exact reconciliation unit covers the identity collision, the workspace-state unit covers loss-safe movement, the Vite browser scenario covers deterministic Monaco/rail/drawer interaction, and the packaged scenario proves production delivery and relaunch. Record the exact commands/results in `02-10-SUMMARY.md`. Do not weaken assertions or run formatters, linters, the full Vitest suite, the full Playwright suite, or unrelated project tests.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/draft-reconciliation.test.ts</automated>
    <automated>npm run test:unit -- tests/unit/workspace-state.test.ts</automated>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "anchored gap closure|exact-byte draft resume"</automated>
    <automated>npm run test:package -- tests/e2e/anchored-review.spec.ts --grep "packaged anchored gap closure"</automated>
    <expected>All four focused commands pass in sequence with no skipped blocker scenario.</expected>
  </verify>
  <acceptance_criteria>
    - Exact path collision, composer movement, browser interaction, and packaged relaunch each have passing executable evidence.
    - Every Phase 02 blocking finding maps to at least one named assertion in the focused set.
    - No project-wide test, formatter, linter, package install, or schema command is used as substitute evidence.
    - Task 2 makes no source or test edits; a regression stops verification and is escalated with evidence to its owning plan.
  </acceptance_criteria>
  <done>Phase 02 gap closure has focused unit, integration, and packaged proof sufficient for verifier re-audit.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Playwright → packaged CLI/server/browser | Tests exercise the production entrypoint, loopback server, and built assets rather than a privileged test-only implementation. |
| real Git fixture → immutable comparison | Commits, blobs, path bytes, and line content originate from Git and must remain pinned across relaunch. |
| draft file → relaunched UI | Persisted local JSON crosses validation/reconciliation before any inline or recorded-file capability is granted. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-G10-01 | Tampering / Elevation | High | packaged exact anchor/relaunch flow | mitigate — blocking | Assert exact side/model line before save and after relaunch of the same commit pair; require one composer and the intended immutable blob/path identity. Packaged scenario must pass. |
| T-02-G10-02 | Spoofing | High | stale/orphan recorded-file action | mitigate — blocking | Seed degraded verification states, require recorded facts and capability gating, and assert no inline relocation or verified styling. Packaged degraded-record scenario must pass. |
| T-02-G10-03 | Information disclosure | Medium | packaged rail/clipboard | mitigate | Trigger copy explicitly, assert only recorded validated facts, and keep the loopback/API security assertions from the existing acceptance harness. |
| T-02-G10-04 | Denial of service | Medium | repeated resize/relaunch | mitigate | Exercise bounded viewport/context/file transitions and verify one composer/listener-visible outcome without unbounded polling. |
| T-02-G10-SC | Tampering | Low | package supply chain | accept | No dependency install occurs; acceptance runs the repository's pinned production build/test tooling. |

High-severity gate: T-02-G10-01 and T-02-G10-02 are blocking. Both named packaged scenarios and their prerequisite focused tests must pass before the phase can be reverified.
</threat_model>

<verification>
1. `npm run test:unit -- tests/unit/draft-reconciliation.test.ts`
2. `npm run test:unit -- tests/unit/workspace-state.test.ts`
3. `npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "anchored gap closure|exact-byte draft resume"`
4. `npm run test:package -- tests/e2e/anchored-review.spec.ts --grep "packaged anchored gap closure"`
5. Packaged smoke: wide non-line-1 unchanged base and head pointer anchors → one composer → deliberate move → Add → relaunch same pair → same exact anchor.
6. Packaged smoke: 1200px and 900px drawer close/inert/focus → stale/orphan recorded details/copy/inspect → no inline relocation.
7. Confirm no formatter, linter, project-wide test, dependency install, ORM/schema artifact, or schema-push step is involved.
</verification>

<acceptance_criteria>
- All three previously blocked requirements—DIFF-07, CMT-01, DRFT-02—have passing focused and packaged evidence.
- CMT-08 related inspection, capability, and responsive focus gaps have passing packaged evidence.
- Every required D-01/D-02/D-05/D-06/D-07/D-09/D-10/D-11/D-12 interaction is observable in the named scenarios.
- The verifier can rerun exact commands without interpreting implementation details.
</acceptance_criteria>

<success_criteria>
The production-built application proves the Phase 02 goal end to end: exact arbitrary-line review, one durable inline composer, loss-safe interaction, and correct same-comparison recovery plus degraded-record inspection.
</success_criteria>

## Artifacts this phase produces

- `tests/helpers/git-fixture.ts`
  - deterministic non-line-1 unchanged base/head anchor fixture support
- `tests/e2e/anchored-review.spec.ts`
  - `packaged anchored gap closure` exact-anchor/relaunch scenario
  - `packaged anchored gap closure` degraded-record/responsive scenario

<output>
Create `.planning/phases/02-anchored-diff-review/02-10-SUMMARY.md` when done.
</output>
