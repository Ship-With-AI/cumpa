---
phase: 02-anchored-diff-review
plan: 02
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - src/contracts/api.ts
  - src/contracts/draft.ts
  - src/domain/anchor.ts
  - src/server/capabilities.ts
  - src/server/routes.ts
  - tests/unit/anchor.test.ts
  - tests/git/anchored-content.test.ts
  - tests/api/anchor.test.ts
autonomous: true
requirements: [DIFF-02, DIFF-03, CMT-01, CMT-02, CMT-08]
must_haves:
  truths:
    - "A browser holding an authenticated launch-scoped opaque file capability can fetch exact frozen base/head supported text, but cannot select repository, ref, commit, object, path, draft key, or filesystem location."
    - "An add request carries only fileId, base/head side, positive side-specific line, and bounded nonblank body; the server derives the exact side path, blob, selected line text, nearby context, context hash, and uniqueness key."
    - "Base comments use the base-side old path/blob and head comments use the head-side new path/blob for asymmetric changes and renames; one comment per exact side-specific line is enforceable without viewport or rendered-row data."
    - "Exact verification returns only verified, stale, or orphaned, preserves the recorded anchor, and never searches, rewrites, or silently relocates it."
  artifacts:
    - path: src/domain/anchor.ts
      provides: "Canonical side-specific anchor construction, framed hashes, uniqueness, and exact verification"
    - path: src/contracts/draft.ts
      provides: "Strict persisted anchor/comment and presentation verification contracts"
    - path: tests/git/anchored-content.test.ts
      provides: "Real-Git frozen side/path/blob/content contract"
    - path: tests/api/anchor.test.ts
      provides: "Closed capability and server-derived anchor API contract"
  key_links:
    - from: src/server/routes.ts
      to: src/server/capabilities.ts
      via: "authenticated opaque fileId resolution before frozen side content access"
      pattern: "fileId"
    - from: src/server/routes.ts
      to: src/domain/anchor.ts
      via: "server builds all immutable anchor fields from resolved frozen content"
      pattern: "build.*Anchor"
---

<objective>
Define and implement the canonical closed content/anchor contract through RED→GREEN→REFACTOR.

Purpose: Make CMT-02 and CMT-08 independently correct at the server boundary, including exact side-specific old/new path and blob authority, before persistence or UI composition.
Output: Strict shared schemas, closed frozen-content route extension, canonical anchor builder/verifier, and focused unit/Git/API proofs.
</objective>

## Phase Goal
**As a** developer reviewing a pinned local comparison, **I want to** inspect immutable text side by side and attach precise comments that remain stable, **so that** I can resume trustworthy feedback without publishing branches or relocating anchors.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-RESEARCH.md
@.planning/phases/02-anchored-diff-review/02-PATTERNS.md
@.planning/phases/02-anchored-diff-review/02-01-SUMMARY.md
</context>

<feature>
  <name>Canonical server-owned side-specific anchors</name>
  <files>src/contracts/api.ts, src/contracts/draft.ts, src/domain/anchor.ts, src/server/capabilities.ts, src/server/routes.ts, tests/unit/anchor.test.ts, tests/git/anchored-content.test.ts, tests/api/anchor.test.ts</files>
  <behavior>
    - fileId + base + line resolves the frozen base blob and old/base path; fileId + head + line resolves the frozen head blob and new/head path.
    - The selected text is the entire exact target model line without its line terminator; up to three existing lines before/after are retained with line numbers.
    - Domain-separated unsigned-64-bit-length-framed SHA-256 is deterministic across UTF-8, CRLF, LF, blank lines, no-final-newline, control-bearing lossless paths, and SHA-1/SHA-256-length opaque Git OIDs.
    - Durable uniqueness includes exact path bytes, side, blob OID, and 1-based line; base/head on the same visual row differ while duplicate exact anchors match.
    - Exact verification produces verified only when path/side/blob/line/text/context/hash match; mismatched available facts are stale; unavailable file/blob/side/out-of-range line is orphaned; no output contains a relocated line.
    - Security/token/Host/Origin denial occurs before capability/object/anchor work; strict requests reject derived authority and unknown keys.
  </behavior>
  <implementation>Extend the actual Phase 1 strict contract, capability, security, object-reader, and route seams recorded in 02-01-SUMMARY.md. The paths in this plan are the expected handoff locations and must be reconciled to actual implemented names before edit; do not create parallel modules when Phase 1 placed the seam elsewhere.</implementation>
</feature>

<tasks>
<task type="auto">
  <name>Task 1: RED — specify side-canonical anchor construction and verification</name>
  <files>tests/unit/anchor.test.ts, tests/git/anchored-content.test.ts, tests/api/anchor.test.ts</files>
  <read_first>Read 02-01-SUMMARY.md's actual Phase 1 seam map, the implemented comparison/path/API/security/object tests, and the proven Monaco model-line contract. Read CMT-02/CMT-08 and Research Patterns 5 and 8.</read_first>
  <action>Write focused tests before production changes. Unit cases cover first/last/only/empty/blank lines, three-line boundaries, LF/CRLF/no-final-newline, Unicode, lossless unusual paths, opaque SHA-1/SHA-256-length OIDs, deterministic framed context hash, uniqueness, duplicate side-line identity, and exact verified/stale/orphaned classification with an assertion that recorded anchors remain byte-for-byte unchanged. Real disposable Git cases cover addition/deletion/modification/rename, old path/base blob versus new path/head blob, revealed unchanged text, later ref/worktree movement, and missing objects. Fastify inject cases cover exact content DTOs, valid base/head add inputs, bounds, whitespace/oversize body, strict unknown-key rejection, arbitrary repo/ref/commit/blob/path/context/hash/draft-key attempts, unknown fileId, unsupported side/content, and auth-before-work spies. RED must be named domain/API behavior after fixture and inherited security prerequisites succeed. Commit `test(02-02): add failing canonical anchor contract`.</action>
  <verify><automated>
    <command expected="nonzero" classification="behavioral-red">npm run test:unit -- tests/unit/anchor.test.ts</command>
    <command expected="nonzero" classification="behavioral-red">npm run test:git -- tests/git/anchored-content.test.ts</command>
    <command expected="nonzero" classification="behavioral-red">npm run test:api -- tests/api/anchor.test.ts</command>
  </automated></verify>
  <done>All three focused layers reach missing anchor behavior, independently prove fixtures/prerequisites, and the RED commit precedes implementation.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — derive exact anchors behind the inherited closed capability</name>
  <files>src/contracts/api.ts, src/contracts/draft.ts, src/domain/anchor.ts, src/server/capabilities.ts, src/server/routes.ts</files>
  <read_first>Read the RED tests and actual reconciled Phase 1 modules only once. Preserve implemented error vocabulary, app registration, strict schema convention, lossless path type, object-read limits, and onRequest guard ordering.</read_first>
  <action>Extend—not replace—the actual Phase 1 file capability with enough frozen side facts to read supported base/head text. Add only `GET /api/files/:fileId/content` and the strict anchor-input portion of `POST /api/draft/comments`; the latter may call an injected mutation port completed in Plan 03, but canonical anchor construction must be real and tested now. Browser authority is exactly fileId, side, line, and body. Return side-existence, safe side path/language hint, full blob OID, and exact text for the content route; choose syntax language from path only and never alter text. Build DurableAnchorV1 server-side using exact lossless side path, safe display path, side, 1-based line, full blob OID, exact target text, up to three numbered surrounding lines, and a domain-separated unsigned-64-bit-length-framed SHA-256 context hash. Build the duplicate key from framed exact path bytes, side, blob, and line. Verification compares exact active pinned facts and returns presentation state/reason without mutation or nearby search. Reuse Phase 1 token/Host/Origin guard before parsing/object work and retain generic external errors. Implement D-12 and CMT-02; do not accept browser-derived anchor fields, read worktrees, follow refs, use textconv, execute repository content, or add Phase 3 reattachment/recovery/lifecycle behavior. Run identical commands to GREEN and commit `feat(02-02): implement canonical side anchors`.</action>
  <verify><automated>
    <command expected="0">npm run test:unit -- tests/unit/anchor.test.ts</command>
    <command expected="0">npm run test:git -- tests/git/anchored-content.test.ts</command>
    <command expected="0">npm run test:api -- tests/api/anchor.test.ts</command>
  </automated></verify>
  <done>Exact frozen content and canonical anchors are GREEN for both sides and all authority/verification/security boundaries.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR — consolidate canonical framing without changing authority</name>
  <files>src/contracts/draft.ts, src/domain/anchor.ts, src/server/capabilities.ts, src/server/routes.ts</files>
  <read_first>Read the GREEN implementation and focused failures/output already available; do not re-open unrelated Phase 1 modules.</read_first>
  <action>Consolidate byte framing, side selection, context extraction, and verification reason construction only where it removes duplication. Keep Node crypto as the sole hash primitive, opaque OIDs unparsed, server authority unchanged, and transport/persisted/presentation schemas distinct. Run the identical three commands unchanged. Commit `refactor(02-02): consolidate anchor derivation` only if production code changes.</action>
  <verify><automated>
    <command expected="0">npm run test:unit -- tests/unit/anchor.test.ts</command>
    <command expected="0">npm run test:git -- tests/git/anchored-content.test.ts</command>
    <command expected="0">npm run test:api -- tests/api/anchor.test.ts</command>
  </automated></verify>
  <done>Post-refactor behavior remains GREEN with the test→feat→optional-refactor sequence recorded.</done>
</task>
</tasks>

<acceptance_criteria>
- Content and anchor routes extend the actual Phase 1 closed launch-scoped app and earliest security guard.
- Every CMT-02 field is derived from the exact selected side's frozen object; rename fixtures prove base-old/head-new path and blob selection.
- Duplicate identity is side-specific and exact; stale/orphan states preserve records and never relocate.
- Strict requests cannot smuggle repository, object, path, comparison, context, hash, or draft authority.
</acceptance_criteria>

## Artifacts this phase produces
- `src/contracts/draft.ts` — strict versioned durable anchor/comment and presentation schemas.
- `src/domain/anchor.ts` — canonical builder, uniqueness key, and exact verifier.
- Focused unit, real-Git, and Fastify anchor contracts under `tests/`.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser → loopback API | Untrusted token, Host, Origin, fileId, side, line, body, and unknown fields. |
| Capability registry → Git objects | Only launch-authorized frozen side facts may select object data. |
| Persisted record → verification view | Stored JSON is validated and compared exactly, never used as live repository authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-05 | Spoofing | loopback caller | mitigate | Reuse Phase 1 high-entropy Bearer plus exact Host and absent-or-exact Origin before work. |
| T-02-06 | Tampering | anchor request | mitigate | Strict four-field request; derive all immutable fields server-side from fileId capability. |
| T-02-07 | Elevation of Privilege | object selection | mitigate | No request-selected repo/ref/commit/blob/path; frozen registry and bounded object reader only. |
| T-02-08 | Information Disclosure | API errors/logs | mitigate | Generic capability/security errors; no tokens, blob text, bodies, absolute paths, Git commands, or attempted values in browser/log assertions. |
| T-02-09 | Repudiation | anchor verification | mitigate | Deterministic versioned SHA-256 context and uniqueness keys over length-framed immutable facts. |
| T-02-10 | Denial of Service | request/content | mitigate | Strict bounded fileId/body/line and inherited text availability/size classification before allocation. |
| T-02-SC | Tampering | dependencies | mitigate | No package installs; use approved lockfile and Node crypto/Zod already present. |
</threat_model>

<verification>Each focused unit, Git, and API command demonstrates RED then GREEN then unchanged post-refactor GREEN. No formatter, linter, aggregate suite, browser matrix, or project-wide test runs.</verification>

<success_criteria>
- DIFF-02/DIFF-03 frozen-content authority and CMT-01/CMT-02/CMT-08 anchor contracts are independently executable and tested.
- TDD gate commits exist in order.
- No Phase 3 edit/delete/resolve/summary/conflict/drift or reattachment behavior is introduced.
</success_criteria>

<output>Create `.planning/phases/02-anchored-diff-review/02-02-SUMMARY.md` with reconciled source paths, RED/GREEN/REFACTOR commands and commits, side/path/blob fixtures, framing version, verification matrix, and closed-authority evidence.</output>
