---
phase: 02-anchored-diff-review
plan: 05
type: execute
wave: 3
depends_on: [02-02, 02-04]
files_modified:
  - src/web/api/client.ts
  - src/web/App.vue
  - src/web/styles.css
  - src/web/components/DiffWorkspace.vue
  - src/web/components/ReviewToolbar.vue
  - src/web/components/KeyboardHelp.vue
  - src/web/components/ui/UiPrimitives.vue
  - tests/integration/anchored-workspace.spec.ts
autonomous: true
requirements: [DIFF-02, DIFF-03, DIFF-04, DIFF-05, DIFF-07, CMT-01]
must_haves:
  truths:
    - "The actual Phase 1 identity header and deterministic changed-file tree remain the single identity/selection surfaces while one active supported file opens an exact read-only syntax-highlighted side-by-side diff; unsupported/no-change/security/session states remain inherited."
    - "Every shell/navigation control has its keyboard peer, accessible name, tooltip/help copy, boundary disabled state, and focus behavior: previous/next file, previous/next change, comments entry, keyboard help, side-line activation, and Monaco accessibility help."
    - "D-10: Persistent Previous/Next file and Previous/Next change buttons are always available, each mirrored by its documented non-conflicting keyboard shortcut and exposed through discoverable tooltip and keyboard-help copy."
    - "Per-file scroll, focus, collapsed context, and one unaccepted composer anchor/text remain session-only and restore only after immutable models and diff are ready through switching and responsive resize."
    - "D-08: Unchanged regions start collapsed and each region offers both a bounded-chunk reveal control and an all-remaining-context reveal control."
    - "The 640px narrow layout remains side by side with compact persistent navigation and reaches 200% zoom without stacked/unified layout, identity loss, or recreated Monaco models."
  artifacts:
    - path: src/web/components/DiffWorkspace.vue
      provides: "One-file side-by-side workspace using the proven Monaco adapter"
    - path: src/web/components/ReviewToolbar.vue
      provides: "Persistent labeled file/change navigation and comments/help entry controls"
    - path: src/web/components/KeyboardHelp.vue
      provides: "Discoverable documented keyboard contract"
    - path: tests/integration/anchored-workspace.spec.ts
      provides: "Focused real-browser shell, navigation, responsive, and session-restoration contract"
  key_links:
    - from: src/web/components/DiffWorkspace.vue
      to: src/web/monaco/diff-adapter.ts
      via: "one mounted adapter driven by opaque file selection and session state"
      pattern: "diff-adapter"
    - from: src/web/App.vue
      to: src/web/model/workspace-state.ts
      via: "Phase 1 selection drives deterministic save/switch/restore commands"
      pattern: "workspace-state"
---

<objective>
Compose the proven adapter, closed content API, and session-state seams into the accessible Phase 2 review shell and navigation slice.

Purpose: Deliver D-07–D-12 shell, responsive, navigation, and restoration behavior without duplicating Phase 1 identity/security/path authority or coupling it to comment persistence and rail work.
Output: Integrated Vue diff shell, exact visible/keyboard navigation, responsive/session states, and the first focused real-browser interaction slice.
</objective>

## Phase Goal
**As a** developer reviewing a pinned local comparison, **I want to** inspect immutable text side by side and attach precise comments that remain stable, **so that** I can resume trustworthy feedback without publishing branches or relocating anchors.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-UI-SPEC.md
@.planning/phases/02-anchored-diff-review/02-01-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-02-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-04-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Compose one-file diff, responsive navigation, and session restoration</name>
  <files>src/web/api/client.ts, src/web/App.vue, src/web/styles.css, src/web/components/DiffWorkspace.vue, src/web/components/ReviewToolbar.vue, src/web/components/KeyboardHelp.vue, src/web/components/ui/UiPrimitives.vue, tests/integration/anchored-workspace.spec.ts</files>
  <read_first>Read the actual Phase 1 App/header/tree/styles/client components and identifiers from 02-01-SUMMARY.md, all Phase 2 summaries, and UI-SPEC Design System, Screen Architecture, Navigation, Collapsed Context, Loading/Empty/Error, and Accessibility sections. Reuse any actual Phase 1 primitive; the proposed component filenames in planning are not proof of current source.</read_first>
  <action>Extend the actual Phase 1 shell rather than replacing it. Keep its one persistent pinned identity header, dirty cue, exact path details, deterministic opaque file tree, unsupported/no-change/security/session states, and selection identity. Replace only the supported-file metadata center with one active-file DiffWorkspace using exact content DTOs and the proven long-lived adapter; no stacked/unified layout or toggle. Implement warm compact UI-SPEC tokens/local primitives without third-party components, one light theme, declared wide columns/drawers, 640px side-by-side narrow fallback, 200% zoom, reduced motion, landmarks, skip links, one active-file h1, and exact Monaco labels. Per D-10, add persistent labeled Previous file (`Alt+Shift+[`), Next file (`Alt+Shift+]`), Previous change (`Shift+F7`), Next change (`F7`), Comments, and Keyboard help (`?` outside text inputs) controls with boundary disabled copy and tooltips; the four previous/next buttons are mirrored by those documented, non-conflicting shortcuts in visible help. Help contains exactly the UI-SPEC keyboard list, including Option/Alt+Enter, Command/Ctrl+Enter, Escape, tree arrows/Enter, Tab/Shift+Tab, and Alt+F1. Scope shortcuts away from incompatible text/detail controls. Per D-08, render every unchanged region collapsed by default and expose separate controls to reveal the configured bounded context chunk or all remaining context in that region through the proven public adapter; restore per-file session state after diff-ready and resize only by layout. Add focused real-browser cases for exact text/read-only/language, inherited states, every button/shortcut/tooltips/help, drawers/focus, both context reveal controls from the collapsed default, A→B→A state, and resize. Do not add duplicate identity/tree/security/API vocabularies or active selector drift.</action>
  <verify><automated><command expected="0">npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "diff navigation and session state"</command></automated></verify>
  <done>D-08 is observable through default-collapsed unchanged regions with bounded-chunk and all-remaining reveal controls; D-10 is observable through persistent previous/next file/change buttons, documented non-conflicting shortcut peers, and discoverable tooltips/help. The one-file workspace preserves Phase 1 authority, all documented navigation paths and states are visible/keyboard-operable, and focused browser behavior passes.</done>
</task>

</tasks>

<acceptance_criteria>
- The actual Phase 1 shell and authority seams are extended once, not reimplemented from PLAN assumptions.
- Every UI-SPEC shell/navigation control, tooltip/help item, focus path, responsive rule, loading/empty/error state, and session-restoration behavior in this slice is present.
- One long-lived real Monaco adapter survives deterministic file/context navigation and resize without model recreation or state loss.
- The interface contains no Phase 3/later lifecycle, summary, conflict, drift, export, reattachment, stacked/unified, thread, or theme behavior.
</acceptance_criteria>

## Artifacts this phase produces
- Vue review shell, toolbar/help, responsive state, and Monaco workspace integrated into the actual Phase 1 shell.
- Local UI primitives/tokens required by this UI-SPEC slice, reusing actual Phase 1 equivalents where present.
- `tests/integration/anchored-workspace.spec.ts` — focused real-browser shell/navigation contract extended by dependent Plan 06.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser UI → closed content API | UI event/state cannot add repository, path, object, comparison, anchor-derived, or draft authority. |
| Phase 1 selection → Monaco workspace | Opaque file identity and frozen DTOs remain the only content/selection authority. |
| Responsive/navigation controls → editor state | Commands use the tested workspace-state transition seam and cannot recreate models or invent anchors. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-21 | Spoofing | session/content UI | mitigate | Reuse in-memory bearer client and strict same-origin DTO validation; never display another comparison's data. |
| T-02-22 | Tampering | navigation identity | mitigate | Opaque fileId and deterministic Phase 1 tree order only; no path/object authority enters a UI command. |
| T-02-23 | Information Disclosure | UI errors/copy | mitigate | Exact safe copy omits token, absolute path, object command/stderr, attempted authority, blob text, and internal correlation details. |
| T-02-24 | Elevation of Privilege | rendered text | mitigate | Vue escapes path text; Monaco is read-only; no dynamic HTML, link execution, repository code execution, or writable command. |
| T-02-25 | Denial of Service | layout/model lifecycle | mitigate | One active file, bounded controls, proven adapter ownership, and resize without model recreation. |
| T-02-26 | Repudiation | state restoration | mitigate | Focus/scroll/context/composer transitions are deterministic and covered through A→B→A browser observations. |
| T-02-27 | Tampering | hidden-context navigation | mitigate | Public Monaco reveal/actions and model lines only; no rendered-row or pixel authority. |
| T-02-SC | Tampering | UI supply chain | mitigate | No packages or remote component registry; local primitives/SVG only. |
</threat_model>

<verification>Run only the focused `diff navigation and session state` real-browser contract. It tests observable behavior in real Monaco/Vue, not source text or private internals. Do not run formatter, linter, aggregate browser suite, API/unit sweep, or project-wide tests.</verification>

<success_criteria>
- The Phase 2 browser shell satisfies DIFF-02–DIFF-05, DIFF-07, CMT-01, D-07–D-12, and its UI-SPEC shell/navigation contracts.
- Accessibility/focus/keyboard behavior, responsive geometry, and per-file session restoration are observable.
- Shell integration occurs only after the mandatory Plan 01 gate and consumes Plans 02 and 04 contracts; Plan 06 adds persistence and rail behavior without reopening this slice.
</success_criteria>

<output>Create `.planning/phases/02-anchored-diff-review/02-05-SUMMARY.md` with actual Phase 1 seams used, focused shell/navigation command evidence, visible/keyboard control matrix, responsive/accessibility observations, per-file restoration proof, and later-phase exclusion audit.</output>
