---
phase: 02-anchored-diff-review
plan: 04
type: tdd
wave: 2
depends_on: [02-01]
files_modified:
  - src/web/model/workspace-state.ts
  - src/web/monaco/diff-adapter.ts
  - tests/unit/workspace-state.test.ts
  - tests/unit/line-mapping.test.ts
autonomous: true
requirements: [DIFF-04, DIFF-05, DIFF-07, CMT-01]
must_haves:
  truths:
    - "Each opaque fileId owns session-memory-only scroll/diff view state, focused side/model line, collapsed/restored/all-revealed context, and one unaccepted composer anchor/text; none enters repository JSON or browser persistence."
    - "A→B→A restores A only after its immutable models and diff update are ready; resize lays out in place without recreating models or resetting state."
    - "Pointer and keyboard activation share one transition: an uncommented side-line opens/focuses its composer, an existing exact side-line focuses the canonical comment, and non-empty composer movement requires explicit discard confirmation."
    - "Persistent previous/next file follows deterministic Phase 1 reviewable-tree order and previous/next change delegates to the proven public Monaco action; hidden-comment navigation switches file, reveals exact context, reconstructs zones, centers, focuses, and announces in order."
  artifacts:
    - path: src/web/model/workspace-state.ts
      provides: "Deterministic session-only per-file interaction state machine"
    - path: tests/unit/workspace-state.test.ts
      provides: "Switch, activation, composer, save acceptance, and reveal transition contract"
  key_links:
    - from: src/web/model/workspace-state.ts
      to: src/web/monaco/diff-adapter.ts
      via: "commands apply state only after diff-ready events and use side/model coordinates"
      pattern: "fileId"
---

<objective>
Specify and implement the session-only per-file workspace interaction state machine through RED→GREEN→REFACTOR.

Purpose: Make D-03–D-06 and D-09–D-12 deterministic before Vue rendering, while reusing the browser-proven Monaco adapter instead of embedding state in DOM/view-zone identities.
Output: Pure transition model, adapter command seam, and focused state/mapping tests.
</objective>

## Phase Goal
**As a** developer reviewing a pinned local comparison, **I want to** inspect immutable text side by side and attach precise comments that remain stable, **so that** I can resume trustworthy feedback without publishing branches or relocating anchors.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-UI-SPEC.md
@.planning/phases/02-anchored-diff-review/02-01-SUMMARY.md
</context>

<feature>
  <name>Session-only anchored workspace transitions</name>
  <files>src/web/model/workspace-state.ts, src/web/monaco/diff-adapter.ts, tests/unit/workspace-state.test.ts, tests/unit/line-mapping.test.ts</files>
  <behavior>
    - Switching saves outgoing diff view/scroll, focused side-line, context mode/expansions, and composer; incoming restore waits for diff-ready and never submits on blur.
    - Empty Escape/discard closes immediately; non-empty Escape/discard enters confirmation; Escape from confirmation keeps writing; only explicit destructive confirmation removes text.
    - Add starts pending but not accepted; failure preserves anchor/text and returns recoverable error; only canonical persisted response replaces composer with accepted comment.
    - Existing exact side-line activation focuses its comment; base/head and same numeric line remain distinct.
    - Hidden-comment navigation uses bounded/public reveal or proven all-context fallback before focus and never changes recorded side/line.
    - Resize only invokes layout; file order comes from actual Phase 1 deterministic reviewable leaves; change navigation invokes public previous/next diff action.
  </behavior>
  <implementation>Use the adapter result proven in 02-01-SUMMARY.md. This model is browser-memory state only and must not introduce localStorage, sessionStorage, IndexedDB, draft fields, or server persistence.</implementation>
</feature>

<tasks>
<task type="auto">
  <name>Task 1: RED — specify session state, composer, navigation, and acceptance transitions</name>
  <files>tests/unit/workspace-state.test.ts, tests/unit/line-mapping.test.ts</files>
  <read_first>Read the proven prototype summary/adapter contract, actual Phase 1 file-tree projection, UI-SPEC Navigation, Per-file state, Comment Creation, Discard, Persistence, and Show comment sequence, plus D-03–D-06/D-09–D-12.</read_first>
  <action>Write tests before production changes. Cover A→B→A exact state restoration after diff-ready, resize without model reset, separate base/head line keys, one active composer per file, per-file non-empty composer retention, empty versus non-empty cancel/confirmation transitions, blur preservation, whitespace validation, explicit Add/Cmd-Ctrl+Enter only, pending/failure/duplicate/success outcomes, canonical-response-only accepted state, deterministic first/last file navigation, public previous/next change commands, hidden verified-comment reveal-before-focus order, stale/orphan no-inline placement, and no persistence serialization of view state. Extend the prototype mapping tests for insertion/deletion/change/unchanged counterpart boundaries only if that logic was not fully covered in Plan 01. RED must be a named missing transition after the current unit harness loads. Commit `test(02-04): add failing workspace state contract`.</action>
  <verify><automated><command expected="nonzero" classification="behavioral-red">npm run test:unit -- tests/unit/workspace-state.test.ts tests/unit/line-mapping.test.ts</command></automated></verify>
  <done>The focused suite reaches missing transition behavior and the RED commit precedes implementation.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — implement immutable-coordinate session transitions</name>
  <files>src/web/model/workspace-state.ts, src/web/monaco/diff-adapter.ts</files>
  <read_first>Read the RED tests, proven adapter public API, and actual Phase 1 deterministic tree API recorded during reconciliation.</read_first>
  <action>Implement a typed in-memory record keyed only by opaque fileId with complete Monaco diff view/scroll state, focused side/model line, context expansion/mode, and optional side-line composer text/status. Make activation, file switch, resize, file navigation, change navigation, comment reveal, cancel/confirmation, add-pending/failure/duplicate/success explicit transitions/commands. Pointer and keyboard dispatch the same activate command. On switch snapshot outgoing state; on incoming diff-ready restore model state, rebuild adapter zones/decorations, reveal exact composer/comment line, then focus. A successful add consumes only the canonical server response; a failure retains exact text/anchor. An existing canonical uniqueness key focuses rather than opens a duplicate. Stale/orphan records remain navigation metadata only and cannot request inline placement. Use the proven public all-context fallback when required. Keep D-09/D-11 state session-only, with no storage event, serialization, route, or repository draft field. Run identical tests to GREEN and commit `feat(02-04): implement workspace state transitions`.</action>
  <verify><automated><command expected="0">npm run test:unit -- tests/unit/workspace-state.test.ts tests/unit/line-mapping.test.ts</command></automated></verify>
  <done>All switching, navigation, composer, persistence-response, and reveal transitions are GREEN and depend only on immutable coordinates.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR — separate pure state from imperative adapter commands</name>
  <files>src/web/model/workspace-state.ts, src/web/monaco/diff-adapter.ts</files>
  <read_first>Read the GREEN state and adapter changes only.</read_first>
  <action>Keep pure transitions separate from imperative Monaco/API effects so tests observe state/commands rather than private editor internals. Preserve transition ordering and adapter public contract. Run the identical command unchanged and commit `refactor(02-04): separate workspace state and effects` only if production changes.</action>
  <verify><automated><command expected="0">npm run test:unit -- tests/unit/workspace-state.test.ts tests/unit/line-mapping.test.ts</command></automated></verify>
  <done>Post-refactor GREEN and TDD commit sequence are recorded.</done>
</task>
</tasks>

<acceptance_criteria>
- Session-only state preserves scroll, focus, expansion, and composer per file through switch/resize without browser or repository persistence.
- One-per-side-line activation and explicit acceptance/cancel semantics implement D-03–D-06.
- File/change controls and comment reveal have deterministic command ordering, including reveal before focus.
- No viewport/pixel/rendered-row/DOM/view-zone identity participates in state or comment placement.
</acceptance_criteria>

## Artifacts this phase produces
- `src/web/model/workspace-state.ts` — pure session interaction state and imperative command outputs.
- Focused workspace/mapping behavior contracts under `tests/unit/`.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Vue/Monaco events → workspace state | Pointer, focus, keyboard, resize, and diff-ready timing are untrusted event order. |
| API response → accepted UI | Only strict canonical server responses may create accepted state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-17 | Tampering | event-derived anchor | mitigate | Side/model position from active Monaco model; one typed activation path; no pixels/rows/DOM IDs. |
| T-02-18 | Repudiation | accepted comment state | mitigate | Pending is distinct; only validated canonical success transitions to accepted; failures retain text. |
| T-02-19 | Denial of Service | repeated switches/resizes | mitigate | In-memory bounded per-file state, layout-only resize, proven adapter disposal, no model recreation on resize. |
| T-02-20 | Information Disclosure | browser persistence | mitigate | View/composer state remains memory-only; no local/session storage or repository serialization. |
| T-02-SC | Tampering | dependencies | mitigate | No installs. |
</threat_model>

<verification>The single focused unit command demonstrates named RED, GREEN, and unchanged post-refactor GREEN. No formatter, linter, browser suite, API suite, or project-wide test runs.</verification>

<success_criteria>
- DIFF-04/DIFF-05/DIFF-07 and interaction-state portions of CMT-01 are deterministic before UI composition.
- TDD gates exist in order.
- No later-phase lifecycle, summary, conflict, drift, export, reattachment, stacked layout, thread, or theme behavior appears.
</success_criteria>

<output>Create `.planning/phases/02-anchored-diff-review/02-04-SUMMARY.md` with RED/GREEN/REFACTOR evidence, transition table, effect ordering, session-only proof, adapter linkage, and commits.</output>
