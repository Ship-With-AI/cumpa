---
phase: 02-anchored-diff-review
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/web/monaco/configure.ts
  - src/web/monaco/diff-adapter.ts
  - src/web/monaco/line-mapping.ts
  - src/web/prototypes/MonacoStabilityPrototype.vue
  - tests/integration/monaco-anchor.spec.ts
autonomous: false
requirements: [DIFF-02, DIFF-03, DIFF-04, DIFF-05, DIFF-07, CMT-01]
must_haves:
  truths:
    - "Execution begins with a no-edit checkpoint that distinguishes implemented Phase 1 source from planned-only seams and approves exact Monaco; installation, lockfile update, production worker wiring, and RED occur only after approval against then-current Phase 1 source."
    - "A production-shaped Chromium page using real Monaco 0.55.1 proves read-only side-by-side exact text, path-selected language, public unchanged-region controls, file/change navigation, and base/head comment activation before broad workspace integration."
    - "Side/model-line anchors and paired zones remain aligned through resize, reveal/collapse, text growth, A→B→A switching, state restore, and at least ten diff recomputations/model swaps without leaked models, zones, listeners, or duplicate composers."
    - "Failure of any stability scenario blocks Plans 02-04; only a visibly verified full-width synchronized row directly below the anchored diff row is an allowed fallback when paired public view zones cannot satisfy D-02/D-07/D-12."
  artifacts:
    - path: package-lock.json
      provides: "Approved exact monaco-editor@0.55.1 artifact resolved by the repository package manager"
    - path: src/web/monaco/configure.ts
      provides: "Production and prototype Vite ESM worker routing plus path-selected language configuration"
    - path: tests/integration/monaco-anchor.spec.ts
      provides: "Blocking real-browser Monaco stability contract"
    - path: src/web/monaco/diff-adapter.ts
      provides: "Production-shaped public-API adapter proven by the prototype"
    - path: src/web/monaco/line-mapping.ts
      provides: "Side-specific model-coordinate counterpart mapping"
  key_links:
    - from: tests/integration/monaco-anchor.spec.ts
      to: src/web/prototypes/MonacoStabilityPrototype.vue
      via: "real Chromium geometry, keyboard, lifecycle, and model-count observations"
      pattern: "MonacoStabilityPrototype"
    - from: src/web/prototypes/MonacoStabilityPrototype.vue
      to: src/web/monaco/diff-adapter.ts
      via: "the same adapter broad workspace integration will consume"
      pattern: "diff-adapter"
---

<objective>
Reconcile the actual Phase 1 handoff, then pass the mandatory production-shaped real-browser Monaco stability gate before any broad workspace integration.

Purpose: Retire the highest-risk D-01/D-02/D-07–D-12 uncertainty with observable browser geometry and public Monaco APIs rather than planning against unexecuted Phase 1 prose.
Output: Reconciliation record in the execution summary, audited exact Monaco dependency, a reusable adapter/prototype, and a blocking focused Chromium contract.
</objective>

## Phase Goal
**As a** developer reviewing a pinned local comparison, **I want to** inspect immutable text side by side and attach precise comments that remain stable, **so that** I can resume trustworthy feedback without publishing branches or relocating anchors.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-RESEARCH.md
@.planning/phases/02-anchored-diff-review/02-UI-SPEC.md
@.planning/phases/02-anchored-diff-review/02-PATTERNS.md
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-01-SUMMARY.md
@.planning/phases/01-pinned-local-comparison/01-09-SUMMARY.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Audit implemented Phase 1 seams and approve the exact Monaco release</name>
  <files>None — pre-edit audit and approval checkpoint; repository files remain unchanged</files>
  <read_first>
    Read every available executed Phase 1 SUMMARY and then inspect the actual package scripts, Vue entry/composition root, styles/primitives, opaque file-selection contract, Fastify app factory/routes/security/capability registry, object reader, API client, and focused test commands. Because Phase 1 may still be planned-only, treat every Phase 1 PLAN path as a proposed seam and report absent implementation explicitly rather than pretending a SUMMARY or source seam exists. Read the current lockfile and official registry metadata for monaco-editor@0.55.1.
  </read_first>
  <action>Before any edit or install, present a reconciliation table in the checkpoint evidence mapping each Phase 2 dependency to its actual implemented Phase 1 path, export, DTO, and focused command, or marking it absent when Phase 1 has not yet executed. Present the monaco-editor@0.55.1 legitimacy audit against the research record and current npm metadata (maintainers, publisher, repository, release time, scripts, engines, provenance/signature). Ask the human to approve or reject both the source reconciliation and exact package. Do not modify package.json, package-lock.json, worker configuration, prototype, tests, or product source in this checkpoint; do not create or require 02-01-SUMMARY.md, which is end-of-plan closeout.</action>
  <verify><automated><command expected="0">npm view monaco-editor@0.55.1 version maintainers _npmUser repository time engines peerDependencies scripts dist</command></automated><human-check>Confirm the reconciliation distinguishes implemented Phase 1 source from proposed PLAN seams, then approve or reject monaco-editor@0.55.1 before installation.</human-check></verify>
  <done>Checkpoint evidence contains the actual-or-absent Phase 1 seam map and exact Monaco audit, with explicit human approval recorded and no repository file changed.</done>
  <resume-signal>Type "approved" to authorize Task 2 to reconcile against the then-current Phase 1 source, install monaco-editor@0.55.1, wire production workers, and begin RED; otherwise describe the rejected evidence and stop.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reconcile approved Phase 1 seams, install Monaco, wire production workers, and establish RED</name>
  <files>package.json, package-lock.json, src/web/monaco/configure.ts, src/web/prototypes/MonacoStabilityPrototype.vue, tests/integration/monaco-anchor.spec.ts</files>
  <read_first>Require the Task 1 checkpoint transcript to contain explicit approval; it is the workflow-valid handoff, not 02-01-SUMMARY.md. Re-read the actual Phase 1 package scripts, Vue entry/composition root, browser build/test configuration, API client, file tree, shell, styles/primitives, app factory/routes/security/capability registry, and focused test commands now present on disk. Use the checkpoint reconciliation only as an audit trail and update it mentally from current source; if Phase 1 is still absent, stop because this phase depends on implemented Phase 1. Read UI-SPEC Navigation Contract, Collapsed Context Contract, Inline Geometry, Accessibility Contract, and Mandatory stability gate plus Monaco 0.55.1 public declarations for diff editor, side editors, view zones, decoration collections, view state, hidden regions, line changes, onDidUpdateDiff, goToDiff, and model disposal.</read_first>
  <action>First reconcile every Phase 2 dependency to the current implemented Phase 1 path/export/DTO/command and reuse those seams without creating a second app factory, security guard, capability registry, path representation, file tree, API client, shell, or test vocabulary. After the recorded Task 1 approval, install exactly monaco-editor@0.55.1 through the repository package manager so package.json and package-lock.json record the approved artifact. Create src/web/monaco/configure.ts as the production worker setup: import Monaco ESM editor workers through Vite worker imports, assign self.MonacoEnvironment.getWorker with label routing required by Monaco 0.55.1, and expose path-to-language selection with plaintext fallback; do not add a Monaco wrapper or Vite plugin. Wire this configuration through the actual Phase 1 Vue/Vite entry seam used by both production and the prototype. Then create the smallest production-shaped Vue route/page that mounts real Monaco through that production worker path and write the focused Chromium test before adapter behavior. Use exact asymmetric base/head fixtures containing unchanged, inserted, changed, and deleted lines. Require model-coordinate comments on both sides, bounded/all hidden-context controls, public hidden-comment reveal, pointer and Option/Alt+Enter parity through one activation action, F7/Shift+F7 change navigation, deterministic previous/next file order, paired-zone top alignment during content growth/resize, A→B→A restoration of scroll/focus/context/non-empty composer, ten forced recomputations/model swaps, and stable live model/listener/zone counts. RED is valid only after npm resolves exact Monaco, the production bundle resolves every worker, the real Chromium page renders Monaco, and a named stability assertion fails; dependency/build/browser/config failures are prerequisites, not RED.</action>
  <verify><automated><command expected="0">npm ls monaco-editor@0.55.1 --depth=0</command><command expected="0">npm run build</command><command expected="nonzero" classification="behavioral-red" required-output="named Monaco alignment, reveal, restoration, or lifecycle assertion">npm run test:browser -- tests/integration/monaco-anchor.spec.ts</command></automated></verify>
  <done>The approved exact dependency and lock entry exist, the production Monaco worker configuration builds from the actual Phase 1 entry seam, and a committed focused real-Chromium test reaches rendered Monaco before failing on a named missing stability behavior; configuration failures, mocks, screenshots without geometry assertions, and private APIs cannot satisfy RED.</done>
</task>

<task type="auto">
  <name>Task 3: Implement and pass the blocking Monaco adapter prototype</name>
  <files>src/web/monaco/configure.ts, src/web/monaco/diff-adapter.ts, src/web/monaco/line-mapping.ts, src/web/prototypes/MonacoStabilityPrototype.vue, tests/integration/monaco-anchor.spec.ts</files>
  <read_first>Read the RED test, Task 2's current-source reconciliation and production worker configuration, and the actual Phase 1 browser seams used by that implementation. Reuse the exact shortcut scoping, accessibility, and test conventions found at execution; do not depend on 02-01-SUMMARY.md before plan closeout.</read_first>
  <action>Create one long-lived read-only side-by-side diff editor with minimap off, glyph margins visible, exact immutable model strings, side-specific URIs/language IDs from paths, caller-owned model disposal, ResizeObserver-driven layout, and public hideUnchangedRegions controls (3 retained lines, collapse threshold 8, bounded reveal 10). Key anchors only by opaque file identity, side, and model line. Map paired zones through public line-change data, rebuild disposable zone/decorations after diff updates, and restore state only after the incoming diff is ready. Preserve D-01/D-02/D-07–D-12: one active file, side-specific inline placement, no pixel/row/view-zone identity, visible and keyboard controls, and session-only restoration. If selective public reveal fails, use explicit all-context mode for that file; if paired zones fail, evaluate only the UI-SPEC-permitted full-width synchronized row and update the browser assertions to prove it is directly below the exact aligned row. Do not use private properties, undocumented contribution IDs, overlays, unified/stacked layout, or broad App integration. Run the identical focused test to GREEN, refactor lifecycle ownership only if the same test remains GREEN, and record the chosen public reveal/alignment behavior in the summary so later plans consume a proven adapter contract.</action>
  <verify><automated><command expected="0">npm run test:browser -- tests/integration/monaco-anchor.spec.ts</command></automated></verify>
  <done>All ten prototype scenarios pass in real Chromium, model/zone/listener counts remain bounded, keyboard/pointer paths converge, and a failed gate prevents downstream plan execution.</done>
</task>
</tasks>

<acceptance_criteria>
- Phase 1 is reconciled from executed source and summaries; no proposed Phase 1 path is claimed as current source before inspection.
- Real Monaco 0.55.1 proves DIFF-02–DIFF-05, DIFF-07, and the layout/activation portion of CMT-01 at the stability boundary.
- The prototype uses only published APIs and blocks broad integration on any anchor movement, side misalignment, hidden-line failure, state loss, or lifecycle leak.
</acceptance_criteria>

## Artifacts this phase produces
- `src/web/monaco/configure.ts` — exact worker/language presentation setup.
- `src/web/monaco/diff-adapter.ts` — proven long-lived diff lifecycle and anchor-zone adapter.
- `src/web/monaco/line-mapping.ts` — counterpart-boundary mapping used only for layout.
- `tests/integration/monaco-anchor.spec.ts` — mandatory real-browser stability gate.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Phase 1 handoff → Phase 2 | Planned names cannot be trusted as implemented contracts; execution reconciles actual source first. |
| Fixture content → Monaco | Immutable text is display data, not executable application content. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-01 | Tampering | anchor placement | mitigate | Model side/line identity only; geometry IDs and pixels are disposable and stress-tested. |
| T-02-02 | Denial of Service | Monaco lifecycle | mitigate | Dispose replaced models/listeners/zones, bounded fixtures, and assert stable counts after repeated swaps. |
| T-02-03 | Elevation of Privilege | rendered repository text | mitigate | Read-only models; no repository code execution, links, textconv, workers beyond pinned Monaco assets, or writable editor commands. |
| T-02-04 | Information Disclosure | prototype | mitigate | Use synthetic fixtures; no token/path/object detail is logged by browser assertions. |
| T-02-SC | Tampering | npm install | mitigate | Exact-release registry audit and blocking human approval before install; lockfile records the approved artifact. |
</threat_model>

<verification>The only behavioral command is the focused real-Chromium Monaco contract. It must first produce named RED after prerequisites and then pass unchanged at GREEN/refactor; no formatter, linter, unit sweep, or project-wide test runs.</verification>

<success_criteria>
- Reconciliation and package approval gates are recorded.
- Stable exact side/model-line behavior survives every required stress transition with public APIs.
- Plans 02-04 have a concrete proven adapter seam and cannot start if the gate fails.
</success_criteria>

<output>Create `.planning/phases/02-anchored-diff-review/02-01-SUMMARY.md` with the Phase 1 reconciliation map, package audit/approval, RED/GREEN evidence, exact browser/Monaco versions, public reveal/alignment result, lifecycle counts, and fallback decision if exercised.</output>
