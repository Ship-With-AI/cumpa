---
phase: 02-anchored-diff-review
plan: 07
type: execute
wave: 5
depends_on: [02-06]
files_modified:
  - tests/helpers/git-fixture.ts
  - tests/e2e/anchored-review.spec.ts
  - src/server/app.ts
  - src/web/App.vue
autonomous: true
requirements: [DIFF-02, DIFF-03, DIFF-04, DIFF-05, DIFF-07, CMT-01, CMT-02, CMT-08, DRFT-01, DRFT-02, DRFT-03]
must_haves:
  truths:
    - "The generated packaged CLI/server/assets—not a dev server or fake DTO—opens exact frozen base/head text in real Monaco and exercises every Phase 2 visible/keyboard path through authenticated launch-scoped capabilities."
    - "A base comment and a head comment, including one revealed unchanged line, remain on exact server-derived side/path/blob/line anchors through expansion, resize, switching, recomputation, closure, and same-pair relaunch."
    - "Injected write failure never shows acceptance and retains composer text; successful add persists atomically; changed selected base/head opens an isolated draft; returning to the original pair restores its comments."
    - "Seeded exact stale/orphan records remain visible in the rail with no relocated inline marker, while unauthenticated, cross-origin, arbitrary-authority, ended-session, and unsupported-content paths remain closed and non-leaking."
    - "Every Phase 2 requirement and D-01–D-12 has focused or packaged evidence, and all deferred Phase 3/later behavior remains absent."
  artifacts:
    - path: tests/e2e/anchored-review.spec.ts
      provides: "Packaged real-Git/real-browser Phase 2 acceptance matrix"
    - path: tests/helpers/git-fixture.ts
      provides: "Independent asymmetric commits, comparison pairs, anchored lines, write faults, and stale/orphan fixtures"
  key_links:
    - from: tests/e2e/anchored-review.spec.ts
      to: src/server/app.ts
      via: "generated CLI, real IPv4 socket, inherited auth, frozen object routes, and atomic draft fault hook"
      pattern: "test:package"
    - from: tests/e2e/anchored-review.spec.ts
      to: src/web/App.vue
      via: "packaged assets drive the completed real-Monaco shell, comment persistence, rail, and relaunch flows"
      pattern: "anchored review"
---

<objective>
Prove the completed Phase 2 review flow through the generated production CLI, real Git objects, real loopback server, real Monaco browser, repository-local persistence, and relaunch.

Purpose: Close the phase with independent production-path evidence for every requirement, locked decision, security boundary, and exclusion after all focused slices are GREEN.
Output: A deterministic packaged acceptance matrix, independent fixtures, fault/relaunch coverage, and final multi-source coverage evidence.
</objective>

## Phase Goal
**As a** developer reviewing a pinned local comparison, **I want to** inspect immutable text side by side and attach precise comments that remain stable, **so that** I can resume trustworthy feedback without publishing branches or relocating anchors.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-UI-SPEC.md
@.planning/phases/02-anchored-diff-review/02-01-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-02-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-03-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-04-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-05-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-06-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Extend independent Git fixtures and write the packaged Phase 2 acceptance matrix</name>
  <files>tests/helpers/git-fixture.ts, tests/e2e/anchored-review.spec.ts, src/server/app.ts</files>
  <behavior>
    - Generated production CLI and assets serve real Monaco over a real authenticated IPv4 loopback session; no dev server, fake DTO, mocked editor, or worktree content path can pass.
    - Independently queried asymmetric base/head commits prove exact side text, rename paths, inserted/deleted/changed/revealed-unchanged model lines, and server-derived path/blob/context anchors.
    - Base/head comments survive switching, context changes, resize, recomputation, close, and same-pair relaunch; changing either selected endpoint creates an empty isolated draft and returning restores the original.
    - A fault before atomic replacement retains composer text and shows no acceptance; later success persists canonical bytes; duplicate add focuses the existing record.
    - Independently seeded exact verified/stale/orphan records show the required rail and inline-marker behavior without relocation.
    - Tokenless, wrong-origin, arbitrary repository/ref/commit/object/blob/path/draft selectors, ended sessions, unsupported content, and leakage assertions stay closed.
  </behavior>
  <read_first>Read every Phase 2 SUMMARY, the actual Phase 1 packaged fixture/test/app-factory seam, REQUIREMENTS acceptance criteria, D-01–D-12, UI-SPEC verification matrix, and the research Layer 5 packaged acceptance guidance. Reconcile identifiers against implemented source; planning paths are not source truth.</read_first>
  <action>Extend the existing disposable real-Git fixture rather than creating another fixture vocabulary. Create at least two independently resolved selected comparison pairs with asymmetric commits, rename-side paths, base/head/changed/inserted/deleted/revealed-unchanged targets, exact line text/context/blob facts queried independently from production code, stale and orphan seed records, and a deterministic repository-local write-failure hook available only through the existing test app-factory seam. Expand the packaged Playwright test to run the generated executable, verify production artifacts before launch, capture the actual Chromium and Monaco versions, and drive the complete UI through a real socket. Exercise D-01–D-12: side-line pointer and keyboard activation; immutable composer heading; blur/cancel/confirmation; pending/failure/success focus and announcements; exact four-field request and canonical accepted response; file/change/context navigation; A→B→A state; resize; verified reveal-before-focus; same-pair resume/different-pair isolation; stale/orphan rail-only behavior. Assert independent Git anchors and canonical draft bytes after relaunch. Exercise inherited Host/Origin/Bearer and ended-session denials plus arbitrary authority-shaped request fields, unsupported content, and safe non-leaking errors. Do not add product-only test bypasses: the app-factory hook may inject the filesystem boundary but must use the production draft algorithm and remain unreachable from browser requests. Write assertions before any necessary test-seam correction and require a named behavioral failure rather than build/browser/fixture failure for RED.</action>
  <verify><automated><command expected="0" classification="browser-prerequisite">npm run verify:prerequisites</command><command expected="0" classification="production-build">npm run build</command><command expected="0" classification="production-artifact" required-output="executable dist/bin.js, non-empty dist/web/index.html, non-empty dist/web/assets file">npm run verify:production-artifacts</command><command expected="nonzero" classification="behavioral-red" required-output="named anchored review, persistence, relaunch, stale/orphan, or security assertion">npm run test:package -- tests/e2e/anchored-review.spec.ts</command></automated></verify>
  <done>The generated production path and independent fixtures reach a named missing Phase 2 behavior after all prerequisites pass; fixture/build/browser failures do not count as RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close packaged behavior and record the complete source-coverage audit</name>
  <files>tests/helpers/git-fixture.ts, tests/e2e/anchored-review.spec.ts, src/server/app.ts, src/web/App.vue</files>
  <behavior>
    - The unchanged four-command production sequence passes twice without state leakage or fixture-order dependence.
    - All 11 Phase 2 requirement IDs, D-01–D-12, research contracts, and UI-SPEC controls/states map to focused or packaged evidence.
    - Phase 3/later edit, delete, resolve, summary mutation, revision conflict, drift recovery, export, fuzzy reattachment, threads/replies, themes, and stacked/unified layout remain absent.
  </behavior>
  <read_first>Read the Task 1 RED output and only the actual source seams implicated by the named failure. Read every focused Phase 2 SUMMARY to build the final GOAL/REQ/RESEARCH/CONTEXT/UI-SPEC evidence matrix without inventing unexecuted proof.</read_first>
  <action>Correct the real integration seam responsible for each named RED while preserving the closed authority model: generated CLI and app factory remain the only runtime entry, server derives anchors and comparison keys, atomic store controls acceptance, one real Monaco adapter owns models/zones/listeners, and the browser sends only opaque IDs plus side/line/body. Keep any filesystem fault injection constructor-only and test-scoped. Run the exact prerequisite/build/artifact/package sequence to GREEN, then repeat it unchanged to prove relaunch and lifecycle determinism. Record a multi-source audit mapping the Phase goal, DIFF-02/03/04/05/07, CMT-01/02/08, DRFT-01/02/03, D-01–D-12, research constraints, and every UI-SPEC visible/keyboard/responsive/stale-orphan contract to Plan 01–07 focused or packaged evidence. Record deferred/excluded behavior explicitly rather than implementing it. Do not weaken assertions, use dev-server/fake DTO shortcuts, or expand into a project-wide test run.</action>
  <verify><automated><command expected="0" classification="browser-prerequisite">npm run verify:prerequisites</command><command expected="0" classification="production-build">npm run build</command><command expected="0" classification="production-artifact" required-output="executable dist/bin.js, non-empty dist/web/index.html, non-empty dist/web/assets file">npm run verify:production-artifacts</command><command expected="0" classification="behavioral-matrix">npm run test:package -- tests/e2e/anchored-review.spec.ts</command></automated></verify>
  <done>The exact production sequence passes twice, all requirement/decision/UI/research coverage is grounded in executed evidence, security/lifecycle counts remain bounded, and no deferred behavior is introduced.</done>
</task>
</tasks>

<acceptance_criteria>
- Generated `dist/bin.js`, production assets, real Git, real IPv4 loopback server, authenticated closed APIs, real Monaco, atomic repository-local draft, and relaunch are exercised together.
- Exact base/head anchors and accepted comments survive every required transition; failures never appear accepted; comparison keys isolate selected pairs.
- Verified, stale, and orphan states retain exact semantics without relocation, while security and unsupported-content attempts remain closed and non-leaking.
- The identical focused production sequence passes twice and the final source audit covers every Phase 2 goal, requirement, D-01–D-12 decision, research constraint, and UI-SPEC contract.
</acceptance_criteria>

## Multi-Source Coverage Audit
| Source | Items | Planned evidence |
|---|---|---|
| GOAL | Exact immutable side-by-side review and stable resumable anchored comments | Prototype 02-01; contracts 02-02–02-04; shell 02-05; comment integration 02-06; packaged proof 02-07 |
| REQ | DIFF-02, DIFF-03, DIFF-04, DIFF-05, DIFF-07 | 02-01, 02-02, 02-04, 02-05; packaged proof 02-07 |
| REQ | CMT-01, CMT-02, CMT-08 | 02-02, 02-03, 02-04, 02-06; packaged proof 02-07 |
| REQ | DRFT-01, DRFT-02, DRFT-03 | 02-03, 02-06; packaged proof 02-07 |
| CONTEXT | D-01, D-02, D-03, D-04, D-05, D-06 comment interaction | 02-02–02-04 and 02-06; packaged proof 02-07 |
| CONTEXT | D-07, D-08, D-09, D-10, D-11, D-12 workspace/navigation/identity | 02-01, 02-04, 02-05, 02-06; packaged proof 02-07 |
| RESEARCH | Blocking real-Monaco gate; canonical closed server anchor; exact verification; atomic comparison-keyed store; session-only state | 02-01 through 02-04; integration 02-05–02-06; packaged proof 02-07 |
| UI-SPEC | All visible/keyboard controls, responsive/accessibility/copy states, comments rail, and stale/orphan contract | shell 02-05; comments 02-06; packaged proof 02-07 |

Deferred/excluded rather than gaps: Phase 3/later lifecycle, summary mutation, revision conflict, drift, corrupt-schema recovery UI, export, fuzzy reattachment, stacked/unified layout, threads/replies, and themes.

## Artifacts this phase produces
- `tests/e2e/anchored-review.spec.ts` — canonical packaged Phase 2 acceptance matrix.
- Phase 2 summaries containing reconciliation, prototype, TDD, focused integration, and coverage evidence, created only by executors at each plan closeout.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Generated CLI/browser → loopback server | Production token/Host/Origin and closed route grammar are exercised over a real socket. |
| Server → Git/filesystem | Frozen objects and fixed repository-local drafts are exercised in disposable real repositories. |
| Persisted draft → relaunched UI | Strict data is independently seeded/reloaded; stale/orphan data cannot become authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-41 | Spoofing | packaged session | mitigate | Real generated launch token, exact Host/Origin, separate process relaunch, and denial matrix. |
| T-02-42 | Tampering | packaged anchor/draft | mitigate | Independent Git facts, canonical response/document inspection, fault injection, and comparison isolation. |
| T-02-43 | Repudiation | acceptance evidence | mitigate | Two unchanged deterministic packaged passes and exact requirement/decision coverage record. |
| T-02-44 | Information Disclosure | browser/terminal/package | mitigate | Assert non-leaking browser responses and absence of token/body/blob/absolute path/command details. |
| T-02-45 | Denial of Service | browser/model/store lifecycle | mitigate | Prototype leak gate plus repeated switches/recomputations and serialized bounded store operations. |
| T-02-46 | Elevation of Privilege | arbitrary authority/repository content | mitigate | Closed capability attempts fail; no source execution, worktree reads, textconv, writable editor, or client-selected paths/objects. |
| T-02-SC | Tampering | supply chain | mitigate | Approved exact lockfile and verified deterministic Chromium prerequisite; no installs in this plan. |
</threat_model>

<verification>Run the exact four-command focused production sequence twice. Build/prerequisite/artifact failures are not behavioral passes. Do not run formatters, linters, aggregate browser/API/unit/Git suites, or project-wide tests.</verification>

<success_criteria>
- All 11 requirement IDs and all D-01–D-12 decisions have packaged or focused evidence with no source-audit gap.
- Security high-severity threats are mitigated and no later-phase behavior is present.
- Phase 2 is executable only after Phase 1 and closes with a real production-path vertical MVP.
</success_criteria>

<output>Create `.planning/phases/02-anchored-diff-review/02-07-SUMMARY.md` with both unchanged command sequences, real-Git facts, anchor/document samples, atomic fault/resume/isolation evidence, stale/orphan/security observations, source coverage audit, Phase 1 seam audit, later-phase exclusion audit, exact Chromium/Monaco versions, and commits.</output>
