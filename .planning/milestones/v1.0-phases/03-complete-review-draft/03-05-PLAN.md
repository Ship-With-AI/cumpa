---
phase: 03-complete-review-draft
plan: 05
type: execute
wave: 5
depends_on: [03-03, 03-04]
files_modified:
  - src/web/App.vue
  - src/web/model/review-draft-state.ts
  - src/web/components/DraftRecovery.vue
  - src/web/styles.css
  - tests/integration/draft-recovery-ui.spec.ts
autonomous: true
requirements: [DRFT-05]
must_haves:
  truths:
    - "Per D-15, malformed/schema-invalid classification replaces mutable Review content with an explicit read-only recovery surface while preserving the pinned identity header and selector-status slot."
    - "Per D-16, the UI requires a second contextual confirmation, submits only the server fingerprint, and never shows a new draft until the server confirms byte-identical durable backup and atomic replacement."
    - "Recovery failure preserves the fixed safe path/details, stays read-only, keeps the original recoverable, and reports a safe backup path when one exists."
    - "Per D-17, newerUnsupported has a distinct upgrade-required surface with found/supported versions and reveal/copy only; no backup/start-new, reset, preview, migration, downgrade or mutation action is present."
    - "Reveal draft file calls only the reconciled no-argument fixed capability for the launch-owned active draft; Copy draft path copies only the bounded safe relative display value, and neither action exposes or accepts an absolute/browser-selected path."
  artifacts:
    - path: "src/web/components/DraftRecovery.vue"
      provides: "Accessible corrupt recovery and newer-version upgrade-only states"
    - path: "tests/integration/draft-recovery-ui.spec.ts"
      provides: "Classification, confirmation, focus, failure and mutation-lockout browser contract"
    - path: "src/web/api/client.ts"
      provides: "Validated no-argument fixed active-draft reveal client method"
  key_links:
    - from: "src/web/App.vue"
      to: "src/web/model/review-draft-state.ts"
      via: "explicit server load classification determines normal versus read-only primary surface"
    - from: "src/web/components/DraftRecovery.vue"
      to: "src/web/api/client.ts"
      via: "Reveal invokes fixed no-body active-draft capability; Copy uses safe display path only"
    - from: "src/web/components/DraftRecovery.vue"
      to: "reconciled API client recovery method"
      via: "expectedFingerprint only; no browser path authority"
---

<objective>
Expose the safe loader/recovery boundary to the reviewer: corrupt current files remain read-only until an explicit verified backup-first recovery succeeds, while newer schemas are immutable upgrade-only states.

Purpose: Complete the user-visible DRFT-05 and D-15–D-17 flow without a silent empty fallback.
Output: Reconciled App state priority, accessible recovery/upgrade component and focused browser integration proof.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-review-draft/03-01-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json
@.planning/phases/03-complete-review-draft/03-03-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-04-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-UI-SPEC.md

<interfaces>
Use the actual App/load-state/UI primitive/API client owners and every actual test path from the approved 03-01-RECONCILIATION.json ledger. Consume the reconciled load/recovery/fixed-reveal result algebra from 03-03-SUMMARY.md; do not reinterpret raw JSON or construct filesystem paths in the browser. Every verification executes its keyed approved executable/argument array, and 03-05-SUMMARY.md records the resolved command and actual test path.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Integrate read-only corrupt recovery with verified replacement acknowledgment</name>
  <files>src/web/App.vue, src/web/model/review-draft-state.ts, src/web/components/DraftRecovery.vue, src/web/styles.css, tests/integration/draft-recovery-ui.spec.ts</files>
  <read_first>
    .planning/phases/03-complete-review-draft/03-UI-SPEC.md#Draft-Load-and-Recovery-Contract
    .planning/phases/03-complete-review-draft/03-03-SUMMARY.md
    src/web/App.vue
    src/web/model/review-draft-state.ts
    src/web/components/ui/UiPrimitives.vue
    tests/integration/complete-review-panel.spec.ts
  </read_first>
  <behavior>
    - Malformed/schema-invalid load hides/disables all comment and summary mutations and shows safe relative path plus bounded problem details.
    - Reveal/copy use the fixed server-authorized active draft; browser sends no path.
    - Reveal sends the authenticated fixed request with no body/query/path and announces bounded success/error without opening or leaking an absolute path; Copy places only the safe relative display path on the clipboard.
    - First Back up and start new only opens confirmation; second sends expectedFingerprint; success UI appears only after recovered response.
    - Failure or recoveryStateChanged remains read-only and preserves the original classification/details; no empty workspace flashes.
  </behavior>
  <action>Add failing focused browser cases, then wire the explicit load-kind priority into the actual App and canonical state owner. Build the recovery surface with exact UI-SPEC headings, read-only badge, safe path/details, reveal/copy and inline confirmation whose safe action receives initial focus. Wire Reveal draft file only to the reconciled no-argument client method from Plan 03-03; it sends no body/query/path, cannot choose another file, and renders only bounded success/error. Wire Copy draft path only to the server-validated safe relative display string; never place an absolute path, raw draft content, token, platform command, or hidden filesystem value in DOM, clipboard, URL, logs, or browser state. Submit only the fingerprint returned by the load state. During pending, disable recovery duplicate controls and show `Backing up existing draft…`; do not construct or render a new canonical draft locally. On recovered response show the safe backup path, then require explicit Open new draft to adopt the returned canonical draft. On any failure remain read-only and preserve details; if backup exists but replacement failed, report safe paths without claiming completion. Keep original pinned identity header and selector-status slot; render no preview/reset/direct path input.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),k='03-05-task-1-corrupt-ui',c=JSON.parse(readFileSync('.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json','utf8')).commands[k]; if(!c)throw Error('unapproved command '+k); console.log('resolved command:',c.executable,...c.args); const r=spawnSync(c.executable,c.args,{stdio:'inherit'}); process.exit(r.status??1)"</automated>
  </verify>
  <acceptance_criteria>
    - The test demonstrates intended RED then GREEN for corrupt classification, two-step confirmation, pending, failure and success.
    - All normal mutation initiators are absent/disabled in corrupt states, including direct client-method bypass cases covered by Plan 03.
    - Recovery success renders only after server response proving backup/replacement; failure never shows empty mutable state.
    - Escape/cancel returns focus predictably and performs no request.
    - Reveal and Copy tests prove no browser-selected/absolute path appears in requests, DOM, clipboard, URL, logs, or error announcements.
  </acceptance_criteria>
  <done>The reviewer can explicitly recover corrupt state without the UI weakening server byte-preservation ordering.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate distinct newer-schema upgrade-only state</name>
  <files>src/web/App.vue, src/web/components/DraftRecovery.vue, tests/integration/draft-recovery-ui.spec.ts</files>
  <read_first>
    .planning/phases/03-complete-review-draft/03-UI-SPEC.md#Newer-unsupported-schema-screen
    .planning/phases/03-complete-review-draft/03-03-SUMMARY.md
    src/web/App.vue
    src/web/components/DraftRecovery.vue
    tests/integration/draft-recovery-ui.spec.ts
  </read_first>
  <behavior>
    - Newer unsupported shows found/supported versions, fixed safe path, exact upgrade guidance and read-only status.
    - Only Reveal draft file and Copy draft path are present.
    - No reset/start-new/recovery request, downgrade/migration, raw JSON preview, normal Review content or mutation control can be reached.
  </behavior>
  <action>Add the distinct newerUnsupported branch rather than routing it through corruption. Render exact UI-SPEC copy and safe values from validated server DTO. Preserve the pinned identity header but hide all mutable review content. Reuse the same no-argument Reveal draft file client method and safe-relative Copy behavior tested for corrupt state; do not create a browser path opener or expose an absolute path on success/failure. Ensure keyboard/200% zoom/focus behavior and alert announcement are stable. Keep recovery state and newer state separate in types and DOM so the latter can never expose the destructive action.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),k='03-05-task-2-newer-ui',c=JSON.parse(readFileSync('.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json','utf8')).commands[k]; if(!c)throw Error('unapproved command '+k); console.log('resolved command:',c.executable,...c.args); const r=spawnSync(c.executable,c.args,{stdio:'inherit'}); process.exit(r.status??1)"</automated>
    <human-check>At phase-end verification, inspect malformed, schema-invalid, recovery-failed/recovered, and newer-version surfaces at 200% zoom with keyboard only.</human-check>
  </verify>
  <acceptance_criteria>
    - Newer state has no recovery/reset/downgrade/mutation path in visible or keyboard interaction.
    - Found/supported versions and safe path are announced without leaking raw content or absolute path.
    - Focus, confirmation and responsive behavior match UI-SPEC and no state flashes mutable content.
  </acceptance_criteria>
  <done>Newer schemas remain visibly upgrade-only and byte-preserving while corrupt current schemas retain explicit recovery.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server load classification → browser primary state | Browser must not reinterpret errors as an empty draft. |
| Recovery confirmation → API | Confirmation grants only one fingerprint-bound action, never path authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-19 | Tampering | recovery UI | mitigate | Two explicit activations; server fingerprint only; canonical adoption only from recovered response. |
| T-03-20 | Elevation of Privilege | reveal/copy/recovery | mitigate | Fixed active-session capability; no browser path/destination input. |
| T-03-21 | Information Disclosure | validation UI | mitigate | Safe relative path and bounded details; no raw bytes, content, absolute paths, stack traces or secrets. |
| T-03-22 | Spoofing | newer versus corrupt state | mitigate | Distinct validated DTO/DOM branches; newer never displays recovery controls. |
| T-03-SC | Tampering | dependencies | mitigate | No package or registry use; reuse local UI primitives. |
</threat_model>

<verification>
Run only the two approved keyed commands in 03-01-RECONCILIATION.json; their argument arrays are the sole authority for actual recovery-UI test paths. Preserve RED/GREEN evidence and record each resolved command in 03-05-SUMMARY.md. Perform the specified end-of-plan human keyboard/zoom check. Do not run formatter, linter, build, aggregate suite, packaged E2E, package manager, or project-wide command.
</verification>

<success_criteria>
- DRFT-05 and D-15–D-17 are visible and operable without weakening backend preservation.
- Corrupt state never becomes silently empty or mutable.
- Replacement is acknowledged only after verified backup-first server success.
- Newer versions remain upgrade-only with no destructive or downgrade action.
</success_criteria>

## Artifacts this phase produces

- Reconciled corrupt-recovery and newer-version primary UI states.
- Focused browser integration evidence in `03-05-SUMMARY.md`.

<output>Create `.planning/phases/03-complete-review-draft/03-05-SUMMARY.md` with actual owners, RED/GREEN command, load-kind/primary-surface matrix, confirmation/focus observations, safe-data audit and newer-version no-action proof.</output>
