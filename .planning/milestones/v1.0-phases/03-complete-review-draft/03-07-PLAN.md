---
phase: 03-complete-review-draft
plan: 07
type: execute
wave: 7
depends_on: [03-04, 03-05, 03-06]
files_modified:
  - tests/helpers/git-fixture.ts
  - tests/e2e/complete-review-draft.spec.ts
  - src/server/app.ts
  - src/web/App.vue
autonomous: true
requirements: [CMT-03, CMT-04, CMT-05, CMT-06, CMT-07, DRFT-04, DRFT-05, DRFT-06]
must_haves:
  truths:
    - "The packaged CLI/server/assets with real Git and real Monaco prove a reviewer can edit, hard-delete, resolve/reopen, count/group/navigate comments and save/preview/reload the one optional Markdown summary (D-01–D-11)."
    - "A real two-tab timeline proves one aggregate expected-revision CAS across add/edit/delete/resolve/reopen/summary: stale writes change zero bytes and return latest canonical state; every attempted local buffer survives conflict and Reload latest with no merge/force/retry (D-12–D-14)."
    - "Seeded malformed and schema-invalid raw bytes remain byte-identical until an explicit recovery creates and verifies an identical backup before replacement; newer schema bytes never change and expose no recovery/downgrade mutation (D-15–D-17)."
    - "Moving selected branch/worktree sources reports affected role plus old/new full OIDs while the open comparison, blobs, draft identity/revision, anchors, Monaco models and local buffers remain pinned."
    - "Security and exclusion probes prove no soft delete/undo, threads/replies, automatic merge, force overwrite, silent empty fallback, silent identity refresh, fuzzy relocation, source mutation or Phase 4 review export."
  artifacts:
    - path: "tests/e2e/complete-review-draft.spec.ts"
      provides: "Canonical packaged Phase 3 acceptance, safety, accessibility and exclusion matrix"
  key_links:
    - from: "tests/e2e/complete-review-draft.spec.ts"
      to: "packaged CLI/Fastify/Vite/Monaco application"
      via: "real authenticated launch-scoped browser sessions and filesystem/Git fixtures"
    - from: "tests/e2e/complete-review-draft.spec.ts"
      to: "repository-local draft bytes"
      via: "independent Buffer/hash/revision assertions across conflict and recovery"
---

<objective>
Prove the complete Phase 3 user story through the packaged application, real Git, real repository-local bytes, two browser tabs and real Monaco—without implementing or anticipating Phase 4 export.

Purpose: Close every Phase 3 requirement and D-01–D-17 with end-to-end observable evidence, including the critical zero-write, attempted-buffer, byte-preservation, no-relocation and pinned-drift properties.
Output: One focused packaged Playwright matrix plus minimal harness wiring needed to expose deterministic fault/fixture controls through test-only launch seams.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-review-draft/03-01-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json
@.planning/phases/03-complete-review-draft/03-02-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-03-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-04-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-05-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-06-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-CONTEXT.md
@.planning/phases/03-complete-review-draft/03-UI-SPEC.md

<interfaces>
Use the actual packaged harness command, test-only fault/fixture boundaries, and actual test paths approved in 03-01-RECONCILIATION.json and recorded in prior summaries. Every verification executes its keyed approved executable/argument array, and 03-07-SUMMARY.md records the resolved command and actual test path. Do not add a production debug API, weaken security, use Vite dev server, fake DTOs, mock Monaco, or execute repository code.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Write and pass packaged complete-draft and two-tab acceptance</name>
  <files>tests/helpers/git-fixture.ts, tests/e2e/complete-review-draft.spec.ts, src/server/app.ts, src/web/App.vue</files>
  <read_first>
    .planning/phases/03-complete-review-draft/03-02-SUMMARY.md
    .planning/phases/03-complete-review-draft/03-04-SUMMARY.md
    .planning/phases/03-complete-review-draft/03-UI-SPEC.md
    tests/e2e/anchored-review.spec.ts
    tests/helpers/git-fixture.ts
    src/server/app.ts
    src/web/App.vue
  </read_first>
  <behavior>
    - A packaged review edits/cancels/fails/saves a verified base/head comment, resolves/reopens, confirms/cancels/deletes, updates derived counts/groups/focus and navigates exact hidden anchors without any anchor-field change.
    - Summary edit/preview/empty/save/scoped shortcut persists across browser close and same-comparison relaunch; hostile Markdown remains inert.
    - Tab A mutation N→N+1 makes Tab B's different mutation at N return conflict; disk remains exactly A's canonical bytes and B retains its exact attempted string plus every other local buffer.
    - Reload latest adopts N+1 while preserving retained attempts; deleted-target and occupied-add-anchor safe states do not recreate/overwrite/merge; a later save requires explicit Continue editing and a fresh CAS.
  </behavior>
  <action>Extend the actual packaged Playwright harness with one focused Phase 3 spec. Launch through the built CLI/server/assets using authenticated launch-scoped capabilities and real Monaco. Seed comments through public behavior or validated fixture boundary, never production shortcuts. Cover open/resolved grouping, Summary first, exact UI copy, keyboard/focus/200% zoom and page close/relaunch. For the two-tab timeline, capture request revision, response kind, disk Buffer/hash/revision, canonical UI snapshot and all local buffers at every step. Assert stale requests create no temp/canonical/timestamp change and return latest canonical entire state. Exercise add collision and deleted edited target after Reload latest. Assert no optimistic canonical rollback code is needed because canonical UI changes only after accepted response.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),k='03-07-task-1-packaged-lifecycle',c=JSON.parse(readFileSync('.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json','utf8')).commands[k]; if(!c)throw Error('unapproved command '+k); console.log('resolved command:',c.executable,...c.args); const r=spawnSync(c.executable,c.args,{stdio:'inherit'}); process.exit(r.status??1)"</automated>
  </verify>
  <acceptance_criteria>
    - CMT-03–CMT-07 and DRFT-04 each have a named passing packaged scenario.
    - Exact anchor path/blob/side/line/context/hash bytes are unchanged across Show, Edit, resolve/reopen and layout transitions.
    - Every stale attempt writes zero bytes and every local attempted buffer survives failure, conflict, reload, close/collapse/switch/resize as specified.
    - Same comparison relaunch restores accepted summary/comment state; no Phase 4 export files or controls appear.
  </acceptance_criteria>
  <done>The complete review lifecycle and lost-update behavior pass through the real packaged application.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pass packaged corrupt/newer/drift/security and exclusion acceptance</name>
  <files>tests/helpers/git-fixture.ts, tests/e2e/complete-review-draft.spec.ts, src/server/app.ts</files>
  <read_first>
    .planning/phases/03-complete-review-draft/03-03-SUMMARY.md
    .planning/phases/03-complete-review-draft/03-05-SUMMARY.md
    .planning/phases/03-complete-review-draft/03-06-SUMMARY.md
    tests/e2e/complete-review-draft.spec.ts
    tests/helpers/git-fixture.ts
    src/server/app.ts
  </read_first>
  <behavior>
    - Malformed/schema-invalid fixtures show read-only recovery; cancel/failure leaves original bytes; confirmed success creates a verified identical backup before valid replacement.
    - Newer-version fixture remains byte/hash/stat identical across GET, all mutation and recovery probes and exposes only upgrade guidance/reveal/copy.
    - Branch and worktree movement shows per-role old/new full OIDs; unavailable shows no fabricated new OID; pinned blob text, comparison/draft/anchor/Monaco identities remain unchanged.
    - Unauthenticated, cross-origin and arbitrary-authority attempts fail before draft/Git/file work; prohibited routes/actions/files remain absent through observable behavior.
    - Reveal draft file succeeds only for the launch-owned active draft through the standard token/Host/Origin boundary; body/query/path attempts fail before platform action, and UI/DTO/log evidence contains no absolute path, draft bytes, stderr, or platform command.
  </behavior>
  <action>Add real repository fixtures with unusual safe paths and exact raw corrupt/newer byte payloads. Exercise recovery confirmation and injected backup/replacement failure through the reconciled test-only seam; independently compare original and backup Buffer/length/SHA-256. Exercise Reveal draft file through the packaged public UI and secured route: prove the fixed launch-owned active draft is the only target, standard token/Host/Origin denials and body/query/path probes occur before adapter work, Copy draft path exposes only the safe relative display value, and bounded result/error surfaces never leak an absolute path, draft bytes, platform command, stderr, token, or stack. Move/delete branch and registered worktree sources after browser launch, poll through public status, and compare full pinned application/disk snapshots. Probe requests carrying path/ref/OID/destination/force/merge/replacement/export authority and unauthenticated/cross-origin calls; assert denial and no side effects. Assert no UI/route generates `review.md`, `review.json`, export directory/hash/instructions; no source file/index/HEAD mutation occurs. Run only the approved focused command and capture complete safety evidence.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),k='03-07-task-2-packaged-safety',c=JSON.parse(readFileSync('.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json','utf8')).commands[k]; if(!c)throw Error('unapproved command '+k); console.log('resolved command:',c.executable,...c.args); const r=spawnSync(c.executable,c.args,{stdio:'inherit'}); process.exit(r.status??1)"</automated>
    <human-check>At end-of-phase verification, review the packaged Review, conflict, recovery, newer-version and drift surfaces with keyboard/screen-reader announcements and confirm no prohibited action is present.</human-check>
  </verify>
  <acceptance_criteria>
    - DRFT-05 and DRFT-06 each have named passing packaged scenarios including byte/full-OID immutable-state evidence.
    - Recovery never replaces without verified backup; newer never mutates or exposes reset/recovery.
    - Drift never refreshes comparison or relocates anchors and reports old/new complete OIDs.
    - Source tree/Git index/HEAD are unchanged and no Phase 4 artifact/control/route exists.
    - Fixed reveal and safe-copy packaged probes prove no browser path authority or absolute-path/error leakage.
  </acceptance_criteria>
  <done>All Phase 3 safety, security, accessibility and exclusion contracts pass through the packaged product.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Packaged browser → secured loopback server | Tests must exercise the production authentication/origin/capability path. |
| Test fixture → repository bytes/Git refs | Fixtures may prepare state but cannot bypass production load/mutation/drift behavior. |
| Phase 3 → Phase 4 boundary | Acceptance must not create export implementation or speculative format. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-28 | Tampering | packaged acceptance harness | mitigate | Test-only launch seams, real public routes/Monaco/filesystem, and independent byte/identity observations. |
| T-03-29 | Spoofing | two-tab canonical state | mitigate | Record full response algebra, aggregate revisions and disk hashes from both pages. |
| T-03-30 | Elevation of Privilege | security/exclusion probes | mitigate | Inherited bearer/Host/Origin/capability boundaries and strict no-authority DTOs. |
| T-03-31 | Information Disclosure | diagnostics | mitigate | Assertions and screenshots omit tokens, raw corrupt content, comment bodies, absolute paths, Git stderr and secrets. |
| T-03-32 | Repudiation | safety claims | mitigate | Named packaged scenarios plus independent Buffer/hash/OID/source-state evidence. |
| T-03-SC | Tampering | dependencies | mitigate | No install; use approved locked packaged toolchain. |
</threat_model>

<verification>
Run only the two approved keyed commands in 03-01-RECONCILIATION.json; their argument arrays are the sole authority for the actual packaged spec path and focused scenario filters. Record each resolved command in 03-07-SUMMARY.md. Do not run formatter, linter, unit/API/aggregate browser suite, project-wide command, package manager, or Phase 4 tests. The plan itself does not run commands; executors run them during implementation.
</verification>

<success_criteria>
- All eight Phase 3 requirements have named packaged proof and appear in this plan's frontmatter.
- D-01–D-17 have observable packaged or focused lower-layer evidence linked in the final summary.
- Critical safety properties—one aggregate CAS, zero stale writes/latest state, buffer retention, backup-before-replace, newer immutability, no relocation, and pinned drift—are independently observed.
- No forbidden/deferred/source-mutation/Phase 4 behavior exists.
</success_criteria>

## Multi-Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Plan(s) | Status | Notes |
|---|---|---|---|---|---|
| GOAL | — | Safely maintain complete draft as comments and selected refs evolve | 03-02–03-07 | COVERED | API, persistence, UI, recovery, drift and packaged proof |
| REQ | CMT-03 | Edit existing comment | 03-02, 03-04, 03-07 | COVERED | Reducer/API, anchored UI, packaged proof |
| REQ | CMT-04 | Delete existing comment | 03-02, 03-04, 03-07 | COVERED | Hard delete/confirmation/successor focus |
| REQ | CMT-05 | Resolve/reopen | 03-02, 03-04, 03-07 | COVERED | Legal transitions and grouped UI |
| REQ | CMT-06 | Counts and jump | 03-04, 03-07 | COVERED | Derived exact-path groups and no-relocation Monaco command |
| REQ | CMT-07 | One summary | 03-02, 03-04, 03-07 | COVERED | Canonical Markdown, safe preview, explicit CAS save |
| REQ | DRFT-04 | No stale overwrite | 03-02, 03-04, 03-07 | COVERED | Aggregate CAS, conflict UI, two-tab bytes |
| REQ | DRFT-05 | Corrupt/newer preservation | 03-03, 03-05, 03-07 | COVERED | Raw loader, backup-first recovery, upgrade-only UI |
| REQ | DRFT-06 | Pinned selector drift | 03-06, 03-07 | COVERED | Full-OID observation/notice with immutable snapshots |
| CONTEXT | D-01–D-06 | Review hierarchy/comment lifecycle/exclusions | 03-02, 03-04, 03-07 | COVERED | Exact groups, navigation, edit/delete/state and flat hard-delete model |
| CONTEXT | D-07–D-11 | Persistent canonical optional Markdown summary | 03-02, 03-04, 03-07 | COVERED | Summary first, safe Edit/Preview, explicit save, retained buffers |
| CONTEXT | D-12–D-14 | Aggregate revision/conflict/no merge-force | 03-02, 03-04, 03-07 | COVERED | Serialized CAS and explicit reload-only workflow |
| CONTEXT | D-15–D-17 | Raw corruption recovery/newer immutability | 03-03, 03-05, 03-07 | COVERED | Byte preservation, verified backup, upgrade-only |
| RESEARCH | — | Raw loader, one reducer/gateway, safe preview, exact groups/navigation, drift side channel | 03-02–03-06 | COVERED | Architectural responsibility map honored |
| UI-SPEC | — | Review/Summary/conflict/recovery/drift responsive accessible contracts | 03-04–03-07 | COVERED | Focused real-browser plus packaged evidence |

Deferred ideas and Phase 4 export are intentionally excluded, not gaps.

## Artifacts this phase produces

- `tests/e2e/complete-review-draft.spec.ts` — canonical packaged Phase 3 acceptance matrix.
- `03-07-SUMMARY.md` — final requirement/decision/source audit, safety evidence and exclusion report created by the executor.
- No `review.md`, `review.json`, export directory, export schema/route/component, source mutation, or Phase 4 artifact.

<output>Create `.planning/phases/03-complete-review-draft/03-07-SUMMARY.md` with focused packaged commands, requirement/decision evidence matrix, two-tab timeline, buffer snapshots, byte/backup hashes, full-OID drift snapshots, accessibility observations, security denials, source-state proof and complete exclusion audit.</output>
