---
phase: 04.1-close-gap-cmt-01-correlate-async-comment-settlement
verified: 2026-07-24T12:42:21Z
status: passed
score: 11/11 must-haves verified
behavior_unverified: 0
overrides_applied: 0
re_verification:
  previous_status: passed
  previous_score: 9/9
  gaps_closed: []
  gaps_remaining: []
  regressions: []
---

# Phase 04.1: Close CMT-01 Async Comment Settlement Verification Report

**Phase Goal:** A reviewer can switch files while an add-comment request is pending, and success or failure settles only its originating composer without weakening canonical atomic draft acceptance or anchored-diff stability.

**Verified:** 2026-07-24T12:42:21Z  
**Status:** passed  
**Re-verification:** Yes — final post-review state

## Goal Achievement

### Observable Truths

| # | Truth | Status | Direct evidence |
| --- | --- | --- | --- |
| 1 | A pending comment command snapshots the origin file and a controller-local request identity before a file switch can occur. | ✓ VERIFIED | `src/web/model/workspace-state.ts:287-295` stores monotonic `requestId` on the pending composer and emits it with origin `fileId`, side, line, and body; `:448-460` owns the monotonically increasing counter per workspace controller. |
| 2 | An accepted result settles only the matching origin composer; with B active it clears A, leaves B unchanged, and emits no off-screen focus/navigation command. | ✓ VERIFIED | `workspace-state.ts:222-244` looks up `files[fileId]`, requires the exact pending request, and emits focus only when the origin is active. The unit off-screen-success regression and final observed mounted-browser accepted A→B case prove the transition. |
| 3 | A rejected persistence result settles only A to retryable state, retaining the original body, side, line, and recoverable error while B remains active. | ✓ VERIFIED | `workspace-state.ts:313-323` replaces only `files[event.fileId]`, removes only the matching request ID, and preserves the remaining composer fields. The mounted failure case returns to A and asserts the exact textarea body, `src/first.ts · Head · line 10`, alert, and enabled retry control. |
| 4 | A revision conflict arriving for held A after B advances the canonical revision reports the true conflict revision and restores only A while B remains active. | ✓ VERIFIED | `App.vue:519-535` uses `result.expectedRevision` (not current mutable revision), dispatches correlated A failure, and announces A's location. `anchored-workspace.spec.ts:613-654` holds A, accepts B, releases A at 409, then verifies B remains active and the conflict reports `Your revision 0` / `Latest revision 1` with A's retry body retained. |
| 5 | A stale success, duplicate, or failure cannot settle a newer composer at the same file, side, and line. | ✓ VERIFIED | The success helper and duplicate/failure branches require the exact pending `requestId` (`workspace-state.ts:230-235`, `:301-302`, `:316-317`). Unit test `ignores every stale settlement for a replacement request at the same anchor` asserts all three stale outcomes return the unchanged replacement-pending state and no commands. |
| 6 | A callback resolving after its originating `WorkspaceController` has been replaced cannot mutate canonical review state, reducer state, conflict metadata, or announce a stale result. | ✓ VERIFIED | `App.vue:507-521` captures `originWorkspace`; its early return is lexically before the conflict branch, `acceptReviewDraft`, `dispatchWorkspace`, and success announcement. The catch branch repeats the guard before failure dispatch/announcement at `:559-563`. This is a direct production source-order guarantee. |
| 7 | Accepted UI state remains based only on the server-returned canonical draft after atomic persistence; browser correlation does not synthesize authority client-side. | ✓ VERIFIED | `App.vue:551-557` executes `acceptReviewDraft(result.draft, undefined, workspaceComment)` before `add-succeeded`. `src/server/routes.ts:215-275` derives the durable anchor and calls the draft store; `src/server/draft-store.ts:249-278` serializes, revision-checks, commits, then and only then returns `{ kind: 'accepted', draft }`. Final observed API suite: 14 files / 98 tests passed. |
| 8 | The browser tests exercise production composer → workspace command → SessionClient promise → App callback → reducer settlement, pausing only HTTP completion. | ✓ VERIFIED | The test fixture parses `DraftMutationRequestSchema` before the response gate and responds using `DraftMutationResultSchema` (`anchored-workspace.spec.ts:162-219`). The four cases use only Monaco/file-tree DOM interactions; no reducer event is directly dispatched and no timing sleep controls settlement. |
| 9 | Origin-specific success, persistence-failure, and revision-conflict announcements remain observable when the origin file is off-screen. | ✓ VERIFIED | `App.vue:509-570` computes `commentLocation` from the command origin and announces that origin in every branch. The three A→B cases assert the visible `.visually-hidden[aria-live="polite"]` message names `src/first.ts`, not active B. |
| 10 | Repeated identical settlement announcements produce distinct live-region updates. | ✓ VERIFIED | `announce` increments `liveMessageVersion` on every call (`App.vue:131-134`); the keyed child at `:906-908` is remounted with its version. `anchored-workspace.spec.ts:656-697` makes the same failure twice and verifies the announcement version changes while the identical text remains exposed. |
| 11 | CMT-01 remains traceable as closed without weakening DIFF-07 anchored-diff stability or DRFT-01 atomic acceptance. | ✓ VERIFIED | Both plans own `CMT-01`; `REQUIREMENTS.md` defines its visible-base/head-line contract. Phase 2 verification and the milestone audit reconcile CMT-01/WR-002/INT-B01/X12/F04 as closed and retain unrelated debt. Final observed focused Monaco suite passed 10/10, and final observed full configured Playwright suite passed 56/56. |

**Score:** 11/11 truths verified (0 present but behavior-unverified)

## Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/web/model/workspace-state.ts` | Origin/request-correlated command/event contract and exact pending-composer reducer guard | ✓ VERIFIED | Substantive immutable reducer; commands and all settlement event variants carry `fileId` + `requestId`; stale events return original state/commands. |
| `src/web/App.vue` | Stale-controller guard; canonical acceptance before settlement; origin-specific announcements; repeatable live region | ✓ VERIFIED | Production callback captures origin controller and guards before every mutation; canonical result remains server-owned; versioned keyed live-region child is wired to `announce`. |
| `tests/unit/workspace-state.test.ts` | Cross-file success/failure, active-origin focus, and same-anchor stale settlement behavior | ✓ VERIFIED | Complete `WorkspaceComment` fixtures conform to the current durable-anchor contract; assertions target observable state/commands rather than reducer internals. Final observed: 18 files / 108 tests passed. |
| `tests/integration/anchored-workspace.spec.ts` | Deterministic A→B accepted, failure, conflict, and repeated-announcement browser regressions | ✓ VERIFIED | Resettable schema-valid response gate; four DOM-driven production-flow cases. Final observed: 4/4 focused async cases passed. |
| `.planning/phases/02-anchored-diff-review/02-VERIFICATION.md` | Current CMT-01 / DIFF-07 / DRFT-01 and finding disposition | ✓ VERIFIED | Reconciles CMT-01, CR-001, WR-001, and WR-002 using focused evidence without claiming unrelated Phase 3 work. |
| `.planning/v1.0-MILESTONE-AUDIT.md` | Current INT-B01/X12 and F04 disposition | ✓ VERIFIED | Records the async-origin closure as wired/complete while preserving unrelated deferred debt. |

## Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Workspace reducer | `App.vue` | `persist-comment(fileId, requestId, side, line, body)` | ✓ WIRED | `App.vue:492-570` consumes the discriminated command and echoes the captured origin into settlement events. |
| `App.vue` callback | Workspace reducer | `dispatchWorkspace` after the guarded promise branch | ✓ WIRED | `dispatchWorkspace` calls the current controller and executes its commands; stale-controller guard precedes every call. |
| Browser test | Production callback | Real composer/tree DOM plus gated `/api/draft/mutations` response | ✓ WIRED | Middleware delays only schema-validated response completion, leaving SessionClient/App/reducer production code intact. |
| Server route | Atomic draft store | Durable anchor → serialized `mutate` → committed accepted draft | ✓ WIRED | Route delegates mutation to the store; store returns canonical acceptance only after commit. |

## Data-Flow Trace (Level 4)

| Artifact | Data / event | Source | Produces real data | Status |
| --- | --- | --- | --- | --- |
| `workspace-state.ts` | Pending composer and settlement target | `state.files[command/event.fileId]` plus exact request ID | Yes | ✓ FLOWING |
| `App.vue` | Server mutation result | `SessionClient.mutate` using command snapshot | Yes — accepted draft and conflict metadata are response-derived | ✓ FLOWING |
| Browser fixture | HTTP outcome | Schema-validated real request and schema-validated accepted/failure/conflict response | Yes — no reducer-state injection | ✓ FLOWING |
| Live region | Announcement update | Origin-specific `announce` calls and incrementing version | Yes — keyed child provides a fresh DOM update for equal text | ✓ FLOWING |

## Behavioral Evidence

No tests were rerun in this final re-verification, per orchestrator instruction. The following are **orchestrator-observed final-state evidence** from 2026-07-24:

| Behavior | Command / gate | Result | Status |
| --- | --- | --- | --- |
| Reducer origin correlation, failure retention, and stale same-anchor no-op | `npm run test:unit -- tests/unit/workspace-state.test.ts` | 18 files / 108 tests passed | ✓ PASS |
| Git regression boundary | Focused Git suite | 8 files / 35 tests passed | ✓ PASS |
| Atomic draft mutation boundary | Focused API suite | 14 files / 98 tests passed | ✓ PASS |
| Four delayed production browser outcomes | Focused `async comment settlement` Playwright cases | 4/4 passed | ✓ PASS |
| Full configured browser/package regression boundary | Canonical configured Playwright suite | 56/56 passed | ✓ PASS |
| Production bundle | Vite production build | 1,443 modules built | ✓ PASS |
| Code review | Final review | clean | ✓ PASS |
| Security review | Final audit | 11/11 closed; 0 open | ✓ PASS |
| UI audit | Final audit | 24/24 | ✓ PASS |

The earlier invalid bare `npm run test:browser` invocation is not used as evidence: its `--config=tests` form collected Vitest files without a Playwright file filter. The final configured Playwright run is the canonical full-suite result. The first full-suite stale `.sr-only` selector failure and second full-suite default-5-second callback wait failure were remediated in the final state: selectors target `.visually-hidden`, and the accepted callback assertion uses a bounded 15-second condition timeout. The final 56/56 run is the relied-on result.

## Requirements Coverage

| Requirement | Source plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| CMT-01 | `04.1-01-PLAN.md`, `04.1-02-PLAN.md` | User can add one comment on any visible base/head line, including unchanged context, without cross-file async settlement corruption. | ✓ SATISFIED | Origin/request reducer protocol plus the four final browser cases provide direct success, failure, conflict, and announcement evidence across a real A→B switch. |
| DIFF-07 (guard, not Phase 04.1-owned) | Both closure plans | Switching, resize, and context expansion preserve correct inline placement/side alignment. | ✓ PRESERVED | Final observed real-Monaco suite passed 10/10; no Monaco production contract was weakened. |
| DRFT-01 (guard, not Phase 04.1-owned) | Both closure plans | UI confirms accepted mutations only after atomic server persistence. | ✓ PRESERVED | Server/store/App ordering and final observed API suite 14 files / 98 tests passed. |

**Requirement ownership:** Both closure plans declare the sole phase requirement, `CMT-01`. `REQUIREMENTS.md` maps CMT-01 historically to Phase 2; this inserted closure phase resolves its audited WR-002 gap and does not introduce a duplicate requirement. No Phase 04.1 orphan requirement IDs exist.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| — | — | No `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, placeholder, or equivalent debt marker in the final changed source/test artifacts. | — | No blocker or warning. |

## Residual Gaps and Human Verification

None. All behavior-dependent goal conditions have focused runtime coverage in the final orchestrator-observed evidence, and final source inspection verifies the controller-replacement and live-region fixes before their mutation/announcement boundaries. No deferred `<human-check>` blocks exist in either phase plan.

The orphan metadata route/client surface and Phase 3 requirement-ledger/UI polish remain explicitly deferred and are not represented as closed by this Phase 04.1 report.

## Deterministic Next Action

Proceed to the milestone's next planned verification or closure workflow. Reopen CMT-01 only on a reproduction that violates the tested sequence: submit on A, switch to B before callback completion, release accepted/persistence-failure/conflict response, then return to A and observe origin-state corruption.

---

_Verified: 2026-07-24T12:42:21Z_  
_Verifier: the agent (gsd-verifier)_
