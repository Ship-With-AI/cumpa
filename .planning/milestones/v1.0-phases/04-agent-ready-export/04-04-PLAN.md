---
phase: 04-agent-ready-export
plan: 04
type: tdd
wave: 4
depends_on: [04-03]
files_modified:
  - src/git/comparison.ts
  - src/git/ignore-status.ts
  - src/server/gitignore-capability.ts
  - src/contracts/api.ts
  - src/server/routes.ts
  - src/server/capabilities.ts
  - tests/git/inventory.test.ts
  - tests/git/ignore-status.test.ts
  - tests/api/gitignore.test.ts
autonomous: true
requirements: [EXP-08, SAFE-04]
must_haves:
  truths:
    - "Canonical changed-record construction excludes `.diff-review` when either exact old/base or new/head identity is the root or child, independent of tracked/ignored/negated/absent state (D-14, SAFE-04)."
    - "Effective ignore status uses one fixed probe through the existing safe Git runner, never `.gitignore` text inference."
    - "A separate secured no-authority action appends exactly `/.diff-review/` only after explicit consent and preserves every prior byte/rule as an exact prefix (D-13, D-17)."
    - "Decline, already-ignored state, append failure, symlink/type/read-only/concurrent cases do not block export and never mutate source control (D-14, EXP-08)."
  prohibitions:
    - "No UI-only, substring, case-folded, display-label, host-path, glob, or ignore-dependent comparison filter."
    - "No `.gitignore` rewrite/temp replacement/normalization/trim/sort/chmod/editor/stage/commit; request accepts no path, text, pattern, repository, content, mode, argv, or overwrite."
    - "No second Git runner, route registry, security guard, capability registry, API algebra, or comparison inventory owner."
  artifacts:
    - path: "src/git/comparison.ts"
      provides: "Unconditional exact two-sided internal path exclusion"
    - path: "src/git/ignore-status.ts"
      provides: "Fixed effective-ignore probe"
      exports: [inspectDiffReviewIgnore]
    - path: "src/server/gitignore-capability.ts"
      provides: "Serialized byte-preserving fixed-rule append capability"
      exports: [appendDiffReviewIgnoreRule]
  key_links:
    - from: "src/server/gitignore-capability.ts"
      to: "src/server/routes.ts"
      via: "serialized extension of Plan 04-03 shared secured registry"
      pattern: "/.diff-review/"
---

<objective>
Implement unconditional internal-path exclusion and the optional exact append-only ignore capability after the shared route/API owners exist.

Purpose: Complete SAFE-04 and source-control safety without colliding with Plan 04-03 or making ignore configuration an export prerequisite.
Output: Canonical inventory filter, effective ignore probe, secured append action, and focused real-Git/API tests.
</objective>

## Phase Goal

**As a** developer with a completed pinned review, **I want to** explicitly export one accepted revision as a stable agent-ready pair, **so that** an applying agent can act on precise feedback without Diff Review modifying source control.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-agent-ready-export/04-CONTEXT.md
@.planning/phases/04-agent-ready-export/04-RESEARCH.md
@.planning/phases/04-agent-ready-export/04-PATTERNS.md
@.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json
@.planning/phases/04-agent-ready-export/04-03-SUMMARY.md

<interfaces>
Validate the ledger and consume Plan 04-03's actual API/result/route/capability owners. This plan is deliberately wave 4 and depends on 04-03 because it serially extends `api.ts`, routes, and capabilities. Missing or divergent predecessor interfaces block rather than authorize parallel definitions.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Exclude generated records and inspect effective ignore behavior</name>
  <files>src/git/comparison.ts, src/git/ignore-status.ts, tests/git/inventory.test.ts, tests/git/ignore-status.test.ts</files>
  <behavior>
    - Exclude exact `.diff-review` root/descendant on either old/base or new/head identity, including add/delete/rename/copy crossings.
    - Do not exclude near matches, nested `src/.diff-review`, case variants, display collisions, or unusual valid byte identities.
    - Result is identical for tracked, untracked, ignored by root/nested/info/global sources, later negated, force-added, or absent paths.
    - Effective status invokes only fixed `check-ignore --no-index` equivalent for `.diff-review/.diff-review-ignore-probe` through fixed argv/environment and changes no bytes.
  </behavior>
  <action>Write failing real-Git cases first. Deepen exactly one canonical changed-record constructor after lossless raw/numstat identity join and before tree/count/capability presentation. Test both old and new exact canonical path identities using the existing comparator. Separately implement fixed effective-ignore inspection through the reconciled shell-free runner. Return bounded typed status; never parse ignore files or accept browser Git/path authority.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-04-inventory-ignore'];if(!c)throw Error('missing 04-04-inventory-ignore');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>Internal exports never enter canonical comparison records, and ignore truth comes only from the fixed Git probe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend the shared secured registry with exact append-only consent action</name>
  <files>src/server/gitignore-capability.ts, src/contracts/api.ts, src/server/routes.ts, src/server/capabilities.ts, tests/api/gitignore.test.ts, tests/git/ignore-status.test.ts</files>
  <behavior>
    - Token, Host, Origin, method, content/body/unknown-field denials occur before Git or filesystem invocation; request contains no fields.
    - Already effectively ignored writes zero bytes. Missing/empty file gets exactly one rule without leading blank; LF-ended nonempty gets rule directly; otherwise exactly one LF precedes rule; final rule ends LF.
    - CRLF, comments, blanks, trailing spaces, UTF-8/non-UTF-8 bytes remain exact prefix; no normalization or mode rewrite.
    - Symlink/non-regular/read-only/containment/concurrent-change/write/flush/reread/probe failure yields safe unchanged/unconfirmed result, persistent warning, and export remains available.
    - Success requires flush, reread exact-prefix/suffix proof, then effective probe success; it never stages, commits, pushes, or executes repository code.
  </behavior>
  <action>Write API/filesystem fixtures first. Extend the exact Plan 04-03 shared schemas and secured registries serially with status and append results; do not reopen export/reveal contracts. Target only fixed repository-root `.gitignore`, serialize in-process append actions, lstat/type/no-follow/containment check, and establish current bytes/size/inode facts immediately before append to detect external change. Append fixed `/.diff-review/\n`, with one leading LF only for nonempty non-LF-ended content. Preserve original bytes exact prefix, flush, reread exact suffix, and rerun fixed probe. Decline is no request. Never use read-modify-temp-rename, decode/re-encode, trim, sort, normalize, chmod, editor, Git mutation, or export coupling.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-04-gitignore-api'];if(!c)throw Error('missing 04-04-gitignore-api');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>The optional fixed append preserves every prior byte, remains separate from export, and grants no generic Git/filesystem authority.</done>
</task>

</tasks>

<acceptance_criteria>
- Shared `api.ts`/route/capability work is serialized after and compatible with 04-03.
- SAFE-04 exclusion is canonical and unconditional; append is exact, explicit, optional, and nonblocking.
- Focused tests prove security zero-invocation, exact-prefix/newline matrices, unusual paths, concurrency/type failures, and no source-control action.
</acceptance_criteria>

## Artifacts this phase produces

- Existing canonical comparison owner extended with exact two-sided `.diff-review` exclusion.
- Fixed effective-ignore probe using existing Git runner.
- Fixed append service and serial additions to Plan 04-03 API/route/capability owners.
- Focused real-Git and API tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Raw Git records → review inventory | Generated paths could recurse into review. |
| Browser → ignore actions | Request could seek arbitrary file/Git authority. |
| Repository `.gitignore` → writer | Existing bytes/types/concurrent state are untrusted. |

## STRIDE Threat Register — ASVS L1, high threats block

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-17 | Tampering | comparison inventory | High | mitigate/block | Exact lossless old/new filter before presentation independent of ignore. |
| T-04-18 | Elevation | append route | High | mitigate/block | Existing strict security, no-field request, fixed root/rule only. |
| T-04-19 | Tampering | `.gitignore` | High | mitigate/block | Append-only, exact prefix/suffix verification, type/no-follow/concurrency checks. |
| T-04-20 | Repudiation | ignore status | Medium | mitigate | Fixed effective Git probe before/after. |
| T-04-21 | Elevation/Tampering | source control | High | mitigate/block | Fixed argv runner; no mutation/network/shell/editor/repository execution. |
| T-04-SC | Tampering | packages | High | mitigate/block | No dependency or install. |
</threat_model>

<verification>Run only the two exact ledger-resolved focused commands after the validator. No installs, formatters, linters, broad suites/builds, or Git commands.</verification>

<success_criteria>
- D-13/D-14/D-17, SAFE-04, and EXP-08 are observable at Git/API/filesystem boundaries.
- Ignore handling neither collides with shared owners nor weakens export availability.
</success_criteria>

<output>Create `.planning/phases/04-agent-ready-export/04-04-SUMMARY.md` with actual predecessor interfaces, resolved owners, RED/GREEN commands, exclusion/effective-ignore matrices, exact-prefix append matrix, authority denials, and nonblocking evidence.</output>
