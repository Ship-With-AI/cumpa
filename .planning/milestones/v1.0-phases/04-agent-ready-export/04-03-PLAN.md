---
phase: 04-agent-ready-export
plan: 03
type: tdd
wave: 3
depends_on: [04-02]
files_modified:
  - src/contracts/api.ts
  - src/server/routes.ts
  - src/server/capabilities.ts
  - src/server/export-store.ts
  - src/native/directory-exchange.cc
  - binding.gyp
  - tests/api/export.test.ts
  - tests/api/export-publication.test.ts
  - tests/native/directory-exchange.test.ts
autonomous: true
requirements: [EXP-01, EXP-05, EXP-06, EXP-08]
must_haves:
  truths:
    - "The secured server snapshots and revalidates exactly one accepted draft revision and current opaque drift observation while retaining server authority over full launch OIDs, comparison, content, paths, and filenames (D-01, D-02, D-10)."
    - "A declared packaged target is re-export-capable only after Plan 04-03 compiles the narrow adapter for that exact target and observes both its exact Darwin `renameatx_np(..., RENAME_SWAP)` or Linux `renameat2(..., RENAME_EXCHANGE)` primitive and same-filesystem continuous old-or-new behavioral probe; no reconciliation claim promotes a target (D-03, D-15)."
    - "Every unprobed, unsupported, unavailable-runner, compile-failed, primitive-failed, or behavioral-probe-failed target is typed `reExportUnsupported` before any existing stable-path touch; first export may rename one fully validated candidate only after proving stable is absent (D-03)."
    - "One secured fixed export-directory reveal capability derives its target solely from server-held active-comparison full launch OIDs and the reusable platform reveal adapter; request/body/query supplies no path authority (D-16)."
    - "Receipt is returned only after final stable reread and exact SHA-256/byte verification of exactly review.json and review.md (D-16, EXP-05, EXP-06)."
  prohibitions:
    - "No stable-to-backup rename, deletion, or recoverable stable-path absence is permitted during re-export."
    - "No portable fallback performs re-export; unsupported state is explicit and typed before any stable mutation."
    - "No browser repository/ref/OID/path/filename/content/hash/Git command/overwrite authority, second route registry/security guard/capability registry/draft store, or generic reveal endpoint is introduced."
  artifacts:
    - path: "src/native/directory-exchange.cc"
      provides: "Narrow native same-filesystem atomic directory exchange adapter"
      exports: [exchangeDirectories, probeDirectoryExchange]
    - path: "src/server/export-store.ts"
      provides: "Validated candidate publication using first-export rename or supported native exchange"
      exports: [publishReviewExport, recoverReviewExport, ReExportUnsupported]
    - path: "src/contracts/api.ts"
      provides: "Strict export, receipt, drift, unsupported-publication, and reveal result algebra"
    - path: "tests/api/export-publication.test.ts"
      provides: "Exact D-03 interruption/fault and fallback guarantee"
  key_links:
    - from: "src/server/export-store.ts"
      to: "src/native/directory-exchange.cc"
      via: "native exchange only after candidate validation and snapshot/drift revalidation"
      pattern: "exchangeDirectories"
    - from: "src/server/routes.ts"
      to: "the ledger-resolved platform reveal adapter"
      via: "fixed stable export directory derived from server-held full launch OIDs"
      pattern: "reveal"
---

<objective>
Build the single secured export/reveal server slice with strict snapshot/drift authority and literal D-03 atomic replacement on supported platforms.

Purpose: Publish an exact validated pair without ever removing an existing stable pair and expose a fixed safe reveal action for the UI.
Output: API algebra, secured routes/capabilities, native exchange adapter and build probe, publication state machine, and focused API/native/fault tests.
</objective>

## Phase Goal

**As a** developer with a completed pinned review, **I want to** explicitly export one accepted revision as a stable agent-ready pair, **so that** an applying agent can act on precise feedback without Diff Review modifying source control.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-agent-ready-export/04-CONTEXT.md
@.planning/phases/04-agent-ready-export/04-RESEARCH.md
@.planning/phases/04-agent-ready-export/04-PATTERNS.md
@.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json
@.planning/phases/04-agent-ready-export/04-02-SUMMARY.md

<interfaces>
Run the reconciliation validator before editing. Substitute the exactly-one API, route, capability, draft queue/store, drift resolver, filesystem port, build owner, and platform reveal adapter. This plan is the first and unique owner of shared `api.ts`/route/capability Phase 4 writes; Plan 04-04 depends on it and extends those owners serially.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the narrow adapter and execute the declared packaged-target exchange matrix</name>
  <files>src/native/directory-exchange.cc, binding.gyp, tests/native/directory-exchange.test.ts, tests/api/export-publication.test.ts</files>
  <behavior>
    - Begin only after the real Plan 04-01 ledger validates with `publicationPolicy.kind = native-exchange-probe-pending`; reconciliation supplies owner/toolchain/packaging declarations but is never runtime-support evidence.
    - For every declared packaged target, the focused matrix invokes that target's grounded packaging runner, compiles/links the actual N-API adapter, and checks only the exact primitive and constants: Darwin `renameatx_np(..., RENAME_SWAP)`; Linux `renameat2(..., RENAME_EXCHANGE)` via the target-supported libc/syscall interface.
    - On every executable packaged target, the produced packaged CLI runs the same-filesystem probe with two complete sibling directories, makes exactly one native exchange call, proves names/content swapped, and repeats under synchronized child-process continuous reader plus interruption observation; stable is always an old or new complete directory and never absent.
    - A target becomes re-export-capable only from its own observed compile/link, primitive, and behavioral results. Missing runner, cross-compiled but unexecuted output, unsupported OS/primitive, compile/link failure, or failed/partial/absence observation yields typed `reExportUnsupported`.
    - Adapter accepts only already-open or fixed server-managed contained sibling identities and rejects cross-device, symlink, unexpected type, containment, and arbitrary path authority.
  </behavior>
  <action>Validate the pending reconciliation ledger first, then write native and integration tests and build the actual adapter; do not ask Plan 04-01 to prove code that did not yet exist. Extend the reconciled build seam with the narrowest N-API adapter performing exactly one atomic directory exchange. On Darwin call `renameatx_np` with `RENAME_SWAP`; on Linux call `renameat2` or the target-supported system-call wrapper with `RENAME_EXCHANGE`. Preserve errno only as an internal typed classification and expose bounded safe results. Add compile guards so all other targets build an explicit unsupported adapter, never a rename sequence. Make `04-03-native-exchange` an executable matrix over every target declared by `publicationPolicy.targets`: use each declaration's grounded package runner/toolchain, compile/link the actual adapter for that target, run the exact primitive availability check, then launch the generated target artifact in its declared executable runner to perform the same-filesystem continuous old-or-new probe. The matrix must fail if a declared executable target is skipped. A target without an executable runner or without all passing observations remains explicitly `reExportUnsupported`; only actually probe-verified target identities may be recorded or exposed as re-export-capable. Feed the immutable current-target observed result into later publication code/tests. Do not add a package, spawn a shell from product code, call a generic move helper, infer support from OS name/compile success, or emulate exchange with multiple renames.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process');require('node:child_process').execFileSync(process.execPath,['.planning/phases/04-agent-ready-export/validate-reconciliation.mjs','.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json'],{stdio:'inherit'});const x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8'));if(x.publicationPolicy?.kind!=='native-exchange-probe-pending')throw Error('native exchange must enter 04-03 pending');const c=x.commands['04-03-native-exchange'];if(!c)throw Error('missing 04-03-native-exchange');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>The focused target matrix attempts every declared packaging target; only a target with its own observed packaged compile/link, exact primitive, and same-filesystem continuous old-or-new probe is re-export-capable, and every skipped/unprobed/unsupported/failed target is typed `reExportUnsupported`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add secured accepted-snapshot export and fixed export-directory reveal routes</name>
  <files>src/contracts/api.ts, src/server/routes.ts, src/server/capabilities.ts, tests/api/export.test.ts</files>
  <behavior>
    - Export route rejects wrong token, Host, Origin, method, content type, bounded body, and unknown fields before draft/Git/serializer/filesystem calls; body is only expectedRevision plus optional opaque driftAcknowledgementToken.
    - Revision conflict, corrupt/schema-invalid/newer draft, and changed drift publish nothing. Drift token is unpredictable, session-bound, and binds role/status/launch/current full OIDs; changed observation invalidates consent without refreshing pinned identities.
    - Reveal route uses one exact method, no body, no query, and no path/ref/OID/repository input. Wrong token/Host/Origin/method/body/query invokes neither filesystem nor platform reveal adapter.
    - Reveal target is `.diff-review/exports/<fullBaseOid>..<fullHeadOid>/` derived only from server-held active-comparison launch OIDs. Result is strict and bounded, leaks no absolute path, temp name, token, argv, stderr, or raw OS error.
  </behavior>
  <action>Add failing real-Fastify zero-invocation tests first. Extend the single strict API algebra, secured registry, and capability registry. At the existing per-comparison queue, load/classify and validate the canonical draft; require expected revision; clone/freeze immutable snapshot plus canonical fingerprint. Re-observe only retained launch descriptors with the fixed Git runner. Require current opaque acknowledgement when moved/unavailable; before publication, reload and revalidate revision/fingerprint and observation. Do not accept replacement review state or authority fields. For reveal, reuse the ledger-discovered platform reveal adapter rather than a draft-file-specific endpoint: create exactly one fixed active-comparison export-directory capability whose server derives the stable full-OID directory. Validate it exists as a complete pair before reveal. Require token, exact Host, acceptable Origin, exact method, empty body and query before resolution or adapter invocation. Do not expose a generic reveal/open path API.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-03-export-api'];if(!c)throw Error('missing 04-03-export-api');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>The existing secured server exposes fixed-authority export and export-directory reveal capabilities with complete security and zero-invocation denial evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Publish the validated pair with atomic re-export or refusal fallback</name>
  <files>src/server/export-store.ts, src/contracts/api.ts, src/server/routes.ts, tests/api/export-publication.test.ts</files>
  <behavior>
    - Stable path derives only from full server-held launch OIDs and contains exactly regular review.json and review.md; receipt is never a third file.
    - Candidate writes are exclusive, restrictive, flushed, closed, reread, revalidated, reprojected byte-identically, exact-hashed, and directory-validated before commit.
    - If stable is absent, first export performs one candidate-to-stable atomic rename only after a second absence/type check and snapshot/drift revalidation; failure before rename exposes neither stable file.
    - If stable exists and exchange is supported, one native exchange makes stable atomically switch from old complete pair to new complete pair. The displaced old pair remains at candidate name until final stable reread/hash and parent sync; cleanup failure never invalidates stable success.
    - If the current packaged target lacks Task 1's exact observed target result, is unprobed/unsupported, has an unavailable runner, failed compile/primitive/behavior probe, is cross-device, or is otherwise unsupported, return `reExportUnsupported` before rename/delete/exchange/metadata/content touch of stable. Portable fallback never re-exports.
    - Interruption before exchange leaves old stable pair; interruption during/after the one-call exchange leaves stable resolving continuously to old or new complete pair; restart validates stable and preserves ambiguous remnants without ever restoring through stable absence.
  </behavior>
  <action>Write full fault/interruption/fallback tests first using the actual filesystem port. Serialize per comparison. Create unpredictable candidate siblings beneath fixed `.diff-review/exports`; reject symlink/type/containment/remnant ambiguity. Write exact Plan 04-02 buffers with restrictive mode, flush/sync/close/reread, parse/revalidate JSON, rerender/compare Markdown, hash, and require exactly two names. Re-enter the draft queue and revalidate revision/fingerprint/drift. Branch only on stable existence: ABSENT uses a single atomic rename after a race-safe absence check; PRESENT may proceed only when the current packaged target exactly matches Task 1's own observed compile/link, primitive, and same-filesystem continuous old-or-new result, then performs exactly one exchange. Any absent/mismatched/unprobed/unsupported/failed target result returns typed `reExportUnsupported` before stat-changing or content-changing contact with stable. Never rename stable to backup. After exchange, sync parent under the probe-verified target policy, reread stable, verify exact hashes/lengths/names, then return relative paths/revision/shared timestamp/drift state. Delete the displaced old generation only after success; cleanup failure is recoverable housekeeping and never removes stable. Recovery validates continuously present stable first and may clean only proven non-stable complete remnants; ambiguous evidence is preserved with typed recovery-required.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-03-publication-faults'];if(!c)throw Error('missing 04-03-publication-faults');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>D-03 is literal: supported re-export atomically exchanges complete directories; unsupported re-export refuses before touching stable; first export atomically renames only after full validation and stable absence.</done>
</task>

</tasks>

<acceptance_criteria>
- Every declared packaging target is attempted by an executable matrix; only target-specific observed compile/link, exact primitive, and same-filesystem continuous old-or-new results grant re-export capability, while unexecuted/unsupported/failed targets are typed `reExportUnsupported` with no stable-path touch.
- Every interruption assertion observes stable continuously as either the old or new complete pair; no accepted test state has temporary stable absence.
- Unsupported/cross-device/probe-failed re-export returns typed unsupported before any stable mutation; first export remains allowed only when stable is absent.
- Fixed reveal is a real secured server capability connected to the reusable platform adapter and accepts no browser path authority.
- This plan owns all initial Phase 4 `api.ts`/route/capability writes; downstream shared-owner edits depend on it.
</acceptance_criteria>

## Artifacts this phase produces

- Narrow native directory exchange adapter and reconciled build integration.
- Existing shared API/route/capability owners extended with export and fixed export-directory reveal.
- Export store with validated candidate, native exchange, refusal fallback, final reread receipt, and recovery.
- Focused native/API/publication tests covering security, drift, exact D-03, faults, and interruptions.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → secured export/reveal routes | Untrusted requests may seek repository, path, content, or command authority. |
| Draft/drift state → candidate | Concurrent accepted state may invalidate generation. |
| Candidate/native adapter → stable filesystem | A weak fallback could remove or corrupt the previous pair. |
| Stable bytes → receipt | Intended bytes are not final publication evidence. |

## STRIDE Threat Register — ASVS L1, high threats block

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-11 | Spoofing/Elevation | export/reveal routes | High | mitigate/block | Token/Host/Origin/method/body/query strict guards and fixed capabilities before work. |
| T-04-12 | Tampering | revision/drift | High | mitigate/block | Immutable snapshot and immediate revision/fingerprint/observation revalidation. |
| T-04-13 | Tampering/Repudiation | re-export | High | mitigate/block | One native swap on supported targets; typed refusal before stable touch otherwise. |
| T-04-14 | Elevation | native paths | High | mitigate/block | Server-derived contained siblings, type/no-follow checks, no generic path interface. |
| T-04-15 | Information Disclosure | route/results | High | mitigate/block | Relative bounded results; no absolute/temp/token/argv/stderr/raw OS detail. |
| T-04-16 | Repudiation | receipt | High | mitigate/block | Final stable exact-byte reread, SHA-256, byte lengths, exactly-two-name check. |
| T-04-SC | Tampering | native build supply chain | High | mitigate/block | Project-owned source, existing toolchain only, no package install, build/runtime probes. |
</threat_model>

<verification>Run only the three exact ledger-resolved focused commands. Do not install packages or run formatters, linters, broad builds, project-wide suites, or Git commands.</verification>

<success_criteria>
- EXP-01/05/06/08 and D-01–D-03/D-10/D-15–D-17 are enforced at API/native/storage boundaries.
- Literal atomic replacement, refusal fallback, fixed reveal, and exact receipt are executable and tested.
</success_criteria>

<output>Create `.planning/phases/04-agent-ready-export/04-03-SUMMARY.md` with resolved owners, the complete declared-target matrix and per-target packaged compile/link, exact primitive, runner, same-filesystem continuous old-or-new observations, explicit unsupported/unexecuted/failed dispositions, API/reveal denial matrix, snapshot/drift results, exact D-03 interruption matrix, refusal-before-touch proof, and receipt evidence.</output>
