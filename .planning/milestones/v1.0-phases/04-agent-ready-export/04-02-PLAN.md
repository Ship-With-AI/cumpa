---
phase: 04-agent-ready-export
plan: 02
type: tdd
wave: 2
depends_on: [04-01]
files_modified:
  - src/contracts/draft.ts
  - src/export/review-export.ts
  - src/export/render-review-markdown.ts
  - tests/unit/review-export.test.ts
  - tests/unit/review-markdown.test.ts
autonomous: true
requirements: [EXP-02, EXP-03, EXP-04, EXP-05, EXP-07]
must_haves:
  truths:
    - "One strict versioned canonical document preserves full pinned identities, accepted revision, drift facts, optional summary, every retained comment state/timestamp, exact anchors, and validated counts (D-02, D-04, EXP-02)."
    - "Canonical JSON uses deterministic total domain order and exact RFC 8785-compatible UTF-8 bytes; unchanged accepted state changes only at the explicit injected export timestamp (D-04, EXP-07)."
    - "Markdown is derived only by reparsing and revalidating those exact JSON bytes and implements the complete actionability truth table and exact applying-agent contract (D-05–D-12, EXP-03, EXP-04)."
    - "Applying-agent instructions require verification of the pinned commit, relevant blob, repository-relative path, base/head side, exact selected text, and context hash before editing; recorded line number is not authority; ambiguous, missing, stale, or orphaned anchors are reported rather than guessed; resolved feedback is never applied (D-08, EXP-04)."
    - "Hashes are SHA-256 over exact final buffers and paths remain repository-relative; no absolute filesystem identity is serialized (D-07, D-16, EXP-05)."
  prohibitions:
    - "No second draft/comment/anchor/path/OID/status vocabulary, path comparator, browser serializer, or independent Markdown review model is introduced."
    - "No stale, orphaned, resolved, ambiguous, or missing anchor becomes requested work; line number is never authority."
  artifacts:
    - path: "src/export/review-export.ts"
      provides: "Strict snapshot mapping, total ordering, canonical serializer/reparser, and exact-byte hashing"
      exports: [ReviewExportV1Schema, buildReviewExportV1, canonicalizeReviewExport, parseCanonicalReviewExport, hashExportBytes]
    - path: "src/export/render-review-markdown.ts"
      provides: "Pure Markdown projection from reparsed canonical export"
      exports: [renderReviewMarkdown]
    - path: "tests/unit/review-export.test.ts"
      provides: "Strict schema, ordering, RFC vector, determinism, and exact-hash evidence"
    - path: "tests/unit/review-markdown.test.ts"
      provides: "Actionability, hostile text, empty state, and exact instruction evidence"
  key_links:
    - from: "src/export/render-review-markdown.ts"
      to: "src/export/review-export.ts"
      via: "accept only parseCanonicalReviewExport result"
      pattern: "parseCanonicalReviewExport"
---

<objective>
Implement the pure canonical export document and the only allowed Markdown projection as a focused TDD slice.

Purpose: Establish exact bytes and agent semantics before server snapshot, native publication, route, or UI work.
Output: Shared strict export schema composition, canonical serializer/reparser/hash functions, Markdown projector, and focused unit tests.
</objective>

## Phase Goal

**As a** developer with a completed pinned review, **I want to** explicitly export one accepted revision as a stable agent-ready pair, **so that** an applying agent can act on precise feedback without Diff Review modifying source control.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-agent-ready-export/04-CONTEXT.md
@.planning/phases/04-agent-ready-export/04-RESEARCH.md
@.planning/phases/04-agent-ready-export/04-PATTERNS.md
@.planning/phases/04-agent-ready-export/04-01-SUMMARY.md
@.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json

<interfaces>
Run the strict reconciliation validator first. Every listed source/test path and symbol is a conditional responsibility label substituted through the approved ledger. Extend the exactly-one shared schema/path/anchor owner and record actual substitutions in 04-02-SUMMARY.md.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Specify and implement canonical accepted-review JSON</name>
  <files>src/contracts/draft.ts, src/export/review-export.ts, tests/unit/review-export.test.ts</files>
  <behavior>
    - Strict schema rejects unknown keys, short/invalid full OIDs, invalid timestamps, lone surrogates, non-finite values, absolute paths, invalid state/resolvedAt relationships, missing anchor identity, and counts inconsistent with records.
    - Map exactly one immutable accepted snapshot with accepted revision; full launch base/head/merge-base/comparison identities; launch/current drift observation and acknowledgement; exact optional summary; every retained comment/state/timestamp/anchor; derived counts.
    - Order exact lossless repository-relative path, explicit base-before-head side rank, side-specific line, blob OID, context hash, stable comment ID; locale, case, display escaping, host separators, and insertion order cannot affect bytes.
    - RFC 8785 vectors prove recursive unsigned UTF-16 property order, ECMAScript primitive encoding, UTF-8, no normalization, arrays preserved in domain order, no whitespace, and no trailing JSON newline.
    - Same accepted snapshot and injected time is byte-identical; changing only time changes only explicit timestamp. SHA-256 is over exact Buffer with lowercase 64-hex and byte length.
  </behavior>
  <action>RED: use ledger-resolved conventions to add strict refinement, RFC vector, unusual-path total-order, determinism, timestamp-only-delta, exact-hash, and no-absolute-path tests. GREEN: compose existing path/OID/comment/anchor/timestamp/status schemas rather than copying them; implement immutable snapshot mapping, a closed I-JSON canonicalizer, exact-byte parser/revalidator, and Buffer SHA-256. JSON has no trailing newline. Do not use browser state, display identities, locale sorting, host path conversion, or object/string reconstruction hashes. REFACTOR without changing golden bytes.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process');require('node:child_process').execFileSync(process.execPath,['.planning/phases/04-agent-ready-export/validate-reconciliation.mjs','.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json'],{stdio:'inherit'});const x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-02-canonical'];if(!c)throw Error('missing 04-02-canonical');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>Strict canonical exact JSON bytes preserve the whole accepted revision, full identities, deterministic order, and exact hashes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Derive exact agent Markdown only from reparsed canonical bytes</name>
  <files>src/export/review-export.ts, src/export/render-review-markdown.ts, tests/unit/review-markdown.test.ts</files>
  <behavior>
    - Parse and revalidate exact canonical JSON bytes before projection; no draft or pre-reparse parallel model reaches Markdown.
    - Markdown fixed order is pinned comparison/drift; summary metadata/body; Open actionable requests grouped by exact path; Needs reviewer attention; applying-agent instructions. LF only, exactly one final LF.
    - Open+verified is requested work; open+stale/open+orphaned is attention-only; resolved+any is count/history-only. JSON retains every record present in accepted revision.
    - Actionable entries include relative path, side, line, blob, exact selected text, nearby context, context hash, and body. Instructions require pinned commit/blob/path/side/text/context verification, call line a hint, forbid guessing/fuzzy relocation, and require ambiguity/missing/stale/orphaned/mismatch reporting.
    - Empty summary prints No summary provided without body; zero-actionable and fully empty drafts remain complete valid artifacts. Hostile Markdown cannot alter fixed instructions.
  </behavior>
  <action>RED: add exact four-row actionability, hostile Markdown, dynamic fence, LF, empty summary, zero-actionable, fully empty, all-anchor-fields, no-absolute-path, and normative instruction tests. GREEN: accept only the `parseCanonicalReviewExport` result and render paths/review text as escaped or dynamically safely fenced data. Use one line array and LF join plus one final LF; never browser preview or `os.EOL`. Preserve one injected timestamp from the canonical value. Resolved records get count/history pointer only; stale/orphaned never requested work.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-02-canonical'];if(!c)throw Error('missing 04-02-canonical');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>Markdown is the sole exact reparse-derived human projection and implements D-05–D-12 without actionability drift.</done>
</task>

</tasks>

<acceptance_criteria>
- Focused tests record RED then GREEN for strict schemas, RFC vectors, total ordering, exact bytes, four-row actionability, hostile text, empty states, and hashes.
- Same accepted snapshot and timestamp are byte-identical; changing only timestamp changes only explicit timestamp occurrences.
- Markdown comes only from reparsed canonical JSON and contains the complete applying-agent identity/no-guess contract.
- The plan touches five pure schema/projection files and no API, route, capability, native, storage, or UI owner.
</acceptance_criteria>

## Artifacts this phase produces

- Ledger-resolved shared schema owner extended by composition with one strict export document.
- Ledger-resolved canonical export module with snapshot mapping, ordering, serialization, reparse, and hash responsibilities.
- Ledger-resolved Markdown module with pure exact-value projection.
- Two focused unit-test owners covering exact machine and human contracts.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Accepted draft → export document | Persisted data remains untrusted until strict validation. |
| Canonical bytes → Markdown | Parallel derivation could mix models or revisions. |
| Review/path text → Markdown instructions | Untrusted text could spoof applying-agent instructions. |

## STRIDE Threat Register — ASVS L1, high threats block

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-06 | Tampering | schema/order | High | mitigate/block | Strict composed schema, exact total order, canonical vectors and count refinements. |
| T-04-07 | Tampering/Repudiation | JSON→Markdown | High | mitigate/block | Reparse and revalidate exact JSON bytes; only parsed value reaches projector. |
| T-04-08 | Spoofing | Markdown content | High | mitigate/block | Fixed normative template and escaped/dynamically fenced untrusted data. |
| T-04-09 | Information Disclosure | paths | High | mitigate/block | Repository-relative validated identities only; absolute paths rejected. |
| T-04-10 | Repudiation | hashes | High | mitigate/block | SHA-256 exact buffers with byte lengths and golden mutation cases. |
| T-04-SC | Tampering | packages | High | mitigate/block | No new dependency or install; use Node core and approved existing schema owner. |
</threat_model>

<verification>Run only the exact ledger-resolved `04-02-canonical` command. Do not install packages or run formatters, linters, broad suites/builds, or Git commands.</verification>

<success_criteria>
- EXP-02/03/04/05/07 and D-02/D-04–D-12/D-16 are proven at pure boundaries.
- Canonical exact bytes and reparsed Markdown are singular, deterministic, and identity-preserving.
</success_criteria>

<output>Create `.planning/phases/04-agent-ready-export/04-02-SUMMARY.md` with resolved paths/symbols, RED/GREEN evidence, schema and ordering profile, golden bytes/hashes, actionability table, empty-state cases, and downstream interfaces.</output>
