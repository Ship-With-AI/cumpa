---
phase: 04-agent-ready-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json
  - .planning/phases/04-agent-ready-export/04-01-RECONCILIATION.md
  - .planning/phases/04-agent-ready-export/validate-reconciliation.mjs
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EXP-04, EXP-05, EXP-06, EXP-07, EXP-08, SAFE-04]
must_haves:
  truths:
    - "Before any Phase 4 source edit, strict independently validated ledger grounds every upstream seam in exactly one readable repository-relative owner and records symbol/interface evidence plus downstream substitution keys."
    - "The ledger enumerates every focused Phase 4 command as resolvable executable explicit argv, proves package-manager/manifest/lock agreement and zero required installs, records only grounded existing toolchain/build configuration plus available packaging-target declarations; native runtime support remains `native-exchange-probe-pending` until Plan 04-03 builds and probes the adapter (D-03, D-15, D-18)."
    - "Reconciliation discovers a reusable platform reveal adapter and secured registry; it does not assume an export-directory reveal endpoint or native exchange adapter already exists (D-16)."
    - "A Node-core mutation self-test independently proves rejection of malformed schema/evidence/cardinality/path/hash/substitution/command/package/publication-policy ledgers before the valid real ledger is checked before 04-02."
  prohibitions:
    - "No prior PLAN path or provisional symbol is implementation evidence; no missing or ambiguous seam authorizes a parallel owner."
    - "Reconciliation is read-only with respect to source, tests, manifests, lockfiles, Git state, package installation, builds, and runtime/native probes; it cannot approve behavior of the unbuilt adapter."
  artifacts:
    - path: ".planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json"
      provides: "Strict grounded owner/interface/substitution/command/package/toolchain ledger with native-exchange-probe-pending target declarations"
      contains: "schemaVersion"
    - path: ".planning/phases/04-agent-ready-export/validate-reconciliation.mjs"
      provides: "Independent fail-closed ledger validator with adversarial mutation self-test"
      exports: [validateReconciliationLedger, runMutationSelfTest]
    - path: ".planning/phases/04-agent-ready-export/04-01-RECONCILIATION.md"
      provides: "Human-readable evidence and prerequisite disposition"
  key_links:
    - from: ".planning/phases/04-agent-ready-export/validate-reconciliation.mjs"
      to: ".planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json"
      via: "strict schema, filesystem/evidence/package/command/publication validation"
      pattern: "requiredCommandKeys"
---

<objective>
Create the mandatory fail-closed upstream reconciliation gate consumed by every later Phase 4 plan.

Purpose: Prevent greenfield responsibility labels from becoming guessed duplicate owners and establish the pre-source reconciliation gate: ground each declared target, packaging runner, toolchain, owner, command, and pending publication policy. Plan 04-03 Task 1 alone builds and probes every declared target before publication implementation; failed, unprobed, or unsupported targets become typed `reExportUnsupported` without touching the stable path (D-03, D-15, D-18).
Output: Grounded machine ledger, readable evidence report, and independent strict validator.
</objective>

## Phase Goal

**As a** developer with a completed pinned review, **I want to** explicitly export one accepted revision as a stable agent-ready pair, **so that** an applying agent can act on precise feedback without Diff Review modifying source control.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-agent-ready-export/04-CONTEXT.md
@.planning/phases/04-agent-ready-export/04-RESEARCH.md
@.planning/phases/04-agent-ready-export/04-PATTERNS.md
@.planning/phases/04-agent-ready-export/04-UI-SPEC.md

<interfaces>
The ledger is the only substitution authority for conditional paths in Plans 04-02–04-08. Later plans must stop with the validator's seam-specific prerequisite error when status is not `approved`; they may not infer an owner from a prior plan.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory every upstream seam and focused command with grounded evidence</name>
  <files>.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json, .planning/phases/04-agent-ready-export/04-01-RECONCILIATION.md</files>
  <action>Read implemented Phase 1–3 summaries, source, tests, manifest, lockfile, existing build/toolchain configuration, packaging declarations, and scripts without editing or executing repository packages. Produce schemaVersion 1 JSON with `status`, `repositoryRoot`, `seams`, `substitutions`, `commands`, `packageEvidence`, `publicationPolicy`, and `failures`. The required seam keys are exactly: sharedSchema; draftStore; draftAnchorVocabulary; exactPathIdentity; comparisonInventory; selectorDriftResolver; safeGitRunner; securedRouteRegistry; capabilityRegistry; apiClient; canonicalBrowserState; reviewPanel; uiPrimitives; filesystemFaultPort; platformRevealAdapter; packageRunner; browserRunner. Each seam is an array constrained later to exactly one owner object containing a unique repository-relative readable regular-file `ownerPath`, nonempty `responsibility`, at least one `symbols` entry, and a unique downstream `substitutionKey`. Each symbol entry records name, kind, export/interface/signature text, evidence owner path, inclusive line range, exact range SHA-256, and whole-file SHA-256 so the claim cannot self-attest against a missing or stale file. Record actual caller/callee or route-registration evidence where the seam is integration rather than a public export. Record reusable platform reveal behavior separately from any draft-specific reveal route. Enumerate command keys exactly: `04-02-canonical`, `04-03-export-api`, `04-03-native-exchange`, `04-03-publication-faults`, `04-04-inventory-ignore`, `04-04-gitignore-api`, `04-05-export-ui-states`, `04-06-receipt-ignore-ui`, `04-07-source-snapshot`, `04-07-real-fs-recovery`, `04-08-packaged-export`. Each command records an absolute executable resolved during reconciliation, a nonempty explicit argv array with no shell string, cwd, purpose, and evidence linking it to actual runner/script/config ownership; this grounds how later tests run without claiming their not-yet-written cases pass. Record package manager, manifest path/hash, lockfile path/hash/kind, packageManager declaration agreement, every existing dependency Phase 4 will use with manifest and lock evidence, and `requiredInstalls: []`. Record every available declared packaging target with exact OS/architecture/target triple, packaging command/runner evidence, existing compiler/linker/N-API configuration, and whether that target can actually be executed by the focused target-matrix command. Set `publicationPolicy.kind` exactly to `native-exchange-probe-pending`; every target has `reExportCapability: pending` and no runtime-support assertion. State that Plan 04-03 alone may promote a target after compiling the narrow adapter and observing its exact primitive plus same-filesystem continuous old-or-new probe; unexecuted, unsupported, or failed targets resolve to `reExportUnsupported`. Do not run tests, builds, package scripts, native probes, installs, formatters, linters, Git commands, or source mutations.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),k=['commands','failures','packageEvidence','publicationPolicy','repositoryRoot','schemaVersion','seams','status','substitutions'];if(JSON.stringify(Object.keys(x).sort())!==JSON.stringify(k)||x.schemaVersion!==1||!Array.isArray(x.failures))throw Error('invalid reconciliation ledger envelope')"</automated>
  </verify>
  <done>The ledger contains every exact seam, substitution, focused command, package/lock/no-install fact, reveal-adapter fact, toolchain/config prerequisite, and available packaging-target declaration, while native exchange publication remains explicitly pending rather than preapproved.</done>
</task>

<task type="auto">
  <name>Task 2: Implement and adversarially self-test the strict fail-closed reconciliation validator</name>
  <files>.planning/phases/04-agent-ready-export/validate-reconciliation.mjs, .planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json, .planning/phases/04-agent-ready-export/04-01-RECONCILIATION.md</files>
  <action>Implement `validate-reconciliation.mjs` using Node core only, with a deterministic `--self-test` mode that clones a known-valid in-memory fixture into one focused mutation per rejection branch and asserts every mutation exits through the expected seam-specific error. The self-test must prove rejection of unknown and omitted top-level or nested keys; wrong types and empty values; zero owners and two owners independently for every required seam; duplicate seam, substitution, symbol, and command keys; absent, directory, symlink, absolute, and repository-escaping owner/evidence/manifest/lock/config paths; stale whole-file hashes and stale inclusive-line-range hashes; missing signature tokens and missing callsite/registration evidence; broken substitution-to-seam, substitution-to-owner, and substitution-to-symbol mappings; missing, extra, unfocused, and unresolvable commands; non-absolute and non-executable command binaries; manifest/lock/package-manager declaration or dependency disagreement; nonempty `requiredInstalls` or any install-needed claim; and every publication-policy rejection branch, including unknown policy kind, a preapproved native-support claim, missing target/toolchain/packaging evidence, duplicate/undeclared targets, a target claimed re-export-capable without an observed Plan 04-03 result, unsupported/failed/unexecuted target claimed capable, portable/stable-to-backup fallback, stable-path absence, or first-export overwrite. Normal validation rejects the same branches, requires exact key sets/types/nonempty values, recomputes whole-file and exact range SHA-256, verifies tokens/callsites, requires exactly one owner and verified evidence per seam, proves one-to-one substitutions, requires the exact eleven focused command keys with absolute executable access and config evidence, parses manifest/lock agreement and zero installs, and accepts at this pre-source stage only `publicationPolicy.kind = native-exchange-probe-pending` with grounded available target/toolchain declarations and every target capability pending. It must not compile, execute, or infer the unbuilt adapter. Generate Markdown only from validated real-ledger facts. `--schema-only` validates shape without approval; `--self-test` uses disposable Node-created fixtures and fails if any invalid mutation is accepted; default validates the real ledger and exits nonzero with focused errors.</action>
  <verify>
    <automated>node .planning/phases/04-agent-ready-export/validate-reconciliation.mjs --self-test &amp;&amp; node .planning/phases/04-agent-ready-export/validate-reconciliation.mjs .planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json</automated>
  </verify>
  <done>The first assertion adversarially proves every specified schema, cardinality, evidence, path, hash, substitution, command, package, install, and publication-policy rejection; the separate second assertion proves the grounded real ledger is valid only with native exchange support pending.</done>
</task>

</tasks>

<acceptance_criteria>
- Exact owner cardinality is one for every required seam, with independently checked readable path and symbol/interface/callsite evidence.
- The mutation self-test rejects every malformed schema/evidence/cardinality/path/hash/substitution/command/package/install/publication-policy branch before the valid real-ledger assertion runs.
- Exact command-key set, executable/argv focus, package/lock agreement, zero installs, reveal-adapter discovery, toolchain/config facts, and available packaging-target declarations are validated; native adapter behavior remains `native-exchange-probe-pending` for Plan 04-03.
</acceptance_criteria>

## Artifacts this phase produces

- `04-01-RECONCILIATION.json`: strict approved owner/substitution/command/package/publication ledger.
- `validate-reconciliation.mjs`: independent Node-core validator.
- `04-01-RECONCILIATION.md`: generated evidence report and failures.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Prior plans/summaries → implemented source claims | Planned names are not evidence. |
| Ledger → downstream source edits | Fabricated owners or commands could create duplicate authority. |
| Manifest/lock/build policy → native publication | Unsupported or uninstalled prerequisites could weaken D-03. |

## STRIDE Threat Register — ASVS L1, high threats block

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-01 | Spoofing | owner evidence | High | mitigate/block | Exactly-one owner, readable contained file, range/file hashes, symbol/interface token and substitution validation. |
| T-04-02 | Tampering | command ledger | High | mitigate/block | Exact key set, executable access, argv/config evidence and no shell string. |
| T-04-03 | Tampering | package/lock evidence | High | mitigate/block | Manifest/lock hashes, manager-kind agreement, dependency evidence and zero required installs. |
| T-04-04 | Tampering/Repudiation | publication policy | High | mitigate/block | Darwin/Linux primitive, build probe, runtime exchange test, supported-target disposition and refusal fallback. |
| T-04-05 | Elevation | reveal authority | High | mitigate/block | Discover platform adapter/security registry only; later endpoint remains fixed server authority. |
| T-04-SC | Tampering | supply chain | High | mitigate/block | No installs; reconciliation rejects requiredInstall entries. |
</threat_model>

<verification>Run only the two Node-core validator invocations above. Do not execute repository packages, tests, builds, formatters, linters, Git commands, or installs.</verification>

<success_criteria>
- The reconciliation ledger is independently grounded, complete, exact, and fail-closed.
- Before publication implementation, the gate grounds each declared target, packaging runner, toolchain, owner, command, and pending publication policy; Plan 04-03 Task 1 alone builds and probes every declared target, with every failed, unprobed, or unsupported target becoming typed `reExportUnsupported` without touching the stable path (D-03, D-15, D-18).
- Plans 04-02–04-08 have deterministic substitutions and focused commands.
</success_criteria>

<output>Create `.planning/phases/04-agent-ready-export/04-01-SUMMARY.md` recording validator results, exact owners/symbol evidence/substitutions, exact command keys, package/lock/no-install evidence, reusable reveal adapter, and supported-target native publication disposition.</output>
