---
phase: 01-pinned-local-comparison
plan: 11
type: tdd
wave: 11
depends_on: [01-10]
files_modified:
  - src/web/App.vue
  - src/web/styles.css
  - src/web/components/IdentityHeader.vue
  - src/web/components/IdentityPanel.vue
  - src/web/components/CopyButton.vue
  - src/web/components/EmptyState.vue
  - src/web/components/ErrorState.vue
  - tests/e2e/pinned-session.spec.ts
requirements: [CMP-08]
autonomous: true
must_haves:
  truths:
    - "Loaded sessions keep ordered identities, pin state, and dirty-byte warnings visible and disclose full copyable IDs and applicable worktree paths."
    - "A zero-change comparison renders validated identities, `0 changed files`, and the deliberate empty state without an error or false loading flash."
    - "Stopped, denied, and loading session states follow the verified hierarchy and non-leaking copy; retry belongs only to an individual file-detail failure in Plan 12."
  artifacts:
    - path: src/web/components/IdentityHeader.vue
      provides: "Persistent ordered identity, pin, and dirty context"
    - path: src/web/components/IdentityPanel.vue
      provides: "Full immutable identity disclosure"
    - path: src/web/components/CopyButton.vue
      provides: "Shared exact-value copy action with success/failure live feedback"
    - path: tests/e2e/pinned-session.spec.ts
      provides: "Named identity/session/empty behavior contract"
  key_links:
    - from: src/web/App.vue
      to: src/web/api/client.ts
      via: "validated session DTO atomically drives identity and session-state hierarchy"
      pattern: "createSessionClient"
---
<objective>
Implement the identity and session-state seam of the final browser experience.

Purpose: Deliver CMP-08 and the identity/session portions of D-06, D-08, D-10, D-14–D-18 with behavior tests created in RED before production edits.
Output: Persistent identity UI, full identity disclosure, empty/session states, and focused packaged RED/GREEN proof.
</objective>
<execution_context>@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md
@.planning/phases/01-pinned-local-comparison/01-10-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify identity session and empty-state behavior</name>
  <read_first><file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file><file>src/contracts/api.ts</file><file>src/web/api/client.ts</file><file>tests/e2e/pinned-session.spec.ts</file></read_first>
  <files>tests/e2e/pinned-session.spec.ts</files>
  <behavior>
    - "The named `identity session and empty states` case asserts ordered safe labels/short IDs, pin cue, dirty badges, full IDs, worktree paths, specifically named copy controls, and no-follow-moving-refs copy."
    - "The case activates each full base/head/merge-base copy action, asserts the adjacent full ID is written, `Copied` is announced politely and resets after two seconds, and clipboard failure is announced without losing the full value."
    - "The case asserts loading replacement, zero-change success with `0 changed files`, stopped/session-unavailable, and security-denial copy without leaking authorities; it does not present retry for a session-wide failure."
  </behavior>
  <action>Extend the existing packaged test first with the exact named case `identity session and empty states`. Reuse the generated-bin fixture and validated API; prove prerequisites separately and require the behavioral run to reach and fail a missing identity/session UI assertion. Do not add metadata/availability or responsive assertions here.</action>
  <verify><automated><command expected="0" classification="prerequisite">npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "generated CLI opens immutable pinned session"</command><command expected="nonzero" classification="behavioral-red">npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "identity session and empty states"</command></automated></verify>
  <acceptance_criteria>The named behavior exists, reaches its UI assertions, and is RED only because the identity/session experience is missing.</acceptance_criteria>
  <done>The identity/session contract is RED before production changes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN — render immutable identities and session states</name>
  <read_first><file>tests/e2e/pinned-session.spec.ts</file><file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file><file>src/web/App.vue</file></read_first>
  <files>src/web/App.vue, src/web/styles.css, src/web/components/IdentityHeader.vue, src/web/components/IdentityPanel.vue, src/web/components/CopyButton.vue, src/web/components/EmptyState.vue, src/web/components/ErrorState.vue</files>
  <action>Implement the exact verified state hierarchy: one loading h1 until validation; then one persistent h1 with ordered safe labels/short IDs, pin cue, dirty badges, and Comparison identities disclosure. Render Base, Head, Merge base in order with non-truncated full IDs, applicable worktree paths, dirty explanations, and the exact pinned/no-moving-refs statement. Create CopyButton now and use it in IdentityPanel for the specifically labeled full base/head/merge-base actions: copy only the adjacent full ID, announce `Copied` through a polite live region, reset after two seconds, and expose non-destructive clipboard failure feedback while retaining the value. Keep the wide/medium panel non-modal; leave narrow focus-trap refinements to Plan 13. Atomically render deliberate zero-change success and session-wide stopped/security/error states with exact UI-spec copy, no retry affordance for session-wide failure, and no interactive tree/details in that state. Do not implement file metadata, unsupported placeholders, or Phase 2 controls.</action>
  <verify><automated><command expected="0">npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "identity session and empty states"</command></automated></verify>
  <acceptance_criteria>All named identity, loading, empty, dirty, pinning, exact copy success/failure/reset, stopped, denial, and error assertions pass through the package.</acceptance_criteria>
  <done>CMP-08 and the identity/session seam are GREEN.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-25 | Information disclosure | identity/error UI | mitigate | Validated frozen DTO only; generic denial copy and no token/path diagnostics. |
| T-01-26 | Spoofing | identity presentation | mitigate | Ordered labels plus full copyable OIDs and explicit pin/dirty language. |
| T-01-SC | Tampering | UI dependencies | mitigate | No installs. |
</threat_model>
<verification>Run only the existing walking-skeleton prerequisite case and the named identity/session packaged case.</verification>
<success_criteria>CMP-08 is owned once, its named test is RED before implementation and GREEN afterward, full identity copy behavior is proven, and this plan touches eight files.</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-11-SUMMARY.md` with RED/GREEN evidence, state matrix, identity/copy contracts, and exact empty/security/stopped copy.</output>
