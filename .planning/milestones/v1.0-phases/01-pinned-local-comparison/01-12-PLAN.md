---
phase: 01-pinned-local-comparison
plan: 12
type: tdd
wave: 12
depends_on: [01-11]
files_modified:
  - src/web/App.vue
  - src/web/styles.css
  - src/web/components/FileTree.vue
  - src/web/components/FileMetadataPane.vue
  - src/web/components/CopyButton.vue
  - src/web/components/InlineNotice.vue
  - tests/e2e/pinned-session.spec.ts
requirements: [DIFF-06]
autonomous: true
must_haves:
  truths:
    - "Selecting a supported file shows metadata-only availability; selecting every unsupported/unavailable record keeps its tree position and shows exact reason-specific explanation rather than a broken editor."
    - "The pane exposes safe and exact paths/bytes, statuses, counts, modes, availability, copy feedback, and retryable detail failure without broadening capability authority."
  artifacts:
    - path: src/web/components/FileMetadataPane.vue
      provides: "Read-only selected-file metadata and availability experience"
    - path: src/web/components/CopyButton.vue
      provides: "Precisely labeled exact-value copy feedback"
    - path: tests/e2e/pinned-session.spec.ts
      provides: "Named metadata/availability packaged behavior contract"
  key_links:
    - from: src/web/components/FileTree.vue
      to: src/web/components/FileMetadataPane.vue
      via: "opaque selected fileId drives validated detail request and pane state"
      pattern: "fileId"
---
<objective>
Implement the selected-file metadata and availability seam after identity/session behavior is stable.

Purpose: Deliver DIFF-06 per D-11–D-14 and D-17 with its named RED test before shared App.vue and styles edits.
Output: Metadata pane, exact copy/notice primitives, tree integration, reason-specific placeholders, and focused packaged proof.
</objective>
<execution_context>@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md
@.planning/phases/01-pinned-local-comparison/01-11-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify metadata and availability behavior</name>
  <read_first><file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file><file>src/contracts/api.ts</file><file>tests/e2e/pinned-session.spec.ts</file></read_first>
  <files>tests/e2e/pinned-session.spec.ts</files>
  <behavior>
    - "The named `metadata and availability states` case selects supported, binary, non-UTF-8, oversized, submodule, symlink, mode/type, and missing-object rows and asserts exact headings, reason, explanation, and stable tree selection."
    - "The case asserts effective/exact old/new path values and copy labels, counts, modes, retry success/failure, no spinner loop, no editor frame, and no broadened request field."
  </behavior>
  <action>Extend the packaged test first with exact named case `metadata and availability states`. Reach behavioral assertions using the existing session/tree fixture, prove the Plan 11 identity case remains GREEN as a prerequisite, then require RED for missing metadata/placeholder behavior. Do not defer these assertions to final acceptance.</action>
  <verify><automated><command expected="0" classification="prerequisite">npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "identity session and empty states"</command><command expected="nonzero" classification="behavioral-red">npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "metadata and availability states"</command></automated></verify>
  <acceptance_criteria>The named metadata test exists, reaches UI assertions, and fails only because the production pane and placeholders are absent.</acceptance_criteria>
  <done>The metadata/availability contract is RED before production changes.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN — render selected-file metadata and exact placeholders</name>
  <read_first><file>tests/e2e/pinned-session.spec.ts</file><file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file><file>src/web/App.vue</file><file>src/web/components/FileTree.vue</file></read_first>
  <files>src/web/App.vue, src/web/styles.css, src/web/components/FileTree.vue, src/web/components/FileMetadataPane.vue, src/web/components/CopyButton.vue, src/web/components/InlineNotice.vue</files>
  <action>Compose the opaque tree selection with FileMetadataPane. Its h2 uses the safe effective path; Paths, Changes, Modes, and Availability expose exact validated values and adjacent specifically labeled copy controls for ordinary, rename/copy, and non-UTF-8 byte identities. Supported text states metadata-only. For each exact unsupported/unavailable reason render the verified heading, machine reason, human explanation, and recovery text without editor chrome or blob fallback. Keep selected rows in place, show retryable detail failure inline under the same h2, retry only that opaque file capability, retain prior metadata when specified, announce copy/retry outcomes, and prevent stale responses from replacing newer selection. Do not add Monaco, content, comments, export, or arbitrary request authority.</action>
  <verify><automated><command expected="0">npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "metadata and availability states"</command></automated></verify>
  <acceptance_criteria>Every supported, unsupported, unavailable, exact-path, copy, mode/count, retry, stale-response, and no-broadened-request assertion passes.</acceptance_criteria>
  <done>DIFF-06 and the metadata/availability seam are GREEN.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-27 | Elevation | detail request | mitigate | Only launch-scoped opaque fileId reaches the existing strict route. |
| T-01-28 | Spoofing | path/reason display | mitigate | Validated exact values remain separate from safe display; explicit textual reasons. |
| T-01-SC | Tampering | UI dependencies | mitigate | No installs. |
</threat_model>
<verification>Run only the named identity prerequisite and metadata/availability packaged cases.</verification>
<success_criteria>DIFF-06 is owned once, its test transitions RED to GREEN before final acceptance, and the shared-file changes remain sequential after Plan 11.</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-12-SUMMARY.md` with RED/GREEN evidence, reason/copy matrix, stale-response and capability proof, and Phase 2 exclusion proof.</output>
