---
phase: 01-pinned-local-comparison
plan: 07
type: tdd
wave: 7
depends_on: [01-06]
files_modified:
  - src/git/objects.ts
  - src/git/availability.ts
  - src/git/inventory.ts
  - src/contracts/comparison.ts
  - tests/unit/availability.test.ts
  - tests/git/availability.test.ts
autonomous: true
requirements: [CMP-09]
must_haves:
  truths:
    - "Binary, non-UTF-8, oversized, submodule, symlink, missing/wrong objects, and unsupported mode/type entries remain in the pinned inventory with one precise machine reason."
    - "Exactly 1 MiB per blob side remains text-eligible and 1 MiB plus one byte is oversized before content allocation."
    - "Classification reads only raw object IDs from the frozen diff and never worktree files, ref:path expressions, filters, textconv, external diff, symlink targets, or repository code."
    - "Post-launch missing objects remain unavailable and never trigger a moving-ref or filesystem substitution."
  artifacts:
    - path: src/git/objects.ts
      provides: "Bounded immutable Git object metadata/read boundary"
      exports: [createObjectReader]
    - path: src/git/availability.ts
      provides: "Ordered content availability classifier"
      exports: [MAX_INLINE_TEXT_BYTES, classifyAvailability]
    - path: tests/git/availability.test.ts
      provides: "Real-object type/size/encoding/disappearance matrix"
  key_links:
    - from: src/git/inventory.ts
      to: src/git/availability.ts
      via: "raw diff modes/blob IDs are classified without path authority"
      pattern: "classifyAvailability"
    - from: src/git/availability.ts
      to: src/git/objects.ts
      via: "type and size are queried before bounded bytes"
      pattern: "createObjectReader"
---

<objective>
Add a precise immutable-object availability classification to every changed file in the working browser session.

Purpose: Deliver CMP-09 and D-18, including the resolved 1 MiB policy, while preventing unsupported content from disappearing, crashing, allocating unbounded bytes, or broadening authority.
Output: Bounded object reader, ordered classifier, strict availability schema, and focused pure/real-Git tests.
</objective>

## Phase Goal

**As a** developer working in a local Git repository, **I want to** choose an ordered base and head and open their pinned PR-style comparison in my browser, **so that** I can verify committed local changes without publishing branches or reading dirty worktree bytes.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>
<context>
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-RESEARCH.md
@.planning/phases/01-pinned-local-comparison/01-PATTERNS.md
@.planning/phases/01-pinned-local-comparison/SKELETON.md
@.planning/phases/01-pinned-local-comparison/01-06-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify immutable availability precedence and bounds</name>
  <read_first>
    <file>.planning/phases/01-pinned-local-comparison/01-RESEARCH.md</file>
    <file>.planning/phases/01-pinned-local-comparison/SKELETON.md</file>
    <file>src/git/inventory.ts</file>
    <file>src/contracts/comparison.ts</file>
    <file>tests/helpers/git-fixture.ts</file>
  </read_first>
  <files>tests/unit/availability.test.ts, tests/git/availability.test.ts</files>
  <behavior>
    - "Classifier precedence distinguishes submodule, missing/wrong object, oversized, binary, non-UTF-8, symlink/mode-or-type, and text deterministically."
    - "Object type and size are checked before content; 1_048_576 bytes is eligible and 1_048_577 is oversized without allocating content."
    - "Changing/deleting worktree files or advancing refs cannot alter classification; deleting the pinned object yields unavailable: missing-object."
  </behavior>
  <action>Write pure precedence/boundary cases and real object fixtures first. Include ordinary UTF-8, NUL binary, Git numstat binary marker, invalid UTF-8, exact-limit and limit-plus-one blobs, gitlink, symlink, unsupported mode/type, wrong object type, and missing object. Instrument object calls and Node filesystem APIs so content-before-size, oversized allocation, symlink following, worktree read, ref re-resolution, or repository-configured conversion fails. Assert the exact reason vocabulary synchronized with SKELETON.md and 01-UI-SPEC.md. Commit named behavioral RED tests first.</action>
  <verify><automated><command expected="nonzero" classification="behavioral-red">npm run test:unit -- tests/unit/availability.test.ts</command><command expected="nonzero" classification="behavioral-red">npm run test:git -- tests/git/availability.test.ts</command></automated></verify>
  <acceptance_criteria>Both suites reach named classifier assertions and fail only because object/classification behavior is absent.</acceptance_criteria>
  <done>The availability precedence and boundary contract is committed in RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN/REFACTOR — classify frozen blobs without fallback</name>
  <read_first>
    <file>tests/unit/availability.test.ts</file>
    <file>tests/git/availability.test.ts</file>
    <file>src/git/runner.ts</file>
    <file>src/git/inventory.ts</file>
    <file>src/contracts/comparison.ts</file>
  </read_first>
  <files>src/git/objects.ts, src/git/availability.ts, src/git/inventory.ts, src/contracts/comparison.ts</files>
  <action>Implement createObjectReader over cat-file machine protocols using only frozen raw-diff object IDs, NUL-safe commands/results, bounded diagnostics, and existing cancellation. Query object type and size before reading any bytes. Apply MAX_INLINE_TEXT_BYTES=1_048_576 per blob side, use NUL/Git-binary signals, then fatal TextDecoder UTF-8 validation, while classifying gitlinks, symlinks, wrong/missing objects, and unknown modes explicitly. Never use worktree paths, ref:path, filters, textconv, external diff, or executable repository configuration. Attach one strict discriminated availability result to every inventory record; preserve already-loaded pinned metadata when an object disappears. Refactor sequencing/reason construction only after identical tests are green.</action>
  <verify><automated><command expected="0">npm run test:unit -- tests/unit/availability.test.ts</command><command expected="0">npm run test:git -- tests/git/availability.test.ts</command></automated></verify>
  <acceptance_criteria>All exact reasons, precedence, boundaries, no-allocation, no-filesystem, and object-disappearance cases pass; every changed record remains visible with text/unsupported/unavailable availability.</acceptance_criteria>
  <done>Frozen-object availability classification is GREEN without fallback.</done>
</task>
</tasks>

## Artifacts this phase produces

- `src/git/objects.ts` — `ObjectReader`, `createObjectReader`, typed object metadata/read results.
- `src/git/availability.ts` — `MAX_INLINE_TEXT_BYTES`, `Availability`, `classifyAvailability`.
- `tests/unit/availability.test.ts` — pure precedence and exact-size boundary contract.
- `tests/git/availability.test.ts` — real-Git object, encoding, mode, and disappearance matrix.
- Updated `src/git/inventory.ts`, `src/contracts/comparison.ts` — per-record classifier wiring and strict availability union.

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-15 | Denial of service | blob reads | mitigate | Type/size before bounded allocation; explicit 1 MiB per-side policy. |
| T-01-16 | Elevation | repository content hooks | mitigate | Raw cat-file only; no filters, textconv, external diff, symlink traversal, or project execution. |
| T-01-17 | Tampering | vanished objects | mitigate | Explicit unavailable result; never substitute moving refs or filesystem bytes. |
| T-01-SC | Tampering | dependencies | mitigate | No new installs. |
</threat_model>

<verification>
Run only the focused availability tests. Record precedence, exact boundaries, call ordering, allocation/read spies, immutable-ref/worktree cases, and missing-object behavior. Do not run formatters, linters, or broad suites.
</verification>
<success_criteria>
- CMP-09 is delivered once in this plan with the exact supported/unsupported/unavailable taxonomy.
- Unsupported content stays visible and actionable without inline content or fallback.
- Object access remains immutable, bounded, cancellation-aware, and incapable of executing repository content.
</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-07-SUMMARY.md` with RED/GREEN evidence, artifacts/symbols, classifier matrix, size/allocation proof, and immutable-object proof.</output>
