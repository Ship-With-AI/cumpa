---
phase: 01-pinned-local-comparison
plan: 06
type: tdd
wave: 6
depends_on: [01-05]
files_modified:
  - src/domain/path-bytes.ts
  - src/git/raw-diff.ts
  - src/git/numstat.ts
  - src/git/inventory.ts
  - src/git/comparison.ts
  - src/contracts/comparison.ts
  - tests/unit/inventory-protocol.test.ts
  - tests/git/inventory.test.ts
autonomous: true
requirements: [CMP-04, CMP-05, CMP-07]
must_haves:
  truths:
    - "The pinned comparison includes additions, modifications, deletions, renames, copies, mode-only/type changes, exact old/new modes and blob IDs, and Git-derived counts when available."
    - "Spaces, Unicode, tabs, newlines, leading dashes, invalid UTF-8 bytes, and display-colliding names survive parsing and selection without path corruption."
    - "Rename/copy records retain exact old and new identities; safe display text and compact labels can never become API or Git authority."
    - "Raw and numstat records are parsed as independent NUL grammars and joined one-to-one by exact byte identity, rejecting malformed, duplicate, missing, or ambiguous joins."
  artifacts:
    - path: src/domain/path-bytes.ts
      provides: "Lossless encoded path identity and control-safe display"
      exports: [createExactPath, orderExactPaths]
    - path: src/git/raw-diff.ts
      provides: "Byte-oriented raw diff parser"
      exports: [parseRawDiff]
    - path: src/git/numstat.ts
      provides: "Independent numstat parser and exact join"
      exports: [parseNumstat, joinDiffStats]
    - path: src/git/inventory.ts
      provides: "Frozen changed-file inventory assembly"
      exports: [createChangedFileInventory]
  key_links:
    - from: src/git/inventory.ts
      to: src/git/comparison.ts
      via: "same frozen merge-base/head OIDs drive raw and numstat commands"
      pattern: "createChangedFileInventory"
    - from: src/domain/path-bytes.ts
      to: src/contracts/comparison.ts
      via: "base64url bytes and escaped display are serialized separately"
      pattern: "bytesBase64url"
---

<objective>
Deepen the working browser session with a complete, byte-exact native-Git changed-file inventory.

Purpose: Deliver CMP-04, CMP-05, and CMP-07 per D-11, D-12, and D-14 while preserving immutable endpoints and making unusual valid paths safe.
Output: Exact-path model, independent raw/numstat parsers, frozen inventory assembler, strict schemas, and pure/real-Git tests.
</objective>

## Phase Goal

**As a** developer working in a local Git repository, **I want to** choose an ordered base and head and open their pinned PR-style comparison in my browser, **so that** I can verify committed local changes without publishing branches or reading dirty worktree bytes.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>
<context>
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-RESEARCH.md
@.planning/phases/01-pinned-local-comparison/01-PATTERNS.md
@.planning/phases/01-pinned-local-comparison/01-05-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify byte-exact diff protocols and inventory joins</name>
  <read_first>
    <file>.planning/phases/01-pinned-local-comparison/01-CONTEXT.md</file>
    <file>.planning/phases/01-pinned-local-comparison/01-RESEARCH.md</file>
    <file>.planning/phases/01-pinned-local-comparison/01-PATTERNS.md</file>
    <file>src/git/runner.ts</file>
    <file>src/git/comparison.ts</file>
    <file>tests/helpers/git-fixture.ts</file>
  </read_first>
  <files>tests/unit/inventory-protocol.test.ts, tests/git/inventory.test.ts</files>
  <behavior>
    - "Pure byte fixtures parse ordinary and rename/copy raw -z and numstat -z records without decoding record boundaries or using shared line grammar."
    - "Real Git fixtures retain A/M/D/R/C, mode-only/type changes, modes, blob IDs, similarity, and numeric or unavailable counts."
    - "Unusual and visually colliding path bytes remain distinct through transport/display; malformed or non-bijective raw-numstat joins fail specifically."
  </behavior>
  <action>Write byte-level parser cases and disposable real-Git cases first. Include spaces, composed/decomposed Unicode, tab, newline, leading dash, invalid UTF-8 where the platform permits, rename/copy old→new pairs, copy-from-unchanged, chmod-only, binary numstat, and two byte-distinct names with the same escaped display. Assert command options include -z, no-abbrev, rename/copy detection, --no-ext-diff, --no-textconv, frozen merge-base/head OIDs, and final option separator. Instrument against shell use, text decoding before NUL framing, working-tree file reads, human brace notation, and display-as-identity. Commit named RED tests first.</action>
  <verify><automated><command expected="nonzero" classification="behavioral-red">npm run test:unit -- tests/unit/inventory-protocol.test.ts</command><command expected="nonzero" classification="behavioral-red">npm run test:git -- tests/git/inventory.test.ts</command></automated></verify>
  <acceptance_criteria>Both focused suites reach named protocol/inventory assertions and fail only because parsers/assembler are absent.</acceptance_criteria>
  <done>The byte-exact protocol and inventory contract is committed in RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN/REFACTOR — expose the frozen lossless inventory</name>
  <read_first>
    <file>tests/unit/inventory-protocol.test.ts</file>
    <file>tests/git/inventory.test.ts</file>
    <file>src/contracts/comparison.ts</file>
    <file>src/git/comparison.ts</file>
  </read_first>
  <files>src/domain/path-bytes.ts, src/git/raw-diff.ts, src/git/numstat.ts, src/git/inventory.ts, src/git/comparison.ts, src/contracts/comparison.ts</files>
  <action>Implement exact path values that retain bytes as base64url, optional strict UTF-8, and control-safe display without normalization. Parse raw -z and numstat -z directly from Buffer boundaries as distinct grammars; retain status/similarity, modes, old/new blob IDs, one or two exact paths, and numeric/unavailable counts. Join records by exact byte identity with stable duplicate detection rather than display strings or array-position guessing. Run both Git commands with identical rename/copy/no-filter options and the frozen merge-base/head OIDs through createGitRunner; never read worktree paths or execute repository diff/textconv configuration. Create stable opaque file IDs from process namespace plus exact record identity, and extend strict comparison schemas without accepting path/object authority from JSON. Unknown valid Git status/mode becomes a retained unsupported record for Plan 05 rather than a parser crash. Refactor only after identical tests are green.</action>
  <verify><automated><command expected="0">npm run test:unit -- tests/unit/inventory-protocol.test.ts</command><command expected="0">npm run test:git -- tests/git/inventory.test.ts</command></automated></verify>
  <acceptance_criteria>The complete status/mode/blob/count/path matrix is green, exact byte identities survive serialization, dirty or moving filesystem state cannot alter output, and malformed joins fail explicitly.</acceptance_criteria>
  <done>The complete frozen changed-file inventory is GREEN.</done>
</task>
</tasks>

## Artifacts this phase produces

- `src/domain/path-bytes.ts` — `ExactPath`, `createExactPath`, `orderExactPaths`, control-safe display helpers.
- `src/git/raw-diff.ts` — `RawDiffRecord`, `parseRawDiff`.
- `src/git/numstat.ts` — `NumstatRecord`, `parseNumstat`, `joinDiffStats`.
- `src/git/inventory.ts` — `createChangedFileInventory`, opaque file-ID construction, frozen inventory types.
- `tests/unit/inventory-protocol.test.ts` — malformed/valid byte protocol and join contracts.
- `tests/git/inventory.test.ts` — real-Git status, mode, blob, count, rename/copy, and unusual-path matrix.
- Updated `src/git/comparison.ts`, `src/contracts/comparison.ts` — inventory wiring and strict changed-file schemas.

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-11 | Tampering | Git diff invocation | mitigate | Discrete arguments, final option separator, frozen OIDs, no external diff/textconv. |
| T-01-12 | Spoofing | unusual paths | mitigate | NUL-first byte parsing, visible control escaping, opaque IDs distinct from display. |
| T-01-13 | Information disclosure | content authority | mitigate | Metadata/blob IDs from commits only; no worktree filesystem reads. |
| T-01-14 | Denial of service | subprocess output | mitigate | Existing streamed bounds and explicit malformed-record failure. |
| T-01-SC | Tampering | dependencies | mitigate | No new installs. |
</threat_model>

<verification>
Run only the focused protocol and inventory tests. Record command arrays, full status matrix, unusual path bytes and safe display, exact joins, moving-ref/dirty exclusion, and malformed-record behavior. Do not run formatters, linters, or broad suites.
</verification>
<success_criteria>
- CMP-04, CMP-05, and CMP-07 are delivered once in this plan.
- Native Git remains the sole source of status, mode, rename/copy, object, and count facts.
- No line parsing, shell command, display identity, filesystem fallback, or request path can corrupt authority.
</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-06-SUMMARY.md` with RED/GREEN evidence, artifacts/symbols, command arrays, byte/path matrix, status/count matrix, and immutability proof.</output>
