---
phase: 01-pinned-local-comparison
plan: 05
type: tdd
wave: 5
depends_on: [01-04]
files_modified:
  - src/git/runner.ts
  - src/git/repository.ts
  - src/git/comparison.ts
  - src/domain/errors.ts
  - src/cli/run.ts
  - src/cli/picker.ts
  - tests/helpers/git-fixture.ts
  - tests/git/comparison.test.ts
  - tests/cli/errors.test.ts
autonomous: true
requirements: [SEL-02, SEL-06, CMP-03]
must_haves:
  truths:
    - "Missing or unsupported Git, non-worktree/bare/empty repositories, and unavailable commits fail before server bind with the exact actionable terminal state."
    - "Equal commits remain truthful picker entries but cannot launch; base remains selected and focus returns to head."
    - "Unrelated histories, multiple best merge bases, and missing required objects are distinct failures and never silently choose, re-resolve, or substitute another identity."
    - "Recoverable endpoint failures preserve the opposite valid selection and return focus/search state to the failed role; fatal environment failures exit without browser/server activity."
  artifacts:
    - path: src/domain/errors.ts
      provides: "Typed pre-session failure taxonomy and recovery ownership"
      exports: [LaunchError]
    - path: src/git/comparison.ts
      provides: "Full-OID equality/object/exactly-one-merge-base state machine"
      exports: [createPinnedComparison]
    - path: tests/git/comparison.test.ts
      provides: "Real-DAG comparison failure matrix"
  key_links:
    - from: src/git/comparison.ts
      to: src/cli/run.ts
      via: "typed recoverability drives exit or picker restoration before bind"
      pattern: "LaunchError"
---

<objective>
Complete the terminal-owned validation and recovery state machine around the working selection-to-browser slice.

Purpose: Deliver SEL-02, SEL-06, and CMP-03 per D-05 and D-15 while protecting the immutable comparison from ambiguous Git history or missing objects.
Output: Capability probes, typed failures, exactly-one merge-base validation, picker recovery, and focused real-DAG/CLI tests.
</objective>

## Phase Goal

**As a** developer working in a local Git repository, **I want to** choose an ordered base and head and open their pinned PR-style comparison in my browser, **so that** I can verify committed local changes without publishing branches or reading dirty worktree bytes.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>
<context>
@.planning/REQUIREMENTS.md
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-RESEARCH.md
@.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md
@.planning/phases/01-pinned-local-comparison/01-04-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify every pre-session validation and recovery state</name>
  <read_first>
    <file>.planning/phases/01-pinned-local-comparison/01-CONTEXT.md</file>
    <file>.planning/phases/01-pinned-local-comparison/01-RESEARCH.md</file>
    <file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file>
    <file>tests/helpers/git-fixture.ts</file>
    <file>src/git/comparison.ts</file>
    <file>src/cli/picker.ts</file>
  </read_first>
  <files>tests/helpers/git-fixture.ts, tests/git/comparison.test.ts, tests/cli/errors.test.ts</files>
  <behavior>
    - "Repository/Git prerequisite failures use the exact D-15 UI-spec terminal matrix and cause zero bind/open calls."
    - "Equal OIDs preserve base and restore head selection; zero or multiple merge bases are distinct recoverable failures."
    - "Object disappearance after resolution fails on the pinned object and never re-resolves a moving ref or reads a worktree file."
  </behavior>
  <action>Extend the deterministic fixture builder with unborn, bare, independent-root, criss-cross two-best-base, equal-commit, and removed-object cases. Prove the criss-cross fixture independently by native Git returning two merge-base --all records before relying on application assertions. Write exact-copy/recovery tests from the 01-UI-SPEC.md D-15 matrix, including retained selections and focus targets. Assert Git 2.43.0 plus every required machine protocol is positively probed and that unsupported capability has one actionable prerequisite error, never a weaker parser fallback. Spy on bind/open and object/filesystem boundaries. Commit named behavioral RED tests first.</action>
  <verify><automated><command expected="0" classification="fixture-proof">npm run test:git -- tests/git/comparison.test.ts --grep "fixture has two best merge bases"</command><command expected="nonzero" classification="behavioral-red">npm run test:git -- tests/git/comparison.test.ts --grep "comparison validation matrix"</command><command expected="nonzero" classification="behavioral-red">npm run test:unit -- tests/cli/errors.test.ts</command></automated></verify>
  <acceptance_criteria>The criss-cross prerequisite independently proves two bases, and application tests reach named validation/recovery assertions before failing on missing implementation.</acceptance_criteria>
  <done>The full pre-session validation and recovery contract is committed in RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN/REFACTOR — enforce unambiguous pinned comparison launch</name>
  <read_first>
    <file>tests/git/comparison.test.ts</file>
    <file>tests/cli/errors.test.ts</file>
    <file>src/git/runner.ts</file>
    <file>src/git/repository.ts</file>
    <file>src/git/comparison.ts</file>
    <file>src/cli/run.ts</file>
  </read_first>
  <files>src/git/runner.ts, src/git/repository.ts, src/git/comparison.ts, src/domain/errors.ts, src/cli/run.ts, src/cli/picker.ts</files>
  <action>Implement positive startup capability checks and typed mapping for Git missing, unsupported protocol, not-in-worktree, bare, and empty/unborn states. Resolve selected endpoints as commit objects, cumpa full OIDs, and on equality preserve base while clearing/restoring head per the exact UI contract. Invoke merge-base --all with discrete OID arguments, parse all complete object-format-aware OIDs, distinguish zero from more than one, and require the sole base plus both endpoints to remain readable commit objects before session creation. Never call merge-base without --all, choose one ambiguous base, reuse labels/refs after resolution, use ref:path expressions, or substitute worktree bytes. Map typed failures in runCli to exact fatal/return behavior and terminal copy; no browser recovery screen is used pre-session. Refactor only after identical focused suites pass.</action>
  <verify><automated><command expected="0">npm run test:git -- tests/git/comparison.test.ts</command><command expected="0">npm run test:unit -- tests/cli/errors.test.ts</command></automated></verify>
  <acceptance_criteria>All prerequisite, equality, unrelated, multiple-base, and missing-object cases produce their exact terminal behavior with zero accidental bind/open and no identity substitution; valid comparisons still launch.</acceptance_criteria>
  <done>Unambiguous comparison validation and exact recovery behavior are GREEN.</done>
</task>
</tasks>

## Artifacts this phase produces

- `src/domain/errors.ts` — `LaunchError` discriminants, recovery metadata, and terminal mapping.
- `tests/git/comparison.test.ts` — repository/object/merge-base real-Git matrix.
- `tests/cli/errors.test.ts` — exact pre-session copy, retained-selection, focus, and zero-bind/open assertions.
- Updated `src/git/runner.ts` — capability probes and stable failure mapping.
- Updated `src/git/repository.ts` — complete worktree/repository validation.
- Updated `src/git/comparison.ts` — equality, object verification, and sole-merge-base invariants.
- Updated `src/cli/run.ts`, `src/cli/picker.ts`, `tests/helpers/git-fixture.ts` — recovery loop and DAG fixtures.

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-08 | Tampering | comparison identity | mitigate | Resolve full commits once, verify objects, require exactly one merge-base --all result. |
| T-01-09 | Elevation | Git arguments | mitigate | Opaque validated OIDs passed as argument-array values; no shell or ref:path expressions. |
| T-01-10 | Repudiation | terminal failures | mitigate | Typed specific diagnostics distinguish environment, endpoint, graph, and object failures. |
| T-01-SC | Tampering | dependencies | mitigate | Existing approved lockfile only. |
</threat_model>

<verification>
Run only the focused Git comparison and CLI error tests. Record independent criss-cross proof, exact error/recovery matrix, zero bind/open assertions, and no-ref/filesystem fallback instrumentation. Do not run formatters, linters, or broad suites.
</verification>
<success_criteria>
- SEL-02, SEL-06, and CMP-03 are delivered once in this plan.
- Every ambiguous or unavailable pre-session identity fails specifically before server creation.
- Valid prior selection state is preserved only where the UI contract says recovery is possible.
</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-05-SUMMARY.md` with RED/GREEN evidence, capability matrix, criss-cross proof, exact terminal states, artifacts/symbols, and no-substitution proof.</output>
