---
phase: 01-pinned-local-comparison
plan: 02
type: tdd
wave: 2
depends_on: [01-01]
files_modified:
  - src/cli/run.ts
  - src/git/runner.ts
  - src/git/repository.ts
  - src/git/comparison.ts
  - src/contracts/comparison.ts
  - tests/helpers/git-fixture.ts
  - tests/git/pinned-comparison.test.ts
  - vitest.config.ts
requirements: [SEL-01, SEL-05, CMP-01, CMP-02, CMP-06]
autonomous: true
must_haves:
  truths:
    - "Launching from a nested directory resolves the canonical non-bare repository and preserves explicit ordered base then head selection."
    - "The comparison freezes full base, head, and exactly one merge-base OID before later server startup, and committed facts exclude all dirty-only bytes."
  artifacts:
    - path: src/git/runner.ts
      provides: "Bounded shell-free argument-array Git boundary"
      exports: [createGitRunner]
    - path: src/git/comparison.ts
      provides: "Pinned full-OID comparison constructor"
      exports: [createPinnedComparison]
    - path: tests/git/pinned-comparison.test.ts
      provides: "Focused real-Git authority proof"
  key_links:
    - from: src/cli/run.ts
      to: src/git/comparison.ts
      via: "ordered endpoint selection is resolved and frozen before returning the launch descriptor"
      pattern: "createPinnedComparison"
---
<objective>
Add the native-Git repository and pinned-comparison core beneath the generated package.

Purpose: Deliver SEL-01, SEL-05, CMP-01, CMP-02, and CMP-06 per D-01, D-08–D-10 while isolating Git authority from lifecycle/browser work.
Output: Real-Git fixture, bounded runner, repository discovery, strict comparison contract, and focused immutable-object tests.
</objective>
<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-RESEARCH.md
@.planning/phases/01-pinned-local-comparison/SKELETON.md
@.planning/phases/01-pinned-local-comparison/01-01-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify native-Git pinned comparison authority</name>
  <read_first><file>.planning/phases/01-pinned-local-comparison/01-CONTEXT.md</file><file>.planning/phases/01-pinned-local-comparison/01-RESEARCH.md</file><file>src/cli/run.ts</file></read_first>
  <files>vitest.config.ts, tests/helpers/git-fixture.ts, tests/git/pinned-comparison.test.ts</files>
  <behavior>
    - "A nested cwd resolves its canonical non-bare worktree root and ordered explicit base/head labels."
    - "Full base/head commits and the sole merge base match independent Git queries."
    - "Committed metadata is stable after refs move and excludes staged, unstaged, and untracked-only paths."
  </behavior>
  <action>Configure the focused Vitest unit/Git categories on the Plan 01 package scripts. Build disposable real-Git fixtures with local config, disabled hooks, no network, and explicit DAGs. Write named tests before implementation; the checked-in verify:prerequisites command owns fixture setup and Git availability, and RED must reach missing createPinnedComparison behavior.</action>
  <verify><automated><command expected="0" classification="prerequisite">npm run verify:prerequisites</command><command expected="nonzero" classification="behavioral-red">npm run test:git -- tests/git/pinned-comparison.test.ts</command></automated></verify>
  <acceptance_criteria>The focused suite reaches domain assertions and fails because the native-Git comparison core is absent.</acceptance_criteria>
  <done>The immutable Git authority contract is RED.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN — resolve and freeze committed comparison identities</name>
  <read_first><file>tests/git/pinned-comparison.test.ts</file><file>tests/helpers/git-fixture.ts</file><file>src/cli/run.ts</file></read_first>
  <files>src/cli/run.ts, src/git/runner.ts, src/git/repository.ts, src/git/comparison.ts, src/contracts/comparison.ts</files>
  <action>Implement createGitRunner with spawn('git', args), shell false, Buffer stdout, bounded stdout/stderr, AbortSignal and timeout; never interpolate command strings or enable repository execution hooks. Discover the canonical non-bare repository root from cwd. Preserve explicit base then head input, resolve each once to a full commit OID, require exactly one merge-base --all result, verify required objects, derive the initial committed fact only from merge-base/head object identities, freeze the DTO with strict Zod schemas, and never read worktree file bytes or re-resolve moving refs.</action>
  <verify><automated><command expected="0">npm run test:git -- tests/git/pinned-comparison.test.ts</command></automated></verify>
  <acceptance_criteria>Independent Git results equal the frozen identities and fact; moving refs and dirty-only bytes cannot change the result.</acceptance_criteria>
  <done>The native-Git walking-skeleton core is GREEN.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-02 | Tampering / Elevation | Git runner | mitigate | Argument arrays, shell false, bounded bytes, cancellation, and repository-code suppression. |
| T-01-03 | Information disclosure | comparison | mitigate | Frozen commit/blob authority only; no worktree-byte or moving-ref fallback. |
| T-01-SC | Tampering | dependencies | mitigate | Reuse Plan 01 approved lockfile; no installs. |
</threat_model>
<verification>Run only the focused real-Git fixture and pinned-comparison tests named above.</verification>
<success_criteria>All five owned requirements are observable at the Git/domain seam through real repositories, within seven modified files.</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-02-SUMMARY.md` with RED/GREEN evidence, command arrays, identities, dirty/moving-ref exclusion, and exported symbols.</output>
