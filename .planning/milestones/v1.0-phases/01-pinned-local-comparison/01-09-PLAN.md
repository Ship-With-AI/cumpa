---
phase: 01-pinned-local-comparison
plan: 09
type: tdd
wave: 9
depends_on: [01-08]
files_modified:
  - src/domain/file-tree.ts
  - src/web/model/file-tree.ts
  - tests/unit/file-tree.test.ts
requirements: []
autonomous: true
must_haves:
  truths:
    - "Immutable changed records project into deterministic directory/file rows without changing opaque identity or exact path bytes."
    - "Compaction, visible-row navigation, and first-leaf selection are pure and testable before browser composition."
  artifacts:
    - path: src/domain/file-tree.ts
      provides: "Deterministic hierarchy, ordering, and compaction projection"
      exports: [buildFileTree]
    - path: src/web/model/file-tree.ts
      provides: "Identity-preserving visible-row navigation model"
      exports: [createFileTreeModel]
    - path: tests/unit/file-tree.test.ts
      provides: "Projection and navigation state contract"
  key_links:
    - from: src/web/model/file-tree.ts
      to: src/domain/file-tree.ts
      via: "visible rows retain the projection's opaque IDs and exact paths"
      pattern: "fileId"
---
<objective>
Extract the pure file-tree projection and navigation model before rendering it.

Purpose: Keep the DIFF-01 browser slice below the preferred file range while giving its Vue task a stable, tested identity model. This supporting unit adds no requirement ownership.
Output: RED/GREEN unit contract for deterministic hierarchy, compaction, identity, and navigation transitions.
</objective>
<execution_context>@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md</execution_context>
<context>@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md
@.planning/phases/01-pinned-local-comparison/01-08-SUMMARY.md</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify deterministic tree projection and navigation</name>
  <read_first><file>src/contracts/api.ts</file><file>src/domain/path-bytes.ts</file><file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file></read_first>
  <files>tests/unit/file-tree.test.ts</files>
  <behavior>
    - "Rows sort by effective exact path bytes with old-path tie break and compact only single-directory/no-file chains."
    - "Duplicate/display-colliding paths retain distinct fileId and exact old/new identity."
    - "First leaf, expand/collapse, Arrow/Home/End, and directory focus transitions never replace selection with a directory."
  </behavior>
  <action>Write table-driven tests first for additions, deletions, rename/copy, mode-only, unusual bytes, collisions, deep directories, empty tree, and navigation transitions. RED must reach missing model behavior.</action>
  <verify><automated><command expected="nonzero" classification="behavioral-red">npm run test:unit -- tests/unit/file-tree.test.ts</command></automated></verify>
  <acceptance_criteria>The suite fails at named projection/navigation assertions rather than fixture or import setup.</acceptance_criteria>
  <done>The pure tree contract is RED.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the identity-preserving tree model</name>
  <read_first><file>tests/unit/file-tree.test.ts</file><file>src/contracts/api.ts</file><file>src/domain/path-bytes.ts</file></read_first>
  <files>src/domain/file-tree.ts, src/web/model/file-tree.ts</files>
  <action>Implement a pure deterministic projection using effective new path for additions/renames/copies and old path for deletions, bytewise ordering, stable old-path tie break, directory-before-descendant order, and compaction only for one directory child with no file child. Preserve fileId and exact old/new path data separately from display. Implement visible-row and roving-focus transitions without DOM or request authority.</action>
  <verify><automated><command expected="0">npm run test:unit -- tests/unit/file-tree.test.ts</command></automated></verify>
  <acceptance_criteria>All ordering, compaction, collision, identity, empty, and navigation transitions pass deterministically.</acceptance_criteria>
  <done>The tree model is GREEN and ready for the browser slice.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-22 | Spoofing | path projection | mitigate | Exact bytes and opaque IDs remain distinct from control-safe display. |
| T-01-SC | Tampering | dependencies | mitigate | No installs. |
</threat_model>
<verification>Run only tests/unit/file-tree.test.ts.</verification>
<success_criteria>The supporting model is proven in three files and introduces no second execution path.</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-09-SUMMARY.md` with RED/GREEN evidence, fixtures, symbols, ordering, compaction, and collision proof.</output>
