---
phase: 02-move-the-implementation-to-supabase
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PAY-03, PAY-04, REC-01, REC-02]
must_haves:
  truths:
    - "Only the official Supabase CLI/Auth clients and Stripe SDK are authorized for the Supabase hosted boundary (D-01)."
    - "No package is installed until its exact registry identity, source repository, lifecycle scripts, and release provenance are human-approved."
    - "Hosted dependencies remain outside the published Cumpa runtime; OAuth, privileged Supabase access, and Stripe secrets never enter package assets (D-03, PAY-04)."
  artifacts: []
  key_links:
    - from: ".planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md"
      to: "02-02 and 02-03 dependency installation tasks"
      via: "Package Legitimacy Audit approval of exact versions"
      pattern: "supabase@2.114.0|@supabase/supabase-js@2.112.3|@supabase/ssr@0.12.4|stripe@22.5.0"
  prohibitions:
    - "HIGH: Do not approve a similarly named package, unpinned range, fork, or package with unexplained install lifecycle code."
    - "HIGH: Do not add a privileged SDK, OAuth token, Stripe secret, or Supabase service credential to the root Cumpa runtime bundle."
---

<objective>
Approve the exact Supabase and Stripe package provenance required by the migration before any install or Edge Function import.

Purpose: Close the supply-chain gate identified by research without changing product or dependency files.
Output: Human authorization for the exact official packages used by Plans 02-02 and 02-03, per D-01 and D-15.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md

<interfaces>
Exact candidates from the research Package Legitimacy Audit:
- `supabase@2.114.0` — project-local CLI used for database-only local work and dev/prod deployment.
- `@supabase/supabase-js@2.112.3` — hosted Edge Function Auth/database client only.
- `@supabase/ssr@0.12.4` — hosted cookie-aware PKCE OAuth adapter only.
- `stripe@22.5.0` — hosted Edge Function signature verification and Checkout creation; already approved in Phase 01, re-check before changing its import/runtime boundary.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve exact hosted package provenance</name>
  <files>None — this gate must not change manifests, lockfiles, imports, or application source.</files>
  <read_first>
    - .planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md — Standard Stack and Package Legitimacy Audit
    - .planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-01-SUMMARY.md — prior exact Stripe approval boundary
    - package.json — published-package file boundary and current dependency graph
    - services/support/package.json — existing Stripe version and legacy hosted-only dependency boundary
  </read_first>
  <action>Gather and present the exact registry metadata described below without installing or changing any file. Approval is limited to the official identities and hosted/tooling locations specified by D-01 and D-03; a mismatch blocks all dependent work.</action>
  <what-built>No code is built. The executor gathers registry metadata for the four exact candidates, including name, version, repository, dist integrity/signatures when exposed, maintainers, publish recency, and lifecycle scripts.</what-built>
  <how-to-verify>
    1. Run `npm view supabase@2.114.0 name version repository maintainers dist.integrity dist.signatures scripts --json`.
    2. Run `npm view @supabase/supabase-js@2.112.3 name version repository maintainers dist.integrity dist.signatures scripts --json`.
    3. Run `npm view @supabase/ssr@0.12.4 name version repository maintainers dist.integrity dist.signatures scripts --json`.
    4. Run `npm view stripe@22.5.0 name version repository maintainers dist.integrity dist.signatures scripts --json`.
    5. Record all four outputs and four explicit expected-versus-returned name/version/repository/lifecycle comparisons in the SUMMARY; confirm official Supabase/Stripe repositories and explain any lifecycle script before approval.
    6. Confirm every returned version exactly matches the research audit; do not approve ranges or substitutes.
    7. Approve only hosted/tooling use: CLI as project development/CI tooling and the three runtime libraries only inside `supabase/functions/**`.
  </how-to-verify>
  <acceptance_criteria>
    - Human explicitly approves all four exact identities and their restricted locations.
    - Any mismatch, unexplained lifecycle script, or unresolved provenance concern blocks Plans 02-02 and 02-03.
    - No repository file changes during this checkpoint.
  </acceptance_criteria>
  <verify>
    <automated>npm view supabase@2.114.0 name version repository maintainers dist.integrity dist.signatures scripts --json &amp;&amp; npm view @supabase/supabase-js@2.112.3 name version repository maintainers dist.integrity dist.signatures scripts --json &amp;&amp; npm view @supabase/ssr@0.12.4 name version repository maintainers dist.integrity dist.signatures scripts --json &amp;&amp; npm view stripe@22.5.0 name version repository maintainers dist.integrity dist.signatures scripts --json</automated>
    <human-check>Compare and record all four returned identities, exact versions, repositories, and lifecycle scripts against the audit and official npmjs.com package pages; type `approved` only if every comparison matches.</human-check>
  </verify>
  <done>Exact official package provenance is approved; otherwise execution remains blocked and no installation occurs.</done>
  <resume-signal>Type `approved` or identify the package/provenance mismatch.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

This gate creates no repository artifact or symbol. Its SUMMARY records exact approved package identities and restricted use locations for dependent plans.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry → repository dependency graph | Registry metadata and downloaded packages become executable tooling or hosted runtime code. |
| Hosted dependency graph → published Cumpa package | Hosted-only libraries and credentials must not cross into distributed local runtime assets. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-01-SC | Tampering | HIGH | npm package selection | mitigate | Human checks exact package/version, official repository, integrity metadata, maintainers, and lifecycle scripts before installation. |
| T-02-01-SP | Spoofing | HIGH | package identity | mitigate | Reject similarly named packages, forks, aliases, tags, and unpinned ranges. |
| T-02-01-ID | Information Disclosure | HIGH | published package boundary | mitigate | Approval restricts Auth/Stripe clients to hosted functions and CLI to development/CI; package-safety proof remains mandatory in 02-06, 02-09, and 02-11. |

Execution MUST stop if any HIGH threat remains unresolved. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
The registry metadata command returns all four exact identities, then the human approval gate records an explicit decision. No formatter, linter, build, source test, install, or manifest mutation runs.
</verification>

<success_criteria>
- D-01 tooling/runtime choices have explicit provenance authorization.
- D-03 and PAY-04 hosted-only boundaries are part of that authorization.
- Plans 02-02 and 02-03 have an unambiguous exact-version gate.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-01-SUMMARY.md` recording the exact approved registry identities and human decision.
</output>
