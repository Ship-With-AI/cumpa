---
phase: 02-move-the-implementation-to-supabase
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - .github/workflows/deploy-supabase-production.yml
  - scripts/verify-supabase-support.mjs
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/support-recovery.spec.ts
  - tests/e2e/support-restore.spec.ts
  - docs/support-service-operations.md
  - supabase/functions/support-flow/index.ts
  - supabase/functions/tests/support-flow.test.ts
autonomous: true
requirements: [PAY-03, PAY-04, REC-01, REC-02]
must_haves:
  truths:
    - "Every push to protected main, with no path filter or manual-dispatch entrypoint, runs one serialized production workflow (D-15)."
    - "A credential-free repository-gates job completes before the production-environment job can access protected inputs."
    - "The protected job compares the SHA-256 of the complete secret project ref with approved lineage immediately before every hosted mutation, including custom-domain reverify and activation, and applies schema before functions."
    - "The sole public origin is a strict ref-free Supabase custom domain; Auth, OAuth callback, support-api, support-flow, and stripe-webhook URLs share it (D-20)."
    - "No deployment/provider secret or complete project ref is accepted from a local process, repository file, package, log, evidence record, or chat (D-13, D-14, D-18)."
    - "Workflow artifacts are immutable and redacted, carrying run/commit/version/order/fingerprint/domain-route facts but no protected value."
  artifacts:
    - path: ".github/workflows/deploy-supabase-production.yml"
      provides: "The sole push-triggered Supabase production deployment path"
    - path: "scripts/verify-supabase-support.mjs"
      provides: "CI-only target guards, deployment/promotion modes, redacted evidence, and offline evidence validation"
    - path: "tests/e2e/support-payment.spec.ts"
      provides: "Workflow, hostile executor, evidence, ordering, and no-local-secret regression contract"
    - path: "supabase/functions/support-flow/index.ts"
      provides: "Custom-origin browser Auth client and HTML completion/invalid-state responses"
  key_links:
    - from: ".github/workflows/deploy-supabase-production.yml:repository-gates"
      to: ".github/workflows/deploy-supabase-production.yml:deploy-production"
      via: "needs plus production environment binding only on the second job"
      pattern: "needs: repository-gates"
    - from: "secrets.SUPABASE_PROJECT_REF"
      to: "vars.APPROVED_SUPABASE_PROJECT_REF_SHA256"
      via: "in-memory SHA-256 equality immediately before every mutating verifier operation"
      pattern: "APPROVED_SUPABASE_PROJECT_REF_SHA256"
    - from: "push to main"
      to: "schema, provider configuration, functions, evidence artifact"
      via: "one concurrency-serialized workflow with schema-before-functions order"
      pattern: "supabase-production"
    - from: "vars.SUPPORT_PUBLIC_ORIGIN"
      to: "Supabase Auth and Edge Functions"
      via: "strict custom-domain URL derivation plus guarded reverify/activate and route probes"
      pattern: "domains (reverify|activate)"
    - from: "SUPPORT_PUBLIC_ORIGIN"
      to: "support-flow browser Auth client"
      via: "validated custom origin for authorize/callback/function URLs while service-role RPCs retain SUPABASE_URL"
      pattern: "createServerClient.*SUPPORT_PUBLIC_ORIGIN"
  prohibitions:
    - "HIGH: No workflow_dispatch, path filter, local deployment mode, local deployment/provider secret loader, raw ref output, or suffix-based target authorization."
    - "HIGH: No hosted mutation in repository-gates and no production environment, environment secret, or environment variable on that job."
    - "No second hosted project, Supabase Git integration, function-before-schema deployment, destructive live matrix, or deployment fallback."
---

<objective>
Replace the uncommitted local-secret deployment path with the earliest GitHub Actions-first production workflow slice.

Purpose: Make the first and every later hosted deployment an automatic, protected, auditable main-push run while preserving ordinary local disablement (D-13 through D-18).
Output: Workflow, CI-only hostile/deployment verifier contract, custom-origin support-flow wiring, focused regression tests, and updated operations boundary.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-06-SUMMARY.md
@scripts/verify-supabase-support.mjs
@tests/e2e/support-payment.spec.ts
@tests/e2e/support-recovery.spec.ts
@tests/e2e/support-restore.spec.ts
@docs/support-service-operations.md
@supabase/functions/support-flow/index.ts
@supabase/functions/tests/support-flow.test.ts

<interfaces>
- Workflow event: `push.branches: [main]`; omit `paths`, `paths-ignore`, and `workflow_dispatch`.
- Workflow permissions: top-level `contents: read`; add no write permission, OIDC permission, pull-request permission, or token persistence unless an implemented command demonstrably requires it.
- Concurrency: one stable production group with `cancel-in-progress: false`, so deployments serialize and an in-flight production mutation is never cancelled.
- `repository-gates` has no `environment` and no reference to `secrets.*` or protected provider variables.
- `deploy-production` has `needs: repository-gates` and `environment: production`; only this job reads the protected production environment.
- Protected secrets: `SUPABASE_ACCESS_TOKEN`, `SUPABASE_PROJECT_REF`, `SUPABASE_DB_PASSWORD`, `SUPABASE_GITHUB_CLIENT_SECRET`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`.
- Protected variables: `APPROVED_SUPABASE_PROJECT_REF_SHA256`, `SUPPORT_PROVIDER_MODE`, `SUPPORT_PUBLIC_ORIGIN`, `SUPABASE_SITE_URL`, `SUPABASE_REDIRECT_URL`, `SUPABASE_GITHUB_CLIENT_ID`, `SUPABASE_GITHUB_CALLBACK_URL`, `STRIPE_PRICE_ID`, `STRIPE_WEBHOOK_ENDPOINT_ID`, `STRIPE_WEBHOOK_URL`.
- `SUPPORT_PUBLIC_ORIGIN` is exactly one HTTPS origin whose hostname is neither the default Supabase project hostname nor any hostname containing the complete ref; all Auth/Function/provider URLs are derived from its documented paths.
- The verifier rejects deployment outside `GITHUB_ACTIONS=true`, rejects missing/malformed protected inputs, and never writes a linked ref file that can survive the run.
- Mutation order is guarded custom-domain reverify; guarded custom-domain activate when not already active; guarded database migration; guarded Auth/provider configuration; guarded Edge Function secret update; guarded `support-api`; guarded `support-flow`; guarded `stripe-webhook`; then read-only route/smoke/evidence.
- Each guard hashes the complete in-memory secret ref and compares it with `APPROVED_SUPABASE_PROJECT_REF_SHA256` in the same verifier process immediately before that one mutation.
- Official routing contract: `https://supabase.com/docs/guides/platform/custom-domains`, `https://supabase.com/docs/guides/functions/limits`, and `https://supabase.com/docs/reference/cli/supabase-domains`.
- Repository-gates toolchain is exact and ordered: `actions/setup-node@v4` with `node-version: 24`; `denoland/setup-deno@v2` with `deno-version: v2.7.14`; `npm ci`; `npx vitest run`; `npx playwright test --config=tests`; `deno test --allow-env --config supabase/functions/deno.json supabase/functions/tests`; then `npx supabase@2.114.0 db start` before reset/test/migration-list/lint.
- Database gate order is `db start`, `db reset --local --no-seed`, `test db`, `migration list --local`, `db lint --local`; the workflow verifier rejects a missing/wrong Node, Deno, or Supabase pin, missing `npm ci`, missing exact suite command, or reordered database step.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace local deployment behavior with a CI-only guarded deployment contract</name>
  <files>scripts/verify-supabase-support.mjs, tests/e2e/support-payment.spec.ts, tests/e2e/support-recovery.spec.ts, tests/e2e/support-restore.spec.ts</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md` D-13 through D-20
    - `.planning/phases/02-move-the-implementation-to-supabase/02-06-SUMMARY.md`
    - `scripts/verify-supabase-support.mjs`
    - `tests/e2e/support-payment.spec.ts`
    - `tests/e2e/support-recovery.spec.ts`
    - `tests/e2e/support-restore.spec.ts`
  </read_first>
  <behavior>
    - Local invocation cannot load deployment/provider inputs or execute a hosted mutation.
    - CI test-mode and live-mode inputs are complete sets; absent, mixed, malformed, or unexpected inputs fail before mutation, and an approved tracked acceptance manifest is the only test-mode cleanup authorization.
- `--verify-workflow WORKFLOW [--require-environment NAME] [--expected-mode MODE] [--require-custom-domain] [--require-release-artifact]` owns exactly the workflow trigger/job/environment/guard/order checks consumed by 02-07, 02-08, and 02-16.
    - `--check-run-evidence EVIDENCE --expected-mode MODE --require-immutable-run --require-zero-authority` owns first-deployment and cleanup-run validation; it accepts only the exact optional flags later consumed: `--require-custom-domain-routes`, `--acceptance PATH`, and `--require-exact-cleanup`.
    - `--check-acceptance-evidence EVIDENCE --deployment PATH --require-hostile-matrix --require-fixture-manifest --require-immutable-run` owns acceptance lineage; it also accepts the exact later gate `--require-approved`.
    - `--check-promotion-evidence EVIDENCE` owns the exact optional arguments consumed by 02-10/02-11: `--acceptance PATH`, `--require-approved`, `--require-cleanup-run`, `--require-live-run`, `--require-one-fingerprint`, `--require-exact-cleanup`, `--require-zero-authority`, `--require-zero-after-cleanup`, `--require-live-smoke`, `--non-destructive`, and `--require-immutable-runs`.
    - Every owned CLI rejects an unknown/duplicate/conflicting option, missing value/field, wrong evidence version/kind, digest/fingerprint/custom-origin mismatch, non-immutable lineage, and prohibited raw/protected/default-origin content; focused tests exercise each failure class before any later plan invokes it.
    - The public-origin parser rejects default/ref-bearing Supabase hosts and derives only `/auth/v1/callback` plus `/functions/v1/support-api`, `/support-flow`, and `/stripe-webhook` routes from the custom origin.
    - Full-ref SHA-256 mismatch fails immediately before every mutation, including each `domains reverify`/`domains activate` attempt; suffixes are display-only.
    - Route evidence records expected status/content-type signatures for Auth settings, invalid-input support-api, invalid-state support-flow HTML, and invalid-signature stripe-webhook while before/after authority counts remain unchanged.
    - Redacted records expose only GitHub run ID/URL, commit, mode, approved fingerprint, display suffix, custom public origin, domain activation state, route outcomes, migration/function versions, ordering, counts/hashed handles, artifact digests, and pass/fail outcomes.
    - The acceptance marker is a versioned, redacted `acceptance_marker.status = \"interactive-matrix-complete\"` record committed by 02-09; only an ordinary main push containing that marker may trigger the prelaunch hostile executor.
    - The CI-only hostile executor owns exactly nine stable case IDs: `wrong-signature`, `wrong-product`, `wrong-amount`, `wrong-currency`, `wrong-binding`, `expired-intent`, `reused-intent`, `sequential-replay`, and `concurrent-replay-settlement`.
    - Each case records isolated hashed fixture handles, explicit before/after invariants for all affected authority rows, expected/actual result, and a full-ref fingerprint guard immediately before every case-owned database, provider, or hosted mutation.
    - After all nine cases the executor snapshots exactly `auth.users`, `support_private.support_intents`, `support_private.supporters`, `support_private.checkout_sessions`, `support_private.stripe_events`, and `support_private.installation_bindings` into sorted count/hashed-handle rows covering browser and driver fixtures.
    - The hostile executor is reachable only inside the protected prelaunch workflow driver; deployed Edge Functions expose no test route, control header, privileged query, or acceptance backdoor.
  </behavior>
  <action>Delete the current `--deploy`/`--prepare-acceptance` local process-environment path instead of wrapping it. Rework the verifier as the single workflow-owned deployment/promotion and acceptance-driver implementation plus offline evidence checker. Before implementation, add focused failure tests in the three named E2E files for every exact `--verify-workflow`, `--check-run-evidence`, `--check-acceptance-evidence`, and `--check-promotion-evidence` interface/option enumerated in `<behavior>`; prove missing values, unknown/duplicate/conflicting flags, schema-kind/version errors, digest/fingerprint/origin mismatches, incomplete hostile/fixture rows, and raw/protected/default-origin data all fail closed. Implement those exact parsers, versioned evidence schemas, serializers, and cross-record invariants in this task so 02-08 through 02-10 invoke only already-owned commands. Implement the acceptance-marker-triggered prelaunch executor now, before 02-09 consumes it: require the exact versioned redacted `interactive-matrix-complete` marker in the pushed commit, reject it outside `GITHUB_ACTIONS=true`, the protected production job, and `prelaunch-test`, and execute exactly the nine stable case IDs listed in `<behavior>`. Give every case isolated non-reversible fixture handles and explicit pre/post assertions; invoke the reusable complete-ref SHA-256 guard immediately before every case-owned database, provider, or hosted mutation; record immutable expected/actual results; then snapshot the exact six-table manifest listed in `<behavior>` with sorted SHA-256 handles and counts for every browser/driver row. Test missing/duplicate/unknown case IDs, cross-case fixture reuse, missing invariants, a mutation without its immediately preceding guard, incomplete/wrong-table manifests, wrong marker/run/mode, and concurrent settlement producing more than one authority result. Keep the executor in the workflow driver only: do not add a route, header, query, exported control, or hidden branch to any deployed function. Require immutable GitHub run/commit context for hosted commands. Load protected inputs only in the production-environment job, validate one complete test or live set, and keep raw values in memory. Implement a strict custom-origin contract per D-20 and the official Supabase custom-domain docs: reject the default project host and any origin containing the complete ref; derive the Auth callback and all three Edge Function URLs from the accepted origin. Implement `domains get` as read-only state discovery and guarded `domains reverify` plus guarded `domains activate`, with the complete-ref fingerprint check immediately before every invocation that can mutate hosted domain state. Use the same reusable guard immediately before database push, Auth configuration update, Edge Function secret update, and each named function deployment. Keep schema first. Add non-mutating custom-origin probes for Auth, support-api, support-flow HTML, and stripe-webhook, assert their documented route/status signatures, and prove all six Auth/authority counts unchanged around probes. In prelaunch-test mode, emit a zero baseline when empty and the hostile/fixture snapshot only after the acceptance marker; when the pushed repository contains an approved manifest, authorize only exact cleanup. In production-live mode require prior zero proof, coherent live inputs, and non-destructive reads/status probes only. Preserve no raw ref, provider value, credential, OAuth data, profile/email, or fixture key.</action>
  <acceptance_criteria>
    - The old local deployment arguments and local provider/deployment environment loader no longer exist.
    - Hosted mutation is unreachable unless running in GitHub Actions with a complete protected input set and matching approved full-ref SHA-256 immediately before that mutation.
    - Tests enumerate domain, database, provider, secret, and function mutations and prove a guard immediately precedes each.
    - Tests reject a default/ref-bearing origin and accept only one strict custom origin with exact Auth and Function route derivation.
    - Route probes prove Auth plus all three Edge Functions are reachable through that custom origin without changing the six-table zero baseline.
    - Every exact verifier CLI/option consumed by 02-08 through 02-11 exists and its focused failure tests pass before this plan completes.
    - The acceptance-marker-triggered executor and its focused tests own all nine stable cases before 02-09, including isolation, per-case invariants, immediate per-mutation guards, exact six-table inventory, and concurrent single-authority proof.
    - Tests prove the hostile driver is unreachable locally/in live mode and no deployed function contains an acceptance control surface.
    - Prelaunch cleanup requires an approved exact manifest; live mode requires prior zero proof and forbids acceptance/payment/OAuth/restore/hostile mutation.
    - Evidence serialization has an explicit allowlist and rejects raw refs, credentials, secrets, OAuth material, PII, and provider identifiers.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/support-recovery.spec.ts tests/e2e/support-restore.spec.ts</automated>
  </verify>
  <done>The only deploy-capable verifier path is CI-owned, target-guarded before every mutation, schema-first, mode-safe, and redacted.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Route browser-facing support flow through the validated custom origin</name>
  <files>supabase/functions/support-flow/index.ts, supabase/functions/tests/support-flow.test.ts</files>
  <read_first>
    - `supabase/functions/support-flow/index.ts`
    - `supabase/functions/tests/support-flow.test.ts`
    - `.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md` D-20
    - `https://supabase.com/docs/guides/platform/custom-domains`
  </read_first>
  <behavior>
    - The browser-facing request-scoped Auth client uses only the validated, ref-free `SUPPORT_PUBLIC_ORIGIN`; its authorize URL, callback URL, and support-flow URLs contain no default Supabase project hostname.
    - The service-role client used only for internal RPCs continues to use `SUPABASE_URL`; no public browser URL is derived from it.
    - Completion and invalid-state browser routes return the planned bounded HTML body with `text/html; charset=utf-8`; API-style invalid methods/origins remain generic and authority-free.
    - Missing, invalid, credential-bearing, path/query/fragment-bearing, default-host, or ref-bearing public origins fail closed before Auth or RPC construction.
  </behavior>
  <action>Write the focused Deno tests first. Split `defaultDependencies()` into the two existing trust purposes without adding an abstraction layer: validate `SUPPORT_PUBLIC_ORIGIN` once as the exact ref-free HTTPS origin; pass it to `createServerClient` for browser-facing Supabase Auth, callback, and function URLs; retain `SUPABASE_URL` only for the internal service-role `createClient` used by RPCs. Change the completion and invalid-state browser response helper/path to emit the planned small HTML document with `text/html; charset=utf-8`, while generic API request failures remain bounded and disclose nothing. Extend `support-flow.test.ts` to inject a recognizable default project host and prove it appears only in the internal RPC client setup, never in Auth authorize, redirectTo, callback, completion, function, response location, or HTML content. Prove every browser-facing URL begins at the custom origin, invalid state returns HTML with the asserted content type and no authority mutation, and malformed/default/ref-bearing public origins fail before any external call. Do not change service-role RPC semantics, OAuth authority, Checkout ownership, or add a proxy/fallback.</action>
  <verify>
    <automated>deno test --allow-env --config supabase/functions/deno.json supabase/functions/tests/support-flow.test.ts</automated>
  </verify>
  <done>The hosted support flow uses the ref-free custom domain for every browser/Auth surface, keeps SUPABASE_URL internal, and serves the planned HTML signature before first deployment.</done>
</task>

<task type="auto">
  <name>Task 3: Add the sole every-push-to-main production workflow</name>
  <files>.github/workflows/deploy-supabase-production.yml, docs/support-service-operations.md</files>
  <read_first>
    - `scripts/verify-supabase-support.mjs`
    - `package.json`
    - `supabase/config.toml`
    - `docs/support-service-operations.md`
  </read_first>
  <action>Create `.github/workflows/deploy-supabase-production.yml` with only the main push trigger, no path filter, top-level `contents: read`, and serialized `supabase-production` concurrency with cancellation disabled. In credential-free `repository-gates`, use `actions/setup-node@v4` with `node-version: 24`, `denoland/setup-deno@v2` with exact `deno-version: v2.7.14`, then run `npm ci`, `npx vitest run`, `npx playwright test --config=tests`, and `deno test --allow-env --config supabase/functions/deno.json supabase/functions/tests`. Install/use only `npx supabase@2.114.0`; run `npx supabase@2.114.0 db start` before the exact ordered database sequence `db reset --local --no-seed`, `test db`, `migration list --local`, and `db lint --local`. Add focused `--verify-workflow` cases in the owned E2E verifier tests that reject a missing/wrong Node 24 setup, missing/wrong Deno 2.7.14 setup, non-lockfile install, missing/altered Vitest/Playwright/Deno command, unpinned Supabase invocation, database action before `db start`, or reset/test/list/lint reordering. Keep repository-gates free of `production`, `secrets.*`, and protected provider variables. Make `deploy-production` depend on that job, bind the protected `production` environment, map the exact protected ownership listed in `<interfaces>`, and invoke the tested CI-only verifier. The verifier must reverify and activate the dashboard-registered custom domain before provider/schema/function configuration, full-ref-guard each domain mutation immediately beforehand, then upload redacted immutable domain/deployment/route evidence with retention and artifact digest. Preserve the acceptance-marker-triggered hostile executor in this same protected job for prelaunch mode only. Do not inline a second deployment implementation, add a manual trigger, add a path filter, or accept local deployment/provider inputs. Update operations documentation with the official custom-domain prerequisite, exact automatic setup/gate order, and guarded activation order; include no local secret/deploy command.</action>
  <acceptance_criteria>
    - Workflow triggers on every main push and has no path/manual trigger.
    - Repository gates cannot access protected credentials and must pass before the production environment is requested.
    - Permissions and concurrency are minimal and production-safe.
    - Guarded custom-domain reverify/activate precedes Auth/Edge deployment and exact custom-origin route probes.
    - Repository gates set up Node 24 and Deno 2.7.14, run `npm ci`, execute the exact Vitest/Playwright/Deno commands, and start pinned Supabase 2.114.0 before reset/test/list/lint.
    - Focused workflow-verifier tests reject every missing/wrong pin, missing command, local-secret boundary violation, and order regression.
    - Deployment invokes the tested CI-only verifier and uploads only redacted immutable evidence.
    - Documentation contains no local secret injection, local deploy command, or manual provider deployment instructions.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --verify-workflow .github/workflows/deploy-supabase-production.yml &amp;&amp; npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/support-recovery.spec.ts tests/e2e/support-restore.spec.ts</automated>
  </verify>
  <done>The workflow is ready for protected-environment configuration and its first automatic test-mode deployment in 02-08; this plan performs no hosted action.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Repository gates → production environment | Untrusted repository work must pass without credentials before protected inputs become available. |
| Protected environment → sole Supabase project | Complete secret target identity and provider inputs authorize hosted mutation. |
| Workflow run → evidence | Operational proof must be immutable without disclosing protected values. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-07-WT | Tampering | deployment target | mitigate | Compare complete-ref SHA-256 with approved lineage immediately before every mutation. |
| T-02-07-SE | Information Disclosure | repository gates/logs | mitigate | Separate ungated and environment jobs; allowlisted redaction; no raw ref/secret output. |
| T-02-07-DO | Denial of Service | concurrent deploys | mitigate | Stable concurrency group with cancellation disabled and schema-before-functions. |
| T-02-07-RP | Repudiation | deployment evidence | mitigate | Immutable GitHub run/commit IDs and artifact digests. |
| T-02-07-EL | Elevation of Privilege | workflow permissions | mitigate | Top-level contents-read only and protected environment restriction to main. |
| T-02-07-CD | Spoofing | public custom origin | mitigate | Strict ref-free origin/path derivation, guarded Supabase domain activation, and Auth/Function route probes. |
</threat_model>

<verification>The focused workflow/verifier contracts pass without provider inputs or network mutation; no hosted command is run in this plan.</verification>
<success_criteria>
- D-15 has one automatic no-path-filter main workflow and no alternate deployment path.
- D-20 uses the supported Supabase custom-domain surface for Auth, OAuth callback, all three Edge Functions, and the packaged capability.
- D-13/D-14/D-18 keep deployment/provider values and the complete ref out of local runtime, repository, package, logs, evidence, and chat.
- PAY-03/PAY-04 and D-17 retain webhook authority and forbid destructive live verification.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-07-SUMMARY.md`.</output>
