---
phase: 02-move-the-implementation-to-supabase
plan: 16
type: execute
wave: 16
depends_on: [02-15]
files_modified:
  - scripts/build-bin.mjs
  - tests/e2e/package-assets.spec.ts
  - tests/integration/support-dialog.spec.ts
  - .github/workflows/deploy-supabase-production.yml
  - scripts/verify-supabase-support.mjs
  - .planning/phases/02-move-the-implementation-to-supabase/02-16-RELEASE-EVIDENCE.md
  - .planning/phases/02-move-the-implementation-to-supabase/02-16-RELEASE-APPROVAL.md
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
must_haves:
  truths:
    - "Ordinary local build, development, test, and pack remain support-disabled unless explicitly configured; they need no hosted service, management ref, or deployment/provider credential (D-13)."
    - "Only the protected workflow derives `https://<project-ref>.supabase.co` from the freshly guarded management ref and passes it ephemerally to the release build; it is not a stored GitHub input (D-15, D-20)."
    - "The configured package contains exactly that canonical public origin in its launcher assignment and no bare ref, second Supabase host, credential, OAuth/PII, provider identifier, or provider secret (D-18, PAY-04)."
    - "The existing every-main-push workflow is extended, not duplicated; repository gates, protected-environment boundary, immediate mutation guards, serialization, schema-first order, and canonical route proof remain."
    - "The exact main push automatically deploys and produces one immutable configured package tied to the same run, commit, canonical origin, promotion, and retirement records; any fingerprint is internally derived supplemental correlation."
    - "Installed-artifact smoke observes Support and generic unverified status while Auth, support-api, support-flow text/plain, and stripe-webhook probes remain non-destructive with unchanged zero authority."
    - "Human approval is a separate immutable attestation digest-bound to the unchanged workflow release record, run ID, and package artifact."
  artifacts:
    - path: "scripts/build-bin.mjs"
      provides: "Release-only canonical public-origin embedding with ordinary-local absence"
    - path: ".github/workflows/deploy-supabase-production.yml"
      provides: "Existing automatic deployment workflow extended with guarded configured package artifact"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-16-RELEASE-EVIDENCE.md"
      provides: "Immutable package digest, canonical route proof, install smoke, zero authority, and protected-value exclusion"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-16-RELEASE-APPROVAL.md"
      provides: "Human approval attestation bound to immutable release evidence and package artifact"
  key_links:
    - from: "guarded SUPABASE_PROJECT_REF"
      to: "scripts/build-bin.mjs"
      via: "protected workflow derives one ephemeral CUMPA_RELEASE_SUPPORT_SERVICE_URL equal to the canonical origin"
      pattern: "CUMPA_RELEASE_SUPPORT_SERVICE_URL"
    - from: "generated launcher"
      to: "src/server support capability"
      via: "assign CUMPA_SUPPORT_SERVICE_URL only when runtime configuration is absent"
      pattern: "CUMPA_SUPPORT_SERVICE_URL"
    - from: "installed package"
      to: "production Auth and three Edge Function routes"
      via: "exact embedded canonical origin plus fixed status/invalid-input probes"
      pattern: "functions/v1/(support-api|support-flow|stripe-webhook)"
  prohibitions:
    - "HIGH: No second workflow, path/manual trigger, local deployment, local provider input, alternate origin, or runtime support fallback."
    - "HIGH: No live paid OAuth, Checkout, valid webhook, restore, hostile, replay, or reset flow; no authority row may be created."
    - "Do not persist CUMPA_RELEASE_SUPPORT_SERVICE_URL as a GitHub variable, accept arbitrary Supabase hosts, expose a bare ref, or weaken package/evidence scans."
---

<objective>
Embed the canonical default Supabase origin in the protected-workflow release package and prove the immutable installed artifact works non-destructively.

Purpose: Deliver configured releases while preserving local-first absence, package safety, one-workflow deployment, D-17 live-smoke limits, and D-20 exact routing.
Output: Release embedding, package/workflow regressions, immutable configured artifact, route evidence, and separate approval.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-15-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-15-RETIREMENT-EVIDENCE.md
@scripts/build-bin.mjs
@scripts/verify-production-artifacts.mjs
@tests/e2e/package-assets.spec.ts
@tests/integration/support-dialog.spec.ts
@.github/workflows/deploy-supabase-production.yml
@scripts/verify-supabase-support.mjs
</context>

<interfaces>
- `CUMPA_RELEASE_SUPPORT_SERVICE_URL` is an ephemeral build-process input derived inside the protected job from the immediately guarded management ref. It must equal `https://<project-ref>.supabase.co` and is never stored as an environment/repository variable.
- The generated launcher sets `CUMPA_SUPPORT_SERVICE_URL` to that exact origin only when the runtime variable is absent; explicit runtime configuration keeps existing precedence.
- `node scripts/verify-supabase-support.mjs --verify-workflow .github/workflows/deploy-supabase-production.yml --require-release-artifact` proves the existing workflow derives the build input after a fresh guard and emits one artifact.
- `node scripts/verify-supabase-support.mjs --check-release-evidence PATH --live-promotion PATH --retirement PATH --require-immutable-run --require-package-digest --require-zero-authority --non-destructive` validates canonical routes unconditionally.
- `node scripts/verify-production-artifacts.mjs --expected-support-origin https://<project-ref>.supabase.co --require-configured-launcher dist/bin/cumpa.mjs` is first invoked against a real configured launcher in this plan's protected workflow; it requires exactly one launcher assignment and rejects any other host or standalone ref.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add guarded release-only canonical-origin embedding and package/workflow checks</name>
  <files>scripts/build-bin.mjs, tests/e2e/package-assets.spec.ts, tests/integration/support-dialog.spec.ts, .github/workflows/deploy-supabase-production.yml, scripts/verify-supabase-support.mjs</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-15-SUMMARY.md`
    - `scripts/build-bin.mjs`
    - `scripts/verify-production-artifacts.mjs`
    - `tests/e2e/package-assets.spec.ts`
    - `tests/integration/support-dialog.spec.ts`
    - `.github/workflows/deploy-supabase-production.yml`
    - `scripts/verify-supabase-support.mjs`
  </read_first>
  <behavior>
    - No release-build input leaves the generated launcher support-disabled; a valid explicit input supplies only the default for absent runtime configuration.
    - The build rejects non-HTTPS, credentials, port, path, query, fragment, malformed project subdomain, and noncanonical host.
    - Official workflow derivation occurs only after fresh canonical 20-character complete-ref validation; the derived origin feeds deployment route checks, package build, production artifact scan, and release evidence without becoming a stored input.
    - Packed/extracted content requires exactly the expected canonical origin and rejects a missing/different origin, bare ref outside that URL, another Supabase host, protected values/names, OAuth/PII, provider IDs, source tree, or legacy service.
    - Release evidence binds immutable run/commit, package digest, promotion, retirement, exact canonical origin/routes, text/plain support-flow probe, and zero before/after authority; any fingerprint is internally derived supplemental correlation.
    - Approval checking is optional until Task 3, then requires one separate `cumpa.release-approval/v1` record bound to release-record SHA-256, run ID, and package artifact digest.
  </behavior>
  <action>Write failing absent/configured/invalid build and workflow/evidence cases first. Add the single build input in `<interfaces>` to `scripts/build-bin.mjs`; validate canonical shape and emit only the minimum launcher assignment before the existing CLI import/run. Preserve explicit runtime override and ordinary local absence. Extend package tests to inspect tarball and extracted launcher recursively, require the exact expected origin once, allow its public ref only inside that complete URL, and reject a second host or standalone ref plus all existing protected/legacy classes. In the existing workflow, immediately guard the management ref, derive the origin in job memory, and pass it ephemerally to the build; do not map a GitHub URL variable. After building, make this workflow the first real configured-release caller of Plan 02-15's scanner by passing `--expected-support-origin "$origin" --require-configured-launcher dist/bin/cumpa.mjs`, then apply the same scan to the packed/extracted artifact before upload. The scan must complete before release evidence is written. Extend the existing release evidence/checker contract to record exact public origin and enumerated Auth/function routes, fixed text/plain support-flow signature, immutable artifact lineage, zero counts, and the fixed approval attestation path. Preserve every trigger, job boundary, mutation guard, schema-first deployment, and prior scanner rule.</action>
  <verify>
    <automated>npx playwright test --config=tests tests/e2e/package-assets.spec.ts tests/integration/support-dialog.spec.ts &amp;&amp; node scripts/verify-supabase-support.mjs --verify-workflow .github/workflows/deploy-supabase-production.yml --require-release-artifact</automated>
  </verify>
  <done>Protected releases can embed exactly the canonical public origin without configuring ordinary local builds, storing another URL input, or exposing protected values.</done>
</task>

<task type="auto">
  <name>Task 2: Push main and validate the immutable configured package against live production</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-16-RELEASE-EVIDENCE.md</files>
  <read_first>
    - `.github/workflows/deploy-supabase-production.yml`
    - `.planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md`
    - `.planning/phases/02-move-the-implementation-to-supabase/02-15-RETIREMENT-EVIDENCE.md`
    - `tests/e2e/package-assets.spec.ts`
  </read_first>
  <action>Commit Task 1 and the retirement record, then push main normally. Do not dispatch or deploy locally. Bind evidence and the npm artifact to the exact automatic production-live run. Require repository gates before environment access; unchanged canonical `public_origin` and immutable GitHub run/commit lineage; immediate canonical 20-character complete-ref validation before every hosted mutation and before release derivation; schema before functions; successful exact-origin package scan; same commit/run lineage; and zero Auth/authority counts around non-destructive route smoke. Permit any retained fingerprint only when derived internally as supplemental correlation. Download only that run's evidence and package artifacts; do not inject protected inputs locally or rebuild a substitute. Stop unless `<interfaces>` validator passes.</action>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --check-release-evidence .planning/phases/02-move-the-implementation-to-supabase/02-16-RELEASE-EVIDENCE.md --live-promotion .planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md --retirement .planning/phases/02-move-the-implementation-to-supabase/02-15-RETIREMENT-EVIDENCE.md --require-immutable-run --require-package-digest --require-zero-authority --non-destructive</automated>
  </verify>
  <done>The exact GitHub-built package supplies optional Support through the canonical origin without local configuration or authority mutation.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve configured package usability and non-destructive live evidence</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-16-RELEASE-EVIDENCE.md, .planning/phases/02-move-the-implementation-to-supabase/02-16-RELEASE-APPROVAL.md</files>
  <what-built>A GitHub-built package containing one canonical public origin and immutable status-only production smoke evidence.</what-built>
  <how-to-verify>
    1. In the clean installed artifact, open Support and cancel back to a usable unrestricted review.
    2. Confirm no local support, build, deployment, or provider variable was set.
    3. Confirm package/evidence contain the exact canonical public origin and enumerated routes, but no bare ref, second Supabase host, credential, OAuth/PII, provider ID, or legacy content.
    4. Confirm generic unverified status, fixed text/plain support-flow response, and zero before/after counts.
    5. Confirm one immutable commit/run/artifact digest, exact canonical origin, promotion, and retirement lineage; any fingerprint is internally derived supplemental correlation.
  </how-to-verify>
  <action>Review only the clean-install surface and redacted immutable evidence from checks 1–5. Do not repeat paid/restore acceptance. After the human returns `approved`, write a new immutable `02-16-RELEASE-APPROVAL.md` with kind `cumpa.release-approval`, version 1, SHA-256 of the unchanged release record, run ID, and package artifact digest. Never edit the workflow release record in place or include the raw ref separately.</action>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --check-release-evidence .planning/phases/02-move-the-implementation-to-supabase/02-16-RELEASE-EVIDENCE.md --live-promotion .planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md --retirement .planning/phases/02-move-the-implementation-to-supabase/02-15-RETIREMENT-EVIDENCE.md --require-approved --require-immutable-run --require-package-digest --require-zero-authority --non-destructive</automated>
    <human-check>Approve only the status-only installed-package smoke after checks 1–5.</human-check>
  </verify>
  <done>Configured package and non-destructive live behavior are approved for final verification.</done>
  <resume-signal>Type `approved` or identify the exact failed redacted/browser fact.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Protected management ref → package | Only the derived canonical public origin may cross. |
| Package → live service | Smoke is generic, fixed-signature, and non-destructive. |
| Workflow run → release evidence | Package provenance, routing, and zero authority are immutable/redacted. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-16-PI | Information Disclosure | packed launcher | mitigate | Exact-origin requirement plus bare-ref, alternate-host, credential, OAuth/PII, and provider scans. |
| T-02-16-CS | Spoofing | capability origin | mitigate | Derive from freshly guarded ref and bind to immutable 02-08/02-10 lineage. |
| T-02-16-LM | Tampering | production smoke | mitigate | Status/invalid-input-only allowlist and before/after zero counts. |
| T-02-16-RP | Repudiation | package provenance | mitigate | Commit/run/package/retirement artifact IDs, digests, and separate approval. |
| T-02-16-WF | Elevation of Privilege | workflow change | mitigate | Regression check preserves sole trigger, job boundary, guards, serialization, and order. |
</threat_model>

<verification>Package/workflow checks pass without hosted values; the main push builds one canonical-origin artifact; installed status-only smoke and separate approval pass with zero authority.</verification>
<success_criteria>
- D-13 ordinary local remains disabled; D-14/D-15 derive the release input only inside the existing automatic protected workflow.
- D-17 forbids destructive live acceptance; D-18/PAY-04 retain package/log/evidence exclusion.
- D-20 permits exactly the canonical public origin and its Auth/function routes while rejecting bare ref and every other host.
- Seven files stay below the plan-level ten-file warning threshold.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-16-SUMMARY.md` when complete.</output>
