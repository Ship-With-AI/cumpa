---
phase: 02-move-the-implementation-to-supabase
plan: 04
type: tdd
wave: 4
depends_on: [02-03]
files_modified:
  - src/contracts/api.ts
  - src/server/support-client.ts
  - src/server/capabilities.ts
  - src/server/routes.ts
  - tests/api/support.test.ts
autonomous: true
requirements: [PAY-03, REC-02]
must_haves:
  truths:
    - "The loopback API remains anonymous/local-first and accepts only explicit Support/Restore actions after the user chooses one (D-03, D-04)."
    - "Only bounded hosted status observation can monotonically promote existing machine-wide state; redirects, action responses, unverified results, and failures never verify or demote it (PAY-03)."
    - "The shared/server contract contains no email, magic-link, recovery token, Supabase session, GitHub profile, payer email, or legacy recovery route (D-08, D-09, D-19, REC-02)."
    - "Previously persisted verified `SupportStateV1` remains valid without migration (D-19)."
  artifacts:
    - path: "src/contracts/api.ts"
      provides: "Strict action-start and status schemas with recovery schemas removed"
      exports: ["SupportActionSchema", "SupportStartRequestSchema", "SupportStartResultSchema", "SupportStatusSchema"]
    - path: "src/server/support-client.ts"
      provides: "HTTPS-only bounded hosted start/status client"
      exports: ["HostedSupportClient", "createHostedSupportClient"]
    - path: "src/server/capabilities.ts"
      provides: "Local start(action)/refresh monotonic support capability"
      exports: ["SupportCapability", "createSupportCapability"]
    - path: "src/server/routes.ts"
      provides: "Bearer/Host/Origin-protected action/status/refresh loopback routes"
  key_links:
    - from: "src/server/routes.ts"
      to: "src/server/capabilities.ts"
      via: "strict support action and refresh handlers"
      pattern: "support/(?:start|status|refresh)"
    - from: "src/server/capabilities.ts"
      to: "src/server/support-store.ts"
      via: "verified-only monotonic promote"
      pattern: "promote.*verified"
    - from: "src/server/support-client.ts"
      to: "supabase/functions/support-api/index.ts"
      via: "POST start and GET status over configured HTTPS origin"
      pattern: "support-api/(?:start|status)"
  prohibitions:
    - "HIGH: No Supabase session/token/code, provider profile/email, service-role key, Stripe secret, or payment authority enters browser/local state/package."
    - "HIGH: No redirect, local browser claim, unverified response, or client action directly writes verified local state."
    - "No recovery alias, email fallback, dual API surface, startup network dependency, or change to `SupportStateV1Schema` (D-02, D-04, D-08, D-19)."
---

<objective>
Cut the shared contracts and loopback server atomically from Phase 01 Checkout/email recovery to the hosted Supabase action/status capability.

Purpose: Establish one strict, token-free local transport and monotonic status boundary before browser callers migrate.
Output: RED/GREEN server contract tests, shared schemas, hosted client, capability, and loopback routes with every obsolete server caller removed.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-04-SUMMARY.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-05-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-03-SUMMARY.md
@src/server/support-store.ts
@src/server/support-client.ts
@src/server/capabilities.ts
@src/server/routes.ts
@src/contracts/api.ts

<interfaces>
Clean local contract:
- `SupportActionSchema = z.enum(['support', 'restore'])`.
- `SupportStartRequestSchema = z.strictObject({ action: SupportActionSchema }).readonly()`.
- `SupportStartResultSchema` is a readonly discriminated union `{ kind: 'ready', flowUrl: z.string().url() } | { kind: 'unavailable' }`.
- `HostedSupportClient.start(action, installationId)` → `{ flowUrl } | undefined`; `status(installationId)` and `close()` retain the HTTPS/5-second/8-KiB behavior.
- `SupportCapability.start(action)` injects the store-owned installation ID; `refresh()` alone calls monotonic promotion.
- Loopback `POST /api/support/start` accepts only `{action}`; `GET /api/support/status` and `POST /api/support/refresh` remain bearer/Host/Origin protected.
- `SessionClient.startSupportAction(action)` parses strict request/response contracts.

Delete, do not alias: `SupportCheckoutResultSchema`, `SupportRecoveryRequestSchema`, `SupportRecoveryResultSchema`, `SupportRecoveryStatusSchema`; `checkout`, `requestRecovery`, `recoveryStatus`; browser `openSupportCheckout`, `requestSupportRecovery`, `getSupportRecoveryStatus`; loopback `/api/support/checkout`, `/api/support/recovery`, `/api/support/recovery-status`; `CUMPA_SUPPORT_PAYMENT_URL`. Keep `CUMPA_SUPPORT_SERVICE_URL` as the sole public HTTPS capability origin.
</interfaces>
</context>

<feature>
  <name>Strict hosted Support/Restore transport and monotonic local authority</name>
  <files>src/contracts/api.ts, src/server/support-client.ts, src/server/capabilities.ts, src/server/routes.ts, tests/api/support.test.ts</files>
  <behavior>
    - Existing verified store → prompt suppression state is returned without hosted call; schema remains unchanged.
    - `support|restore` → strict loopback action → bounded HTTPS hosted flow URL.
    - Hosted unavailable/malformed/oversized/slow response → generic unavailable result; no local state change.
    - Hosted verified status → store promotes once; unverified/network result never verifies or demotes.
    - Old checkout/recovery schemas, methods, routes, aliases, and payment URL environment lookup do not remain in shared/server production code.
  </behavior>
  <implementation>Drive the server cutover from one failing API contract, implement every shared/server caller in one clean change, and remove obsolete interfaces rather than aliasing them. Keep `support-store.ts` unchanged unless the contract exposes a real compatibility defect.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — replace server payment/recovery tests with hosted action contracts</name>
  <files>tests/api/support.test.ts</files>
  <behavior>
    - Strict action input injects the store-owned installation ID and accepts only one configured HTTPS origin.
    - Bounds, aborts, malformed responses, redirects, and unverified status cannot promote local authority.
    - Existing verified state skips hosted status and remains readable without migration.
    - Obsolete checkout/recovery server contracts are absent.
  </behavior>
  <read_first>
    - tests/api/support.test.ts — current hosted client/capability/route/store behavior
    - src/server/support-store.ts — compatibility invariant that tests must preserve
    - src/server/support-client.ts — current Checkout/recovery transport to replace
    - src/server/capabilities.ts — current authority seam
    - src/server/routes.ts — current loopback callers
  </read_first>
  <action>Replace the API tests with failing contracts for strict action input, injected installation ID, fixed HTTPS origin, response/time/size bounds, abort, no direct verification, monotonic refresh, already-verified startup without network, generic unavailable behavior, and complete absence of old checkout/recovery methods and routes. Run the single focused file and require RED against the existing interfaces; if it passes, correct the test.</action>
  <acceptance_criteria>
    - Tests fail for the obsolete interface and missing action-start contract.
    - Every shared/server caller and existing verified-state compatibility behavior is represented.
    - No test treats an action or redirect as payment authority.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/api/support.test.ts</automated>
  </verify>
  <done>The shared/server action contract is demonstrably RED; commit `test(02-04): specify hosted support server cutover`.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — cut shared contracts, hosted client, capability, and loopback routes</name>
  <files>src/contracts/api.ts, src/server/support-client.ts, src/server/capabilities.ts, src/server/routes.ts, tests/api/support.test.ts</files>
  <read_first>
    - tests/api/support.test.ts — RED transport/capability/route contract
    - src/contracts/api.ts — readonly strict Zod conventions and current support schemas
    - src/server/support-client.ts — HTTPS validation, AbortController, timeout, 8 KiB response parser
    - src/server/capabilities.ts — `SupportCapability`, `createSupportCapability`, monotonic `promote`
    - src/server/routes.ts — bearer-protected support block and generic malformed request handling
  </read_first>
  <action>Implement the exact `<interfaces>` contract. Replace rather than extend old support schemas and methods. In `createHostedSupportClient`, validate one HTTPS service origin, POST strict action+installation to fixed `/functions/v1/support-api/start`, GET fixed status with encoded installation ID, keep AbortController/five-second timeout/8-KiB ceiling, and accept only same configured HTTPS origin for returned `flowUrl` (or an explicitly locked allowlisted Stripe URL only if the hosted API contract requires it; do not accept arbitrary redirects). In `createSupportCapability`, remove transient recovery state and expose only `status`, `start`, `refresh`, `close`; inject the store installation ID server-side and keep `promote` verified-only. Replace the three obsolete routes and Checkout route with one strict `/api/support/start`; retain status/refresh and all existing loopback Host/Origin/bearer/body guards. Remove `CUMPA_SUPPORT_PAYMENT_URL`. Do not touch store schema/persistence or ordinary capability authorization.</action>
  <acceptance_criteria>
    - Focused API tests pass for start/status/refresh/abort/bounds/monotonicity and already-verified compatibility.
    - Old schemas, methods, route registrations, recovery state, and payment URL environment lookup no longer exist in shared/server production code.
    - Loopback accepts only `support|restore`; hosted request owns installation ID and local browser never supplies it.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/api/support.test.ts tests/api/support-store.test.ts tests/api/security.test.ts</automated>
  </verify>
  <done>The server-side clean cutover is green with store/security regressions intact; commit `feat(02-04): cut support capability to Supabase actions`.</done>
</task>


</tasks>

## Artifacts this phase produces

- `src/contracts/api.ts`: `SupportActionSchema`, `SupportStartRequestSchema`, `SupportStartResultSchema`, retained `SupportStatusSchema`; obsolete support recovery/checkout exports removed.
- `src/server/support-client.ts`: `HostedSupportClient.start/status/close` using one HTTPS capability origin.
- `src/server/capabilities.ts`: `SupportCapability.start/status/refresh/close` with verified-only promotion.
- `src/server/routes.ts`: `/api/support/start`, `/api/support/status`, `/api/support/refresh` only.
- `tests/api/support.test.ts`: RED/GREEN contract for every shared/server caller and clean-cutover removal.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → authenticated loopback API | Browser input is untrusted but protected by existing Host/Origin/bearer boundary. |
| Loopback server → hosted Supabase capability | Public HTTPS responses are untrusted until bounded and schema/origin validated. |
| Hosted boolean status → machine-wide JSON | Only verified status may monotonically promote durable local state. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-04-LA | Elevation of Privilege | HIGH | `/api/support/start` | mitigate | Existing bearer/Host/Origin guards plus strict action-only body; installation identity injected server-side. |
| T-02-04-HR | Spoofing/Tampering | HIGH | hosted responses | mitigate | HTTPS-only fixed origin, five-second abort, 8 KiB ceiling, strict schemas, flow URL origin allowlist, generic unavailable result. |
| T-02-04-LS | Elevation of Privilege | HIGH | local support store | mitigate | Only refresh + hosted `verified` calls unchanged monotonic `promote`; redirect/action/unverified/network cannot write/demote. |
| T-02-04-AU | Information Disclosure | HIGH | browser/store/package | mitigate | No OAuth/token/code/profile/email/secret fields in contracts or state; packed artifact assertion. |
| T-02-04-FG | Elevation of Privilege | HIGH | review capabilities | mitigate | Support status controls dialog visibility only; anonymous review APIs and UI remain constructed identically. |
| T-02-04-DOS | Denial of Service | MEDIUM | polling/hosted calls | mitigate | Existing bounded cadence, visibility coalescing, timeout, close abort; failures leave workspace usable. |

Execution MUST stop if any HIGH threat remains unresolved or uncovered by focused tests. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
Run the focused API/store/security contracts. Browser and hosted-provider behavior are owned by Plans 02-05 and 02-06; this plan claims only the shared/server cutover.
</verification>

<success_criteria>
- D-03/D-04 keep the loopback anonymous and token-free.
- D-08/D-09/D-19 remove every shared/server email recovery and compatibility surface.
- PAY-03 and REC-02 local authority boundaries fail closed.
- Existing verified machine-wide state survives without migration.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-04-SUMMARY.md` with RED/GREEN evidence and an explicit shared/server caller-removal inventory.
</output>
