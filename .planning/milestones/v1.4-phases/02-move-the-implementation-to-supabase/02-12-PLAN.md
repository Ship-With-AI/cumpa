---
phase: 02-move-the-implementation-to-supabase
plan: 12
type: execute
wave: 12
depends_on: [02-11]
files_modified:
  - services/support/src/app.ts
  - services/support/src/server.ts
  - services/support/src/recovery.ts
  - services/support/src/recovery-store.ts
  - services/support/src/routes/installations.ts
  - services/support/src/routes/recovery.ts
  - services/support/src/routes/stripe-webhook.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
must_haves:
  truths:
    - "The second deletion slice starts only after 02-11 proves the first seven legacy provider/schema files are absent."
    - "The legacy Node support app/server, email-recovery state, installation/recovery routes, and Node Stripe webhook route no longer exist (D-02, D-08, D-19)."
    - "The local Cumpa loopback application and all three active Supabase Edge Functions remain present; their built-in SUPABASE_URL and canonical default-origin routing from 02-08 are unchanged (D-20)."
  artifacts: []
  key_links:
    - from: ".planning/phases/02-move-the-implementation-to-supabase/02-11-SUMMARY.md"
      to: "remaining legacy runtime/routes deletion"
      via: "first-slice exact absence gate before second-slice deletion"
      pattern: "seven"
  prohibitions:
    - "HIGH: Do not delete remaining runtime/routes before the 02-11 summary proves its exact seven-file slice."
    - "HIGH: Do not delete src/server app/routes, Supabase Edge Functions, or active support client/store/UI contracts."
    - "Do not retain compatibility endpoints, fallback servers, archived source, recovery aliases, or an alternate public-origin path."
---

<objective>
Remove the remaining legacy Node support runtime and routes in a second bounded deletion slice.

Purpose: Complete hosted runtime retirement sequentially without exceeding the standard file-scope warning threshold or touching active local/Supabase surfaces.
Output: Seven obsolete runtime/route files removed with replacement-presence proof.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-11-SUMMARY.md
@services/support/src/app.ts
@services/support/src/server.ts
@services/support/src/recovery.ts
@services/support/src/recovery-store.ts
@services/support/src/routes/installations.ts
@services/support/src/routes/recovery.ts
@services/support/src/routes/stripe-webhook.ts
@src/server/app.ts
@supabase/functions/support-api/index.ts
@supabase/functions/support-flow/index.ts
@supabase/functions/stripe-webhook/index.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate and delete the legacy Node app, server, and installation route</name>
  <files>services/support/src/app.ts, services/support/src/server.ts, services/support/src/routes/installations.ts</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-11-SUMMARY.md`
    - `services/support/src/app.ts`
    - `services/support/src/server.ts`
    - `services/support/src/routes/installations.ts`
    - `src/server/app.ts`
    - `supabase/functions/support-api/index.ts`
  </read_first>
  <action>Verify the 02-11 summary enumerates exactly its seven removed files and cites the immutable 02-10 gate. Then delete the standalone support Fastify app, its server entrypoint, and legacy installation route. Preserve Cumpa's loopback `src/server/app.ts` and the active Supabase `support-api` function; root Fastify remains required by the local review application. Leave no workspace-level server fallback, compatibility endpoint, or copied source.</action>
  <verify>
    <automated>test ! -e services/support/src/app.ts &amp;&amp; test ! -e services/support/src/server.ts &amp;&amp; test ! -e services/support/src/routes/installations.ts &amp;&amp; test -e src/server/app.ts &amp;&amp; test -e supabase/functions/support-api/index.ts</automated>
  </verify>
  <done>The legacy standalone app/server/installation route are absent while the local app and support-api remain.</done>
</task>

<task type="auto">
  <name>Task 2: Delete recovery state and remaining legacy routes</name>
  <files>services/support/src/recovery.ts, services/support/src/recovery-store.ts, services/support/src/routes/recovery.ts, services/support/src/routes/stripe-webhook.ts</files>
  <read_first>
    - `services/support/src/recovery.ts`
    - `services/support/src/recovery-store.ts`
    - `services/support/src/routes/recovery.ts`
    - `services/support/src/routes/stripe-webhook.ts`
    - `supabase/functions/support-flow/index.ts`
    - `supabase/functions/stripe-webhook/index.ts`
  </read_first>
  <action>Delete the email recovery lifecycle/store, email recovery route, and Node webhook route. Preserve the GitHub OAuth support-flow and signature-verified Supabase stripe-webhook functions. Do not create aliases from old route names, add a recovery fallback, or move deleted code into another directory. Confirm all seven files owned by this plan are absent before handing off to active-test migration.</action>
  <verify>
    <automated>test ! -e services/support/src/recovery.ts &amp;&amp; test ! -e services/support/src/recovery-store.ts &amp;&amp; test ! -e services/support/src/routes/recovery.ts &amp;&amp; test ! -e services/support/src/routes/stripe-webhook.ts &amp;&amp; test -e supabase/functions/support-flow/index.ts &amp;&amp; test -e supabase/functions/stripe-webhook/index.ts</automated>
  </verify>
  <done>All remaining legacy runtime/routes are absent and the Supabase OAuth/webhook replacements remain.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| First retirement slice → second slice | The first bounded removal must be complete before the remaining runtime is deleted. |
| Legacy support server → active local/Supabase servers | Similar names must not cause deletion of current replacement surfaces. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-12-01 | Tampering | sequential retirement | mitigate | Require exact 02-11 summary/absence proof before any second-slice deletion. |
| T-02-12-02 | Denial of Service | active local/Supabase runtime | mitigate | Exact seven-file list plus positive checks for local app and three functions. |
| T-02-12-03 | Elevation of Privilege | compatibility routes | mitigate | Clean deletion with no aliases or fallback endpoints. |
</threat_model>

<verification>The 02-11 slice is complete; all seven remaining legacy runtime/route files are absent; active local/Supabase entrypoints remain.</verification>
<success_criteria>
- D-02/D-08/D-19 remove the standalone runtime, email recovery, and legacy webhook without fallback.
- The plan modifies seven files, below the standard ten-file warning threshold.
- Plan 02-13 can migrate active evidence assertions and remove only stale test files after both deletion slices.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-12-SUMMARY.md` when complete.</output>
