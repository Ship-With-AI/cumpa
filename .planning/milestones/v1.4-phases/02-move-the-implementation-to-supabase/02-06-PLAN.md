---
phase: 02-move-the-implementation-to-supabase
plan: 06
type: tdd
wave: 6
depends_on: [02-05]
files_modified:
  - src/contracts/api.ts
  - src/server/app.ts
  - src/server/capabilities.ts
  - src/cli/run.ts
  - src/web/App.vue
  - src/web/components/IdentityHeader.vue
  - tests/api/support.test.ts
  - tests/cli/request.test.ts
  - tests/cli/selection.test.ts
  - tests/integration/support-dialog.spec.ts
autonomous: true
requirements: [PAY-02, PAY-04, SUP-01, SUP-03, SUP-04, SUP-05]
must_haves:
  truths:
    - "With `CUMPA_SUPPORT_SERVICE_URL` omitted, every ordinary local launch and both session-app factories create no support store/client, expose no support capability/routes, schedule no polling, render no support prompt/control, and make zero Supabase/Auth/Edge Function/Stripe calls (D-13)."
    - "The disabled local mode covers interactive pinned launches, stdin range launches, exact-patch launches, attached sessions, and direct ordinary/exact session factory use."
    - "Review selection, diffs, comments, drafts, export, and exact-patch behavior remain usable without authentication or hosted availability (D-03, D-04, D-13)."
    - "An explicit valid HTTPS service URL still enables the completed 02-04/02-05 support contracts without a no-op capability or fallback implementation (D-12, D-19)."
  artifacts:
    - path: "src/contracts/api.ts"
      provides: "Explicit session capability contract shared by pinned and exact-patch sessions"
    - path: "src/server/app.ts"
      provides: "Single configuration gate before support store/client construction"
    - path: "src/cli/run.ts"
      provides: "All CLI launch paths consume the same optional capability"
    - path: "src/web/App.vue"
      provides: "Capability-driven support lifecycle with no disabled-mode polling or prompt"
  key_links:
    - from: "src/cli/run.ts"
      to: "src/server/app.ts"
      via: "one shared configured-support factory used by all launch paths"
      pattern: "create.*Support"
    - from: "src/server/capabilities.ts"
      to: "src/contracts/api.ts"
      via: "session payload advertises support only when capability exists"
      pattern: "support.*enabled"
    - from: "src/web/App.vue"
      to: "src/web/components/IdentityHeader.vue"
      via: "session capability controls rendering, refresh, timers, prompt, and dialog"
      pattern: "supportEnabled"
  prohibitions:
    - "HIGH: Decide absence before constructing the support store or hosted client; a constructed no-op client does not satisfy D-13."
    - "HIGH: Do not add a disabled response route, unavailable support button, inert action, polling probe, Supabase client, auth bootstrap, or Stripe request in ordinary local mode."
    - "Do not remove or weaken completed configured HTTPS support behavior."
---

<objective>
Make ordinary local development a support-free runtime through one observable RED-to-GREEN capability gate per D-13, while preserving every review feature and the explicitly configured hosted support path.

Purpose: Eliminate hidden hosted construction, routes, browser work, and network calls when the public service URL is omitted.
Output: Optional session capability, one shared construction gate, capability-driven browser behavior, and focused server/CLI/browser proof.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-04-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-05-SUMMARY.md
@src/contracts/api.ts
@src/server/app.ts
@src/server/capabilities.ts
@src/cli/run.ts
@src/web/App.vue
@src/web/components/IdentityHeader.vue

<interfaces>
- Only an explicit valid HTTPS `CUMPA_SUPPORT_SERVICE_URL` enables support.
- The gate returns `SupportCapability | undefined` and does not instantiate the store/client on the undefined branch.
- Both session responses omit support metadata when disabled and include `{ enabled: true }` only when configured.
- Browser controls, dialog, refresh, timers, visibility refresh, and delayed prompt require the advertised capability.
- Local persistence remains installation ID plus monotonic verification state only (D-09, D-18).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — specify support-free local behavior across every entry point</name>
  <files>tests/api/support.test.ts, tests/cli/request.test.ts, tests/cli/selection.test.ts, tests/integration/support-dialog.spec.ts</files>
  <behavior>
    - Omitted or non-HTTPS configuration invokes no support factories, omits metadata, and exposes no support routes in pinned and exact-patch apps.
    - Interactive pinned, stdin range, attached, and exact-patch launches make no outbound support request.
    - Capability-absent browser sessions render no support UI, schedule no polling, send no support request, and still complete comment, draft, and export interactions.
    - Valid HTTPS configuration advertises support and retains existing support/restore/polling behavior.
  </behavior>
  <action>Write failing behavioral contracts before production changes. Reuse Fastify injection, CLI dependency seams, and Playwright request observation; do not inspect source text instead of runtime behavior. Isolate and restore environment per test. Cover ordinary and alternate session factories and all named launch paths. Confirm failure is the current unconditional behavior, not fixture error. This implements D-13 test-first while preserving D-03/D-04. The ordinary-package release assertion moves intact to 02-11, after runtime/toolchain deletion, where the package/remnant proof already runs.</action>
  <verify>
    <automated>npm test -- --run tests/api/support.test.ts tests/cli/request.test.ts tests/cli/selection.test.ts &amp;&amp; npx playwright test --config=tests tests/integration/support-dialog.spec.ts</automated>
  </verify>
  <done>The focused suite fails on unconditional construction/UI and includes a configured-path regression oracle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — gate capability construction and browser lifecycle</name>
  <files>src/contracts/api.ts, src/server/app.ts, src/server/capabilities.ts, src/cli/run.ts, src/web/App.vue, src/web/components/IdentityHeader.vue</files>
  <behavior>
    - No URL means no support object exists in the session composition graph.
    - Valid HTTPS configuration creates one capability per session and closes it once.
    - Session metadata and route availability agree for both session kinds.
    - Disabled browser sessions perform no support action while review behavior remains unchanged.
  </behavior>
  <action>Implement the smallest shared configuration gate in the existing server composition boundary and export it for CLI reuse. Validate HTTPS before calling either dependency constructor; return `undefined` otherwise. Replace unconditional defaults in both app factories and pinned, attached, range, and exact-patch launch paths; remove duplicate exact-patch construction and make close hooks optional. Extend both strict session schemas/builders with conditional metadata. Derive one browser `supportEnabled` value and guard refresh, timers, visibility handling, prompt, dialog, and header control. Preserve configured client/routes/store/actions and monotonic promotion per D-12/D-19. Add no disabled implementation or fallback.</action>
  <verify>
    <automated>npm test -- --run tests/api/support.test.ts tests/cli/request.test.ts tests/cli/selection.test.ts &amp;&amp; npx playwright test --config=tests tests/integration/support-dialog.spec.ts</automated>
  </verify>
  <done>The RED suite turns GREEN across every local entry point while configured HTTPS behavior remains available.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — retain one launch decision</name>
  <files>src/server/app.ts, src/cli/run.ts, tests/api/support.test.ts, tests/cli/request.test.ts, tests/cli/selection.test.ts</files>
  <behavior>
    - One configuration decision controls every default construction site.
    - Explicit injected capabilities still work.
    - No launch path recreates support after an absent decision.
  </behavior>
  <action>Remove only duplication exposed by GREEN. Every default path calls the same gate once; injected capabilities bypass default construction. Keep existing types/helpers rather than adding a second abstraction. Re-run the focused contract unchanged.</action>
  <verify>
    <automated>npm test -- --run tests/api/support.test.ts tests/cli/request.test.ts tests/cli/selection.test.ts &amp;&amp; npx playwright test --config=tests tests/integration/support-dialog.spec.ts</automated>
  </verify>
  <done>One default support decision remains and the contract stays GREEN.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Environment → local composition | Missing/untrusted configuration decides whether hosted capability exists. |
| Local session → browser | Metadata must agree with routes and UI. |
| Packaged CLI → public network | Ordinary local use must not initiate provider traffic. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-06-CG | Tampering | configuration gate | mitigate | Accept explicit HTTPS and decide before construction. |
| T-02-06-NC | Information Disclosure | local/provider boundary | mitigate | Assert zero factories, routes, UI requests, and packaged outbound requests. |
| T-02-06-CS | Spoofing | session capability | mitigate | Derive metadata and routes from one optional capability. |
| T-02-06-RG | Denial of Service | review workspace | mitigate | Exercise comment/draft/export with support absent. |
| T-02-06-LF | Elevation of Privilege | configured path | mitigate | Preserve completed Auth/webhook authority; add no fallback. |
</threat_model>

<verification>
Focused RED-to-GREEN proof covers absence before construction, routes/UI/polling, zero calls, every launch/session factory, configured regression, and usable review workflows; 02-11 owns the unchanged ordinary-package assertion after deletion.
</verification>

<success_criteria>
- D-13 is observable across server, CLI, and browser boundaries here, with ordinary-package proof preserved in dependent plan 02-11.
- D-03/D-04 keep review anonymous and available.
- D-09/D-18 keep identity and privileged values out of local persistence here; dependent 02-11 proves package-output absence.
- D-12/D-19 preserve configured hosted behavior without fallback.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-06-SUMMARY.md`.
</output>
