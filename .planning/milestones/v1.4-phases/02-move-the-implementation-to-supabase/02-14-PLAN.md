---
phase: 02-move-the-implementation-to-supabase
plan: 14
type: execute
wave: 14
depends_on:
  - 02-13
files_modified:
  - services/support/package.json
  - services/support/package-lock.json
  - services/support/tsconfig.json
  - services/support/vitest.config.ts
  - services/support/scripts/test-postgres.mjs
  - services/support/scripts/migrate.mjs
  - scripts/test-support-e2e-postgres.mjs
  - package.json
  - package-lock.json
autonomous: true
requirements:
  - PAY-04
  - REC-01
  - REC-02
must_haves:
  truths:
    - "The retired services/support workspace, standalone PostgreSQL runners, and their root-only scripts/dependencies are absent (D-19)."
    - "Cumpa's local Fastify server and active Supabase CLI/migration/test dependencies remain installed and resolvable."
    - "The npm lockfile describes only the retained root package and active dependency graph; no package or script recreates a stored public-origin or alternate routing path (D-20)."
  artifacts:
    - path: "package.json"
      provides: "Root scripts/workspaces/dependencies with legacy service entries removed"
    - path: "package-lock.json"
      provides: "Regenerated active-only npm graph"
  key_links:
    - from: "package.json"
      to: "package-lock.json"
      via: "existing npm lockfile-only regeneration after exact manifest cleanup"
      pattern: "lockfileVersion"
  prohibitions:
    - "HIGH: Do not remove Fastify or dependencies used by the 127.0.0.1 Cumpa server."
    - "HIGH: Do not remove Supabase CLI, Deno, Playwright, or Vitest commands used by the active workflow/final verification."
    - "Do not add a replacement workspace, dependency, package manager, compatibility script, or URL-derivation helper."
---

<objective>
Remove only the retired support-service workspace/toolchain and reconcile the root npm graph.

Purpose: Finish the dependency side of D-19 without damaging local Cumpa or Supabase verification.
Output: Nine bounded manifest/config/script changes and a valid active-only npm graph.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
@GSD core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-13-SUMMARY.md
@package.json
@package-lock.json
@services/support/package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove the legacy workspace and exact root callers</name>
  <files>services/support/package.json, services/support/package-lock.json, services/support/tsconfig.json, services/support/vitest.config.ts, services/support/scripts/test-postgres.mjs, services/support/scripts/migrate.mjs, scripts/test-support-e2e-postgres.mjs, package.json, package-lock.json</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-13-SUMMARY.md`
    - `services/support/package.json`
    - `services/support/package-lock.json`
    - `services/support/tsconfig.json`
    - `services/support/vitest.config.ts`
    - `services/support/scripts/test-postgres.mjs`
    - `services/support/scripts/migrate.mjs`
    - `scripts/test-support-e2e-postgres.mjs`
    - `package.json`
    - `package-lock.json`
  </read_first>
  <action>Delete the six remaining support-workspace manifest/config/script files and the root standalone-Postgres wrapper. In root `package.json`, remove only workspace entries, scripts, and dependencies whose complete caller set was deleted across 02-11 through 02-13. Explicitly preserve Fastify and every dependency/script used by Cumpa's loopback server, Supabase migrations/functions/database checks, Vitest, Playwright, package build, and the every-main-push workflow. Add no replacement helper, alias, stored-origin script, or alternate routing command. Regenerate `package-lock.json` with the repository's existing npm package manager using lockfile-only/ignore-scripts behavior, not a new dependency install path.</action>
  <verify>
    <automated>npm install --package-lock-only --ignore-scripts &amp;&amp; npm ls --all</automated>
  </verify>
  <done>The exact legacy workspace/toolchain callers are removed and the retained dependency graph resolves.</done>
</task>

<task type="auto">
  <name>Task 2: Prove no legacy workspace command remains</name>
  <files>services/support/package.json, services/support/package-lock.json, services/support/tsconfig.json, services/support/vitest.config.ts, services/support/scripts/test-postgres.mjs, services/support/scripts/migrate.mjs, scripts/test-support-e2e-postgres.mjs, package.json, package-lock.json</files>
  <action>Assert all seven deleted service/root toolchain files are absent, the root package no longer declares the support workspace or retired Postgres/Render/Resend commands, and active local/server/Supabase/test/build commands still resolve through `npm ls --all`. Do not run the full suites here; 02-15 and 02-17 own the package/final proof. Record exact absence and retained-dependency results in the 02-14 summary.</action>
  <verify>
    <automated>test ! -e services/support/package.json &amp;&amp; test ! -e services/support/package-lock.json &amp;&amp; test ! -e services/support/tsconfig.json &amp;&amp; test ! -e services/support/vitest.config.ts &amp;&amp; test ! -e services/support/scripts/test-postgres.mjs &amp;&amp; test ! -e services/support/scripts/migrate.mjs &amp;&amp; test ! -e scripts/test-support-e2e-postgres.mjs &amp;&amp; npm ls --all</automated>
  </verify>
  <done>The support workspace is gone while every active root dependency remains coherent.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Deleted workspace → root graph | Root cleanup must remove obsolete callers without removing local/server/Supabase dependencies. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-14-01 | Denial of Service | root dependency graph | mitigate | Remove only dependencies with no retained caller; preserve named active surfaces and run npm graph validation. |
| T-02-14-02 | Tampering | lockfile | mitigate | Existing npm, lockfile-only regeneration, ignored lifecycle scripts, and `npm ls --all`. |
</threat_model>

<verification>The seven obsolete toolchain files are absent and the regenerated root lockfile resolves all active dependencies.</verification>
<success_criteria>
- D-19 leaves no service workspace/script/dependency compatibility path.
- Local Fastify and Supabase verification tooling remain usable.
- The plan modifies nine files, below the standard ten-file warning threshold.
</success_criteria>

<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-14-SUMMARY.md` when complete.</output>
