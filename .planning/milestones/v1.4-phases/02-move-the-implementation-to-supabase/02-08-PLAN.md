---
phase: 02-move-the-implementation-to-supabase
plan: 08
type: execute
wave: 8
depends_on: [02-07]
files_modified:
  - .github/workflows/deploy-supabase-production.yml
  - scripts/verify-supabase-support.mjs
  - supabase/functions/support-api/index.ts
  - supabase/functions/support-flow/index.ts
  - supabase/functions/tests/support-flow.test.ts
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/support-recovery.spec.ts
  - tests/e2e/support-restore.spec.ts
  - docs/support-service-operations.md
autonomous: false
requirements: [PAY-03, PAY-04, SUP-01, REC-01, REC-02]
user_setup:
  - service: github
    why: "Protected main and the production environment must exist before the first automatic deployment; the environment exclusively owns the public management ref variable, deployment credentials, and provider inputs per D-15."
    dashboard_config:
      - task: "Require credential-free repository-gates checks; restrict production deployments to protected main and require the chosen deployment reviewer."
        location: "GitHub repository Settings -> Rulesets and Settings -> Environments -> production"
      - task: "Add the temporary prelaunch inputs only to production. Do not create repository-level copies or any stored public-URL variables."
        location: "GitHub repository Settings -> Environments -> production"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Protected production environment secret; sole-project management token"
      - name: SUPABASE_PROJECT_REF
        source: "Protected production environment variable; complete public sole-project management ref"
      - name: SUPABASE_DB_PASSWORD
        source: "Protected production environment secret; sole-project database credential"
      - name: SUPABASE_GITHUB_CLIENT_SECRET
        source: "Protected production environment secret; temporary prelaunch GitHub OAuth app"
      - name: STRIPE_SECRET_KEY
        source: "Protected production environment secret; coherent Stripe test-mode key"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Protected production environment secret; exact test endpoint signing secret"
      - name: SUPPORT_PROVIDER_MODE
        source: "Protected production environment variable; exact value prelaunch-test"
      - name: SUPABASE_GITHUB_CLIENT_ID
        source: "Protected production environment variable; temporary prelaunch GitHub OAuth app"
      - name: STRIPE_PRICE_ID
        source: "Protected production environment variable; active test-mode one-time USD $49.99 Price"
      - name: STRIPE_WEBHOOK_ENDPOINT_ID
        source: "Protected production environment variable; enabled test-mode endpoint"
  - service: github-oauth
    why: "GitHub must return to Supabase Auth at the sole project's canonical public origin."
    dashboard_config:
      - task: "Configure exactly https://<project-ref>.supabase.co/auth/v1/callback, request no repository scopes, and retain no email or magic-link provider."
        location: "GitHub Settings -> Developer settings -> OAuth Apps"
  - service: stripe
    why: "A coherent test-mode Price and webhook endpoint must exist before the first automatic run."
    dashboard_config:
      - task: "Confirm one active non-recurring USD $49.99 test Price and one endpoint at https://<project-ref>.supabase.co/functions/v1/stripe-webhook subscribed only to checkout.session.completed and checkout.session.async_payment_succeeded; store only the endpoint ID, Price ID, signing secret, and provider key in production."
        location: "Stripe Dashboard -> test mode Product catalog and Developers -> Webhooks"
must_haves:
  truths:
    - "Before any human setup or push, source, workflow, focused tests, and operations documentation implement D-15/D-20's canonical origin and reduced protected-input contract."
    - "Supabase Postgres, Auth, and the three Edge Functions remain the sole hosted implementation while Stripe remains the webhook provider (D-01)."
    - "The protected executor validates the complete ref's canonical 20-character shape before deriving exactly https://<project-ref>.supabase.co; no duplicate stored URL input remains."
    - "Every main push still passes credential-free repository gates before protected inputs, shape-validates the complete ref immediately before every hosted mutation, applies schema before functions, and serializes without cancellation."
    - "Support functions derive browser routes from built-in SUPABASE_URL and return fixed bounded text/plain completion, invalid-state, and unavailable responses."
    - "Only after both corrective implementation tasks pass does the human configure protected inputs and canonical callbacks; Plan 02-08 still performs no commit, push, or hosted mutation."
    - "The corrected verifier is ready for 02-09's first ordinary push to emit exact-origin-aware immutable route and zero-authority evidence while rejecting bare ref leakage, other Supabase hosts, credentials, OAuth/PII, and provider secrets."
  artifacts:
    - path: "scripts/verify-supabase-support.mjs"
      provides: "Guarded default-origin derivation, reduced protected inputs, deployment, route probes, and exact-origin-aware evidence validation"
    - path: "supabase/functions/support-flow/index.ts"
      provides: "SUPABASE_URL-derived OAuth flow with fixed bounded text/plain browser terminal responses"
    - path: ".github/workflows/deploy-supabase-production.yml"
      provides: "Corrected every-main-push workflow ready for 02-09's first automatic prelaunch deployment"
  key_links:
    - from: "guarded SUPABASE_PROJECT_REF"
      to: "Auth, support-api, support-flow, stripe-webhook, and release origin"
      via: "one in-memory canonical origin derivation after complete-ref shape validation"
      pattern: "https://.*\\.supabase\\.co"
    - from: "supabase/functions/support-flow/index.ts"
      to: "SUPABASE_URL"
      via: "platform-injected origin for OAuth redirects and terminal routes"
      pattern: "SUPABASE_URL"
    - from: "corrected protected-main workflow"
      to: "02-09 first automatic deployment"
      via: "Plan 02-08 stops after setup; the next plan owns the first commit, push, and immutable evidence"
      pattern: "prelaunch-test"
  prohibitions:
    - "HIGH: Do not perform human setup until Tasks 1–2 corrective cutover checks pass, and do not commit or push anywhere in this plan."
    - "HIGH: Do not run provider or deployment commands locally, use manual workflow dispatch, create a second project/environment/front door, or weaken an immediate mutation guard."
    - "Do not persist a replacement origin variable, expose the raw management ref outside protected executor memory, or interpolate OAuth/error/query data into browser text."
---

<objective>
Correct the completed 02-07 deployment implementation to the sole project's canonical default origin before any setup or push, then configure the protected provider inputs without triggering deployment.

Purpose: Make D-15/D-20 the only executable routing and evidence contract while preserving the guarded one-project promotion boundary and a strict correction → setup → later push order.
Output: Corrected workflow/verifier/functions/tests/runbook plus protected environment and provider callbacks ready for 02-09's first ordinary push.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-07-SUMMARY.md
@.github/workflows/deploy-supabase-production.yml
@scripts/verify-supabase-support.mjs
@supabase/functions/support-api/index.ts
@supabase/functions/support-flow/index.ts
@docs/support-service-operations.md
</context>

<interfaces>
- Canonical public origin: `https://<ref>.supabase.co`, derived from the protected `SUPABASE_PROJECT_REF` variable only after validating the complete ref against the canonical 20-character shape.
- Auth provider callback: `${origin}/auth/v1/callback`; Auth `site_url`: `${origin}/functions/v1/support-flow/complete`; redirect allowlist: `${origin}/functions/v1/support-flow/callback`.
- Function routes: `${origin}/functions/v1/support-api`, `${origin}/functions/v1/support-flow`, `${origin}/functions/v1/stripe-webhook`.
- Fixed browser terminal responses use `content-type: text/plain; charset=utf-8`: completion `Support flow complete. You can return to Cumpa.`, invalid `This support link is invalid or expired. Return to Cumpa and try again.`, unavailable `Support is temporarily unavailable. Return to Cumpa and try again.`.
- `node scripts/verify-supabase-support.mjs --verify-workflow .github/workflows/deploy-supabase-production.yml --require-environment production --expected-mode prelaunch-test` is the corrected credential-free workflow gate.
- `node scripts/verify-supabase-support.mjs --check-run-evidence PATH --expected-mode prelaunch-test --require-immutable-run --require-zero-authority` validates canonical routes unconditionally; there is no optional routing mode.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cut the protected workflow and verifier to the canonical default origin</name>
  <files>.github/workflows/deploy-supabase-production.yml, scripts/verify-supabase-support.mjs, tests/e2e/support-payment.spec.ts, tests/e2e/support-recovery.spec.ts, tests/e2e/support-restore.spec.ts</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md` D-15 and D-20
    - `.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md` D-20 Decision Delta, Auth and Provider Configuration, and Evidence Package Scanner Contract
    - `.planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md`
    - `.github/workflows/deploy-supabase-production.yml`
    - `scripts/verify-supabase-support.mjs`
    - `tests/e2e/support-payment.spec.ts`
    - `tests/e2e/support-recovery.spec.ts`
    - `tests/e2e/support-restore.spec.ts`
  </read_first>
  <behavior>
    - Workflow verification rejects every stored duplicate URL input and domain-lifecycle command while retaining the unfiltered main trigger, repository-gates boundary, protected production job, serialization, immediate mutation guards, and schema-before-functions.
    - The executor validates the complete ref's canonical 20-character shape before every hosted mutation and before deriving the one canonical origin and every route in `<interfaces>`.
    - The guarded Auth PATCH uses exactly `external_github_enabled`, protected GitHub client ID/secret, completion `site_url`, and callback `uri_allow_list`; the guarded function-secret POST sets exactly the three source-consumed Stripe values.
    - Deployment records replace domain state with exact `public_origin`, route results, immutable GitHub run/commit lineage, mutation order, and redacted authority snapshots; any `fingerprint` is SHA-256 derived internally from the ref for correlation only.
    - Evidence permits only the exact canonical origin and enumerated routes, then rejects the bare ref elsewhere, another Supabase host, credentials, OAuth/PII, provider identifiers/secrets, and raw fixture values.
  </behavior>
  <action>Write focused failing workflow/verifier cases first, then make the D-15/D-20 cutover. Delete `APPROVED_SUPABASE_PROJECT_REF_SHA256` and the stored `SUPPORT_PUBLIC_ORIGIN`, `SUPABASE_SITE_URL`, `SUPABASE_REDIRECT_URL`, `SUPABASE_GITHUB_CALLBACK_URL`, and `STRIPE_WEBHOOK_URL` mappings without replacement from active setup, workflow, verifier, scanner, and absence contracts. Reduce `PROTECTED_INPUTS`; define `guardTarget(inputs)` to require the complete `SUPABASE_PROJECT_REF` match the canonical 20-character shape, and invoke it immediately before schema, Auth/provider PATCH, function-secret POST, and each function deployment. Derive `origin` only after validation; remove domain discovery/reverify/activate, domain state, display suffix, and route flags; and derive Auth/provider/function/webhook/release URLs from `<interfaces>`, with schema before all functions. If redacted evidence retains `fingerprint` or `--require-one-fingerprint`, derive SHA-256 internally from the ref solely for correlation; never read, configure, approve, or compare an independent fingerprint. Make the Auth PATCH body exactly `external_github_enabled: true`, `external_github_client_id: inputs.SUPABASE_GITHUB_CLIENT_ID`, `external_github_secret: inputs.SUPABASE_GITHUB_CLIENT_SECRET`, `site_url: routes.supportFlowComplete`, and `uri_allow_list: routes.supportFlowCallback`; keep GitHub's distinct callback as `routes.authCallback`. Make the function-secret POST set exactly `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, and `STRIPE_PRICE_ID`; built-in Supabase values, provider mode, endpoint ID, and origin are not function secrets. Make route/evidence checks unconditional and exact-origin-aware, and update the three closest Playwright suites for input reduction, payloads, mutation order/shape guards, route probes, exact-origin allow/deny behavior, and exact canonical origin plus immutable GitHub run/commit cross-record authority. Do not add aliases or compatibility options. Stop without committing, pushing, or mutating a hosted system.</action>
  <verify>
    <automated>npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/support-recovery.spec.ts tests/e2e/support-restore.spec.ts &amp;&amp; node scripts/verify-supabase-support.mjs --verify-workflow .github/workflows/deploy-supabase-production.yml --require-environment production --expected-mode prelaunch-test</automated>
  </verify>
  <done>The sole protected workflow/verifier path implements guarded canonical derivation, exact provider application, and exact-origin-aware evidence before any setup or push.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Cut Edge Functions, focused tests, and runbook to built-in origin and fixed plain text</name>
  <files>supabase/functions/support-api/index.ts, supabase/functions/support-flow/index.ts, supabase/functions/tests/support-flow.test.ts, docs/support-service-operations.md</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md` D-15 and D-20
    - `.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md` Edge Function Browser Response Constraint and Public Routing Data vs Protected Credentials
    - `supabase/functions/support-api/index.ts`
    - `supabase/functions/support-flow/index.ts`
    - `supabase/functions/tests/support-flow.test.ts`
    - `docs/support-service-operations.md`
  </read_first>
  <behavior>
    - `support-api` and `support-flow` derive the project gateway only from platform-injected `SUPABASE_URL`; neither reads a separate public-origin value.
    - OAuth callback, completion, API, and webhook URLs use only the canonical origin/routes in `<interfaces>` while service-role RPC and Stripe webhook authority remain unchanged.
    - Completion, invalid, and unavailable browser branches return the exact fixed bounded `text/plain; charset=utf-8` signatures without reflected input, identifiers, OAuth data, PII, or provider text.
    - Task 1's verifier probes cover the support-api canonical route; focused support-flow Deno tests cover exact callback derivation, status, content type, body, byte bounds, and negative sensitive-substring cases.
    - Operations documentation has only the default-origin setup, callback-role distinction, protected derivation, automatic trigger, redaction, and evidence path; no add-on, DNS, proxy, activation, alternate host, or duplicate URL input remains.
  </behavior>
  <action>Write the focused support-flow Deno failures first. In both hosted functions replace separate public-origin reads and the prior host-secrecy checks with built-in `SUPABASE_URL`, preserving the platform service-role key, RPC boundaries, session isolation, signed-webhook authority, and fixed provider semantics. Replace browser-page generation with the three exact terminal responses in `<interfaces>` and keep them non-interpolated and bounded. Use Task 1's verifier route regressions for support-api origin coverage; update `support-flow.test.ts` for callback derivation and exact plain-text success/invalid/unavailable responses. Rewrite the operations runbook around the one default project origin, distinct GitHub Auth and post-auth callbacks, reduced protected input set, built-in function origin, automatic main trigger, and exact-origin-aware redaction. Remove every custom routing/add-on/DNS/proxy/activation instruction and all five obsolete URL input names except an explicit deletion checklist. Stop without human setup, commit, push, or hosted action.</action>
  <verify>
    <automated>deno test --allow-env --config supabase/functions/deno.json supabase/functions/tests/support-flow.test.ts</automated>
  </verify>
  <done>The hosted code, focused tests, and operator contract use only built-in/canonical routing and fixed bounded plain text before any setup or push.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Configure protected environment and default-origin provider callbacks</name>
  <what-built>Tasks 1–2 have corrected and verified the workflow, verifier, functions, tests, and runbook; no commit, push, or hosted mutation has occurred.</what-built>
  <how-to-verify>
    1. Confirm protected main requires repository-gates and production permits protected main only with reviewer approval.
    2. Confirm production contains exactly the public management ref variable, deployment credentials, provider, provider-mode, Price, and endpoint inputs listed in `user_setup`, with no configured project-ref fingerprint or deleted URL variable.
    3. Confirm GitHub OAuth uses exactly `https://&lt;project-ref&gt;.supabase.co/auth/v1/callback`.
    4. Confirm Stripe test mode uses one active USD $49.99 Price and `https://&lt;project-ref&gt;.supabase.co/functions/v1/stripe-webhook`.
    5. Confirm no provider/deployment credential or bare management ref left protected browser settings; the public ref appears outside only inside the two canonical URLs.
  </how-to-verify>
  <action>After both corrective tasks pass, configure the protected `production` environment and provider dashboards exactly as `user_setup` specifies. Retain the public management target variable, deployment credentials, provider inputs, provider mode, Price ID, and webhook endpoint ID. Delete any existing `APPROVED_SUPABASE_PROJECT_REF_SHA256`, `SUPPORT_PUBLIC_ORIGIN`, `SUPABASE_SITE_URL`, `SUPABASE_REDIRECT_URL`, `SUPABASE_GITHUB_CALLBACK_URL`, and `STRIPE_WEBHOOK_URL` environment entries without reading or disclosing their values; create no URL input. Set only the two canonical provider callback routes in checks 3–4, keep credentials inside their browser dashboards, and stop before any commit, push, or hosted deployment.</action>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --verify-workflow .github/workflows/deploy-supabase-production.yml --require-environment production --expected-mode prelaunch-test</automated>
    <human-check>Complete checks 1–5 without disclosing any protected value.</human-check>
  </verify>
  <done>Protected ownership and the two default-origin dashboard routes are ready for 02-09's first ordinary main push.</done>
  <resume-signal>Type `production default origin ready` without any value, or identify the missing numbered prerequisite.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Protected public ref → public routing | Only the shape-validated canonical origin and enumerated routes may leave executor memory; GitHub protected-environment ownership selects the target. |
| Human settings → protected environment | Provider and deployment inputs never transit local tooling, evidence, or chat. |
| Main push → hosted mutation | The sole automatic workflow must gate and guard every mutation. |
| Workflow → evidence | Exact public routing is allowed; credentials, bare ref, OAuth/PII, and provider secrets are not. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-08-WT | Tampering | deployment target | mitigate | Protected-environment ownership plus immediate canonical 20-character ref validation before every mutation. |
| T-02-08-OR | Spoofing | public origin | mitigate | Derive one canonical origin from the guarded ref and reject every other host. |
| T-02-08-SE | Information Disclosure | package/log/evidence | mitigate | Exact-origin allowlist plus bare-ref, credential, OAuth/PII, and provider scans. |
| T-02-08-BP | Elevation of Privilege | main/environment | mitigate | Credential-free repository gates, protected-main restriction, and reviewer gate. |
| T-02-08-RP | Repudiation | correction/setup handoff | mitigate | Both corrective task checks precede setup, and the plan stops uncommitted/unpushed for 02-09's immutable first-run capture. |
| T-02-08-TX | Information Disclosure | browser terminal response | mitigate | Fixed bounded text/plain bodies with no reflected input. |
</threat_model>

<verification>Two bounded corrective source/test tasks pass before setup; protected default-origin callbacks are configured only afterward; no commit, push, or hosted mutation occurs until 02-09.</verification>
<success_criteria>
- D-15 retains the sole unfiltered automatic deployment boundary and every immediate mutation guard.
- D-20 supplies exactly one canonical public origin without stored URL duplication or an alternate routing lifecycle.
- PAY-03/PAY-04 retain webhook authority and package/evidence secret exclusion while permitting only the canonical public origin.
- REC-01/REC-02 callbacks and browser terminal responses derive from the canonical origin without returning OAuth material to Cumpa.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-08-SUMMARY.md` when complete.</output>
