---
phase: 01-add-voluntary-stripe-support-payment-and-email-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: false
requirements: [PAY-03, PAY-04]
must_haves:
  truths:
    - "The hosted deployable uses only registry-verified official, maintained packages for Stripe and PostgreSQL authority."
  artifacts: []
  key_links: []
  prohibitions:
    - "Do not install a package until its exact registry identity, repository, maintainership, scripts, and release provenance are accepted."
    - "No hosted dependency or credential may be added to the root published Cumpa package."
---

<objective>
Approve the exact hosted-only dependencies required for Stripe signature verification and transactional PostgreSQL fulfillment before any install occurs (PAY-03, PAY-04).

Purpose: Close the package-legitimacy supply-chain gate raised by the research audit without weakening the requirement to use Stripe's official verifier.
Output: A blocking human decision recorded by the executor; this plan creates no repository artifact.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-CONTEXT.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-RESEARCH.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify hosted dependency provenance</name>
  <files>services/support/package.json, services/support/package-lock.json</files>
  <read_first>
    - .planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-RESEARCH.md — Package Legitimacy Audit and Don't Hand-Roll sections
    - package.json — confirm the root distributed package remains unchanged
  </read_first>
  <action>Open the canonical npm registry pages and linked source repositories for `stripe@22.5.0`, `pg@8.23.0`, and development-only `@types/pg@8.21.0`. Confirm `stripe` links to `stripe/stripe-node`, `pg` links to `brianc/node-postgres`, none has an unexpected lifecycle install script, and the named versions are maintained releases. Approval authorizes installation only in `services/support/package.json`; the root `package.json` remains untouched per PAY-04. Reject or report any registry/repository mismatch rather than substituting a similarly named package.</action>
  <verify>
    <automated>npm view stripe@22.5.0 name version repository scripts --json &amp;&amp; npm view pg@8.23.0 name version repository scripts --json &amp;&amp; npm view @types/pg@8.21.0 name version repository scripts --json</automated>
    <human-check>Compare https://www.npmjs.com/package/stripe, https://www.npmjs.com/package/pg, and https://www.npmjs.com/package/@types/pg with their linked canonical repositories.</human-check>
  </verify>
  <done>
    - The human explicitly approves all three exact package identities and versions.
    - Any suspicious provenance blocks Plan 01-02; no package is installed on ambiguity.
    - Approval is limited to the separately deployed hosted service.
  </done>
  <resume-signal>Type "approved" only after the registry and repository checks pass, or describe the mismatch.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

This gate creates no file or symbol. It authorizes Plan 01-02 to create the separately deployed `services/support/` dependency graph.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| npm registry → hosted build | Third-party package bytes enter the only service allowed to hold Stripe and database credentials. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-01-01-SC | Tampering | HIGH | `stripe`, `pg`, `@types/pg` installs | mitigate | Blocking human provenance review of exact versions before installation; lockfile required in Plan 01-02. |
| T-01-01-ID | Information Disclosure | HIGH | Root npm package | mitigate | Approval is hosted-directory-only; later package artifact scan proves no hosted source or credential entered the tarball. |

Any unresolved HIGH threat blocks approval and Plan 01-02. There is no accepted HIGH risk.
</threat_model>

<verification>
The automated npm metadata command succeeds and the human provenance comparison explicitly approves each exact package. Do not run formatters, linters, builds, or test suites.
</verification>

<success_criteria>
- Exact hosted package provenance is approved.
- Root package dependencies remain unchanged.
- All HIGH supply-chain findings are closed or implementation remains blocked.
</success_criteria>

<output>
Create `.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-01-SUMMARY.md` when done.
</output>
