---
phase: 01-add-voluntary-stripe-support-payment-and-email-recovery
plan: 05
type: execute
wave: 4
depends_on: [01-03, 01-04]
files_modified:
  - src/web/App.vue
  - src/web/components/IdentityHeader.vue
  - src/web/components/SupportDialog.vue
  - src/web/styles.css
  - tests/integration/support-dialog.spec.ts
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/support-recovery.spec.ts
  - tests/e2e/package-assets.spec.ts
  - services/support/scripts/test-postgres.mjs
  - scripts/test-support-e2e-postgres.mjs
  - scripts/verify-production-artifacts.mjs
  - docs/support-service-operations.md
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
must_haves:
  truths:
    - "After the real review workspace is ready, an unverified installation sees one candid optional dialog with Support Cumpa — $49.99 primary, Restore support and Not now secondary (D-01, D-02, D-04, SUP-01, SUP-02)."
    - "Not now closes for the current process session only, never reopens automatically that session, and leaves every review function usable (D-03, PAY-02, SUP-03)."
    - "Checkout opens a second tab while workspace/dialog remain; the dialog waits cancellably and never offers an I-paid claim or labels cancellation, abandonment, timeout, network, or delay as payment failure (D-05, D-06, D-08, PAY-03)."
    - "Verified payment or recovery alone shows a 1500 ms thank-you, closes automatically, persists despite late verification, and suppresses later launch prompts (D-07, D-08, SUP-04, SUP-05)."
    - "Recovery asks for email again, reveals no payer identity/existence, and works on second and third isolated installations (D-10, REC-01–03)."
    - "A quiet Support Cumpa menu action opens payment/recovery/status UI on demand; no supporter badge or persistent review-surface status exists (D-09, D-12)."
    - "Packaged payment and recovery E2E run the real hosted service against the deployment migration on a fresh loopback-only PostgreSQL 17.6 container; no fake or in-memory store can supply authority."
    - "The packed npm artifact contains no hosted service source, env/config secrets, email/database credentials, or Stripe secret patterns (PAY-04)."
  artifacts:
    - path: "src/web/components/SupportDialog.vue"
      provides: "accessible invitation, waiting, recovery, verified, and thank-you states"
      contains: "Support Cumpa — $49.99"
    - path: "src/web/App.vue"
      provides: "workspace-ready prompt, session dismissal, foreground/background polling, late verification, auto-close"
      contains: "SupportDialog"
    - path: "scripts/test-support-e2e-postgres.mjs"
      provides: "reusable loopback PostgreSQL lifecycle, hosted migration/env injection, packaged Playwright execution, and unconditional teardown"
    - path: "tests/e2e/support-payment.spec.ts"
      provides: "generated-package payment/waiting/late verification and no-gating proof"
    - path: "tests/e2e/support-recovery.spec.ts"
      provides: "second/third installation recovery proof"
    - path: "docs/support-service-operations.md"
      provides: "exact sandbox/live configuration, migration, webhook, email, backup, rotation, replay, and smoke procedure"
  key_links:
    - from: "src/web/App.vue"
      to: "src/web/api/client.ts"
      via: "workspace-ready status plus one guarded poll lifecycle"
      pattern: "getSupportStatus|refreshSupportStatus|getSupportRecoveryStatus"
    - from: "src/web/components/SupportDialog.vue"
      to: "src/web/App.vue"
      via: "typed emits for checkout, recovery, dismiss, close and focus restoration"
      pattern: "checkout|recovery|dismiss|close"
    - from: "tests/e2e/support-payment.spec.ts"
      to: "dist/npm package"
      via: "generated package launch with real hosted service on migrated PostgreSQL and a real authenticated browser"
      pattern: "npm pack"
    - from: "scripts/test-support-e2e-postgres.mjs"
      to: "services/support/scripts/migrate.mjs"
      via: "shared test-postgres lifecycle supplies one ephemeral DATABASE_URL to the deployment migration and Playwright-hosted service"
      pattern: "postgres:17\\.6-alpine|DATABASE_URL|migrate\\.mjs|playwright"
    - from: "tests/e2e/package-assets.spec.ts"
      to: "package.json files allowlist"
      via: "tar inventory and content secret scan"
      pattern: "services/support|STRIPE_WEBHOOK_SECRET"
  prohibitions:
    - "HIGH: No UI action, redirect, tab close, success query, local mutation, or client response may claim payment; thank-you requires local API verified state."
    - "HIGH: No Stripe/email/database credential, `.env` file, hosted source, or production-shaped secret fixture may enter `dist/` or npm tarball."
    - "Payment must not disable, hide, intercept, or condition any review/draft/export/finish capability (PAY-02)."
    - "HIGH: Packaged E2E authority must never use a fake, mocked, browser-only, or in-memory persistence implementation; payment/recovery writes and reads use the migrated ephemeral PostgreSQL database."
    - "No persistent supporter badge, payer email, repository-local support file, I-paid action, payment-failed state, timer-delayed initial prompt, or persisted dismissal is allowed (D-01, D-03, D-08–D-11)."
---

<objective>
Finish the phase end to end with workspace-ready accessible UI, verified-only waiting/recovery transitions, late fulfillment, package secret safety, and concrete deployment/configuration proof.

Purpose: Turn the hosted/local authority into the exact voluntary user journey without adding a license gate or contaminating the release artifact.
Output: Support dialog/menu orchestration, focused browser/package tests, production artifact scan, and operator runbook.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-CONTEXT.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-RESEARCH.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-PATTERNS.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-03-SUMMARY.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-04-SUMMARY.md
@src/web/App.vue
@src/web/components/IdentityPanel.vue
@src/web/components/IdentityHeader.vue
@src/web/styles.css
@tests/e2e/package-assets.spec.ts
@scripts/verify-production-artifacts.mjs

<interfaces>
SupportDialog props: `open:boolean`, `mode:'invitation'|'waiting'|'recovery'|'recoveryPending'|'verified'|'thankYou'`, `busy:boolean`; emits `checkout`, `restore`, `submitRecovery(email)`, `dismiss`, `close`; expose `focusInitial()`.
App state: `supportStatus:'loading'|'unverified'|'verified'|'unavailable'`; `supportDialogMode`; `dismissedForSession:boolean`; `supportOnDemand:boolean`; one in-flight refresh, one AbortController/timer, 1500 ms thank-you timer.
Workspace-ready means session and draft initialization have completed and `primarySurface === 'workspace'`; never a wall-clock timer from mount.
Polling: immediate after Checkout/recovery, then 2/3/5/8/10 seconds, every 15 seconds while visible waiting; after close/dismiss keep 30-second process-background refresh and refresh on `visibilitychange`; clear all on unmount. Next unverified launch refreshes hosted status before/while prompting so late fulfillment persists.
Accessibility: `role=dialog`, `aria-modal=true`, labelled heading/status, initial focus, Tab containment, Escape/close, inert workspace, focus restoration to opener, one authoritative polite live announcement.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add accessible workspace-ready support journey</name>
  <files>src/web/App.vue, src/web/components/IdentityHeader.vue, src/web/components/SupportDialog.vue, src/web/styles.css, tests/integration/support-dialog.spec.ts</files>
  <read_first>
    - src/web/components/IdentityPanel.vue — modal semantics, focus trap, initial focus, explicit close
    - src/web/components/IdentityHeader.vue — quiet disclosure trigger and focus restoration seam
    - src/web/App.vue — primarySurface readiness, identity modal, patch-status polling, visibility cleanup
    - src/web/api/client.ts — support methods created in Plan 01-04
    - .planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-CONTEXT.md — D-01 through D-12
  </read_first>
  <action>Create `SupportDialog.vue` with the exact interface above and compact copy: Cumpa stays fully usable; one optional $49.99 payment supports development; paying once stops the launch prompt. Primary label is exactly `Support Cumpa — $49.99`; Restore support and Not now are secondary. Reuse IdentityPanel's dialog/focus mechanics, not its identity semantics. Integrate after actual workspace/draft readiness per D-01. `Not now` sets only process-memory dismissal and closes; on-demand menu ignores that dismissal. Checkout uses `window.open(url,'_blank','noopener,noreferrer')`, keeps dialog open in cancellable Waiting for confirmation state, and never interprets navigation/tab state as proof. Add the exact guarded polling lifecycle above; only API verified changes mode to thankYou, announce once, wait 1500 ms, then close/persisted future suppression. D-08 requires waiting close plus continued 30-second/visibility background sync and no failure copy. Recovery collects email in a non-retained input, submits once, clears it after request, shows generic check-email/pending/expired copy, and uses the same verified thank-you. Add a quiet `Support Cumpa` button/menu item adjacent to IdentityHeader controls; verified status appears only inside on-demand dialog. No badge or review capability conditional. Add focused integration browser tests for invitation timing/hierarchy, dismissal, waiting/late verification, recovery vocabulary, focus/inert/escape/restoration, 1500 ms auto-close, menu opening, and no-gating. Use fake timers/provider calls only around the real mounted UI boundary.</action>
  <verify>
    <automated>npx playwright test --config=tests tests/integration/support-dialog.spec.ts</automated>
  </verify>
  <done>
    - D-01–D-12 each has an observable passing assertion in `support-dialog.spec.ts`.
    - Prompt never appears over loading/recovery surfaces and never reopens automatically after Not now in one process.
    - Waiting/recovery thank only verified API status; late verified state persists even after dialog close.
    - Every existing review control remains reachable/unchanged after dismissing; no supporter badge exists.
    - Modal keyboard, inert, focus restoration, and live status behavior pass.
  </done>
</task>

<task type="auto">
  <name>Task 2: Prove generated-package payment, recovery, and secret safety</name>
  <files>services/support/scripts/test-postgres.mjs, scripts/test-support-e2e-postgres.mjs, tests/e2e/support-payment.spec.ts, tests/e2e/support-recovery.spec.ts, tests/e2e/package-assets.spec.ts, scripts/verify-production-artifacts.mjs</files>
  <read_first>
    - tests/e2e/package-assets.spec.ts — existing generated package/tar inventory pattern
    - services/support/scripts/test-postgres.mjs — existing executable Docker lifecycle and focused Vitest forwarding owned by Plans 01-02/01-03
    - playwright.config.ts — packaged executable fixture convention
    - scripts/verify-production-artifacts.mjs — current artifact allowlist/integrity checks
    - package.json — `files` allowlist and prepack behavior
    - services/support/src/app.ts — real hosted application boundary and injectable external-provider seams
    - src/server/support-store.ts — isolated machine state root seam
  </read_first>
  <action>Add the smallest reusable root E2E wrapper `scripts/test-support-e2e-postgres.mjs`. Avoid a second Docker lifecycle by generalizing `services/support/scripts/test-postgres.mjs` to export one `withTestPostgres` lifecycle while preserving its direct executable behavior and optional Vitest-path forwarding from Plan 01-03; the new wrapper imports that helper and supplies the Playwright child callback. The lifecycle must start a uniquely named `postgres:17.6-alpine` container with `POSTGRES_USER=cumpa`, `POSTGRES_PASSWORD=cumpa`, `POSTGRES_DB=cumpa_test`, and `-p 127.0.0.1::5432`; discover the assigned loopback port with `docker port`; wait for `docker exec <name> pg_isready -U cumpa -d cumpa_test`; build the ephemeral URL; run the real deployment command `node services/support/scripts/migrate.mjs` with `DATABASE_URL` set; then spawn `npx playwright test` with the wrapper's forwarded spec arguments. Pass that same `DATABASE_URL` and `TEST_DATABASE_URL` into the Playwright process, and require the Playwright hosted-service fixture to start the real `services/support/src/app.ts` with exact configuration `NODE_ENV=test`, loopback `HOST=127.0.0.1`, ephemeral `PORT`, `DATABASE_URL`, test-only `STRIPE_API_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID`, `STRIPE_PAYMENT_LINK_ID`, `EMAIL_LOOKUP_HMAC_KEY`, `RECOVERY_TOKEN_HMAC_KEY`, its loopback `PUBLIC_BASE_URL`, `RESEND_API_KEY`, and `EMAIL_FROM`. The packaged local-service fixture receives `CUMPA_SUPPORT_SERVICE_URL` from that hosted origin and the fixed test `CUMPA_SUPPORT_PAYMENT_URL`. External-provider seams may inject Stripe Session retrieval and capture Resend fetch, but all entitlement, binding, event, challenge, rate-limit, and recovery authority reads/writes must use that PostgreSQL URL. Use argument arrays and inherited stdio, propagate nonzero migration/Playwright exits, and guarantee `docker rm -f <name>` in `finally` plus signal handlers on success, assertion failure, migration failure, child failure, or interruption. Do not implement fake, mocked, browser-only, or in-memory authority persistence.

Add generated-package Playwright payment proof using an isolated machine-state root and that real hosted Fastify/PostgreSQL harness. Launch unpaid repository → workspace-ready prompt → open fixed Checkout URL with exact installation reference. Build a complete `checkout.session.completed` JSON payload, sign its untouched bytes using `stripe.webhooks.generateTestHeaderString` and the test endpoint secret, send those exact bytes through `POST /v1/stripe/webhook`, and let the server-side injected Stripe client retrieve the Session with expanded line items before any grant. Assert the retrieval call—not event fields—supplies configured Payment Link/Price, quantity, USD 4999, paid state, email, and installation. Prove no entitlement/status change for missing signature, the same signature with one mutated payload byte, success redirect/query, client/local response, Checkout tab close, cancellation, abandonment, timeout, or direct browser assertion; tests must not seed entitlement/binding rows or call any status-toggle helper. Only the signed route may grant. Then local poll → thank-you/auto-close → different repository/same machine root has no prompt; late signed webhook after close suppresses next launch. Recovery E2E may seed only the prerequisite paid entitlement through that same signed webhook, then exercises request → captured email URL → inert GET → consuming POST → poll/persist; replay/cross-install fail and second/third installations restore. Extend package/tar scans for hosted source and credential absence. Run only the three named Playwright files through the PostgreSQL wrapper and then the artifact verifier.</action>
  <verify>
    <automated>node scripts/test-support-e2e-postgres.mjs tests/e2e/support-payment.spec.ts tests/e2e/support-recovery.spec.ts tests/e2e/package-assets.spec.ts &amp;&amp; node scripts/verify-production-artifacts.mjs</automated>
  </verify>
  <done>
    - The wrapper applies the real hosted migration to fresh PostgreSQL 17.6, gives Playwright and the hosted service the same database URL and complete test environment, and removes the container on success or any failure/signal.
    - Generated-package payment flow proves raw signed webhook authority, late verification, atomic machine persistence, and cross-repository suppression without direct grant seeding.
    - Generated-package recovery proves generic responses, GET inertness, POST single use, second/third installation restoration, and no local email.
    - Unverified, waiting, offline, and verified launches expose identical review capability behavior.
    - Tar inventory/content scan proves hosted source and every secret/config credential class absent.
    - Payment, recovery, and status authority is persisted only in PostgreSQL; substituting fake or in-memory authority storage fails the E2E contract.
    - Any HIGH authority or secret-scan finding blocks completion.
  </done>
</task>

<task type="auto">
  <name>Task 3: Record reproducible live configuration and smoke proof</name>
  <files>docs/support-service-operations.md</files>
  <read_first>
    - render.yaml — exact service/database/env declaration
    - services/support/src/config.ts — required hosted variables
    - services/support/migrations/001_init.sql — migration/backup scope
    - services/support/src/routes/stripe-webhook.ts — webhook endpoint/events
    - services/support/src/routes/recovery.ts — public recovery routes
    - .planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-RESEARCH.md — Deployment Configuration Requirements
  </read_first>
  <action>Create `docs/support-service-operations.md` because deployment proof is phase scope. Record exact Render Blueprint deployment, `preDeployCommand` migration, health check, every server-only/public variable name and source, separate test/live Stripe endpoint secrets, fixed card-only USD 49.99 Price/Payment Link settings (quantity one; no delayed methods, discounts, automatic tax, optional items, or adjustable quantity), Resend verified sender, PostgreSQL backup/restore check, key rotation, redacted logging/rate limits, and idempotent Stripe replay. Document precise test-mode smoke: complete the configured Payment Link with a test card and unique installation reference; capture the actual signed `checkout.session.completed` at deployed `POST /v1/stripe/webhook`; record event ID, HTTP 200, retrieved Session/line-item invariant log, and subsequent verified status; demonstrate redirect/tab close alone leaves a different installation unverified. Submit a paid recovery email; record generic 202, Resend delivery ID, receipt, inert magic GET, consuming POST, verified poll/status, second POST rejection, and redacted logs. Do not claim completion from Stripe CLI synthetic events alone because provider Checkout/Payment Link configuration is part of the proof.</action>
  <verify>
    <automated>npm --prefix services/support test -- --run tests/fulfillment.test.ts tests/recovery.test.ts tests/deployment.test.ts &amp;&amp; npm --prefix services/support run test:postgres &amp;&amp; node scripts/verify-production-artifacts.mjs</automated>
  </verify>
  <done>
    - Fresh operator can configure Render, PostgreSQL, Stripe, and Resend without guessing an env var, dashboard setting, migration, route, event, or rotation/replay action.
    - Automated hosted authority/recovery/deployment and artifact evidence passes.
    - Deployed provider smoke remains incomplete until the following checkpoint captures actual Stripe test-mode Payment Link webhook/status evidence and actual Resend delivery/magic-link restoration evidence.
    - No unresolved HIGH finding remains.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Approve deployed Stripe and Resend proof</name>
  <files>docs/support-service-operations.md</files>
  <what-built>Render Node service and PostgreSQL migration, card-only USD 49.99 Stripe test-mode Payment Link/webhook fulfillment, Resend recovery delivery, and the operations evidence record.</what-built>
  <action>Perform the five deployed-provider checks below and record only redacted evidence in the operations document; keep the checkpoint blocked unless every Stripe and Resend observation succeeds.</action>
  <how-to-verify>
    1. Open the deployed Cumpa workspace on an unverified installation and complete the configured Stripe test-mode Payment Link using its USD 49.99 card payment.
    2. Confirm the evidence record contains the real Stripe event ID, a 200 response from deployed `POST /v1/stripe/webhook`, server-side Session/line-item retrieval with redacted invariant success, and `GET /v1/installations/:installationId/status` changing to `verified`.
    3. Use a different installation to close, redirect, or abandon Checkout without payment and confirm status remains `unverified`.
    4. Start recovery with the paid email and confirm the browser sees generic 202 while Resend records delivery. Open the received magic link with GET and confirm status remains pending; submit its POST and confirm poll plus installation status become verified.
    5. Submit the same magic link again and confirm it cannot grant again. Inspect Render logs and recorded evidence for absence of plaintext email, raw token, signature, API key, or database URL.
  </how-to-verify>
  <verify>
    <automated>test -s docs/support-service-operations.md</automated>
  </verify>
  <done>Human approval records real Stripe event/status and Resend delivery/single-use recovery evidence; without approval Phase 01 remains blocked.</done>
  <resume-signal>Type `approved` only with the Stripe event/status evidence and Resend delivery/single-use recovery evidence recorded in `docs/support-service-operations.md`; otherwise describe the failed step and keep Phase 01 blocked.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `src/web/components/SupportDialog.vue` — invitation/waiting/recovery/verified/thank-you dialog and `focusInitial`.
- `src/web/App.vue` — support state machine, workspace-ready prompt, session dismissal, guarded polling, late verification, thank-you auto-close.
- `src/web/components/IdentityHeader.vue` — quiet `Support Cumpa` on-demand trigger and focus exposure.
- `src/web/styles.css` — responsive modal/action/status styling using existing semantic tokens.
- `tests/integration/support-dialog.spec.ts` — D-01–D-12 accessible UI contracts.
- `scripts/test-support-e2e-postgres.mjs` — reusable migrated PostgreSQL lifecycle and packaged Playwright runner with unconditional cleanup.
- `services/support/scripts/test-postgres.mjs` — shared `withTestPostgres` lifecycle while retaining its executable focused-Vitest interface.
- `tests/e2e/support-payment.spec.ts` — packed payment/late-verification/no-gating journey.
- `tests/e2e/support-recovery.spec.ts` — packed multi-install recovery journey.
- `tests/e2e/package-assets.spec.ts`, `scripts/verify-production-artifacts.mjs` — npm secret/source exclusion gates.
- `docs/support-service-operations.md` — exact production/sandbox configuration and operational proof.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| UI action/navigation → support state | Presentation must never become payment authority. |
| Generated npm artifact → user machine | Published bytes must contain public client code only, no hosted source/secret. |
| Operator dashboards → hosted runtime | Secrets/configuration cross manual setup boundary and must be exact, isolated, and tested. |
| Real provider sandbox → end-to-end flow | Signed webhook/email proof validates deployed integration beyond injected seams. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-01-05-FG | Elevation of Privilege | HIGH | Browser payment UI | mitigate | No claim action; only local API verified state triggers thank-you/persistence; packed E2E proves redirects/tab behavior cannot grant. |
| T-01-05-PK | Information Disclosure | HIGH | npm tarball | mitigate | Files allowlist plus tar inventory/content scan and production artifact verifier block hosted source/env/credential classes. |
| T-01-05-UI | Spoofing | HIGH | Success/failure presentation | mitigate | Verified-only thank-you; cancellation/outage/delay remain neutral waiting/closed states; browser assertions cover. |
| T-01-05-NG | Elevation of Privilege/DoS | HIGH | Review workspace | mitigate | Support state controls prompt only; packed tests compare capabilities across all states. |
| T-01-05-CF | Tampering | HIGH | Live provider config | mitigate | Exact Price/link/event/env/runbook plus sandbox signed-webhook smoke required before release. |
| T-01-05-AX | Denial of Service | MEDIUM | Modal accessibility | mitigate | Focus containment, inert background, Escape/close, restoration, live-region and full dismissal coverage. |

All HIGH findings block phase/release completion. None may be accepted based on voluntary/non-gating impact.
</threat_model>

<verification>
Run only the named integration/E2E files, focused hosted tests, and production artifact verifier. Execute documented sandbox smoke when credentials are available and record observed output. Skip formatters, linters, builds, and broad test suites except builds that existing package Playwright fixtures necessarily perform.
</verification>

<success_criteria>
- Every D-01–D-12 decision is visible and proven in executable plan/tests.
- PAY-01–04, SUP-01–05, REC-01–03 work end to end through hosted, local, and browser boundaries.
- Payment changes only automatic prompt visibility; every review feature remains available.
- No redirect/client/local claim grants payment and no payer email persists locally.
- Package artifact contains no secret or hosted authority source.
- Deployment/configuration and sandbox proof are reproducible; every HIGH threat is closed.
</success_criteria>

<output>
Create `.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-05-SUMMARY.md` when done.
</output>
