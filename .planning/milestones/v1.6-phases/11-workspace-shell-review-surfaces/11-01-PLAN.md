---
phase: 11-workspace-shell-review-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/css-token-contract.mjs
  - scripts/verify-semantic-css.mjs
  - src/web/styles.css
  - src/web/App.vue
  - src/web/components/ui/ModalDialog.vue
  - src/web/components/SupportDialog.vue
  - tests/e2e/responsive-session.spec.ts
autonomous: true
requirements: [SHELL-03, SHELL-04, REV-03, REV-04]

must_haves:
  truths:
    - "The stylesheet, the shell script gate, and App.vue agree on exactly three viewport breakpoints — 760, 1050, 1650 — and those match the values DiffWorkspace.vue already uses."
    - "A reviewer at a wide viewport sees a wider changed-files sidebar than at a compact desktop viewport, and no sidebar track at the narrow viewport, driven by one declared token."
    - "The support dialog still traps Tab in a tight cycle, still marks the review shell inert, still closes on Escape, and still returns focus to its opener — through one shared primitive instead of its own hand-rolled trap."
    - "npm run verify:semantic-css passes with the new tokens declared, consumed, and allowlisted."
  artifacts:
    - path: "src/web/components/ui/ModalDialog.vue"
      provides: "Single modal host owning role=dialog, aria-modal, Tab containment over all focusable control types, local Escape, initial focus, and focus return"
      min_lines: 60
      exports: ["default"]
    - path: "scripts/css-token-contract.mjs"
      provides: "CANONICAL_TOKENS extended with the four deferred geometry tokens"
      contains: "--sidebar-width"
    - path: "scripts/verify-semantic-css.mjs"
      provides: "Overlay allowlist and responsive-query pins realigned to the unified breakpoints"
      contains: "overlayAllowlist"
    - path: "src/web/styles.css"
      provides: "Canonical :root geometry tokens plus the unified responsive blocks"
      contains: "--sidebar-width"
  key_links:
    - from: "src/web/styles.css"
      to: "scripts/css-token-contract.mjs"
      via: "assertTokenRoot compares the :root declaration set against CANONICAL_TOKENS exactly"
      pattern: "--sidebar-width"
    - from: "src/web/components/SupportDialog.vue"
      to: "src/web/components/ui/ModalDialog.vue"
      via: "SupportDialog renders its content inside ModalDialog instead of its own section/containFocus pair"
      pattern: "ModalDialog"
    - from: "src/web/App.vue"
      to: "src/web/components/DiffWorkspace.vue"
      via: "both read the same narrow cutoff through window.matchMedia"
      pattern: "max-width: 760px"
---

<objective>
Establish the coordinate system every later Phase 11 plan measures against, and pre-extract the one primitive that two later plans would otherwise both create.

Three deliverables, all blocking:
1. Declare the four geometry tokens Phase 08 deferred to this phase — each with its consumer in the same change, because the drift gate rejects orphan tokens.
2. Collapse the four disagreeing breakpoint systems onto the three values `DiffWorkspace.vue:294-295` already uses.
3. Extract `ModalDialog.vue` from the more complete of the two hand-rolled focus traps and prove it against the shipped support-dialog contract.

Purpose: plans 02-06 assert geometry. Doing this second means measuring twice. And `ModalDialog.vue` is the only file plan 03 and plan 04 would both need to create.
Output: four canonical tokens, three breakpoints, one modal primitive, a green drift gate.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md
@.planning/phases/11-workspace-shell-review-surfaces/11-PATTERNS.md
@mockups/01-quiet-workspace.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the four deferred geometry tokens with their consumers</name>
  <files>scripts/css-token-contract.mjs, src/web/styles.css</files>
  <read_first>
    - `scripts/css-token-contract.mjs:1-28` — the `CANONICAL_TOKENS` array opens at line 1 and closes at line 27; `COLOR_TOKEN` is line 28. `--control-height-standard` is already present on line 3.
    - `scripts/verify-semantic-css.mjs:60-65` (`rootRule`) and `:406` (`assertTokenRoot(sourceRoot, 'source CSS', true)`) — the gate enforces an exact set and rejects any `:root` nested inside a grouping at-rule.
    - `src/web/styles.css:1` — the single canonical `:root`. `src/web/styles.css:129` — `--control-height-standard: 36px`, the neighbour to file beside.
    - `src/web/styles.css:1436-1448` — `.review-shell` and `.review-shell--files-collapsed` grid tracks.
    - `src/web/styles.css:2437` and `:2488` — the two narrow sidebar literals.
    - `src/web/styles.css:695-714` — `.support-dialog-backdrop` / `.support-dialog`, the shipped modal-geometry precedent.
    - `mockups/01-quiet-workspace.html:9-12` — the normative numeric source.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md` — Canonical Geometry Tokens section.
  </read_first>
  <action>
    Add exactly four names to `CANONICAL_TOKENS` in alphabetical position: `--control-height-compact`, `--dialog-max-height`, `--dialog-width`, `--sidebar-width`. None of them match the `COLOR_TOKEN` regex on line 28, so `assertCanonicalTokenValues` will not demand a hex/rgb value — do not invent one.

    Declare all four in the canonical `:root` of `src/web/styles.css`, taking values from `mockups/01-quiet-workspace.html`: the sidebar grid track from the `.workspace` rule on line 9, the dialog width and max-height from the `dialog` rule on line 9, and the compact control height from the `.filters button` / `.change-nav button` rules on line 9. Declare them in the root only — a redeclaration inside a grouping at-rule is what `rootRule()` rejects.

    Wire every token to a consumer in this same change, because an orphan token fails the gate:
    - `.review-shell` grid track: replace the literal sidebar column with `var(--sidebar-width)` and drop the trailing rail track entirely — the comments rail becomes an absolute overlay in Task 2, so it must not own a grid column at any width.
    - `.review-shell--files-collapsed`: single content track only.
    - Redeclare `--sidebar-width` (not the grid track) inside the compact-desktop and wide responsive blocks created in Task 2, using the `.workspace` values from `mockups/01-quiet-workspace.html:10` and `:11`.
    - Remove the `.file-tree-pane` fixed `flex-basis`/`width` pair inside the block that opens at `src/web/styles.css:2415`; the pane fills its grid column, which is now the token's job.
    - `--control-height-compact` consumes on the compact toolbar controls only. `--dialog-width` and `--dialog-max-height` consume on the shared modal panel rule you add for `ModalDialog.vue` in Task 3.

    Do not introduce a second palette, a gradient, a colour literal, or a theme wrapper. Do not promote the bounded padding values from the mockup into general spacing tokens.
  </action>
  <acceptance_criteria>
    - `node -e "import('./scripts/css-token-contract.mjs').then(m=>console.log(['--control-height-compact','--dialog-max-height','--dialog-width','--sidebar-width'].every(t=>m.CANONICAL_TOKENS.includes(t))))"` prints `true`.
    - `grep -c -- '--sidebar-width' src/web/styles.css` is at least 4 (one root declaration, at least two responsive redeclarations, at least one consumer).
    - `grep -c 'minmax(0, 1fr) 360px' src/web/styles.css` is `0` (currently 2).
    - `grep -c -- '288px' src/web/styles.css` is `0` (currently 2).
    - `grep -c -- '256px' src/web/styles.css` is `0` (currently 1).
    - `grep -c -- '--dialog-width' src/web/styles.css` and `grep -c -- '--dialog-max-height' src/web/styles.css` are each at least 2 (declaration plus consumer).
    - `grep -c -- '--control-height-compact' src/web/styles.css` is at least 2.
    - `npm run verify:semantic-css` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:semantic-css</automated>
  </verify>
  <done>All four tokens are in `CANONICAL_TOKENS`, declared once in the canonical `:root`, consumed by at least one shipped rule each, and the drift gate passes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Collapse four breakpoint systems onto 760 / 1050 / 1650</name>
  <files>src/web/styles.css, src/web/App.vue, scripts/verify-semantic-css.mjs, tests/e2e/responsive-session.spec.ts</files>
  <read_first>
    - `src/web/components/DiffWorkspace.vue:290-303` — the already-correct system. `:294` reads `(min-width: 1650px)`, `:295` reads `(max-width: 760px)`. This is the target; do not change this file.
    - `src/web/App.vue:1036-1045` (`onMounted`) — three `matchMedia` calls on lines 1038, 1039, 1040 plus `commentsOpen.value = !commentsDrawerMedia.matches` on 1041 and three `addEventListener` calls.
    - `src/web/App.vue:888-896` (`handleViewportChange`) — reads all three medias into `isFilesDrawer`, `isCommentsDrawer`, `isNarrow`, `isCompact`, then calls `dispatchWorkspace({ type: 'resize' })` and `diffWorkspace.value?.layout()`.
    - `src/web/App.vue:1096-1103` (`onBeforeUnmount`) — the symmetric teardown.
    - `src/web/App.vue:156-158` — `identityModal` computed, which reads `isCompact` and `isNarrow`.
    - `src/web/App.vue:1304-1312` — the comments-rail `<aside>` with `v-show="commentsOpen"`, `:inert`, `:aria-hidden`.
    - `src/web/styles.css:2415-2433` — the `768`/`1279` block (header wrap, identity-panel width, file-tree-pane size).
    - `src/web/styles.css:2435-2464` — the `1439` block: `.review-shell` narrow track, `.review-shell--files-collapsed`, the `.comments-rail` absolute-overlay rules, `.comments-rail--open` with `box-shadow: var(--shadow-overlay)` at `:2458`, and `.comments-rail:not(.comments-rail--open) { display: none }`.
    - `src/web/styles.css:2467-2506` — the `1099` block: `.review-shell { display: block }`, `.review-main { height: 100% }`, the `.review-context-header__context` two-row grid at `:2476-2481`, the `.review-files` left-overlay rules, `.review-files--open` with the overlay shadow at `:2501`.
    - `src/web/styles.css:2509` — the `767` block.
    - `scripts/verify-semantic-css.mjs:230-233` — `overlayAllowlist`, seven selectors.
    - `scripts/verify-semantic-css.mjs:247-251` — `fail()` on unallowlisted overlay shadow, then the two hard-coded responsive-query substring pins for `.comments-rail--open` and `.review-files--open`.
    - `tests/e2e/responsive-session.spec.ts:1333-1338` — asserts the rail is visible and 360px wide at width 1440 without any user action.
    - `tests/e2e/responsive-session.spec.ts:1369, 1378, 1428, 1528, 1548` — the five `setViewportSize` boundary probes.
    - `mockups/01-quiet-workspace.html:10-12` — the three normative responsive blocks.
  </read_first>
  <behavior>
    Re-author these assertions first and watch them fail — they are the RED step for this task, and the source edits come after:
    - Test 1: the five `setViewportSize` probes move off the retired widths onto the three declared cutoffs and their immediate neighbours. `:1369` (1439) and `:1378` (1280) become the compact-desktop boundary pair 1051/1050; `:1428` (1279) and `:1528` (1099) become the narrow boundary pair 761/760; `:1548` (768) becomes a below-narrow width. Each probe still asserts the layout its original width asserted.
    - Test 2: `:1333-1338` re-authored. At width 1440 the comments rail is not visible until `Review` is activated; after activation it overlays the right edge at its declared width, and the diff canvas bounding width is identical before and after the activation. The shipped assertion expects the rail visible with no user action, so it must fail first.
    - Test 3: at 1651 the changed-files track is wider than at 1051, and below 760 no persistent sidebar track is rendered at all — one declared token drives all three states.
    - Test 4: the compact-identity state and the files-drawer state flip at the same 760 cutoff, so no viewport width exists where one is narrow and the other is not.
    - Test 5: crossing the 760 cutoff still relayouts the workspace — the diff canvas re-renders at the new width with no manual interaction, proving the surviving `handleViewportChange` still dispatches the workspace resize and requests Monaco layout after three medias collapse to one.
  </behavior>
  <action>
    Three systems change; `DiffWorkspace.vue` is the fixed reference and is not edited.

    **App.vue — collapse three medias to one.** The compact-identity media and the files-drawer media resolve to the same narrow cutoff under the UI-SPEC, and the comments rail becomes an overlay at every width, so its media disappears entirely. Keep one `window.matchMedia('(max-width: 760px)')` and drive `isNarrow`, `isFilesDrawer`, and `isCompact` from it. Delete the comments-drawer media ref, its `onMounted` listener, its read inside `handleViewportChange`, and its `onBeforeUnmount` removal in the same edit — an orphaned listener registration is the failure mode here. Because the rail no longer has a width-conditional default, initialise `commentsOpen` closed and let `Review` open it. Leave `handleViewportChange`'s existing `dispatchWorkspace({ type: 'resize' })` + `diffWorkspace.value?.layout()` pair intact.

    **styles.css — three responsive blocks plus one new wide block.**
    - Retarget the block that opens at `:2415` to a lower bound just above the narrow cutoff and an upper bound at the compact-desktop cutoff. Drop the `.file-tree-pane` sizing pair (Task 1).
    - Dissolve the block that opens at `:2435`. Its `.review-shell` narrow grid track is superseded by the `--sidebar-width` redeclaration from Task 1. Lift the `.comments-rail` overlay rules, `.comments-rail--open`, and `.comments-rail:not(.comments-rail--open)` to top level so the rail overlays the right edge at every width with `--shadow-overlay` when open — that is the REV-02 contract and it is what removes this breakpoint system.
    - Split the block that opens at `:2467`. The `.review-context-header__context` two-row grid at `:2476-2481` is what preserves file → Base → Head reading order; it belongs to the compact-desktop cutoff. Everything that turns the sidebar into an overlay drawer — `.review-shell { display: block }`, `.review-main { height: 100% }`, the `.review-files` overlay rules, `.review-files--open`, `.review-files:not(.review-files--open)` — belongs to the narrow cutoff, because the sidebar stays a real grid column down to the narrow cutoff now.
    - Retarget the block that opens at `:2509` to the narrow cutoff.
    - Add one `min-width` block for the wide cutoff holding only the `--sidebar-width` redeclaration.

    **verify-semantic-css.mjs — the gate is a deliverable, not collateral.** The rail's overlay shadow no longer sits inside any responsive query, and the files drawer's has moved. Drop the rail arm of the responsive-query pin at `:248-251` entirely and retarget the files-drawer arm's substring to the narrow cutoff. Leave `overlayAllowlist` membership unchanged in this task.

    **responsive-session.spec.ts.** Write the assertion changes before the source edits (see `<behavior>`): move the five boundary probes onto the new cutoffs and their immediate neighbours, and re-author `:1333-1338` so it opens the rail before asserting the overlay geometry. Do not touch the typography table rows or the `.session-header h1` assertions — plan 02 owns those.
  </action>
  <acceptance_criteria>
    - `grep -c -- '1099' src/web/styles.css src/web/App.vue` totals `0` (currently 1 each).
    - `grep -c -- '1439' src/web/styles.css src/web/App.vue` totals `0` (currently 1 each).
    - `grep -c -- '767' src/web/styles.css src/web/App.vue` totals `0` (currently 1 each).
    - `grep -c -- '1279' src/web/styles.css` is `0` and `grep -c '(min-width: 768px)' src/web/styles.css` is `0`.
    - `grep -c 'max-width: 760px' src/web/styles.css` is at least 2 and `grep -c 'max-width: 1050px' src/web/styles.css` is at least 1 and `grep -c 'min-width: 1650px' src/web/styles.css` is at least 1.
    - `grep -c 'matchMedia' src/web/App.vue` is exactly `1`, and that call's argument string is `(max-width: 760px)`.
    - `grep -c 'commentsDrawerMedia' src/web/App.vue` is `0`.
    - `grep -c 'comments-rail--open' scripts/verify-semantic-css.mjs` is exactly `1` (the allowlist entry only; the responsive pin arm is gone).
    - `npm run verify:semantic-css` exits 0.
    - `npm run typecheck:web` exits 0.
    - `npm run build:web && npx playwright test tests/e2e/responsive-session.spec.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:semantic-css &amp;&amp; npm run typecheck:web &amp;&amp; npm run build:web &amp;&amp; npx playwright test tests/e2e/responsive-session.spec.ts</automated>
  </verify>
  <done>One media query in App.vue, three responsive cutoffs in styles.css matching `DiffWorkspace.vue:294-295` and the UI-SPEC, a gate whose responsive pins match the stylesheet, and a green responsive spec.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extract ModalDialog.vue and prove it against the shipped support-dialog contract</name>
  <files>src/web/components/ui/ModalDialog.vue, src/web/components/SupportDialog.vue, src/web/styles.css, scripts/verify-semantic-css.mjs</files>
  <read_first>
    - `src/web/components/SupportDialog.vue:30-43` — `containFocus`, the more complete of the two hand-rolled traps. Its `querySelectorAll` selector covers `button` and `input` only.
    - `src/web/components/SupportDialog.vue:53-66` — the backdrop `div`, the `section` carrying `ref`, `role="dialog"`, `aria-modal="true"`, `aria-labelledby`, `aria-describedby`, `@keydown="containFocus"`, `@keydown.esc.prevent="emit('close')"`, and the close button carrying `ref="initial"`.
    - `src/web/components/IdentityPanel.vue:50-58` — the second copy of the same trap. Line 56 queries `button:not(:disabled)` only; a dialog hosting a textarea, a search input, or a link would leak focus. This is the defect to fix while extracting, not to copy.
    - `src/web/App.vue:979-990` — `openSupportDialog` / `closeSupportDialog`, the capture-plus-fallback focus-return shape.
    - `src/web/App.vue:237-241, 251-254` — the capture-only variant. `src/web/App.vue:839-842` — the fixed-trigger variant. Three shapes for one behaviour.
    - `src/web/App.vue:1160` — `:inert="(identityOpen && identityModal) || supportDialogOpen"` on `.review-shell`. Background suppression is `inert`, never `display: none`.
    - `src/web/App.vue:1361-1374` — the canonical overlay mount point: every overlay is a sibling of `.review-shell` under `.session-shell`.
    - `src/web/App.vue:844-856` — `handleKeydown`; line 856 returns early when the event target is an input, textarea, or select. The central Escape chain therefore never reaches a dialog whose focus is inside a form control. Escape must be owned locally.
    - `tests/e2e/responsive-session.spec.ts:1586-1599` — the contract the primitive must not break: `aria-modal="true"`, close button focused on open, `.review-shell` carrying the bare `inert` attribute, a tight Tab wrap from the last control straight back to close with no BODY stop, Escape closing, and focus returning to the disclosure.
    - `src/web/styles.css:675-681` — the shipped overlay-shadow rule group. `src/web/styles.css:695-714` — `.support-dialog-backdrop` and `.support-dialog`, including `width: min(100%, 460px)` and the overlay shadow at `:713`.
    - `scripts/verify-semantic-css.mjs:230-233` — `overlayAllowlist`; a new panel selector using `var(--shadow-overlay)` must be added here or the gate fails.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-RESEARCH.md` Q5b — native `<dialog>` + `showModal()` was rejected on measured evidence: its Tab cycle passes through BODY, breaking the tight-wrap assertion, and it never sets the pinned `inert` attribute on the review shell. Do not reach for it.
  </read_first>
  <behavior>
    Write these as Playwright assertions against the support dialog before editing either component, and confirm the new ones fail for the right reason first:
    - Test 1 (already green, must stay green): `tests/integration/support-dialog.spec.ts` and `tests/e2e/support-payment.spec.ts` pass byte-unchanged. The support dialog's state behaviour and placement do not change.
    - Test 2 (already green, must stay green): `tests/e2e/responsive-session.spec.ts:1586-1599` — tight Tab wrap, `.review-shell[inert]`, Escape close, focus return.
    - Test 3 (new, RED first): with focus on the last control inside the dialog, Shift+Tab from the first control lands on the last control — the reverse arm of the wrap, currently only exercised forward.
    - Test 4 (new, RED first): a dialog whose content includes a `textarea` and an `a[href]` includes both in its Tab cycle. Prove this against a throwaway harness mount of `ModalDialog.vue`; the widened selector is the whole point of the extraction and `IdentityPanel.vue:56` proves the narrow selector ships today.
    - Test 5 (new, RED first): pressing Escape while focus is inside a `textarea` inside the dialog closes it. This is the `App.vue:856` early-return hole.
  </behavior>
  <action>
    Create `src/web/components/ui/ModalDialog.vue` as a single always-modal host. It owns: the backdrop element, the panel carrying `role="dialog"`, `aria-modal="true"`, and an `aria-labelledby` wired to a heading slot; Tab and Shift+Tab containment; a local Escape handler that does not delegate to the central chain; initial focus on its close control; and focus return to the element that was `document.activeElement` at open time, with a caller-supplied fallback when that element is gone. Props carry the accessible name and the close-control copy; a single `close` event reports dismissal. Widen the focusable query beyond `button` and `input` to cover at minimum `textarea`, `select`, `a[href]`, and `[tabindex]:not([tabindex="-1"])`, all with the `:not(:disabled)` guard the current selector already applies.

    Give the primitive one shared panel rule in `src/web/styles.css` consuming `--dialog-width`, `--dialog-max-height`, `--radius-overlay`, `--space-6`, and `var(--shadow-overlay)`, and add that selector to `overlayAllowlist` in `scripts/verify-semantic-css.mjs:230-233` in the same change. The panel owns its own vertical scrolling rather than extending the document.

    Migrate `SupportDialog.vue` to render its existing content inside `ModalDialog.vue`, deleting its local `containFocus` and its own `role`/`aria-modal`/`@keydown` wiring. Its visible copy, its emitted events, its busy and mode states, and its mount position at `App.vue:1361-1371` do not change — this is a focus-trap extraction, not a support redesign. Keep `App.vue`'s `:inert` binding on `.review-shell` responsible for background suppression; the primitive must not use `display: none`, because a zero-height background would clamp the paired anchor zones measured by `DiffWorkspace.vue:105-108` to their 280px floor.

    Leave `IdentityPanel.vue` alone — plan 03 converts it.
  </action>
  <acceptance_criteria>
    - `src/web/components/ui/ModalDialog.vue` exists and `grep -c 'aria-modal' src/web/components/ui/ModalDialog.vue` is at least 1.
    - `grep -Ec 'textarea|a\[href\]' src/web/components/ui/ModalDialog.vue` is at least 1 — the widened focusable query.
    - `grep -c 'containFocus' src/web/components/SupportDialog.vue` is `0`.
    - `grep -c 'ModalDialog' src/web/components/SupportDialog.vue` is at least 1.
    - `grep -c 'role="dialog"' src/web/components/SupportDialog.vue` is `0` — the primitive owns it.
    - `grep -r 'showModal' src/web` prints nothing — native `<dialog>` was rejected on measured evidence, so neither the primitive nor any consumer may reach for it. (`-r` is required: the target is a directory.)
    - `npm run build:web && npx playwright test tests/integration/support-dialog.spec.ts tests/e2e/support-payment.spec.ts` passes with those two spec files unmodified (`git diff --name-only` lists neither).
    - `npm run build:web && npx playwright test tests/e2e/responsive-session.spec.ts` passes, including the identity-sheet trap block at `:1586-1599`.
    - `npm run verify:semantic-css` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:semantic-css &amp;&amp; npm run build:web &amp;&amp; npx playwright test tests/integration/support-dialog.spec.ts tests/e2e/support-payment.spec.ts tests/e2e/responsive-session.spec.ts</automated>
  </verify>
  <done>One modal primitive exists, the support dialog consumes it with its own trap deleted, all five behaviours above hold, and both the support contract specs and the identity-sheet trap block pass unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → browser | Branch labels, worktree paths, file paths, and commit OIDs originate in the reviewed repository and are attacker-influenced when reviewing untrusted code. |
| browser DOM → reviewer attention | Modal surfaces can obscure or misattribute state; a focus trap that leaks control is an availability and integrity issue for keyboard-only reviewers. |
| build gate → shipped stylesheet | `verify-semantic-css.mjs` is the only mechanical barrier against palette drift and unreviewed overlay surfaces. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01-01 | Tampering | `ModalDialog.vue` accessible-name prop | mitigate | Dialog names in this plan are static literals supplied by the calling component, never repository-derived strings. Plan 03 and plan 04, which do render repository identities, route them through `controlSafeDisplay` from `src/domain/path-bytes.ts`. |
| T-11-01-02 | Denial of service | `.review-shell` background suppression | mitigate | Background suppression stays on the `inert` attribute bound at `App.vue:1160`. `display: none` would zero `scrollHeight` inside `DiffWorkspace.vue:105-108` and clamp paired anchor zones to the 280px floor, silently breaking comment alignment. Asserted by the unchanged `.review-shell[inert]` check at `responsive-session.spec.ts:1592`. |
| T-11-01-03 | Elevation of privilege | Focus containment | mitigate | The widened focusable selector plus Shift+Tab containment closes the `IdentityPanel.vue:56` leak where a keyboard reviewer escapes a modal into an inert background and operates controls the modal intended to block. Proven by Tests 3-5. |
| T-11-01-04 | Tampering | `scripts/verify-semantic-css.mjs` | mitigate | The overlay allowlist and responsive pins are edited only to match the stylesheet changes in this plan. No allowlist entry is removed, no check is weakened, and the gate must still exit 0 on every task. |
| T-11-01-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install occurs in this plan. `ModalDialog.vue` is authored from existing in-repo code; no new dependency is added to `package.json`. Verified by `git diff --stat package.json package-lock.json` being empty. |
</threat_model>

<verification>
- `npm run verify:semantic-css` exits 0 after each task.
- `npm run typecheck:web` exits 0.
- `npm run build:web && npx playwright test tests/e2e/responsive-session.spec.ts tests/integration/support-dialog.spec.ts tests/e2e/support-payment.spec.ts` passes.
- `git diff --stat package.json package-lock.json` is empty.
- No `:root` block exists inside any grouping at-rule in `src/web/styles.css` (the gate's own self-check at `scripts/verify-semantic-css.mjs:287-297` covers this).

**Known-red window:** Task 2 edits `src/web/styles.css`, `src/web/App.vue`, `scripts/verify-semantic-css.mjs`, and `tests/e2e/responsive-session.spec.ts` as one unit. The gate's responsive-query pins and the stylesheet are mutually dependent — moving either alone fails `verify:semantic-css`. Do not commit a partial Task 2. `tests/e2e/pinned-session.spec.ts` and `tests/integration/anchored-workspace.spec.ts` are expected to stay green throughout this plan; if either goes red here, the breakpoint move leaked into layout that plan 02 owns.
</verification>

<success_criteria>
- `CANONICAL_TOKENS` contains exactly four more names than before, all four are declared once in the canonical `:root`, and each has at least one shipped consumer.
- `src/web/App.vue` holds exactly one `matchMedia` call, and its query matches `DiffWorkspace.vue:295`.
- `src/web/styles.css` contains no occurrence of the superseded breakpoint numbers and at least one block for each of the three unified cutoffs.
- `scripts/verify-semantic-css.mjs` pins the files-drawer overlay shadow to the narrow cutoff and no longer pins the rail's overlay shadow to any query.
- `src/web/components/ui/ModalDialog.vue` exists, `SupportDialog.vue` consumes it, and no `containFocus` copy remains in `SupportDialog.vue`.
- The support-dialog contract specs pass with zero edits to those spec files.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Produced by |
|---|---|---|
| `src/web/components/ui/ModalDialog.vue` | new component | Task 3 |
| `--sidebar-width`, `--dialog-width`, `--dialog-max-height`, `--control-height-compact` | canonical tokens | Task 1 |
| Unified 760 / 1050 / 1650 breakpoint system across `styles.css`, `App.vue`, and the drift gate | config | Task 2 |
| Realigned `overlayAllowlist` and responsive-query pins in `scripts/verify-semantic-css.mjs` | gate config | Tasks 2, 3 |
| Rewritten breakpoint sweep and rail-geometry assertions in `tests/e2e/responsive-session.spec.ts` | test | Task 2 |

<output>
Create `.planning/phases/11-workspace-shell-review-surfaces/11-01-SUMMARY.md` when done.
</output>
