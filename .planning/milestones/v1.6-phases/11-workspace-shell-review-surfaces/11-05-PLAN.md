---
phase: 11-workspace-shell-review-surfaces
plan: 05
type: execute
wave: 6
depends_on: ["11-01", "11-02", "11-03", "11-04", "11-07"]
files_modified:
  - src/web/components/ChangedFilesDialog.vue
  - src/web/components/FileTree.vue
  - src/web/App.vue
  - src/web/styles.css
  - scripts/verify-semantic-css.mjs
  - tests/e2e/responsive-session.spec.ts
  - tests/e2e/file-tree.spec.ts
  - tests/e2e/pinned-session.spec.ts
autonomous: true
requirements: [SHELL-04]

must_haves:
  truths:
    - "At the narrow cutoff the workspace is one column, no persistent sidebar is rendered, and the Files control opens a modal Changed files dialog."
    - "The dialog hosts the same FileTree instance state the desktop sidebar uses — same filter text, same expansion set, same selection, same roving tabindex — not a second tree."
    - "Initial focus in the dialog lands on the Filter files control; choosing a file closes the dialog, opens the file, and moves focus to the active-file heading once Monaco is ready."
    - "Close, Escape, or cancellation returns focus to the exact Files opener."
    - "Reading order stays file then Base then Head for a pinned comparison, and file then Preimage then Postimage for an exact patch; only the diff viewport scrolls horizontally, never the document."
  artifacts:
    - path: "src/web/components/ChangedFilesDialog.vue"
      provides: "Modal host for the shipped FileTree at the narrow cutoff"
      min_lines: 40
      exports: ["default"]
  key_links:
    - from: "src/web/components/ChangedFilesDialog.vue"
      to: "src/web/components/FileTree.vue"
      via: "the dialog hosts the same FileTree element, swapping its container rather than forking the tree"
      pattern: "FileTree"
    - from: "src/web/components/ChangedFilesDialog.vue"
      to: "src/web/components/ui/ModalDialog.vue"
      via: "focus trap, inert background, local Escape, focus return"
      pattern: "ModalDialog"
    - from: "src/web/App.vue"
      to: "src/web/components/ActiveFileToolbar.vue"
      via: "the Files control opens the dialog and receives focus back on close"
      pattern: "Open changed files"
---

<objective>
SHELL-04: replace the narrow left-slide drawer with a modal Changed files dialog hosting the same tree, and finish the narrow reflow.

The drawer is not merely restyled — it is removed. Its CSS block, its open/close state, its focus-return path, and its entry in the drift gate's overlay allowlist and responsive-query pin all go with it.

Purpose: at the narrow cutoff the diff needs the whole column, and a modal is the only form that keeps the tree's filter input out of the main review region.
Output: one dialog, one drawer deleted, one gate entry retired, one skip link resolved.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md
@.planning/phases/11-workspace-shell-review-surfaces/11-PATTERNS.md
@.planning/phases/09-changed-file-tree/09-UI-SPEC.md
@.planning/phases/11-workspace-shell-review-surfaces/11-04-SUMMARY.md
@mockups/01-quiet-workspace.html
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Host the shipped FileTree in a modal Changed files dialog</name>
  <files>src/web/components/ChangedFilesDialog.vue, src/web/components/FileTree.vue, src/web/App.vue, src/web/styles.css</files>
  <read_first>
    - `src/web/App.vue:1162-1176` — the current sidebar host. `:1163` `v-if="isFilesDrawer || !filesCollapsed"`; `:1165` `class="review-files"`; `:1166` `id="changed-files"`; `:1167-1168` the drawer `inert` and `aria-hidden` bindings; `:1169` `aria-label="Changed files"`; `:1170` `tabindex="-1"`; `:1173` the `Close files` button; `:1174` the `FileTree` mount with its `select` and `activate` handlers; `:1175-1178` the zero-files empty section carrying `id="changed-files-heading"`.
    - `src/web/components/FileTree.vue:148` — `<nav class="file-tree-pane" aria-label="Changed files">`. Together with `App.vue:1165` this is two identically-named nested navigation landmarks today; collapse one during the swap.
    - `src/web/components/FileTree.vue:150-154` — the `h2#changed-files-heading` with `tabindex="-1"`, using `visually-hidden` at `:152` for its accessible text.
    - `src/web/components/FileTree.vue:156-165` — the filter block: `visually-hidden` label at `:158`, and `input#file-tree-filter ref="filterInput" type="search"` at `:159-165`. This is the UI-SPEC's required initial-focus target.
    - `src/web/App.vue:237-241` (`openFiles`) — captures `filesOpener` from `document.activeElement`, sets `filesOpen`, then focuses the drawer container rather than the filter. `:243-249` (`toggleFiles`) — as amended by plan 02. `:251-254` (`closeFiles`) — returns focus to `filesOpener`.
    - `src/web/App.vue:1361-1374` — the overlay sibling band. `tests/e2e/pinned-session.spec.ts:1418` counts zero `input`, `textarea`, or `select` inside `main.review-main`, so a dialog carrying `#file-tree-filter` must mount here, exactly as `SupportDialog` does.
    - `tests/integration/anchored-workspace.spec.ts:1203-1208` — the `.review-main` overflow canary excludes only `.diff-workspace__viewport` and the `sr-only` class. `FileTree.vue:152,158` use `visually-hidden`, which is safe only while the tree is outside `.review-main` — true in both the sidebar and the sibling band, and the reason the dialog cannot move inside the main region.
    - `src/web/App.vue:856` — `handleKeydown` returns early for input, textarea, and select targets. A dialog focused on `#file-tree-filter` gets no Escape from the central chain; plan 01's primitive owns Escape locally.
    - `src/web/components/ui/ModalDialog.vue` — the primitive. Its widened focusable query is what lets a `type="search"` input participate in the Tab cycle.
    - `mockups/01-quiet-workspace.html:9` — `.mobile-files-list { max-height: 50dvh; overflow: auto }`, the results-scroll cap. `:12` — the narrow block where `.workspace` collapses to one column and `.sidebar` is not rendered.
    - `.planning/phases/09-changed-file-tree/09-UI-SPEC.md` — the tree contract that must not regress: sorting, compaction, filtering, selection, roving tabindex.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md` — the Narrow viewport section and the file-toggle copy table.
  </read_first>
  <behavior>
    Write these first and watch them fail:
    - Test 1: at the narrow cutoff, activating `Files` opens a modal dialog named `Changed files`, and initial focus is on the `Filter files` control.
    - Test 2: the dialog and the desktop sidebar show the same tree state — type a filter and expand a directory on the desktop layout, resize to narrow, open the dialog, and both the filter text and the expansion set are intact.
    - Test 3: the page contains exactly one navigation landmark named `Changed files` while the dialog is open — not two nested ones.
    - Test 4: Escape closes the dialog while focus is inside the filter input.
    - Test 5: the results list scrolls within its own cap while the dialog title, filter, and actions stay reachable; the document gains no vertical or horizontal overflow.
    - Test 6: `main.review-main` still contains zero `input`, `textarea`, or `select` while the dialog is open.
  </behavior>
  <action>
    Create `ChangedFilesDialog.vue` rendering inside `ModalDialog.vue`, named `Changed files`, with the close control copy and accessible name from the UI-SPEC file-toggle table. It hosts the same `FileTree` element the sidebar hosts — swap its container, do not fork the component and do not mount a second instance with its own state. Set initial focus to `#file-tree-filter` rather than to the container.

    Collapse the duplicate navigation landmark. `FileTree.vue:148` already declares a `nav` named `Changed files`; the outer wrapper in `App.vue` declares a second. Keep exactly one, and keep `id="changed-files"` on whichever element the toolbar's `aria-controls` points at while a persistent host exists.

    Mount the dialog in the `.session-shell` overlay sibling band with the other dialogs. Give the results region its own scroll with the cap from the mockup so the title, filter, and actions stay reachable.

    Delete the narrow drawer path: the drawer close button, the `filesOpen` drawer state, and the `v-if`/`inert`/`aria-hidden` combination that served it. The persistent desktop sidebar keeps its `v-if` hide from plan 02 — that is SHELL-03 and is unaffected.
  </action>
  <acceptance_criteria>
    - `src/web/components/ChangedFilesDialog.vue` exists, `grep -c 'ModalDialog' src/web/components/ChangedFilesDialog.vue` is at least 1, and `grep -c 'FileTree' src/web/components/ChangedFilesDialog.vue` is at least 1.
    - `grep -c 'file-tree-filter' src/web/components/ChangedFilesDialog.vue` is at least 1 — the initial-focus target.
    - `grep -c 'aria-label="Changed files"' src/web/App.vue` is `0` — the outer duplicate landmark is gone; `grep -c 'aria-label="Changed files"' src/web/components/FileTree.vue` is exactly `1`.
    - `grep -c 'Close files' src/web/App.vue src/web/components/ChangedFilesDialog.vue` totals `0` — replaced by the UI-SPEC copy.
    - `grep -c 'drawer-close' src/web/App.vue src/web/styles.css` totals `0`.
    - Reading `src/web/App.vue` after the edit shows the `ChangedFilesDialog` mount as a direct child of `.session-shell`, not inside `.review-main` or `.review-shell`.
    - `npm run typecheck:web` exits 0.
    - `npm run build:web && npx playwright test tests/e2e/file-tree.spec.ts` passes unchanged — the Phase 09 tree contract does not regress.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web &amp;&amp; npm run build:web &amp;&amp; npx playwright test tests/e2e/file-tree.spec.ts</automated>
  </verify>
  <done>Behaviours 1-6 hold, one navigation landmark named `Changed files` exists, the tree is hosted not forked, and Phase 09's spec passes with zero edits.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Selection flow, focus return, and narrow reading order</name>
  <files>src/web/App.vue, src/web/styles.css, tests/e2e/responsive-session.spec.ts, tests/e2e/pinned-session.spec.ts</files>
  <read_first>
    - `src/web/App.vue:292-318` (`loadFile`) — the selection path the dialog's `select` and `activate` handlers reach, including the `requestVersion` stale-response guard and the loading flag the "after Monaco is ready" condition depends on.
    - `src/web/components/ActiveFileToolbar.vue` (created in plan 02) — the heading carrying `id="cumpa-heading"` and the three-state file toggle with its UI-SPEC accessible names.
    - `src/web/App.vue:1126-1128` — the three skip links. `:1126` targets `#changed-files-heading`, which at the narrow cutoff now lives inside a closed dialog and is unreachable. This is the deliberate resolution required by this plan.
    - `src/web/components/DiffWorkspace.vue:290-303` — `onMounted` including the `ResizeObserver` that plan 02 taught to re-measure; `:295` is the narrow media the component reads for its own density.
    - `src/web/styles.css` — the compact-desktop block holding `.review-context-header__context`'s two-row grid, retargeted by plan 01 and moved into the active-file toolbar's stylesheet by plan 02. This is what preserves file → Base → Head order under reflow.
    - `.planning/phases/10-diff-reading-surface/10-UI-SPEC.md:46-55` — the 640px minimum side-by-side Monaco canvas, a deliberate inherited departure. Narrow layout scrolls only the diff viewport; Phase 11 must not convert it to a unified diff.
    - `tests/e2e/responsive-session.spec.ts:1096, 1137, 1203, 1282, 1529` — `Files` button sites; `:1558, 1571, 1603` — narrow viewport probes; `:1604-1607` — the narrow open/close round trip.
    - `tests/e2e/pinned-session.spec.ts:1418` — the editable-control count inside `main.review-main`.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md` — Narrow viewport bullets, and the Accessibility Contract clause that layout changes never reset focus to `body`.
  </read_first>
  <behavior>
    Write these first and watch them fail:
    - Test 1: choosing a file in the dialog closes the dialog, opens that file in the diff, and moves focus to the active-file heading — after the file is ready, not before.
    - Test 2: closing with the close control, with Escape, or by cancelling returns focus to the exact `Files` control that opened the dialog. Focus never lands on `body`.
    - Test 3: at the narrow cutoff the workspace is a single column and no persistent sidebar node exists in the DOM.
    - Test 4: reading order is file, then Base, then Head for a pinned comparison and file, then Preimage, then Postimage for an exact patch, asserted at the narrow cutoff.
    - Test 5: the document has no horizontal overflow at the narrow cutoff; the diff viewport is the only horizontal overflow owner and the Monaco canvas stays at or above its 640px minimum side-by-side width.
    - Test 6: the `Skip to changed files` link never targets a node inside a closed dialog.
  </behavior>
  <action>
    Wire the dialog's `select` and `activate` events to the existing selection path, then close the dialog and move focus to the active-file heading once the file has loaded — gate on the existing loading flag rather than a timer. Route focus return through the primitive's opener capture; do not add a fourth bespoke focus-return shape in `App.vue`.

    Resolve the narrow skip link deliberately rather than leaving it pointing into a closed dialog. Render `Skip to changed files` only while a persistent sidebar host exists; at the narrow cutoff the `Files` control in the active-file toolbar is already the reachable entry point for the same destination, so a skip link that opens a modal would be a second, weaker path to it. Record the decision in the plan summary.

    Finish the narrow reflow in the stylesheet: single workspace column, no sidebar track, and the bounded mobile header and toolbar padding from the mockup. Keep the diff viewport as the only horizontal overflow owner and keep Monaco side-by-side — do not convert to a unified diff.
  </action>
  <acceptance_criteria>
    - `grep -c 'Skip to changed files' src/web/App.vue` is exactly `1`, and reading `src/web/App.vue:1126-1128` after the edit shows that link rendered conditionally on a persistent sidebar host existing.
    - `grep -c 'filesOpener' src/web/App.vue` is `0` — focus return is the primitive's job now.
    - `npm run build:web && npx playwright test tests/e2e/responsive-session.spec.ts` passes, including the narrow probes at `:1558, 1571, 1603` and the open/close round trip at `:1604-1607`.
    - `npm run build:web && npx playwright test tests/e2e/pinned-session.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/monaco-anchor.spec.ts` passes — the editable-control count, the overflow canary, and Phase 10's canvas minimum all hold.
    - `npm run typecheck:web` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web &amp;&amp; npm run build:web &amp;&amp; npx playwright test tests/e2e/responsive-session.spec.ts tests/e2e/pinned-session.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/monaco-anchor.spec.ts</automated>
  </verify>
  <done>Behaviours 1-6 hold, the skip-link ambiguity is resolved and recorded, and the narrow layout keeps Monaco side-by-side with the document free of horizontal overflow.</done>
</task>

<task type="auto">
  <name>Task 3: Retire the drawer's CSS and its drift-gate entries</name>
  <files>src/web/styles.css, scripts/verify-semantic-css.mjs, tests/e2e/responsive-session.spec.ts</files>
  <read_first>
    - `src/web/styles.css` — the narrow block plan 01 created, holding the `.review-files` absolute-overlay rules, `.review-files--open` with `box-shadow: var(--shadow-overlay)`, and `.review-files:not(.review-files--open) { display: none }`. All three are dead once the drawer is a dialog.
    - `scripts/verify-semantic-css.mjs:230-233` — `overlayAllowlist`, whose `.review-files--open` entry becomes orphaned when that rule is deleted.
    - `scripts/verify-semantic-css.mjs:247-251` — the unallowlisted-overlay `fail()` and the responsive-query pin, whose files-drawer arm plan 01 retargeted to the narrow cutoff and which now has no selector to pin.
    - `scripts/verify-semantic-css.mjs:406` — `assertTokenRoot(sourceRoot, 'source CSS', true)` enforces an exact token set; if the deleted rules were the only consumers of a token, that token becomes an orphan and the gate fails.
    - `tests/e2e/responsive-session.spec.ts:1541, 1607` — remaining `Close files` sites; `:1162, 1183, 1195, 1261` — earlier ones. All are superseded by the dialog's close copy.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md` — Canonical Geometry Tokens, to confirm which tokens keep a consumer after the deletion.
  </read_first>
  <action>
    Delete the drawer rules from the stylesheet and remove the `.review-files--open` entry from `overlayAllowlist` in the same change — an orphaned allowlist entry and an orphaned rule are equally a gate mismatch. With the rail's arm already gone in plan 01 and the drawer's arm now gone, the responsive-query pin has no remaining selector; remove the whole conditional rather than leaving a branch that can never fire.

    Before committing, confirm every token still has at least one consumer. The dialog panel rule from plan 01 already consumes `--dialog-width` and `--dialog-max-height`; `--sidebar-width` must still be consumed by the persistent sidebar track and its two responsive redeclarations. If the deletion strands a token, wire it or remove it from `CANONICAL_TOKENS` — do not leave the gate to discover it.

    Replace the remaining `Close files` assertion sites with the dialog's close copy.
  </action>
  <acceptance_criteria>
    - `grep -c 'review-files' src/web/styles.css scripts/verify-semantic-css.mjs` totals `0`.
    - `grep -c 'Close files' tests/e2e/responsive-session.spec.ts` is `0`.
    - `grep -c 'rule.context.some' scripts/verify-semantic-css.mjs` is `0` — the responsive-query pin conditional is gone, not left as a dead branch.
    - `grep -c 'overlayAllowlist' scripts/verify-semantic-css.mjs` is at least 2 — the allowlist itself and its membership check survive; only the retired selector is removed.
    - `grep -c -- '--sidebar-width' src/web/styles.css` is at least 4 — the token keeps its declaration, both responsive redeclarations, and a consumer.
    - `npm run verify:semantic-css` exits 0.
    - `npm run build:web && npx playwright test tests/e2e/responsive-session.spec.ts tests/e2e/file-tree.spec.ts` passes.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:semantic-css &amp;&amp; npm run build:web &amp;&amp; npx playwright test tests/e2e/responsive-session.spec.ts tests/e2e/file-tree.spec.ts</automated>
  </verify>
  <done>The drawer leaves no CSS, no allowlist entry, no dead gate branch, and no orphaned token; the gate exits 0 and both specs pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → file tree rows | Every path, directory name, and status in the dialog is repository-controlled. |
| dialog → background shell | The Changed files dialog contains the only text input in the shell at the narrow cutoff; a leaky trap or a failed inert lets a keyboard reviewer reach suppressed controls. |
| stylesheet ↔ drift gate | Deleting a rule and its allowlist entry must stay in lockstep, or the gate silently enforces less than it did. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-05-01 | Spoofing | `FileTree.vue` rows inside the dialog | mitigate | The tree is hosted, not forked, so its Phase 09 path-rendering and control-byte handling are unchanged. Asserted by `tests/e2e/file-tree.spec.ts` passing with zero edits. |
| T-11-05-02 | Elevation of privilege | dialog focus containment with a text input | mitigate | `src/web/App.vue:856` returns early for input targets, so the central Escape chain cannot close this dialog and the central keydown chain cannot be relied on. Plan 01's primitive owns Escape locally and its widened focusable query includes the `type="search"` input. Asserted by Task 1 Test 4. |
| T-11-05-03 | Information disclosure | dialog mount position | mitigate | Mounting a filter input inside `.review-main` breaks `tests/e2e/pinned-session.spec.ts:1418` and puts an editable control in the diff region. The dialog mounts in the `.session-shell` sibling band, asserted by reading `App.vue` and by Task 1 Test 6. |
| T-11-05-04 | Tampering | drift-gate coverage | mitigate | Removing the drawer's overlay rule without removing its allowlist entry — or removing the entry while a rule survives — weakens or breaks the only mechanical barrier against unreviewed overlay surfaces. Task 3 removes both in one change and asserts zero remaining references in either file, with the gate exiting 0. |
| T-11-05-05 | Denial of service | narrow document overflow | mitigate | A dialog or reflowed toolbar that widens the document makes the review unusable on a 390px viewport. Asserted by Task 2 Test 5 and the `.review-main` overflow canary at `tests/integration/anchored-workspace.spec.ts:1203-1208`. |
| T-11-05-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install occurs; the dialog composes the existing tree and the existing modal primitive. Verified by `git diff --stat package.json package-lock.json` being empty. |
</threat_model>

<verification>
- `npm run typecheck:web` and `npm run verify:semantic-css` exit 0.
- `npm run build:web && npx playwright test tests/e2e/responsive-session.spec.ts tests/e2e/file-tree.spec.ts tests/e2e/pinned-session.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/monaco-anchor.spec.ts` passes.
- `git diff --stat package.json package-lock.json` is empty.
- `tests/e2e/file-tree.spec.ts` is not listed by `git diff --name-only` — the Phase 09 contract is proven by an unmodified spec.

**Known-red window:** Task 1 removes the narrow drawer markup while Task 3 still has its CSS and gate entries in place. Between them, `verify:semantic-css` may report the drawer rule as an orphan and `responsive-session.spec.ts` is red at its `Close files` sites. Run all three tasks before committing, or commit Tasks 1 and 2 together and Task 3 immediately after.

**Deliberate decision to record in the summary:** the `Skip to changed files` link is rendered only while a persistent sidebar host exists. At the narrow cutoff the `Files` control is the single reachable entry point to the tree; a skip link that opens a modal would be a second, weaker path to the same destination.
</verification>

<success_criteria>
- `ChangedFilesDialog.vue` exists, hosts the shipped `FileTree` without forking it, and mounts in the `.session-shell` overlay sibling band.
- Exactly one navigation landmark named `Changed files` exists.
- Initial focus is the filter control; selection closes the dialog, opens the file, and focuses the active-file heading after load; close, Escape, and cancel all return focus to the `Files` opener.
- At the narrow cutoff the workspace is one column with no persistent sidebar node, reading order is preserved, and only the diff viewport overflows horizontally.
- The drawer leaves no CSS rule, no `overlayAllowlist` entry, and no dead responsive-query branch in the gate.
- `tests/e2e/file-tree.spec.ts` passes unmodified.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Produced by |
|---|---|---|
| `src/web/components/ChangedFilesDialog.vue` | new component | Task 1 |
| Narrow drawer markup, state, CSS, and focus path removed | deletion | Tasks 1-3 |
| Collapsed duplicate `Changed files` navigation landmark | bug fix | Task 1 |
| `.review-files--open` retired from `overlayAllowlist` and the responsive-query pin removed | gate config | Task 3 |
| Conditional `Skip to changed files` link | decision | Task 2 |
| Narrow reflow assertions and dialog-copy updates in `tests/e2e/responsive-session.spec.ts` | test | Tasks 2-3 |

<output>
Create `.planning/phases/11-workspace-shell-review-surfaces/11-05-SUMMARY.md` when done.
</output>
