---
phase: 09-changed-file-tree
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/css-token-contract.mjs
  - src/web/styles.css
  - tests/e2e/file-tree.spec.ts
  - tests/e2e/responsive-session.spec.ts
autonomous: true
requirements: [TREE-01, TREE-02, TREE-03]

must_haves:
  truths:
    - "A reviewable changed-file row renders at the mockup's 34px density in the shipped sidebar host"
    - "The selected file shows an inset accent rail that survives grayscale alongside its boundary and aria-selected state"
    - "Row indentation still steps 8px at root and 16px per level"
    - "Every new canonical token has a declared consumer, and the drift gate passes"
  artifacts:
    - path: "scripts/css-token-contract.mjs"
      provides: "--file-row-min-height, --radius-file-row, --radius-scrollbar, --space-5 added to CANONICAL_TOKENS in alphabetical position"
      contains: "--file-row-min-height"
    - path: "src/web/styles.css"
      provides: "Four token declarations plus dense .tree-row, rail ::before, scoped badge/count/availability geometry, tree chrome rules, and the scoped scrollbar consumer"
      contains: "--file-row-min-height"
    - path: "tests/e2e/file-tree.spec.ts"
      provides: "Row-height assertion re-pinned to the 34px contract"
    - path: "tests/e2e/responsive-session.spec.ts"
      provides: "Selection non-color cue assertion re-pinned to the inset rail pseudo-element"
  key_links:
    - from: "src/web/styles.css"
      to: "scripts/css-token-contract.mjs"
      via: "every added :root declaration also appears in CANONICAL_TOKENS and is referenced by at least one var()"
      pattern: "var\\(--file-row-min-height\\)"
    - from: "src/web/styles.css"
      to: "src/web/components/FileRow.vue"
      via: ".tree-row keeps padding-left: var(--tree-indent), which the row components inject per level"
      pattern: "var\\(--tree-indent\\)"
---

<objective>
Land the four geometry tokens Phase 08 deferred to this phase, each with its consumer in the same change, and rewrite the tree's row and chrome CSS to the mockup's dense treatment.

Purpose: TREE-01's dense single-line row and TREE-02's directory treatment are geometry and typography, and Phase 08's drift gate rejects both a stray colour literal and a declared token with no consumer. This plan is where the token contract and the density contract land together, along with the two shipped assertions that the density change invalidates.
Output: `--file-row-min-height`, `--radius-file-row`, `--radius-scrollbar`, and `--space-5` are canonical and consumed; `.tree-row` is the mockup's 34px row with a transparent boundary, 5px radius, and an inset accent rail; the tree's header, filter, scroller, empty-state, and hint rules exist ready for the markup in plan 09-04.

Out of scope: the `.file-tree-pane` grid/overflow switch and the focus-offset retirement, both of which need the new markup and belong to plan 09-04. No component file changes here. No `.review-shell` grid-track change — `--sidebar-width` is Phase 11's.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
@GSD core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-changed-file-tree/09-UI-SPEC.md
@.planning/phases/09-changed-file-tree/09-RESEARCH.md
@.planning/phases/09-changed-file-tree/09-PATTERNS.md
@.planning/phases/08-semantic-visual-foundation/08-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Declare the four deferred geometry tokens with the allowlist</name>
  <files>scripts/css-token-contract.mjs, src/web/styles.css</files>
  <read_first>
    - scripts/css-token-contract.mjs:1-32 (the alphabetically sorted `CANONICAL_TOKENS` array and the `COLOR_TOKEN` prefix regex)
    - src/web/styles.css:96-146 (the typography, geometry, and spacing blocks of the single canonical `:root`)
    - scripts/verify-semantic-css.mjs:75-115 (`assertTokenRoot` exact-set check, `assertDeclaredTokensConsumed` no-orphan check, `assertTokenValueShapes`)
    - .planning/phases/09-changed-file-tree/09-UI-SPEC.md:42-53 (the four tokens, their values, and their named consumers)
    - .planning/phases/08-semantic-visual-foundation/08-06-SUMMARY.md:61-96 (the retirement table recording each of the four as Phase 09 scope)
  </read_first>
  <action>
Add exactly four names to `CANONICAL_TOKENS` in `scripts/css-token-contract.mjs`, each in its alphabetical position: `--file-row-min-height` after `--diff-unchanged-background`, `--radius-file-row` after `--radius-control`, `--radius-scrollbar` after `--radius-pill`, and `--space-5` after `--space-4`. The array is sorted and `assertTokenRoot` runs an exact-set comparison, so an out-of-order or duplicated entry is a silent maintenance hazard even though it would still pass.

None of the four matches the `COLOR_TOKEN` prefix regex at `scripts/css-token-contract.mjs:28`, so `assertCanonicalTokenValues` will not demand colour-shaped values from them. Do not rename any of the four to a colour-prefixed name.

Declare the four in `src/web/styles.css`'s `:root`, each beside its existing peers rather than in a new block: `--radius-file-row: 5px` after `--radius-control`, `--radius-scrollbar: 8px` after `--radius-overlay`, `--file-row-min-height: 34px` in the geometry run near `--selected-rail-width` and `--control-height-standard`, and `--space-5: 20px` in the numeric gap between `--space-4` and `--space-6`. Every value is cited by `09-UI-SPEC.md:48-51`; do not round, rescale, or substitute a different mockup value.

Do not add a `/* Approved continuity */` comment to any of the four. That comment convention marks a value carried over without a mockup citation; all four have `mockups/01b-quiet-workspace-tree.html` citations and are therefore derived, not carried.

Do not predeclare any Phase 10 or Phase 11 token — `--sidebar-width`, `--dialog-width`, `--dialog-max-height`, `--control-height-compact`, `--diff-row-height`, `--diff-gutter-width`, and `--diff-sign-width` all stay retired until their owning surface consumes them.

This task leaves the gate red on purpose: the four are declared but not yet referenced, so `assertDeclaredTokensConsumed` will report them as orphans. Task 2 authors the consumers.
  </action>
  <verify>
    <automated>node -e "const {CANONICAL_TOKENS}=await import('./scripts/css-token-contract.mjs');const want=['--file-row-min-height','--radius-file-row','--radius-scrollbar','--space-5'];const sorted=[...CANONICAL_TOKENS].every((t,i,a)=>i===0||a[i-1]&lt;=t);if(!want.every(t=>CANONICAL_TOKENS.includes(t)))throw new Error('missing token');if(new Set(CANONICAL_TOKENS).size!==CANONICAL_TOKENS.length)throw new Error('duplicate token');if(!sorted)throw new Error('unsorted');console.log('ok',CANONICAL_TOKENS.length)" --input-type=module</automated>
  </verify>
  <acceptance_criteria>
    - The verify command prints `ok` followed by the new token count and exits zero, proving all four names are present, unique, and in sorted order.
    - `grep -c -- '--file-row-min-height: 34px' src/web/styles.css` returns 1; the same holds for `--radius-file-row: 5px`, `--radius-scrollbar: 8px`, and `--space-5: 20px`.
    - Comment-filtered, no out-of-scope token was predeclared: `grep -v '^\s*/\*' src/web/styles.css | grep -v '^\s*//' | grep -c -- '--sidebar-width\|--dialog-width\|--diff-row-height'` returns 0, and the same filtered pipeline over `scripts/css-token-contract.mjs` returns 0. Filter the comment lines: this plan's own rationale names those tokens, and an unfiltered count would trip on a comment rather than on a declaration.
    - `npm run verify:semantic-css` exits non-zero and its message reports the four names as canonical tokens with no consumer. This is the expected intermediate state, closed by task 2.
  </acceptance_criteria>
  <done>The four tokens are canonical and declared with the exact UI-SPEC values, no out-of-scope token was predeclared, and the only gate failure is the expected orphan report.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite the tree row and author the tree chrome rules</name>
  <files>src/web/styles.css</files>
  <read_first>
    - src/web/styles.css:96-146 (canonical `:root` after task 1)
    - src/web/styles.css:160-166 (the global `:focus-visible` outline and offset)
    - src/web/styles.css:288-314 (shared heading typography, including `.file-tree-pane h2`)
    - src/web/styles.css:351-418 (the shared badge base rule and the status/availability colour arms)
    - src/web/styles.css:810-820 (the shared monospace path/count list)
    - src/web/styles.css:896-1105 (visually-hidden, the whole tree block, path display, counts, availability shape)
    - src/web/styles.css:1275-1310 (the shipped shell grid and `.review-files` scroll owner)
    - src/web/styles.css:2254-2295 (the responsive host widths this plan must not touch)
    - src/web/styles.css:2630-2712 (the reduced-motion block and the terminal forced-colors repair, including `.tree-row--selected`)
    - scripts/verify-semantic-css.mjs:117-268 (`assertNoLegacy`, `assertDirectColorConfinement`, `assertAuthorStyle` with its inset-shadow allowlist and pressed-control rule)
    - .planning/phases/09-changed-file-tree/09-UI-SPEC.md:55-107 (existing tokens, spacing scale, mockup exceptions), :111-147 (typography and colour rules), :150-208 (composition, row anatomy, directory row), :211-234 (state contract), :247-266 (copy), :316-323 (non-colour meaning)
    - .planning/phases/09-changed-file-tree/09-RESEARCH.md:249-291 (Pitfalls 4 through 9)
    - mockups/01b-quiet-workspace-tree.html:15-34 (the normative tree geometry) and line 9 (`.sidebar-top`, `.sidebar-heading`, `.filter`, `input[type=search]`, `.file-scroll`, `.empty-search`)
  </read_first>
  <action>
Work inside the existing tree block so the stylesheet keeps one home for tree rules. Every new rule must land **before** `@media (prefers-reduced-motion: reduce)`, because `assertAuthorStyle` requires the forced-colors repair to remain the single terminal media block positioned after it.

**Row box.** Rewrite `.tree-row`: add `position: relative`; swap `min-height: 40px` for `var(--file-row-min-height)`; set the gap to zero in the block axis and `var(--space-2)` in the inline axis; set `padding-block: 5px`, `padding-right: 10px`, and keep `padding-left: var(--tree-indent)` exactly as it is; replace `border-bottom` with a full `var(--border-width-default)` solid transparent boundary; add `border-radius: var(--radius-file-row)`; change `cursor` to pointer.

Three constraints on that rewrite. Keeping `padding-left: var(--tree-indent)` is mandatory: it is the token's only consumer and dropping it fails the no-orphan gate. Zeroing the block-axis gap is mandatory: the availability column below is created as an implicit track, and a non-zero row gap would add height to a 34px row. Leaving `font-size`, `font-weight`, and `line-height` untouched is mandatory: `tests/e2e/responsive-session.spec.ts:933-949` reads the computed body size, weight `400`, and body line height off a synthetic `.tree-row.file-row` fixture, and that assertion must stay byte-unchanged.

**Density bump retirement.** Delete `.review-files .tree-row` (the `min-height: 44px` rule). The shipped tree renders inside `.review-files`, so this rule would otherwise defeat the 34px contract entirely, and `09-UI-SPEC.md:291` states row density does not change on mobile. Leave `.review-files .tree-row:focus-visible` alone — retiring that negative outline offset needs the scroller inset that plan 09-04 authors, and retiring it early would clip the focus ring on a row flush with the pane's top edge.

**Hover.** Point `.tree-row:hover` at `--surface-raised` per `09-UI-SPEC.md:142`. `--surface-interactive-hover` keeps two other consumers, so no orphan results.

**Selection and the rail.** Keep the selector name `.tree-row--selected` verbatim: `scripts/verify-semantic-css.mjs:225-229` keys its inset-shadow allowlist on that literal string, and `src/web/styles.css:2695-2704` keys the forced-colors repair on it. Replace its `border-left` shorthand with `border-color: var(--selection-border)`, keep `background: var(--selection-background)`, keep `box-shadow: none`, and keep the semibold weight. Add a `::before` pseudo-element for the rail: absolutely positioned, inset 5px in the block axis, starting one pixel before the inline-start edge, `var(--selected-rail-width)` wide, 2px radius, filled with `var(--interactive-accent)`, with empty string content. Do not express the rail as a box-shadow — the only allowlisted inset value for this selector is the `--selection-border` form, and `09-UI-SPEC.md:219` requires an inset rail with a radius that a box-shadow cannot produce. The 2px radius is mockup-specific component geometry, not a reusable root token.

Leave the forced-colors repair untouched. Its `.tree-row--selected` arm carries the Highlight fill and the inset Highlight rail that make selection survive forced-colors mode.

**Badge and marker geometry, scoped to tree rows.** Inside `.tree-row`, give `.status-badge` a zero minimum height, `3px 4px` padding, a 3px radius, `var(--font-mono)`, `0.04em` letter spacing, and a unit line height, per `09-UI-SPEC.md:181`. Inside `.tree-row`, give `.availability-marker` only a zero minimum height and `3px var(--space-1)` padding. Do not touch the availability marker's font family, size, weight, or line height anywhere: `tests/e2e/responsive-session.spec.ts:948` reads all four off the first `.availability-marker` in the document, which is inside a real tree row, and that assertion must stay byte-unchanged. Do not touch the shared base rule's declarations either — `.pin-cue` and `.dirty-badge` share it and are not tree surfaces.

**Counts.** Inside `.tree-row`, set `.line-counts` to a `6px` gap and regular weight, and clear the badge-style fill off `.line-counts__added` and `.line-counts__deleted` by setting their background to transparent. `09-UI-SPEC.md:183` specifies the signed pair as added and deleted *foreground* only; the shared arms at `src/web/styles.css:394-406` currently also paint a fill, which is a badge treatment the row must not borrow. The `6px` gap is a documented mockup exception (`09-UI-SPEC.md:101`) and must not be promoted to a spacing token. `transparent` is explicitly permitted on `background` by the gate's non-palette allowance.

**File row columns.** Set `.file-row` to three explicit columns — auto, a zero-floored flexible track, auto — and delete `grid-template-areas` together with the four `grid-area` declarations on `.file-row`'s status, path, counts, and availability children. Source order already matches the column order, so the named areas are redundant, and a fixed four-area template cannot collapse for reviewable rows. Add one variant using `:has()` that widens `.file-row` to four columns only when it contains an availability marker that is not visually hidden. This is why three columns is the base: a reviewable row's marker is `.visually-hidden`, therefore absolutely positioned, therefore neither a grid item nor a creator of an implicit track — so it costs no phantom column and no phantom gap. A non-reviewable row's marker is in flow and needs the fourth track, because the default row auto-flow would otherwise wrap it onto a second line. Keep `.file-row > .path-display`'s nowrap and overflow rules and the ellipsis rule on its nested path text: `09-UI-SPEC.md:198` makes the filename the only shrinking column.

Never place a comma inside the `:has()` argument. `scripts/verify-semantic-css.mjs` splits selectors on commas to build its arm list, so a comma nested in a functional pseudo-class would produce mangled arms and unpredictable gate behaviour.

**Filename size.** Scope `var(--font-size-code)` to the filename span inside `.file-row` only. The same span appears in the shell's active-file header, where `09-UI-SPEC.md:120` keeps the shipped page-heading role until Phase 11.

**Directory row.** Set `.directory-row` to a 12px disclosure track, a zero-floored flexible track, and an auto count track. Give `.directory-row__path` the metadata size, semibold weight, and metadata line height; leave it in the shared monospace list, which is consistent with `09-UI-SPEC.md:24` assigning monospace to paths. Author `.directory-row__count` with muted colour, monospace, metadata size, regular weight, metadata line height, and no wrapping. Do not add a font-size literal for the disclosure glyph: `09-UI-SPEC.md:113` permits exactly the four inherited sizes, and the glyph already resolves to the metadata size through the shared rule.

**Pane chrome.** Add `height: 100%` and `background: var(--surface-sidebar)` to `.file-tree-pane`, per `09-UI-SPEC.md:59` and `:133`. Replace the `.file-tree-pane h2` rule's padding and bottom border with flex alignment, an inline gap of `var(--space-2)`, and `var(--space-4)` of bottom margin, matching the mockup's `.sidebar-heading`. Then author, ready for plan 09-04's markup:

- a top block carrying `var(--space-5) 18px 14px` of padding — this is `--space-5`'s consumer, and the 18px and 14px are documented mockup exceptions from `09-UI-SPEC.md:102`;
- a muted monospace metadata count span and a muted metadata eyebrow span, the eyebrow pushed to the inline end with an automatic start margin so the header reads as name, count, then eyebrow;
- a relatively positioned filter label whose absolutely positioned decorative glyph sits `var(--space-3)` from the inline start and `var(--space-2)` from the block start, matching the mockup's `.filter span`;
- the search input at full width with a zero minimum width, `9px 12px 9px 33px` padding, a `var(--border-width-default)` solid `var(--border-control)` boundary, `var(--radius-control)`, primary text on `var(--surface-canvas)`, and inherited font;
- a `:has()` variant that widens the input's inline-end padding to `var(--control-height-standard)` when the clear button is present, so the query text never runs under it;
- an absolutely positioned clear button at the inline end, vertically centred, `var(--control-height-standard)` square, borderless, `var(--radius-control)`, muted on transparent, with `var(--icon-size)` glyph size and a unit line height; a hover arm on `--surface-raised` with primary text; and an `:active` arm on `--surface-interactive-active` that **must** explicitly declare `box-shadow: none`, because `assertAuthorStyle` fails any rule whose selector contains `:active` and does not;
- a scroller with a zero minimum height, `var(--space-2)` inline and block-start padding and `var(--space-4)` block-end padding, `overflow-y: auto`, contained overscroll, and a stable scrollbar gutter;
- a native scrollbar rule on that scroller: a `var(--space-2)`-wide track and a thumb carrying `border-radius: var(--radius-scrollbar)` and `background: var(--scrollbar-thumb)`. This is `--radius-scrollbar`'s consumer and the repository's first native scrollbar rule — there is no in-repo precedent for the selector shape, and it is authored here deliberately rather than dropping the token, because `09-UI-SPEC.md:50` assigns the token the changed-file scroll thumb role;
- a no-match block with `var(--space-6)` block and `var(--space-2)` inline padding on muted text, its heading in primary text at body size and semibold weight with a `var(--space-2)` bottom margin, and its paragraph with a `var(--space-4)` bottom margin;
- a hint paragraph with `var(--space-2)` top margin, `10px` padding, a `var(--border-width-default)` solid `var(--border-default)` top boundary, muted metadata text. The `10px` inset and `8px` separation are documented mockup exceptions from `09-UI-SPEC.md:105`.

The scroller's block-start padding is a **deliberate departure** from `mockups/01b-quiet-workspace-tree.html:9`, which gives `.file-scroll` no top inset. Plan 09-04 retires the tree's negative focus offset so the focus ring uses the contract's 2px width at 3px offset, and `tests/e2e/responsive-session.spec.ts:382-435` fails any focusable element whose ring perimeter is clipped by an overflow ancestor. Five pixels of clearance above the first row is the reason for the inset; record it as a departure with that rationale.

Introduce no colour literal and no second palette anywhere. Every colour must be a `var()` reference to a canonical token, with `transparent` the only literal allowed and only on border, background, and outline.
  </action>
  <verify>
    <automated>npm run verify:semantic-css</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:semantic-css` exits zero.
    - `grep -c 'var(--tree-indent)' src/web/styles.css` returns at least 1.
    - `grep -c 'var(--file-row-min-height)' src/web/styles.css`, `grep -c 'var(--radius-file-row)' src/web/styles.css`, `grep -c 'var(--radius-scrollbar)' src/web/styles.css`, and `grep -c 'var(--space-5)' src/web/styles.css` each return at least 1.
    - `grep -n 'tree-row--selected' src/web/styles.css` shows the ordinary-block rule, its `::before` rail, and the two forced-colors arms still present.
    - `grep -n 'min-height: 44px' src/web/styles.css` produces no match inside a `.review-files .tree-row` rule.
    - `grep -n 'grid-template-areas' src/web/styles.css` produces no match for `.file-row`.
    - Every rule whose selector contains `:active` also declares `box-shadow: none`; `npm run verify:semantic-css` is the mechanical proof, since it fails otherwise.
    - `grep -n ',' ` applied to the `:has(` lines of `src/web/styles.css` shows no comma inside any `:has()` argument.
    - The last `@media` block in `src/web/styles.css` is still `@media (forced-colors: active)` and it still follows `@media (prefers-reduced-motion: reduce)`; `npm run verify:semantic-css` fails otherwise.
  </acceptance_criteria>
  <done>The row renders at the 34px contract with a transparent boundary, 5px radius, and inset accent rail; all four new tokens have live consumers; the chrome rules exist; the drift gate is green.</done>
</task>

<task type="auto">
  <name>Task 3: Re-pin the two shipped assertions this density change invalidates</name>
  <files>tests/e2e/file-tree.spec.ts, tests/e2e/responsive-session.spec.ts</files>
  <read_first>
    - tests/e2e/file-tree.spec.ts:319-431 (the packaged tree test, in particular the modified-row box measurement at :334-339)
    - tests/e2e/responsive-session.spec.ts:546-584 (`expectNonColorStateCues`, in particular the selection cue assertions at :559-569)
    - tests/e2e/responsive-session.spec.ts:259-264 (`expectMinimumTarget`, which is not applied to tree rows)
    - tests/e2e/responsive-session.spec.ts:925-951 (the synthetic `.tree-row.file-row` typography assertions that must stay byte-unchanged)
    - .planning/phases/09-changed-file-tree/09-RESEARCH.md:275-278 (Pitfall 6) and :491-524 (the enumerated must-update and must-stay-unchanged tables)
    - .planning/phases/09-changed-file-tree/09-UI-SPEC.md:180 (row geometry) and :219 (selection rail)
  </read_first>
  <action>
Update exactly two assertions.

In `tests/e2e/file-tree.spec.ts`, the modified row's height check currently demands at least 40 pixels. Re-pin it to the new contract: assert the measured height equals 34. With a 34px minimum height, 5px of block padding, a 1px transparent boundary, and border-box sizing, the reviewable row resolves to exactly the minimum. Run the suite, read the real measured value from the failure or pass, and pin that observed number; if it is not 34, pin what the browser reports and record the discrepancy and its cause in the SUMMARY rather than loosening the assertion back to an inequality. A pinned equality is the only form that actually defends the density contract.

In `tests/e2e/responsive-session.spec.ts`, `expectNonColorStateCues` asserts the selected row has a 3px left border. That cue moved from a border to an inset pseudo-element rail, so the left border is now the row's 1px transparent boundary. Replace that single assertion with an evaluation of the selected row's `::before` computed style, asserting its width equals the resolved `--selected-rail-width`. Use the `resolveToken` and `canonicalTokens` helpers already used in this file so the expectation stays derived from the canonical root rather than hard-coded. Keep every other assertion in that helper exactly as it is — the height ceiling, the visually-hidden text-availability marker, and the counts-within-pane containment checks are all still true and all still load-bearing.

Change nothing else in either file. In particular do not touch the synthetic `.tree-row.file-row` typography assertions: this plan deliberately left the row's font declarations alone so they stay green. Do not touch `tests/e2e/pinned-session.spec.ts` at all — the heading accessible name, the tree name, the `data-file-id` selection assertions, and the `main` landmark name are all preserved by this plan and are pinned as unchanged for the whole phase.
  </action>
  <verify>
    <automated>npm run test:browser -- tests/e2e/file-tree.spec.ts tests/e2e/responsive-session.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:browser -- tests/e2e/responsive-session.spec.ts` exits zero.
    - `npm run test:browser -- tests/e2e/file-tree.spec.ts` reports exactly one failure, the selected-treeitem `tabindex` assertion at `tests/e2e/file-tree.spec.ts:331`, and no height failure.
    - `git diff --stat tests/e2e/pinned-session.spec.ts` shows no change.
    - `grep -c 'toBeGreaterThanOrEqual(40)' tests/e2e/file-tree.spec.ts` returns 0.
    - `grep -c "border-left-width" tests/e2e/responsive-session.spec.ts` returns 0.
    - `grep -c 'data-normal-file' tests/e2e/responsive-session.spec.ts` returns 3, unchanged.
  </acceptance_criteria>
  <done>Both invalidated assertions are re-pinned to the new contract, the responsive suite is fully green, and the tree suite's only remaining failure is the pre-existing tab-stop defect owned by plan 09-04.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository content → rendered row | Adversarial path bytes reach row text; CSS decides only geometry and colour, never escaping |
| stylesheet → drift gate | `src/web/styles.css` is the single canonical palette source; any second palette is a contract breach |

## STRIDE Threat Register

ASVS level 1; blocking threshold `high`. This plan adds no endpoint, request, persistence, or file write; only V5 output-encoding concerns apply, and CSS carries none of them.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-05 | Tampering | `src/web/styles.css` colour surface | mitigate | Reference canonical tokens only; `transparent` is the sole literal, permitted by the gate on border, background, and outline. `npm run verify:semantic-css` is the enforcing check. |
| T-09-06 | Spoofing | `.tree-row--selected` state presentation | mitigate | Selection keeps three independent cues — `aria-selected`, the boundary colour, and the inset rail — plus the forced-colors Highlight repair, so a colour-blind, grayscale, or forced-colors reviewer cannot be misled about which file is open. Task 3's rail assertion is the regression gate. |
| T-09-07 | Information disclosure | ellipsized filename | accept | `09-UI-SPEC.md:182,307` requires the full identity to remain in the treeitem name and `title`, so truncation hides nothing; the full path is still reachable. |
| T-09-08 | Tampering | `assertAuthorStyle` allowlist coupling | mitigate | Keep `.tree-row--selected` named verbatim and keep `box-shadow: none` on it; declare `box-shadow: none` on the clear button's `:active` arm. Both are enforced by the gate rather than by review. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs nothing. |
</threat_model>

<verification>
- `npm run verify:semantic-css` green, which proves the exact canonical set, every token consumed, no direct colour outside the root, the shadow allowlist, the pressed-control rule, and the terminal forced-colors block position.
- `npm run test:browser -- tests/e2e/responsive-session.spec.ts` green.
- `npm run test:browser -- tests/e2e/file-tree.spec.ts` red only at `:331`.
- `tests/e2e/pinned-session.spec.ts` byte-unchanged.
- `.review-shell` grid tracks at `src/web/styles.css:1282` and `:2276` byte-unchanged; no `--sidebar-width` introduced.
</verification>

<success_criteria>
- A reviewable changed-file row measures exactly the `--file-row-min-height` contract in the shipped sidebar host, with 5px block padding, 10px right padding, a 1px transparent boundary, and a 5px radius (TREE-01).
- Status marks, signed counts, and the availability marker all fit that row without losing their visible text or their accessible labels (TREE-01).
- Directory rows carry the same density and a dedicated count track (TREE-02).
- Indentation still steps 8px at root and 16px per level through `--tree-indent` (TREE-03).
- Selection presents a boundary plus an inset accent rail and still survives grayscale and forced-colors (TREE-03).
- All four deferred tokens are canonical, valued as `09-UI-SPEC.md:48-51` specifies, and consumed in the same change.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

`scripts/css-token-contract.mjs`
- `CANONICAL_TOKENS` gains `--file-row-min-height`, `--radius-file-row`, `--radius-scrollbar`, `--space-5`

`src/web/styles.css` — new `:root` declarations
- `--file-row-min-height: 34px`
- `--radius-file-row: 5px`
- `--radius-scrollbar: 8px`
- `--space-5: 20px`

`src/web/styles.css` — new or rewritten selectors
- `.tree-row` (rewritten: relative positioning, token min-height, zero block gap, transparent boundary, file-row radius, pointer cursor; `padding-left: var(--tree-indent)` preserved)
- `.tree-row:hover` (repointed to `--surface-raised`)
- `.tree-row--selected` (border-colour form, `box-shadow: none` preserved)
- `.tree-row--selected::before` (new inset accent rail)
- `.tree-row .status-badge`, `.tree-row .availability-marker` (new scoped geometry)
- `.tree-row .line-counts`, `.tree-row .line-counts__added`, `.tree-row .line-counts__deleted` (new scoped gap, weight, fill clearing)
- `.file-row` (three-column base) and its `:has()` four-column non-reviewable variant
- `.file-row .path-text__filename` (new code-size scoping)
- `.directory-row` (three-column template), `.directory-row__path` (new type scoping), `.directory-row__count` (new)
- `.file-tree-pane` (full height, sidebar surface), `.file-tree-pane h2` (header flex composition)
- `.file-tree-pane__top`, `.file-tree-pane__count`, `.file-tree-pane__eyebrow` (new)
- `.file-tree-pane__filter`, `.file-tree-pane__filter-glyph`, its search-input rule, its `:has()` padding variant, `.file-tree-pane__clear` with hover and `:active` arms (new)
- `.file-tree-pane__scroller` plus its `::-webkit-scrollbar` and `::-webkit-scrollbar-thumb` rules (new)
- `.tree-empty` with its heading and paragraph rules, `.tree-hint` (new)

`src/web/styles.css` — removed
- `.review-files .tree-row` density bump
- `.file-row` `grid-template-areas` and the four child `grid-area` declarations
- `.file-tree-pane h2` padding and bottom border

Test assertions re-pinned
- `tests/e2e/file-tree.spec.ts` modified-row height equality
- `tests/e2e/responsive-session.spec.ts` selection rail via `::before` width

No component change, no model change, no new file.
</artifacts>

<output>
Create `.planning/phases/09-changed-file-tree/09-02-SUMMARY.md` when done. Record: the four token values and their exact consumers; the measured reviewable-row height; the scroller block-start inset recorded as a deliberate departure from `01b:9` with its focus-ring rationale; the decision to author a real scrollbar consumer rather than drop `--radius-scrollbar`; the `.file-tree-pane h2` selector used as the header consumer in place of the UI-SPEC's `__header` name; the intermediate state that the chrome rules are inert until plan 09-04 adds their markup and that the header is briefly unpadded; and that `tests/e2e/file-tree.spec.ts:331` remains red and owned by plan 09-04.
</output>
