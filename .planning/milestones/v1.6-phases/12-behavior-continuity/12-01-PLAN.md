---
phase: 12-behavior-continuity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/e2e/agent-ready-export.spec.ts
  - package.json
  - .planning/phases/12-behavior-continuity/12-CONTINUITY.md
autonomous: true
requirements: [CON-02]

must_haves:
  truths:
    - "All six tests in `tests/e2e/agent-ready-export.spec.ts` execute and pass against a locally packed runtime artifact, with no operator secret required."
    - "Every re-authored assertion still drives the packaged CLI through the production surface and still asserts an observable result — accepted HTTP status, exported bytes, or attached stdout — not Vue structure or CSS wiring."
    - "No covered flow was skipped, fixme'd, short-circuited, or weakened to make the suite green; the only conditional skip in the file remains the pre-existing local-archive support guard."
    - "A maintainer can run the runtime-artifact specs through a named npm script instead of an undocumented `npx` invocation, and the recipe for producing the four custody inputs is written down."
    - "The two genuinely external specs are recorded as documented prerequisites with the concrete reason each cannot run locally, not converted into skips."
  artifacts:
    - path: "tests/e2e/agent-ready-export.spec.ts"
      provides: "Review-notes-scoped locators for the relocated summary, export, and Finish controls"
      contains: "async function openReviewNotes"
    - path: "package.json"
      provides: "Named runner that makes the runtime-artifact specs reachable"
      contains: "playwright.runtime-artifact.config.ts"
    - path: ".planning/phases/12-behavior-continuity/12-CONTINUITY.md"
      provides: "CON-02 evidence record: RED baseline, re-authoring ledger, GREEN result, runner-reachability decision, external prerequisites"
      min_lines: 60
  key_links:
    - from: "tests/e2e/agent-ready-export.spec.ts"
      to: "src/web/components/ReviewNotesDialog.vue"
      via: "role+name dialog locator matches the ModalDialog title the Phase 11 relocation introduced"
      pattern: "dialog', \\{ name: 'Review notes'"
    - from: "package.json"
      to: "playwright.runtime-artifact.config.ts"
      via: "named script passes the runtime-artifact config to the Playwright CLI"
      pattern: "playwright.runtime-artifact.config.ts"
    - from: "tests/e2e/agent-ready-export.spec.ts"
      to: "scripts/pack-runtime.mjs"
      via: "four CUMPA_RUNTIME_* inputs read by tests/helpers/runtime-artifact.ts are produced by the in-repo packer"
      pattern: "CUMPA_RUNTIME_CUSTODY_DIR"
---

<objective>
Repair the one real defect this phase owns: `tests/e2e/agent-ready-export.spec.ts` — a spec the ROADMAP names as presentation-sensitive — has never executed during the milestone, and when actually run it fails 6/6 from a single Phase 11 relocation.

Purpose: CON-02 exists to catch exactly this. Phase 11 moved `Write summary`, `Save summary`, `Export review`, and `Finish attached review` out of the always-mounted Review rail into the `Review notes` modal dialog. This spec still queries them on the bare page. That is a legitimate re-authoring case under the `12-UI-SPEC.md` Test Reconciliation Rule, not a product defect — the flows, the HTTP results, and the exported bytes are unchanged.

Purpose (second half): the reason nobody noticed is a runner-reachability gap. `playwright.config.ts:6-9` removes the spec from both `npm run test:browser` and `npm run test:package` (`package.json:40` is a bare `playwright test`), and its only other runner, `tests/package/agent-ready-export.test.ts:178`, sits behind an operator secret checked at `:135-136`. Fixing the locators without fixing reachability guarantees the same silent rot next milestone.

Output: a repaired spec, a named runner, and `12-CONTINUITY.md` — the CON-02 evidence record the verifier will cite.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-behavior-continuity/12-UI-SPEC.md
@.planning/phases/12-behavior-continuity/12-RESEARCH.md
@.planning/phases/12-behavior-continuity/12-PATTERNS.md
@.planning/phases/11-workspace-shell-review-surfaces/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the custody harness locally and capture the RED baseline</name>
  <files>.planning/phases/12-behavior-continuity/12-CONTINUITY.md</files>
  <read_first>
    - `.planning/phases/12-behavior-continuity/12-RESEARCH.md:100-140` — the `[VERIFIED: commands run in this session]` block. It contains the exact two-step recipe that produced all four custody inputs locally in 5.15s, plus the synthetic support origin that makes test `:780` reach the same root cause as the other five.
    - `tests/helpers/runtime-artifact.ts:259-288` — `readRuntimeArtifact()` requires exactly four inputs: `CUMPA_RUNTIME_CUSTODY_DIR`, `CUMPA_RUNTIME_ARCHIVE_BASENAME`, `CUMPA_RUNTIME_ARCHIVE_SHA256`, `CUMPA_RUNTIME_EVIDENCE`, and cross-checks the producer evidence against the real archive bytes (basename, byteLength, sha256, sha1, sha512). Hand-written evidence cannot pass — this is why the packer is the only route.
    - `tests/helpers/runtime-artifact.ts:47-48` — `ProducerEvidenceSchema` accepts `purpose: 'development-check'` and `status: 'development-check'`.
    - `scripts/pack-runtime.mjs:204-216` — argument parsing. `--purpose`, `--custody-dir`, and `--evidence` are all mandatory and each destination must not already exist (`:215`). A bare `npm run pack:runtime-artifact --purpose development-check` cannot work: without `--`, npm passes nothing through to the script.
    - `scripts/pack-runtime.mjs:187-190` — `safeAbsoluteDirectory` rejects a non-absolute path and a parent that is not a real directory. On macOS `/tmp` is a symlink to `/private/tmp`, so use a realpath'd parent.
    - `scripts/pack-runtime.mjs:229-233` — `supportConfiguration()` returns `{configured:false}` without failing only for `development-check`; every other purpose demands the operator-held support origin.
    - `scripts/pack-runtime.mjs:537` — `development-check` is also the only purpose that tolerates a dirty tracked tree.
    - `scripts/pack-runtime.mjs:654-656` — stderr redaction of the support origin. Do not defeat it, and do not paste a real origin anywhere.
    - `playwright.runtime-artifact.config.ts:1-17` — the config whose `testMatch` (`:6-11`) actually includes `agent-ready-export.spec.ts`.
    - `tests/e2e/agent-ready-export.spec.ts:782` — `test.skip(acceptance.source !== 'local-archive', …)`. This is the only conditional skip in the file today and it is legitimate: it guards local-archive-only support evidence. A locally packed artifact *is* `local-archive`, so test `:780` runs.
    - `.planning/phases/08-semantic-visual-foundation/08-VERIFICATION.md:55-70` and `.planning/phases/11-workspace-shell-review-surfaces/11-VERIFICATION.md:95-99` — the command-ledger and documented-external-prerequisite record shapes to reproduce.
  </read_first>
  <action>
    Produce the four custody inputs with the in-repo packer, using the long form from RESEARCH. The short form is impossible — npm swallows the flags without `--`, and the script requires all three options.

    Pack once with a syntactically valid synthetic support origin so the support scenario is `configured`. `supportConfiguration()` only format-checks the origin against `https://<20 lowercase alphanumerics>.supabase.co`; it never dials it. Configured-but-unreachable is literally the state test `:780` asserts. Use the realpath'd, fresh, non-existent custody directory and evidence path shown in RESEARCH — both destinations must not pre-exist, and on macOS the parent must be `/private/tmp`, not `/tmp`.

    Because Phase 12 changes zero files under `src/`, one pack serves the whole phase. Do not re-pack after editing specs; the tarball carries the built CLI runtime, not the test tree.

    Export the four environment variables, deriving `CUMPA_RUNTIME_ARCHIVE_SHA256` from the evidence JSON rather than recomputing it by hand, then run the two runtime-artifact specs directly against `playwright.runtime-artifact.config.ts`, naming both spec paths explicitly on the command line. Do not invoke the config bare: its `testMatch` (`:6-11`) also matches `tests/e2e/public-support-states.spec.ts` and `tests/e2e/marketplace-review.spec.ts`, which throw locally for the external reasons Task 3 records, and those two extra failures would contaminate the RED baseline.

    Record the observed RED baseline. This is the TDD RED step for this plan: the failures already exist, so capture them before touching the spec. Create `.planning/phases/12-behavior-continuity/12-CONTINUITY.md` with a `## RED baseline` section holding the exact command, the total counts, and a row per failing test naming the test's declaration line, the line that failed, and the locator that was not found. Confirm or correct the six-row ledger from RESEARCH against what you actually observe — the declaration lines are `:380`, `:524`, `:581`, `:628`, `:690`, `:780`.

    Note in the same section that `tests/e2e/package-assets.spec.ts` passes under the same config and same inputs, which isolates the failure to the spec rather than the harness. Also note the cascading `afterAll` evidence-gate failure at `agent-ready-export.spec.ts:71-80`, which clears itself once the six pass and needs no edit.

    Do not commit the custody directory, the evidence JSON, the tarball, or any absolute home path into the document. Record the paths as placeholders.
  </action>
  <acceptance_criteria>
    - `.planning/phases/12-behavior-continuity/12-CONTINUITY.md` exists and `grep -c '## RED baseline' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1.
    - The RED section names all six failing test declaration lines: `grep -c ':380\|:524\|:581\|:628\|:690\|:780' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 6.
    - `grep -c 'pack:runtime-artifact -- --purpose development-check' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1 — the recorded recipe uses the long form that actually works.
    - `grep -ci 'supabase.co' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` shows only the synthetic placeholder origin; no real operator origin appears.
    - `git status --porcelain` shows no tarball, custody directory, or evidence JSON inside the repository tree.
    - `git diff --stat -- tests/ src/` is empty at the end of this task — the RED capture changes no code.
  </acceptance_criteria>
  <verify>
    <automated>test -f .planning/phases/12-behavior-continuity/12-CONTINUITY.md &amp;&amp; grep -q '## RED baseline' .planning/phases/12-behavior-continuity/12-CONTINUITY.md &amp;&amp; git diff --quiet -- tests src</automated>
  </verify>
  <done>The four custody inputs are reproducible from a written recipe, the six failures are observed and recorded with their real line numbers, and no source or test file has changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Scope the relocated controls to the Review notes dialog across the complete call-site ledger</name>
  <files>tests/e2e/agent-ready-export.spec.ts</files>
  <read_first>
    - `tests/e2e/complete-review-draft.spec.ts:141-157` — the settled convention, and the exact precedent for two coexisting disclosure helpers in one spec. `ensureReviewOpen` (`:141-150`) toggles the comments rail; `openReviewNotes` (`:152-157`) clicks the `Review notes` button and waits for `getByRole('dialog', { name: 'Review notes' })`. Copy `openReviewNotes` verbatim. Three further identical copies exist at `tests/integration/anchored-workspace.spec.ts:1066-1071`, `tests/integration/selector-drift-ui.spec.ts`, and `tests/integration/export-receipt-ui.spec.ts`. A fourth variant would be a second convention.
    - `tests/e2e/complete-review-draft.spec.ts:336-339` and `:480-487` — the closing half of the convention: an explicit `page.getByRole('button', { name: 'Close review notes' }).click()`. There is no idempotent open helper; the shell is inert while the dialog is mounted, so a second unconditional open would fail. Pair opens with closes wherever a later step needs the bare shell.
    - `tests/e2e/agent-ready-export.spec.ts:245-249` — the spec's own `ensureReviewOpen`. This is the correct insertion anchor. It is NOT at `:269-271`; `:251-271` is `addHeadComment`, and editing there would corrupt composer fill, `waitForResponse`, and the `Add comment` click.
    - `tests/e2e/agent-ready-export.spec.ts:273-279` — `saveSummary`. `:274` clicks `Write summary`, `:277` clicks `Save summary`, and `:276`/`:278` already pair the click with an accepted `/api/draft/mutations` response. Called from tests `:380` and twice from `:628` (at `:643` and `:644`).
    - `tests/e2e/agent-ready-export.spec.ts:410-416` — on `resumedPage`: `ensureReviewOpen` at `:411`, then `.review-summary__preview` at `:412` and `.comments-rail__comment` at `:413`, then `Export review` at `:416`. This is the call site both RESEARCH and PATTERNS under-specified: `.review-summary__preview` renders in `SummarySection.vue:226`, which `ReviewNotesDialog.vue:151` mounts *inside* the dialog, so the dialog must be open before `:412`, not before `:416`.
    - `tests/e2e/agent-ready-export.spec.ts:442-443` — `Export review again` on `resumedPage`, still inside the same dialog-scoped region if nothing closed it.
    - `tests/e2e/agent-ready-export.spec.ts:546-551` — test `:524`: `ensureReviewOpen` at `:548`, then `getByRole('region', { name: 'Finish attached review' })` at `:549` and its `Finish review` button at `:550`.
    - `tests/e2e/agent-ready-export.spec.ts:590-599` — test `:581`: `ensureReviewOpen` at `:593`, `Finish review` visible at `:594`, clicked at `:597`.
    - `tests/e2e/agent-ready-export.spec.ts:640-663` — test `:628`: `Promise.all([ensureReviewOpen(page), ensureReviewOpen(secondPage)])` at `:642`; `saveSummary` at `:643` and `:644`; `ensureReviewOpen(page)` at `:652` then `Finish review` at `:654`; `ensureReviewOpen(secondPage)` at `:659` then `Finish review` at `:661`. Note `:652` and `:659` click a rail button that lives in the shell — the shell is inert while the dialog is mounted, so whatever opened the dialog at `:643`/`:644` must have closed it again.
    - `tests/e2e/agent-ready-export.spec.ts:710-718` — test `:690`: no `ensureReviewOpen` at all; `addHeadComment` at `:714`, then `Finish review` at `:716`.
    - `tests/e2e/agent-ready-export.spec.ts:815-822` — test `:780`: `ensureReviewOpen` at `:817`, `Finish review` at `:819`. RESEARCH cites `:818`; that line is the `waitForResponse`, the click is `:819`.
    - `tests/e2e/agent-ready-export.spec.ts:412-413` — `.review-summary__preview` (dialog) and `.comments-rail__comment` (rail) asserted two lines apart. This is the proof that `ensureReviewOpen` must be KEPT, not replaced: `Review` is the comments-rail disclosure (`src/web/components/ReviewToolbar.vue:81-90`, `aria-controls="review-panel"` at `:84`), and the rail still owns comment history.
    - `src/web/components/ReviewNotesDialog.vue:108-114` — `title="Review notes"` (`:111`), `close-aria-label="Close review notes"` (`:112`).
    - `src/web/components/ReviewNotesDialog.vue:189-191` — `<section … aria-labelledby="finish-attached-review-heading">` with `<h3 id="finish-attached-review-heading">Finish attached review</h3>`. A `<section>` with an accessible name is exposed as `region`, so `getByRole('region', { name: 'Finish attached review' })` needs no rewrite — only the enclosing modal must be open.
    - `src/web/components/ui/ModalDialog.vue:11,15,87` — `keepMounted` defaults `false` and the backdrop renders under `v-if="open || keepMounted"`, so a closed dialog is absent from the DOM entirely. That is why every one of these locators fails rather than merely being hidden.
    - `src/web/components/IdentityHeader.vue:144` — the `Review notes` trigger lives in the always-present shell header, so `openReviewNotes` works from the bare page in every one of these tests.
    - `src/web/components/ExportSection.vue:146,166` — `Export review` / `Export review again`, mounted by `ReviewNotesDialog.vue:170`.
    - `.planning/phases/12-behavior-continuity/12-UI-SPEC.md` — the Test Reconciliation Rule. All five re-authoring conditions must hold for every edit here.
  </read_first>
  <behavior>
    The RED is already captured in Task 1: six tests fail, each on a locator for a control the Phase 11 relocation moved into the `Review notes` dialog. Re-run after each region is scoped and watch the failure count fall.
    - Test 1 (`:380`, resume after relaunch): after the fix, the resumed session shows the saved summary preview and the saved rail comment, then completes both the first export and the target-aware re-export, with `readdirSync` still seeing exactly `review.json` and `review.md`.
    - Test 2 (`:524`, unsaved composer): the `Finish attached review` region resolves and its `Finish review` button is found *disabled* — the block-while-unsaved contract is unchanged. The test then continues past that point: clicking `Review draft in …changed.ts` (`:562`) jumps back to the inline composer with `Unsaved inline feedback` intact (`:564-567`), clearing the composer (`:568`) and re-expanding the comments rail (`:569`) both succeed against the bare shell, and re-opening the Review notes dialog shows that same `Finish review` button now *enabled* (`:570`). Both the disabled and the enabled observation must hold. A fix that scopes only as far as the disabled assertion leaves `:569-570` failing, so this test is not green until the close-and-re-open choreography is in place.
    - Test 3 (`:581`, attached range): `Finish review` is visible, the click yields HTTP 201 from `/api/review-completion/finish`, the CLI exits 0, and stdout holds exactly one canonical V2 document that was empty before Finish.
    - Test 4 (`:628`, equivalent ranges): both attached sessions save distinct summaries into distinct drafts and each Finishes independently, with the second session's stdout still empty while the first finishes.
    - Test 5 (`:690`, exact-patch V3): Finish yields 201 and the emitted document is canonical V3.
    - Test 6 (`:780`, configured support): the support dialog behaviour at `:799-815` still passes and Finish now yields 201 and a V2 document.
    - Negative behaviour: with the production `Review notes` dialog mounted but a relocated control genuinely broken, every one of these still fails. The replacement assertions keep the accepted-HTTP-plus-bytes pairing, so they cannot pass on a cosmetic stub.
  </behavior>
  <action>
    Add `openReviewNotes` to the spec, copied verbatim from `tests/e2e/complete-review-draft.spec.ts:152-157`, placed immediately after `ensureReviewOpen` ends at `:249`. Do not invent a variant, do not add an idempotence guard, and do not delete or modify `ensureReviewOpen` — the rail is still asserted at `:412-413`.

    Then scope every region that touches a relocated control. Insert one open at the first line of each contiguous dialog-scoped region that needs the dialog. Where a region is interrupted by work that must reach the bare shell, close the dialog with the `Close review notes` button before that step and re-open it afterwards if a dialog-scoped locator is asserted again. The shell is inert while the dialog is mounted, so this applies to the rail toggles at `:569`, `:652`, and `:659`, and the re-open applies to test `:524` at `:570`. The complete ledger, verified by grep against HEAD, is:

    | Test | Region anchor | Controls it must cover |
    |---|---|---|
    | helper `saveSummary` (`:273-279`), called from tests `:380`, `:628` | first statement of the helper body, before `:274` | `Write summary` (`:274`), `Save summary` (`:277`) |
    | `:380` | after `:411`, before `:412` | `.review-summary__preview` (`:412`), `Export review` (`:416`), `Export review again` (`:443`) |
    | `:524` | open after `:548`; hold the dialog through `:562`; click `Close review notes` after `:563`; re-open after `:569`, before `:570` | region `Finish attached review` (`:549`), the `completion`-scoped `finish` locator (`:550`), the disabled assertion (`:551`), the `finish.evaluate` click (`:552-556`), `Review draft in …changed.ts` (`:558`, `:562`); then `:568` (`restoredComposer.fill('')`) and `:569` (`ensureReviewOpen(page)`) are shell interactions that need the dialog CLOSED, and `:570` (`expect(finish).toBeEnabled()`) needs it OPEN again |
    | `:581` | after `:593` | `Finish review` (`:594`, `:597`) |
    | `:628` | after `:652`, and again after `:659` for `secondPage` | `Finish review` (`:654`, `:661`) |
    | `:690` | after `:714`, before `:715` | `Finish review` (`:716`) |
    | `:780` | after `:817` | `Finish review` (`:819`) |

    Test `:524` is the only region that needs a close *and* a re-open, so do not treat it as a single open at `:548`. `:563` asserts `#review-panel` is `aria-hidden="true"`, but `App.vue:687-690` `reviewInlineComposer()` runs only `selectFile(fileId); commentsOpen.value = false;` — it collapses the comments rail and leaves the Review notes dialog mounted. The locator assertions at `:563-567` still pass underneath the backdrop because they read attributes, values, and CSS visibility. `:568` and `:569` do not: they are real interactions, the full-viewport backdrop owns the hit target, and both time out with the dialog open. Conversely, once the dialog closes, `ModalDialog.vue:11,15,87` removes the whole subtree from the DOM, so the `completion`-scoped `finish` locator captured at `:550` cannot resolve at `:570` until the dialog is re-opened. Close after `:563`, re-open after `:569`. That is the same open → close → re-open shape as `complete-review-draft.spec.ts:336-340`.

    Because `saveSummary` opens the dialog, give it a matching close before it returns, so `:652` and `:659` can still reach the rail toggle and so callers are not left holding an implicit open dialog. That mirrors `complete-review-draft.spec.ts:336-339`.

    Line numbers shift as you insert. Re-grep for `Finish review`, `Write summary`, `Save summary`, `Export review`, and `review-summary__preview` after each edit and confirm the count still matches: seven `Finish review` sites, one `Write summary`, one `Save summary`, one `Export review`, one `Export review again`, one `.review-summary__preview`.

    Change nothing else. Do not touch the `waitForResponse` pairings, the status-code expectations, the `readFileSync` stdout assertions, the `parseCanonicalReviewExport` calls, or the `afterAll` evidence gate at `:71-80` — that gate clears itself once the six pass. Do not weaken a locator to a broader match, do not remove an assertion, do not add a skip or fixme annotation, and do not introduce a conditional early return. The pre-existing conditional skip at `:782` stays exactly as it is: it is the legitimate local-archive guard for the support scenario, not a workaround.

    If any test still fails for a reason that is not the dialog relocation, stop and record it as an owning-phase defect rather than adjusting the assertion.
  </action>
  <acceptance_criteria>
    - `grep -c 'async function openReviewNotes' tests/e2e/agent-ready-export.spec.ts` is exactly `1`.
    - `grep -c 'async function ensureReviewOpen' tests/e2e/agent-ready-export.spec.ts` is exactly `1` — the rail helper is retained.
    - `grep -c "dialog', { name: 'Review notes' }" tests/e2e/agent-ready-export.spec.ts` is at least `1`.
    - `grep -c 'openReviewNotes(' tests/e2e/agent-ready-export.spec.ts` is at least `10`. The nine call sites are `saveSummary`, `:380`, `:524` twice (the open after `:548` and the re-open after `:569`), `:581`, both `:628` sessions, `:690`, and `:780`; the `async function openReviewNotes` definition line matches the same pattern, giving `10`. The bound is `10` and not `9` on purpose: definition-plus-eight-call-sites is exactly `9`, which is what an under-scoped `:524` with no re-open produces, so a bound of `9` or lower cannot catch the missing re-open.
    - `grep -c "name: 'Close review notes'" tests/e2e/agent-ready-export.spec.ts` is at least `2` — one close inside `saveSummary` before it returns, and one in test `:524` after `:563`. Fewer than two means either `:652`/`:659` or `:568`/`:569` is left reaching for the shell through the dialog backdrop, which times out rather than failing fast.
    - `grep -c "name: 'Finish review'" tests/e2e/agent-ready-export.spec.ts` is exactly `7` — unchanged from HEAD; no Finish assertion was dropped.
    - `grep -c "region', { name: 'Finish attached review' }" tests/e2e/agent-ready-export.spec.ts` is exactly `1` — the region locator was not rewritten.
    - `grep -c 'review-summary__preview' tests/e2e/agent-ready-export.spec.ts` is exactly `1` and `grep -c 'comments-rail__comment' tests/e2e/agent-ready-export.spec.ts` is exactly `1`.
    - `grep -c '\.fixme(' tests/e2e/agent-ready-export.spec.ts` is `0`.
    - `grep -c '\.skip(' tests/e2e/agent-ready-export.spec.ts` is exactly `1`, and `grep -n '\.skip(' tests/e2e/agent-ready-export.spec.ts` still shows the local-archive support guard, not a new one.
    - `grep -c 'waitForResponse' tests/e2e/agent-ready-export.spec.ts` is greater than or equal to its HEAD value — no accepted-HTTP pairing was removed.
    - With the Task 1 environment exported, `npx playwright test --config playwright.runtime-artifact.config.ts tests/e2e/agent-ready-export.spec.ts` reports 6 passed, 0 failed, 0 skipped-that-were-not-skipped-before.
    - `npx tsc --noEmit -p tsconfig.json` (or the project's existing test typecheck) exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --config playwright.runtime-artifact.config.ts tests/e2e/agent-ready-export.spec.ts</automated>
  </verify>
  <done>All six tests pass, the seven `Finish review` sites and the `Finish attached review` region locator are unchanged in shape, both disclosure helpers coexist, and no assertion was weakened or skipped.</done>
</task>

<task type="auto">
  <name>Task 3: Close the runner-reachability gap and record the CON-02 ledger</name>
  <files>package.json, .planning/phases/12-behavior-continuity/12-CONTINUITY.md</files>
  <read_first>
    - `playwright.config.ts:1-15` — `testDir: './tests'`, `testMatch: '**/*.spec.ts'`, and a `testIgnore` array at `:6-9` naming `**/e2e/package-assets.spec.ts` and `**/e2e/agent-ready-export.spec.ts`.
    - `package.json:37-49` — the existing script block. `:40` `test:package` is a bare `playwright test`; `:41` `test:browser` is `npm run build:web && playwright test`. Both therefore inherit the `testIgnore`. `:43` `pack:runtime-artifact`, `:45` `accept:runtime-artifact`, `:46` `accept:public-artifact`, `:47` `accept:marketplace-profile` show the `test:`/`accept:` naming convention to follow. There is no `test:runtime-artifact` script today.
    - `playwright.runtime-artifact.config.ts:6-11` — `testMatch` includes `package-assets.spec.ts`, `agent-ready-export.spec.ts`, `public-support-states.spec.ts`, and `marketplace-review.spec.ts`.
    - `tests/package/agent-ready-export.test.ts:134-136` — the Vitest driver throws unless `CUMPA_RELEASE_SUPPORT_SERVICE_URL` is set, and `:178` is where it finally invokes the Playwright CLI with the runtime-artifact config. This is why the spec is unreachable in practice.
    - `tests/e2e/public-support-states.spec.ts:327-330` — `beforeAll` resolves the acceptance runtime and, at `:329`, hard-refuses `local-archive`. Supplying custody inputs locally does NOT unblock this spec: it needs a runtime installed from a published registry package plus a live hosted support origin.
    - `tests/e2e/marketplace-review.spec.ts:97-99` — throws unless `CUMPA_MARKETPLACE_URL_MARKER` names a marker file written by a supervised, registry-installed CLI.
    - `.planning/phases/08-semantic-visual-foundation/08-VERIFICATION.md:62-70` — the precedent record for a gated spec reported as a documented external prerequisite rather than skipped. Note it cites `public-support-states.spec.ts:348`; that line number is stale at HEAD.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-VERIFICATION.md:95-99` — the command-ledger row shape with an explicit scope disclaimer.
  </read_first>
  <action>
    Add one npm script that makes the runtime-artifact specs reachable by name, following the existing `test:` naming: a `test:runtime-artifact` entry that runs the Playwright CLI with `--config playwright.runtime-artifact.config.ts`. One line. Do not build a wrapper that packs, exports, and runs — the four inputs are environment-supplied by design and a wrapper would have to either hold them or fabricate them.

    Do NOT remove `**/e2e/agent-ready-export.spec.ts` from `playwright.config.ts:6-9`, and do not otherwise edit that file. State the reason explicitly in the ledger: the spec's fixtures call `readRuntimeArtifact()`, which throws when the four `CUMPA_RUNTIME_*` inputs are absent, so joining the default suite would make `npm run test:browser` hard-fail for every developer who has not packed an artifact. The correct fix for silent rot is a discoverable named runner plus a written recipe, not a default-suite entry that cannot pass by default.

    Extend `.planning/phases/12-behavior-continuity/12-CONTINUITY.md` with four sections, all in the `file:line` evidence style of `11-VERIFICATION.md:95-99`:

    A `## Re-authoring ledger` section: one row per re-authored call site, giving the test, the control, the old locator scope, the new locator scope, and which of the five `12-UI-SPEC.md` Test Reconciliation conditions it satisfies. Every row must be able to point at an observable replacement — accepted HTTP status, exported bytes, persisted draft, or attached stdout.

    A `## GREEN result` section: the command, the counts, and the note that the `afterAll` evidence gate cleared itself without an edit.

    A `## Runner reachability` section: the defect (the spec had zero executions across the milestone), the two excluding mechanisms with their `file:line` citations, the decision not to join the default suite with the reason above, the new script name, and the full local recipe — pack command, the four exported variables, and the runner invocation. Write the runner invocation with explicit spec paths (`npm run test:runtime-artifact -- tests/e2e/agent-ready-export.spec.ts tests/e2e/package-assets.spec.ts`) and state why: `playwright.runtime-artifact.config.ts:6-11` also matches `public-support-states.spec.ts` and `marketplace-review.spec.ts`, so a bare `npm run test:runtime-artifact` additionally reports the two documented external prerequisites recorded in the section below. Say plainly that those two failures are the recorded prerequisites and are not regressions and not a broken script — a future maintainer who runs the script bare must be able to tell the difference without re-deriving it.

    A `## Documented external prerequisites` section covering `tests/e2e/public-support-states.spec.ts` and `tests/e2e/marketplace-review.spec.ts`, with a three-point rationale for why recording them here does not violate the CON-02 no-skip rule: neither is among the six presentation-sensitive specs the ROADMAP names; neither is presentation-sensitive at all, so the restyle cannot have affected them; and both failed identically before the restyle began, per `08-VERIFICATION.md:62-70`. Cite `public-support-states.spec.ts:328-329` as the current throw site and note that the `:348` citation carried by Phases 08 and 11 is stale at HEAD, so the ledger stays checkable. Do not fabricate markers, a published package, or a hosted origin for either spec.
  </action>
  <acceptance_criteria>
    - `node -e "process.exit(require('./package.json').scripts['test:runtime-artifact'] ? 0 : 1)"` exits 0.
    - `node -e "process.exit(/playwright\.runtime-artifact\.config\.ts/.test(require('./package.json').scripts['test:runtime-artifact']) ? 0 : 1)"` exits 0.
    - `git diff --stat -- playwright.config.ts` is empty — the `testIgnore` entry is intentionally retained.
    - `git diff --stat -- package-lock.json` is empty — no dependency was added.
    - `grep -c '## Re-authoring ledger' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1.
    - `grep -c '## Runner reachability' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1.
    - `grep -c '## Documented external prerequisites' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1.
    - `grep -c 'public-support-states.spec.ts:328' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1 — the current throw site is cited, not the stale `:348`.
    - `grep -c 'marketplace-review.spec.ts:98' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1.
    - `grep -c 'test:runtime-artifact -- tests/e2e/agent-ready-export.spec.ts' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1 — the recorded recipe passes explicit spec paths rather than relying on a bare invocation.
    - `grep -c 'not regressions' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1 — the `## Runner reachability` section classifies the two extra failures a bare invocation produces.
    - `npm run test:runtime-artifact -- tests/e2e/agent-ready-export.spec.ts` with the Task 1 environment exported reports 6 passed.
  </acceptance_criteria>
  <verify>
    <automated>node -e "const s=require('./package.json').scripts['test:runtime-artifact']; if(!s || !/playwright\.runtime-artifact\.config\.ts/.test(s)) process.exit(1)" &amp;&amp; git diff --quiet -- playwright.config.ts package-lock.json</automated>
  </verify>
  <done>The runtime-artifact specs are reachable through a named script, the default-suite exclusion is retained with a written reason, and the CON-02 ledger records the re-authoring, the GREEN result, the reachability decision, and the two external prerequisites with current line citations.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged CLI → loopback browser session | The spec drives a real installed CLI over an ephemeral `127.0.0.1` origin. Assertions here are the only thing standing between a silent behaviour regression and a shipped release. |
| producer evidence → test harness | `readRuntimeArtifact()` trusts four environment-supplied values to identify the archive under test. A forged or mismatched set would let a different artifact masquerade as the one reviewed. |
| operator support origin → repository artifacts | The packer and the evidence documents both handle a support service URL that is operator-held in real releases. |
| test suite → release gate | A weakened assertion is indistinguishable from a passing one at the gate. This is the threat CON-02 exists to address. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-01-01 | Tampering | `tests/e2e/agent-ready-export.spec.ts` | mitigate | Re-authoring a spec to make it green is the exact shape of a suppressed regression. Bounded by the five `12-UI-SPEC.md` Test Reconciliation conditions, by retaining all seven `Finish review` sites and the `Finish attached review` region locator unchanged, by preserving every `waitForResponse` status pairing and every stdout/exported-bytes assertion, and by the Task 2 criteria pinning the skip count at exactly 1 (the pre-existing local-archive guard) and the fixme count at 0. |
| T-12-01-02 | Spoofing | `tests/helpers/runtime-artifact.ts` custody inputs | mitigate | Hand-written producer evidence would let an unreviewed archive pass as the reviewed one. Not attempted: `runtime-artifact.ts:259-288` cross-checks basename, byteLength, sha256, sha1, and sha512 against the real archive bytes. Task 1 produces all four inputs with the in-repo packer and derives the SHA-256 from the packer's own evidence JSON rather than by hand. |
| T-12-01-03 | Information disclosure | `12-CONTINUITY.md`, packer stderr | mitigate | A real operator support origin, or an absolute home path, must not reach a committed document. Task 1 uses only the syntactically valid synthetic origin, records custody paths as placeholders, and leaves the packer's stderr redaction at `scripts/pack-runtime.mjs:654-656` intact. Task 1 criteria assert no tarball, custody directory, or evidence JSON lands inside the repository tree. |
| T-12-01-04 | Repudiation | CON-02 evidence record | mitigate | An unverifiable ledger is worthless at the next milestone. Every ledger row carries a command, a count, and a `file:line` citation, and the stale `public-support-states.spec.ts:348` citation inherited from Phases 08 and 11 is corrected to the current `:328-329` so the record stays checkable. |
| T-12-01-05 | Denial of service | `npm run test:browser` | mitigate | Adding `agent-ready-export.spec.ts` to the default suite would hard-fail the standard developer gate, because `readRuntimeArtifact()` throws without the four custody inputs. Task 3 explicitly retains the `playwright.config.ts:6-9` exclusion and reaches the spec through a separate named script instead. |
| T-12-01-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs in this plan. The only `package.json` change is one `scripts` entry invoking an already-installed binary against an already-present config. Verified by `git diff --stat package-lock.json` being empty; no Package Legitimacy Audit is required because there are no install tasks. |
</threat_model>

<verification>
- With the Task 1 custody environment exported, `npm run test:runtime-artifact -- tests/e2e/agent-ready-export.spec.ts tests/e2e/package-assets.spec.ts` passes: 6 + 1 tests, 0 failed.
- `git diff --stat -- src/` is empty. Phase 12 changes zero production source files; a diff here means an earlier phase's contract was wrong and the finding belongs to that phase.
- `git diff --stat -- playwright.config.ts package-lock.json` is empty.
- `grep -c '\.skip(' tests/e2e/agent-ready-export.spec.ts` is exactly `1` and it is the pre-existing local-archive support guard.
- `.planning/phases/12-behavior-continuity/12-CONTINUITY.md` contains the RED baseline, the re-authoring ledger, the GREEN result, the runner-reachability decision, and the two documented external prerequisites.

**Known-red window:** between the start of Task 2 and its last ledger row, the spec is partially repaired and the failure count falls from 6 toward 0. That is the intended RED→GREEN gradient, not a broken intermediate state. Do not commit mid-ledger; land Task 2 as one change.

**Do not conflate:** Task 3's external-prerequisite record covers two specs that the Task 1 custody work does NOT unblock. `public-support-states.spec.ts:329` hard-refuses `local-archive`, which is exactly what a locally packed artifact is. Claiming otherwise in the ledger is a defect.
</verification>

<success_criteria>
- `tests/e2e/agent-ready-export.spec.ts` executes and passes 6/6 against a locally packed development-check artifact, with no operator secret.
- `openReviewNotes` is present exactly once, copied from the settled convention, and `ensureReviewOpen` is retained alongside it.
- The seven `Finish review` sites, the `Finish attached review` region locator, the `.review-summary__preview` and `.comments-rail__comment` assertions, and every accepted-HTTP pairing survive unchanged in shape.
- Skip count is exactly 1 and is the pre-existing local-archive guard; fixme count is 0; no conditional early return was added.
- `npm run test:runtime-artifact` exists and reaches the runtime-artifact config; `playwright.config.ts` is unchanged and the reason is written down.
- `12-CONTINUITY.md` records the RED baseline, the re-authoring ledger, the GREEN result, the reachability decision, and the two external prerequisites with current line citations.
- Zero files under `src/` changed.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Produced by |
|---|---|---|
| `openReviewNotes` helper and dialog-scoped call sites in `tests/e2e/agent-ready-export.spec.ts` | test | Task 2 |
| `test:runtime-artifact` script in `package.json` | config | Task 3 |
| `.planning/phases/12-behavior-continuity/12-CONTINUITY.md` — RED baseline, re-authoring ledger, GREEN result, runner-reachability decision, documented external prerequisites | doc (evidence) | Tasks 1 and 3 |
| Reproducible local custody recipe (pack command, four environment inputs, runner invocation) | doc (recipe, inside `12-CONTINUITY.md`) | Task 1 |

<output>
Create `.planning/phases/12-behavior-continuity/12-01-SUMMARY.md` when done.
</output>
