---
phase: 13-exact-patch-grounding
plan: 03
type: tdd
wave: 3
depends_on:
  - 13-02
files_modified:
  - src/cli/run.ts
  - src/contracts/draft.ts
  - src/export/review-export.ts
  - src/export/render-review-markdown.ts
  - tests/cli/selection.test.ts
  - tests/unit/review-export.test.ts
  - tests/unit/review-markdown.test.ts
autonomous: true
requirements:
  - PATCH-01
  - PATCH-03
  - PATCH-04
  - PATCH-05
must_haves:
  truths:
    - "PATCH-01: a strict mode=patch request is grounded once and opens createExactPatchSessionApp directly; selection, repair, approval, and range resolution are never entered."
    - "Exact patch launch consumes GroundedExactPatch and the PatchSnapshot-backed app from Plans 13-01/13-02; it never invokes a generic app factory, reparses patch bytes, or resolves live content after acceptance."
    - "PATCH-03/PATCH-05: canonical JSON and Markdown preserve the frozen patch digest, validation target, review key, snapshot provenance, raw paths, Git-derived states, and accepted feedback without browser-authored identity."
    - "PATCH-04/PATCH-05: ordinary export reads the accepted draft plus frozen snapshot authority and remains usable after source drift; terminal snapshot integrity/ownership loss blocks it before any live-source read."
    - "Interactive and revision-range launch/export contracts retain their current behavior and serialized forms."
  artifacts:
    - path: "src/cli/run.ts"
      provides: "Exclusive exact-patch dispatch through the actual Phase 13 grounding and app factories"
      contains: "createExactPatchSessionApp"
    - path: "src/contracts/draft.ts"
      provides: "Strict ReviewExportV3Schema/ReviewExportV3 exact-patch export provenance variant over the Plan 13-02 draft comparison"
      exports: ["ReviewExportV3Schema", "ReviewExportV3"]
    - path: "src/export/review-export.ts"
      provides: "Canonical exact-patch export builder consuming the strict ReviewExportV3 contract"
      exports: ["buildReviewExportV3"]
    - path: "src/export/render-review-markdown.ts"
      provides: "Deterministic Markdown rendering for exact-patch provenance and findings"
      exports: ["renderReviewMarkdown"]
    - path: "tests/cli/selection.test.ts"
      provides: "Direct patch dispatch and existing range/interactive non-regression evidence"
    - path: "tests/unit/review-export.test.ts"
      provides: "Strict exact-patch canonical JSON provenance and tamper-rejection evidence"
    - path: "tests/unit/review-markdown.test.ts"
      provides: "Exact-patch Markdown identity, escaping, and deterministic rendering evidence"
  key_links:
    - from: "src/cli/run.ts"
      to: "src/git/exact-patch.ts"
      via: "validated patch.content and patch.target call createGroundedExactPatch and receive GroundedExactPatch"
      pattern: "createGroundedExactPatch|GroundedExactPatch"
    - from: "src/cli/run.ts"
      to: "src/server/app.ts"
      via: "the grounded result is passed directly to createExactPatchSessionApp before browser launch"
      pattern: "createExactPatchSessionApp"
    - from: "src/export/review-export.ts"
      to: "src/contracts/draft.ts"
      via: "Plan 13-03 defines ReviewExportV3Schema beside the exact-patch draft comparison, then buildReviewExportV3 parses it"
      pattern: "ReviewExportV3Schema"
    - from: "src/export/review-export.ts"
      to: "src/export/render-review-markdown.ts"
      via: "canonical bytes retain one exact-patch provenance object for both output formats"
      pattern: "buildReviewExportV3|renderReviewMarkdown"
---

<objective>
Wire the accepted exact patch into the real CLI launch and ordinary export pipeline using only the interfaces produced by Plans 13-01 and 13-02.

Purpose: Separate non-visual launch/export integration from the browser task so one honest RED/GREEN/REFACTOR cycle can become fully green without hidden Vue work.
Output: Direct `createGroundedExactPatch` → `createExactPatchSessionApp` dispatch plus canonical exact-patch JSON/Markdown export with existing launch/export compatibility.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/13-exact-patch-grounding/13-CONTEXT.md
@.planning/phases/13-exact-patch-grounding/13-RESEARCH.md
@.planning/phases/13-exact-patch-grounding/13-PATTERNS.md
@.planning/phases/13-exact-patch-grounding/13-01-SUMMARY.md
@.planning/phases/13-exact-patch-grounding/13-02-SUMMARY.md
@src/cli/run.ts
@src/contracts/draft.ts
@src/export/review-export.ts
@src/export/render-review-markdown.ts
@tests/cli/selection.test.ts
@tests/unit/review-export.test.ts
@tests/unit/review-markdown.test.ts
</context>

<interfaces>
Use these exact upstream exports and no aliases:
- `createGroundedExactPatch(...)`, `GroundedExactPatch`, and `ExactPatchGroundingError` from `src/git/exact-patch.ts`/`src/contracts/comparison.ts`;
- `materializePatchSnapshot(...)`, `PatchSnapshot`, and `PatchSnapshotError` from `src/server/patch-snapshot.ts`;
- `createExactPatchSessionApp(grounded: GroundedExactPatch, options)` from `src/server/app.ts`;
- the exact-patch draft comparison produced in Plan 13-02, extended here with `ReviewExportV3Schema`/`ReviewExportV3`.

`run.ts` passes the validated request patch bytes and target once to `createGroundedExactPatch`, then passes that returned `GroundedExactPatch` to `createExactPatchSessionApp`. It does not invoke a generic app factory, construct `PatchSnapshot` itself, reuse `createSessionApp`, or resolve range selectors in patch mode.

`buildReviewExportV3(snapshot, patchScope, exportedAt)` extends the existing deterministic export family with the strict server-owned exact-patch provenance from Plan 13-02. `parseCanonicalReviewExport` accepts V1/V2/V3 while retaining strict per-version parsing. `renderReviewMarkdown` branches on the parsed contract and never accepts browser-computed digest, target, key, path, or status authority.
</interfaces>

<feature>
  <name>Exact-patch dispatch and frozen ordinary export</name>
  <files>src/cli/run.ts, src/contracts/draft.ts, src/export/review-export.ts, src/export/render-review-markdown.ts, tests/cli/selection.test.ts, tests/unit/review-export.test.ts, tests/unit/review-markdown.test.ts</files>
  <behavior>
    - A valid exact-patch request calls `createGroundedExactPatch` once and `createExactPatchSessionApp` once, bypassing every interactive/range-only branch.
    - `ExactPatchGroundingError` or `PatchSnapshotError` fails before browser launch with bounded actionable terminal diagnostics and no patch-byte/path/object leakage.
    - Existing interactive and revisions requests still use their current factories and selection behavior.
    - Exact-patch canonical JSON/Markdown binds findings to full digest, validation target, review key, snapshot provenance, raw exact paths, file status/modes/availability, and internal base/head anchor compatibility while presenting preimage/postimage semantics.
    - Source drift does not disable ordinary export because export reads frozen snapshot/draft evidence; `snapshotUnavailable` blocks export without object/worktree fallback.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — specify direct dispatch and canonical frozen export</name>
  <files>tests/cli/selection.test.ts, tests/unit/review-export.test.ts, tests/unit/review-markdown.test.ts</files>
  <behavior>
    - Patch dispatch calls the exact Wave 1 grounder and Wave 2 app factory once with the same grounded authority, then opens the browser only after the app is ready.
    - Patch dispatch never calls interactive selection, range pinning, repair, approval, `createSessionApp`, or any generic app factory.
    - Exact-patch JSON/Markdown round-trips strict server provenance, exact path/status/mode facts, comments, summary, and deterministic bytes while rejecting spoofed/mismatched/unknown identity.
    - Frozen export remains valid when patch status is `drifted`; terminal `snapshotUnavailable` rejects before any current repository/worktree content read.
    - Existing interactive/range launch plus V1/V2 export cases remain green.
  </behavior>
  <action>
Extend the three existing focused test files before production edits. In `tests/cli/selection.test.ts`, submit the strict patch request from Plan 13-01 through the existing injected CLI seams; assert one `createGroundedExactPatch` call receives exact content/target, its `GroundedExactPatch` result is passed unchanged to `createExactPatchSessionApp`, browser launch waits for the exact app, and grounding/snapshot failure exits without launching. Keep the current interactive and revisions cases in the same command.

In the export tests, build the strict exact-patch draft/export provenance created by Plan 13-02. Assert canonical V3 parse/serialize identity, deterministic ordering, full digest/target/review-key/snapshot provenance, raw old/new paths, Git statuses/modes/availability, accepted comments/summary, hostile-text escaping, and preimage/postimage Markdown wording. Assert browser-authored or mismatched digest, target, key, provenance, path, and unknown fields fail. Model `drifted` as readable frozen authority and `snapshotUnavailable` as the only export block, with spies proving neither case consults current/live bytes.

Run the focused command. RED must fail only because direct exact dispatch and V3 export building/rendering are absent; existing interactive/range and V1/V2 fixtures must pass.
  </action>
  <verify>
    <automated>npm exec -- vitest run tests/cli/selection.test.ts tests/unit/review-export.test.ts tests/unit/review-markdown.test.ts</automated>
    <expected>RED: exits nonzero on missing createExactPatchSessionApp dispatch and exact-patch V3 JSON/Markdown behavior while existing interactive/range and V1/V2 cases pass.</expected>
  </verify>
  <done>The identical focused suite isolates missing launch/export integration without depending on unfinished Vue presentation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — dispatch the grounded patch and render frozen exports</name>
  <files>src/cli/run.ts, src/contracts/draft.ts, src/export/review-export.ts, src/export/render-review-markdown.ts</files>
  <behavior>
    - The exact request branch uses only GroundedExactPatch and createExactPatchSessionApp.
    - JSON/Markdown derive identity and file facts from server-owned exact-patch provenance.
    - Drifted exports remain ordinary frozen exports; terminal snapshot loss blocks without fallback.
    - Existing launch and export variants stay compatible.
  </behavior>
  <action>
In `run.ts`, branch on the already-validated request discriminant before interactive/range selection. For `mode: "patch"`, call `createGroundedExactPatch` with server-owned `cwd`, exact submitted content, strict target, and cancellation signal, then pass the returned `GroundedExactPatch` directly to `createExactPatchSessionApp`. Await the actual exact app readiness before browser launch and translate `ExactPatchGroundingError`/`PatchSnapshotError` through bounded terminal diagnostics. Do not invoke a generic app factory, `createSessionApp`, selectors, repair, approval, or live content resolution in this branch; leave the interactive/revisions branches unchanged.

Extend `src/contracts/draft.ts` with strict `ReviewExportV3Schema`/`ReviewExportV3` over the exact-patch draft comparison from Plan 13-02, retaining V1/V2 parsing unchanged. Implement `buildReviewExportV3` against that schema and extend canonical parsing without weakening earlier versions. Map only accepted draft and snapshot-owned exact-patch provenance; preserve raw path/status/mode/availability and anchor facts. Extend `renderReviewMarkdown` to identify `Exact patch`, full digest, validation target, and frozen provenance, use truthful preimage/postimage terminology, and retain established escaping/actionability sections. Do not revisit draft storage, add a new export action, or read repository/worktree content. A `drifted` status does not block these frozen builders; `snapshotUnavailable` is rejected at the existing capability boundary before builder entry.

Run the identical focused command until it passes.
  </action>
  <verify>
    <automated>npm exec -- vitest run tests/cli/selection.test.ts tests/unit/review-export.test.ts tests/unit/review-markdown.test.ts</automated>
    <expected>GREEN: exits 0 with direct GroundedExactPatch → createExactPatchSessionApp launch, strict V3 canonical JSON/Markdown, drift-readable frozen export, terminal no-fallback blocking, and existing variants passing.</expected>
  </verify>
  <done>The exact patch launches through the real Phase 13 factories and ordinary exports stay bound to frozen server evidence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — preserve one dispatch and one export authority</name>
  <files>src/cli/run.ts, src/contracts/draft.ts, src/export/review-export.ts, src/export/render-review-markdown.ts</files>
  <action>
After GREEN, perform behavior-preserving cleanup only. Remove any duplicated source-branch or export mapping introduced in GREEN by using existing discriminants and canonical helpers; retain one patch branch, one `buildReviewExportV3`, and one parsed Markdown renderer branch. Do not move patch parsing into CLI/export code, create aliases for the exact upstream names, add a generic factory/provider, or change any request/export wire shape. If GREEN is already direct, record a no-op REFACTOR instead of manufacturing churn.
  </action>
  <verify>
    <automated>npm exec -- vitest run tests/cli/selection.test.ts tests/unit/review-export.test.ts tests/unit/review-markdown.test.ts</automated>
    <expected>REFACTOR: exits 0 with every direct-dispatch, strict provenance, drift/snapshot, hostile-data, deterministic-output, and interactive/range non-regression assertion unchanged from GREEN.</expected>
  </verify>
  <done>REFACTOR changes structure only, never behavior; the same focused command remains green.</done>
</task>

</tasks>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Submit an exact already-applied patch and review only immutable repository-proven bytes | 13-01 + 13-02 + 13-03 + 13-04 | COVERED |
| REQ | PATCH-01 | Exclusive input, direct launch, patch-only workspace | 13-01 + 13-03 + 13-04 | COVERED |
| REQ | PATCH-02 | Repository-object preimages and byte-exact implemented postimages | 13-01 | COVERED |
| REQ | PATCH-03 | Git-derived path/status/mode/type/unsupported truth | 13-01 + 13-02 + 13-03 + 13-04 | COVERED |
| REQ | PATCH-04 | No worktree/index/ref/object-store mutation | 13-01 + 13-02 + 13-03 + 13-04 | COVERED |
| REQ | PATCH-05 | Frozen readable review, explicit drift, terminal snapshot failure without fallback | 13-02 + 13-03 + 13-04 | COVERED |
| RESEARCH | — | Strict server-side grounding and GroundedExactPatch ownership | 13-01 + 13-03 | COVERED |
| RESEARCH | — | PatchSnapshot-only capabilities and one /api/patch-status contract | 13-02 + 13-04 | COVERED |
| RESEARCH | — | Exact draft/export identity and no browser authority | 13-02 + 13-03 | COVERED |
| RESEARCH | — | Approved exact-patch Vue presentation and grounded browser suites | 13-04 | COVERED |
| CONTEXT | — | No D-XX decisions; all implementation choices discretionary and no deferred ideas | 13-01 + 13-02 + 13-03 + 13-04 | COVERED |
</source_coverage>

<threat_model>
## ASVS Level 1 — blocking threshold: HIGH

Execution MUST stop before GREEN if any HIGH threat lacks its concrete mitigation and focused proof. MEDIUM/LOW findings require an explicit disposition in the summary.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Validated request → CLI dispatch | The discriminant must select one source mechanism without mixed authority or pre-launch side effects. |
| GroundedExactPatch → exact session app | Frozen accepted bytes cross into PatchSnapshot ownership without aliasing or live resolution. |
| Frozen draft/snapshot → export | Only server-derived provenance and evidence may enter canonical JSON/Markdown. |
| Untrusted review strings → serialized outputs | Paths/comments/summary must remain inert data and cannot forge export structure or instructions. |

## STRIDE Threat Register

| Threat ID | Category | ASVS L1 mapping | Component | Severity | Disposition | Blocking mitigation and proof |
|---|---|---|---|---|---|---|
| T-13-03-01 | Spoofing / Elevation of Privilege | V4.1 access control; V5.1 input validation | CLI dispatch | HIGH | mitigate | Strict request discriminant, exact upstream factories only, no browser identity, and tests proving patch mode never enters selector/repair/range factories. |
| T-13-03-02 | Tampering | V8.3 integrity | grounded app handoff | HIGH | mitigate | Pass the same GroundedExactPatch to createExactPatchSessionApp once; grounding/snapshot failures stop before browser; no alias, reconstruction, or current-content read. |
| T-13-03-03 | Spoofing / Repudiation | V5.1 validation; V8.3 integrity | canonical export | HIGH | mitigate | ReviewExportV3Schema parses full server digest/target/key/provenance and exact file facts; mismatch/unknown-field tests block output. |
| T-13-03-04 | Information Disclosure / XSS | V5.3 output encoding; V7.4 safe errors | terminal and Markdown | HIGH | mitigate | Bounded grounding/snapshot diagnostics omit patch/path/object bytes; established Markdown fencing and structural JSON keep hostile values inert. |
| T-13-03-05 | Tampering | V8.3 integrity; V12.1 file resources | drifted export | HIGH | mitigate | Export uses frozen capability input while drifted and blocks on snapshotUnavailable; spies prove no object/worktree content fallback. |
| T-13-03-SC | Tampering | V14.2 dependency integrity | package supply chain | LOW | accept | No install or dependency change; use existing CLI, Zod, canonical serializer, Vitest, and export renderer only. |
</threat_model>

<verification>
1. RED, GREEN, and REFACTOR run the identical focused Vitest command and record the expected outcome stated in each task.
2. Inspect dispatch evidence: exact mode calls `createGroundedExactPatch` then `createExactPatchSessionApp`; no generic app factory, `createSessionApp`, selector, repair, approval, or live content path is called.
3. Parse exact canonical JSON as V3 and verify full frozen provenance, exact file facts, findings, deterministic bytes, and rejection of spoofed/unknown identity.
4. Render exact Markdown from those canonical bytes and verify truthful terminology, fixed instructions, hostile-data fencing, and no browser-derived identity.
5. Prove ordinary frozen export succeeds while source status is `drifted`; `snapshotUnavailable` blocks before any live/object content read.
6. Run no broad suite, formatter, linter, build, browser, package install, or application smoke test in this plan.
</verification>

<success_criteria>
- Patch mode uses the exact upstream GroundedExactPatch, ExactPatchGroundingError, PatchSnapshot, PatchSnapshotError, and createExactPatchSessionApp contracts with no stale aliases.
- One honest RED/GREEN/REFACTOR sequence uses the same focused command and each phase states its expected outcome.
- Canonical JSON/Markdown preserve strict frozen exact-patch provenance and remain ordinary exports after source drift.
- Terminal snapshot integrity/ownership loss blocks export without live fallback.
- Interactive/range launch and V1/V2 exports remain compatible, and every HIGH threat has focused mitigation evidence.
</success_criteria>

<output>
Create `.planning/phases/13-exact-patch-grounding/13-03-SUMMARY.md` after execution with RED/GREEN/REFACTOR evidence, exact dispatch call chain, export V3 shape, drift/snapshot behavior, and non-regression results.
</output>
