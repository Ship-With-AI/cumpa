---
phase: 12-request-protocol-range-grounding
plan: 03
type: tdd
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - src/cli/run.ts
  - src/contracts/api.ts
  - src/server/capabilities.ts
  - tests/api/session.test.ts
  - tests/cli/request.test.ts
  - tests/cli/selection.test.ts
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - AGENT-03
  - RANGE-03
must_haves:
  truths:
    - "AGENT-01: normal non-TTY stdin is owned by the versioned request path, never Inquirer, and one valid request launches exactly one pinned session."
    - "AGENT-02: malformed requests and rejected revisions, ancestry, or native Git pathspecs produce exit 1, bounded actionable stderr, empty stdout, and no listener/browser launch."
    - "AGENT-03: TTY launch preserves the injected discover, ordered pick, pin, confirm, and launch authority already asserted in tests/cli/selection.test.ts."
    - "RANGE-03: the session response projects the frozen full base/head OIDs and exact ordered pathspecs from one pinned comparison without exposing storage or Git authority."
  artifacts:
    - path: "src/cli/run.ts"
      provides: "Pre-prompt TTY/non-TTY dispatch and typed pre-browser request/range failure boundary"
    - path: "src/contracts/api.ts"
      provides: "Strict optional range projection for the authenticated session response"
    - path: "src/server/capabilities.ts"
      provides: "Server-derived immutable session scope from the pinned comparison"
    - path: "tests/cli/selection.test.ts"
      provides: "Existing injected TTY event-order and recovery authority"
    - path: "tests/cli/request.test.ts"
      provides: "Injected non-TTY launch and safe invalid-native-pathspec process contract"
    - path: "tests/api/session.test.ts"
      provides: "Frozen range session projection and authority-exclusion contract"
  key_links:
    - from: "src/cli/run.ts"
      to: "src/cli/request.ts and src/git/comparison.ts"
      via: "non-TTY action reads one request, builds one pinned range, and only then calls launchPinnedComparison"
      pattern: "readAgentReviewRequest|createPinnedRangeComparison"
    - from: "src/cli/run.ts"
      to: "tests/cli/selection.test.ts"
      via: "TTY branch delegates unchanged to dependency-injected runCli orchestration"
      pattern: "runCli"
    - from: "src/server/capabilities.ts"
      to: "src/contracts/api.ts"
      via: "SessionResponseSchema parses a projection derived only from PinnedComparison.range"
      pattern: "SessionResponseSchema"
---

<objective>
Wire the strict request and immutable range into the production entry action while preserving the existing injected TTY flow, then expose only the approved frozen scope in the session response.

Purpose: Establish the safe pre-browser process boundary before persistence, export, and browser presentation build on the same pinned comparison.
Output: Dependency-injected TTY/non-TTY dispatch, stable invalid-pathspec failure handling, strict range session projection, and focused regression contracts.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-request-protocol-range-grounding/12-CONTEXT.md
@.planning/phases/12-request-protocol-range-grounding/12-RESEARCH.md
@.planning/phases/12-request-protocol-range-grounding/12-PATTERNS.md
@.planning/phases/12-request-protocol-range-grounding/12-UI-SPEC.md
@.planning/phases/12-request-protocol-range-grounding/12-01-SUMMARY.md
@.planning/phases/12-request-protocol-range-grounding/12-02-SUMMARY.md
@src/cli/run.ts
@src/contracts/api.ts
@src/server/capabilities.ts
@tests/api/session.test.ts
@tests/cli/request.test.ts
@tests/cli/selection.test.ts
</context>

<interfaces>
Plan 12-01 provides the strict request schema, published bounds, `AgentRequestError`, and `readAgentReviewRequest`. Plan 12-02 provides `createPinnedRangeComparison`, immutable `PinnedComparison.range`, and the typed invalid-pathspec range error.

Add a strict optional range projection to `SessionResponseSchema` only when `PinnedComparison.range` exists:
- `kind: "revisions"`
- `baseOid` and `headOid`: full object IDs matching the session endpoints
- `pathspecs`: exact readonly ordered strings

Do not expose submitted revision labels, repository root, blob OIDs, review key, draft/export filesystem paths, session token, or capability IDs through this projection.
</interfaces>

<feature>
  <name>Pre-browser request dispatch and frozen session scope</name>
  <files>src/cli/run.ts, src/contracts/api.ts, src/server/capabilities.ts, tests/cli/request.test.ts, tests/cli/selection.test.ts, tests/api/session.test.ts</files>
  <behavior>
    - The existing packaged launch seam remains intact. For the ordinary no-option action, TTY delegates to unchanged `runCli({ cwd })`; non-TTY reads exactly one request before any picker behavior exists.
    - A valid non-TTY range request creates one immutable comparison and invokes the existing `launchPinnedComparison` exactly once.
    - Request, revision, ancestry, and native Git pathspec rejection become stable bounded stderr diagnostics with status 1, empty stdout, and zero listener/browser launch calls.
    - The invalid-pathspec diagnostic is actionable but does not echo Git stderr or submitted pathspec text.
    - TTY preservation is proved through the existing injected `tests/cli/selection.test.ts` event-order and recovery authority; no PTY harness or non-TTY branch-text bypass is introduced.
    - The authenticated session response receives exact full OIDs and ordered pathspecs from the frozen comparison and no browser-controlled scope/storage authority.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — specify stream ownership, safe range failure, and session projection</name>
  <files>tests/cli/request.test.ts, tests/cli/selection.test.ts, tests/api/session.test.ts</files>
  <read_first>
    - `.planning/phases/12-request-protocol-range-grounding/12-01-SUMMARY.md` and `12-02-SUMMARY.md` — consume actual request/range exports and error kinds rather than guessing names.
    - `tests/cli/request.test.ts` — extend the focused request suite with an injected ordinary-action boundary; keep raw framing cases intact.
    - `tests/cli/selection.test.ts` — use its existing `RunCliDependencies` event-order and recovery assertions as the sole TTY authority; do not create a PTY harness.
    - `tests/api/session.test.ts` — extend the existing frozen `PinnedComparison` and Fastify capability fixture instead of creating a second app.
    - `src/cli/run.ts` — preserve `runCli`, `launchPinnedComparison`, `CUMPA_LAUNCH_OPTIONS`, Commander help, stderr URL/fallback output, and shutdown ownership.
    - `src/contracts/api.ts` and `src/server/capabilities.ts` — preserve strict response schemas, opaque capabilities, and existing security projection.
  </read_first>
  <action>
Write failing assertions before production integration. Extend `tests/cli/request.test.ts` with an injected ordinary-action harness that controls TTY state, stdin chunks, request parsing, range construction, stderr/stdout sinks, exit status, and launch capability. Prove TTY delegates only to `runCli`; non-TTY delegates only to the request path; a valid range launches one frozen comparison; and request, revision, ancestry, and invalid native Git pathspec failures return status 1 with one bounded actionable stderr line, empty stdout, and no listener/browser launch. The invalid-pathspec case must use the typed Plan 12-02 range error and assert neither raw Git stderr nor the submitted pathspec is present.

Keep `tests/cli/selection.test.ts` in the same RED command and extend only where necessary to drive the new injected ordinary-action TTY branch while retaining its complete discover → ordered Base/Head pick → pin → confirm → launch and recovery assertions. Do not invent a PTY harness, do not modify `tests/performance/production-picker.mjs`, and do not accept piped branch text as interactive input.

In `tests/api/session.test.ts`, add a frozen range comparison fixture and future failing assertions that the session returns matching full base/head OIDs plus exact ordered pathspecs, stays unchanged if submitted refs move, and omits labels, review key, root, blob OIDs, token, filesystem paths, and client-supplied scope authority. Preserve all existing interactive response vectors.
  </action>
  <acceptance_criteria>
    - The focused future command fails on missing ordinary-action dispatch and range session projection, not on fixture setup.
    - Invalid native Git pathspec rejection has the same process contract as every other pre-launch range failure: nonzero status, bounded safe stderr, empty stdout, zero listener/browser launch.
    - The existing injected TTY test remains the source of truth for full interactive ordering and recovery.
    - Interactive sessions remain schema-compatible and range sessions expose only the approved immutable projection.
  </acceptance_criteria>
  <verify>
    <automated>npm exec -- vitest run tests/cli/request.test.ts tests/cli/selection.test.ts tests/api/session.test.ts</automated>
    <expected>RED during execution: exits nonzero only on the newly specified dispatch, invalid-pathspec process, and range session behavior.</expected>
  </verify>
  <done>The RED gate records the production ownership and frozen-session contract before modifying the entrypoint or API projection.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — route before prompts and project one immutable session scope</name>
  <files>src/cli/run.ts, src/contracts/api.ts, src/server/capabilities.ts</files>
  <read_first>
    - `tests/cli/request.test.ts`, `tests/cli/selection.test.ts`, and `tests/api/session.test.ts` — implement only the behavior established by RED.
    - `src/cli/run.ts` — reuse current injected orchestration and fatal output conventions without changing `runCli()` behavior.
    - `src/contracts/api.ts` — add one strict optional range response member and infer types from Zod.
    - `src/server/capabilities.ts` — derive the response projection from the already-frozen comparison; keep file/blob/anchor capability maps unchanged.
  </read_first>
  <action>
In `src/cli/run.ts`, keep `runCli()` and the packaged `CUMPA_LAUNCH_OPTIONS` path unchanged. Extract only the smallest dependency-injected ordinary-action function required by the RED tests. Decide ownership from the injected TTY boolean before any picker construction: TTY calls current `runCli({ cwd })`; non-TTY calls `readAgentReviewRequest`, maps the validated revisions request into `createPinnedRangeComparison`, then passes the returned frozen comparison to existing `launchPinnedComparison`.

At that boundary, catch only the typed request and range launch errors. Translate invalid native Git pathspec rejection to the stable actionable diagnostic supplied by Plan 12-02, write one bounded line to stderr, set status 1, and return before listener/browser launch. Apply the same stdout-empty, no-launch contract to revision and ancestry errors. Preserve unexpected-error behavior, never sniff JSON shape, never echo request/Git text, and never add a PTY or performance-harness bypass.

Extend `SessionResponseSchema` with the optional strict range projection from `<interfaces>`. Have `createCapabilityRegistry` populate it only from `comparison.range`, cross-check its OIDs against session endpoints through schema parsing, preserve the exact frozen pathspec order, and expose no server-held key/path/object authority. Leave interactive session bytes and capability behavior unchanged.
  </action>
  <acceptance_criteria>
    - A valid non-TTY request reaches `launchPinnedComparison` exactly once without any picker call.
    - Every typed request/range failure, including invalid native Git pathspec syntax, returns status 1 with bounded actionable stderr, empty stdout, and zero listener/browser launch.
    - The TTY branch uses the existing injected `runCli` flow and retains its full event order and recovery behavior.
    - Range session scope is derived only from the frozen comparison and interactive session contracts remain valid.
  </acceptance_criteria>
  <verify>
    <automated>npm exec -- vitest run tests/cli/request.test.ts tests/cli/selection.test.ts tests/api/session.test.ts</automated>
    <expected>GREEN during execution: exits 0 with stream ownership, safe invalid-pathspec failure, TTY ordering, and frozen session projection passing.</expected>
  </verify>
  <done>The identical focused command that established RED passes through the production entry action and session capability owner.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — remove only duplicate dispatch or projection glue</name>
  <files>src/cli/run.ts, src/server/capabilities.ts</files>
  <read_first>
    - `src/cli/run.ts` — inspect GREEN for duplicated status/output/launch branching while preserving distinct TTY and non-TTY owners.
    - `src/server/capabilities.ts` — inspect GREEN for duplicated endpoint projection without moving authority into the browser.
    - `tests/cli/request.test.ts`, `tests/cli/selection.test.ts`, and `tests/api/session.test.ts` — retain the identical focused command and assertions.
  </read_first>
  <action>
After GREEN, perform only behavior-preserving cleanup. Extract a private helper solely when it removes actual duplicate safe-error output or immutable endpoint projection without hiding the TTY/non-TTY ownership decision. Do not add new behavior, new assertions, a dispatch framework, strategy interface, error registry, PTY harness, or second capability model. If GREEN is already direct and non-duplicative, record a no-op REFACTOR gate. Run the identical focused command after any cleanup.
  </action>
  <acceptance_criteria>
    - RED precedes GREEN and REFACTOR changes no public behavior or test expectation.
    - TTY and non-TTY ownership remain visibly distinct, and the pre-browser gate cannot be bypassed.
    - Session projection remains server-derived, strict, immutable, and free of storage/Git authority.
    - No new dependency, subprocess harness, protocol sniffing, client filter, or compatibility shim exists.
  </acceptance_criteria>
  <verify>
    <automated>npm exec -- vitest run tests/cli/request.test.ts tests/cli/selection.test.ts tests/api/session.test.ts</automated>
    <expected>REFACTOR during execution: exits 0 with the complete ownership, failure, TTY, and session contracts unchanged.</expected>
  </verify>
  <done>The production process/session seam remains small and behavior-identical after optional cleanup.</done>
</task>

</tasks>

<source_coverage>
| Source | ID | Feature / requirement | Plan | Status |
|--------|----|-----------------------|------|--------|
| GOAL | — | Safe versioned range request reaches one pinned browser session while TTY remains interactive | 12-03 | COVERED |
| REQ | AGENT-01 | One piped versioned request, no interactive prompt | 12-01 + 12-03 | COVERED |
| REQ | AGENT-02 | Actionable pre-browser failure matrix, including invalid native Git pathspec | 12-01 + 12-02 + 12-03 | COVERED |
| REQ | AGENT-03 | Existing injected TTY picker/browser flow retained | 12-03 | COVERED |
| REQ | RANGE-03 | Frozen comparison scope projected to session without browser authority | 12-02 + 12-03 | COVERED |
| RESEARCH | — | Thin non-TTY adapter around existing CLI with typed safe failure boundary | 12-03 | COVERED |
| RESEARCH | — | Native Git accepts pathspec syntax; invalid syntax becomes stable pre-browser range error | 12-02 + 12-03 | COVERED |
| CONTEXT | — | No D-XX locked decisions; phase boundary preserved | 12-03 | COVERED |
| UI-SPEC | — | Invalid requests have no browser UI and frozen range scope is read-only | 12-03 | COVERED |
</source_coverage>

<threat_model>
## ASVS Level 1 — blocking threshold: HIGH

Execution must stop and report a blocker before commit if any HIGH-severity threat below is unmitigated. MEDIUM/LOW findings require an explicit disposition in the plan summary.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Process stdin/TTY state → CLI owner | Exactly one owner must be selected before prompt or request consumption. |
| Validated request/pinned comparison → listener/browser | Only a fully grounded immutable comparison may cross the launch boundary. |
| Git/request errors → terminal | Untrusted content must become bounded stable diagnostics without stdout protocol ambiguity. |
| Frozen comparison → session API | The browser receives approved scope facts and opaque capabilities, never storage or Git authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-03-01 | Spoofing / Tampering | stdin owner dispatch | HIGH | mitigate | Select by the strict TTY boolean before prompt construction; no content sniffing or branch-text bypass. |
| T-12-03-02 | Elevation of Privilege | pre-launch gate | HIGH | mitigate | Complete request and Git grounding before `launchPinnedComparison`; typed failures have zero bind/open capability. |
| T-12-03-03 | Spoofing / Information Disclosure | invalid pathspec diagnostics | HIGH | mitigate | Translate the typed range error to one bounded actionable stderr line without Git stderr, request text, or stdout. |
| T-12-03-04 | Elevation of Privilege / Information Disclosure | session projection | HIGH | mitigate | Derive strict range scope from the frozen comparison and omit root, blobs, keys, tokens, paths, and client-supplied authority. |
| T-12-03-05 | Repudiation | TTY regression | MEDIUM | mitigate | Retain the existing injected full event-order and recovery assertions in `tests/cli/selection.test.ts`. |
| T-12-03-SC | Tampering | package supply chain | LOW | accept | No package-manager install or new dependency; reuse the locked repository toolchain. |
</threat_model>

## Artifacts this phase produces

| Path | New or changed symbol / artifact | Contract |
|------|----------------------------------|----------|
| `src/cli/run.ts` | dependency-injected ordinary-action dispatcher | Preserves packaged and TTY paths; non-TTY grounds one request before launch and reports typed failures safely. |
| `src/contracts/api.ts` | optional strict range session projection | Full pinned endpoint OIDs plus exact ordered pathspecs only. |
| `src/server/capabilities.ts` | frozen session projection | Projects range scope without exposing storage/Git authority. |
| focused CLI/API tests | ownership, invalid-pathspec, TTY, session contracts | Future RED/GREEN/REFACTOR evidence uses the same focused Vitest command. |

<verification>
1. During execution, run the identical focused Vitest command in RED, GREEN, and REFACTOR and record the initial missing-behavior failure before production edits.
2. Inspect the injected invalid-pathspec case: status 1, bounded stderr, empty stdout, and no listener/browser launch call.
3. Confirm `tests/cli/selection.test.ts` remains the TTY authority and no PTY/performance-harness bypass was added.
4. Confirm the range session projection is exact and frozen while interactive response fixtures remain unchanged.
</verification>

<success_criteria>
- AGENT-01 reaches one existing pinned session from one non-TTY request without prompts.
- AGENT-02 includes stable invalid-native-pathspec failure before browser/listener launch with bounded stderr and empty stdout.
- AGENT-03 remains protected by the existing injected interactive ordering/recovery tests.
- RANGE-03 session scope uses full pinned OIDs and exact ordered pathspecs from one immutable comparison.
- RED, GREEN, and behavior-preserving REFACTOR use one focused future command.
</success_criteria>

<output>
Create `.planning/phases/12-request-protocol-range-grounding/12-03-SUMMARY.md` with RED failure evidence, GREEN passing evidence, REFACTOR disposition, typed process-error vectors, TTY event order, and session projection artifacts.
</output>
