---
phase: 12-request-protocol-range-grounding
plan: 06
type: tdd
wave: 5
depends_on:
  - 12-01
  - 12-02
  - 12-03
  - 12-04
  - 12-05
files_modified:
  - src/web/App.vue
  - src/web/components/IdentityHeader.vue
  - src/web/components/IdentityPanel.vue
  - src/web/styles.css
  - tests/e2e/pinned-session.spec.ts
autonomous: true
requirements:
  - AGENT-01
  - AGENT-02
  - RANGE-03
must_haves:
  truths:
    - "AGENT-01: the generated CLI accepts one valid piped range request and opens directly into the existing authenticated workspace with only scoped files."
    - "AGENT-02: an invalid native Git pathspec exits nonzero with bounded stderr, empty stdout, and no opener/listener event or browser UI."
    - "RANGE-03: the browser discloses full pinned OIDs and exact ordered control-safe pathspecs from the frozen session without client filtering or ref refresh."
    - "Range empty, pinned-content error, retry, desktop region, narrow modal, focus, Escape, and focus-return behavior match 12-UI-SPEC exactly."
    - "Interactive identity UI and ordinary review behavior remain unchanged."
  artifacts:
    - path: "tests/e2e/pinned-session.spec.ts"
      provides: "Future failing generated-binary and browser assertions followed by passing range UI integration evidence"
    - path: "src/web/components/IdentityHeader.vue"
      provides: "Conditional View review scope disclosure on the existing pinned identity header"
    - path: "src/web/components/IdentityPanel.vue"
      provides: "Accessible Review scope region/dialog with full OIDs and semantic ordered pathspecs"
    - path: "src/web/App.vue"
      provides: "Server-authoritative scoped inventory plus exact range empty/error/retry copy"
    - path: "src/web/styles.css"
      provides: "Existing-token responsive range scope layout, wrapping, focus, and forced-colors behavior"
  key_links:
    - from: "tests/e2e/pinned-session.spec.ts"
      to: "generated compare CLI and browser workspace"
      via: "real piped request, opener/listener observation, and authenticated Playwright navigation"
      pattern: "spawn"
    - from: "src/web/components/IdentityPanel.vue"
      to: "SessionResponse.range.pathspecs"
      via: "semantic ordered list of controlSafeDisplay values without sorting or filtering"
      pattern: "controlSafeDisplay"
    - from: "src/web/App.vue"
      to: "SessionResponse.files and pinned content capabilities"
      via: "existing FileTree and Monaco sources remain server-authoritative"
      pattern: "session.files"
---

<objective>
Drive the approved range UI from future failing generated-path E2E assertions to passing Vue/CSS behavior, then permit only behavior-preserving cleanup.

Purpose: Prove a developer sees and can inspect the exact immutable Git scope while invalid requests still fail before any browser exists.
Output: RED/GREEN/REFACTOR E2E evidence, conditional scope disclosure, exact range states, and unchanged interactive workspace behavior.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/12-request-protocol-range-grounding/12-CONTEXT.md
@.planning/phases/12-request-protocol-range-grounding/12-RESEARCH.md
@.planning/phases/12-request-protocol-range-grounding/12-PATTERNS.md
@.planning/phases/12-request-protocol-range-grounding/12-UI-SPEC.md
@.planning/phases/12-request-protocol-range-grounding/12-03-SUMMARY.md
@.planning/phases/12-request-protocol-range-grounding/12-04-SUMMARY.md
@.planning/phases/12-request-protocol-range-grounding/12-05-SUMMARY.md
@src/web/App.vue
@src/web/components/IdentityHeader.vue
@src/web/components/IdentityPanel.vue
@src/web/styles.css
@tests/e2e/pinned-session.spec.ts
</context>

<interfaces>
Consume the optional range projection established by Plan 12-03: `kind: "revisions"`, full `baseOid`, full `headOid`, and exact readonly ordered `pathspecs`. Do not derive scope from labels, refs, FileTree rows, draft/export data, URL state, or browser input.

Use the exact `12-UI-SPEC.md` copy and interaction contract. The browser remains a viewer/editor for review feedback, not a request builder or protocol recovery surface.
</interfaces>

<feature>
  <name>Immutable review-scope disclosure and generated-path browser proof</name>
  <files>tests/e2e/pinned-session.spec.ts, src/web/App.vue, src/web/components/IdentityHeader.vue, src/web/components/IdentityPanel.vue, src/web/styles.css</files>
  <behavior>
    - A generated executable receiving one valid pipe bypasses prompts and opens the existing authenticated workspace with only the server-scoped file inventory and pinned blob content.
    - Invalid native Git pathspec syntax exits nonzero with bounded actionable stderr, empty stdout, and no opener/listener URL or browser launch.
    - Agent range header action reads `View review scope`; the existing panel becomes `Review scope` with Base commit, Head commit, no duplicate merge-base row, and Ordered Git pathspecs.
    - Full OIDs and exact control-safe pathspecs are selectable/copyable; pathspecs use a semantic ordered list in request order and `All changed paths` when empty.
    - Range zero inventory and pinned-content failure/retry use the exact approved copy; session files and pinned content capabilities remain the only FileTree/Monaco sources.
    - Desktop region, narrow modal focus containment, Tab, Escape, focus return, mobile targets, wrapping, forced colors, and reduced motion reuse existing behavior.
    - Interactive identity copy, file navigation, comments, summary, persistence, export, and ordinary review flow remain unchanged.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — add future failing generated-path and range UI assertions</name>
  <files>tests/e2e/pinned-session.spec.ts</files>
  <read_first>
    - `.planning/phases/12-request-protocol-range-grounding/12-UI-SPEC.md` — copy exact text, hierarchy, state matrix, responsive behavior, accessibility behavior, and non-goals.
    - `tests/e2e/pinned-session.spec.ts` — reuse its generated CLI, process-stream, loopback URL, external opener, authenticated browser, viewport, focus, and pinned-content helpers; do not create a development-only route.
    - `src/web/components/IdentityHeader.vue` and `IdentityPanel.vue` — assert existing interactive markup/copy remains unchanged while specifying conditional range behavior.
    - `src/web/App.vue` and `src/web/styles.css` — assert against established FileTree/Monaco/state and responsive/focus patterns, not implementation-local class names unless already public test hooks.
  </read_first>
  <action>
Before any Vue or CSS edit, extend `tests/e2e/pinned-session.spec.ts` with future failing generated-binary assertions. Pipe a valid versioned range request and assert no prompt text, one opener/listener handoff, authenticated workspace entry, only Git-scoped files, pinned blob content, and unchanged displayed scope after submitted refs move. Add an invalid native Git pathspec process case asserting nonzero exit, bounded actionable stderr, empty stdout, and no opener/listener URL or browser launch; assert neither Git stderr nor the submitted value leaks.

Add future failing browser assertions for exact `View review scope`, `Review scope`, `Base commit`, `Head commit`, `Ordered Git pathspecs`, `All changed paths`, full matching OIDs, semantic ordered control-safe pathspecs, value-specific copy labels, and no merge-base row in range mode. Cover exact range empty state, pinned unavailable error, retry copy, no client-side file filtering, desktop labelled region, narrow labelled modal dialog, focus move/containment, Tab, Escape, disclosure focus return, wrapping without ellipsis, mobile target size, and unchanged interactive identity/workspace behavior.

The RED command is the exact build plus Playwright command used by GREEN and REFACTOR. It must fail on the missing range UI assertions after earlier plans have made the generated request/process path functional. Do not add a PTY harness or move TTY preservation out of `tests/cli/selection.test.ts`.
  </action>
  <acceptance_criteria>
    - The future command builds the prior plans, reaches the new E2E cases, and fails on missing Vue/CSS range behavior rather than fixture or launch setup.
    - The invalid native pathspec case proves the real generated path never emits stdout or opens a listener/browser.
    - Assertions cover exact UI-SPEC copy, scope order, pinned content, empty/error/retry states, focus, responsiveness, and interactive non-regression.
    - No production UI file changes during RED.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm exec -- playwright test tests/e2e/pinned-session.spec.ts</automated>
    <expected>RED during execution: exits nonzero on the newly added range UI assertions after generated-path setup succeeds.</expected>
  </verify>
  <done>The RED gate records real generated-path and browser behavior gaps before any Vue/CSS implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the approved immutable range scope UI</name>
  <files>src/web/App.vue, src/web/components/IdentityHeader.vue, src/web/components/IdentityPanel.vue, src/web/styles.css</files>
  <read_first>
    - `tests/e2e/pinned-session.spec.ts` — implement only the future failing behavior established by RED.
    - `.planning/phases/12-request-protocol-range-grounding/12-UI-SPEC.md` — exact copy and interaction contract is authoritative.
    - `src/web/components/IdentityHeader.vue` — preserve heading, pin cue, dirty badges, disclosure focus export, and interactive label.
    - `src/web/components/IdentityPanel.vue` — extend the existing region/dialog, close focus, Tab containment, CopyButton, and identity rows instead of adding a panel.
    - `src/web/App.vue` — preserve identity open/close/Escape/focus return, drawers, server-authoritative `session.files`, and pinned Monaco content client.
    - `src/web/styles.css` — reuse existing GitHub-dark tokens, spacing/type scales, responsive sheet, focus, forced-colors, and reduced-motion rules.
  </read_first>
  <action>
Implement only the approved range projection in the existing components. `IdentityHeader` conditionally labels the same disclosure button `View review scope`. `IdentityPanel` conditionally uses `Review scope`, Base/Head commit rows, no merge-base row, an `<ol>` of exact `controlSafeDisplay()` values in request order, value-specific copy labels, `All changed paths` when empty, and the exact pinned-scope footer. Preserve interactive markup, copy, focus exports, and behavior.

In `App.vue`, keep `session.files` as the sole FileTree source and pinned content capabilities as the sole Monaco source. Add only the conditional range empty and pinned-unavailable/retry copy from UI-SPEC; do not filter, sort, normalize, recompute, refresh, or follow refs in the browser. Preserve comments, summary, drafts, exports, existing error semantics, and selection behavior.

Add the smallest CSS required for semantic ordered rows, selectable/wrapping full values, approved 520/480px panel bounds, mobile full-width sheet and 44px targets, using current tokens and focus/forced-colors/reduced-motion behavior. Add no new component, icon, dependency, animation, protocol screen/editor, patch UI, Finish action, or result claim. Run the exact same build plus Playwright command from RED; it must now pass.
  </action>
  <acceptance_criteria>
    - The identical future command that established RED passes after only the four Vue/CSS files change.
    - Full pinned OIDs and exact ordered safe pathspecs are disclosed/copyable without becoming client-side authority.
    - Exact empty, unavailable, retry, region/dialog, focus, Escape, responsive, and interactive non-regression assertions pass.
    - Invalid request/pathspec process cases still exit before any browser/listener and emit no stdout.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm exec -- playwright test tests/e2e/pinned-session.spec.ts</automated>
    <expected>GREEN during execution: exits 0 with generated range launch, failure gate, immutable scope UI, accessibility, responsive, and interactive cases passing.</expected>
  </verify>
  <done>The exact future RED command passes through the approved existing Vue workspace.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR — perform behavior-preserving UI cleanup only</name>
  <files>src/web/App.vue, src/web/components/IdentityHeader.vue, src/web/components/IdentityPanel.vue, src/web/styles.css</files>
  <read_first>
    - The GREEN Vue/CSS changes — inspect only for duplicated range/interative projection or styles that existing component patterns can replace.
    - `tests/e2e/pinned-session.spec.ts` — retain the exact RED/GREEN command and all assertions without adding new behavior.
  </read_first>
  <action>
After GREEN, permit only behavior-preserving refactoring. Remove actual duplicate computed projection or CSS declarations by reusing existing helpers/tokens when that leaves DOM semantics, copy, ordering, focus, responsive geometry, FileTree/Monaco authority, and process behavior unchanged. Do not add, broaden, or reinterpret behavior; do not add tests, features, abstractions, components, dependencies, animations, browser filtering, or protocol UI. If GREEN is already direct and consistent, record a no-op REFACTOR gate. Run the identical build plus Playwright command after any cleanup.
  </action>
  <acceptance_criteria>
    - RED precedes GREEN and REFACTOR changes no assertion, copy string, DOM role, focus transition, responsive result, process output, or server-authority boundary.
    - The same future command remains green without adding behavior during REFACTOR.
    - No client filter, ref refresh, new UI surface, compatibility shim, or dependency exists.
  </acceptance_criteria>
  <verify>
    <automated>npm run build &amp;&amp; npm exec -- playwright test tests/e2e/pinned-session.spec.ts</automated>
    <expected>REFACTOR during execution: exits 0 with every RED/GREEN generated-path and UI assertion unchanged.</expected>
  </verify>
  <done>The range UI is behavior-identical after optional cleanup, with no REFACTOR scope creep.</done>
</task>

</tasks>

<source_coverage>
## Phase 12 Multi-Source Coverage Audit

| Source | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Safe versioned range request with exact pinned Git scope and unchanged developer launch | 12-01 through 12-06 | COVERED | Protocol, Git grounding, process, draft, export, and UI form one dependency-ordered path. |
| REQ | AGENT-01 | One piped versioned request, no interactive prompt | 12-01 + 12-03 + 12-06 | COVERED | Reader, production dispatch, and generated-path proof. |
| REQ | AGENT-02 | Complete actionable pre-browser failure matrix | 12-01 + 12-02 + 12-03 + 12-06 | COVERED | Includes invalid native Git pathspec with process and generated-path evidence. |
| REQ | AGENT-03 | Existing TTY picker/browser flow retained | 12-03 | COVERED | Existing injected `tests/cli/selection.test.ts` authority; no PTY harness. |
| REQ | RANGE-01 | Explicit ancestor endpoints resolved once to full OIDs | 12-02 | COVERED | Native commit peel and ancestry predicate. |
| REQ | RANGE-02 | Exact ordered native include/exclude/attribute pathspec semantics | 12-02 | COVERED | Git accepts exact argv; Compare does not reinterpret. |
| REQ | RANGE-03 | Inventory, blobs, draft identity, and feedback bound to pinned scope | 12-02 + 12-03 + 12-04 + 12-05 + 12-06 | COVERED | Frozen comparison/key propagates through session, draft, export, and UI. |
| RESEARCH | — | Published 1 MiB request, 4 KiB string, and 256 pathspec limits | 12-01 | COVERED | Shared contract exports and boundary tests. |
| RESEARCH | — | Resolve once, explicit ancestry/equality, base-OID diff | 12-02 | COVERED | Native Git TDD matrix. |
| RESEARCH | — | Native attribute magic accepted unchanged; invalid magic safely translated | 12-02 + 12-03 + 12-06 | COVERED | Range, process, and generated-path contracts. |
| RESEARCH | — | Interactive export V1 and strict range export V2 | 12-05 | COVERED | Versioned union with preserved V1 bytes. |
| RESEARCH | — | Domain-separated scope key owns draft/export provenance | 12-02 + 12-04 + 12-05 | COVERED | Separate persistence and publication plans. |
| RESEARCH | — | No package install | all | COVERED | Existing Node, Zod, Git, Vitest, and Playwright only. |
| CONTEXT | — | No D-XX locked decisions; all choices discretionary within boundary | all | COVERED | Research-backed smallest safe integration. |
| CONTEXT | — | No deferred ideas | all | COVERED | No deferred/future item introduced. |
| UI-SPEC | — | Scope panel, exact range copy, responsive/accessibility states, unchanged interactive UI | 12-06 | COVERED | Dedicated RED/GREEN/behavior-preserving REFACTOR plan. |

No source item is missing. Future request multiplexing, patch mode, attached Finish/result delivery, headless/HTTP control, optional instructions, and cross-submission collision integration remain excluded because REQUIREMENTS/ROADMAP assign them outside Phase 12.
</source_coverage>

<threat_model>
## ASVS Level 1 — blocking threshold: HIGH

Execution must stop and report a blocker before commit if any HIGH-severity threat below is unmitigated. MEDIUM/LOW findings require an explicit disposition in the plan summary.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Generated stdin process → listener/browser | Only valid fully grounded requests may create a browser session. |
| Session scope → Vue presentation | Untrusted labels/pathspecs are display data, never client authority. |
| Frozen session files/content → FileTree/Monaco | Browser must consume server capabilities without ref or pathspec recomputation. |
| Keyboard/viewport → scope panel | Accessible region/dialog focus behavior must not trap or lose user focus. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-12-06-01 | Elevation of Privilege | generated launch gate | HIGH | mitigate | E2E invalid-pathspec case asserts nonzero, bounded stderr, empty stdout, and no listener/browser event. |
| T-12-06-02 | Spoofing / Information Disclosure | scope presentation | HIGH | mitigate | Use server projection and control-safe display; copy canonical full values without rendering controls or secrets. |
| T-12-06-03 | Tampering | client inventory/content | HIGH | mitigate | Keep `session.files` and opaque pinned content capabilities as the sole FileTree/Monaco sources; no client filter or ref refresh. |
| T-12-06-04 | Denial of Service | long scope display | MEDIUM | mitigate | Reuse published request count/string caps, internal panel scrolling, and wrapping without unbounded browser-derived data. |
| T-12-06-05 | Spoofing | modal focus/roles | MEDIUM | mitigate | Reuse labelled region/dialog, containment, Escape, focus return, visible focus, forced-colors, and reduced-motion behavior. |
| T-12-06-SC | Tampering | package supply chain | LOW | accept | No package-manager install, registry content, or new dependency; reuse existing Vue/CSS/Playwright toolchain. |
</threat_model>

## Artifacts this phase produces

| Path | New or changed symbol / artifact | Contract |
|------|----------------------------------|----------|
| `tests/e2e/pinned-session.spec.ts` | generated request/process and browser range matrix | Future failing RED assertions, same-command GREEN, behavior-preserving REFACTOR. |
| `src/web/components/IdentityHeader.vue` | conditional scope disclosure | Exact approved action without changing interactive identity. |
| `src/web/components/IdentityPanel.vue` | accessible immutable range scope | Full OIDs, semantic ordered exact pathspecs, no duplicate merge base. |
| `src/web/App.vue` | range state copy on existing workspace | Server-authoritative files/content and exact empty/error/retry behavior. |
| `src/web/styles.css` | responsive scope presentation | Existing tokens, wrapping, target sizes, focus, and forced-colors behavior. |

<verification>
1. During execution, add future failing E2E assertions first and run the exact same build plus Playwright command for RED, GREEN, and REFACTOR.
2. Confirm RED fails on missing range UI after generated request setup succeeds; GREEN passes after only Vue/CSS work.
3. Confirm invalid native pathspec produces nonzero exit, bounded stderr, empty stdout, and no opener/listener/browser event.
4. Confirm REFACTOR is behavior-preserving only: no assertion, copy, DOM role, focus, responsive, process, or authority change.
</verification>

<success_criteria>
- A valid generated range request opens the existing authenticated workspace with only frozen scoped files/content.
- Invalid native Git pathspec syntax fails before listener/browser launch with bounded stderr and empty stdout.
- Exact scope, empty/error/retry, accessibility, responsive, and interactive non-regression behavior matches UI-SPEC.
- UI/E2E work follows explicit RED, same-command GREEN, then behavior-preserving-only REFACTOR.
- All six Phase 12 requirements remain covered across plan frontmatter and source audit.
</success_criteria>

<output>
Create `.planning/phases/12-request-protocol-range-grounding/12-06-SUMMARY.md` with RED failure evidence, GREEN passing evidence, REFACTOR disposition, generated-process outputs, browser assertions, and UI artifacts.
</output>
