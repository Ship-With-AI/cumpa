---
phase: 07-github-familiar-review-surfaces
plan: 06
type: execute
wave: 6
depends_on:
  - 07-05
files_modified:
  - src/web/components/ExportSection.vue
  - src/web/components/ExportProgress.vue
  - src/web/components/DriftExportAcknowledgement.vue
  - src/web/components/ExportReceipt.vue
  - src/web/components/ReceiptFileRow.vue
  - src/web/components/GitignoreStatus.vue
  - src/web/components/ExportReadinessSummary.vue
  - src/web/styles.css
  - tests/integration/export-receipt-ui.spec.ts
autonomous: true
requirements:
  - REVW-04
must_haves:
  truths:
    - "Users scan one framed Export section whose ready, drift, conflict, pending, failed, unavailable, exported, re-export, ignore, and receipt states use the Phase 07 icon/label/edge language while every existing transition and explicit action remains unchanged (D-09, D-13, D-16)."
    - "Export starts only from the explicit current primary action: Export review, Export review again, or Export pinned review; opening, saving, reloading, acknowledging drift, or appending .gitignore never exports automatically."
    - "Selector drift remains an acknowledgement gate over the original pinned comparison, can export without refresh, invalidates consent on a second drift, and never hides pinned/current identities or exact consent copy."
    - "Progress shows one shared spinner, exact preparing/validating/publishing stage copy, and no partial success; failure never displays a new success receipt and any previous confirmed pair is explicitly subordinate."
    - "A success receipt appears only after both files are validated/published and retains repository-relative paths, SHA-256, byte counts, accepted revision, timestamp, drift details, copy, and reveal actions without exposing absolute filesystem authority."
    - "Gitignore consent remains optional and independent from export; only the two-step explicit Append ignore rule action mutates, with localized `Appending one ignore rule…` feedback and no false success/rollback claim."
  artifacts:
    - path: "src/web/components/ExportSection.vue"
      provides: "Framed explicit export action and complete branch-specific status hierarchy"
    - path: "src/web/components/ExportProgress.vue"
      provides: "Exact publication-stage status using only shared `.ui-spinner`; obsolete `.export-progress__spinner` and duplicate export keyframes are removed"
    - path: "src/web/components/DriftExportAcknowledgement.vue"
      provides: "Pinned/current identity warning and explicit consent presentation"
    - path: "src/web/components/ExportReceipt.vue"
      provides: "Confirmed/previous receipt hierarchy and bounded copy/reveal outcomes"
    - path: "src/web/components/ReceiptFileRow.vue"
      provides: "Repository-relative file/hash/byte presentation and copy feedback"
    - path: "src/web/components/GitignoreStatus.vue"
      provides: "Checking/success/warning/error/pending consent language with localized append busy state"
    - path: "src/web/components/ExportReadinessSummary.vue"
      provides: "Labeled readiness values and non-actionable attention/empty-review notices"
    - path: "src/web/styles.css"
      provides: "Export frame, readiness, drift, progress, failure, previous/current receipt, gitignore, wrapping, and busy styling"
    - path: "tests/integration/export-receipt-ui.spec.ts"
      provides: "Focused real-browser contract for explicit export, all status branches, receipt integrity, drift, ignore consent, and no false success"
  key_links:
    - from: "src/web/components/ExportSection.vue"
      to: "src/web/model/review-draft-state.ts"
      via: "existing ReviewExportState.phase/pending/progress/conflict/drift/receipt branches remain the sole state authority"
      pattern: "exportState.phase|exportState.progress|exportState.receipt"
    - from: "src/web/components/ExportSection.vue"
      to: "src/web/components/ExportProgress.vue"
      via: "pending branch passes accepted revision and existing stage"
      pattern: "ExportProgress.*revision.*stage"
    - from: "src/web/components/ExportProgress.vue"
      to: "src/web/styles.css"
      via: "progress markup consumes only `.ui-spinner`; the legacy `.export-progress__spinner` hook and `export-spinner` keyframes are deleted with no compatibility alias or retained class"
      pattern: "ui-spinner"
    - from: "src/web/components/ExportSection.vue"
      to: "src/web/components/ExportReceipt.vue"
      via: "exported confirmed receipt and failed previousConfirmedReceipt branches remain distinct"
      pattern: "previousConfirmedReceipt|receipt|previous"
    - from: "src/web/components/GitignoreStatus.vue"
      to: "src/contracts/api.ts"
      via: "existing fixed appendIgnoreRule/refreshIgnoreStatus closures and typed outcomes"
      pattern: "appendIgnoreRule|refreshIgnoreStatus"
    - from: "src/web/components/ExportReceipt.vue"
      to: "src/web/components/ReceiptFileRow.vue"
      via: "existing validated receipt files in canonical review.json then review.md order"
      pattern: "receipt.files\[0\]|receipt.files\[1\]"
  prohibitions:
    - "No export API/client/schema/publication/persistence/drift/revision/readiness/.gitignore mutation change and no new browser filesystem/ref/path authority."
    - "No automatic export, refresh, drift acceptance, .gitignore edit, partial-success receipt, guessed anchor relocation, or absolute filesystem path disclosure."
    - "No change to exact action/consent/support/failure/receipt copy recorded in the approved UI-SPEC except presentation wrappers and icon markup."
    - "No status icon as a click target, spinner-only progress, success styling on failed attempts, hidden previous-failure context, new breakpoint, remote asset, package, or final Phase 08 claim."
---

<objective>
Finish the shared GitHub-familiar status language across Export, selector drift, readiness, progress, receipts, and optional gitignore consent without changing any export or filesystem behavior.

Purpose: Complete REVW-04 and the Phase 07 roadmap goal across the most state-dense review surface while preserving the existing publication/safety contract exactly.
Output: Export-specific state presentation, focused real-browser proof, and complete phase source/decision coverage evidence.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md
@.planning/phases/07-github-familiar-review-surfaces/07-RESEARCH.md
@.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md
@.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md
@.planning/phases/07-github-familiar-review-surfaces/07-05-SUMMARY.md
@.planning/intel/API-SURFACE.md

<interfaces>
`ExportSection.vue` receives `ReviewExportState` and fixed closures/events. Its `stateLabel` and exact branches already distinguish drift, pending, exported, failed, unavailable, conflict, and ready. Preserve them rather than creating a second state machine.

`ExportProgress.vue` receives `revision` and `stage: 'preparing' | 'validating' | 'publishing'` and already owns exact progressive copy. Plan 01/04 provide the shared `.ui-spinner` and notice tone/icon anatomy. The clean cutover uses only `.ui-spinner`: remove `.export-progress__spinner`, its reduced-motion selector, and duplicate `@keyframes export-spinner` entirely, with no compatibility alias or optional retained class.

`ExportReceipt.vue` receives a validated receipt plus fixed `revealExportDirectory()` and optional `previous`; `ReceiptFileRow` receives already validated repository-relative file facts. Existing copy/reveal focus and message behavior remains authoritative.

`GitignoreStatus.vue` receives typed ignore status plus fixed no-browser-authority append/refresh closures. Existing two-step confirmation, append-only result handling, focus, and export independence are safety contracts.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Frame Export and normalize ready, drift, conflict, pending, failed, and unavailable states</name>
  <read_first>
    - `src/web/components/ExportSection.vue` — modified complete export branch/disclosure/action source of truth.
    - `src/web/components/ExportProgress.vue` — modified exact stage/status/spinner source.
    - `src/web/components/DriftExportAcknowledgement.vue` — modified drift identity/consent/stale-acknowledgement source.
    - `src/web/styles.css` — modified Export frame, notice, action, progress, wrapping, and responsive hooks.
    - `src/web/components/ui/UiIcon.vue` — Plan 01 fixed local icon source.
    - `src/web/components/ui/ReviewStateBadge.vue` — Plan 02 compact status presentation source.
    - `src/web/components/InlineNotice.vue` — Plan 04 full notice anatomy.
    - `src/web/model/review-draft-state.ts` — no-change ReviewExportState phase/transition authority.
    - `src/contracts/api.ts` — no-change export/drift/request/result contracts; API-SURFACE hint is non-authoritative.
    - `.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md` — D-09, D-13 through D-16.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — Summary and Export, Stale Revision, Selector Drift, Progress/Failure, exact copy, and verification items 14–18.
    - `.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md` — ExportSection/Progress existing branch analog and no-backend-change boundary.
  </read_first>
  <files>src/web/components/ExportSection.vue, src/web/components/ExportProgress.vue, src/web/components/DriftExportAcknowledgement.vue, src/web/styles.css</files>
  <action>Keep Export last and retain its disclosure, stateLabel computation, refs/focus watcher, props, emits, and exact branch ordering. Present its current state label through the shared icon-plus-label badge and bind the existing explicit primary CTA only in the ready/exported/drift-confirmed task region. Preserve exact actions/copy: `Export review`, `Export review again`, `Export pinned review`, neutral `Try export again`, `Reload latest`, `Return to review`, `Cancel export`, the accepted-revision support sentence, and every current disable rule. Opening/collapsing, reloading, reviewing unsaved text, or acknowledging drift must never emit export except the explicit action.

Give conflict/unavailable/failure/unsaved states the shared full-notice icon/heading/body/3px-edge anatomy while retaining current `role`, aria-labelledby, focus targets, and exact text. Keep expected/actual revision and `Nothing from this export attempt was published.` visible. A failed attempt with `previousConfirmedReceipt` remains error-dominant and the previous receipt stays explicitly labeled/subordinate; never style the failed attempt as success.

In `DriftExportAcknowledgement.vue`, retain pinned Base/Head plus current moved/unavailable identities, unchecked default consent, exact consent label, stale `Selected sources changed again`, and second-drift invalidation. Apply warning structure and independent selected/focus/disabled/busy controls without changing events or requiring refresh. Keep `Export pinned review` disabled until current consent and never export automatically after acknowledgement.

Replace the ExportProgress spinner markup with the shared `.ui-spinner` class while preserving `role="status"`, `aria-live`, exact three stage headings, accepted revision, and both-files publication copy. Delete the obsolete `.export-progress__spinner` selector, its reduced-motion rule, and duplicate `@keyframes export-spinner` from `styles.css`; do not retain the old class in markup, compose it with `.ui-spinner`, or add a compatibility alias. The sole shared spinner is hidden from the accessibility tree, rotates at the shared 600ms rate while motion is allowed, stops under the existing shared reduced-motion rule, and is never the only progress cue. Frame Export with the existing major-section contract; use min-width zero/anywhere wrapping and inherited breakpoints only.
  <acceptance_criteria>
    - Export remains the last framed rail section with unchanged disclosure, state ownership, branch transitions, refs, and focus destinations.
    - Ready/exported/drift use exact explicit primary actions; all non-export actions are neutral and cannot export automatically.
    - Conflict/failure/unavailable/unsaved states have icon+heading/body+edge and exact roles/copy; failure cannot show a new success receipt.
    - Drift consent starts unchecked, gates Export pinned review, retains pinned/current identities, supports export without refresh, and invalidates on a second observation.
    - Pending stages use only one shared hidden `.ui-spinner` plus exact preparing/validating/publishing words and both-files integrity copy; `.export-progress__spinner` and `@keyframes export-spinner` are absent with no alias or retained class, and reduced motion preserves meaning.
    - No export request/result, revision, drift, publication, persistence, focus, or filesystem authority changes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/integration/export-receipt-ui.spec.ts --grep "Phase 07 explicit export and status states|retains it after reveal and re-export failures|recovery surface open"</automated>
    <browser-check>Run the focused export spec with `--headed`; exercise ready, unsaved, drift, second drift, conflict, pending stages, failed, recovery-required, unavailable, exported, and re-export. Verify exact explicit CTAs, no auto-export, state icons/edges/roles, previous receipt subordination, focus, and reduced-motion progress.</browser-check>
  </verify>
  <done>The complete Export state machine is visually explicit and GitHub-familiar without any behavior, authority, or publication change.</done>
</task>

<task type="auto">
  <name>Task 2: Complete readiness, receipt, copy reveal, and gitignore consent hierarchy</name>
  <read_first>
    - `src/web/components/ExportReceipt.vue` — modified confirmed/previous receipt, drift disclosure, copy/reveal, focus, and message source.
    - `src/web/components/ReceiptFileRow.vue` — modified file identity/hash/bytes/copy presentation source.
    - `src/web/components/GitignoreStatus.vue` — modified checking/ignored/unavailable/notIgnored/confirmation/pending/outcome source.
    - `src/web/components/ExportReadinessSummary.vue` — modified readiness counts and attention/empty notices.
    - `src/web/styles.css` — modified receipt/readiness/gitignore/status/action/wrapping styles.
    - `src/web/components/ui/UiIcon.vue` — shared fixed local glyph source.
    - `src/web/components/ui/ReviewStateBadge.vue` — shared compact status source.
    - `src/web/components/InlineNotice.vue` — shared full notice source.
    - `src/contracts/api.ts` — no-change validated receipt and ignore outcome types.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — Readiness, Gitignore Consent, Publication Integrity, Receipt, copy, and verification items 17–19.
    - `.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md` — receipt/gitignore state analog and existing shared-spinner recommendation.
  </read_first>
  <files>src/web/components/ExportReceipt.vue, src/web/components/ReceiptFileRow.vue, src/web/components/GitignoreStatus.vue, src/web/components/ExportReadinessSummary.vue, src/web/styles.css</files>
  <action>Retain ExportReadinessSummary calculations and labels exactly: Accepted revision, Open actionable, Need reviewer attention, Resolved, Summary, and Ignore status. Present values with compact structural badges only where informative, keep stale/unavailable anchors explicitly non-actionable, preserve the exact attention instruction not to guess/relocate, and style empty/attention blocks through information/warning notice anatomy without turning values/rows into click targets.

In ExportReceipt, keep success display conditional on the existing confirmed receipt, exact `Review export complete`, both-files sentence, accepted revision, `<time datetime>`, drift details, comparison/acknowledged identity disclosures, repository-relative paths, algorithm, full SHA-256, byte counts, and actions. Give the current confirmed receipt success/check hierarchy; keep `previous` dashed, neutral, explicitly `Previous confirmed export`, and subordinate to a later failure. Preserve copy/reveal status/alert roles, current focus movement, and exact reveal failure. In ReceiptFileRow, keep validated file order/values and copy behavior; add non-color copy success/failure structure without exposing absolute paths or raw filesystem capability.

In GitignoreStatus, keep export independence and exact states/actions. Checking uses pending icon + status heading/body; ignored uses success/check; unavailable/failure uses error; not ignored and ambiguous/unconfirmed outcomes use warning/error as currently classified. Preserve the two-step `Add to .gitignore` confirmation, exact `/.diff-review/` append-only copy, `Keep .gitignore unchanged`, `Append ignore rule`, `Try append again`, focus/Escape, and fixed closures. While appending, show the shared spinner + exact `Appending one ignore rule…` + local aria-busy only on the confirmation/action; never claim success, replacement, or rollback for ambiguous/unconfirmed outcomes. Style receipt/readiness/gitignore content with flat frames, no shadow, min-width zero, anywhere wrapping, and inherited action wrapping.</action>
  <acceptance_criteria>
    - Readiness retains all six labeled values and exact actionable/non-actionable classification/instruction without new click targets.
    - Current receipt uses success icon/heading/edge only after confirmed pair publication and contains every required revision/time/drift/path/hash/byte/action field.
    - Previous receipt is dashed, visibly labeled, subordinate to failure, and never mistaken for success of the later attempt.
    - Receipt and file-row copy/reveal outcomes retain existing roles/focus and expose repository-relative data only.
    - Gitignore checking/ignored/notIgnored/unavailable/pending/outcome states have explicit icon/text/structure; only confirmed two-step append invokes mutation and export remains independent.
    - Appending busy state is local, exact, reduced-motion safe, and never makes an ambiguous outcome sound successful or rolled back.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/integration/export-receipt-ui.spec.ts</automated>
    <browser-check>Run the export receipt spec with `--headed`; inspect ready/attention values, confirmed and previous receipts, copy/reveal success/failure, checking/ignored/not-ignored/unavailable gitignore states, confirmation, pending, retry, ambiguous/unconfirmed outcomes, and wrapped long hashes/paths.</browser-check>
  </verify>
  <done>Receipt/readiness/gitignore states satisfy D-16 and the approved publication-safety presentation contract without changing export or filesystem behavior.</done>
</task>

<task type="auto">
  <name>Task 3: Prove the complete export status and safety matrix in the existing browser harness</name>
  <read_first>
    - `tests/integration/export-receipt-ui.spec.ts` — modified real Vue/Vite browser harness covering receipt, recovery-required, and ignore consent.
    - `src/web/components/ExportSection.vue` — modified Task 1 state/action contract under test.
    - `src/web/components/ExportProgress.vue` — modified Task 1 progress contract under test.
    - `src/web/components/DriftExportAcknowledgement.vue` — modified Task 1 drift consent contract under test.
    - `src/web/components/ExportReceipt.vue` — modified Task 2 receipt contract under test.
    - `src/web/components/ReceiptFileRow.vue` — modified Task 2 file detail contract under test.
    - `src/web/components/GitignoreStatus.vue` — modified Task 2 consent/outcome contract under test.
    - `src/web/components/ExportReadinessSummary.vue` — modified Task 2 classification contract under test.
    - `tests/integration/agent-ready-export-states.spec.ts` — no-change Vitest state-transition/source-of-truth analog; do not duplicate reducer assertions in source-text tests.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — verification expectations 14–19, 21–24 and exact copy matrix.
  </read_first>
  <files>tests/integration/export-receipt-ui.spec.ts</files>
  <action>Extend the existing Playwright/Vite harness and fixtures to expose the authoritative existing export branches without replacing the state reducer or HTTP client. Add a focused `Phase 07 explicit export and status states` journey that asserts ready and empty accepted review, unsaved text exclusion, drift consent, second-drift invalidation, revision conflict, all three pending stages, failed/recovery-required, unavailable, confirmed exported, and re-export states. Record request count/order to prove only explicit Export review/again/pinned/retry controls emit export; opening/collapsing, Reload latest, drift checkbox, Cancel export, review-unsaved-text, ignore refresh, and append consent do not.

Assert every error/warning/information/success/pending/disabled/open/resolved state used by Export has a visible label/heading, decorative fixed icon, and structural edge/boundary beyond computed color. For progress, assert the DOM uses only `.ui-spinner`, never the removed `.export-progress__spinner` class, and reduced motion stops the shared spinner transform while exact progress words/role remain. Cover current versus previous receipt hierarchy, no partial receipt during progress/failure, exact repository-relative paths/full hashes/bytes/revision/time/drift, copy/reveal focus outcomes, and failure no-success semantics. Cover gitignore checking, ignored, not ignored, unavailable, confirm, Escape, appending, appended, unchanged, appendUnconfirmed, ambiguous, and unconfirmed outcomes with exact mutation counts and export independence. Retain existing safety assertions and use the production components/state contracts rather than static source-text tests.
  <acceptance_criteria>
    - The browser matrix covers every approved Export state and exact CTA/copy without relying on static source inspection.
    - Request evidence proves export occurs only after an explicit export action and gitignore mutation only after explicit second confirmation.
    - Drift can export the pinned comparison without refresh, second drift invalidates consent, and neither acknowledgement nor reload auto-exports.
    - Progress/failure never exposes partial/new success; current/previous receipts remain correctly distinguished with complete validated metadata.
    - Every state meaning includes visible text/icon/structure and retains roles, focus, keyboard, disabled, reduced-motion, wrapping, and inherited overflow behavior.
    - No API/schema/persistence/publication/filesystem authority is altered to construct a test state.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/integration/export-receipt-ui.spec.ts</automated>
    <browser-check>Repeat the focused file with `--headed`, reduced motion, and representative 1280/768/640 widths. Inspect every state and overlap, exact CTA/consent copy, current/previous receipt hierarchy, no partial success, long receipt wrapping, focus, and document overflow.</browser-check>
  </verify>
  <done>REVW-04 and the export/status portion of the approved Phase 07 contract are fully proven in the existing real-browser convention.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New exported files or business symbols:** none.
- **Existing local symbols newly presented, not changed:** `ExportSection.stateLabel`, `ExportProgress.heading`, `ReviewExportState.phase/progress/conflict/driftObservation/driftStale/receipt/previousConfirmedReceipt/ignoreStatus`, and GitignoreStatus `confirming`, `appending`, `appendOutcome`.
- **New fixed CSS hooks:** `.export-section__status-badge`, `.export-section__notice-content`, `.export-section__action--busy`, `.export-progress__content`, `.export-receipt__status`, `.export-receipt__previous-label`, `.receipt-file-row__status`, `.export-readiness__status`, `.gitignore-status__state`, and `.gitignore-status__action--busy`.
- **New visible presentation:** fixed status icons/edges/badges and shared spinner consumption; exact product state labels and actions already exist.
- **Removed presentation hook and animation:** `.export-progress__spinner`, its reduced-motion selector, and duplicate `@keyframes export-spinner` are deleted. `ExportProgress` markup uses only `.ui-spinner`; there is no compatibility alias, composed legacy class, optional retained selector, or second keyframes animation.
- **New API endpoint/client method, request/result field, draft/export JSON/Markdown schema field, persistence/publication/drift/ignore behavior, feature flag, package, dependency, registry block, remote asset, environment variable, or human setup:** none.
- **Schema-push assessment:** not applicable. Phase paths contain only Vue/CSS/Playwright files; no ORM/schema/migration pattern exists, so no blocking live-schema push task is fabricated.

<contribution_decisions>
- TDD mode is active, but this plan changes presentation and browser glue over an existing tested export state machine. No defined-I/O business logic/API/data transform is introduced, so it remains `type: execute`; existing Vitest state contracts stay unchanged and focused Playwright proves the new observable UI.
- The incomplete API-SURFACE artifact is not used to infer absence. Direct component/model/contract inspection establishes the API, persistence, and publication boundaries are sufficient and unchanged.
- Existing exact export/recovery/gitignore copy is contractual. Icons, wrappers, and CSS may reinforce it but may not rewrite or shorten it.
- No package legitimacy checkpoint is needed because there is no install; all icons/assets are local and existing dependencies remain pinned.
</contribution_decisions>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Required outcome | Plan/task | Status |
|--------|----|------------------|-----------|--------|
| GOAL | — | Every existing review surface is operable through a close GitHub dark diff adaptation with clear hierarchy and complete interaction-state feedback | 07-01 through 07-06 | COVERED |
| ROADMAP | SC-1 | Compact current-file/Base/Head header retains every control | 07-01/T1-T2 | COVERED |
| ROADMAP | SC-2 | Rest/hover/pressed/selected/focused/disabled/destructive/busy controls distinguishable | 07-01/T1-T2; 07-03/T2; 07-04/T2; 07-05/T1; 07-06/T1-T2 | COVERED |
| ROADMAP | SC-3 | Inline composer/comment anchors/headings/validation/lifecycle are clear | 07-02/T1-T2 | COVERED |
| ROADMAP | SC-4 | Review rail heading/count/group/card/form/selection hierarchy is clear | 07-03/T1-T2 | COVERED |
| ROADMAP | SC-5 | Error/warning/info/success/pending/disabled/open/resolved have non-color meaning | 07-02 through 07-06 | COVERED |
| REQ | VIS-04 | Active file and Base/Head context in compact complete header | 07-01 | COVERED |
| REQ | REVW-01 | Complete existing-control state matrix | 07-01 | COVERED |
| REQ | REVW-02 | Operable dark inline comment cards with anchors/validation/lifecycle | 07-02 | COVERED |
| REQ | REVW-03 | Scannable review rail hierarchy and selection | 07-03 | COVERED |
| REQ | REVW-04 | Shared non-color status language across all states | 07-04 through 07-06 | COVERED |
| CONTEXT | D-01 | One bordered two-row active-file/control header | 07-01/T2 | COVERED |
| CONTEXT | D-02 | Compact safe Base/Head label and short OID blocks aligned to panes | 07-01/T2 | COVERED |
| CONTEXT | D-03 | Muted directories, strong filenames, complete rename relationship | 07-01/T1-T2; 07-03/T2 | COVERED |
| CONTEXT | D-04 | Local nav icons; visible Review/help; preserved names/tooltips/commands/disabled | 07-01/T1-T2 | COVERED |
| CONTEXT | D-05 | Composer and accepted comment conversation cards retain source-line cue | 07-02/T1-T2 | COVERED |
| CONTEXT | D-06 | Filename and Base/Head line lead; directory/fixed explanation remains | 07-02/T1 | COVERED |
| CONTEXT | D-07 | Field-adjacent guidance/validation/failure and explicit pending label | 07-02/T1-T2 | COVERED |
| CONTEXT | D-08 | Separate lifecycle/anchor badges; anchor cue remains separate | 07-02/T1-T2; 07-03/T2 | COVERED |
| CONTEXT | D-09 | Fixed rail order and flat framed major sections | 07-03/T2; 07-04/T2; 07-06/T1 | COVERED |
| CONTEXT | D-10 | Rail-level labeled Open/Resolved counts and disclosure counts | 07-03/T2 | COVERED |
| CONTEXT | D-11 | One file-group frame with divider rows and path hierarchy | 07-03/T2 | COVERED |
| CONTEXT | D-12 | Persistent selection rail/surface/heading distinct from focus/status/destructive | 07-03/T1-T2 | COVERED |
| CONTEXT | D-13 | Neutral/primary/destructive action tiers | 07-01/T1; 07-03/T2; 07-04/T2; 07-05/T1; 07-06/T1-T2 | COVERED |
| CONTEXT | D-14 | Separate hover/pressed/selected/focus channels | 07-01/T1-T2; 07-03/T1-T2; 07-04/T2; 07-05/T1; 07-06/T1-T2 | COVERED |
| CONTEXT | D-15 | Local spinner + exact progressive verb + existing disable rules | 07-01/T1; 07-02/T1; 07-03/T2; 07-04/T2; 07-05/T1; 07-06/T1-T2 | COVERED |
| CONTEXT | D-16 | One icon/label/structure status language at notice and badge scales | 07-02 through 07-06 | COVERED |
| RESEARCH | R-1 | Reuse App/ReviewToolbar/PathDisplay and existing session data; no backend fetch | 07-01 | COVERED |
| RESEARCH | R-2 | Preserve one measured paired view zone and Monaco authority | 07-02 | COVERED |
| RESEARCH | R-3 | Mirror focus-comment transiently; no persistence state | 07-03 | COVERED |
| RESEARCH | R-4 | Container frames plus divider rows; no nested cards | 07-03 | COVERED |
| RESEARCH | R-5 | Shared local icon/badge/notice/spinner primitives over existing state | 07-01, 07-02, 07-04, 07-05, 07-06 | COVERED |
| RESEARCH | R-6 | Targeted real-browser control/comment/rail/export/geometry proof | Every plan focused verification | COVERED |
| RESEARCH | R-7 | No dependency, API, schema, persistence, Git, or publication contribution | Every plan artifact/contribution ledger | COVERED |
| UI-SPEC | UI-1 | Only inherited semantic root, four-point scale, four sizes/two weights, local SVG, no shadows/static elevation | 07-01 through 07-06 | COVERED |
| UI-SPEC | UI-2 | Exact grouped header/path/toolbar anatomy and all controls | 07-01 | COVERED |
| UI-SPEC | UI-3 | Exact interaction tiers/matrix, independent focus/selected, reduced-motion busy | 07-01 through 07-06 | COVERED |
| UI-SPEC | UI-4 | Conversation-card anatomy, field linkage, lifecycle/anchor independence, paired geometry | 07-02 | COVERED |
| UI-SPEC | UI-5 | Exact rail order/count/group/divider/selected/empty-state hierarchy | 07-03 | COVERED |
| UI-SPEC | UI-6 | Full notice/compact badge semantics and Summary behavior | 07-04 | COVERED |
| UI-SPEC | UI-7 | Explicit export, drift, conflict, readiness, progress, receipt, gitignore, recovery safety | 07-05/T1; 07-06/T1-T3 | COVERED |
| UI-SPEC | UI-8 | Existing breakpoints, wrap/min-width, no page overflow, keyboard/ARIA/focus preserved; Phase 08 proof deferred | Every plan browser boundary | COVERED |
| UI-SPEC | NG-1–9 | No new mechanics/IA/API/schema/Monaco/theme/asset/breakpoint/click target/automatic unsafe action | Frontmatter prohibitions and all tasks | COVERED |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing export state/result → visible status | UI must not synthesize success, partial publication, current consent, or actionable classification. |
| Validated receipt data → browser DOM/clipboard/reveal | Repository-relative paths/hashes may be displayed/copied; browser must not gain arbitrary filesystem authority or absolute paths. |
| Drift observation/consent → export action | Checkbox acknowledgement gates the pinned export but must not itself refresh, export, or persist stale consent. |
| Gitignore status/confirmation → fixed append capability | Optional consent may call only the existing fixed append closure after the second explicit action. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07-21 | Spoofing | high | export progress/failure/receipt | mitigate | Bind to existing phase/result, require exact words/icons/edges, use only the shared `.ui-spinner` with the legacy selector/keyframes removed, show no receipt before confirmed pair, subordinate prior receipt on failure, and exercise all branches in browser. |
| T-07-22 | Repudiation | high | explicit export/drift consent | mitigate | Count requests, require explicit CTA, unchecked current consent, second-drift invalidation, exact pinned/current identities, and prove reload/acknowledgement/other actions do not export. |
| T-07-23 | Elevation of Privilege | high | reveal/copy/ignore actions | mitigate | Preserve fixed closures and no browser-supplied path/ref/body authority; retain two-step append, fixed `/.diff-review/`, and request-shape/count assertions. |
| T-07-24 | Information Disclosure | high | receipt/path rendering | mitigate | Render/copy validated repository-relative paths, hashes, counts, and safe identities only; forbid absolute paths, stderr, remote assets, and new API/query fields. |
| T-07-25 | Tampering | high | readiness/actionable classification | mitigate | Keep existing computed rules: only open+verified actionable, stale/unavailable non-actionable, resolved historical; preserve exact applying-agent warning and prohibit guessed relocation. |
| T-07-26 | Denial of Service | medium | long receipt/state content | mitigate | Use min-width zero, anywhere wrapping, bounded existing lists, stable buttons, and existing scroll owners; no new polling/listeners/animation beyond stopped reduced-motion spinner. |
| T-07-SC | Tampering | low | npm supply chain | accept | No install, upgrade, manifest, lockfile, registry, or remote asset change occurs. |

High-severity gate: T-07-21 through T-07-25 require the named branch binding, explicit-action, fixed-authority, bounded-data, classification, and real-browser mitigations. None may be accepted or transferred.
</threat_model>

<verification>
1. Run only `npm run test:browser -- tests/integration/export-receipt-ui.spec.ts`; the focused progress assertions require `.ui-spinner` and reject the removed legacy class while exercising real browser behavior.
2. Repeat the focused file with `--headed`, reduced motion, and representative 1280/768/640 widths; inspect all export, drift, progress, failure, receipt, readiness, copy/reveal, and gitignore states.
3. Verify request evidence demonstrates explicit export and two-step ignore consent; no static source-text assertion substitutes for observable behavior.
4. Confirm no API, schema, persistence, publication, Git, filesystem service, package, lockfile, or Phase 08 file changed.
</verification>

<success_criteria>
- REVW-04 and roadmap criterion 5 are complete across Export/readiness/drift/conflict/progress/failure/receipt/gitignore states with text/icon/structure beyond color.
- The phase-wide source audit covers the goal, all 5/5 requirements, D-01 through D-16, research constraints, and the approved UI-SPEC across six sequential plans without a missing item.
- Existing explicit export, revision, drift, publication integrity, receipt, copy/reveal, and optional append-only ignore behavior remains byte/transition compatible.
- Every high-severity ASVS L1 threat is mitigated and no backend/API/schema/persistence/package/remote asset or live-schema operation is introduced.
</success_criteria>

<output>
Create `.planning/phases/07-github-familiar-review-surfaces/07-06-SUMMARY.md` when done.
</output>
