---
phase: 07-github-familiar-review-surfaces
plan: 04
type: execute
wave: 4
depends_on:
  - 07-03
files_modified:
  - src/web/components/InlineNotice.vue
  - src/web/components/FileMetadataPane.vue
  - src/web/components/ReviewPanel.vue
  - src/web/components/SelectorDriftNotice.vue
  - src/web/components/SummarySection.vue
  - src/web/styles.css
  - tests/e2e/review-panel-resolved.spec.ts
  - tests/e2e/pinned-session.spec.ts
autonomous: true
requirements:
  - REVW-04
must_haves:
  truths:
    - "Users distinguish informational, warning, error, success, disabled, open, and resolved meanings through shared icon + visible heading/label + structural edge language rather than hue alone; pending remains `.ui-spinner` + action-specific progressive text + existing status semantics, never an InlineNotice tone or UiIcon (D-16)."
    - "Full notices preserve existing alert/status/note semantics, exact focus destinations, body actions, and announcements while gaining one 16px tone icon and 3px leading edge."
    - "Summary remains the first framed rail section; Edit/Preview selection stays separate from focus, validation/failure stays field-adjacent, and only Save summary shows localized spinner + `Saving summary…` while pending."
    - "Selector drift remains an informational/warning presentation over the original pinned comparison and does not refresh, acknowledge, or export automatically."
  artifacts:
    - path: "src/web/components/InlineNotice.vue"
      provides: "Shared fixed tone-icon/content anatomy over existing notice roles"
    - path: "src/web/components/FileMetadataPane.vue"
      provides: "State-derived retry-error and availability notice headings with preserved role, retry, loading, disabled, safe-text, and focus behavior"
    - path: "src/web/components/ReviewPanel.vue"
      provides: "Icon/heading/edge conflict and operation-failure notices without focus or mutation changes"
    - path: "src/web/components/SelectorDriftNotice.vue"
      provides: "Warning structure for pinned-selector drift without behavior changes"
    - path: "src/web/components/SummarySection.vue"
      provides: "Framed Summary hierarchy, selected tabs, field failure, and localized save busy presentation"
    - path: "src/web/styles.css"
      provides: "Shared full-notice grid, tones, Summary, badge, busy, wrapping, and focus-overlap styling"
    - path: "tests/e2e/review-panel-resolved.spec.ts"
      provides: "Mounted Summary/notice/control-state browser proof"
    - path: "tests/e2e/pinned-session.spec.ts"
      provides: "Production browser proof for text, unsupported, unavailable, and retry-error metadata notice anatomy"
  key_links:
    - from: "src/web/components/InlineNotice.vue"
      to: "src/web/components/ui/UiIcon.vue"
      via: "closed tone-to-fixed-glyph mapping with decorative SVG output"
      pattern: "UiIcon|inline-notice__icon"
    - from: "src/web/components/FileMetadataPane.vue"
      to: "src/web/components/InlineNotice.vue"
      via: "authoritative error and availability branches supply explicit state-derived headings while retaining existing roles and actions"
      pattern: "availabilityNoticeHeading|metadataErrorHeading|InlineNotice"
    - from: "src/web/components/SummarySection.vue"
      to: "src/web/styles.css"
      via: "aria-selected tab, localized aria-busy control, and framed summary classes"
      pattern: "review-summary|ui-button--selected|ui-spinner"
    - from: "src/web/components/ReviewPanel.vue"
      to: "src/web/styles.css"
      via: "existing role/ref/focusable notice roots receive icon/content hooks without replacement"
      pattern: "review-panel__conflict|review-panel__failure|inline-notice"
  prohibitions:
    - "No second live region, changed role, changed focus move, hidden semantic heading, icon-only status, color-only status, spinner-only pending state, pending InlineNotice tone, or invented pending UiIcon."
    - "No automatic source refresh, drift acceptance, export, or metadata retry."
    - "No new responsive breakpoint, forced-color suppression, shadowed static card, smooth motion, or final Phase 08 acceptance claim."
---

<objective>
Apply one non-color status language to shared notices, Summary, and selector drift while preserving every shipped semantic role, action, and safety transition.

Purpose: Deliver the notice and Summary portion of REVW-04 before the dedicated recovery and export plans.
Output: Tone-icon notice anatomy, Summary state styling, and focused mounted/real-browser evidence.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md
@.planning/phases/07-github-familiar-review-surfaces/07-RESEARCH.md
@.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md
@.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md
@.planning/phases/07-github-familiar-review-surfaces/07-03-SUMMARY.md

<interfaces>
Plan 01 provides fixed decorative `UiIcon` and shared `.ui-spinner`; Plan 02 provides presentation-only `ReviewStateBadge`; Plan 03 frames Summary/Open/Resolved/Export and retains ReviewPanel notice roots, refs, and focus behavior.

`InlineNotice.vue` currently accepts `tone?: 'neutral' | 'warning' | 'error'` and `role?: 'alert' | 'note' | 'status'`. Its role and slot content remain authoritative; its closed notice ownership expands only to `neutral | information | success | warning | error`. Pending is not a notice tone: it remains the shared `.ui-spinner` plus action-specific progressive text and existing status semantics, with no pending `UiIcon`.

`SummarySection.vue` owns native disclosure, Edit/Preview tablist/tabpanel behavior, `status`, `unsaved`, save/cancel/confirmation events, exact focus restoration, and failure alert focus. Presentation may consume those states but must not add a reducer.

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Normalize full notices and FileMetadataPane states without changing roles, refs, or retry actions</name>
  <read_first>
    - `src/web/components/InlineNotice.vue` — modified shared notice primitive.
    - `src/web/components/ReviewPanel.vue` — modified raw conflict/failure notice markup with existing refs/focus roles.
    - `src/web/components/SelectorDriftNotice.vue` — modified raw selector-drift notice with existing role/copy.
    - `src/web/styles.css` — modified `.inline-notice` and tone modifiers; sole semantic token authority.
    - `src/web/components/ui/UiIcon.vue` — Plan 01 closed fixed glyph source.
    - `src/web/components/FileMetadataPane.vue` — modified existing InlineNotice consumer and authority for retry-error plus text/unsupported/unavailable availability branches.
    - `tests/e2e/review-panel-resolved.spec.ts` — modified mounted ReviewPanel notice fixture harness; Summary-specific assertions remain in Task 2.
    - `tests/e2e/pinned-session.spec.ts` — modified production browser `metadata and availability states` scenario.
    - `src/web/components/CommentComposer.vue` — no-change Plan 02 field-alert anatomy; do not convert compact field errors into oversized full notices.
    - `.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md` — D-16 and preservation boundary.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — Notice and Badge, accessibility roles, forced-colors compatibility, and motion contracts.
    - `.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md` — InlineNotice/raw-notice analog and no-second-live-region rule.
  </read_first>
  <files>src/web/components/InlineNotice.vue, src/web/components/FileMetadataPane.vue, src/web/components/ReviewPanel.vue, src/web/components/SelectorDriftNotice.vue, src/web/styles.css, tests/e2e/review-panel-resolved.spec.ts, tests/e2e/pinned-session.spec.ts</files>
  <action>Expand `InlineNotice` tone to the closed union `neutral | information | success | warning | error`, with neutral and information sharing the informational icon/edge unless the caller supplies a more specific existing state. Add a computed fixed tone-to-`UiIcon` mapping and render `.inline-notice__icon` plus `.inline-notice__content` around the existing slot. InlineNotice owns only neutral/information/success/warning/error. Pending remains the shared `.ui-spinner` plus action-specific progressive text and the existing status/live semantics; do not add a pending tone, `.inline-notice--pending`, or pending `UiIcon`. Keep the caller-supplied `role` exactly `alert | note | status`, preserve fallthrough ARIA attributes, and emit no new live region, focus behavior, action, or state. Decorative icons remain hidden from the accessibility tree; headings/body remain the semantic meaning.

For ReviewPanel conflict/failure and SelectorDriftNotice, retain native section roots, current `role`, `tabindex`, `aria-labelledby`, refs, exact heading/body/action copy, and focus movement. Add the same fixed `UiIcon` plus `.inline-notice__content` anatomy directly where using the component would change the existing HTMLElement ref surface. Conflict and selector drift use warning; operation failure uses error. Do not duplicate a live announcement already provided by App.

In `FileMetadataPane.vue`, add explicit state-derived heading computations for both notice families: a retry-error heading for `errorMessage`, plus availability headings keyed by `text`, `unsupported`, and `unavailable` rather than leaving the normal text branch or retry error as body-only copy. Render those headings inside the existing InlineNotice branches and retain the current alert/status/note role placement, retry action and exact loading/disabled rules, safe Vue text rendering, error text, machine reason, availability explanation, pane heading ref, and focus behavior. Do not turn a heading or icon into a live region or action.

Extend `review-panel-resolved.spec.ts` by importing and mounting the relevant notice fixtures, including `SelectorDriftNotice`, and add an exact browser case named `Phase 07 notice status language`. Cover warning, error, information, and success notices with their existing alert/status/note roles, focus destinations, fixed decorative icon, explicit heading, and computed 3px inline-start edge. Keep every Summary-specific assertion in Task 2. Extend the existing `metadata and availability states` scenario in `pinned-session.spec.ts` to exercise FileMetadataPane text, unsupported, unavailable, and retry-error branches and prove each has its state-derived heading, fixed icon, and 3px edge while retry focus, loading, disabling, safe text, and request authority remain unchanged.

In `styles.css`, make every full `.inline-notice` a four-part-compatible grid with 16px icon column, min-width-zero content, 16px padding, 1px border, 6px radius, subtle inherited tone background, and 3px inline-start structural edge. Add explicit information and success tone modifiers from the existing root tokens; neutral/information/success/warning/error are the complete InlineNotice tone set. Do not add `.inline-notice--pending`: pending actions consume `.ui-spinner`, action-specific progressive text, and existing status semantics. Keep headings/body/actions wrapping, align actions beneath headings, and ensure icon/edge/text remain when tint is absent. Do not apply the full-notice grid to compact inline field messages unless they have the new child anatomy.
  <acceptance_criteria>
    - InlineNotice supports only `neutral | information | success | warning | error` and always renders a fixed decorative 16px icon plus content/edge; no repository string selects an icon/class, and pending has no InlineNotice modifier or UiIcon.
    - Alert/status/note roles, aria-labelledby relationships, focusable headings/refs, exact copy, actions, and App live announcements remain unchanged.
    - Review conflict, operation failure, and selector drift expose warning/error through icon, explicit heading/body, and 3px edge in addition to color.
    - FileMetadataPane retry-error and text/unsupported/unavailable availability branches each expose an explicit state-derived heading, fixed icon, and 3px edge while preserving roles, retry/loading/disabled behavior, safe text, and focus.
    - The exact `Phase 07 notice status language` browser case mounts relevant fixtures including SelectorDriftNotice and proves warning/error/information/success roles, focus, icon, heading, and edge; Summary assertions remain isolated to Task 2.
    - Notice content wraps without document overflow and remains structurally meaningful if background tint is removed or forced colors overrides it.
    - Compact field validation retains field adjacency and does not become a second live region or oversized full notice.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/e2e/review-panel-resolved.spec.ts --grep "Phase 07 notice status language" &amp;&amp; npm run test:browser -- tests/e2e/pinned-session.spec.ts --grep "metadata and availability states"</automated>
    <browser-check>Run both focused cases with `--headed`. In `Phase 07 notice status language`, inspect warning, error, information, and success fixtures including SelectorDriftNotice for unchanged roles/focus plus fixed icon, explicit heading, and 3px edge; do not add Summary assertions. In `metadata and availability states`, inspect text, unsupported, unavailable, and retry-error FileMetadataPane notices for state-derived heading/icon/edge while retry focus, loading/disabled rules, safe text, and request authority stay unchanged.</browser-check>
  </verify>
  <done>D-16 full-notice anatomy is shared without changing semantics, focus, or state ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Complete Summary selected, field, pending, and failure states</name>
  <read_first>
    - `src/web/components/SummarySection.vue` — modified native disclosure/tab/form/save/failure/confirmation source of truth.
    - `src/web/styles.css` — modified framed Summary, selected tab, field support/failure, busy, and confirmation styles.
    - `tests/e2e/review-panel-resolved.spec.ts` — modified existing Summary disclosure/tab/keyboard/discard/focus browser analog.
    - `src/web/components/ui/UiIcon.vue` — shared icon source.
    - `src/web/components/ui/ReviewStateBadge.vue` — shared Saved/Unsaved/Conflict/Retained status presentation primitive where appropriate.
    - `.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md` — D-09, D-13 through D-16.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — Summary, action tiers, copy, keyboard/focus, notice, and verification item 13.
    - `.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md` — existing SummarySection exact tab/focus/busy analog.
  </read_first>
  <files>src/web/components/SummarySection.vue, src/web/styles.css, tests/e2e/review-panel-resolved.spec.ts</files>
  <action>Keep Summary first and preserve its disclosure, Edit/Preview tablist/tabpanel, ArrowLeft/ArrowRight, Escape, focus restoration, Markdown preview, buffers, save/cancel emits, retained text, and exact discard confirmation copy/actions. Present the existing computed `status` as an icon-plus-label badge without changing its values. Bind `.ui-button--selected` to the active tab in parallel with `aria-selected`, leaving the global focus-visible ring independent. Keep `Review summary (Markdown)` visible and connect guidance plus present failure/retained status adjacent to the textarea through stable existing or new `summarySupportId`/`summaryFeedbackId` IDs.

On the Save summary button only when `saving`, add the shared decorative spinner, exact `Saving summary…`, and `aria-busy="true"`; reserve geometry, retain the existing native disable expression, and do not make Cancel or the whole section look busy. Keep failure as an error icon/heading/body/edge with `role="alert"` and current focus destination; retained-after-reload remains non-blocking status with exact text. Style the section as the already planned flat framed major section with 16px padding/gaps, 96px textarea minimum, wrapped actions, and no shadow.

Extend the Summary portion of `review-panel-resolved.spec.ts` to assert section order/frame, disclosure semantics, selected/focus independence on both tabs, field-described guidance/failure, retained text, Save summary busy spinner/progressive verb/stable bounds, sibling disabled-not-busy behavior, exact discard confirmation/Escape/focus recovery, and no automatic save or lost buffer.</action>
  <acceptance_criteria>
    - Summary remains first, framed once, and keeps exact disclosure/tab roles, controls, keyboard paths, focus targets, preview, buffers, and confirmation copy.
    - Edit/Preview selected boundary and focus ring remain independent; no tab state depends on color alone.
    - Visible field guidance and present failure/retained feedback are adjacent and programmatically associated with the textarea.
    - Only Save summary shows spinner + `Saving summary…` + aria-busy while saving; bounds stay stable and sibling controls do not falsely show progress.
    - Failure/retained/saved/unsaved/conflict meaning uses visible text/icon/structure and the existing alert/status semantics.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/e2e/review-panel-resolved.spec.ts</automated>
    <browser-check>Run the mounted rail spec with `--headed`; keyboard through Summary Edit/Preview, create unsaved text, trigger saving/failure/retained and discard confirmation, and verify selected/focus/busy/error channels without lost text or changed focus restoration.</browser-check>
  </verify>
  <done>Summary consumes the shared status/control language while all shipped editing and keyboard mechanics remain exact.</done>
</task>


</tasks>

## Artifacts this phase produces

- **Expanded InlineNotice tone type:** `neutral | information | success | warning | error`; no new role values, pending tone, pending modifier, or pending UiIcon.
- **New InlineNotice-local symbol:** closed neutral/information/success/warning/error tone-to-UiIcon presentation mapping.
- **New Summary-local field-wiring symbols:** `summarySupportId` and `summaryFeedbackId` if existing stable IDs are insufficient; no data state.
- **New FileMetadataPane-local presentation symbols:** state-derived `metadataErrorHeading` and `availabilityNoticeHeading` (or equivalently named computed values) for retry-error plus text/unsupported/unavailable notice branches; no new data or behavior state.
- **Expanded focused browser artifacts:** exact `Phase 07 notice status language` in `tests/e2e/review-panel-resolved.spec.ts` and extended `metadata and availability states` in `tests/e2e/pinned-session.spec.ts`.
- **New fixed CSS hooks:** `.inline-notice__icon`, `.inline-notice__content`, `.inline-notice--information`, `.inline-notice--success`, `.review-summary__badges`, `.review-summary__feedback`, and `.review-summary__save--busy`; `.inline-notice--pending` is intentionally absent.
- **New visible presentation:** fixed tone icons and Summary status badges; all headings, roles, actions, and state values already exist.
- **New API endpoint/client method, persistence/Zod/schema/export field, reducer state, feature flag, package, dependency, registry block, remote asset, environment variable, or human setup:** none.
- **Schema-push assessment:** not applicable; no ORM/schema/migration file is modified and no live-schema operation is planned.

<contribution_decisions>
- TDD mode is active, but this plan styles existing Vue state branches and exercises them in Playwright; it introduces no defined-I/O business logic or API/data transformation and remains `type: execute`.
- Existing roles and focusable native roots take precedence over component consolidation. Raw notices with HTMLElement refs receive shared icon/content classes directly rather than being wrapped in a way that changes ref/focus behavior.
- Icons are redundant visual reinforcement; exact headings, body copy, roles, badges, and progressive verbs remain the semantic source.
</contribution_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing state branch → status presentation | Visual tone must accurately represent the authoritative existing branch and never synthesize completion. |
| Live/focus semantics → notices | Added icons/structure must not duplicate announcements, change roles, or redirect focus. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07-16 | Spoofing | high | notice/status meaning | mitigate | Bind the closed neutral/information/success/warning/error notice tones to existing branches, require icon+heading/label+edge, keep pending on `.ui-spinner` + progressive text + existing status semantics, and exercise information/warning/error/success notices plus pending/disabled states in browser. |
| T-07-20 | Denial of Service | medium | long details/notices | mitigate | Use min-width zero and anywhere wrapping within existing scroll owners; retain bounded validation detail contracts and avoid animation except reduced-motion-aware spinner. |
| T-07-SC | Tampering | low | npm supply chain | accept | No install, upgrade, manifest, lockfile, or remote asset change occurs. |

High-severity gate: T-07-16 requires the named state-binding, role/focus, and real-browser mitigations; it may not be accepted or transferred.
</threat_model>

<verification>
1. Run `npm run test:browser -- tests/e2e/review-panel-resolved.spec.ts --grep "Phase 07 notice status language"` for warning/error/information/success notice roles, focus, icon, heading, and 3px edge.
2. Run `npm run test:browser -- tests/e2e/pinned-session.spec.ts --grep "metadata and availability states"` for FileMetadataPane text/unsupported/unavailable/retry-error headings, icons, edges, retry, loading, disabling, safe text, and focus.
3. Run `npm run test:browser -- tests/e2e/review-panel-resolved.spec.ts` for Summary state, keyboard, and focus behavior.
4. Repeat the focused files with `--headed`; inspect notice icon+heading+edge/status, reduced-motion pending, Summary selected/focus, and metadata availability/retry boundaries.
5. Confirm no API/schema/persistence/package/lockfile file changed.
</verification>

<success_criteria>
- REVW-04 is materially satisfied for neutral/information/success/warning/error notices, FileMetadataPane availability/retry states, Summary, selector drift, spinner-plus-progressive-text pending, and disabled states.
- D-09 and D-13 through D-16 plus all relevant UI-SPEC Summary/notice copy, accessibility, keyboard, responsive, and motion contracts remain visible and executable.
- Status styling never changes live roles, focus, mutation, drift, retry, or persistence behavior.
- The high-severity ASVS L1 notice-spoofing threat is mitigated without adding backend authority or a completion claim.
</success_criteria>

<output>
Create `.planning/phases/07-github-familiar-review-surfaces/07-04-SUMMARY.md` when done.
</output>
