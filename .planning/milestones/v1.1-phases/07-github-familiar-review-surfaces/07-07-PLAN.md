---
phase: 07-github-familiar-review-surfaces
plan: 07
type: execute
wave: 7
depends_on:
  - 07-06
files_modified:
  - src/web/components/DiffWorkspace.vue
  - tests/integration/anchored-workspace.spec.ts
autonomous: true
gap_closure: true
requirements:
  - VIS-04
  - REVW-01
  - REVW-02
  - REVW-03
must_haves:
  truths:
    - "A long accepted inline comment grows the existing paired Monaco zone before it can overlap or hide code, while Base and Head zones remain equal in height (D-05, D-08)."
    - "The accepted-card and composer branches use the same measured-height formula, `Math.max(280, contentHeight + 16)`, without creating a second zone or changing adapter, model, line-map, focus, or source-line-rail authority."
    - "The production browser scenario proves the accepted card is contained by its zone and does not overlap the next rendered code region, not merely that short cards render."
    - "The already verified grouped header/control and review-rail selection contracts remain operable after the inline fix (D-01 through D-04, D-09 through D-15)."
  artifacts:
    - path: "src/web/components/DiffWorkspace.vue"
      provides: "Shared content-height application for composer and accepted-card annotation branches"
    - path: "tests/integration/anchored-workspace.spec.ts"
      provides: "Real-Monaco long accepted-card containment, equal paired-zone height, and surrounding Phase 07 regression proof"
  key_links:
    - from: "src/web/components/DiffWorkspace.vue:renderAnnotation"
      to: "src/web/monaco/diff-adapter.ts:setAnchorZoneHeight"
      via: "post-render scrollHeight measurement through the existing adapter instance"
      pattern: "setAnchorZoneHeight\\(Math\\.max\\(280, contentHeight \\+ 16\\)\\)"
    - from: "tests/integration/anchored-workspace.spec.ts"
      to: "src/web/components/DiffWorkspace.vue:renderAnnotation"
      via: "persisted long comment rendered by the production Vue/Vite/Monaco workspace"
      pattern: "Phase 07 inline conversation states"
---

<objective>
Close G-01 / WR-01 by measuring accepted inline comment cards after render and proving long accepted content remains contained inside equal paired Monaco zones, per D-05 and D-08.

Purpose: Restore the failed REVW-02 must-have without weakening the verified VIS-04, REVW-01, or REVW-03 surfaces or changing review persistence and Monaco authority.
Output: One localized renderer correction and one focused production-browser regression extension.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md
@.planning/phases/07-github-familiar-review-surfaces/07-RESEARCH.md
@.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md
@.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md
@.planning/phases/07-github-familiar-review-surfaces/07-VERIFICATION.md
@.planning/phases/07-github-familiar-review-surfaces/07-REVIEW.md
@.planning/phases/07-github-familiar-review-surfaces/07-02-SUMMARY.md
@.planning/phases/07-github-familiar-review-surfaces/07-03-SUMMARY.md

<interfaces>
`DiffWorkspace.vue:renderAnnotation()` currently renders an accepted `.inline-accepted-comment` at lines 90–104 and returns at line 105. The composer branch alone measures `zone.firstElementChild?.scrollHeight ?? zone.scrollHeight` inside `nextTick()` and calls `adapter?.setAnchorZoneHeight(Math.max(280, contentHeight + 16))`.

`tests/integration/anchored-workspace.spec.ts` already owns `Phase 07 inline conversation states`, canonical server-backed accepted comments, `readMonacoGeometry()`, and the production Monaco zone locators. Extend that case rather than creating a mock adapter or another harness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Resize accepted cards through the existing paired-zone contract</name>
  <read_first>
    - `src/web/components/DiffWorkspace.vue:71-135` — current unmount and `renderAnnotation()` branches; accepted rendering returns before the composer-only measurement.
    - `tests/integration/anchored-workspace.spec.ts:555-668` — current Phase 07 inline scenario, canonical accepted mutation, short-card checks, and paired-zone geometry assertions.
    - `.planning/phases/07-github-familiar-review-surfaces/07-VERIFICATION.md` — G-01 failed truth and exact missing evidence.
    - `.planning/phases/07-github-familiar-review-surfaces/07-REVIEW.md` — WR-01 source diagnosis and required long-content browser proof.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — D-05/D-08-compatible conversation-card containment and the immutable `Math.max(280, contentHeight + 16)` contract.
    - `.planning/phases/06-monaco-diff-semantics/06-UI-SPEC.md` — paired-zone, counterpart spacer, source-line rail, and no-reflow invariants.
  </read_first>
  <files>src/web/components/DiffWorkspace.vue, tests/integration/anchored-workspace.spec.ts</files>
  <action>Extract the existing synchronous height calculation and adapter call into one file-local `resizeAnchorZoneToContent(zone: HTMLElement): void` helper. The helper must read `zone.firstElementChild?.scrollHeight ?? zone.scrollHeight` and call the existing adapter with exactly `Math.max(280, contentHeight + 16)`; it must not create a zone, listener, observer, model, timer loop, or adapter API. In the accepted-comment branch, call that helper from `nextTick()` immediately after `render(...)` and before returning. In the composer branch, replace the duplicated calculation with the same helper inside the existing `nextTick()` while leaving the conditional `requestAnimationFrame` focus handoff in its current branch and order. Preserve accepted body rendering as Vue text, `data-comment-id`, heading focus, lifecycle/anchor badges, the one composer zone, one counterpart spacer, and the Phase 06 source-line rail per D-05 and D-08.

Extend the existing `Phase 07 inline conversation states` browser case with an accepted comment body long enough that its rendered card height exceeds the initial 280px zone. Use the existing canonical mutation response path; do not inject DOM or client-synthesize an accepted card. After acceptance settles, assert: the card's `scrollHeight` exceeds the initial zone minimum; both `.monaco-anchor-zone--composer` and `.monaco-anchor-zone--spacer` have equal measured heights; the accepted card's bottom is within the composer-zone bottom; the next visible code content begins at or below the zone/card boundary so no code and card rectangles overlap; and the existing code x-origin, gutter width, pane ratio, source-line rail, and action position remain unchanged except for equal vertical zone growth. Keep the short Open/Resolved assertions and A→B→A cleanup coverage intact. Also run only the existing Phase 07 header/control and rail-selection cases in this same spec as regression boundaries for VIS-04, REVW-01, and REVW-03; do not modify those verified contracts or their source files.</action>
  <acceptance_criteria>
    - Long accepted content triggers the same post-render height formula as composer content and no accepted branch returns before scheduling measurement.
    - Exactly one composer zone and one counterpart spacer remain; their tops stay aligned within 1px and their heights are equal after long accepted content renders.
    - The accepted card rectangle is fully contained by its zone and does not overlap the following visible code rectangle.
    - Code x-origin, gutter width, 50/50 panes, source-line rail, comment action position, focus handoff, models, listeners, persistence settlement, and cleanup remain unchanged.
    - The focused header/control and rail-selection browser cases still pass, preserving D-01 through D-04 and D-09 through D-15 while G-01 closes.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "Phase 07 header and control states|Phase 07 inline conversation states|Phase 07 rail selection follows focus-comment"</automated>
    <browser-check>Run only the `Phase 07 inline conversation states` case with `--headed` at 1280×760. Inspect the persisted long accepted card, equal Base/Head zone growth, following code position, independent source-line rail, and retained Open/Resolved badges.</browser-check>
  </verify>
  <done>G-01 / WR-01 is closed by observable real-Monaco containment evidence, and the three verified Phase 07 requirements sharing this production workspace remain intact.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New file-local production symbol:** `resizeAnchorZoneToContent(zone: HTMLElement): void` in `src/web/components/DiffWorkspace.vue`; it centralizes the already existing content-height formula and has no exported or behavioral authority.
- **Extended existing browser artifact:** `Phase 07 inline conversation states` in `tests/integration/anchored-workspace.spec.ts` gains canonical long accepted-comment containment and code-overlap assertions.
- **New source path, component, exported type, prop, event, API/client method, persistence/schema/export field, adapter method, listener, observer, package, dependency, remote asset, environment variable, or human setup:** none.
- **Schema-push assessment:** not applicable; only an existing Vue component and Playwright spec are modified, with no ORM/schema/migration path.

<contribution_decisions>
- TDD mode is active, but this is Vue/Monaco rendering glue and real-browser geometry, not defined-I/O business logic, an API contract, or a data transformation; the plan remains `type: execute`.
- The fix is at the rendering source: accepted content gets the same measured paired-zone update as composer content. No CSS clipping, fixed height, overflow suppression, or input special case may substitute for measurement.
- The post-07-06 verified tree is the baseline, so this independent gap plan joins closure wave 7 and depends on 07-06; its files do not overlap 07-08 or 07-09.
</contribution_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Draft comment text → accepted Vue card | Repository-local review text is untrusted and may be very long; it must remain inert text and may affect only measured presentation height. |
| Accepted card DOM → Monaco paired-zone geometry | Rendered height may resize the existing pair but must not gain model, line-map, listener, or editor-layout authority. |
| Browser geometry evidence → completion claim | A short fixture or equal-zone-only assertion can hide card/code overlap and must not be accepted as proof. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07-G01 | Denial of Service | high | `DiffWorkspace.renderAnnotation` | mitigate | Keep comment text as Vue text, measure after accepted render, retain the bounded minimum formula and one paired zone, and prove long content containment without new observers/listeners. |
| T-07-G02 | Tampering | high | Monaco zone/editor boundary | mitigate | Call only the existing `setAnchorZoneHeight`, preserve adapter/model/line-map/source-rail owners, and assert pane, gutter, code-origin, action, root, and spacer invariants. |
| T-07-G03 | Repudiation | high | browser verification | mitigate | Persist the long comment through the canonical mutation response and assert both rectangle containment and non-overlap in real Chromium; source-text or mock evidence is insufficient. |
| T-07-SC | Tampering | low | npm supply chain | accept | No install, upgrade, manifest, lockfile, package, registry, or remote asset change occurs. |

High-severity gate: T-07-G01 through T-07-G03 must be mitigated by the named implementation constraints and focused production-browser assertions; none may be accepted or transferred.
</threat_model>

<verification>
1. Run only the targeted Playwright command in Task 1; do not run formatters, linters, builds, unit suites, or project-wide suites.
2. Confirm long accepted content is server-accepted, exceeds the initial minimum, remains contained, grows both zones equally, and leaves the following code non-overlapping.
3. Confirm the two modified paths are exactly those declared and no Monaco adapter, API, persistence, schema, package, or Phase 08 file changes.
</verification>

<success_criteria>
- REVW-02 and roadmap criterion 3 are no longer blocked by accepted-card height drift.
- VIS-04, REVW-01, and REVW-03 remain verified through the existing focused production cases without source changes.
- D-01 through D-15 remain preserved, with D-05 and D-08 directly enforced at the accepted-card/paired-zone seam.
- Every high-severity threat is concretely mitigated and no backend, schema, dependency, or review-mechanic scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/07-github-familiar-review-surfaces/07-07-SUMMARY.md` when done.
</output>
