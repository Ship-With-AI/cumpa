---
phase: 07-github-familiar-review-surfaces
plan: 09
type: execute
wave: 7
depends_on:
  - 07-06
files_modified:
  - src/web/components/DraftRecovery.vue
  - src/web/components/SelectorDriftNotice.vue
  - tests/integration/draft-recovery-ui.spec.ts
  - tests/e2e/review-panel-resolved.spec.ts
autonomous: true
gap_closure: true
requirements:
  - REVW-04
must_haves:
  truths:
    - "The recovered-draft receipt has exactly one polite announcement owner: the existing success InlineNotice with `role=status`; its containing card is structural only (D-16)."
    - "Selector-drift copy success or failure has exactly one polite announcement owner: the existing parent notice with `role=status`; the mutable result paragraph is not a nested live region (D-16)."
    - "Removing duplicate live ownership does not remove visible status text, icons, structural edges, focus behavior, copy/reveal actions, recovery safety, or pinned-selector authority."
    - "Focused browser assertions count live-region owners in the actual recovered and copied states rather than relying on source inspection."
  artifacts:
    - path: "src/web/components/DraftRecovery.vue"
      provides: "Recovered receipt with one retained status owner"
    - path: "src/web/components/SelectorDriftNotice.vue"
      provides: "Selector-drift copy feedback with one retained parent status owner"
    - path: "tests/integration/draft-recovery-ui.spec.ts"
      provides: "Recovered receipt live-owner and recovery-regression browser evidence"
    - path: "tests/e2e/review-panel-resolved.spec.ts"
      provides: "Selector-drift copy-result live-owner browser evidence"
  key_links:
    - from: "src/web/components/DraftRecovery.vue"
      to: "src/web/components/InlineNotice.vue"
      via: "the recovered branch retains `role=status` on InlineNotice and removes outer live ownership"
      pattern: "tone=\"success\" role=\"status\""
    - from: "src/web/components/SelectorDriftNotice.vue:copyPinnedCommit"
      to: "src/web/components/SelectorDriftNotice.vue template"
      via: "the existing `copied` text updates inside the single parent `role=status` scope"
      pattern: "role=\"status\"|copied"
---

<objective>
Close G-03a / WR-04 and G-03b / WR-05 by giving recovered-draft and selector-drift feedback exactly one live-region owner each, per D-16 and the UI-SPEC no-second-live-region rule.

Purpose: Restore the failed REVW-04 announcement contract without changing visible presentation, recovery capability, copy behavior, focus, or pinned-comparison semantics.
Output: Two minimal semantic corrections and focused browser assertions over the exact dynamic states that previously announced twice.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md
@.planning/phases/07-github-familiar-review-surfaces/07-RESEARCH.md
@.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md
@.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md
@.planning/phases/07-github-familiar-review-surfaces/07-VERIFICATION.md
@.planning/phases/07-github-familiar-review-surfaces/07-REVIEW.md
@.planning/phases/07-github-familiar-review-surfaces/07-04-SUMMARY.md
@.planning/phases/07-github-familiar-review-surfaces/07-05-SUMMARY.md

<interfaces>
`DraftRecovery.vue` currently gives the recovered `.draft-recovery__card` `aria-live="polite"` and nests `InlineNotice tone="success" role="status"`. Retain the InlineNotice status owner because it already contains the success heading/body and shared non-color anatomy.

`SelectorDriftNotice.vue` currently makes the outer warning notice `role="status"` and nests `<p aria-live="polite">{{ copied }}</p>`. Retain the parent status owner because it announces the whole pinned-source notice and copy-result mutation; make the result paragraph plain visible text.

`tests/integration/draft-recovery-ui.spec.ts` already reaches verified recovery through the fingerprint-bound response. `tests/e2e/review-panel-resolved.spec.ts:Phase 07 notice status language` already mounts SelectorDriftNotice with a moved Base source. Extend these existing cases rather than creating another harness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Keep one recovered-receipt announcement owner</name>
  <read_first>
    - `src/web/components/DraftRecovery.vue:108-126` — recovered branch with outer `aria-live=polite` and nested success InlineNotice `role=status`.
    - `tests/integration/draft-recovery-ui.spec.ts:135-236` — canonical corrupt→failed→verified recovery browser path and recovered receipt assertions.
    - `src/web/components/InlineNotice.vue` — fallthrough role behavior and shared icon/content anatomy.
    - `.planning/phases/07-github-familiar-review-surfaces/07-VERIFICATION.md` — G-03a failed truth.
    - `.planning/phases/07-github-familiar-review-surfaces/07-REVIEW.md` — WR-04 diagnosis and allowed ownership choice.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — one status owner, recovery receipt, and focus/copy contracts.
  </read_first>
  <files>src/web/components/DraftRecovery.vue, tests/integration/draft-recovery-ui.spec.ts</files>
  <action>Remove `aria-live="polite"` from the recovered `.draft-recovery__card` only. Retain `InlineNotice tone="success" role="status"` as the one polite announcement owner containing `New draft started` and the preserved-before-created outcome. Keep the Recovered badge, verified backup path, Copy backup path, Open new draft, recovered-result gate, focus behavior, and all recovery request/byte-safety authority unchanged per D-16. Do not move the role to a new wrapper, add hidden text, change the success copy, or alter other action-message/failure live regions in mutually exclusive branches.

In the existing `corrupt drafts remain read only until the fingerprint-bound recovery response succeeds` browser case, after `New draft started` appears, scope to `.draft-recovery__card` and assert the union of `[role="status"]` and `[aria-live="polite"]` contains exactly one element, that this element is the success InlineNotice, and that it contains the exact heading and outcome text. Retain existing assertions for verified result timing, backup path, fixed request body, Read only failure, and Open new draft continuation.</action>
  <acceptance_criteria>
    - The recovered card has no `aria-live` attribute and contains exactly one status/live owner.
    - The retained owner is the existing success InlineNotice and includes visible recovery heading/body, icon, and structural status treatment.
    - No recovery success appears before the verified recovered result; request shape, backup path, copy, focus, and continuation behavior remain unchanged.
    - The browser assertion counts the rendered owner in the recovered state and does not use source-text inspection.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/integration/draft-recovery-ui.spec.ts --grep "corrupt drafts remain read only until the fingerprint-bound recovery response succeeds"</automated>
    <browser-check>Run only the same recovery case with `--headed`; reach verified recovery, inspect one announced success notice, and confirm the badge, backup path, copy action, and Open new draft remain visible and operable.</browser-check>
  </verify>
  <done>G-03a / WR-04 is closed with one recovered-receipt announcement owner and unchanged recovery safety.</done>
</task>

<task type="auto">
  <name>Task 2: Keep one selector-drift copy-feedback announcement owner</name>
  <read_first>
    - `src/web/components/SelectorDriftNotice.vue:33-77` — copy result mutation, parent `role=status`, and nested result `aria-live=polite`.
    - `tests/e2e/review-panel-resolved.spec.ts:109-150` — existing moved-Base SelectorDriftNotice fixture.
    - `tests/e2e/review-panel-resolved.spec.ts:306-346` — existing `Phase 07 notice status language` roles/icons/edge/focus assertions.
    - `.planning/phases/07-github-familiar-review-surfaces/07-VERIFICATION.md` — G-03b failed truth.
    - `.planning/phases/07-github-familiar-review-surfaces/07-REVIEW.md` — WR-05 diagnosis and allowed ownership choice.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — selector drift remains pinned, one status owner, exact button naming, and no automatic refresh/export behavior.
  </read_first>
  <files>src/web/components/SelectorDriftNotice.vue, tests/e2e/review-panel-resolved.spec.ts</files>
  <action>Remove `aria-live="polite"` from the paragraph that renders `copied`; keep the paragraph and exact success/failure strings visible. Retain the outer `.selector-drift-notice` `role="status"` and `aria-labelledby="selector-drift-heading"` as the sole announcement owner. Preserve the warning icon/edge, moved/unavailable source details, safe label/OID rendering, exact Copy pinned commit accessible name, clipboard operation, Launch new comparison disclosure, and pinned-review explanation. Do not change selector polling, refresh, export, drift acknowledgement, filesystem authority, or focus per D-16.

Extend `Phase 07 notice status language` in the existing mounted browser harness. Scope to `.selector-drift-notice`, assert it has exactly one element matching `[role="status"], [aria-live="polite"]`, click the Base `Copy pinned commit` button, and assert visible text becomes `Pinned Base commit copied.` while the owner count remains one. Also retain the existing role, heading, decorative icon, 3px edge, and focus assertions. If the existing harness clipboard requires a deterministic stub, install it through `page.evaluate` before the click and keep the component production path unchanged; do not replace the click with direct ref mutation.</action>
  <acceptance_criteria>
    - SelectorDriftNotice retains one parent `role=status`; its copy-result paragraph has no live-region attribute.
    - Copy success remains visible as `Pinned Base commit copied.` and is within the single owner; failure copy remains unchanged in production code.
    - Warning heading/icon/edge, pinned/current identities, copy button name, launch disclosure, and focus behavior remain intact.
    - No automatic refresh, export, drift acknowledgement, or authority change is introduced.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/e2e/review-panel-resolved.spec.ts --grep "Phase 07 notice status language"</automated>
    <browser-check>Run only the same notice case with `--headed`; activate Copy pinned commit and inspect one status owner, visible copy feedback, warning anatomy, and unchanged launch disclosure.</browser-check>
  </verify>
  <done>G-03b / WR-05 is closed with one selector-drift announcement owner and unchanged pinned-comparison behavior.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Modified existing semantic ownership:** recovered `.draft-recovery__card` becomes structural while its existing success InlineNotice remains the owner; selector-drift result text becomes a plain paragraph while the existing parent notice remains the owner.
- **Extended existing browser artifacts:** `corrupt drafts remain read only until the fingerprint-bound recovery response succeeds` and `Phase 07 notice status language` gain exact live-owner counts in the dynamic recovered/copied states.
- **New source path, production or test helper symbol, component, prop, event, API/client method, request/result field, persistence/schema/export field, feature flag, package, dependency, remote asset, environment variable, or human setup:** none.
- **Schema-push assessment:** not applicable; only existing Vue and Playwright files are modified, with no ORM/schema/migration path.

<contribution_decisions>
- TDD mode is active, but this is semantic Vue markup and browser accessibility behavior over existing state; it is not defined-I/O business logic, an API, or a data transformation and remains `type: execute`.
- Ownership choices are explicit: recovery retains the nested success InlineNotice; selector drift retains the outer notice. Both choices preserve the most complete existing semantic region and require only removal of the redundant owner.
- This plan shares no modified path with 07-07 or 07-08 and therefore executes beside them in closure wave 7 against the post-07-06 baseline.
</contribution_decisions>

<gap_traceability>
## Verification Gap Traceability

| Gap | Verification / review finding | Closing plan/task | Observable closure |
|-----|-------------------------------|-------------------|--------------------|
| G-01 / WR-01 | Accepted long card returns before paired-zone measurement | 07-07/T1 | Long canonical accepted card is contained, paired zones equal, code non-overlapping |
| G-02a / WR-02 | Conflict error badge falsely says Ready | 07-08/T1 | Conflict heading badge says Review changed and not Ready |
| G-02b / WR-03 | Null ignore check falsely says unavailable | 07-08/T1 | Held null state says Checking; concrete unavailable says unavailable |
| G-03a / WR-04 | Recovered receipt nests two polite owners | 07-09/T1 | Recovered card contains exactly one status/live owner |
| G-03b / WR-05 | Selector copy feedback nests two polite owners | 07-09/T2 | Copied state contains exactly one status/live owner |
</gap_traceability>

<source_coverage_audit>
## Gap-Closure Multi-Source Coverage Audit

| Source | ID | Required outcome | New plan | Status |
|--------|----|------------------|----------|--------|
| GOAL | — | Every existing review surface remains operable with clear hierarchy and complete truthful interaction feedback | 07-07 through 07-09 | COVERED |
| REQ | VIS-04 | Compact Base/file/Head header remains intact while inline geometry changes | 07-07 targeted header regression | COVERED |
| REQ | REVW-01 | Existing control states remain intact while inline geometry changes | 07-07 targeted control regression | COVERED |
| REQ | REVW-02 | Accepted inline comments remain contained in measured paired zones | 07-07/T1 | COVERED |
| REQ | REVW-03 | Review rail selection and hierarchy remain intact | 07-07 targeted rail-selection regression | COVERED |
| REQ | REVW-04 | Conflict, pending/unavailable, recovery success, and copy feedback are truthful and singly announced | 07-08/T1; 07-09/T1-T2 | COVERED |
| RESEARCH | R-2 | Preserve one measured paired view zone and Monaco authority | 07-07/T1 | COVERED |
| RESEARCH | R-5 | Reuse existing shared badge/notice/spinner primitives over authoritative state | 07-08/T1; 07-09/T1-T2 | COVERED |
| RESEARCH | R-6 | Use targeted real-browser geometry/status/accessibility evidence | Every new task | COVERED |
| RESEARCH | R-7 | Add no dependency, API, schema, persistence, Git, publication, or filesystem contribution | Every artifact/threat ledger | COVERED |
| CONTEXT | D-01–D-04 | Existing grouped header, endpoints, path identity, and toolbar contracts remain unchanged | 07-07 targeted header/control regression | COVERED |
| CONTEXT | D-05–D-08 | Conversation cards retain identity, field/lifecycle structure, source rail, and measured zone containment | 07-07/T1 | COVERED |
| CONTEXT | D-09–D-12 | Existing rail order, counts, groups, and selection remain unchanged | 07-07 targeted rail-selection regression | COVERED |
| CONTEXT | D-13–D-15 | Action tiers, focus/state channels, and pending semantics remain authoritative | 07-07 regression; 07-08/T1 | COVERED |
| CONTEXT | D-16 | Every corrected state uses truthful visible language and exactly one intended announcement owner | 07-08/T1; 07-09/T1-T2 | COVERED |

Deferred ideas: none in `07-CONTEXT.md`. Phase 08-only forced-colors, final 320px/400%/grayscale/composite-contrast/full-workflow proof remains excluded, not missing.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Recovery result → success announcement | Success must remain tied to the verified recovered result and be announced once, not optimistically or twice. |
| Clipboard outcome → selector-drift announcement | Copy result must remain visible and announced once without changing pinned-source authority. |
| Semantic DOM → assistive technology | Nested polite owners can duplicate or reorder feedback even when visual output is correct. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07-G08 | Spoofing | high | recovered success receipt | mitigate | Retain success role only on the authoritative verified-result InlineNotice, remove the outer live owner, and assert exact owner count after the real recovery response. |
| T-07-G09 | Denial of Service | high | live-region announcement tree | mitigate | Enforce one owner per dynamic surface and count rendered role/live attributes after state mutation to prevent duplicate assistive output. |
| T-07-G10 | Tampering | high | selector-drift copy/pinned identity | mitigate | Change only the redundant live attribute; retain Vue text, fixed OID clipboard source, exact button name, parent status, and no refresh/export/acknowledgement authority. |
| T-07-G11 | Elevation of Privilege | medium | recovery/copy capabilities | mitigate | Preserve fingerprint-bound recovery and fixed clipboard inputs; add no path body, selector, API, filesystem, or automatic action. |
| T-07-SC | Tampering | low | npm supply chain | accept | No install, upgrade, manifest, lockfile, package, registry, or remote asset change occurs. |

High-severity gate: T-07-G08 through T-07-G10 require the named single-owner markup and dynamic browser assertions; none may be accepted or transferred.
</threat_model>

<verification>
1. Run only the two targeted Playwright commands named in the tasks; do not run formatters, linters, builds, unit suites, or project-wide suites.
2. Observe one live owner after verified recovery and after selector-drift copy success while retaining exact visible content and existing actions.
3. Confirm the four modified paths are exactly those declared and no API, persistence, schema, recovery service, selector polling, export, package, or Phase 08 file changes.
</verification>

<success_criteria>
- REVW-04 and roadmap criterion 5 are no longer blocked by duplicate recovery or selector-copy announcements.
- All five deterministic findings in `07-VERIFICATION.md` and `07-REVIEW.md` are assigned to explicit new tasks with behavioral acceptance criteria; none is omitted or deferred.
- D-01 through D-16 remain covered across the gap plan set, with D-16 directly enforced through truthful labels, non-color structure, and one intended status owner.
- Every high-severity threat is concretely mitigated and no backend, schema, dependency, filesystem authority, review mechanic, or Phase 08 scope is introduced.
</success_criteria>

<output>
Create `.planning/phases/07-github-familiar-review-surfaces/07-09-SUMMARY.md` when done.
</output>
