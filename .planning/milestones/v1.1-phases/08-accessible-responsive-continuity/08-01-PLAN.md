---
phase: 08-accessible-responsive-continuity
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/App.vue
  - src/web/components/DiffWorkspace.vue
  - src/web/styles.css
  - tests/integration/anchored-workspace.spec.ts
autonomous: true
requirements:
  - RESP-01
  - CONT-01
must_haves:
  truths:
    - "At 1440/1280/1100 CSS px the existing Base | active file | Head composition remains recognizable, while at 1099–768 CSS px the active file precedes paired Base/Head endpoints and at 767–320 CSS px it precedes stacked Base then Head endpoints (D-01, D-02)."
    - "The full active path, including renamed/copied old → new identity, remains visible without ellipsis or hover-only disclosure, and the three existing toolbar groups wrap only as indivisible groups in file/change/actions order (D-03, D-04)."
    - "The document, session shell, review shell, review main, headers, toolbar, drawers, comments, notices, and forms fit the visual viewport; only `.diff-workspace__viewport` owns application-level horizontal overflow around the unchanged 640px side-by-side canvas."
    - "Base/Head labels, add-comment affordance, Monaco host, pane split, gutter/code origin, anchors, paired view zones, and Monaco vertical scrolling stay in one unchanged canvas coordinate space."
    - "Files and Review remain the same Vue-controlled drawers with the same inertness, Escape behavior, focus entry/return, events, and caps, but are positioned relative to the review shell with an 8px narrow viewport gutter."
  artifacts:
    - path: "src/web/App.vue"
      provides: "Semantic file → Base → Head context-band source order over the existing data and handlers"
    - path: "src/web/components/DiffWorkspace.vue"
      provides: "Presentation-only diff viewport and fixed inner canvas wrappers"
    - path: "src/web/styles.css"
      provides: "Fluid outer workspace, responsive grid/wrapping, drawer containment, and sole localized diff overflow ownership"
    - path: "tests/integration/anchored-workspace.spec.ts"
      provides: "Boundary-width geometry and unchanged Monaco-coordinate regression evidence"
  key_links:
    - from: "src/web/App.vue:.review-context-header__context"
      to: "src/web/styles.css:.review-context-header__context"
      via: "semantic source order plus desktop/mid/narrow grid-area assignment"
      pattern: "review-context-header__endpoint--base|review-context-header__endpoint--head"
    - from: "src/web/components/DiffWorkspace.vue:.diff-workspace__viewport"
      to: "src/web/styles.css:.diff-workspace__canvas"
      via: "fluid overflow viewport containing exactly one 640px positioned comparison canvas"
      pattern: "diff-workspace__(viewport|canvas)"
    - from: "src/web/components/DiffWorkspace.vue:.diff-workspace__canvas"
      to: "src/web/monaco/diff-adapter.ts"
      via: "existing host ref, ResizeObserver layout, anchor top calculation, and public side-by-side adapter remain untouched inside the new coordinate boundary"
      pattern: "ref=\"host\"|diff-workspace__gutter-action"
    - from: "tests/integration/anchored-workspace.spec.ts:readMonacoGeometry"
      to: "src/web/styles.css:.diff-workspace__viewport"
      via: "rendered document/review-shell/review-main/viewport/canvas scroll and rectangle measurements"
      pattern: "scrollWidth|clientWidth|diff-workspace__viewport|diff-workspace__canvas"
---

<objective>
Establish Phase 08's responsive ownership boundary and semantic header order without changing any review behavior: fluidize the outer workspace, move the unavoidable 640px comparison floor into a single diff canvas, and prove Monaco geometry remains authoritative.

Purpose: Satisfy RESP-01's local two-dimensional comparison exception and create the stable geometry that all later contrast, focus, forced-color, and continuity evidence depends on, while preserving CONT-01.
Output: Reordered existing context-band blocks, two presentation-only diff wrappers, centralized responsive CSS, and focused real-browser geometry assertions.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-accessible-responsive-continuity/08-CONTEXT.md
@.planning/phases/08-accessible-responsive-continuity/08-RESEARCH.md
@.planning/phases/08-accessible-responsive-continuity/08-PATTERNS.md
@.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md
@.planning/phases/06-monaco-diff-semantics/06-03-SUMMARY.md
@.planning/phases/07-github-familiar-review-surfaces/07-06-SUMMARY.md

<interfaces>
`App.vue` currently renders Base, file, Head under `.review-context-header__context`; only those existing blocks may move. The selected `SessionFile`, safe `PathDisplay`, Base/Head session endpoint values, `ReviewToolbar` props/emits, drawer state, and focus methods remain the sole authorities.

`DiffWorkspace.vue` currently renders `.diff-workspace__side-labels`, `.diff-workspace__gutter-action`, and the host ref `.diff-workspace__editor` as direct section children, followed by `.diff-workspace__context-help`. The first three belong inside the positioned 640px canvas; help remains outside the horizontal viewport.

`styles.css` currently gives `.diff-workspace` and narrow `.review-main` a 640px floor, makes `.review-shell` an overflow owner, positions drawers with `top: 64px`, ellipsizes context identities, and emits the narrow widening notice. Those are the obsolete ownership rules; Vue state, adapter geometry, and review mechanics are not obsolete.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move the fixed comparison floor into one semantic diff viewport</name>
  <read_first>
    - `src/web/App.vue:780-883` — existing session shell, drawer state bindings, grouped header, Base/file/Head blocks, and untouched `ReviewToolbar` event wiring.
    - `src/web/components/DiffWorkspace.vue:1-297` — host ref, anchor action calculation, `ResizeObserver` layout, view-zone lifecycle, exposed commands, and exact template seam receiving wrappers.
    - `src/web/styles.css:1-120` — canonical token root and global focus contract; do not create a second token or responsive root.
    - `src/web/styles.css:806-833` — current path ellipsis and old → new display rules that must become complete wrapping.
    - `src/web/styles.css:1056-1511` — review shell/header/toolbar/diff workspace ownership and current 640px floor.
    - `src/web/styles.css:1896-2133` — current 1439/1099/767 drawer, fixed-main, overflow, and widening-notice rules.
    - `src/web/components/ReviewToolbar.vue:24-95` — exact file-navigation, change-navigation, and Review/Keyboard Help group boundaries and source order.
    - `src/web/components/PathDisplay.vue:1-44` and `src/web/components/ui/PathText.vue:1-17` — safe full path, renamed/copied label, directory, filename, and arrow authorities.
    - `src/web/monaco/diff-adapter.ts:98-125,286-289` — immutable side-by-side editor, pane/accessibility ownership, and public layout seam that must not change.
    - `.planning/phases/08-accessible-responsive-continuity/08-CONTEXT.md` — locked D-01 through D-04 and presentation-only phase boundary.
    - `.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md:150-198` — exact overflow owner, source/visual order, breakpoint, wrapping, and drawer contracts.
  </read_first>
  <files>src/web/App.vue, src/web/components/DiffWorkspace.vue, src/web/styles.css</files>
  <action>In `App.vue`, reorder only the existing context children to active file identity → Base endpoint → Head endpoint per D-01/D-02. Add stable endpoint modifier hooks `review-context-header__endpoint--base` and retain `review-context-header__endpoint--head`; do not duplicate blocks, reconstruct endpoint/path data, touch script state, change events, or use CSS `order` to fake semantic reading order. Preserve the toolbar as the following band and preserve all props, emits, accessible names, commands, disabled rules, and focus destinations byte-for-byte.

In `DiffWorkspace.vue`, add only `diff-workspace__viewport` and nested `diff-workspace__canvas`. The canvas must contain the existing side-label row, conditional gutter action, and host-ref editor in their existing order so `host.offsetTop`, 50/50 panes, anchor coordinates, and paired zones share one positioned coordinate space. Keep `diff-workspace__context-help` after/outside the viewport so it wraps to the page width. Add no script symbol, prop, emit, ref, ARIA owner, event, state, adapter call, or runtime breakpoint branch.

In `styles.css`, preserve the single semantic root and existing breakpoints. Make the document/session/review ancestors fluid with `min-width: 0`, `max-width: 100%`, and no page-level horizontal scroll owner. Move `min-width: 640px` exclusively to positioned `.diff-workspace__canvas`; make `.diff-workspace__viewport` the single application horizontal scroll owner with visible/keyboard-operable overflow-x and no competing vertical scroll. Keep the help outside that floor. Remove the narrow `.review-main` fixed width/minimum, `.review-shell` overflow ownership, and `review-shell::after` widening message rather than concealing overflow with hidden/clip.

Assign desktop grid areas Base | file | Head; at 768–1099px render file across row one and paired Base/Head on row two; at <=767px use file, Base, Head rows with Base-before-Head and left-aligned explicit labels. Allow selector labels/OIDs and all active path fragments to wrap with `min-width: 0`, `max-width: 100%`, and appropriate `overflow-wrap`; preserve quiet directory/semibold filename and visible old → new identity, permitting its three-part stack at <=767px per D-04. Keep each `.review-toolbar__group` non-wrapping/atomic while the outer toolbar wraps whole groups in source order, with the actions group moving as a unit per D-03.

Make `.review-shell` the drawer containing block. Anchor Files and Review with block inset 0 instead of a 64px header guess; preserve 288px/360px caps, set `max-width: calc(100vw - 16px)`, an exact 8px narrow outer gutter, internal vertical scrolling, visible close controls, and existing transforms/overlay-only shadows. Do not add a backdrop, focus trap, duplicate drawer, horizontal drawer scroll, API/state path, package, remote asset, or private Monaco selector.</action>
  <verify>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "diff navigation and session state|preserves production Base Head labels and no-reflow Monaco semantic channels at every phase viewport"</automated>
    <browser-check>Run the same two focused cases headed and inspect 1440, 1099, 767, and 320 CSS px: file-first narrow reading order, Base-before-Head stack, atomic toolbar groups, complete path identity, 8px drawer gutter, no document scrollbar, and one visible local diff scrollbar.</browser-check>
  </verify>
  <acceptance_criteria>
    - `App.vue` contains one active-file block, one Base block, one Head block, and one unchanged `ReviewToolbar`; source order is file → Base → Head → toolbar per D-01/D-02.
    - `.diff-workspace__viewport` is the only new horizontal overflow owner and `.diff-workspace__canvas` is the only application element retaining the 640px comparison floor.
    - Side labels, add-comment action, and host ref remain together inside the canvas; context help is outside it; no `DiffWorkspace.vue` script behavior changes.
    - D-03 toolbar groups remain indivisible and ordered; D-04 normal/moved paths and endpoint identities wrap without ellipsis or hover-only dependence.
    - Files/Review use existing state, inertness, Escape, focus entry/return, and caps, with review-shell-relative containment and 8px narrow gutters.
    - No API, state, persistence, command, Monaco mapping, anchor, view-zone, export, package, or asset semantics change.
  </acceptance_criteria>
  <done>The outer workspace reflows semantically and the inner diff viewport alone owns the fixed side-by-side comparison overflow.</done>
</task>

<task type="auto">
  <name>Task 2: Prove localized scroll ownership without Monaco reflow</name>
  <read_first>
    - `tests/integration/anchored-workspace.spec.ts:299-386` — `MonacoGeometry`, `readMonacoGeometry`, and existing no-reflow invariants.
    - `tests/integration/anchored-workspace.spec.ts:408-444` — obsolete 640px `.review-main` expectation in the canonical session/navigation case.
    - `tests/integration/anchored-workspace.spec.ts:1119-1160` — production Base/Head, anchor, selection, pane, sash, gutter, and view-zone viewport matrix.
    - `src/web/App.vue:796-855` — exact shell/header/drawer DOM whose geometry the test must measure.
    - `src/web/components/DiffWorkspace.vue:273-297` — final viewport/canvas DOM and host/action coordinate relationship from Task 1.
    - `src/web/styles.css:1056-1511,1896-2133` — final outer-fluid/local-canvas ownership rules from Task 1.
    - `.planning/phases/06-monaco-diff-semantics/06-03-SUMMARY.md` — inherited no-reflow, independent semantic-channel, and public-Monaco baseline.
    - `.planning/phases/08-accessible-responsive-continuity/08-RESEARCH.md:476-536` — three-layer test strategy and conjunctive phase gate.
    - `.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md:401-413` — required viewport list and local overflow assertions.
  </read_first>
  <files>tests/integration/anchored-workspace.spec.ts</files>
  <action>Extend the existing `MonacoGeometry`/`readMonacoGeometry` contract rather than creating a second harness. Add rendered scroll dimensions/rectangles for `.review-shell`, `.diff-workspace__viewport`, and `.diff-workspace__canvas` while retaining document, `.review-main`, Monaco pane, sash, gutter, line, action, scroll-owner, and zone measurements. Replace the obsolete assertion that `.review-main >= 640px` with document-fit, shell/main containment, viewport/canvas ownership, and reachability assertions.

Expand the production viewport loop to `[1440, 1280, 1100, 1099, 768, 767, 640, 320]`. At every width require document `scrollWidth <= clientWidth`, review main contained by review shell, non-diff surfaces not owning horizontal overflow, canvas width >=640, and both Monaco panes/Base/Head labels present. At narrow widths require viewport `scrollWidth > clientWidth`; scroll it from start to end and prove both Base and Head halves, labels, editor surfaces, and add-comment target are reachable without moving the document horizontally. Retain the before/anchored/selected equality checks for code x-origin, gutter widths, sash/pane geometry, action placement, paired-zone y/height, and Monaco scroll owners so a passing overflow test cannot mask editor reflow.

Do not inspect source text as evidence, lower the 640px canvas, stack panes, loosen line-mapping assertions, mutate adapter behavior, or add arbitrary sleeps/screenshot-only gates.</action>
  <verify>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "diff navigation and session state|preserves production Base Head labels and no-reflow Monaco semantic channels at every phase viewport"</automated>
    <browser-check>Run the viewport matrix headed; at 320px use the localized scrollbar to reveal Base then Head while confirming the browser page itself does not shift horizontally and the anchor action stays aligned to its target line.</browser-check>
  </verify>
  <acceptance_criteria>
    - `MonacoGeometry` reports document, review shell, review main, diff viewport, diff canvas, panes, sashes, gutters, action, zones, and Monaco scroll-owner geometry from rendered DOM.
    - The exact width matrix includes 1440, 1280, 1100, 1099, 768, 767, 640, and 320 CSS px.
    - Every width proves document fit and outer containment; 320 proves positive localized diff overflow and end-to-end Base/Head reachability.
    - Existing code-origin, pane split, sash, gutter, anchor-action, selection, and paired-zone invariants remain equal before/after anchor and overlap states.
    - The test no longer encodes a 640px `.review-main` floor and cannot pass by hidden/clipped content.
  </acceptance_criteria>
  <done>Real-browser geometry makes document fit and local diff overflow conjunctive while preserving all inherited Monaco mechanics.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New DOM/CSS hooks:** `review-context-header__endpoint--base`, `diff-workspace__viewport`, and `diff-workspace__canvas`.
- **Extended test symbols:** `MonacoGeometry` and `readMonacoGeometry` gain review-shell, diff-viewport, and diff-canvas geometry fields; no parallel geometry helper is created.
- **New filesystem path, Vue component, script symbol, prop, emit, ref, state field, API/client method, request/result field, persistence/schema/export field, command, package, dependency, remote asset, environment variable, or human setup:** none.
- **Schema-push assessment:** not applicable; only Vue presentation, global CSS, and browser integration evidence change.

<contribution_decisions>
- TDD mode is active, but this plan is Vue template ordering, CSS layout/overflow, and real-browser geometry glue, not eligible defined-I/O business logic; it remains `type: execute`.
- Wave 1 is a hard prerequisite because later focus and contrast geometry must measure the final clipping ancestors and overflow owner.
- Exact breakpoints use the established 1099/767 boundaries from research and UI-SPEC; no runtime media-query state or duplicate narrow component is introduced.
- D-01 through D-04 are implemented directly in Task 1 and observed in Task 2; none is deferred or approximated.
</contribution_decisions>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Required outcome | Plan/task | Status |
|--------|----|------------------|-----------|--------|
| GOAL | — | Unchanged workflow remains operable across narrow/zoomed contexts | 08-01 establishes its responsive boundary; 08-03 proves full continuity | COVERED |
| REQ | RESP-01 | No page-wide horizontal scroll; only side-by-side diff owns local 2D scrolling | 08-01/T1-T2 | COVERED |
| REQ | CONT-01 | No information architecture, command, persistence, or review-mechanic change | 08-01/T1-T2 plus 08-03 | COVERED |
| RESEARCH | R-RESP | Fluid viewport around fixed 640px comparison canvas | 08-01/T1-T2 | COVERED |
| RESEARCH | R-DRAWER | Review-shell-relative drawers with 8px gutter and existing state | 08-01/T1 | COVERED |
| RESEARCH | R-GEOMETRY | Conjunctive document-fit/local-overflow/Monaco no-reflow proof | 08-01/T2 | COVERED |
| CONTEXT | D-01 | File identity first, endpoints second, toolbar last at narrow widths | 08-01/T1-T2 | COVERED |
| CONTEXT | D-02 | Paired endpoints until smallest Base-before-Head stack | 08-01/T1-T2 | COVERED |
| CONTEXT | D-03 | Atomic toolbar groups in file/change/actions order | 08-01/T1-T2 | COVERED |
| CONTEXT | D-04 | Complete path and old → new identity wrap without truncation | 08-01/T1-T2 | COVERED |

Deferred ideas: none. A11Y-01 through A11Y-03 are assigned to 08-02 and acceptance is consolidated in 08-03; they are later-wave dependencies, not gaps.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository/session text → Vue DOM | Untrusted paths and selector labels must remain text-interpolated while becoming wrappable. |
| Responsive DOM → existing mutation handlers | Reflow must not duplicate a control/component or create a second API/state path. |
| Fluid page → localized diff viewport | Overflow ownership must not hide destructive actions, focus, or pinned Base/Head meaning. |
| Canvas → Monaco adapter | Presentation wrappers must not alter immutable models, line mapping, commands, anchors, or pane geometry. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering / Elevation of Privilege | high | path and selector rendering | mitigate | Preserve `PathDisplay`/`PathText` and Vue interpolation; add no `v-html`, generated untrusted CSS content, or HTML string path reconstruction; verify full text through rendered DOM. |
| T-08-02 | Tampering | high | responsive control tree | mitigate | Move existing nodes once and use CSS reflow only; assert one file/Base/Head/toolbar/drawer instance and preserve exact emits/commands/API paths. |
| T-08-03 | Spoofing / Repudiation | high | clipped/hidden actions and Base/Head provenance | mitigate | Block completion unless document fit, complete labels, local-scroll reachability, drawer actions, and Base-before-Head order pass at 320px; never conceal failures with page overflow clipping. |
| T-08-04 | Denial of Service | medium | diff viewport and Monaco layout | mitigate | Keep one bounded 640px canvas and existing ResizeObserver layout; assert panes, sashes, gutters, anchor action, and zones remain stable across every boundary. |
| T-08-SC | Tampering / Information Disclosure | low | package/asset supply chain | accept | No install, manifest, lockfile, remote font, icon, registry, network asset, or CSP change occurs. |

High-severity gate: T-08-01 through T-08-03 must be mitigated by the named single-DOM-path and rendered geometry assertions; no high-severity threat may be accepted or transferred.
</threat_model>

<verification>
1. Run only the focused `anchored-workspace.spec.ts` command named in both tasks; do not run formatters, linters, or project-wide suites.
2. Observe the exact boundary-width matrix with conjunctive document-fit and diff-local-overflow evidence plus unchanged Monaco geometry.
3. Review the Vue diff for presentation-only changes: one DOM reorder, two wrappers, no script/state/event/API/adapter/persistence/export changes.
4. Record headed observations for desktop, breakpoint edges, 320px, both drawers, complete path wrapping, and Base/Head local-scroll reachability in `08-01-SUMMARY.md`.
</verification>

<success_criteria>
- RESP-01 has a concrete, browser-proven scroll owner: `.diff-workspace__viewport`; no ancestor or non-diff surface retains a 640px floor or horizontal navigation role.
- D-01 through D-04 are visible in semantic DOM, CSS geometry, and browser assertions without duplicate UI or hidden controls.
- Monaco remains side-by-side at a 640px minimum canvas with unchanged panes, code origin, gutters, sashes, anchor action, paired zones, commands, and vertical scrolling.
- CONT-01 boundaries are preserved: no API, state, persistence, command, line-mapping, anchor, export, package, or information-architecture contribution.
- Every high-severity ASVS/STRIDE threat in this plan is concretely mitigated.
</success_criteria>

<output>
Create `.planning/phases/08-accessible-responsive-continuity/08-01-SUMMARY.md` when done.
</output>
