---
phase: 08-accessible-responsive-continuity
plan: "03"
type: execute
wave: 3
depends_on:
  - 08-02
files_modified:
  - tests/e2e/responsive-session.spec.ts
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - RESP-01
  - CONT-01
must_haves:
  truths:
    - "The complete boundary-width matrix (1440, 1280, 1100, 1099, 768, 767, 640, 320 CSS px) proves file-first semantic reflow, paired-then-stacked Base/Head, atomic toolbar groups, complete long content, 8px drawer gutters, document fit, and local diff reachability."
    - "A true 400% browser-zoom pass starting at 1280px behaves like the accepted 320 CSS px contract without transform/screenshot scaling, page-wide horizontal scrolling, hidden actions, or a substitute workflow."
    - "In grayscale and forced colors, unchanged/addition/deletion/selection/active/anchor/focus, review lifecycle/status, recovery, and export states retain labels, signs, patterns, borders, rails, and focus independent of author hue/tint/shadow."
    - "Keyboard-only users can follow the existing destination order and commands from skip links through Files, Monaco review/commenting, Review comment lifecycle, Summary, recovery/drift/gitignore gates, Export, receipt actions, and Escape/focus return at desktop and 320/400% contexts."
    - "Existing packaged comment/draft persistence, conflict/recovery, selector-drift, export, relaunch, receipt, and exact-byte safety suites remain unchanged and passing; presentation evidence adds no API, state, persistence, command, mapping, or publication path."
  artifacts:
    - path: "tests/e2e/responsive-session.spec.ts"
      provides: "Consolidated Phase 08 boundary-width, long-content, drawer, keyboard, grayscale, forced-color, overlap, and responsive workflow evidence"
    - path: "tests/e2e/complete-review-draft.spec.ts"
      provides: "Unmodified packaged comment lifecycle, Summary, relaunch, conflict, recovery, and selector-drift continuity contract"
    - path: "tests/e2e/agent-ready-export.spec.ts"
      provides: "Unmodified packaged intentional export, receipt, relaunch, and exact-byte recovery continuity contract"
  key_links:
    - from: "tests/e2e/responsive-session.spec.ts:expectPhase08ReflowAtWidth"
      to: "src/web/App.vue and src/web/styles.css"
      via: "rendered DOM order/rectangles, complete text, group geometry, drawer containment, and document-fit assertions"
      pattern: "1440|1280|1100|1099|768|767|640|320"
    - from: "tests/e2e/responsive-session.spec.ts:expectNonColorStateCues"
      to: "src/web/styles.css forced-color and public Phase 06/07 state classes"
      via: "visible labels/signs/patterns/edges/rails/focus under achromatopsia and forced-color emulation"
      pattern: "setEmulatedVisionDeficiency|forcedColors"
    - from: "tests/e2e/responsive-session.spec.ts keyboard journey"
      to: "App.vue/ReviewToolbar.vue/DiffWorkspace.vue/ReviewPanel.vue existing handlers"
      via: "actual Tab/shortcut/click/typing/Escape operations with focus and request/response assertions"
      pattern: "Alt\+Shift|Shift\+F7|F7|Alt\+Enter|Keyboard help|Export review"
    - from: "tests/e2e/complete-review-draft.spec.ts and tests/e2e/agent-ready-export.spec.ts"
      to: "existing API/draft/export implementation"
      via: "unchanged packaged behavioral regression boundary"
      pattern: "complete draft lifecycle|packaged-resume-after-relaunch"
---

<objective>
Close Phase 08 with one conjunctive real-browser acceptance matrix that demonstrates responsive accessibility and the unchanged end-to-end local review workflow together, rather than allowing presentation checks or old behavioral suites to pass independently.

Purpose: Prove the roadmap goal and all five requirements across desktop, boundary widths, effective 320 CSS px, true 400% zoom, keyboard-only input, grayscale, forced colors, and real review/export states.
Output: Consolidated packaged Playwright evidence plus unchanged packaged draft/export contract runs; no new production behavior.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-accessible-responsive-continuity/08-CONTEXT.md
@.planning/phases/08-accessible-responsive-continuity/08-RESEARCH.md
@.planning/phases/08-accessible-responsive-continuity/08-PATTERNS.md
@.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md
@.planning/phases/08-accessible-responsive-continuity/08-01-SUMMARY.md
@.planning/phases/08-accessible-responsive-continuity/08-02-SUMMARY.md
@.planning/phases/07-github-familiar-review-surfaces/07-09-SUMMARY.md

<interfaces>
`responsive-session.spec.ts` remains the single packaged accessibility/responsive harness. It inherits the 08-01 document/local-canvas geometry and 08-02 rendered composite/focus/forced-color helpers; this plan adds the final boundary/media/workflow matrix without creating another test application or production hook.

`complete-review-draft.spec.ts` and `agent-ready-export.spec.ts` remain unchanged behavioral authorities. They are read and run, not rewritten into responsive variants: their actual packaged requests, persistence, relaunch, selector-drift, exact-byte recovery, all-or-nothing publication, and receipt assertions are the CONT-01 regression boundary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Consolidate boundary, zoom, grayscale, forced-color, and overlap evidence</name>
  <read_first>
    - `tests/e2e/responsive-session.spec.ts:1-203` — packaged CLI/session fixture and long/moved file identities.
    - `tests/e2e/responsive-session.spec.ts:204-327` — final 08-02 computed-style/composite/focus helpers and Monaco interaction seams.
    - `tests/e2e/responsive-session.spec.ts:328-452` — route controls and build/package lifecycle.
    - `tests/e2e/responsive-session.spec.ts:453-1035` — final real-state responsive/accessibility journey to extend, not duplicate.
    - `src/web/App.vue:780-883` — canonical context/header/drawer/diff/review composition and destination order.
    - `src/web/components/ReviewToolbar.vue:24-95` — exact semantic group boundaries and visible control order.
    - `src/web/components/PathDisplay.vue` and `src/web/components/ui/PathText.vue` — complete safe normal/renamed/copied path identity.
    - `src/web/components/DiffWorkspace.vue` — viewport/canvas, public editor/add-comment targets, and paired-zone coordinate authority.
    - `src/web/styles.css` — final 08-01/08-02 responsive, focus, composite, grayscale-independent, reduced-motion, and forced-color contract.
    - `tests/integration/anchored-workspace.spec.ts:1119-1160` — no-reflow Monaco geometry analog at production widths.
    - `tests/integration/monaco-anchor.spec.ts` — overlapping unchanged/addition/deletion/selection/active/anchor/focus analog.
    - `.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md:150-198` — exact overflow/reflow/header/drawer/long-content contract.
    - `.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md:401-427` — required width, zoom, drawer, contrast, grayscale, forced-color, focus, and workflow evidence matrix.
    - `.planning/phases/08-accessible-responsive-continuity/08-RESEARCH.md:476-536` — layered test strategy, manual matrix, and conjunctive phase gate.
  </read_first>
  <files>tests/e2e/responsive-session.spec.ts</files>
  <action>Add test-only `expectPhase08ReflowAtWidth(page, width)` and `expectNonColorStateCues(page)` helpers inside the existing packaged spec. The reflow helper must set each exact width `[1440, 1280, 1100, 1099, 768, 767, 640, 320]`, wait on rendered geometry rather than timeouts, and assert: document `scrollWidth <= clientWidth`; no non-diff horizontal scroll; file → Base → Head semantic source order; desktop Base|file|Head visual layout; 768–1099 file row then paired endpoints; <=767 file/Base/Head stack; complete normal and moved old → new text; indivisible file/change/actions toolbar rectangles in source order; all controls in bounds; and the diff viewport/canvas ownership inherited from 08-01. At 320, scroll the local viewport to both horizontal ends and prove both panes/labels/action targets remain reachable while document scrollLeft stays unchanged.

Exercise long selector labels, short OIDs, moved paths, long comments, validation copy, selector drift, gitignore confirmation, failure/progress/success notices, full relative receipt paths and SHA-256 hashes through real routes/controls. Open Files and Review at 320, require exact 8px viewport gutter, cap widths, internal vertical/no horizontal overflow, visible close/current focus destination, correct `inert`/`aria-hidden`, Escape dismissal, and opener focus return. Require inline composer/accepted paired zones to grow through the existing measured-height path without clipping, inner horizontal scroll, code overlap, or pane/gutter geometry change.

Use a Chromium CDP session only for grayscale: send `Emulation.setEmulatedVisionDeficiency` with `achromatopsia`, call `expectNonColorStateCues` over representative diff, selection, anchor/focus, selected/resolved/stale/unavailable comment, notice, recovery, and export states, then reset to `none` in guaranteed cleanup. The helper must assert visible labels, literal signs, Base dashed/Head solid bars, icons, 3px edges, selected/source rails, boundaries, and focus—not hue names. Repeat the representative helper under Playwright forced-colors emulation and restore ordinary media. Retain the 08-02 rendered contrast thresholds and reduced-motion assertions.

Automate the effective 320 CSS px contract in the matrix. Additionally, record a headed true 400% browser-zoom pass starting at a 1280px-wide browser by using the browser's zoom control/shortcut and verifying the same 320 contract; a CSS transform, screenshot scale, device-pixel-ratio-only change, or `style.zoom` is not acceptable evidence. Record the observed effective CSS viewport, document/local-diff scroll dimensions, header order, drawer geometry, and focused control in `08-03-SUMMARY.md`. Chromium forced-color emulation is blocking; a real Windows High Contrast pass is additional evidence when an environment exists, not a macOS blocker.

Do not add runtime breakpoint state, duplicate components, screenshots as the sole gate, arbitrary sleeps, private Monaco selectors, API response changes, or production hooks. If an assertion exposes a production semantic defect outside `src/web/styles.css`, stop and re-scope rather than modifying state/API/persistence/command/mapping/export code in this phase.</action>
  <verify>
    <automated>npm run test:package -- tests/e2e/responsive-session.spec.ts --grep "responsive keyboard and accessibility contract"</automated>
    <browser-check>Run the focused case headed and inspect 1440, 1099, 767, 320, real 400% zoom from 1280, achromatopsia, and forced colors. Save the exact observed dimensions/cues/focus destination in the summary; do not use screenshot scaling as zoom proof.</browser-check>
  </verify>
  <acceptance_criteria>
    - The exact eight-width matrix proves header/order/group/path/drawer/content reflow plus conjunctive document fit and local diff overflow.
    - 320px proves both diff halves locally reachable, all non-diff surfaces contained, exact 8px drawer gutters, and unchanged drawer inertness/Escape/focus return.
    - A headed true 400% browser zoom from 1280 records the same effective 320 CSS px invariants without transform/style/screenshot scaling.
    - Achromatopsia and forced colors preserve labels, literal signs, dashed/solid provenance, icons, boundaries, rails, status edges, and persistent focus across representative real states.
    - Long comments, confirmations, relative paths, full hashes, and paired Monaco zones wrap/grow without clipping, page overflow, or changed code/gutter geometry.
    - Reduced motion, composited contrast, and focus-clipping gates from 08-02 remain passing.
  </acceptance_criteria>
  <done>One packaged browser matrix supplies representative desktop/narrow/400%-zoom/grayscale/forced-color/overlap evidence for A11Y-01 through RESP-01.</done>
</task>

<task type="auto">
  <name>Task 2: Prove the unchanged keyboard review, persistence, recovery, and export workflow</name>
  <read_first>
    - `tests/e2e/responsive-session.spec.ts` — final Phase 08 browser harness and route controls from Task 1.
    - `tests/e2e/complete-review-draft.spec.ts:315-923` — packaged comment lifecycle, Summary/relaunch, two-tab conflict, exact-byte recovery, read-only newer draft, and selector/worktree drift tests.
    - `tests/e2e/agent-ready-export.spec.ts:1-420` — packaged relaunch, accepted review, intentional export, receipt, target-aware second export, and exact-byte recovery contract.
    - `src/web/App.vue` — existing skip links, commands, focus-comment selection, drawer/help dismissal, and all state/API wiring that must remain unchanged.
    - `src/web/components/ReviewPanel.vue` — Summary → Open → Resolved → Export order and exact existing lifecycle destinations.
    - `src/web/components/ExportSection.vue`, `src/web/components/DriftExportAcknowledgement.vue`, `src/web/components/GitignoreStatus.vue`, and `src/web/components/ExportReceipt.vue` — intentional export, drift, two-step ignore consent, progress/failure/receipt, and copy/reveal authorities.
    - `src/web/components/DraftRecovery.vue` and `src/web/components/SelectorDriftNotice.vue` — read-only recovery and pinned-comparison authorities with single status ownership.
    - `.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md:202-236` — required focus inventory and unchanged command/focus transfers.
    - `.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md:282-367` — complete workflow, non-actionable record, intentional export, conflict/drift/recovery/gitignore/publication contracts.
    - `.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md:422-436` — focus/workflow/export integrity acceptance gates.
    - `.planning/REQUIREMENTS.md:39-41` — CONT-01 unchanged mechanics requirement.
  </read_first>
  <files>tests/e2e/responsive-session.spec.ts</files>
  <action>Extend the existing Phase 08 packaged journey with a keyboard-only 320px path over the current single component/state tree: use all three skip links; open Files, select/navigate files, and restore opener focus; use `Alt+Shift+[`/`]`, `Shift+F7`, `F7`, both Monaco panes, context reveal, and `Alt/Option+Enter`; add a line comment with existing textarea focus and exercise retain/discard; open Review and use focus-comment selection plus edit/resolve/reopen/delete under existing eligibility; use Summary disclosure and arrow-key Write/Preview tabs plus save/discard; exercise Keyboard Help `?`/Escape; and verify every focus destination/perimeter with the 08-02 helper. Keep the exact destination order and assert no duplicate control/live-region owner enters the tab sequence.

Continue through existing selector-drift acknowledgement, stale-revision reload/conflict, read-only recovery, two-step gitignore consent, and Export surfaces when the canonical fixture state permits. Assert publication occurs only after one of the exact intentional actions `Export review`, `Export review again`, `Export pinned review`, or `Try export again`; never after comment/Summary save, reload, acknowledgement, ignore append, or readiness check. Preserve exact no-partial-publication messages, relative receipt paths, full hashes, accepted revision, timestamp, drift/byte fields, copy/reveal focus behavior, and non-actionable resolved/stale/unavailable records/no-guess instruction.

Do not duplicate the persistence/export scenarios already authoritative in the two packaged suites. Keep `tests/e2e/complete-review-draft.spec.ts` and `tests/e2e/agent-ready-export.spec.ts` read-only and source-unmodified, run each suite in full without `--grep`, and use their request bodies, revisions, filesystem bytes, relaunch state, conflict, corrupt recovery, recovery failure, newer-draft, selector/worktree drift, atomic export, and receipt assertions as the behavioral half of the conjunctive gate. The Phase 08 test contributes presentation/focus/visibility evidence only. No API, route implementation, draft schema, request/response, command, line mapping, persistence, filesystem, export, or publication file may change.

If either unchanged full packaged suite fails, diagnose whether Phase 08 presentation made an existing control unreachable. Fix only presentation within the already approved `src/web/styles.css` contract in the owning prior plan before accepting this plan; do not edit either authority suite, weaken old assertions, filter out failing lifecycle cases, or special-case behavior in the responsive test.</action>
  <verify>
    <automated>npm run test:package -- tests/e2e/responsive-session.spec.ts --grep "responsive keyboard and accessibility contract" &amp;&amp; npm run test:package -- tests/e2e/complete-review-draft.spec.ts &amp;&amp; npm run test:package -- tests/e2e/agent-ready-export.spec.ts</automated>
    <browser-check>Run the responsive case headed at 320px and true 400% zoom; complete the keyboard flow from skip links through comment, Review, Summary, export receipt, and Escape/focus return while confirming only the diff scrolls horizontally.</browser-check>
  </verify>
  <acceptance_criteria>
    - The 320px keyboard journey covers the exact UI-SPEC focus inventory and commands without a hidden, duplicated, reordered, or narrow-only destination.
    - File navigation, side-by-side diff/context, commenting, lifecycle, Summary, help, drift/recovery/ignore gates, Export, receipt copy/reveal, Escape, and focus return remain reachable with persistent focus.
    - Export requests follow only intentional CTA actions; comment/Summary save, reload, drift acknowledgement, ignore append, and readiness checks do not publish.
    - The source-unmodified complete draft suite runs in full and proves accepted mutations, revisions, persistence, relaunch, two-tab conflict, corrupt recovery, recovery failure, exact-byte recovery, read-only newer-draft behavior, selector drift, and registered-worktree drift.
    - The source-unmodified agent-ready export suite runs in full and proves atomic pair publication, intentional repeat export, relative receipt data, relaunch, exact-byte recovery, and source-control safety.
    - No shared grep filters either authority suite, and no API/state/persistence/command/Monaco mapping/anchor/view-zone/export/filesystem semantics or old behavioral assertions change.
  </acceptance_criteria>
  <done>CONT-01 is proven by one responsive keyboard presentation journey plus unchanged packaged draft/export behavioral authorities.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New test-only symbols in `responsive-session.spec.ts`:** `expectPhase08ReflowAtWidth` and `expectNonColorStateCues`.
- **Extended existing browser artifact:** `responsive keyboard and accessibility contract` gains the exact eight-width matrix, long-content/drawer/local-scroll checks, achromatopsia and repeated forced-color cues, and the 320px keyboard continuity journey.
- **New recorded evidence:** `08-03-SUMMARY.md` records headed 1440/1099/767/320, true 400%-zoom-from-1280, grayscale, forced-color, overlap, and workflow observations with exact rendered dimensions; the summary is the standard executor output, not a new product/test path.
- **Changes to `complete-review-draft.spec.ts`, `agent-ready-export.spec.ts`, production symbols/paths, API/client methods, request/result fields, persistence/schema/export fields, commands, packages, dependencies, remote assets, environment variables, or human setup:** none.
- **Schema-push assessment:** not applicable; only existing Playwright acceptance coverage is extended.

<contribution_decisions>
- TDD mode is active, but this is browser-flow, media-emulation, geometry, and continuity evidence over existing behavior; it remains `type: execute` rather than a TDD business-logic plan.
- This plan depends on 08-02 because the final width/media/workflow matrix must exercise the completed scroll, contrast, focus, and forced-color contract, not an intermediate cascade.
- True browser zoom is a headed evidence requirement because a 320px viewport alone is only the automated equivalent; transform/style/screenshot scaling is explicitly rejected.
- Existing draft/export suites remain unmodified to prevent Phase 08 from weakening or forking behavioral authority. Presentation and behavioral gates must both pass.
</contribution_decisions>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Required outcome | Plan/task | Status |
|--------|----|------------------|-----------|--------|
| GOAL | — | Complete unchanged review workflow across desktop, narrow, zoomed, keyboard, and forced-color contexts | 08-01 through 08-03; 08-03/T1-T2 conjunctive gate | COVERED |
| REQ | A11Y-01 | Composited WCAG AA text/indicator contrast | 08-02 implementation; 08-03/T1 repeated overlap/media evidence | COVERED |
| REQ | A11Y-02 | Persistent unclipped focus on every operable control/editor affordance | 08-02 implementation; 08-03/T1-T2 full inventory journey | COVERED |
| REQ | A11Y-03 | Durable forced-color labels, markers, borders, and focus | 08-02 implementation; 08-03/T1 forced-color/grayscale matrix | COVERED |
| REQ | RESP-01 | No page-wide scroll at narrow/400%; only side-by-side diff local 2D scroll | 08-01 implementation; 08-03/T1 exact width/zoom matrix | COVERED |
| REQ | CONT-01 | Unchanged navigation/review/comment/Summary/export/commands/persistence/mechanics | 08-03/T2 plus unchanged packaged suites | COVERED |
| RESEARCH | R-TEST-1 | Anchored geometry boundary-width proof | 08-01/T2, consumed 08-03/T1 | COVERED |
| RESEARCH | R-TEST-2 | Real-state responsive/contrast/focus/forced-color proof | 08-02/T2 and 08-03/T1 | COVERED |
| RESEARCH | R-TEST-3 | Reuse unchanged packaged workflow continuity suites | 08-03/T2 | COVERED |
| RESEARCH | R-MANUAL | Desktop/narrow/400%-zoom/grayscale/forced-color manual observations | 08-03/T1 browser-check and summary evidence | COVERED |
| CONTEXT | D-01 | Narrow order file → endpoints → toolbar | 08-01 implementation; 08-03/T1 matrix | COVERED |
| CONTEXT | D-02 | Paired endpoints then Base-before-Head stack | 08-01 implementation; 08-03/T1 matrix | COVERED |
| CONTEXT | D-03 | Atomic toolbar groups preserve file/change/actions order | 08-01 implementation; 08-03/T1-T2 matrix/journey | COVERED |
| CONTEXT | D-04 | Full path and old → new identity remain available | 08-01 implementation; 08-03/T1 long-content matrix | COVERED |

Deferred ideas: none. All GOAL, REQ, RESEARCH, and CONTEXT items are assigned; no plan introduces a future requirement or shrinks a locked decision.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keyboard/browser context → existing UI capabilities | Responsive/media modes must not expose a duplicate, hidden, or reordered capability path. |
| Existing UI controls → loopback API/draft/export | Presentation must preserve exact requests, revisions, consent, persistence, and publication authority. |
| Browser evidence → acceptance decision | Synthetic states, screenshots, or a 320px viewport alone must not impersonate true zoom/full workflow proof. |
| Receipt/recovery text → browser display | Relative safe values and exact authoritative copy must remain visible without leaking absolute paths or guessing anchors. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-08-10 | Spoofing / Repudiation | high | incomplete accessibility evidence | mitigate | Require exact width matrix, headed true 400% zoom, achromatopsia, forced colors, rendered focus/composite gates, and visible cues; reject screenshot/transform-only proof. |
| T-08-11 | Tampering | high | duplicate responsive mutation/export path | mitigate | Drive one existing component tree and exact controls; assert publication only after intentional CTA and run unchanged packaged request/persistence/export contracts. |
| T-08-12 | Denial of Service | high | hidden/clipped destructive or recovery controls | mitigate | Complete keyboard focus inventory at 320/400%, assert geometry, drawer/internal scroll, confirmation/action visibility, Escape, and opener return. |
| T-08-13 | Information Disclosure / Elevation of Privilege | high | recovery/export values and browser authority | mitigate | Preserve repository-relative receipt/recovery display, fixed safe clipboard/reveal methods, no absolute path/body authority, and existing read-only/consent gates. |
| T-08-14 | Tampering | medium | weakened regression assertions | mitigate | Keep complete-review-draft and agent-ready-export source unchanged; fix presentation reachability rather than loosening behavioral assertions. |
| T-08-SC | Tampering / Information Disclosure | low | package/asset supply chain | accept | No install, package, lockfile, registry, font, icon, remote asset, or external service contribution occurs. |

High-severity gate: T-08-10 through T-08-13 must be mitigated by the named browser and unchanged packaged-flow evidence; no high-severity threat may be accepted or transferred.
</threat_model>

<verification>
1. Run the focused `responsive-session.spec.ts` case separately, then run `complete-review-draft.spec.ts` in full and `agent-ready-export.spec.ts` in full with no shared grep; do not run formatters, linters, or project-wide suites.
2. Require all five dimensions conjunctively: document fit/local diff overflow; rendered contrast; persistent focus; grayscale/forced-color cues; unchanged packaged workflow/persistence/export behavior.
3. Perform headed observations at 1440, 1099, 767, 320, and true 400% zoom from 1280; record exact dimensions and representative cues in `08-03-SUMMARY.md`.
4. Confirm the only source/test diff in this plan is `tests/e2e/responsive-session.spec.ts`; existing packaged behavioral suites and all production/API/state/persistence/export files remain unchanged.
</verification>

<success_criteria>
- Every roadmap success criterion is exercised in a real browser and every requirement ID A11Y-01, A11Y-02, A11Y-03, RESP-01, CONT-01 has explicit passing evidence.
- Representative desktop, narrow, exact breakpoint, effective 320, true 400%-zoom, grayscale, forced-color, overlapping-state, and complete keyboard workflow contexts are recorded.
- Presentation and behavior pass together: no page overflow, local diff scroll present, focus/composites/cues durable, and existing draft/export suites unchanged and green.
- D-01 through D-04 remain exact, with no hidden/menu-replaced controls, truncated identity, stacked Monaco panes, or changed information architecture.
- No API, state, persistence, command, Monaco mapping, anchor/view-zone, export/publication, package, asset, or filesystem semantics change.
- Every high-severity ASVS/STRIDE threat in this plan is concretely mitigated.
</success_criteria>

<output>
Create `.planning/phases/08-accessible-responsive-continuity/08-03-SUMMARY.md` when done.
</output>
