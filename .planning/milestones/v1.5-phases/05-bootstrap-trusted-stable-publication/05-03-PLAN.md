---
phase: 05-bootstrap-trusted-stable-publication
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - .github/workflows/publish-npm.yml
  - docs/distribution-operations.md
autonomous: true
requirements: [REL-02]
must_haves:
  truths:
    - "Per D-01, one manual workflow run/attempt builds, scans, installs, seals, transfers, gates, and publishes one fresh configured `@shipwithai/cumpa@1.5.0` candidate without a second build or pack."
    - "Per D-05, the workflow is dispatch-only and fixed to the reviewed public main SHA; the first candidate step rejects a missing, malformed, or moved-main SHA before checkout, installation, or origin use."
    - "Per D-06/D-07, the candidate job runs on GitHub-hosted `macos-15`, asserts `darwin/arm64`, receives only the separately masked transient origin secret, and proves configured runtime/legal/native/installed acceptance."
    - "Per D-08, only the protected `npm-release` publish job has `id-token: write`; it downloads the build job's exact artifact ID, validates the unchanged seal/archive/current run context, and directly publishes that supplied tgz through npm trusted publishing."
    - "Per D-02, operative policy designates only a newly approved CI candidate for publication while preserving Phase 3/4 history, the old archive/evidence/approval, and legacy artifact dispositions unchanged."
    - "Per D-03/D-04/D-09, policy keeps source/bootstrap/configuration/candidate/stable authorities separate and defines actual registry, attestation, global, and npx evidence as completion rather than intended commands."
  prohibitions:
    - "HIGH: No push, pull_request, release, tag, schedule, workflow_call, nonempty dispatch input, floating action ref, caller-selected package/version/path/ref/digest, or automatic publish retry."
    - "HIGH: No checkout, dependency install, support-origin use, build, or pack may precede the temporary expected-source guard; no build/pack/prepack/package mutation occurs in the publish job."
    - "HIGH: No raw `SUPABASE_PROJECT_REF` expression in workflow command/action/env metadata; no local `.env`, permanent duplicate, npm-release copy, or assumption that `GITHUB_TOKEN` can administer environment variables/secrets."
    - "HIGH: No npm token, `NODE_AUTH_TOKEN`, `npm whoami` OIDC inference, staged publication detour, provenance disabling, or signature-only success claim."
    - "No edit to `.github/workflows/deploy-supabase-production.yml`; 05-04 exclusively owns separately authorized source push and collateral Supabase prevention."
    - "No rewrite, deletion, fallback publication, or retrospective CI/provenance claim for Phase 4 archive/evidence/approval or legacy artifacts."
  artifacts:
    - path: ".github/workflows/publish-npm.yml"
      provides: "Input-free same-run Darwin ARM64 candidate and protected token-free stable publisher"
      contains: "workflow_dispatch"
    - path: "docs/distribution-operations.md"
      provides: "Operative new-CI-candidate distribution policy while retaining historical Phase 3/4 records"
      contains: "same workflow run and attempt"
  key_links:
    - from: ".github/workflows/publish-npm.yml build-candidate"
      to: "05-01 producer/scanner/installed acceptance and 05-02 seal-candidate"
      via: "one configured build/pack followed by full checks and sealed evidence before upload"
      pattern: "seal-candidate|candidate-upload"
    - from: "build-candidate outputs"
      to: ".github/workflows/publish-npm.yml publish"
      via: "exact native artifact ID/digest and independently hashed inner evidence/archive"
      pattern: "artifact-id|artifact-digest|expected-evidence-sha256"
    - from: ".github/workflows/publish-npm.yml publish"
      to: "npm trusted publisher"
      via: "protected npm-release environment and job-only OIDC direct tarball publish"
      pattern: "id-token: write|npm publish"
---

<objective>
Add the fixed manual GitHub Actions workflow that produces and later publishes one newly approved candidate in the same run/attempt, then replace only obsolete operative Phase 5 policy with that selected design.

Purpose: Encode D-01's truthful CI provenance path and least-privilege publication boundary before any source or registry mutation is authorized.
Output: `.github/workflows/publish-npm.yml` and an updated `docs/distribution-operations.md` Phase 5 policy.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-CONTEXT.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-RESEARCH.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-PATTERNS.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-01-SUMMARY.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-02-SUMMARY.md
@.planning/phases/04-exact-runtime-tarball/04-04-SUMMARY.md
@.github/workflows/deploy-supabase-production.yml
@scripts/pack-runtime.mjs
@scripts/verify-production-artifacts.mjs
@scripts/verify-npm-release.mjs
@tests/package/agent-ready-export.test.ts
@docs/distribution-operations.md
@package.json
@package-lock.json
</context>

<interfaces>
- Trigger is exactly input-free `workflow_dispatch`; repository/workflow policy constants remain `Ship-With-AI/cumpa`, `.github/workflows/publish-npm.yml`, `main`, package `@shipwithai/cumpa`, version `1.5.0`, archive basename `shipwithai-cumpa-1.5.0.tgz`.
- Use one fixed workflow concurrency group `cumpa-npm-stable-1.5.0` with `cancel-in-progress: false`, plus 05-05's operational no-existing-run/competing-run checks. No logic cancels a running publish or replays it. Queued runs are not permission to reuse temporary configuration; unexpected runs stop the operating plan, and owned-source-guard removal fences later admissions during abort cleanup.
- Reviewed immutable action pins current on 2026-09-09: `actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1` (v7.0.1), `actions/setup-node@820762786026740c76f36085b0efc47a31fe5020` (v7.0.0), `actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a` (v7.0.1), and `actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c` (v8.0.1). Keep version comments beside full SHAs and confirm the referenced commits still belong to those official release tags before committing; never replace them with a tag or placeholder.
- Both jobs use Node 24 and exact npm `11.19.1`, the current supported npm documented during planning. `setup-node` has `package-manager-cache: false`; the release build has no dependency cache. Assert actual Node major/npm version rather than assuming runner contents.
- `build-candidate` uses `runs-on: macos-15`, `environment: production`, job `permissions: {contents: read}`, and first runs a source guard with only `CUMPA_RELEASE_SOURCE_SHA: ${{ vars.CUMPA_RELEASE_SOURCE_SHA }}`. It requires one lowercase 40-hex value exactly equal to `GITHUB_SHA` before checkout, npm setup/install, or mapping/using the origin secret.
- Only the producer, scanner, and installed-acceptance steps map `${{ secrets.CUMPA_RELEASE_BUILD_ORIGIN }}` to `CUMPA_RELEASE_SUPPORT_SERVICE_URL`; no job-level, seal, upload, or publish step receives it, and the authoritative `SUPABASE_PROJECT_REF` never appears in workflow expressions. Assert `process.platform === "darwin"`, `process.arch === "arm64"`, and GitHub-hosted runner context before production.
- Candidate sequence is exactly checkout of `GITHUB_SHA` with persisted credentials disabled -> Node/npm setup -> `npm ci` -> `npx playwright install --with-deps chromium` using the lockfile-installed Playwright -> one `pack-runtime.mjs --purpose candidate` invocation -> scanner report -> complete `accept:runtime-artifact` report -> `seal-candidate` -> independent archive/evidence hashing -> read-only files -> one artifact upload. Browser provisioning receives no origin and is not a Cumpa build. There is no earlier `npm run build` or later `npm pack`.
- Set finite job execution limits: `build-candidate` timeout 45 minutes and `publish` timeout 15 minutes. Environment waiting and overall run expiry remain separate provider limits recorded by 05-05; no automatic rerun follows any timeout.
- Artifact name is `cumpa-stable-candidate-${{ github.run_id }}-${{ github.run_attempt }}`. Upload only the tgz and sealed evidence once, with no overwrite or retention override. Build job outputs exactly `artifact-id`, `artifact-digest`, `evidence-sha256`, `archive-sha256`, and `archive-byte-length`; artifact ID/digest come from upload action after the sealed payload exists.
- `publish` needs `build-candidate`, runs on fixed `ubuntu-24.04`, uses protected `environment: npm-release`, and alone has `permissions: {contents: read, id-token: write}`. It checks out the same source only for the stdlib verifier, sets up Node/npm without cache or `npm ci`, downloads the exact artifact ID, compares the two archive-hash/length needs outputs in the preceding stdlib step, then invokes `verify-candidate` using only its declared flags. The sole mutating command publishes an absolute `.tgz` with `--tag latest --access public --ignore-scripts --fetch-retries=0 --registry https://registry.npmjs.org/`.
- Before invoking `verify-candidate`, a separate stdlib step recomputes the local `.tgz` SHA-256 and byte length and compares them with `needs.build-candidate.outputs.archive-sha256` and `archive-byte-length`. The helper receives only its declared archive/evidence/evidence-seal/artifact-ID/artifact-digest flags; do not invent extra hash/length options.
- Run npm publication from fresh mode-0700 scratch outside the source checkout with a fresh HOME/cache and distinct empty user/global npm config files. Remove inherited npm credentials/config overrides while retaining the real GitHub OIDC and CI context variables required by npm. This does not create a token or change CI identity. Do not rely on ambient runner/project npm configuration, and do not pass the deleted production origin to this job.
- Publication has no retry/concurrency cancellation that can replay npm. Any nonzero/timeout/ambiguous outcome ends the job; 05-06 performs read-only registry reconciliation and records the actual result.
- Both bootstrap and stable `npm publish` commands disable npm's transport retry configuration with `--fetch-retries=0`; one CLI invocation must not hide a package-upload retry. Read-only reconciliation remains a separate operating step.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Encode the one-build same-run candidate and protected publisher</name>
  <files>.github/workflows/publish-npm.yml</files>
  <read_first>
    - `05-CONTEXT.md` D-01, D-03, D-05, D-06, D-07, and D-08 plus the temporary source-guard decision
    - `05-01-SUMMARY.md` exact bootstrap/stable producer, verifier, profile, and acceptance interfaces
    - `05-02-SUMMARY.md` exact seal/prepublish commands and evidence fields
    - `05-PATTERNS.md` workflow analog and no-producing-analog warning
    - `.github/workflows/deploy-supabase-production.yml` for repository YAML style and the boundary that must not be edited or copied as publication behavior
    - `scripts/pack-runtime.mjs`, `scripts/verify-production-artifacts.mjs`, `scripts/verify-npm-release.mjs`, `tests/package/agent-ready-export.test.ts`, `package.json`
    - Official release/tag records for the four pinned actions; official GitHub `macos-15` public runner documentation; official npm trusted-publisher/current provenance documentation
  </read_first>
  <action>Create the input-free dispatch workflow with exactly `build-candidate` and `publish`. Use the full action SHAs and exact tool/runtime versions in `<interfaces>`, minimal job permissions, no cache, and no source/ref/path/version/digest inputs. The candidate job's first executable step must validate temporary `production` variable `CUMPA_RELEASE_SOURCE_SHA` as exact lowercase 40-hex equal to `GITHUB_SHA`; put checkout, package installation, runtime origin mapping, and all production work after that gate. Recheck the actual checkout SHA after checkout without weakening the first gate.

Per D-01/D-06/D-07, perform one configured producer invocation and one scripts-disabled pack on the actual GitHub-hosted Darwin ARM64 runner. Capture producer evidence, standalone scanner JSON, and complete installed acceptance, then use 05-02 `seal-candidate`; require configured origin fingerprint, legal/runtime checks, actual native re-export, source head, runner, repository, workflow, run and attempt. Seal evidence before uploading. Make archive/evidence read-only and rehash them; upload only those two files once. Export native artifact observations and independent inner hashes without mutating sealed evidence or claiming the transport digest is the tgz digest.

Per D-03/D-08, keep `publish` blocked behind `npm-release`. Download by exact artifact ID, rehash sealed evidence/archive and run the stdlib-only `verify-candidate` in the actual same source/run/attempt. Prepare the isolated outside-checkout npm publication context and execute exactly one absolute-tarball `npm publish` with explicit `--tag latest --access public --ignore-scripts --registry https://registry.npmjs.org/`. Retain real OIDC context while stripping static npm authentication/configuration. Do not build, repack, run lifecycle scripts, import the full origin/Vue-dependent scanner, stage, retry or fall back to Phase 4. The separate actual stable authority is handled in 05-06.</action>
  <verify>
    <automated>node --input-type=module -e 'import assert from "node:assert/strict"; import {readFileSync} from "node:fs"; const y=readFileSync(".github/workflows/publish-npm.yml","utf8"); for (const s of ["workflow_dispatch:","build-candidate:","runs-on: macos-15","CUMPA_RELEASE_SOURCE_SHA","cumpa-stable-candidate-","artifact-id","publish:","environment: npm-release","id-token: write","npm publish"]) assert.ok(y.includes(s),s); assert.equal((y.match(/id-token: write/g)||[]).length,1); assert.equal((y.match(/pack-runtime\.mjs/g)||[]).length,1);'</automated>
  </verify>
  <acceptance_criteria>The committed workflow has an input-free manual trigger, source guard first, one Darwin ARM64 build/pack, complete checks, sealed pre-upload evidence, exact artifact-ID transfer, one protected token-free direct publish, and no alternate/retry path.</acceptance_criteria>
  <done>The source defines the fixed same-run mechanism; no workflow has been pushed, dispatched, approved, uploaded, or published by this task.</done>
</task>

<task type="auto">
  <name>Task 2: Replace obsolete Phase 5 operative policy without rewriting history</name>
  <files>docs/distribution-operations.md</files>
  <read_first>
    - `05-CONTEXT.md` D-01 through D-09 and deferred ideas
    - `docs/distribution-operations.md` complete Phase 3, Phase 4, Phase 5, unchanged-boundary, and Supabase sections
    - `04-04-SUMMARY.md` exact historical archive disposition and limitations
    - `.github/workflows/publish-npm.yml` implemented Task 1 contract
    - `.planning/REQUIREMENTS.md` PKG-01, PKG-02, REL-01, REL-02, and carried-forward REL-03 invariant
  </read_first>
  <action>Update only the operative Phase 5 section and any directly contradictory forward-looking sentence. Per D-01/D-02, state that the old Phase 4 archive/evidence/approval remains immutable historical acceptance but its publication designation is superseded only by actual approval of a new CI candidate; it is never fallback publication input and its local build is never called CI provenance. Preserve Phase 3 and Phase 4 records, old hashes, legacy artifact dispositions, and all historical claims byte-for-byte unless a narrow cross-reference must distinguish the new policy.

Document the complete future execution contract: distinct usable bootstrap identity/tag and revoked isolated interactive authorization per D-04; main-only reviewed source and separately authorized `[skip ci]` collateral-deployment prevention owned by 05-04 per D-05; the authoritative production variable, transient masked origin secret and temporary expected-source variable with absent-only creation/read-back/cleanup per D-06; GitHub-hosted Darwin ARM64 one-build candidate and full installed/native/legal acceptance per D-07; same run/attempt sealed evidence, native artifact ID handoff, actual digest approval, protected environment, separate publication authority, direct OIDC publication, no retry/rebuild/fallback, and exact claim inspection per D-08; exact public registry download plus cryptographic attestation, separate clean global install, and exact npx proof per D-09.

Make explicit that planning, setup, bootstrap, CI artifact approval, and successful checks do not grant stable publication. Preserve automatic provenance but claim only actual cryptographically verified, source/run-matched facts; missing/mismatched/ambiguous results stop and are recorded honestly. Keep Phase 6/7, no-framework, privacy, legal, support, and unchanged-product boundaries intact. Do not modify the Supabase workflow or suggest weakening it.</action>
  <verify>
    <automated>node --input-type=module -e 'import assert from "node:assert/strict"; import {readFileSync} from "node:fs"; const d=readFileSync("docs/distribution-operations.md","utf8"); for (const s of ["New CI-built stable candidate","same workflow run and attempt","CUMPA_RELEASE_SOURCE_SHA","CUMPA_RELEASE_BUILD_ORIGIN","npm-release","npm audit signatures","npx --yes @shipwithai/cumpa@1.5.0"]) assert.ok(d.includes(s),s); for (const s of ["Phase 3","Phase 4","e7766d43f7f804b138e694b298480cdec62ebfc16960f86d4c1e3c7959cf1dca"]) assert.ok(d.includes(s),s);'</automated>
  </verify>
  <acceptance_criteria>Operative policy exactly reflects the selected new-CI same-run release, authorization/privacy/failure/public proof boundaries, while historical Phase 3/4 and legacy dispositions remain records rather than rewritten inputs.</acceptance_criteria>
  <done>Maintainers have an executable policy aligned with the workflow; documentation itself authorizes no source, configuration, CI, npm, or publication mutation.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `.github/workflows/publish-npm.yml`
  - jobs: `build-candidate`, `publish`
  - transient inputs consumed: `vars.CUMPA_RELEASE_SOURCE_SHA`, `secrets.CUMPA_RELEASE_BUILD_ORIGIN`
  - build outputs: `artifact-id`, `artifact-digest`, `evidence-sha256`, `archive-sha256`, `archive-byte-length`
  - fixed action pins: checkout `3d3c42e5aac5ba805825da76410c181273ba90b1`, setup-node `820762786026740c76f36085b0efc47a31fe5020`, upload-artifact `043fb46d1a93c77aae656e7c1c64a875d1fc6a0a`, download-artifact `3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c`
- `docs/distribution-operations.md`
  - operative Phase 5 new-CI-candidate, same-run publication, exact authority, cleanup, evidence, and public verification policy
</artifacts_this_phase_produces>

<threat_model>
## ASVS Level 1 / HIGH Threat Register

| Threat ID | STRIDE | Boundary | Risk | Disposition | Specific mitigation and proof |
|---|---|---|---|---|---|
| T-05-03-01 | Spoofing | Dispatch/main movement -> build source | HIGH | mitigate | Input-free dispatch plus absent-only expected-SHA variable; first step compares exact 40-hex to `GITHUB_SHA` before any source/origin action. |
| T-05-03-02 | Tampering | Build outputs -> publish input | HIGH | mitigate | Seal producer/scanner/acceptance/CI evidence before upload; transfer exact artifact ID; rehash inner evidence/tgz and live context in publish. |
| T-05-03-03 | Elevation of privilege | Candidate job -> npm publication | HIGH | mitigate | Candidate job has contents-read only; only protected publish job receives job-scoped `id-token: write`; no npm token exists. |
| T-05-03-04 | Information disclosure | Production variable -> Actions logs/metadata | HIGH | mitigate | Workflow never references raw authoritative variable; origin arrives only as separately authorized masked transient secret on producer step. |
| T-05-03-05 | Repudiation | Environment approval -> unrelated publication | HIGH | mitigate | Fixed package/version/path/source/run/artifact, no dispatch inputs, native pending deployment, and separate 05-06 stable authorization. |
| T-05-03-06 | Tampering | Publish lifecycle -> approved bytes | HIGH | mitigate | Publish job has no dependency install/build/pack/prepack and runs prepublish verifier immediately before one supplied-tarball publish. |
| T-05-03-07 | Denial of service | Ambiguous npm response -> replay | HIGH | mitigate | No retry/rebuild/rerun logic; job stops and 05-06 performs read-only exact-version reconciliation. |
| T-05-03-SC | Tampering | Third-party Actions/npm toolchain | HIGH | mitigate | Official Actions are pinned to reviewed full release commits, npm exact version is asserted, package cache disabled, no new dependency. |
</threat_model>

<verification>
- Focused static workflow invariant check passes; actual GitHub workflow recognition, first-step guard, runner identity, artifact outputs, environment wait, OIDC exchange, and publication are proven only by 05-04/05-05/05-06 execution.
- Distribution policy no longer directs publication of the Phase 4 local tarball and still preserves historical records and hashes.
- Existing Supabase workflow is not modified.
</verification>

<success_criteria>
- One fixed input-free workflow can keep a fresh candidate and eventual publication in one top-level run/attempt with exact native artifact transfer.
- Only a protected publish job can obtain OIDC; no build, pack, fallback, retry, or long-lived npm credential exists there.
- Operative policy covers D-01 through D-09 without revising historical Phase 3/4 evidence.
</success_criteria>

<output>
Create `.planning/phases/05-bootstrap-trusted-stable-publication/05-03-SUMMARY.md` when done.
</output>
