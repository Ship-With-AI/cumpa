---
phase: 05-bootstrap-trusted-stable-publication
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - scripts/verify-npm-release.mjs
  - tests/package/npm-release-verifier.test.ts
autonomous: true
requirements: [PKG-01, PKG-02, REL-02]
must_haves:
  truths:
    - "Per D-01, one small verifier binds a newly built stable candidate's producer, scanner, installed-acceptance, archive, and GitHub CI source/run/attempt facts before those bytes can reach publication."
    - "Per D-08, prepublication validation fails closed unless the sealed evidence, exact archive, repository, workflow, source SHA, run ID, run attempt, hosted runner, Darwin ARM64 producer, and passed candidate checks all agree with the current workflow context."
    - "Per D-09, public verification independently downloads exact registry bytes, compares byte length/SHA-256/SHA-1/SHA-512, delegates signature and attestation cryptography to current npm, inspects decoded claims, and proves separate clean global and exact-version npx paths."
    - "Per D-02 and D-03, the helper never reads or designates the Phase 4 local archive and never treats a verifier result as human approval or publication authority."
    - "Per D-06 and D-07, bounded output retains legal/support/native facts without cleartext configured origin or custody paths and requires the real Darwin ARM64 native re-export observation plus its explicit fallback limitation."
  prohibitions:
    - "HIGH: No old-local-artifact fallback, bootstrap profile, arbitrary package/version/registry/workflow input, skip-verification flag, rebuild, pack, repack, source checkout dist fallback, or fake registry success path."
    - "HIGH: No custom Sigstore, Fulcio, Rekor, DSSE signature, or certificate verifier; supported npm performs cryptographic verification before project policy inspects decoded claims."
    - "HIGH: No signature-only or publisher-exit success claim; missing/mismatched attestation, registry ambiguity, version collision, wrong source/run/attempt, or consumer failure remains an explicit failed/ambiguous result."
    - "HIGH: No inherited npm publication credential, private custody path, production origin/project reference, token, login URL, or unbounded npm/GitHub payload in output/evidence."
    - "No release/authentication/approval framework, new dependency, package.json script, automatic publish retry, unpublish, rerun, or rebuild behavior."
  artifacts:
    - path: "scripts/verify-npm-release.mjs"
      provides: "Fixed stable-candidate sealing, same-run prepublish validation, and public npm registry/provenance/consumer verification"
      exports: ["sealCiCandidateEvidence", "verifyCiCandidateForPublish", "inspectNpmProvenance", "verifyPublicNpmRelease"]
    - path: "tests/package/npm-release-verifier.test.ts"
      provides: "Focused behavioral coverage for candidate/context binding and provenance/public evidence policy"
  key_links:
    - from: "scripts/verify-npm-release.mjs seal-candidate"
      to: "producer/scanner/acceptance records"
      via: "independent digest and exact logical identity validation before writing the sealed CI evidence"
      pattern: "sealCiCandidateEvidence|cumpa.runtime-artifact-evidence/v1"
    - from: "scripts/verify-npm-release.mjs verify-candidate"
      to: "GitHub publish job context"
      via: "sealed evidence SHA-256 plus exact repository/workflow/source/run/attempt comparison"
      pattern: "verifyCiCandidateForPublish|GITHUB_RUN_ATTEMPT"
    - from: "scripts/verify-npm-release.mjs verify-public"
      to: "public npm registry and npm audit signatures"
      via: "exact tarball download, npm cryptographic verification, decoded claim checks, and isolated consumer commands"
      pattern: "verifyPublicNpmRelease|npm audit signatures"
---

<objective>
Create one fixed-purpose Node standard-library verifier for the fresh CI-built stable candidate, same-run prepublish gate, and actual public npm evidence.

Purpose: Make the irreversible workflow depend on exact byte/source/run bindings and make PKG-01, PKG-02, and provenance success observable without creating a release, credential, or approval subsystem.
Output: `scripts/verify-npm-release.mjs` and one focused behavioral test file.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-CONTEXT.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-RESEARCH.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-PATTERNS.md
@.planning/phases/04-exact-runtime-tarball/04-04-SUMMARY.md
@scripts/pack-runtime.mjs
@scripts/verify-production-artifacts.mjs
@tests/helpers/runtime-artifact.ts
@tests/package/agent-ready-export.test.ts
@tests/package/runtime-artifact-verifier.test.ts
@package.json
@docs/distribution-operations.md
</context>

<interfaces>
- The helper is fixed to `@shipwithai/cumpa@1.5.0`, registry `https://registry.npmjs.org/`, repository `Ship-With-AI/cumpa`, repository ID `1327753770`, workflow `.github/workflows/publish-npm.yml`, branch `main`, and GitHub-hosted producer `darwin/arm64`. These are policy constants, not caller-selected release inputs.
- CLI command `seal-candidate` accepts exactly `--archive`, `--producer-evidence`, `--scanner-report`, `--acceptance-report`, and new absolute `--output`; it obtains CI identity only from validated `GITHUB_REPOSITORY`, `GITHUB_REPOSITORY_ID`, `GITHUB_WORKFLOW_REF`, `GITHUB_SHA`, `GITHUB_RUN_ID`, `GITHUB_RUN_ATTEMPT`, `RUNNER_ENVIRONMENT`, `RUNNER_OS`, and `RUNNER_ARCH`.
- The sealed file stays `kind: "cumpa.runtime-artifact-evidence/v1"`, `status: "verified"`, `purpose: "candidate"`, preserves the producer core, and adds exact `scanner`, `acceptance`, and `ci` members. `ci` has only `repository`, `repositoryId`, `workflowPath`, `workflowRef`, `ref`, `sourceSha`, `runId`, `runAttempt`, `runnerEnvironment`, `producerPlatform`, and `producerArch`.
- `scanner` and `acceptance` each retain their source `kind`, `status`, bounded logical observations/limitations, and an independently calculated `sha256` of the input report. Their archive/package/support/legal/native/check facts must agree with each other and the producer core; `ci.sourceSha === source.head`.
- CLI command `verify-candidate` accepts exactly `--archive`, `--evidence`, `--expected-evidence-sha256`, `--artifact-id`, and `--artifact-digest`. It uses Node standard-library stable-file hashing and strict sealed-record checks, not an import/invocation of `verify-production-artifacts.mjs`: that full scanner requires the configured origin and Vue/parser dependencies absent from the publish job. The prior scanner/acceptance results are trusted only through the exact sealed evidence digest and matching archive identities. Compare live repository/workflow/source/run/attempt/hosted context with sealed `ci`; validate the recorded Darwin ARM64 producer separately from the Ubuntu publisher OS. Emit bounded `cumpa.ci-candidate-prepublish/v1` JSON. Transport IDs/digests remain external observations and never mutate the seal.
- CLI command `verify-public` accepts exactly `--evidence`, `--expected-evidence-sha256`, `--artifact-id`, `--artifact-digest`, and new absolute `--output`. It writes `cumpa.npm-release-verification/v1`; approval and publication authorization references are added by the operating plan to the final `05-RELEASE-EVIDENCE.json`, not invented by this helper.
- `inspectNpmProvenance` is policy inspection after `npm audit signatures --json --include-attestations` succeeds. It requires one matching verified package attestation with in-toto Statement v1, one exact PURL subject, the registry archive SHA-512 hex, SLSA GitHub workflow build type, exact repository/workflow/ref, resolved `gitCommit`, `workflow_dispatch`, repository/owner IDs when emitted, GitHub-hosted runner identity, and run/attempt invocation URL. It records expected/observed/pass rows and never claims a SLSA level or exhaustive inputs.
- `verifyPublicNpmRelease` requires exact supported npm `11.19.1` and the `npx` from that same isolated installation, then uses private mode-0700 temporary roots and separate credential-free HOME/userconfig/cache/prefix locations for audit, global install, and npx. It fetches the exact-version packument and tarball with bounded redirects/bytes, compares `dist.shasum` and `dist.integrity`, runs npm cryptographic audit, invokes the generated global `cumpa --version`, then separately runs literal `npx --yes @shipwithai/cumpa@1.5.0 --version` from an empty cache/no-global prefix and requires exact stdout `1.5.0`.
- Before `npm audit signatures`, create a fresh private audit consumer manifest and install exact `@shipwithai/cumpa@1.5.0` from the fixed public registry with `--ignore-scripts --no-audit --save-exact`. Require both its actual installed node and lockfile entry, exact name/version, and lock integrity matching the approved archive. An empty tree, lockfile-only tree or zero-attestation audit cannot prove the target. Run `npm audit signatures --json --include-attestations` inside this populated root, and inspect the verified Cumpa bundle returned by that same audit, not a separately fetched unauthenticated statement.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the strict fixed-purpose release verifier through RED-GREEN-REFACTOR</name>
  <files>scripts/verify-npm-release.mjs, tests/package/npm-release-verifier.test.ts</files>
  <read_first>
    - `05-CONTEXT.md` D-01, D-02, D-03, D-06, D-07, D-08, and D-09
    - `05-RESEARCH.md` selected-direction addendum, `Critical Provenance Semantics Gate`, `Registry, Attestation, and Consumer Evidence Design`, and `Don't Hand-Roll`
    - `05-PATTERNS.md` verifier, evidence, bounded-redaction, and temporary-custody analogs
    - `scripts/verify-production-artifacts.mjs` argument parsing, bounded evidence parsing, stable-file hashing, verifier output, extraction cleanup, and redacted top-level error pattern
    - `tests/helpers/runtime-artifact.ts` safe custody/rehash and protected npm environment patterns
    - `tests/package/agent-ready-export.test.ts` actual acceptance report schema and native/support/cleanup observations
    - `tests/package/runtime-artifact-verifier.test.ts` subprocess boundary and mutation/rebinding test conventions
    - npm trusted publishing, provenance, and `npm audit signatures --json --include-attestations` official documentation cited by research
  </read_first>
  <behavior>
    - "RED 1: `sealCiCandidateEvidence` accepts only a stable candidate whose producer, scanner, acceptance, archive identities, source head, configured-support fingerprint, complete passed checks, legal/runtime package facts, Darwin ARM64 native re-export, and actual GitHub context all agree; output contains the exact bounded `ci` keys and report digests."
    - "RED 2: sealing rejects wrong repository/repository ID/workflow/ref/source/run/attempt/runner/platform/arch, malformed or extra CLI options, failed/missing scanner or acceptance checks, crossed hashes/package identities, mutable input during read, cleartext private values, and any attempt to use the historical Phase 4 identity as fallback authority."
    - "RED 3: `verifyCiCandidateForPublish` accepts the unchanged seal and archive only in the exact same GitHub run/attempt/source/workflow and with well-formed upload artifact ID/digest; every mismatch fails before any publish command can run."
    - "RED 4: `inspectNpmProvenance` accepts npm-cryptographically-verified DSSE only when subject SHA-512 and every required source/workflow/run/attempt claim matches sealed CI evidence; missing bundle, signature-only data, ambiguous duplicate, source mismatch, wrong event/runner, malformed base64/JSON, or failed npm audit rejects success."
    - "RED 5: public result policy requires downloaded byte length/SHA-256/SHA-1/SHA-512 and exact metadata to match the new CI candidate, plus distinct successful global and npx proofs with `1.5.0`; metadata-only, shared-cache, local-tarball, checkout-dist, or partial-consumer observations fail."
    - "RED 6: bounded failure/result serialization omits auth configuration, environment values, absolute custody/temp paths, raw npm payloads, and live login/origin values while preserving explicit failed/ambiguous status and safe field comparisons."
  </behavior>
  <action>Write the focused tests first and observe the intended failures. Implement the four named exports and thin three-command CLI using only Node 24 standard-library APIs, installed npm/npx, and current project test tooling. Reuse the existing stable-file identity, bounded JSON, cleanup and redaction algorithms/patterns without importing or invoking the origin/dependency-requiring full scanner in the publish path. Keep private mode-0700 custody, stripped npm environments, redirect/size caps and exact-key parsing. Do not add a generalized runner/client/schema package or alternate registry mode.

Per D-01/D-08, treat sealed candidate evidence as a new CI build record: preserve producer core, add validated scanner/acceptance/CI facts, write once with `wx`/atomic sibling-link semantics, calculate its seal after close, and never inject later upload ID or approval. `verify-candidate` must independently rehash the archive/evidence and compare sealed facts to its live workflow context before returning success. Per D-02, do not read Phase 4 custody/evidence or accept its digest as a release option.

Per D-09, populate the audit root before cryptographic verification: write a temporary `package.json` with `private: true`, perform exact scripts-disabled public installation to create both node_modules and package-lock, validate target identity/lock integrity, and run the exact npm audit in that root. Reject a missing target, empty audit or absent verified target bundle even if the CLI exits successfully. Decode and inspect the Cumpa bundle returned by that same successful audit. Global and npx roots remain separate normal public-consumption proofs. Strip static npm authentication/configuration, capture only bounded observations, and never publish, retry, rebuild, repack or grant approval from this helper.</action>
  <verify>
    <automated>npx vitest run --no-file-parallelism tests/package/npm-release-verifier.test.ts</automated>
  </verify>
  <acceptance_criteria>The behavior suite proves strict new-candidate sealing, same-run prepublish rejection, npm-verified attestation claim matching, registry-byte equality policy, separate clean global/npx policy, bounded output, and fail-closed ambiguity without testing mere wiring or wording.</acceptance_criteria>
  <done>One small stable-only helper can be invoked by the fixed workflow and actual publication operations; it creates no publication, authentication, approval, or retry framework.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `scripts/verify-npm-release.mjs`
  - exports: `sealCiCandidateEvidence`, `verifyCiCandidateForPublish`, `inspectNpmProvenance`, `verifyPublicNpmRelease`
  - commands: `seal-candidate`, `verify-candidate`, `verify-public`
  - flags: `--archive`, `--producer-evidence`, `--scanner-report`, `--acceptance-report`, `--evidence`, `--expected-evidence-sha256`, `--artifact-id`, `--artifact-digest`, `--output`
  - sealed CI fields: `ci.repository`, `ci.repositoryId`, `ci.workflowPath`, `ci.workflowRef`, `ci.ref`, `ci.sourceSha`, `ci.runId`, `ci.runAttempt`, `ci.runnerEnvironment`, `ci.producerPlatform`, `ci.producerArch`
- `tests/package/npm-release-verifier.test.ts`
  - behavioral cases for seal/context/evidence identity, npm-verified provenance claims, registry identities, isolated consumers, redaction, and explicit ambiguity
</artifacts_this_phase_produces>

<threat_model>
## ASVS Level 1 / HIGH Threat Register

| Threat ID | STRIDE | Boundary | Risk | Disposition | Specific mitigation and proof |
|---|---|---|---|---|---|
| T-05-02-01 | Tampering | Producer/scanner/acceptance files -> sealed candidate | HIGH | mitigate | Stable-file reads, independent SHA-256, exact identity joins, write-once output, and mutation/rebinding tests. |
| T-05-02-02 | Spoofing | Workflow environment -> candidate/publish decision | HIGH | mitigate | Exact repository ID/path/ref/SHA/run/attempt/hosted-runner/platform/arch allowlist; mismatch tests fail before publish. |
| T-05-02-03 | Tampering | Artifact transfer -> publish job | HIGH | mitigate | Validate numeric native artifact ID and transport digest, then independently rehash sealed evidence and inner tgz; transport metadata never replaces inner identity. |
| T-05-02-04 | Spoofing | npm metadata/attestation -> provenance claim | HIGH | mitigate | Current npm performs cryptographic verification; project code requires exact subject/source/workflow/run/attempt and tests missing/mismatched bundles. |
| T-05-02-05 | Information disclosure | npm/GitHub/process output -> durable evidence/log | HIGH | mitigate | Bounded allowlisted output, stripped auth/config environment, path/origin/token redaction tests, no raw provider payload persistence. |
| T-05-02-06 | Elevation of privilege | Verification helper -> publication/approval | HIGH | mitigate | Helper has no publish/auth/approval command or credential handling and returns only evidence; human/native gates remain external. |
| T-05-02-07 | Denial of service | Registry redirect/body/JSON -> local verifier | MEDIUM | mitigate | HTTPS host allowlist, redirect and byte limits, exact shapes, timeouts, protected temp cleanup, explicit ambiguity. |
| T-05-02-SC | Tampering | New package/dependency supply chain | HIGH | mitigate | No dependency/install change is introduced; use Node standard library, existing Vitest, and the supported npm CLI rather than adding a package. |
</threat_model>

<verification>
- Focused Vitest file passes in RED-GREEN-REFACTOR order.
- CLI option contracts reject unknown/duplicate/missing inputs and never offer caller-selected release identity or bypass flags.
- No actual registry publish, workflow dispatch, credential/configuration mutation, or Phase 4 file mutation occurs in this plan.
</verification>

<success_criteria>
- Fresh candidate producer/scanner/acceptance/CI facts can be sealed once and revalidated unchanged in the same run/attempt.
- Public success requires exact registry bytes, npm-verified and policy-matched attestation, and separate clean global/npx observations.
- Failures remain honest, bounded, and non-retrying; no private or credential-bearing data is durable.
</success_criteria>

<output>
Create `.planning/phases/05-bootstrap-trusted-stable-publication/05-02-SUMMARY.md` when done.
</output>
