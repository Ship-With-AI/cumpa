---
phase: 04-exact-runtime-tarball
plan: 04
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - .planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json
  - .planning/phases/04-exact-runtime-tarball/04-ARTIFACT-APPROVAL.md
autonomous: false
requirements: [PKG-03, PKG-04, PKG-05, REL-03]
must_haves:
  truths:
    - "One final configured `@shipwithai/cumpa@1.5.0` candidate is produced once, and scanner, installation, browser/Finish acceptance, custody, evidence, and approval use those unchanged bytes."
    - "The candidate itself is one durable outside-checkout read-only archive identified by basename, SHA-256, byte length, npm SHA-1, and npm SHA-512 SRI; no additional copy is required."
    - "One bounded evidence JSON binds local source/build/toolchain/legal/native/npm inventory, verifier, isolated installation, dependency tree, browser/review/support/V3 Finish observations, read-only custody, and honest limitations."
    - "A human explicitly approves the actual rehashed archive SHA-256 and byte length and acknowledges the displayed limitations; generic, prospective, stale, proxy, or agent-authored approval fails."
    - "Developer-configured absent checks remain absent, while the final candidate remains configured and its origin is represented durably only by boolean/fingerprint."
    - "The approval record hands Phase 5 the same local bytes without claiming upload, publication, registry availability/equality, provenance, public-source alignment, or transport authorization."
  prohibitions:
    - "HIGH: Do not approve if any producer, scanner, install, dependency, browser/Finish, support, exact-patch, source-control, legal/notice, native, cleanup, archive-hash, length, or read-only observation is missing, failed, stale, or mismatched."
    - "HIGH: Do not patch, repair, append to, repack, transform, or overwrite a candidate. Reject it and begin a new unique one-build/one-pack cycle after correcting the owning source/tool cause."
    - "HIGH: Do not display, log, put on argv, or persist the canonical support origin; exhaust existing protected local inputs first and store only configured boolean/fingerprint."
    - "Do not require or fabricate candidate-plus-two-copy custody, inode/device diversity, non-writable custody directories, remote custody, signing, or a second manifest/evidence store."
    - "A byte-identical backup is optional operator policy only: Phase 4 does not create or require it, and its absence cannot block verification or approval."
    - "Do not publish/stage/upload, choose or authorize transport, push source, assert public alignment, invoke trusted publishing, claim provenance, deploy support infrastructure, or re-download registry bytes."
    - "Do not claim native support beyond observed Darwin ARM64 or claim bounded signature scanning proves every unknown secret absent."
  artifacts:
    - path: ".planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json"
      provides: "Single bounded machine-readable candidate, verification, acceptance, one-archive custody, approval, and limitation record"
      contains: "cumpa.runtime-artifact-evidence/v1"
    - path: ".planning/phases/04-exact-runtime-tarball/04-ARTIFACT-APPROVAL.md"
      provides: "Attributable actual-SHA-256/byte-length/limitations approval and Phase 5 unchanged-byte handoff"
      contains: "EXACT RUNTIME ARTIFACT APPROVED"
  key_links:
    - from: "scripts/pack-runtime.mjs"
      to: ".planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json"
      via: "one environment-configured candidate build/pack records basename and algorithm-specific identity without a private path"
      pattern: "candidate|npmIntegritySha512"
    - from: "scripts/verify-production-artifacts.mjs and installed acceptance"
      to: "final evidence JSON"
      via: "caller-held custody directory plus basename resolves the same archive; bounded results preserve immutable core"
      pattern: "archive|acceptance"
    - from: "accepted candidate"
      to: "durable custody"
      via: "the candidate file itself becomes read-only and is independently rehashed; optional backup is not a gate"
      pattern: "readOnly"
    - from: ".planning/phases/04-exact-runtime-tarball/04-ARTIFACT-APPROVAL.md"
      to: "Phase 5"
      via: "actual digest/length/basename and acknowledged limitations prohibit substitution or implied remote claims"
      pattern: "Phase 5|SHA-256"
---

<objective>
Execute one final candidate cycle, inspect and exercise the exact archive, make that archive durable and read-only outside the checkout, obtain explicit digest-and-limitations approval, and hand the unchanged bytes to Phase 5 without remote or publication work.

Purpose: Convert the package contract and acceptance machinery into one auditable local artifact whose identity and limitations are understood before any separately authorized transfer.
Output: One accepted read-only `.tgz` outside the checkout, one bounded evidence JSON, and one exact-digest human approval/Phase 5 handoff record.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-exact-runtime-tarball/04-RESEARCH.md
@.planning/phases/04-exact-runtime-tarball/04-PATTERNS.md
@.planning/phases/04-exact-runtime-tarball/04-01-SUMMARY.md
@.planning/phases/04-exact-runtime-tarball/04-02-SUMMARY.md
@.planning/phases/04-exact-runtime-tarball/04-03-SUMMARY.md
@.planning/phases/03-distribution-contract-legal-boundary/03-03-SUMMARY.md
@.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md
@.planning/phases/03-distribution-contract-legal-boundary/03-RIGHTS-REVIEW.md
@.planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md
@docs/distribution-operations.md
@docs/support-service-operations.md
@package.json
@package-lock.json
@README.md
@LICENSE
@THIRD_PARTY_NOTICES.md
@scripts/pack-runtime.mjs
@scripts/verify-production-artifacts.mjs
@tests/e2e/package-assets.spec.ts
@tests/e2e/agent-ready-export.spec.ts
@tests/package/agent-ready-export.test.ts
</context>

<interfaces>
- Final producer: `CUMPA_RELEASE_SUPPORT_SERVICE_URL="$CUMPA_RELEASE_SUPPORT_SERVICE_URL" node scripts/pack-runtime.mjs --purpose candidate --custody-dir "$CUMPA_RUNTIME_CUSTODY_DIR" --evidence .planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json`. No origin-valued argv exists.
- Default durable custody leaf: `~/.local/share/cumpa/release-candidates/1.5.0/<unique-run-id>`; `CUMPA_RUNTIME_CUSTODY_DIR` may explicitly name a different non-existing outside-checkout leaf. This directory remains caller-held and is never written to evidence.
- Derive the archive input from `CUMPA_RUNTIME_CUSTODY_DIR` plus `evidence.archive.basename`; require realpath containment. Export `CUMPA_RUNTIME_ARCHIVE_BASENAME`, `CUMPA_RUNTIME_ARCHIVE_SHA256`, `CUMPA_RUNTIME_EVIDENCE`, `CUMPA_RUNTIME_ACCEPTANCE_REPORT`, and the parent-only support environment exactly as Plan 04-03 defines.
- Final evidence remains `cumpa.runtime-artifact-evidence/v1`: immutable `purpose: "candidate"` and producer core; stage `candidate` -> `verified` after scanner/installed acceptance/read-only custody -> `accepted-local` only after digest-bound human approval. Every stage remains re-verifiable by Plan 04-02.
- Durable archive identity is only `archive.{basename,byteLength,sha256,npmShasumSha1,npmIntegritySha512}`. SHA-256 is lowercase hex, npm shasum is SHA-1 lowercase hex, and integrity is `sha512-` plus base64; each is independently recomputed/compared with only its matching field.
- Custody record: `custody.{label:"accepted-candidate",archiveBasename,sha256,byteLength,readOnly:true}`. `backup` is optional and omitted by default; absence is valid and never blocks. No private absolute path, inode/device assertion, or directory-non-writable requirement is durable policy.
- Approval record: `approval.{status:"approved",sha256,byteLength,limitationsAcknowledged:true,approvedAt,approvedBy}` plus matching `04-ARTIFACT-APPROVAL.md`. The response must repeat actual displayed values; a prospective placeholder cannot advance status.
- Phase 5 receives basename, SHA-256, length, npm SHA-1/SHA-512 SRI, evidence digest, `accepted-candidate` custody label, local source/build/legal/native facts, and limitations. It must locate and rehash the exact local archive before any separately authorized action.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Produce and verify one final configured candidate</name>
  <files>.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json</files>
  <read_first>
    - `.planning/phases/04-exact-runtime-tarball/04-01-SUMMARY.md` producer/version contract
    - `.planning/phases/04-exact-runtime-tarball/04-02-SUMMARY.md` supplied verifier and caller contract
    - `.planning/phases/04-exact-runtime-tarball/04-03-SUMMARY.md` dedicated acceptance contract
    - `.planning/phases/03-distribution-contract-legal-boundary/03-RIGHTS-REVIEW.md` notice/native/final-byte/scan limitations
    - `.planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md` exact MIT text approval
    - `.planning/phases/04-exact-runtime-tarball/04-RESEARCH.md` sections `Legal and Third-Party Notice Reconciliation`, `Exact Artifact Evidence`, `Security Domain`, and `Verification Strategy`
    - `docs/distribution-operations.md`, `package.json`, `package-lock.json`, `LICENSE`, and `THIRD_PARTY_NOTICES.md`
    - `scripts/pack-runtime.mjs` and `scripts/verify-production-artifacts.mjs`
  </read_first>
  <action>Begin from completed 04-01/02/03 summaries and clean committed source/index with no concurrent producer. Record actual local HEAD/tree as local build facts only; do not compare them to a public ref or claim public-source alignment. Recompute package/lock/license/notices digests and verify the approved LICENSE text. Reconcile the final lockfile, emitted browser/native contents, and conveyed licenses against `THIRD_PARTY_NOTICES.md`; unresolved copied-component/license/notice material, changed approved legal text, unknown native source-to-binary pair, or any HIGH finding blocks artifact approval.

Recover the already-authorized canonical support origin only from the current protected executor environment or documented existing protected local configuration. Validate it in memory without output/history/evidence cleartext. If unavailable, use a dynamic human input gate rather than inventing or contacting a remote provider. Store only `support.configured: true` and fingerprint; do not alter the configured-absent development checks established in 04-02.

Create one new outside-checkout custody leaf and invoke the producer exactly once with `--purpose candidate`. Retain the custody directory only in executor state, derive the archive from its evidence basename, and independently recompute length, SHA-256 hex, SHA-1 hex, and SHA-512 SRI. Compare each only with its matching producer/npm field. Run the scanner once with its three flags and environment origin, capture bounded JSON, require configured candidate purpose/status and complete package/legal/dist/web/native/support checks, then rehash all identities.

Atomically incorporate verifier output and actual legal reconciliation into the evidence without changing immutable core or storing a private path; keep status `candidate` until Task 2. Failure rejects this identity and requires a new unique cycle after correcting the owning earlier-plan source/tool cause. Never patch, reuse, overwrite, upload, publish, or make provenance/alignment claims about rejected or passing bytes.</action>
  <verify>
    <automated>CUMPA_RELEASE_SUPPORT_SERVICE_URL="$CUMPA_RELEASE_SUPPORT_SERVICE_URL" node scripts/verify-production-artifacts.mjs --archive "$CUMPA_RUNTIME_CUSTODY_DIR/$CUMPA_RUNTIME_ARCHIVE_BASENAME" --expected-sha256 "$CUMPA_RUNTIME_ARCHIVE_SHA256" --evidence .planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json</automated>
  </verify>
  <acceptance_criteria>One configured build and one pack create one archive whose algorithm-specific identities, inventory, local source/build/legal/native facts, support fingerprint, and strict scanner result agree without private paths or remote claims.</acceptance_criteria>
  <done>A statically eligible exact candidate exists outside the checkout; it is neither behaviorally accepted nor human-approved yet.</done>
</task>

<task type="auto">
  <name>Task 2: Accept installed behavior and make the candidate the durable read-only archive</name>
  <files>.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json</files>
  <read_first>
    - `.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json` candidate/verifier identity from Task 1
    - `.planning/phases/04-exact-runtime-tarball/04-03-SUMMARY.md` acceptance configs, command, and report schema
    - `tests/e2e/package-assets.spec.ts` install/dependency/version/assets contract
    - `tests/e2e/agent-ready-export.spec.ts` review/support/V3 Finish/native/source-control contract
    - `tests/package/agent-ready-export.test.ts` bounded aggregator
    - `docs/distribution-operations.md` artifact and publication boundary
  </read_first>
  <action>Set the Plan 04-03 inputs from the caller-held custody directory and Task 1 evidence basename/digest, with a protected temporary acceptance report and parent-only canonical support environment. Rehash all archive identities before the run, capture source control, and run `npm run accept:runtime-artifact`. Require scanner, isolated scripts-disabled global install, exact runtime dependency versions, generated-bin version/help, browser graph, review/relaunch, configured-support unavailable plus `Not now`, canonical V2 and grounded V3 exact-patch Finish, cleanup, source-control, and real native/fallback observations. Any mismatch rejects this candidate.

Validate the bounded report against producer/verifier identity. Atomically incorporate only logical/digest/count/status observations and limitations; remove temporary reports. Preserve immutable producer core and set status `verified`, not `accepted-local`.

Make the candidate archive file itself read-only (`0444`, subject to platform representation), without changing its containing directory policy. Reopen and recompute SHA-256, SHA-1, SHA-512 SRI, and length; require exact equality. Record the single `accepted-candidate` custody label/basename/digest/length/read-only observation, never its absolute path. Do not create backup copies by default; an optional byte-identical backup is outside the approval gate and absence is valid. Record limitations: local artifact only; Darwin ARM64 native observation/fallback elsewhere; bounded secret scan; no upload, publication, registry result, provenance, public-source alignment, or transport authorization.</action>
  <verify>
    <automated>CUMPA_RUNTIME_CUSTODY_DIR="$CUMPA_RUNTIME_CUSTODY_DIR" CUMPA_RUNTIME_ARCHIVE_BASENAME="$CUMPA_RUNTIME_ARCHIVE_BASENAME" CUMPA_RUNTIME_ARCHIVE_SHA256="$CUMPA_RUNTIME_ARCHIVE_SHA256" CUMPA_RUNTIME_EVIDENCE=.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json CUMPA_RELEASE_SUPPORT_SERVICE_URL="$CUMPA_RELEASE_SUPPORT_SERVICE_URL" CUMPA_RUNTIME_ACCEPTANCE_REPORT="$CUMPA_RUNTIME_ACCEPTANCE_REPORT" npm run accept:runtime-artifact</automated>
  </verify>
  <acceptance_criteria>The unchanged candidate passes dedicated installed acceptance and becomes the one durable read-only outside-checkout archive; no backup is required and evidence remains re-verifiable.</acceptance_criteria>
  <done>Behaviorally verified local bytes and bounded evidence are ready for actual-digest human approval.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve the actual digest and record Phase 5 handoff</name>
  <files>.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json, .planning/phases/04-exact-runtime-tarball/04-ARTIFACT-APPROVAL.md</files>
  <read_first>
    - `.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json` complete verified record
    - `.planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md` exact MIT approval
    - `.planning/REQUIREMENTS.md` REL-03 and Phase 5 boundary
    - `docs/distribution-operations.md` archive/source/support/publication separation
  </read_first>
  <what-built>One verified configured `@shipwithai/cumpa@1.5.0` archive, held durably and read-only outside the checkout, with basename/length/SHA-256/npm SHA-1/SHA-512 SRI and bounded source/build/legal/native/scanner/install/dependency/browser/support/V3 Finish evidence.</what-built>
  <how-to-verify>
    1. Review the displayed actual archive basename, SHA-256, byte length, npm SHA-1/SHA-512 SRI, package identity, local source/tree, legal digests, inventory/dependency digests, and support configured fingerprint.
    2. Confirm scanner and installed results cover runtime/legal-only bytes, browser/worker/font graph, generated bin, exact runtime dependencies, review/relaunch, support unavailable dismissal, V2/V3 Finish, source-control/cleanup, and real Darwin ARM64 native or explicit fallback.
    3. Confirm the accepted candidate itself was rehashed after becoming read-only and matches the displayed actual SHA-256 and byte length. No additional custody copy is required; optional backup absence is not a failure.
    4. Acknowledge limitations: local bytes only; bounded secret scan; native observation limited to Darwin ARM64 with fallback elsewhere; no upload, staging, publication, registry availability/equality, provenance, public-source alignment, source push, transport authorization, support deployment, or retention/permission action occurred.
    5. Confirm Phase 5 must independently locate and rehash this exact archive before any separately authorized action and must never rebuild the approved release.
  </how-to-verify>
  <action>Display actual values from the verified evidence and an immediate independent rehash. Require the exact response format below with those values and explicit limitations acknowledgement. Generic approval, placeholders, stale/mismatched values, or agent-authored assent does not qualify. A rejection or byte-affecting request rejects this candidate and requires a new cycle.

After a valid attributable response, rehash all algorithms/length once more, verify read-only mode, atomically set evidence status `accepted-local`, and add `approval` with the exact SHA-256/length, `limitationsAcknowledged: true`, actual approver identity, and timestamp. Create `04-ARTIFACT-APPROVAL.md` with the verbatim statement, archive basename/algorithm identities, evidence digest, `accepted-candidate` label, local package/source/legal/native facts, limitations, and Phase 5 obligations. Do not include private paths/origin or mutate archive/evidence afterward.</action>
  <verify>
    <automated>CUMPA_RUNTIME_EVIDENCE=.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json CUMPA_RUNTIME_CUSTODY_DIR="$CUMPA_RUNTIME_CUSTODY_DIR" node --input-type=module -e 'import assert from "node:assert/strict"; import {createHash} from "node:crypto"; import {readFile,stat} from "node:fs/promises"; import {basename,resolve} from "node:path"; const e=JSON.parse(await readFile(process.env.CUMPA_RUNTIME_EVIDENCE,"utf8")); const p=resolve(process.env.CUMPA_RUNTIME_CUSTODY_DIR,e.archive.basename); assert.equal(basename(p),e.archive.basename); const b=await readFile(p); const s=await stat(p); assert.equal(b.byteLength,e.archive.byteLength); assert.equal(createHash("sha256").update(b).digest("hex"),e.archive.sha256); assert.equal(createHash("sha1").update(b).digest("hex"),e.archive.npmShasumSha1); assert.equal("sha512-"+createHash("sha512").update(b).digest("base64"),e.archive.npmIntegritySha512); assert.equal(s.mode&amp;0o222,0); assert.equal(e.status,"accepted-local"); assert.deepEqual(e.custody,{label:"accepted-candidate",archiveBasename:e.archive.basename,sha256:e.archive.sha256,byteLength:e.archive.byteLength,readOnly:true}); assert.equal(e.approval.sha256,e.archive.sha256); assert.equal(e.approval.byteLength,e.archive.byteLength); assert.equal(e.approval.limitationsAcknowledged,true);'</automated>
    <human-check>Approve only by repeating the displayed actual SHA-256 and byte length and acknowledging the displayed limitations.</human-check>
  </verify>
  <acceptance_criteria>An attributable human binds approval to the actual rehashed read-only archive SHA-256/length and limitations; evidence and handoff name the same unchanged local bytes without remote claims.</acceptance_criteria>
  <done>The exact local bytes—not a rebuild, filename, future package, or registry result—are accepted and designated for Phase 5.</done>
  <resume-signal>Type exactly `EXACT RUNTIME ARTIFACT APPROVED: @shipwithai/cumpa@1.5.0 sha256 &lt;displayed-actual-64-hex&gt; bytes &lt;displayed-actual-integer&gt;; limitations acknowledged` or identify the blocker.</resume-signal>
</task>

</tasks>

<threat_model>
## Enforcement
ASVS Level 1 applies. Every HIGH finding blocks. Automation cannot waive actual-digest human approval.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Protected local support configuration -> final build | Canonical origin enters the configured build through environment only; durable records receive boolean/fingerprint. |
| Clean local source -> candidate archive | One build and one pack create the only bytes eligible for this cycle; source facts do not assert public alignment. |
| Candidate -> scanner/npm/browser/agent | Every stage derives one transient path from caller custody plus basename and rehashes unchanged bytes without repack. |
| Verified candidate -> durable custody | The candidate file itself becomes read-only outside checkout; backup is optional and not a gate. |
| Automated evidence -> human approval | Assent must repeat actual rehashed SHA-256/length and acknowledge limitations. |
| Phase 4 -> Phase 5 | Local bytes cross by identity only; upload/publication/provenance/alignment/transport remain unperformed and unauthorized here. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-04-SO | Information Disclosure | support origin | HIGH | mitigate | Protected local recovery, environment-only build/scanner input, redacted output, fingerprint-only durable state. |
| T-04-04-AS | Spoofing / Tampering | candidate substitution | HIGH | mitigate | Basename containment plus independent algorithm-specific length/hash checks before/after every stage and exact human statement. |
| T-04-04-RP | Tampering | repaired/repacked candidate | HIGH | mitigate | One unique cycle; failure/change rejects identity and requires a new cycle; no overwrite or approval reuse. |
| T-04-04-LG | Information Disclosure / Repudiation | legal/notice completeness | HIGH | mitigate | Exact legal digests and final lock/emitted-material reconciliation block unresolved grants/notices. |
| T-04-04-CU | Tampering / Denial of Service | local custody | HIGH | mitigate | One durable outside-checkout archive is made read-only and rehashed; optional backup absence never blocks. |
| T-04-04-AP | Spoofing / Repudiation | approval | HIGH | mitigate | Attributable response repeats actual SHA-256/length and limitations acknowledgement after review; reject placeholders/generic assent. |
| T-04-04-PV | Repudiation | false remote claim | HIGH | mitigate | Evidence/approval explicitly disclaim upload, publication, registry, provenance, public alignment, and transport. |
| T-04-04-NT | Repudiation | native overclaim | MEDIUM | mitigate | Record actual Darwin ARM64 binary/observation and explicit fallback elsewhere without a matrix promise. |
| T-04-04-ED | Information Disclosure | durable evidence | HIGH | mitigate | Bounded JSON/approval with basename/logical label/digests only; no origin, paths, credentials, fixtures, dependency tree, or logs. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- Outside checkout: one exact configured `@shipwithai/cumpa@1.5.0` `.tgz` candidate made durable and read-only; a backup is optional and not created or required by this plan.
- Final producer uses `--purpose candidate`, `--custody-dir`, and `--evidence`; `CUMPA_RELEASE_SUPPORT_SERVICE_URL` is environment-only. Acceptance uses `CUMPA_RUNTIME_CUSTODY_DIR`, `CUMPA_RUNTIME_ARCHIVE_BASENAME`, `CUMPA_RUNTIME_ARCHIVE_SHA256`, `CUMPA_RUNTIME_EVIDENCE`, `CUMPA_RUNTIME_ACCEPTANCE_REPORT`, and parent-only support input.
- `.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json` — `cumpa.runtime-artifact-evidence/v1` preserves producer `kind/status/purpose/package/archive/source/build/contents/legal/native/support`, adds bounded `verifier`, `acceptance`, `custody.{label,archiveBasename,sha256,byteLength,readOnly}`, `limitations`, and `approval.{status,sha256,byteLength,limitationsAcknowledged,approvedAt,approvedBy}`, and stores no private path/origin.
- `.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-APPROVAL.md` — attributable actual-SHA-256/byte-length/limitations approval and unchanged-byte Phase 5 handoff, not a duplicate manifest.
</artifacts_this_phase_produces>

<verification>
- Confirm one final configured candidate build and one real npm pack occurred; scanner and dedicated acceptance contain no build/pack.
- Recompute SHA-256, SHA-1, SHA-512 SRI, and byte length before scanner, before/after installed acceptance, after read-only mode, and immediately before/after human approval.
- Validate evidence contains bounded local source/package/lock/legal/toolchain/native/npm inventory, scanner, exact dependencies, browser/support/V2/V3 Finish, cleanup, custody, limitation, and approval facts without origin/private paths.
- Confirm candidate remains configured, development checks remain configured-absent, and each consumer derives the same transient archive from caller custody plus recorded basename.
- Confirm no backup is required and no remote/package-registry/workflow deployment/push/provenance/alignment/transport action or claim occurred.
</verification>

<success_criteria>
- PKG-03: the exact approved installed archive reports `1.5.0` and retains Node.js 24+/Git prerequisite guidance.
- PKG-04: the exact approved bytes and runtime dependency tree exercise every required compiled Node/browser/native/legal review, relaunch, support-dismissal, V2/V3 Finish behavior.
- PKG-05: exact inventory excludes source/maps/tests/planning/workflow/Git/credential/local-state/skill material, with bounded scan limitations disclosed.
- REL-03: one configured one-build/one-pack archive remains byte-identical through verifier, install, behavior, read-only custody, actual-digest approval, and Phase 5 handoff.
- Phase 4 makes no upload, publication, registry, provenance, public-source-alignment, or transport claim.
</success_criteria>

<output>Create `.planning/phases/04-exact-runtime-tarball/04-04-SUMMARY.md` when complete. Phase 5 begins by independently locating and rehashing the read-only accepted candidate against `04-ARTIFACT-APPROVAL.md`; it never rebuilds the approved release.</output>
