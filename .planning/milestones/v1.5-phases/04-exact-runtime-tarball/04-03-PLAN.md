---
phase: 04-exact-runtime-tarball
plan: 03
type: execute
wave: 3
depends_on: [04-02]
files_modified:
  - tests/e2e/package-assets.spec.ts
  - tests/e2e/agent-ready-export.spec.ts
  - tests/package/agent-ready-export.test.ts
  - vitest.config.ts
  - vitest.runtime-artifact.config.ts
  - playwright.config.ts
  - playwright.runtime-artifact.config.ts
  - package.json
autonomous: true
requirements: [PKG-03, PKG-04, PKG-05, REL-03]
must_haves:
  truths:
    - "Candidate-only acceptance requires explicit custody directory, archive basename, SHA-256, producer evidence, configured origin for the scanner, and report path; missing inputs fail rather than skip, while ordinary tests never discover these suites."
    - "npm installs the exact hash-bound archive into fresh global consumer state using an allowlisted child environment, scripts disabled, public-registry config, and no checkout dependency or credential inheritance."
    - "The installed package owns its exact runtime dependency tree; acceptance validates exact direct versions and retains only a sorted redacted dependency inventory count/digest."
    - "The npm-generated `cumpa` bin proves version/help, browser assets, review/relaunch, configured-support unavailable dismissal, V3 exact-patch Finish, source-control integrity, and real target-native/fallback behavior."
    - "A bounded acceptance report binds observations to the same archive basename, SHA-256, byte length, SHA-1, SHA-512 SRI, and immutable producer/verifier core consumed by final approval."
  prohibitions:
    - "HIGH: No acceptance test/config/script may build, pack, extract as an install shortcut, symlink checkout `node_modules`, or substitute checkout `dist` for npm's generated bin."
    - "HIGH: Construct npm child environments from an allowlist; never clone the ambient environment and delete only known secrets."
    - "Do not inherit npm auth/config/token variables, `NODE_AUTH_TOKEN`, `NPM_TOKEN`, `NODE_PATH`, checkout HOME/cache/config, workspace links, or lifecycle scripts."
    - "Do not expose the configured support origin to installed product processes or use a runtime override to mask a broken embedded build; supply it only to the scanner."
    - "Do not mock native/support success, exact-patch output, web assets, review state, Finish, or archive identity."
    - "Do not silently skip missing candidate inputs, let ordinary test discovery execute candidate acceptance, mutate the archive, retain private paths/origin cleartext, upload, or publish."
  artifacts:
    - path: "tests/e2e/package-assets.spec.ts"
      provides: "Supplied-archive isolated install/dependency-tree, version/legal/browser, and immutability acceptance."
    - path: "tests/e2e/agent-ready-export.spec.ts"
      provides: "Installed review/relaunch/support-unavailable/V3 exact-patch Finish/native behavior."
    - path: "tests/package/agent-ready-export.test.ts"
      provides: "Parent-owned configured scanner, candidate-only aggregator, and bounded acceptance report."
    - path: "vitest.runtime-artifact.config.ts"
      provides: "Dedicated aggregator discovery isolated from ordinary Vitest."
    - path: "playwright.runtime-artifact.config.ts"
      provides: "Dedicated two-spec candidate discovery isolated from ordinary Playwright."
  key_links:
    - from: "tests/package/agent-ready-export.test.ts"
      to: "playwright.runtime-artifact.config.ts"
      via: "one candidate-only invocation with custody/basename/digest/evidence/run/report inputs"
      pattern: "playwright.runtime-artifact.config"
    - from: "tests/package/agent-ready-export.test.ts"
      to: "scripts/verify-production-artifacts.mjs"
      via: "transient archive path plus SHA-256/evidence and parent-only support environment"
      pattern: "CUMPA_RELEASE_SUPPORT_SERVICE_URL"
    - from: "tests/e2e/package-assets.spec.ts"
      to: "npm-generated cumpa bin"
      via: "scripts-disabled global install and exact `npm ls` runtime dependency validation"
      pattern: "--global|--prefix|--omit=dev"
    - from: "tests/e2e/agent-ready-export.spec.ts"
      to: "ReviewExportV3Schema"
      via: "installed request reaches Finish with grounded exact-patch provenance and bytes"
      pattern: "ReviewExportV3Schema|createGroundedExactPatch"
    - from: "scenario report"
      to: "cumpa.runtime-artifact-acceptance/v1"
      via: "run ID and immutable archive/package/behavior equality"
      pattern: "CUMPA_AGENT_READY_EVIDENCE_RUN_ID"
---

<objective>
Prove the supplied immutable archive works as an actually installed package, including its browser review/relaunch and attached-agent Finish/export paths, with no checkout dependency bleed-through or acceptance-time repack.

Purpose: Move confidence from extracted build output to consumer installation semantics and the exact archive bytes Phase 4 may approve.
Output: Refactored package and Playwright acceptance plus a bounded exact-archive behavioral report and explicit no-build acceptance command.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-exact-runtime-tarball/04-RESEARCH.md
@.planning/phases/04-exact-runtime-tarball/04-PATTERNS.md
@.planning/phases/04-exact-runtime-tarball/04-01-SUMMARY.md
@.planning/phases/04-exact-runtime-tarball/04-02-SUMMARY.md
@package.json
@scripts/verify-production-artifacts.mjs
@tests/e2e/package-assets.spec.ts
@tests/e2e/agent-ready-export.spec.ts
@tests/package/agent-ready-export.test.ts
@tests/package/agent-ready-export-safety.test.ts
@tests/helpers/agent-ready-export-target.ts
<interfaces>
- Aggregator required environment: `CUMPA_RUNTIME_CUSTODY_DIR`, `CUMPA_RUNTIME_ARCHIVE_BASENAME`, `CUMPA_RUNTIME_ARCHIVE_SHA256`, `CUMPA_RUNTIME_EVIDENCE`, `CUMPA_RELEASE_SUPPORT_SERVICE_URL`, and `CUMPA_RUNTIME_ACCEPTANCE_REPORT`. Reject missing/malformed inputs and unsafe paths. Playwright specs require only the first four identity inputs; they neither receive the origin nor run the scanner.
- Derive the transient archive input as `resolve(CUMPA_RUNTIME_CUSTODY_DIR, CUMPA_RUNTIME_ARCHIVE_BASENAME)` and require basename/realpath containment plus equality with producer evidence. Durable outputs never retain custody/evidence/report/temp absolute paths.
- Scenario bridge: `CUMPA_AGENT_READY_EVIDENCE_REPORT` plus `CUMPA_AGENT_READY_EVIDENCE_RUN_ID`, owned and validated by the aggregator. Both are required during aggregate acceptance. Standalone spec smoke checks may omit both, but never emit a complete acceptance report; a partial bridge fails.
- Only the Vitest aggregator invokes the scanner, before Playwright, with `CUMPA_RELEASE_SUPPORT_SERVICE_URL`; it retains the bounded scanner result in parent memory. npm, installed CLI, browser-opener and Playwright children omit that key and all runtime-origin overrides. The aggregator combines its own scanner result with scenario reports; scenarios never supply or authenticate a scanner result.
- npm child environment is constructed from an allowlist: `PATH`; on Windows `SystemRoot`, `COMSPEC`, `PATHEXT`; temp keys `TMPDIR`/`TMP`/`TEMP`; locale `LANG`/`LC_ALL`; and `TZ` when set. Override `HOME`, `USERPROFILE`, npm cache, userconfig, globalconfig, prefix, and registry with fresh protected paths and `https://registry.npmjs.org/`. Do not inherit other `npm_*`/`NPM_*` keys, auth/token variables, `NODE_AUTH_TOKEN`, `NPM_TOKEN`, or `NODE_PATH`.
- Isolated install: `npm install --global --prefix <fresh-prefix> --ignore-scripts --no-audit --no-fund --registry https://registry.npmjs.org/ <absolute-tgz>`. Run `npm ls --global --prefix <fresh-prefix> --omit=dev --json`, reject missing/extraneous/invalid nodes, and compare direct runtime versions against `evidence.package.runtimeDependencies`, which the parent scanner has matched to the hash-bound archived manifest.
- Acceptance report `cumpa.runtime-artifact-acceptance/v1`: `status: "passed"`, `purpose: "candidate"`, algorithm-specific `archive.{basename,sha256,byteLength,npmShasumSha1,npmIntegritySha512}`, `package`, `install.{packageLabel,binLabel,manifestSha256,dependencyCount,dependencyInventorySha256}`, `scanner`, `browser`, `review`, `support`, `exactPatch`, `native`, `cleanup`, `sourceControl`, `checks`, and `limitations`. No raw dependency tree, private paths, origin, credentials, fixture contents, or stdout/stderr.
- Dedicated configs: `vitest.runtime-artifact.config.ts` includes only `tests/package/agent-ready-export.test.ts`; ordinary `vitest.config.ts` excludes it. `playwright.runtime-artifact.config.ts` includes only `tests/e2e/package-assets.spec.ts` and `tests/e2e/agent-ready-export.spec.ts`; ordinary `playwright.config.ts` ignores both.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Replace repacking package checks with isolated supplied-archive installation</name>
  <files>tests/e2e/package-assets.spec.ts, playwright.config.ts, playwright.runtime-artifact.config.ts</files>
  <read_first>
    - `playwright.config.ts` current discovery/runtime settings; create the dedicated config here before this task's smoke command uses it
    - `tests/e2e/package-assets.spec.ts` current build/pack/extract/install/workflow checks and stale unscoped installed path
    - `scripts/verify-production-artifacts.mjs` exact supplied-archive interface and structured result from Plan 04-02
    - `scripts/pack-runtime.mjs` evidence identity, lockfile facts, and npm inventory
    - `package.json` final name/version/engine/bin/files/dependencies contract
    - `src/cli/run.ts` version and ordinary-action boundaries from Plan 04-01
    - `.planning/phases/04-exact-runtime-tarball/04-RESEARCH.md` sections `Isolated Install Acceptance`, `Browser Asset Runtime Completeness`, `Exact Artifact Evidence`, `Security Domain`, and `Verification Strategy`
  </read_first>
  <behavior>
    - Test 1: missing/malformed common identity inputs or path containment fails at fixture setup; archive/evidence mismatch fails before npm. The spec receives no origin and never invokes the scanner.
    - Test 2: one real scripts-disabled global install with the allowlisted child environment creates `@shipwithai/cumpa@1.5.0` and npm's generated bin without checkout resolution or lifecycle effects.
    - Test 3: `npm ls --global --prefix <prefix> --omit=dev --json` has no problems and direct runtime nodes exactly match `evidence.package.runtimeDependencies`; retain only sorted redacted inventory count/digest.
    - Test 4: generated `cumpa --version` prints `1.5.0` and `--help` works from a non-repository directory without browser launch, checkout modules, or support-origin process environment.
    - Test 5: installed children inherit no configured-origin override; installed page and recursive JS/CSS/font/Monaco worker/codicon requests succeed.
    - Test 6: archive SHA-256, SHA-1, SHA-512 SRI, and byte length are unchanged after install/checks, and install scripts never run.
  </behavior>
  <action>Start with RED coverage for input, hash, environment, install, dependency, and installed asset boundaries. Create `playwright.runtime-artifact.config.ts` matching only the two candidate specs and make ordinary `playwright.config.ts` ignore both. Preserve existing runtime settings. Delete this suite's build/pack/direct extraction/checkout dependency-link/origin-string/workflow-text checks; Plan 04-02 owns producer/verifier behavior, and Task 3 owns configured scanner invocation.

Validate the four common identity inputs and optional paired scenario bridge, derive the archive path from custody directory plus safe basename, and compare immutable identity with evidence. Do not request the origin, invoke the scanner, or fabricate a scanner result inside either Playwright spec.

Construct npm's environment from the bounded system allowlist with fresh protected HOME/cache/config/prefix and explicit public registry. Install the archive with scripts disabled and resolve the scoped package/generated executable. Run `npm ls --global --prefix <prefix> --omit=dev --json` under that environment; reject problems and direct-version mismatches against `evidence.package.runtimeDependencies`. Sort normalized name/version observations to retain only dependency count/SHA-256. Checkout test libraries drive the harness only, never installed product resolution.

Invoke npm's generated bin normally for version/help and installed browser delivery. Assert consumer-visible identity/engine/bin/legal/runtime inventory and real recursive page assets. Rehash all three archive algorithms/length after checks, clean only temporary consumer state, and leave candidate/evidence untouched. After GREEN, refactor and rerun the boundary cases.</action>
  <verify>
    <automated>env -u CUMPA_RELEASE_SUPPORT_SERVICE_URL npx playwright test --config playwright.runtime-artifact.config.ts tests/e2e/package-assets.spec.ts</automated>
  </verify>
  <acceptance_criteria>The exact candidate installs at the scoped global path under a non-inheriting environment; its generated bin, direct runtime dependency tree, legal/runtime/browser bytes, and all archive identities verify without checkout resolution.</acceptance_criteria>
  <done>Package metadata, prerequisites, dependencies, runtime/legal contents, and browser graph work from the unchanged supplied archive.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run installed review, support-unavailable, and exact-patch Finish flows</name>
  <files>tests/e2e/agent-ready-export.spec.ts</files>
  <read_first>
    - `tests/e2e/agent-ready-export.spec.ts` pack/extract/symlink setup, CLI launchers, existing scenarios, teardown, and evidence writer
    - `tests/helpers/agent-ready-export-target.ts` exact Darwin ARM64 native observation rule
    - `tests/helpers/source-control-snapshot.ts` repository/index/ref/untracked snapshots
    - `tests/package/agent-ready-export-safety.test.ts` durable export/source-control safety contracts
    - `src/contracts/draft.ts` `ReviewExportV3Schema` and exact-patch provenance contract
    - `src/server/native-exchange-capability.ts` `reExportUnsupported` behavior
    - `src/web/App.vue` and `src/web/components/SupportDialog.vue` configured prompt, unavailable state, and `Not now`
    - `.planning/phases/04-exact-runtime-tarball/04-PATTERNS.md` Playwright and installed-launcher seams
  </read_first>
  <behavior>
    - Test 1: comments/summary survive installed-process/browser restart; recovered canonical JSON/Markdown bytes and receipts match the accepted ordered OIDs.
    - Test 2: attached range emits nothing before Finish, then exactly one canonical V2 document with diagnostics on stderr and clean child termination.
    - Test 3: a real installed exact-patch request reaches Finish and yields a `ReviewExportV3Schema` document whose provenance, OIDs, file identity, preimage anchor, patch, and exported bytes match `createGroundedExactPatch()`.
    - Test 4: the embedded configured-support prompt opens; outbound support access is aborted, unavailable state appears, `Not now` dismisses it, and review/export/Finish remain unrestricted without payment or hosted mutation.
    - Test 5: equivalent attached ranges preserve provenance with isolated draft/delivery and no source/index/ref/untracked changes outside approved fixture `.cumpa` outputs.
    - Test 6: Darwin ARM64 observes the actual installed addon for target-aware second export; other targets report `reExportUnsupported` and preserve first stable-pair bytes. No native-success mock is permitted.
    - Test 7: every installed process/resource closes and the supplied archive identity matches before/after all scenarios.
  </behavior>
  <action>Replace packed-root/build/pack/extract setup and checkout dependency symlink with the custody/basename/digest/evidence contract and the same scripts-disabled global install/environment allowlist from Task 1. The scanner parent may consume `CUMPA_RELEASE_SUPPORT_SERVICE_URL`; omit it from installed CLI and Playwright scenario children. Spawn npm's generated executable in `startGeneratedCli()` and `startAttachedCli()`, preserving controlled selector/stdin and fake browser-opener seams with Git on PATH.

Retain existing review/comment/summary/relaunch/recovery/V2 export/receipt/Finish scenarios. Add one installed exact-patch scenario through the real request/Finish boundary; validate output with `ReviewExportV3Schema` and assert grounded provenance/bytes against `createGroundedExactPatch()` using committed fixture OIDs. Existing unit/API tests do not substitute for this installed path.

Exercise configured support without hosted mutation: abort every non-loopback outbound browser request, open the actual embedded support prompt, initiate its real action, observe unavailable state, click `Not now`, then complete review/export/Finish. Do not inject a different origin, fake success, authorize payment, or create remote state.

Preserve source-control snapshots. Report bounded archive algorithms, installed logical labels, dependency digest/count, manifest digest, exact-patch/support outcomes, and native target observation—not checkout launcher comparisons or paths. Terminate children, remove only install/temp/browser state, and rehash the candidate after scenarios. Use `hasObservedNativeReExport(process.platform, process.arch)` only.</action>
  <verify>
    <automated>env -u CUMPA_RELEASE_SUPPORT_SERVICE_URL npx playwright test --config playwright.runtime-artifact.config.ts tests/e2e/agent-ready-export.spec.ts</automated>
  </verify>
  <acceptance_criteria>The unchanged installed candidate completes review/relaunch, V2 and grounded V3 Finish, configured-support failure/dismissal, and real native/fallback behavior without checkout dependencies or remote mutation.</acceptance_criteria>
  <done>The exact installable runtime—not checkout output—completes the browser and attached-agent contracts, including exact patch and neutral support behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Isolate candidate runners and aggregate bounded acceptance evidence</name>
  <files>tests/package/agent-ready-export.test.ts, vitest.config.ts, vitest.runtime-artifact.config.ts, package.json</files>
  <read_first>
    - `tests/package/agent-ready-export.test.ts` current Playwright command, launcher comparison, requirement arrays, and evidence assertions
    - `vitest.config.ts` ordinary package-test discovery
    - `playwright.config.ts` ordinary spec discovery
    - `package.json` `test:package`, `test:browser`, `test:package-contract`, and Phase 4 scripts
    - `tests/e2e/package-assets.spec.ts` Task 1 report contract
    - `tests/e2e/agent-ready-export.spec.ts` Task 2 report contract
    - `.planning/REQUIREMENTS.md` PKG-03/04/05 and REL-03
  </read_first>
  <behavior>
    - Test 1: ordinary Vitest and Playwright configs exclude candidate-only acceptance; dedicated configs include exactly the aggregator or two candidate specs respectively.
    - Test 2: missing/mismatched candidate inputs fail the dedicated aggregator, never skip; ordinary credential-free suites remain runnable without those inputs.
    - Test 3: aggregator rejects a scenario report with different archive algorithms/length, package identity, target, run ID, basename, support/exact-patch result, or dependency digest/count.
    - Test 4: output contains bounded logical labels/digests/counts/statuses and only Phase 4 requirement IDs; it excludes paths, origin, credentials, dependency tree, fixtures, and launcher hashes.
    - Test 5: all archive algorithm identities still match after aggregation; failure produces no passing report.
  </behavior>
  <action>Create `vitest.runtime-artifact.config.ts` including only the aggregator and exclude it from ordinary `vitest.config.ts`. Use the dedicated Playwright config already created in Task 1. Missing aggregate inputs fail closed; ordinary `test:package`, `test:browser`, broad CI Vitest and ordinary Playwright remain credential-free. Remove the aggregator from ordinary `test:package-contract` while retaining source-level safety coverage.

Refactor the aggregator around all required parent inputs and its paired scenario bridge. Validate archive/evidence identity, invoke the scanner here with the environment-only origin, and retain its bounded result in memory. A scanner failure prevents spawning Playwright. Then invoke `playwright test --config playwright.runtime-artifact.config.ts` once with common identity inputs/run/report bridge but without the origin or runtime override keys. `accept:runtime-artifact` invokes `vitest run --config vitest.runtime-artifact.config.ts --no-file-parallelism`; no other process owns configured scanning.

Validate scenario run ID and archive/package/install/dependency/browser/review/support/V3/native/cleanup/source-control results. Combine them with the parent's own scanner result only after all archive identities agree. Atomically write the bounded acceptance report, rehash the candidate, and remove temporary scenario reports. Never trust a scenario-authored scanner result, retain private values, build, pack, publish or skip.</action>
  <verify>
    <automated>CUMPA_RUNTIME_CUSTODY_DIR="$CUMPA_RUNTIME_CUSTODY_DIR" CUMPA_RUNTIME_ARCHIVE_BASENAME="$CUMPA_RUNTIME_ARCHIVE_BASENAME" CUMPA_RUNTIME_ARCHIVE_SHA256="$CUMPA_RUNTIME_ARCHIVE_SHA256" CUMPA_RUNTIME_EVIDENCE="$CUMPA_RUNTIME_EVIDENCE" CUMPA_RELEASE_SUPPORT_SERVICE_URL="$CUMPA_RELEASE_SUPPORT_SERVICE_URL" CUMPA_RUNTIME_ACCEPTANCE_REPORT="$CUMPA_RUNTIME_ACCEPTANCE_REPORT" npm run accept:runtime-artifact</automated>
  </verify>
  <acceptance_criteria>One dedicated no-build command accepts the exact candidate and emits a bounded hash-linked report; ordinary runners do not discover or skip candidate tests.</acceptance_criteria>
  <done>Installed-runtime acceptance is explicit, isolated from credential-free suites, and bound to one unchanged archive.</done>
</task>

</tasks>

<threat_model>
## Enforcement
ASVS Level 1 applies. Every HIGH-severity installation, dependency-isolation, archive-identity, disclosure, or behavioral-integrity finding blocks acceptance.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Supplied archive/evidence -> scanner/npm installer | Exact hash-bound bytes are scanned with parent-only origin, then enter a scripts-disabled global install under an allowlisted consumer environment. |
| Installed npm prefix -> product process | Only npm's generated scoped-package bin launches; origin/auth/npm config/checkout modules do not cross. |
| Browser -> loopback server/outbound support | Installed assets/review APIs stay loopback; configured support outbound access is aborted and its real unavailable/dismissal behavior is observed. |
| Attached agent -> canonical export | V2 and grounded V3 exact-patch requests remain silent until Finish and preserve schema/provenance/bytes. |
| Native addon -> filesystem export | Real Darwin ARM64 exchange is observed; failure/other targets return explicit safe fallback. |
| Test process -> durable evidence | Reports retain bounded hashes/logical labels/counts/outcomes, not dependency trees, credentials, origins, fixture content, or private paths. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-03-DB | Elevation of Privilege / Tampering | dependency bleed-through | HIGH | mitigate | Allowlisted child environment, fresh HOME/config/cache/prefix, public registry, no auth/npm extras/NODE_PATH/symlink, generated bin, exact `npm ls` runtime tree. |
| T-04-03-LS | Elevation of Privilege | install lifecycle scripts | HIGH | mitigate | Real npm install with `--ignore-scripts`; no acceptance build/pack/producer or install hook. |
| T-04-03-AS | Spoofing / Tampering | archive substitution | HIGH | mitigate | Custody+basename containment, required evidence/SHA-256, all-algorithm pre/post rehash, report identity equality. |
| T-04-03-WA | Tampering | installed assets/runtime tree | HIGH | mitigate | Real installed page graph plus exact global runtime dependency versions; checkout tools are harness-only. |
| T-04-03-AF | Spoofing / Repudiation | Finish export | HIGH | mitigate | Real installed silence-until-Finish V2 plus schema-validated grounded V3 provenance/bytes and clean termination. |
| T-04-03-SP | Spoofing / Elevation of Privilege | configured support | HIGH | mitigate | Exercise embedded configuration with outbound abort, visible unavailable state, `Not now` dismissal, no payment/hosted mutation, and unrestricted core workflow. |
| T-04-03-NT | Repudiation | native support | MEDIUM | mitigate | Real installed addon observation only on Darwin ARM64; explicit fallback and stable-byte retention elsewhere. |
| T-04-03-ER | Information Disclosure | acceptance reports | HIGH | mitigate | Atomic bounded digest/count/outcome report, no paths/origin/raw dependency tree/content, temporary bridge cleanup. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `tests/e2e/package-assets.spec.ts` — no-build supplied-archive install/dependency/version/legal/browser/immutability checks; no origin input or scanner invocation.
- `tests/e2e/agent-ready-export.spec.ts` — installed review/relaunch, configured-support unavailable dismissal, grounded V3 exact-patch Finish, and real native/fallback behavior.
- `tests/package/agent-ready-export.test.ts` — parent-owned configured scanner invocation and result plus candidate-only scenario aggregation into `cumpa.runtime-artifact-acceptance/v1`.
- `vitest.runtime-artifact.config.ts` / `vitest.config.ts` — dedicated candidate aggregator discovery and explicit ordinary-suite exclusion.
- `playwright.runtime-artifact.config.ts` / `playwright.config.ts` — dedicated two-spec candidate discovery and explicit ordinary-suite ignore.
- `package.json` — transparent `accept:runtime-artifact`; ordinary package/browser/contract scripts remain candidate-free.
- Required parent inputs: `CUMPA_RUNTIME_CUSTODY_DIR`, `CUMPA_RUNTIME_ARCHIVE_BASENAME`, `CUMPA_RUNTIME_ARCHIVE_SHA256`, `CUMPA_RUNTIME_EVIDENCE`, `CUMPA_RELEASE_SUPPORT_SERVICE_URL`, and `CUMPA_RUNTIME_ACCEPTANCE_REPORT`; scenario-only bridge `CUMPA_AGENT_READY_EVIDENCE_REPORT` plus `CUMPA_AGENT_READY_EVIDENCE_RUN_ID`.
- Acceptance report: `kind`, `status`, `purpose`, `archive.{basename,sha256,byteLength,npmShasumSha1,npmIntegritySha512}`, `package`, `install.{packageLabel,binLabel,manifestSha256,dependencyCount,dependencyInventorySha256}`, `scanner`, `browser`, `review`, `support`, `exactPatch`, `native`, `cleanup`, `sourceControl`, `checks`, and `limitations`; no path, origin, credential, raw dependency tree/log, or fixture content.
</artifacts_this_phase_produces>

<verification>
- Supply one disposable candidate's custody directory, basename, SHA-256, evidence, configured scanner origin, and report destination; missing inputs must fail in dedicated configs and remain absent from ordinary discovery.
- Observe bounded child environments and runtime tree: no auth/npm extras/NODE_PATH/origin override reaches product children; installed direct versions equal the producer manifest map verified by the parent scanner.
- Confirm installed review/relaunch/recovery, support unavailable plus `Not now`, V2/V3 Finish/export, source-control/process cleanup, recursive assets, and target-specific native outcomes.
- Recompute SHA-256/SHA-1/SHA-512-SRI/length before and after aggregate acceptance and require exact evidence/report equality.
</verification>

<success_criteria>
- PKG-03: npm's installed `cumpa --version` prints exactly `1.5.0`; metadata retains Node.js `>=24` and ordinary Git prerequisite guidance.
- PKG-04: installed candidate loads its exact runtime dependency tree and complete Node/browser/native bytes, then completes review, support dismissal, export, relaunch, recovery, V2/V3 Finish without checkout files.
- PKG-05: installed inventory is runtime/legal only; scripts are disabled and the product cannot resolve source/tests/planning/local state/skill/developer dependencies from checkout.
- REL-03: all dedicated acceptance receives and rehashes one custody-directory-plus-basename archive; no runner rebuilds, repacks, substitutes, mutates, or silently skips it.
</success_criteria>

<output>Create `.planning/phases/04-exact-runtime-tarball/04-03-SUMMARY.md` when complete. Preserve temporary acceptance output only until Plan 04-04 incorporates it into the single final evidence record.</output>
