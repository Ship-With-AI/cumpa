---
phase: 06-independent-mit-marketplace-skill
plan: "02"
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - .kimi-code/skills/cumpa/SKILL.md
  - .kimi-code/skills/cumpa/LICENSE
  - .planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json
external_repository_files:
  repository: https://github.com/Ship-With-AI/skills.git
  checkout: "$MARKETPLACE_CHECKOUT"
  paths:
    - skills/cumpa/SKILL.md
    - skills/cumpa/LICENSE
    - skills/cumpa/scripts/check-cumpa.mjs
    - README.md
    - .claude-plugin/plugin.json
    - .claude-plugin/marketplace.json
autonomous: false
requirements:
  - SKL-01
  - SKL-02
  - SKL-03
artifacts_this_phase_produces:
  - .kimi-code/skills/cumpa/SKILL.md
  - .kimi-code/skills/cumpa/LICENSE
  - .planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json
  - "External Ship-With-AI/skills candidate commit at logical $MARKETPLACE_CHECKOUT"
must_haves:
  truths:
    - "A thin, self-contained `skills/cumpa/` directory joins the existing `ship-with-ai` collection and carries the exact Cumpa MIT license; no standalone plugin or marketplace is created (D-01, D-02)."
    - "The skill runs the Plan 01 preflight first and documents exact installation, discovery, invocation, and lifecycle behavior for Claude Code, Codex, Pi, and OMP as four distinct targets (D-03, D-04, D-06, D-07)."
    - "After preflight, the skill preserves native Git identity resolution and delegates grounding, diffing, browser review, persistence, Finish, and canonical export to Cumpa; readiness remains distinct from successful exit (D-08)."
    - "A fresh exact marketplace baseline, its actual parent and candidate commits, canonical next-minor/reset-patch collection version, installer version/integrity, four-target/two-stage `commandMatrix`, RFC 8785 command digest, target row bindings, controlled `fixtureResultSha256`, empty append-only `targetProofAuthority.publicProofAttempts`, and complete reviewed path list are frozen before any provider or publication approval (D-10)."
  prohibitions:
    - "No application source, npm package, runtime artifact, Phase 5 evidence/authority, Supabase setting, release workflow, AI/UI/database/schema behavior, standalone plugin, sync framework, or remote ref is changed (D-09, D-10)."
    - "No automatic Cumpa install/upgrade, Cumpa npx fallback, unbounded target claim, detached process, tmux requirement, floating marketplace HEAD, or floating installer version is allowed (D-04, D-05, D-07, D-08)."
  artifacts:
    - path: .kimi-code/skills/cumpa/SKILL.md
      provides: "Canonical thin skill instructions and four-target lifecycle branches"
      contains: "license: LICENSE"
    - path: .kimi-code/skills/cumpa/LICENSE
      provides: "Exact self-contained MIT grant and attribution"
    - path: "external:$MARKETPLACE_CHECKOUT/skills/cumpa/"
      provides: "Byte-matched files in the separately owned external marketplace Git commit"
    - path: .planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json
      provides: "Immutable candidate, exact command matrix and row bindings, controlled fixture-result digest, and one bounded target-proof authority lifecycle"
  key_links:
    - from: .kimi-code/skills/cumpa/SKILL.md
      to: .kimi-code/skills/cumpa/scripts/check-cumpa.mjs
      via: "first workflow step invokes the bundled checker before Git or Cumpa review work"
      pattern: "scripts/check-cumpa\\.mjs"
    - from: "external:$MARKETPLACE_CHECKOUT/.claude-plugin/plugin.json"
      to: "external:$MARKETPLACE_CHECKOUT/skills/cumpa/SKILL.md"
      via: "existing `./skills/` collection discovery root and exact collection version transition"
      pattern: "./skills/"
    - from: .kimi-code/skills/cumpa/
      to: "external:$MARKETPLACE_CHECKOUT/skills/cumpa/"
      via: "one direct reviewed byte-for-byte copy of SKILL.md, LICENSE, and check-cumpa.mjs"
      pattern: "skills/cumpa"
---

## Execution amendment — 2026-09-11

User instruction: **“ignore this test part”** (direct current-chat instruction; see CONTEXT D-07/D-11).

Tasks 1 and 2 are complete. Task 3 is **waived**, not authorized or passed: do not request `TEMP AUTH READY`, collect four-target proof assent, run its verifier, or start sign-in/installer/provider actions. The corresponding proof-related must-haves and success criteria below are superseded by this amendment. Preserve the original candidate record and review evidence; do not fabricate a consumed authority or target-proof result. Close this plan with the completed source/candidate work and an explicit verification limitation, then proceed only to the separate marketplace publication checkpoint.

<objective>
Author the complete thin skill and freeze one exact existing-collection marketplace candidate, implementing D-01 through D-10 without publishing it.

Purpose: Establish the exact source-to-marketplace payload and honest four-target instructions that the next plan must prove before publication.
Output: Canonical source skill, exact MIT license, local external-repository candidate, and a bounded candidate record.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<planning_constraints>
Schema Push Detection Gate result: no phase file matches Payload CMS, Prisma, Drizzle, Supabase migration, or TypeORM schema patterns. "If no schema-relevant files are detected: Skip silently." No schema push task applies.

Each PLAN.md must include a <threat_model> block when security enforcement is active. Use the configured ASVS level and blocking threshold from workflow.security_asvs_level and workflow.security_block_on.

<tdd_mode_active>
**TDD Mode is ENABLED.** Apply TDD heuristics to all eligible tasks:
- Business logic with defined I/O → type: tdd
- API endpoints with request/response contracts → type: tdd
- Data transformations, validation, algorithms → type: tdd
- UI, config, glue code, CRUD → standard plan (type: execute)
Each TDD plan gets one feature with RED/GREEN/REFACTOR gate sequence.
</tdd_mode_active>

This plan contains instruction, license, metadata, and copy wiring only; defined-I/O validation was isolated in Plan 01. MVP mode is false. Granularity is coarse. Nyquist validation is disabled, so do not create Nyquist artifacts. `.planning/intel/API-SURFACE.md` is stale with zero symbols and is only an incomplete hint.
</planning_constraints>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-independent-mit-marketplace-skill/06-CONTEXT.md
@.planning/phases/06-independent-mit-marketplace-skill/06-RESEARCH.md
@.planning/phases/06-independent-mit-marketplace-skill/06-PATTERNS.md
@.planning/phases/06-independent-mit-marketplace-skill/06-01-SUMMARY.md
@.kimi-code/skills/cumpa/SKILL.md
@.kimi-code/skills/cumpa/scripts/check-cumpa.mjs
@LICENSE
@README.md
@docs/distribution-operations.md
</context>

<interfaces>
Preserve these application-owned boundaries rather than copying application schemas:
- Revision request: one strict `cumpa.review-request` v1 object with full pinned merge-base/head OIDs and optional ordered user-requested pathspecs.
- Exact-patch request: only an exact user-supplied patch; never synthesize a patch or temporary commit.
- Launch: run from the reviewed repository, unset `CMUX_WORKSPACE_ID`, redirect stdin from the private request file and stdout to the private result file.
- Completion: a loopback URL is readiness only; consume output only after observed exit 0, then require one non-empty JSON object with `kind: "cumpa/export"`.
- Cleanup: delete only the unique temporary request/result/diagnostic resources owned by the current operation, after result consumption or explicit terminal failure.

Use one exact private location contract across Plans 02 and 03: derive `CUMPA_PHASE6_PRIVATE_ROOT="${XDG_STATE_HOME:-$HOME/.local/state}/cumpa/phase6-marketplace"`, then `MARKETPLACE_CHECKOUT="$CUMPA_PHASE6_PRIVATE_ROOT/Ship-With-AI-skills"`, `TARGET_ROOT="$CUMPA_PHASE6_PRIVATE_ROOT/targets"`, and `TARGET_PROJECT="$CUMPA_PHASE6_PRIVATE_ROOT/project"`. Target-specific client environments come from the reviewed command matrix: Claude uses CLAUDE_CONFIG_DIR and its reviewed XDG roots; Codex uses CODEX_HOME; Pi uses the owned HOME and PI_CODING_AGENT_SESSION_DIR; OMP uses PI_CODING_AGENT_DIR and its reviewed XDG roots. Apply the complete reviewed environment only to the corresponding child client; never invent PI_CODEX_HOME or OMP_HOME aliases, or change the orchestrator HOME. Every automated command independently assigns and exports, in that order, the common roots and every target mapping it uses; no command inherits prior shell state. Create the root mode 0700 and `$CUMPA_PHASE6_PRIVATE_ROOT/operation.json` mode 0600. That private journal alone stores expanded absolute paths, expanded Claude slash-command text, and recovery details. Committed records use only these logical variable names and never a private operation ID.

`06-MARKETPLACE-CANDIDATE.json` records `kind: "cumpa.marketplace-candidate/v1"`, `status: "prepared"`, repository URL, full `baselineOid`/`candidateOid`, exact repository-relative `reviewedPaths`, `reviewedFileHashes` for every changed path, `skillFileHashes` for exactly `skills/cumpa/SKILL.md`, `LICENSE`, and `scripts/check-cumpa.mjs`, exact `collectionVersion.from`/`.to`, installer package/version/integrity, and the complete logical `commandMatrix`. `commandMatrix` has exactly the ordered target keys `claude-code`, `codex`, `pi`, and `omp`; each target is `{agentVersion:string,authRoute:string,local:Stage,public:Stage}`, and each `Stage` is `{installActions:string[],discoveryPath:string,invocationIdentity:string}`. `installActions` preserves the reviewed logical order. Claude's local action uses `/plugin marketplace add <MARKETPLACE_CHECKOUT>` as the logical template; immediately before interactive dispatch Plan 03 must read `MARKETPLACE_CHECKOUT` from the private journal, substitute the literal checkout path into the slash-command text, and keep that expanded text private because Claude slash commands do not perform shell expansion.

`commandScopeSha256` is lowercase hex SHA-256 of JCS (RFC 8785) canonical UTF-8 bytes of the complete `commandMatrix`. Every local/public proof row binds its `targetId` and stage to that matrix row by exact equality of `agentVersion`, `authRoute`, normalized logical `discoveryPath`, and `invocationIdentity`, plus `installActionsSha256` equal to lowercase hex SHA-256 of JCS (RFC 8785) canonical UTF-8 bytes of the stage's ordered `installActions` array. Missing or merely nonempty binding fields fail closed. The candidate also records lowercase hex `fixtureResultSha256`, computed before assent over the exact raw controlled compatible-fixture result bytes; every later `compatibleFixture.resultSha256` must equal it and include the result/invocation bindings in the derived proof digest. The fixture remains explicitly non-release proof.

`targetProofAuthority` preserves actual attributed/timestamped assent, immutable candidate/installer/command/auth scopes and proof stages, state `authorized|consumed|completed|failed`, nullable consumption/outcome, and `publicProofAttempts`, initialized by this plan as the empty append-only array. Plan 02 never appends a public attempt. Public committed records contain logical templates, repository-relative paths, labels, OIDs, and approved digests only; expanded private paths, command text, credentials, raw receipts, raw fixture bytes, and nonce material remain in `$CUMPA_PHASE6_PRIVATE_ROOT/operation.json`.
</interfaces>
<tasks>

<task type="auto">
  <name>Task 1: Make the canonical source skill self-contained and target-specific</name>
  <read_first>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-CONTEXT.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-RESEARCH.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-PATTERNS.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-01-SUMMARY.md</file>
    <file>.kimi-code/skills/cumpa/SKILL.md</file>
    <file>LICENSE</file>
    <file>README.md</file>
  </read_first>
  <files>.kimi-code/skills/cumpa/SKILL.md, .kimi-code/skills/cumpa/LICENSE</files>
  <action>Copy root `LICENSE` byte-for-byte to the skill and add `license: LICENSE` to skill frontmatter per D-02. Keep the existing thin delegation protocol and make `node "<skill-directory>/scripts/check-cumpa.mjs"` the first operation before Git resolution per D-03/D-04; state that 1.5.0 is proven while later accepted stable 1.x versions are policy only per D-05. Add four concise target branches per D-06/D-07: Claude Code native collection invocation `/ship-with-ai:cumpa` with supported background Bash and no detached fallback; Codex project `.agents/skills` invocation `$cumpa` or `/skills` with its returned PTY-backed execution session; Pi project `.pi/skills` invocation `/skill:cumpa` using foreground Bash with no explicit timeout, stdout redirected to result, stderr left visible in Pi live partial rendering, and result read only after final exit; OMP native collection invocation `/skill:cumpa` with a stable named process, readiness observation, and a distinct exit wait. For Pi, cite the v0.80.2 static-flow conclusion in prose: humans can see streamed stderr URL before the foreground call returns, while model-visible pre-return consumption is unproven and not required. Preserve D-08: full OIDs from native Git, one private temporary handoff, URL readiness not completion, Finish/exit 0 before outer `cumpa/export` check, no agent review or edits, and owned cleanup after consumption. Per D-09/D-10, do not change the app, published package, remote, or any private release/auth state.</action>
  <acceptance_criteria>
    <criterion>The skill-local LICENSE is byte-identical to root LICENSE and frontmatter points to it.</criterion>
    <criterion>The first workflow action is the shared checker, with exact failure guidance and no installation side effect.</criterion>
    <criterion>Claude Code, Codex, Pi, and OMP each have exact discovery/invocation and supported lifetime handling; Pi and OMP evidence is not conflated.</criterion>
    <criterion>The existing revision, exact-patch, Finish, result, non-mutation, and cleanup contracts remain complete.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>cmp LICENSE .kimi-code/skills/cumpa/LICENSE && npm exec --offline -- vitest run tests/cli/check-cumpa.test.ts</automated>
  </verify>
  <done>The repository-owned skill is the complete self-contained source payload for SKL-01/SKL-02/SKL-03 and contains no duplicated application behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Freeze one exact external marketplace candidate and logical proof contract</name>
  <read_first>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-CONTEXT.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-RESEARCH.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-PATTERNS.md</file>
    <file>.kimi-code/skills/cumpa/SKILL.md</file>
    <file>.kimi-code/skills/cumpa/LICENSE</file>
    <file>.kimi-code/skills/cumpa/scripts/check-cumpa.mjs</file>
  </read_first>
  <files>External repository `$MARKETPLACE_CHECKOUT`: skills/cumpa/SKILL.md, skills/cumpa/LICENSE, skills/cumpa/scripts/check-cumpa.mjs, README.md, .claude-plugin/plugin.json, .claude-plugin/marketplace.json; current repository: .planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json</files>
  <action>Derive and export the exact private root variables from `<interfaces>` sequentially, create only the owned mode-0700 root and mode-0600 private journal, and clone `https://github.com/Ship-With-AI/skills.git` into `MARKETPLACE_CHECKOUT`; never create a nested `Ship-With-AI/skills` path in the Cumpa checkout. Refresh `main` immediately before editing, bind its full baseline OID, and stop on stale private state rather than reuse another operation. Inspect the exact baseline README, catalog, manifest, skill layout, applicable repository policy, push-triggered workflow files, and local hooks. Per D-01/D-02, copy exactly the three reviewed source files into `skills/cumpa/` and make only the smallest truthful existing README/listing changes. Read `.claude-plugin/plugin.json` from the exact baseline OID before editing; require canonical stable SemVer, increment the minor component by exactly one with unchanged major and patch reset to zero, and stop on noncanonical input or policy conflict. Do not use JavaScript `Number` for unbounded version components. Commit this bounded external delta, require the candidate commit's parent to equal the recorded baseline, and require the actual committed candidate manifest to equal `collectionVersion.to`. Preserve the existing `ship-with-ai` marketplace entry shape; do not add or require an entry-level version field, and change catalog description text only when the candidate makes existing text stale.

Pin the exact reviewed `skills` installer version and registry integrity after read-only legitimacy inspection; `[SUS]` remains a human-gated supply-chain fact, not a vulnerability claim. Persist the exact four-target/two-stage logical commands from Research in `commandMatrix`, including `agentVersion`, `authRoute`, ordered local/public `installActions`, logical normalized `discoveryPath`, and `invocationIdentity`. Use only `MARKETPLACE_CHECKOUT` as the private marketplace token. Store Claude's local marketplace-add as `/plugin marketplace add <MARKETPLACE_CHECKOUT>` and retain only that logical template publicly; its later literal-path expansion is private. Compute `commandScopeSha256` from JCS (RFC 8785) canonical UTF-8 bytes of the complete matrix. Record `fixtureResultSha256` from the exact raw controlled compatible-fixture result bytes before authority is requested. Initialize `targetProofAuthority.publicProofAttempts=[]` and freeze the exact row-binding rule from `<interfaces>`; do not pre-populate public attempts. Store expanded paths and recovery only in the private journal. Do not install anything, invoke a provider, inspect/copy credentials, mutate a remote, or consume Phase 5 authority.</action>
  <acceptance_criteria>
    <criterion>The external checkout has one exact fresh baseline and one locally committed candidate; Cumpa-repository staging owns only canonical source and planning records.</criterion>
    <criterion>The three external `skills/cumpa/` files are byte-identical to canonical source and the reviewed external path list is complete.</criterion>
    <criterion>The actual baseline and `HEAD^` manifest versions agree with `collectionVersion.from`; the actual candidate manifest is canonical next-minor/reset-patch `collectionVersion.to`; the established marketplace entry shape is preserved.</criterion>
    <criterion>The installer identity, exact command matrix and RFC 8785 digest, target row-binding rule, controlled fixture-result digest, and empty public-attempt ledger are recorded before assent.</criterion>
    <criterion>Committed records contain logical root tokens, OIDs and hashes but no expanded absolute path, private operation ID, credential/profile content or provider transcript.</criterion>
    <criterion>No remote mutation, package install, provider invocation, app source change, or credential access occurs.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>CUMPA_PHASE6_PRIVATE_ROOT="${XDG_STATE_HOME:-$HOME/.local/state}/cumpa/phase6-marketplace"; export CUMPA_PHASE6_PRIVATE_ROOT; MARKETPLACE_CHECKOUT="$CUMPA_PHASE6_PRIVATE_ROOT/Ship-With-AI-skills"; export MARKETPLACE_CHECKOUT; TARGET_ROOT="$CUMPA_PHASE6_PRIVATE_ROOT/targets"; export TARGET_ROOT; TARGET_PROJECT="$CUMPA_PHASE6_PRIVATE_ROOT/project"; export TARGET_PROJECT; git -C "$MARKETPLACE_CHECKOUT" diff --check HEAD^ HEAD && cmp .kimi-code/skills/cumpa/SKILL.md "$MARKETPLACE_CHECKOUT/skills/cumpa/SKILL.md" && cmp .kimi-code/skills/cumpa/LICENSE "$MARKETPLACE_CHECKOUT/skills/cumpa/LICENSE" && cmp .kimi-code/skills/cumpa/scripts/check-cumpa.mjs "$MARKETPLACE_CHECKOUT/skills/cumpa/scripts/check-cumpa.mjs" && node --input-type=module <<'CUMPA_PLAN_CHECK'
const fs=await import('node:fs/promises');const {createHash}=await import('node:crypto');const {execFileSync}=await import('node:child_process');const r=JSON.parse(await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json','utf8'));const root=process.env.MARKETPLACE_CHECKOUT;const same=(a,b)=>jcs(a)===jcs(b);const jcs=x=>x===null||typeof x!=='object'?JSON.stringify(x):Array.isArray(x)?'['+x.map(jcs).join(',')+']':'{'+Object.keys(x).sort().map(k=>JSON.stringify(k)+':'+jcs(x[k])).join(',')+'}';const sha=x=>createHash('sha256').update(Buffer.from(x,'utf8')).digest('hex');const git=(...a)=>execFileSync('git',['-C',root,...a],{encoding:'utf8'}).trim();const oid=git('rev-parse','HEAD');const parent=git('rev-parse','HEAD^');const paths=git('diff','--name-only','HEAD^','HEAD').split('\n').filter(Boolean).sort();const manifest=s=>JSON.parse(git('show',s+':.claude-plugin/plugin.json'));const baseVersion=manifest(r.baselineOid).version;const parentVersion=manifest('HEAD^').version;const candidateVersion=manifest(r.candidateOid).version;const workingVersion=JSON.parse(await fs.readFile(root+'/.claude-plugin/plugin.json','utf8')).version;const stable=/^(0|[1-9]\d*)\.(0|[1-9]\d*)\.(0|[1-9]\d*)$/;if(!stable.test(baseVersion)||!stable.test(candidateVersion))process.exit(1);const from=baseVersion.split('.').map(BigInt);const to=candidateVersion.split('.').map(BigInt);const baselineCatalog=JSON.parse(git('show',r.baselineOid+':.claude-plugin/marketplace.json'));const candidateCatalog=JSON.parse(git('show',r.candidateOid+':.claude-plugin/marketplace.json'));const baselineEntry=baselineCatalog.plugins?.find(x=>x.name==='ship-with-ai');const candidateEntry=candidateCatalog.plugins?.find(x=>x.name==='ship-with-ai');const ids=['claude-code','codex','pi','omp'];const matrixOk=same(Object.keys(r.commandMatrix||{}),ids)&&ids.every(id=>{const row=r.commandMatrix[id];return same(Object.keys(row||{}).sort(),['agentVersion','authRoute','local','public'])&&typeof row?.agentVersion==='string'&&row.agentVersion.length>0&&typeof row.authRoute==='string'&&row.authRoute.length>0&&['local','public'].every(stageName=>{const stage=row[stageName];return same(Object.keys(stage||{}).sort(),['discoveryPath','installActions','invocationIdentity'])&&Array.isArray(stage.installActions)&&stage.installActions.length>0&&stage.installActions.every(x=>typeof x==='string'&&x.length>0)&&typeof stage.discoveryPath==='string'&&stage.discoveryPath.length>0&&typeof stage.invocationIdentity==='string'&&stage.invocationIdentity.length>0})});const allHashes=Object.fromEntries(await Promise.all(paths.map(async p=>[p,createHash('sha256').update(await fs.readFile(root+'/'+p)).digest('hex')])));const payloads=['skills/cumpa/SKILL.md','skills/cumpa/LICENSE','skills/cumpa/scripts/check-cumpa.mjs'];const skillHashes=Object.fromEntries(payloads.map(p=>[p,allHashes[p]]));const text=JSON.stringify(r);const bad=/(operationId|privateJournal|\/Users\/|\/home\/|\/private\/|\/var\/folders\/|\/tmp\/)/.test(text)||[process.env.HOME,process.env.CUMPA_PHASE6_PRIVATE_ROOT,root,process.env.TARGET_ROOT,process.env.TARGET_PROJECT].filter(Boolean).some(x=>text.includes(x));if(git('ls-remote','origin','refs/heads/main').split(/\s+/)[0]!==r.baselineOid||r.kind!=='cumpa.marketplace-candidate/v1'||r.status!=='prepared'||r.baselineOid!==parent||r.candidateOid!==oid||git('rev-parse',r.candidateOid+'^')!==r.baselineOid||baseVersion!==parentVersion||r.collectionVersion?.from!==baseVersion||r.collectionVersion?.to!==candidateVersion||workingVersion!==candidateVersion||to[0]!==from[0]||to[1]!==from[1]+1n||to[2]!==0n||!same(candidateEntry,baselineEntry)||!same(r.reviewedPaths?.slice().sort(),paths)||!same(r.reviewedFileHashes,allHashes)||!same(r.skillFileHashes,skillHashes)||r.installer?.package!=='skills'||r.installer?.version!=='1.5.25'||!r.installer?.integrity||!matrixOk||r.commandScopeSha256!==sha(jcs(r.commandMatrix))||!/^[0-9a-f]{64}$/.test(r.fixtureResultSha256)||!same(r.targetProofAuthority?.publicProofAttempts,[])||r.logicalRoots?.marketplaceCheckout!=='$MARKETPLACE_CHECKOUT'||bad||git('status','--porcelain')!=='')process.exit(1)
CUMPA_PLAN_CHECK
    </automated>
  </verify>
  <done>One exact external candidate commit and privacy-safe logical command contract are ready for non-auto-approvable target-proof assent; nothing is published or installed.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Record exact operator authentication prerequisites and target-proof assent</name>
  <read_first>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-RESEARCH.md</file>
  </read_first>
  <files>.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json</files>
  <action>Present the exact candidate OID, `skills@version` plus integrity, four installed target versions, logical roots, complete four-target local/public `commandMatrix`, row-binding rule, `fixtureResultSha256`, empty `targetProofAuthority.publicProofAttempts`, and authentication preconditions. Never assume an empty `HOME` or relocated config retains authentication. For each target, require the operator to name one exact allowed ambient route label, whose value the agent never reads/copies, or separately provision/authenticate the owned temporary target state. If temporary state is required, stop until the operator gives the exact `TEMP AUTH READY` statement below; record only the four selected target-to-route labels, attribution/source, timestamp, and digest, never credential/profile contents. Before the authority prompt, write each selected label to `commandMatrix[targetId].authRoute`, recompute `commandScopeSha256` from JCS (RFC 8785) canonical UTF-8 bytes of the final matrix, and present the final complete matrix, digest, row bindings, controlled fixture-result digest, and empty public-attempt ledger. Then request the exact scope-bearing authorization line below. Human verification or auto-advance is not assent. Accept only a direct current-chat statement matching the displayed candidate, installer, final command digest, all four targets, and auth digest; reject missing, stale, mismatched, ambiguous, declined, or consumed authority.

On valid resume, append the verbatim statements, approver attribution, source `direct current-chat assent`, actual timestamp, immutable scope, and state `authorized` with empty `consumedAt` and outcome. The immutable scope contains `candidateOid`, installer integrity, `commandScopeSha256`, proof stages `["local-candidate","public-same-hash-after-separate-publication"]`, exact row binding `{fields:["agentVersion","authRoute","stage","installActionsSha256","discoveryPath","invocationIdentity"],installActionsCanonicalization:"JCS (RFC 8785) UTF-8 then SHA-256",missingFields:"fail-closed"}`, `fixtureResultSha256`, and `publicProofAttempts:[]`; the authority's top-level `publicProofAttempts` also remains empty. This authorizes one future Plan 03 consumption covering exact prepublication local-candidate proof and, only after separate publication authority establishes identical public bytes, the same command-scoped public target proof. It excludes every remote mutation, application install/upgrade, marketplace push, workflow/deployment, source/npm publication, global profile mutation, and Phase 5 authority.</action>
  <acceptance_criteria>
    <criterion>Every target has an explicit supported ambient route or separately operator-provisioned temporary-auth prerequisite; no credential is inspected, copied or recorded.</criterion>
    <criterion>Durable assent binds the exact candidate, installer, four-target command scope, row-binding rule, fixture-result digest, empty public-attempt ledger, and auth-prerequisite digest with attribution and timestamp.</criterion>
    <criterion>The authority is unconsumed and single-use; decline, mismatch, prior consumption or unavailable target leaves publication blocked.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>node --input-type=module <<'CUMPA_PLAN_CHECK'
const fs=await import('node:fs/promises');const {createHash}=await import('node:crypto');const r=JSON.parse(await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json','utf8'));const a=r.targetProofAuthority;const same=(x,y)=>jcs(x)===jcs(y);const jcs=x=>x===null||typeof x!=='object'?JSON.stringify(x):Array.isArray(x)?'['+x.map(jcs).join(',')+']':'{'+Object.keys(x).sort().map(k=>JSON.stringify(k)+':'+jcs(x[k])).join(',')+'}';const sha=x=>createHash('sha256').update(Buffer.from(x,'utf8')).digest('hex');const ids=['claude-code','codex','pi','omp'];const stages=['local-candidate','public-same-hash-after-separate-publication'];const fields=['agentVersion','authRoute','stage','installActionsSha256','discoveryPath','invocationIdentity'];const expected='AUTHORIZE FOUR-TARGET PROOF: candidate '+r.candidateOid+'; installer skills@'+r.installer.version+' integrity '+r.installer.integrity+'; commands sha256 '+r.commandScopeSha256+'; targets claude-code,codex,pi,omp; auth prerequisites sha256 '+a.authPrerequisiteSha256+'; local candidate plus conditional identical public bytes; no remote mutation.';const routesOk=same(Object.keys(r.commandMatrix||{}),ids)&&ids.every(id=>typeof r.commandMatrix[id]?.authRoute==='string'&&r.commandMatrix[id].authRoute.length>0);if(r.status!=='prepared'||!r.candidateOid||!r.installer?.integrity||!routesOk||r.commandScopeSha256!==sha(jcs(r.commandMatrix))||!/^[0-9a-f]{64}$/.test(r.fixtureResultSha256)||!a||a.state!=='authorized'||a.consumedAt!==null||a.scope?.candidateOid!==r.candidateOid||a.scope?.installerIntegrity!==r.installer.integrity||a.scope?.commandScopeSha256!==r.commandScopeSha256||!same(a.scope?.proofStages,stages)||!same(a.scope?.rowBinding?.fields,fields)||a.scope?.rowBinding?.installActionsCanonicalization!=='JCS (RFC 8785) UTF-8 then SHA-256'||a.scope?.rowBinding?.missingFields!=='fail-closed'||a.scope?.fixtureResultSha256!==r.fixtureResultSha256||!same(a.scope?.publicProofAttempts,[])||!same(a.publicProofAttempts,[])||a.verbatimAssent!==expected||!a.approver||a.source!=='direct current-chat assent'||!a.recordedAt||!a.authPrerequisiteSha256)process.exit(1)
CUMPA_PLAN_CHECK
    </automated>
    <human-check>Supply the separate authentication-prerequisite statement when needed, then the exact target-proof authorization statement. Approval is not publication authority.</human-check>
  </verify>
  <resume-signal>First, when any temporary auth is needed: `TEMP AUTH READY: routes {claude-code=ambient-or-temp,codex=ambient-or-temp,pi=ambient-or-temp,omp=ambient-or-temp}; original profiles unchanged; cleanup only owned temporary state.` Then: `AUTHORIZE FOUR-TARGET PROOF: candidate {candidateOid}; installer skills@{version} integrity {integrity}; commands sha256 {commandScopeSha256}; targets claude-code,codex,pi,omp; auth prerequisites sha256 {authPrerequisiteSha256}; local candidate plus conditional identical public bytes; no remote mutation.` Replace every braced token with the exact displayed value; no other wording authorizes execution.</resume-signal>
  <done>One attributed, immutable, unconsumed authority record exists for the exact all-four-target proof, or execution remains blocked without installation/provider use.</done>
</task>
</tasks>

<threat_model>
Security enforcement: ASVS Level 1. Any unresolved high-severity finding blocks candidate approval and downstream publication.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Cumpa source skill → public marketplace candidate | Executable instructions and checker bytes cross repository ownership and distribution boundaries. |
| External marketplace `main` → local candidate | Fast-moving untrusted remote state becomes the publication base. |
| Marketplace skill → agent runtime/Git/Cumpa | Instructions can invoke tools and influence repository operations. |
| Installer/provider approval → target proof | External code/provider use requires narrow, explicit authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-02-01 | Tampering | source-to-marketplace copy | mitigate | Byte-compare all three copied files and record SHA-256 values in the candidate report. |
| T-06-02-02 | Elevation of Privilege | skill instructions | mitigate | Preserve thin delegation and explicit non-mutation boundaries; add no agent-side review implementation. |
| T-06-02-03 | Spoofing / Tampering | marketplace baseline | mitigate | Refresh official repository, bind full baseline/candidate OIDs, and reject later drift. |
| T-06-02-04 | Repudiation | collection version | mitigate | Record exact old/new versions before approval; never describe floating HEAD as reviewed. |
| T-06-02-05 | Information Disclosure | private checkout/target/auth state | mitigate | Expanded paths and recovery stay only in a mode-0600 private journal; committed records use logical roots and hashes, and credentials/profiles are never inspected or copied. |
| T-06-02-06 | Tampering | exact MIT grant | mitigate | Copy root LICENSE byte-for-byte and reference it from skill frontmatter. |
| T-06-02-07 | Denial of Service | interrupted candidate preparation | mitigate | Reconcile the fixed private journal against exact OIDs; preserve uncertain state and never auto-retry or silently reuse. |
| T-06-02-SC | Tampering / Elevation of Privilege | exact `skills` installer package | mitigate | Bind exact registry version/integrity and require a non-auto-approvable human-action gate with durable single-use assent before installer/provider use; heuristic age flags are not vulnerability evidence. |
</threat_model>

<verification>
Verify the included focused preflight test, exact license/copy bytes, separate external candidate commit, privacy-safe logical matrix, and unconsumed attributed human authority. Do not install, invoke providers, push, publish, deploy, build, or run a broad suite in this plan.
</verification>

<success_criteria>
- D-01 through D-10 are represented in executable skill/candidate instructions without expanding product scope.
- The self-contained source and marketplace copies contain only SKILL.md, LICENSE, and the one checker script.
- The candidate is bound to exact external baseline/candidate OIDs, exact version transition, installer identity, logical roots and command-scope digest without committing private paths.
- A non-auto-approvable checkpoint records one exact unconsumed authority for the next plan's four-target proof, including explicit authentication prerequisites.
</success_criteria>

<output>
Create `.planning/phases/06-independent-mit-marketplace-skill/06-02-SUMMARY.md` when done.
</output>
