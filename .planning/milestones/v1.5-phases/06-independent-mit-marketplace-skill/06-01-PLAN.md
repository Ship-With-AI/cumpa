---
phase: 06-independent-mit-marketplace-skill
plan: "01"
type: tdd
wave: 1
depends_on: []
files_modified:
  - .kimi-code/skills/cumpa/scripts/check-cumpa.mjs
  - tests/cli/check-cumpa.test.ts
autonomous: true
requirements:
  - SKL-02
artifacts_this_phase_produces:
  - .kimi-code/skills/cumpa/scripts/check-cumpa.mjs
  - tests/cli/check-cumpa.test.ts
must_haves:
  truths:
    - "The skill preflight accepts canonical stable Cumpa versions >=1.5.0 and <2.0.0, including valid build metadata, before any Git or review work (D-03)."
    - "Missing, failed, timed-out, signaled, malformed, prerelease, older, and 2.x-or-later Cumpa checks fail closed with the exact 1.5.0 recovery command and Node.js 24+/Git 2.43.0+ guidance (D-04)."
    - "The compatibility policy does not claim that accepted future stable 1.x releases were exercised; 1.5.0 remains the independently verified release (D-05)."
  prohibitions:
    - "No semver dependency, package install, automatic install/upgrade, npx fallback, source checkout, local tarball, or arbitrary-precision loss is introduced."
    - "No source-text assertion test, duplicate case padding, Cumpa protocol implementation, schema/API/UI/database change, or Phase 5 artifact/authority use is allowed (D-09, D-10)."
  artifacts:
    - path: .kimi-code/skills/cumpa/scripts/check-cumpa.mjs
      provides: "Dependency-free executable Cumpa version preflight"
      contains: "cumpa --version"
    - path: tests/cli/check-cumpa.test.ts
      provides: "Behavioral boundary and executable-failure coverage"
  key_links:
    - from: tests/cli/check-cumpa.test.ts
      to: .kimi-code/skills/cumpa/scripts/check-cumpa.mjs
      via: "launches the executable entrypoint against isolated PATH fixtures and observes exit/stdout/stderr"
      pattern: "check-cumpa\\.mjs"
---

<objective>
Implement the one necessary standard-library Cumpa compatibility preflight with TDD, enforcing D-03, D-04, and D-05 without changing the released CLI or claiming future-version proof.

Purpose: Give every supported agent one deterministic fail-closed gate before Git resolution or review launch.
Output: A dependency-free checker and one focused behavioral test file.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<planning_constraints>
Schema Push Detection Gate result: no phase file matches Payload CMS, Prisma, Drizzle, Supabase migration, or TypeORM schema patterns. "If no schema-relevant files are detected: Skip silently." No schema push task applies.

Each PLAN.md must include a <threat_model> block when security enforcement is active. Use the configured ASVS level and blocking threshold from workflow.security_asvs_level and workflow.security_block_on.

<tdd_mode_active>
**TDD Mode is ENABLED.** Apply TDD heuristics to all eligible tasks:
- Business logic with defined I/O → type: tdd
- API endpoints with request/response contracts → type: tdd
- Data transformations, validation, algorithms → type: tdd
- UI, config, glue code, CRUD → standard plan (type: execute)
Each TDD plan gets one feature with RED/GREEN/REFACTOR gate sequence.
</tdd_mode_active>

MVP mode is false. Granularity is coarse. Nyquist validation is disabled, so do not create Nyquist artifacts. `.planning/intel/API-SURFACE.md` is stale with zero symbols and is only an incomplete hint, never evidence that implementation is absent.
</planning_constraints>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-independent-mit-marketplace-skill/06-CONTEXT.md
@.planning/phases/06-independent-mit-marketplace-skill/06-RESEARCH.md
@.planning/phases/06-independent-mit-marketplace-skill/06-PATTERNS.md
@.kimi-code/skills/cumpa/SKILL.md
@scripts/verify-prerequisites.mjs
@tests/cli/help.test.ts
@package.json
</context>

<interfaces>
The new module exposes only its executable contract. Keep parsing, range comparison, subprocess probing, and `main` module-local. It executes literal `cumpa --version` with `shell: false`, a 10-second timeout, and a small output bound; exits 0 only for one canonical stable version in `>=1.5.0 <2.0.0`; and otherwise exits nonzero with exactly the recovery command and prerequisite line. Add no exported helper API solely for tests.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Drive the closed stable-1.x preflight through RED, GREEN, and REFACTOR</name>
  <read_first>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-CONTEXT.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-RESEARCH.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-PATTERNS.md</file>
    <file>scripts/verify-prerequisites.mjs</file>
    <file>tests/cli/help.test.ts</file>
    <file>package.json</file>
  </read_first>
  <files>.kimi-code/skills/cumpa/scripts/check-cumpa.mjs, tests/cli/check-cumpa.test.ts</files>
  <behavior>
    - Acceptance: `1.5.0`, a representative later stable 1.x release, `1.5.0+build.1`, and canonical core components larger than JavaScript safe integers when their mathematical values remain in the range all exit 0.
    - Rejection: `1.4.9`, `2.0.0`, prereleases, empty output, leading-zero core components, `v` prefixes, labels, extra lines, and malformed build metadata all take one failure path.
    - Execution failures: missing executable, spawn error, nonzero status, signal, timeout, and output-bound failure all take the same failure path.
    - Timeout-case feasibility: retain the checker's 10,000 ms subprocess timeout and give only the executable timeout behavior test a 15,000 ms Vitest timeout. Do not change the global test timeout or add sleeps.
    - Failure output: stderr contains exactly `npm install --global @shipwithai/cumpa@1.5.0` followed by `Requires Node.js 24+ and Git 2.43.0+.`; the checker never runs that command.
    - Proof boundary: success reports only the observed accepted version and never says a future stable 1.x version was tested or verified.
  </behavior>
  <action>RED: create `tests/cli/check-cumpa.test.ts` in the existing Vitest include using temporary executable fixtures, isolated `PATH`, and subprocess observations of the checker entrypoint. Write only behavior-level cases that can fail for plausible parser or process-boundary defects; do not import implementation helpers, assert source text, or repeat equivalent rows for coverage padding. Confirm the focused test fails because the executable module is absent. GREEN: create `.kimi-code/skills/cumpa/scripts/check-cumpa.mjs` using only `node:child_process` and `node:url`; keep all helpers module-local, parse canonical three-component SemVer with optional valid build metadata and no prerelease, and compare components with `BigInt` or equivalent arbitrary-precision decimal comparison so numeric overflow cannot narrow D-03. Execute literal `cumpa --version` as an argument array with no shell, a 10-second timeout, bounded output, and closed handling for missing command, spawn error, nonzero exit, signal, timeout, malformed/multiline output, or overflow. The entrypoint exits 0 only for the accepted policy and reports only the observed version; every failure exits nonzero and writes exactly the D-04 command and prerequisite line without executing either. REFACTOR: remove any export or seam not needed by the executable, keep one failure formatter, rerun the focused behavioral test, and retain the policy-versus-proof wording required by D-05.</action>
  <acceptance_criteria>
    <criterion>The accepted and rejected boundaries above are observed only through the executable process, not imported internals or source strings.</criterion>
    <criterion>Valid SemVer build metadata is accepted and arbitrarily large canonical numeric components are compared without Number overflow.</criterion>
    <criterion>Every failed probe stops before Git or review work and emits only the locked recovery guidance.</criterion>
    <criterion>No dependency or installation path is added.</criterion>
    <criterion>The one timeout behavior case has a 15,000 ms per-case deadline, so it observes the checker's 10,000 ms timeout before Vitest terminates the test.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>npm exec --offline -- vitest run tests/cli/check-cumpa.test.ts</automated>
  </verify>
  <done>The focused test passes after a demonstrated RED failure; the standard-library checker enforces D-03/D-04/D-05 and is ready to be referenced by the thin skill.</done>
</task>

</tasks>

<threat_model>
Security enforcement: ASVS Level 1. Any unresolved high-severity finding blocks completion.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Agent skill → user PATH | An instruction bundle invokes a separately installed executable whose path is user-controlled. |
| `cumpa --version` stdout/process state → preflight | Untrusted output, exit state, signal, timeout, and size determine whether review work may begin. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-01-01 | Spoofing / Elevation of Privilege | PATH-resolved `cumpa` | mitigate | Execute without a shell, require one canonical stable version, and state that version compatibility is not binary provenance. |
| T-06-01-02 | Tampering | SemVer comparison | mitigate | Reject noncanonical/prerelease input and compare arbitrary-precision components so overflow cannot alter the locked range. |
| T-06-01-03 | Denial of Service | version subprocess | mitigate | Apply a 10-second timeout and small output bound; signal, timeout, or overflow closes the gate. |
| T-06-01-04 | Repudiation | compatibility claim | mitigate | Report only the observed version; preserve 1.5.0 as the sole independently verified release per D-05. |
| T-06-01-05 | Tampering | recovery behavior | mitigate | Print guidance only; never install, upgrade, execute npx, or substitute checkout/tarball paths. |
</threat_model>

<verification>
Run only the focused preflight test. No build, formatter, broad suite, installation, or remote operation belongs to this plan.
</verification>

<success_criteria>
- The preflight contract is proven at its meaningful version and subprocess boundaries.
- Valid build metadata and arbitrary-precision numeric components behave according to D-03.
- All failures produce the exact D-04 recovery text without side effects.
- No future stable 1.x release is described as exercised proof.
</success_criteria>

<output>
Create `.planning/phases/06-independent-mit-marketplace-skill/06-01-SUMMARY.md` when done.
</output>
