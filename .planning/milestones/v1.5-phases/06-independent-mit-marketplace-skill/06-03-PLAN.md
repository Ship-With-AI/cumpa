---
phase: 06-independent-mit-marketplace-skill
plan: "03"
type: execute
wave: 3
depends_on:
  - 06-02
files_modified:
  - .planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json
  - .planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json
  - .planning/phases/06-independent-mit-marketplace-skill/06-PUBLICATION-EVIDENCE.json
autonomous: false
requirements:
  - SKL-01
  - SKL-02
  - SKL-03
artifacts_this_phase_produces:
  - .planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json
  - .planning/phases/06-independent-mit-marketplace-skill/06-PUBLICATION-EVIDENCE.json
  - "Public Ship-With-AI/skills main containing the exact reviewed external candidate commit"
must_haves:
  truths:
    - "Before publication, Claude Code, Codex, Pi, and OMP independently install/discover the exact local candidate through the reviewed `commandMatrix`; every row exactly binds agent version, auth route, ordered install-action digest, discovery path, invocation identity, skill hashes and fixture result hash (D-03, D-04, D-06, D-07, D-08)."
    - "One installed-skill integration smoke launches the real separately installed released Cumpa 1.5.0 against a disposable Git repository, observes actual loopback readiness, then controlled-aborts without Finish/export or a Phase 7 browser-acceptance claim (D-05, D-08, D-09)."
    - "Publication remains blocked unless observation-derived proof binds the external baseline/candidate OIDs, installer, command scope, copied bytes, exact collection version transition, consumed target authority, and zero unresolved high findings (D-01, D-02, D-10)."
    - "A fresh non-auto-approvable assent binds one exact inspected push command, immutable effects digest and post-push proof; it grants no auth changes, application/npm action, unrelated workflow/deployment, Supabase action, or Phase 5 authority (D-09, D-10)."
    - "Recovery reconciles an already-applied exact candidate and any interrupted public-proof action read-only without replay; every new public call requires a durably recorded in-progress attempt over the exact admitted remaining scope, while Phase 7 remains untouched (D-07, D-09, D-10)."
  prohibitions:
    - "No selected target may be omitted, conflated, replaced by documentation, or credited from another target's result."
    - "No floating HEAD/version push, force push, automatic retry, workflow dispatch/cancellation, app/npm publication, auto-install/upgrade, Cumpa npx fallback, detached shell, tmux fallback, agent credential inspection/copy/modification, private path/operation ID in committed evidence, or unrelated user-state mutation is allowed."
    - "No full public browser acceptance claim, new review behavior, schema/API/UI/database/AI scope, or duplicated Cumpa implementation is allowed (D-08, D-09)."
  artifacts:
    - path: .planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json
      provides: "Consumed target-proof authority lifecycle and immutable command bindings"
    - path: .planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json
      provides: "Four independent missing/incompatible/compatible fixture observations plus one real released-CLI readiness/abort smoke"
      contains: "cumpa.marketplace-target-proof/v1"
    - path: .planning/phases/06-independent-mit-marketplace-skill/06-PUBLICATION-EVIDENCE.json
      provides: "Attributed push assent/attempt recovery, exact public OID/bytes, effect reconciliation and four public installation observations"
      contains: "cumpa.marketplace-publication/v1"
  key_links:
    - from: .planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json
      to: .planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json
      via: "every target row consumes the exact commandMatrix local stage and binds its agentVersion, authRoute, installActionsSha256, discoveryPath, invocationIdentity, candidate file hashes and fixtureResultSha256"
      pattern: "candidateOid"
    - from: .planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json
      to: "Ship-With-AI/skills refs/heads/main"
      via: "observation-derived proof and one attributed effects-bound authority gate the exact non-force candidate-OID refspec"
      pattern: "commandScopeSha256"
    - from: "Ship-With-AI/skills refs/heads/main"
      to: .planning/phases/06-independent-mit-marketplace-skill/06-PUBLICATION-EVIDENCE.json
      via: "append-only publicProofAttempts intent/reconciliation, anonymous exact-OID/file-hash proof, approved-effect observation and exact commandMatrix public rows"
      pattern: "observedPublicOid"
---

## Execution amendment — 2026-09-11

User instruction: **“ignore this test part”** (direct current-chat instruction; see CONTEXT D-07/D-11). This amendment takes precedence over the original proof-related frontmatter, tasks, verifiers and success criteria below.

- Task 1 is **waived**, including four-agent local installation/discovery, prerequisite/fixture lifecycle runs and the associated real-Cumpa readiness/abort smoke. Do not perform authentication, installer, provider/model or review calls for this exercise.
- Task 2 remains a blocking publication checkpoint. Inspect current exact candidate bytes, remote baseline, push effects and protections. Obtain separate attributed assent for the exact marketplace-only push. Replace the old target-proof binding with the explicit user waiver; do not manufacture a proof digest or require authentication prerequisites.
- Task 3 retains a single authorized non-force exact-OID push (or read-only reconciliation of an already-applied push), anonymous public OID/file-hash verification and approved-effect reconciliation. The four-agent public installation stage is waived. No installer/provider recovery authority is needed or granted.
- The original test-dependent automated commands below are historical and must not run under this amendment. Retain candidate/remote/effect checks without synthesizing successful target rows, test results, credential readiness or authority consumption.
- Keep all four documented target branches. Report runtime compatibility and fresh four-agent public installation as **not exercised, user-waived**. Preserve Phase 7, existing authentication, the Cumpa/npm/Supabase non-mutation boundary, and the requirement for separate marketplace-push approval.

<objective>
Prove the frozen candidate on all four selected agents, obtain exact marketplace-only authority, publish exactly that candidate, and prove anonymous public installability while honoring D-01 through D-10.

Purpose: Close SKL-01/SKL-02/SKL-03 with real target and public distribution evidence without consuming Phase 7 browser acceptance or Phase 5 authority.
Output: Four-target prepublication evidence, one exact public marketplace transition, and bounded public proof.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<planning_constraints>
Schema Push Detection Gate result: no phase file matches Payload CMS, Prisma, Drizzle, Supabase migration, or TypeORM schema patterns. "If no schema-relevant files are detected: Skip silently." No schema push task applies.

Each PLAN.md must include a <threat_model> block when security enforcement is active. Use the configured ASVS level and blocking threshold from workflow.security_asvs_level and workflow.security_block_on.

<tdd_mode_active>
**TDD Mode is ENABLED.** Apply TDD heuristics to all eligible tasks:
- Business logic with defined I/O → type: tdd
- API endpoints with request/response contracts → type: tdd
- Data transformations, validation, algorithms → type: tdd
- UI, config, glue code, CRUD → standard plan (type: execute)
Each TDD plan gets one feature with RED/GREEN/REFACTOR gate sequence.
</tdd_mode_active>

This plan executes already-defined installation/lifecycle/publication procedures; the only defined-I/O validation was completed under TDD in Plan 01. MVP mode is false. Granularity is coarse. Nyquist validation is disabled, so do not create Nyquist artifacts. `.planning/intel/API-SURFACE.md` is stale with zero symbols and is only an incomplete hint.
</planning_constraints>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-independent-mit-marketplace-skill/06-CONTEXT.md
@.planning/phases/06-independent-mit-marketplace-skill/06-RESEARCH.md
@.planning/phases/06-independent-mit-marketplace-skill/06-PATTERNS.md
@.planning/phases/06-independent-mit-marketplace-skill/06-01-SUMMARY.md
@.planning/phases/06-independent-mit-marketplace-skill/06-02-SUMMARY.md
@.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json
</context>

<interfaces>
`06-MARKETPLACE-CANDIDATE.json` is the producer contract from Plan 02. Its `commandMatrix` has exactly the top-level keys `claude-code`, `codex`, `pi`, and `omp`; each target has non-empty string `agentVersion` and `authRoute`, plus `local` and `public` objects containing ordered string-array `installActions`, logical normalized string `discoveryPath`, and string `invocationIdentity`. `commandScopeSha256` is the SHA-256 of RFC 8785 JCS for that matrix. The candidate also supplies a non-empty `fixtureResultSha256`, calculated from the known controlled result's raw bytes before assent, and initializes append-only `targetProofAuthority.publicProofAttempts: []`. Do not accept aliases or an extra marketplace-entry version.

Every independently launched shell derives, assigns, and exports in order `CUMPA_PHASE6_PRIVATE_ROOT="${XDG_STATE_HOME:-$HOME/.local/state}/cumpa/phase6-marketplace"`, `MARKETPLACE_CHECKOUT="$CUMPA_PHASE6_PRIVATE_ROOT/Ship-With-AI-skills"`, `TARGET_ROOT="$CUMPA_PHASE6_PRIVATE_ROOT/targets"`, and `TARGET_PROJECT="$CUMPA_PHASE6_PRIVATE_ROOT/project"` before use; it never inherits them from a prior task or shell. The mode-0600 `$CUMPA_PHASE6_PRIVATE_ROOT/operation.json` privately materializes target-specific project/profile mappings. Before each target command/session, derive its isolated mapping under those roots and apply the complete reviewed child-client environment from Plan 02 and the command matrix, without changing the orchestrator HOME or using guessed profile-variable aliases. Expanded paths and Claude's dispatched `/plugin marketplace add <literal-expanded-MARKETPLACE_CHECKOUT>` text stay private; candidate/evidence hashes use the logical matrix strings.

`06-TARGET-EVIDENCE.json` contains `kind: "cumpa.marketplace-target-proof/v1"`, immutable candidate bindings, and exactly four `targets` rows keyed as above. For target `name`, the row must equal `commandMatrix[name]` for `agentVersion`, `authRoute`, `local.discoveryPath`, and `local.invocationIdentity`; `installActionsSha256` must equal SHA-256 of RFC 8785 JCS for `commandMatrix[name].local.installActions` (equivalent to exact `JSON.stringify` for this string array). Missing or mismatched fields fail. Each `compatibleFixture.resultSha256` equals candidate `fixtureResultSha256`. `targetRowsSha256` equals SHA-256 of RFC 8785 JCS for the ordered array `["claude-code","codex","pi","omp"].map(name => [name, targets[name]])`, thereby binding every actual row observation, including invocation bindings and actual fixture result SHA. Missing/incompatible/compatible-fixture observations and the distinct real released-1.5.0 OMP readiness/abort smoke retain their existing meanings.

`targetProofAuthority.publicProofAttempts` replaces every old `publicProofOutcome` admission/status path. Each attempt contains the exact normalized `assent` object plus `scope.assentSha256`, candidate OID, installer integrity, `commandScopeSha256`, auth-prerequisite digest, public-action-scope digest, ordered `admittedActionIds` and their RFC-8785-JCS digest, attempted action IDs, state `in_progress|uncertain|terminal`, per-action observations/reconciliation, and terminal outcome. The public action set comprises anonymous public OID, reviewed-byte, plugin-version and effect observations followed by each target's ordered matrix install actions and invocation. Initial intent copies the original target-proof assent into `assent.kind: "original-target-proof"`; recovery intent copies the fresh Task 2 record into `assent.kind: "public-proof-recovery"` and binds prior-attempt and exact remaining-action digests. The array only grows; no entry is deleted or reused.

Only public-proof attempt index 0 may carry original-target-proof assent. Each later attempt carries exactly the immutable recovery fields kind, verbatimAssent, approver, source, recordedAt and scope, bound to the immediately preceding reconciled attempt. At admission, consume the single authorized targetProofAuthority.publicProofRecoveryAssent exactly once: atomically persist its state consumed, consumedAt and consumedByAttemptIndex together with the new in-progress entry and that entry's recoveryAssentConsumption {state: consumed, assentSha256, consumedAt, attemptIndex}. Preserve this marker in terminal history. The final verifier requires unique normalized recovery-assent digests, every marker/index/digest, and the current consumed assent to equal the latest historical recovery. Neither original assent on a later attempt nor a reused, unconsumed, stale or non-adjacent recovery assent can pass.

`06-PUBLICATION-EVIDENCE.json` retains its separate push `publicationAttempts` lifecycle, inspected `pushEffects`, anonymous public OID/bytes and collection version. Its exactly four `publicInstalls` rows consume the matching `commandMatrix[name].public` stage with the same exact `agentVersion`, `authRoute`, `installActionsSha256`, `discoveryPath`, and `invocationIdentity` predicates. `publicInstallRowsSha256` binds the ordered complete public rows. Fixture proof stays only in target evidence; release/public proof never relabels it as released behavior.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Consume exact assent and derive four-target proof from bound observations</name>
  <read_first>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-CONTEXT.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-RESEARCH.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-PATTERNS.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-02-SUMMARY.md</file>
  </read_first>
  <files>.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json, .planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json</files>
  <action>In every independent shell, derive, assign, and export all four deterministic roots in the exact order from `<interfaces>`; before each target operation materialize its isolated project/profile mapping privately and export only its profile variable. Reject a missing, non-mode-0600, malformed, stale, or mismatched operation journal. Recompute candidate OID, reviewed-file hashes, installer identity, RFC-8785-JCS `commandScopeSha256`, and the raw-byte expected `fixtureResultSha256`; fetch official remote `main` read-only and stop on drift. Require exact unconsumed attributed target authority and no obsolete `publicProofOutcome`. Before the first local installer/provider action, durably consume that authority in candidate and private journal. For each selected target independently, execute only `commandMatrix[name].local.installActions`; for Claude, expand `MARKETPLACE_CHECKOUT` to a literal private path before dispatch while retaining only the logical action in committed evidence. Observe exact target placement/invocation, agent version and auth route, and reject absent/mismatched values. Record `installActionsSha256`, logical normalized `discoveryPath`, `invocationIdentity`, actual exits and skill hashes. Run separate missing, incompatible `2.0.0`, and compatible-fixture `1.5.0` cases. Recompute the fixture result from actual raw result bytes and require its SHA-256 equals candidate `fixtureResultSha256`; include the complete rows in RFC-8785-JCS `targetRowsSha256`. Keep the fixture labelled `fixture-lifecycle`, not released proof. Then retain the real OMP smoke unchanged: through the installed OMP candidate skill, require actual `cumpa --version` exit/stdout `0`/`1.5.0`, create a disposable Git repository and strict full-OID request, run literal `env -u CMUX_WORKSPACE_ID cumpa < "$CUMPA_REQUEST" > "$CUMPA_RESULT"`, observe real loopback readiness, controlled SIGINT/target-native cancel, terminal nonzero/130, no Finish and no accepted export. Record terminal/uncertain failure without replay and clean only owned target/fixture state.</action>
  <acceptance_criteria>
    <criterion>Every independent shell derives/exports all four deterministic roots before use; every target uses an isolated private profile mapping, and expanded Claude slash text remains private.</criterion>
    <criterion>Exactly four local rows fail closed unless agentVersion, authRoute, installActionsSha256, discoveryPath and invocationIdentity equal the matching reviewed commandMatrix local stage.</criterion>
    <criterion>Every compatible fixture actual result SHA equals candidate fixtureResultSha256 and is bound with all row observations by targetRowsSha256; fixture and released proof remain distinct.</criterion>
    <criterion>Missing/incompatible branches stop before Git/review launch; the real installed OMP 1.5.0 smoke reaches readiness then controlled-aborts without Finish/export or Phase 7 acceptance.</criterion>
    <criterion>Authority is consumed before the first local call, obsolete publicProofOutcome is absent, and any failure/uncertainty blocks replay.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>CUMPA_PHASE6_PRIVATE_ROOT="${XDG_STATE_HOME:-$HOME/.local/state}/cumpa/phase6-marketplace"; export CUMPA_PHASE6_PRIVATE_ROOT; MARKETPLACE_CHECKOUT="$CUMPA_PHASE6_PRIVATE_ROOT/Ship-With-AI-skills"; export MARKETPLACE_CHECKOUT; TARGET_ROOT="$CUMPA_PHASE6_PRIVATE_ROOT/targets"; export TARGET_ROOT; TARGET_PROJECT="$CUMPA_PHASE6_PRIVATE_ROOT/project"; export TARGET_PROJECT; node --input-type=module <<'CUMPA_PLAN_CHECK'
const fs=await import('node:fs/promises');const {createHash}=await import('node:crypto');const c=JSON.parse(await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json','utf8'));const r=JSON.parse(await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json','utf8'));const journalPath=process.env.CUMPA_PHASE6_PRIVATE_ROOT+'/operation.json';const journal=JSON.parse(await fs.readFile(journalPath,'utf8'));const journalMode=(await fs.stat(journalPath)).mode&0o777;const names=['claude-code','codex','pi','omp'];const jcs=v=>v===null||typeof v!=='object'?JSON.stringify(v):Array.isArray(v)?'['+v.map(jcs).join(',')+']':'{'+Object.keys(v).sort().map(k=>JSON.stringify(k)+':'+jcs(v[k])).join(',')+'}';const shaBytes=v=>createHash('sha256').update(v).digest('hex');const shaJcs=v=>shaBytes(jcs(v));const same=(a,b)=>jcs(a)===jcs(b);const guidanceSha=shaBytes('npm install --global @shipwithai/cumpa@1.5.0\nRequires Node.js 24+ and Git 2.43.0+.');const current=Object.fromEntries(await Promise.all(c.reviewedPaths.map(async p=>[p,shaBytes(await fs.readFile(process.env.MARKETPLACE_CHECKOUT+'/'+p))])));const matrix=c.commandMatrix;const matrixShape=matrix&&Object.keys(matrix).sort().join(',')===names.slice().sort().join(',')&&names.every(n=>{const m=matrix[n];return Object.keys(m||{}).sort().join(',')==='agentVersion,authRoute,local,public'&&typeof m?.agentVersion==='string'&&m.agentVersion.length>0&&typeof m.authRoute==='string'&&m.authRoute.length>0&&['local','public'].every(k=>Object.keys(m[k]||{}).sort().join(',')==='discoveryPath,installActions,invocationIdentity'&&Array.isArray(m[k]?.installActions)&&m[k].installActions.every(a=>typeof a==='string')&&typeof m[k].discoveryPath==='string'&&m[k].discoveryPath.length>0&&typeof m[k].invocationIdentity==='string'&&m[k].invocationIdentity.length>0)});const rows=matrixShape&&names.every(n=>{const m=matrix[n],x=r.targets?.[n];return x?.bindings?.candidateOid===c.candidateOid&&x.bindings.installerIntegrity===c.installer.integrity&&x.agentVersion===m.agentVersion&&x.authRoute===m.authRoute&&x.installActionsSha256===shaJcs(m.local.installActions)&&x.discoveryPath===m.local.discoveryPath&&x.invocationIdentity===m.local.invocationIdentity&&same(x.observedSkillFileHashes,c.skillFileHashes)&&x.installExit===0&&x.agentExit===0&&Number.isInteger(x.missing?.preflightExit)&&x.missing.preflightExit!==0&&x.missing.guidanceSha256===guidanceSha&&x.missing.gitOrReviewLaunchObserved===false&&x.incompatible?.reportedVersion==='2.0.0'&&Number.isInteger(x.incompatible.preflightExit)&&x.incompatible.preflightExit!==0&&x.incompatible.guidanceSha256===guidanceSha&&x.incompatible.gitOrReviewLaunchObserved===false&&x.compatibleFixture?.reportedVersion==='1.5.0'&&x.compatibleFixture.preflightExit===0&&x.compatibleFixture.readinessObserved===true&&x.compatibleFixture.finishReleased===true&&x.compatibleFixture.processExit===0&&x.compatibleFixture.resultKind==='cumpa/export'&&x.compatibleFixture.resultSha256===c.fixtureResultSha256&&x.compatibleFixture.cleanupObserved===true});const s=r.releasedSmoke;const privateRootKeys=['CUMPA_PHASE6_PRIVATE_ROOT','MARKETPLACE_CHECKOUT','TARGET_ROOT','TARGET_PROJECT'];const privateValues=privateRootKeys.map(k=>journal.paths?.[k]);const privateMappingOk=privateRootKeys.every(k=>typeof process.env[k]==='string'&&process.env[k].length>0&&journal.paths?.[k]===process.env[k]);const serialized=JSON.stringify([c,r]);const bad=!privateMappingOk||/(operationId|privateJournal|\/Users\/|\/home\/|\/private\/|\/var\/folders\/|\/tmp\/)/.test(serialized)||privateValues.some(v=>typeof v==='string'&&serialized.includes(JSON.stringify(v).slice(1,-1)));if(journalMode!==0o600||r.kind!=='cumpa.marketplace-target-proof/v1'||Object.keys(r.targets||{}).sort().join(',')!==names.slice().sort().join(',')||r.bindings?.candidateOid!==c.candidateOid||r.bindings?.baselineOid!==c.baselineOid||r.bindings?.installerIntegrity!==c.installer.integrity||r.bindings?.commandScopeSha256!==c.commandScopeSha256||r.bindings?.fixtureResultSha256!==c.fixtureResultSha256||!matrixShape||c.commandScopeSha256!==shaJcs(matrix)||!/^[a-f0-9]{64}$/.test(c.fixtureResultSha256)||!same(current,c.reviewedFileHashes)||c.targetProofAuthority?.state!=='consumed'||!c.targetProofAuthority.consumedAt||c.targetProofAuthority.prepublicationOutcome!=='completed'||Object.hasOwn(c.targetProofAuthority,'publicProofOutcome')||!Array.isArray(c.targetProofAuthority.publicProofAttempts)||!rows||r.targetRowsSha256!==shaJcs(names.map(n=>[n,r.targets[n]]))||s?.target!=='omp'||s.versionExit!==0||s.observedVersion!=='1.5.0'||s.readinessObserved!==true||s.controlledAbortRequested!==true||!Number.isInteger(s.processExit)||s.processExit===0||![130,'nonzero'].includes(s.exitClass)||s.finishObserved!==false||s.acceptedExport!==false||s.cleanupObserved!==true||bad)process.exit(1)
CUMPA_PLAN_CHECK
    </automated>
  </verify>
  <done>Observation-derived prepublication proof covers all four targets plus one real 1.5.0 readiness/abort smoke; any failed or uncertain condition blocks publication.</done>
</task>
<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Inspect push effects and record one exact publication assent</name>
  <read_first>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-CONTEXT.md</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-RESEARCH.md</file>
  </read_first>
  <files>.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json, .planning/phases/06-independent-mit-marketplace-skill/06-PUBLICATION-EVIDENCE.json</files>
  <action>Begin with the same self-contained deterministic four-root derivation/export sequence and rerun the exact observation-derived target verifier. Re-fetch anonymous `main`; require baseline unless reconciling an exact prior push. Recompute candidate/proof/file/command digests and zero unresolved high findings. Inspect and bind all visible push hooks, workflow triggers, rules/protection, Pages/deployment integrations, webhooks/apps and other push reactions; incomplete visibility, unbounded effects, or a blocking protection stops authority. Keep push-attempt authorization separate from target public-proof intent. Create/update `06-PUBLICATION-EVIDENCE.json` with immutable bindings, expected effects and one pending push attempt; accept only the existing exact effects-bound publication assent. If `targetProofAuthority.publicProofAttempts` contains `in_progress` or `uncertain`, do not authorize silent continuation: first reconcile its admitted/attempted actions read-only. Already observed/applied calls are never replayed. If exact remaining public actions cannot be proven without calls, display their ordered IDs and RFC-8785-JCS digest and accept only an additional direct current-chat `AUTHORIZE PUBLIC PROOF RECOVERY` statement bound to candidate OID, installer integrity, command scope, auth-prerequisite digest, RFC-8785-JCS prior-attempt digest and exact ordered remaining-action IDs/digest. Durably record that normalized statement as `targetProofAuthority.publicProofRecoveryAssent` with `kind: "public-proof-recovery"`, verbatim assent, approver, source, timestamp and those exact scope fields; it authorizes only Task 3 copying it into one newly appended in-progress attempt for those remaining calls and never authorizes or replays a push. Do not inspect or modify credentials.</action>
  <acceptance_criteria>
    <criterion>All inspectable push effects/protection are bound; exact attributed publication assent remains separate from public-proof recovery authority.</criterion>
    <criterion>An interrupted/in-progress/uncertain public-proof attempt permits only read-only reconciliation or a fresh exact remaining-action assent; completed/possibly invoked calls and pushes cannot replay.</criterion>
    <criterion>Every durable assent binds the exact candidate, installer, command/auth/proof/effect or remaining-action scopes and no credential or private path.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>CUMPA_PHASE6_PRIVATE_ROOT="${XDG_STATE_HOME:-$HOME/.local/state}/cumpa/phase6-marketplace"; export CUMPA_PHASE6_PRIVATE_ROOT; MARKETPLACE_CHECKOUT="$CUMPA_PHASE6_PRIVATE_ROOT/Ship-With-AI-skills"; export MARKETPLACE_CHECKOUT; TARGET_ROOT="$CUMPA_PHASE6_PRIVATE_ROOT/targets"; export TARGET_ROOT; TARGET_PROJECT="$CUMPA_PHASE6_PRIVATE_ROOT/project"; export TARGET_PROJECT; node --input-type=module <<'CUMPA_PLAN_CHECK'
const fs=await import('node:fs/promises');const {createHash}=await import('node:crypto');const c=JSON.parse(await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json','utf8'));const tr=await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json');const p=JSON.parse(await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-PUBLICATION-EVIDENCE.json','utf8'));const names=['claude-code','codex','pi','omp'];const jcs=v=>v===null||typeof v!=='object'?JSON.stringify(v):Array.isArray(v)?'['+v.map(jcs).join(',')+']':'{'+Object.keys(v).sort().map(k=>JSON.stringify(k)+':'+jcs(v[k])).join(',')+'}';const shaBytes=v=>createHash('sha256').update(v).digest('hex');const shaJcs=v=>shaBytes(jcs(v));const a=p.publicationAttempts?.at(-1);const proofSha=shaBytes(tr);const effectsSha=shaJcs(p.pushEffects?.expected);const commandSha=shaBytes(a?.scope?.pushCommand||'');const matrix=c.commandMatrix;const proofActions=[{id:'public:anonymous-main',kind:'observe-public-oid',repository:'https://github.com/Ship-With-AI/skills.git'},{id:'public:reviewed-bytes',kind:'observe-reviewed-bytes',paths:c.reviewedPaths},{id:'public:plugin-version',kind:'observe-plugin-version',path:'.claude-plugin/plugin.json'},{id:'public:effects',kind:'observe-push-effects',expectedSha256:p.pushEffects?.expectedSha256}];const actionSet=[...proofActions,...names.flatMap(n=>[...matrix[n].public.installActions.map((action,index)=>({id:n+':install:'+index,target:n,kind:'install',action})),{id:n+':invoke',target:n,kind:'invoke',invocationIdentity:matrix[n].public.invocationIdentity}])];const actionById=new Map(actionSet.map(x=>[x.id,x]));const attempts=c.targetProofAuthority?.publicProofAttempts||[];const prior=attempts.at(-1);const q=c.targetProofAuthority?.publicProofRecoveryAssent;const remaining=q?.scope?.remainingActionIds;const attempted=new Set(attempts.flatMap(x=>x.attemptedActionIds||[]));const active=attempts.filter(x=>['in_progress','uncertain'].includes(x.state)&&x.reconciliation?.complete!==true);const recoveryOk=!q?active.length===0:(q.kind==='public-proof-recovery'&&q.source==='direct current-chat assent'&&q.state==='authorized'&&q.scope?.candidateOid===c.candidateOid&&q.scope?.installerIntegrity===c.installer.integrity&&q.scope?.commandScopeSha256===c.commandScopeSha256&&q.scope?.authPrerequisiteSha256===c.targetProofAuthority.authPrerequisiteSha256&&prior&&q.scope?.priorAttemptSha256===shaJcs(prior)&&Array.isArray(remaining)&&remaining.length>0&&remaining.every(id=>actionById.has(id)&&!attempted.has(id))&&q.scope?.remainingActionsSha256===shaJcs(remaining.map(id=>actionById.get(id)))&&q.verbatimAssent&&q.approver&&q.recordedAt);const bad=/(operationId|privateJournal|\/Users\/|\/home\/|\/private\/|\/var\/folders\/|\/tmp\/)/.test(JSON.stringify(p));if(c.commandScopeSha256!==shaJcs(matrix)||p.kind!=='cumpa.marketplace-publication/v1'||p.bindings?.candidateOid!==c.candidateOid||p.bindings?.targetProofSha256!==proofSha||p.pushEffects?.expectedSha256!==effectsSha||p.pushEffects.visibilityComplete!==true||!a||a.state!=='authorized'||a.consumedAt!==null||a.scope?.candidateOid!==c.candidateOid||a.scope?.effectsSha256!==effectsSha||a.scope?.targetProofSha256!==proofSha||a.scope?.pushCommandSha256!==commandSha||!a.verbatimAssent||!a.approver||a.source!=='direct current-chat assent'||!a.recordedAt||Object.hasOwn(c.targetProofAuthority,'publicProofOutcome')||!Array.isArray(c.targetProofAuthority.publicProofAttempts)||!recoveryOk||bad)process.exit(1)
CUMPA_PLAN_CHECK
    </automated>
    <human-check>Provide the effects-bound publication statement. Only when interrupted public proof leaves unproven remaining actions, separately provide the exact displayed recovery statement; neither authority broadens the other.</human-check>
  </verify>
  <resume-signal>When ambient push auth is unavailable, first: `PUSH AUTH READY: operator-owned temporary route for Ship-With-AI/skills; agent made no credential change; cleanup after attempt assured.` Then: `AUTHORIZE MARKETPLACE PUSH: repository https://github.com/Ship-With-AI/skills.git; baseline {baselineOid}; candidate {candidateOid}; refspec {candidateOid}:refs/heads/main; command sha256 {pushCommandSha256}; effects sha256 {effectsSha256}; target proof sha256 {targetProofSha256}; no other mutation.` Only after an interrupted public-proof attempt requires calls for unproven remaining actions: `AUTHORIZE PUBLIC PROOF RECOVERY: candidate {candidateOid}; installer integrity {installerIntegrity}; commands sha256 {commandScopeSha256}; auth prerequisites sha256 {authPrerequisiteSha256}; prior attempt sha256 {priorAttemptSha256}; remaining actions sha256 {remainingActionsSha256}; no push or replay.` Replace every braced token with the displayed exact value; no other wording authorizes execution.</resume-signal>
  <done>Exact push authority exists independently; any interrupted public proof either reconciles read-only or has fresh exact remaining-action assent, never silent replay.</done>
</task>
<task type="auto">
  <name>Task 3: Reconcile or apply once, then derive public proof and collateral effects</name>
  <read_first>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-PUBLICATION-EVIDENCE.json</file>
    <file>.planning/phases/06-independent-mit-marketplace-skill/06-CONTEXT.md</file>
    <file>docs/distribution-operations.md</file>
  </read_first>
  <files>.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json, .planning/phases/06-independent-mit-marketplace-skill/06-PUBLICATION-EVIDENCE.json</files>
  <action>In every independent shell, re-derive, assign, and export the four deterministic roots in order; before each target action materialize its isolated profile mapping privately and apply the complete reviewed child-client environment without changing the orchestrator HOME. Reconcile the private journal, latest push attempt and anonymous remote `main` before mutation. Preserve existing exact push lifecycle: candidate already public means read-only reconciliation and no push; baseline permits the literal non-force candidate-OID refspec only from one exact authorized unconsumed push attempt after durable consumption; consumed/terminal/uncertain baseline requires fresh Task 2 publication assent; other OIDs block. Once candidate publication is established, derive the ordered public-proof action set: anonymous public OID, reviewed-byte, plugin-version and approved-effect observations, then each `commandMatrix[name].public.installActions` plus its invocation identity. Before the first of those public-proof installer/provider calls, atomically append to candidate and private journal an `in_progress` `publicProofAttempts` entry containing the exact normalized original target assent or Task 2 recovery assent and binding its RFC-8785-JCS digest, candidate OID, installer integrity, command/auth scope, complete public-action-scope digest, exact admitted action IDs and their RFC-8785-JCS digest; when consuming a recovery assent, durably mark that pending assent consumed in the same write. If any prior attempt is `in_progress`/`uncertain`, make no new call: reconcile its exact attempted actions read-only, transition known facts durably, and either finish from observation or return to Task 2 for fresh assent over only unproven remaining actions. A recovery attempt must bind the exact reconciled prior-attempt digest and its admitted IDs/digest must equal that assent's remaining scope. Never replay a possibly invoked action or any push. Only attempt index 0 may copy original target-proof assent. Every later admission requires the single pending publicProofRecoveryAssent to be authorized and bound to the immediately preceding reconciled attempt. Copy only its six immutable normalized fields into attempt.assent; in the same durable write mark the pending assent consumed with consumedAt and consumedByAttemptIndex, and append recoveryAssentConsumption with state consumed, that assent digest, the same consumedAt and attemptIndex. Never admit the same normalized recovery assent twice. Before each admitted call, persist its attempted action ID; after it, persist the observation before the next call. Interruption stays `uncertain`; actual completion becomes `terminal` with explicit `completed|failed` outcome. Under the durable attempt, verify anonymous OID, reviewed hashes, plugin-manifest collection version and approved effects; do not require marketplace entry `version`. For each public target, execute only its admitted matrix public actions in a new isolated mapping; expand Claude's marketplace path only in private dispatched text. Require actual `agentVersion`, `authRoute`, `installActionsSha256`, logical normalized `discoveryPath`, `invocationIdentity`, exits and skill hashes to equal the reviewed stage. Invoke with `cumpa` absent and require exact D-04 guidance/nonzero/no launch. Compute RFC-8785-JCS `publicInstallRowsSha256` over all rows. Remove all `publicProofOutcome`-only admission/verification logic. Keep fixture evidence distinct, record Phase 7 as unrun, retain the private journal until automated verification completes, then clean only owned state.</action>
  <acceptance_criteria>
    <criterion>Every shell derives/exports its roots before use; every target stays isolated and Claude receives only a privately expanded literal marketplace path.</criterion>
    <criterion>Push recovery never replays; public-proof recovery makes only read-only observations until a fresh exact remaining-action assent exists.</criterion>
    <criterion>Only the first public-proof attempt may use original assent; every later attempt binds the immediate reconciled predecessor, a fresh single-use consumed recovery assent, and its durable consumption marker.</criterion>
    <criterion>Before any admitted public call, an append-only in-progress attempt is durable in candidate and private journal; its exact scope, attempted actions, reconciliation and terminal outcome remain auditable.</criterion>
    <criterion>Anonymous public OID, reviewed bytes, plugin collection version and observed effects equal candidate/approved values without any marketplace-entry version requirement.</criterion>
    <criterion>Exactly four public rows fail closed unless agentVersion, authRoute, installActionsSha256, discoveryPath, invocationIdentity, exits and hashes equal the matching commandMatrix public stage; publicInstallRowsSha256 binds all observations.</criterion>
    <criterion>No obsolete publicProofOutcome, private path/operation ID/credential/transcript, fixture-as-release claim, or Phase 7 acceptance appears.</criterion>
  </acceptance_criteria>
  <verify>
    <automated>CUMPA_PHASE6_PRIVATE_ROOT="${XDG_STATE_HOME:-$HOME/.local/state}/cumpa/phase6-marketplace"; export CUMPA_PHASE6_PRIVATE_ROOT; MARKETPLACE_CHECKOUT="$CUMPA_PHASE6_PRIVATE_ROOT/Ship-With-AI-skills"; export MARKETPLACE_CHECKOUT; TARGET_ROOT="$CUMPA_PHASE6_PRIVATE_ROOT/targets"; export TARGET_ROOT; TARGET_PROJECT="$CUMPA_PHASE6_PRIVATE_ROOT/project"; export TARGET_PROJECT; node --input-type=module <<'CUMPA_PLAN_CHECK'
const fs=await import('node:fs/promises');const {createHash}=await import('node:crypto');const {execFileSync}=await import('node:child_process');const c=JSON.parse(await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-MARKETPLACE-CANDIDATE.json','utf8'));const tr=await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-TARGET-EVIDENCE.json');const p=JSON.parse(await fs.readFile('.planning/phases/06-independent-mit-marketplace-skill/06-PUBLICATION-EVIDENCE.json','utf8'));const journal=JSON.parse(await fs.readFile(process.env.CUMPA_PHASE6_PRIVATE_ROOT+'/operation.json','utf8'));const names=['claude-code','codex','pi','omp'];const jcs=v=>v===null||typeof v!=='object'?JSON.stringify(v):Array.isArray(v)?'['+v.map(jcs).join(',')+']':'{'+Object.keys(v).sort().map(k=>JSON.stringify(k)+':'+jcs(v[k])).join(',')+'}';const shaBytes=v=>createHash('sha256').update(v).digest('hex');const shaJcs=v=>shaBytes(jcs(v));const same=(a,b)=>jcs(a)===jcs(b);const guidanceSha=shaBytes('npm install --global @shipwithai/cumpa@1.5.0\nRequires Node.js 24+ and Git 2.43.0+.');const proofSha=shaBytes(tr);const effectsSha=shaJcs(p.pushEffects?.observed);const remoteOid=execFileSync('git',['ls-remote','https://github.com/Ship-With-AI/skills.git','refs/heads/main'],{encoding:'utf8'}).trim().split(/\s+/)[0];const matrix=c.commandMatrix;const matrixShape=matrix&&Object.keys(matrix).sort().join(',')===names.slice().sort().join(',')&&names.every(n=>{const m=matrix[n];return Object.keys(m||{}).sort().join(',')==='agentVersion,authRoute,local,public'&&typeof m?.agentVersion==='string'&&m.agentVersion.length>0&&typeof m.authRoute==='string'&&m.authRoute.length>0&&['local','public'].every(k=>Object.keys(m[k]||{}).sort().join(',')==='discoveryPath,installActions,invocationIdentity'&&Array.isArray(m[k]?.installActions)&&m[k].installActions.every(a=>typeof a==='string')&&typeof m[k].discoveryPath==='string'&&m[k].discoveryPath.length>0&&typeof m[k].invocationIdentity==='string'&&m[k].invocationIdentity.length>0)});const proofActions=[{id:'public:anonymous-main',kind:'observe-public-oid',repository:'https://github.com/Ship-With-AI/skills.git'},{id:'public:reviewed-bytes',kind:'observe-reviewed-bytes',paths:c.reviewedPaths},{id:'public:plugin-version',kind:'observe-plugin-version',path:'.claude-plugin/plugin.json'},{id:'public:effects',kind:'observe-push-effects',expectedSha256:p.pushEffects?.expectedSha256}];const actionSet=matrixShape?[...proofActions,...names.flatMap(n=>[...matrix[n].public.installActions.map((action,index)=>({id:n+':install:'+index,target:n,kind:'install',action})),{id:n+':invoke',target:n,kind:'invoke',invocationIdentity:matrix[n].public.invocationIdentity}])]:[];const actionById=new Map(actionSet.map(x=>[x.id,x]));const actionScopeSha=shaJcs(actionSet);const rows=matrixShape&&names.every(n=>{const m=matrix[n],x=p.publicInstalls?.[n];return x?.agentVersion===m.agentVersion&&x.authRoute===m.authRoute&&x.installActionsSha256===shaJcs(m.public.installActions)&&x.discoveryPath===m.public.discoveryPath&&x.invocationIdentity===m.public.invocationIdentity&&x.installExit===0&&x.agentExit===0&&x.invocationObserved===true&&same(x.observedSkillFileHashes,c.skillFileHashes)&&Number.isInteger(x.missing?.preflightExit)&&x.missing.preflightExit!==0&&x.missing.guidanceSha256===guidanceSha&&x.missing.gitOrReviewLaunchObserved===false&&x.cleanupObserved===true});const authority=c.targetProofAuthority;const originalAssent={kind:'original-target-proof',verbatimAssent:authority?.verbatimAssent,approver:authority?.approver,source:authority?.source,recordedAt:authority?.recordedAt,scope:authority?.scope,authPrerequisiteSha256:authority?.authPrerequisiteSha256};const attempts=authority?.publicProofAttempts;const attempted=[];const attemptsOk=Array.isArray(attempts)&&attempts.length>0&&attempts.every((x,i)=>{const admitted=x.admittedActionIds;const expected=Array.isArray(admitted)&&admitted.every(id=>actionById.has(id))?shaJcs(admitted.map(id=>actionById.get(id))):null;if(Array.isArray(x.attemptedActionIds))attempted.push(...x.attemptedActionIds);const originalOk=i===0&&x.assent?.kind==='original-target-proof'&&same(x.assent,originalAssent);const recoveryOk=x.assent?.kind==='public-proof-recovery'&&x.assent.source==='direct current-chat assent'&&x.assent.scope?.candidateOid===c.candidateOid&&x.assent.scope?.installerIntegrity===c.installer.integrity&&x.assent.scope?.commandScopeSha256===c.commandScopeSha256&&x.assent.scope?.authPrerequisiteSha256===authority.authPrerequisiteSha256&&same(x.assent.scope?.remainingActionIds,admitted)&&x.assent.scope?.remainingActionsSha256===expected&&i>0&&attempts[i-1]?.reconciliation?.complete===true&&shaJcs(attempts[i-1])===x.assent.scope?.priorAttemptSha256&&Object.keys(x.assent).sort().join(',')==='approver,kind,recordedAt,scope,source,verbatimAssent'&&x.assent.verbatimAssent&&x.assent.approver&&x.assent.recordedAt&&x.recoveryAssentConsumption?.state==='consumed'&&x.recoveryAssentConsumption.assentSha256===shaJcs(x.assent)&&x.recoveryAssentConsumption.attemptIndex===i&&Number.isFinite(Date.parse(x.recoveryAssentConsumption.consumedAt));return ['terminal','uncertain'].includes(x.state)&&x.scope?.candidateOid===c.candidateOid&&x.scope?.installerIntegrity===c.installer.integrity&&x.scope?.commandScopeSha256===c.commandScopeSha256&&x.scope?.authPrerequisiteSha256===authority.authPrerequisiteSha256&&x.scope?.assentSha256===shaJcs(x.assent)&&x.scope?.publicActionScopeSha256===actionScopeSha&&x.admittedActionsSha256===expected&&Array.isArray(x.attemptedActionIds)&&x.attemptedActionIds.every(id=>admitted.includes(id))&&(originalOk||recoveryOk)&&(x.state!=='uncertain'||x.reconciliation?.readOnly===true)});const recoveryAttempts=Array.isArray(attempts)?attempts.filter(x=>x?.assent?.kind==='public-proof-recovery'):[];const pendingRecovery=authority?.publicProofRecoveryAssent;const normalizedRecovery=pendingRecovery&&{kind:pendingRecovery.kind,verbatimAssent:pendingRecovery.verbatimAssent,approver:pendingRecovery.approver,source:pendingRecovery.source,recordedAt:pendingRecovery.recordedAt,scope:pendingRecovery.scope};const lastRecovery=recoveryAttempts.at(-1);const recoveryUsageOk=recoveryAttempts.length===new Set(recoveryAttempts.map(x=>shaJcs(x.assent))).size&&(recoveryAttempts.length===0||(pendingRecovery?.state==='consumed'&&same(normalizedRecovery,lastRecovery.assent)&&pendingRecovery.consumedAt===lastRecovery.recoveryAssentConsumption?.consumedAt&&pendingRecovery.consumedByAttemptIndex===attempts.indexOf(lastRecovery)));const last=attempts?.at(-1);const noReplay=attempted.length===new Set(attempted).size;const completeActions=attempted.length===actionSet.length&&actionSet.every(x=>attempted.includes(x.id));const privateRootKeys=['CUMPA_PHASE6_PRIVATE_ROOT','MARKETPLACE_CHECKOUT','TARGET_ROOT','TARGET_PROJECT'];const privateValues=privateRootKeys.map(k=>journal.paths?.[k]);const privateMappingOk=privateRootKeys.every(k=>typeof process.env[k]==='string'&&process.env[k].length>0&&journal.paths?.[k]===process.env[k]);const serialized=JSON.stringify([c,p]);const bad=!privateMappingOk||Object.hasOwn(authority||{},'publicProofOutcome')||/(operationId|privateJournal|\/Users\/|\/home\/|\/private\/|\/var\/folders\/|\/tmp\/)/.test(serialized)||privateValues.some(v=>typeof v==='string'&&serialized.includes(JSON.stringify(v).slice(1,-1)));const a=p.publicationAttempts?.at(-1);if(!matrixShape||c.commandScopeSha256!==shaJcs(matrix)||p.kind!=='cumpa.marketplace-publication/v1'||p.bindings?.candidateOid!==c.candidateOid||p.bindings?.targetProofSha256!==proofSha||remoteOid!==c.candidateOid||p.observedPublicOid!==remoteOid||!same(p.observedReviewedFileHashes,c.reviewedFileHashes)||p.observedCollectionVersion!==c.collectionVersion.to||!same(p.pushEffects?.observed,p.pushEffects?.expected)||p.pushEffects.observedSha256!==effectsSha||effectsSha!==p.pushEffects.expectedSha256||!a||!['terminal','applied-reconciled'].includes(a.state)||a.outcome!=='candidate-public'||Object.keys(p.publicInstalls||{}).sort().join(',')!==names.slice().sort().join(',')||!rows||p.publicInstallRowsSha256!==shaJcs(names.map(n=>[n,p.publicInstalls[n]]))||!attemptsOk||!recoveryUsageOk||!noReplay||!completeActions||last.state!=='terminal'||last.outcome!=='completed'||p.phase7BrowserAcceptance!=='not-run-in-phase-6'||bad)process.exit(1)
CUMPA_PLAN_CHECK
    </automated>
  </verify>
  <done>The exact candidate is public; append-only intent/recovery proves four exact commandMatrix public rows without replay, old outcome-only authority, private-state disclosure, fixture/release conflation, Phase 7 overclaim or Phase 5 reuse.</done>
</task>
</tasks>

<threat_model>
Security enforcement: ASVS Level 1. Any unresolved high-severity finding blocks publication or success.

## Trust Boundaries

| Boundary | Description |
|---|---|
| Exact installer/agent binaries → isolated proof profiles | External executable code and provider access operate in temporary user-controlled scopes. |
| Installed skill → Git/PATH/process supervisor | Skill instructions can invoke local tools and long-running processes. |
| Cumpa stderr/stdout/exit → agent handoff | Readiness, diagnostics, canonical output, and completion must remain ordered and separated. |
| Reviewed local candidate → public `Ship-With-AI/skills` main | A remote mutation makes executable instructions public. |
| Public repository → fresh agent installation | Anonymous consumers must receive exact reviewed bytes rather than local/cached content. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-06-03-01 | Spoofing | target discovery/invocation | mitigate | Require each local/public row to equal the reviewed commandMatrix agentVersion, authRoute, install-action digest, logical discovery path and invocation identity; missing fields fail closed. |
| T-06-03-02 | Tampering / Elevation of Privilege | installer/provider use | mitigate | Consume exact local authority before use; before public calls append an in-progress attempt bound to exact admitted action and assent scopes. |
| T-06-03-03 | Tampering | PATH fixtures and request | mitigate | Bind every actual compatible-fixture raw result SHA to candidate fixtureResultSha256 and the complete targetRowsSha256; never label fixture lifecycle released proof. |
| T-06-03-04 | Repudiation | released Cumpa lifecycle | mitigate | Real 1.5.0 smoke binds strict request/readiness/controlled abort and accepts no export; successful fixture lifecycle requires exit before result read. |
| T-06-03-05 | Tampering | Pi combined tool stream | mitigate | Redirect only stdout to result, leave stderr in live partial rendering, omit timeout, and read result only after final exit. |
| T-06-03-06 | Denial of Service | orphaned long-running review | mitigate | Use target-native supervision, controlled abort for the bounded real smoke, owned-process cleanup and no automatic retry. |
| T-06-03-07 | Information Disclosure | profiles/private roots/evidence | mitigate | Every shell deterministically re-derives and exports all four roots, materializes only isolated target profiles privately, keeps expanded Claude text private, and rejects private values in evidence. |
| T-06-03-08 | Tampering / Repudiation | baseline/candidate/proof drift | mitigate | Recompute OIDs, installer, RFC-8785-JCS command/action scopes, file hashes, fixture equality and complete row digests rather than trusting status flags. |
| T-06-03-09 | Elevation of Privilege | publication authority | mitigate | A non-auto-approvable human-action checkpoint records exact attributed assent for one repository/OID/refspec/effect digest only. |
| T-06-03-10 | Tampering / Repudiation | push and recovery | mitigate | Persist push consumption before invocation; public-proof attempts separately persist intent/action observations, reconcile read-only, never replay, and require fresh exact assent for remaining calls. |
| T-06-03-11 | Elevation of Privilege / Denial of Service | push-triggered effects | mitigate | Inspect workflows/rules/Pages/deployments/hooks before assent; bind unavoidable effects or block, then reject any unapproved observed collateral action. |
| T-06-03-12 | Spoofing | public installability | mitigate | Resolve anonymously, compare exact public OID/file hashes/plugin version, and require all five matrix-binding fields plus actual exits/hashes for each target. |
| T-06-03-13 | Repudiation | proof scope | mitigate | Explicitly record that Phase 7 full browser acceptance and future stable 1.x compatibility proof were not performed here. |
| T-06-03-SC | Tampering | npm `skills` installer supply chain | mitigate | Preserve exact approved package/version/integrity, commandMatrix digest and per-stage install-action digests under scoped attempt authority; heuristic recency is not vulnerability evidence. |
</threat_model>

<source_coverage>
## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status |
|---|---|---|---|---|
| GOAL | — | Public independently MIT-licensed Cumpa skill delegates to separately installed released CLI | 01-03 | COVERED |
| REQ | SKL-01 | Installable public ShipWithAI marketplace skill with independent MIT license | 02-03 | COVERED |
| REQ | SKL-02 | Installed skill checks separate executable and stops with exact recovery/prerequisites | 01-03 | COVERED |
| REQ | SKL-03 | Thin delegation preserves Git/review/Finish/canonical output ownership | 02-03 | COVERED |
| RESEARCH | — | Dependency-free canonical stable SemVer gate with valid build metadata and arbitrary-precision comparison | 01 | COVERED |
| RESEARCH | — | Exact source-to-external-marketplace copy, fresh-baseline version transition and logical private-root mapping | 02 | COVERED |
| RESEARCH | — | Exact local/public command-auth matrix and four real target missing/incompatible/compatible fixture observations | 02-03 | COVERED |
| RESEARCH | — | One real installed 1.5.0 readiness/controlled-abort smoke, distinct from fixture lifecycle and Phase 7 | 03 | COVERED |
| RESEARCH | — | Single-use attributed assent, push-effect inspection, recovery without replay and anonymous public proof | 02-03 | COVERED |
| RESEARCH | — | ASVS 1, high-severity blocking, privacy-safe isolation, stream separation and bounded cleanup | 01-03 | COVERED |
| CONTEXT | D-01 | Existing `ship-with-ai` collection and selective skill | 02-03 | COVERED |
| CONTEXT | D-02 | Exact self-contained MIT license and attribution | 02-03 | COVERED |
| CONTEXT | D-03 | Stable >=1.5.0 <2.0.0, prereleases excluded | 01-03 | COVERED |
| CONTEXT | D-04 | Exact recovery command/prerequisites; no auto install/upgrade/npx fallback | 01-03 | COVERED |
| CONTEXT | D-05 | Policy versus 1.5.0 proof distinction | 01-03 | COVERED |
| CONTEXT | D-06 | Claude Code, Codex, Pi, OMP are distinct required targets | 02-03 | COVERED |
| CONTEXT | D-07 | Real target installation/discovery/prerequisite/lifecycle checks | 02-03 | COVERED |
| CONTEXT | D-08 | Native Git/Cumpa authority, supervised Finish/exit/result contract | 02-03 | COVERED |
| CONTEXT | D-09 | Runtime npm/Phase 7/no-new-behavior boundaries | 01-03 | COVERED |
| CONTEXT | D-10 | Local preparation then fresh exact scoped publication authority | 01-03 | COVERED |

Deferred DIST-02 lifecycle management and Phase 7 ACC-01..ACC-04 full public browser acceptance are excluded by source authority, not missing.
</source_coverage>

<verification>
Require exact commandMatrix-bound four-target local evidence, fixtureResultSha256 equality, complete targetRowsSha256 and the distinct real released-1.5.0 readiness/abort smoke before publication assent. Derive roots independently in every shell. Inspect/bind push effects and preserve exact push recovery. Before public calls append exact admitted-scope intent; reconcile interruptions read-only or obtain fresh remaining-action assent. Then require anonymous OID/bytes/plugin-version/effects proof and four exact public rows bound by publicInstallRowsSha256. Never use obsolete publicProofOutcome-only admission, replay a call/push, require marketplace entry.version, run Phase 7, or expose private state.
</verification>

<success_criteria>
- All four local rows exactly consume the reviewed commandMatrix fields and candidate fixtureResultSha256; complete row digests bind actual observations and missing fields fail closed.
- One installed OMP skill reaches real released Cumpa 1.5.0 readiness then controlled-aborts without Finish/export; fixture and release proof remain distinct.
- Push authority and append-only public-proof intent are separate, exact, durable, and recoverable only through read-only reconciliation or fresh remaining-action assent without replay.
- Public main, observed effects, plugin version and all four public rows match approved commandMatrix scopes; no extra marketplace entry version is required.
- Evidence contains no old publicProofOutcome path, credentials, private paths/operation IDs or receipts and preserves Phase 5/Phase 7 boundaries.
</success_criteria>

<output>
Create `.planning/phases/06-independent-mit-marketplace-skill/06-03-SUMMARY.md` when done.
</output>
