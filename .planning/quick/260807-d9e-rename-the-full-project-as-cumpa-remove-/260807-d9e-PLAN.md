---
phase: quick
plan: 260807-d9e
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - README.md
  - .claude/CLAUDE.md
  - scripts/build-native-addon.mjs
  - src/**
  - tests/**
  - .kimi-code/skills/spike-findings-cumpa/** -> .kimi-code/skills/spike-findings-cumpa/**
  - .planning/**
autonomous: true
requirements:
  - QUICK-260807-D9E
must_haves:
  truths:
    - "The product is branded Cumpa everywhere a developer can see its name."
    - "The private package, sole CLI command, generated executable, environment variables, protocol kinds, theme IDs, temporary names, persistence namespace, and internal identifiers use lowercase `cumpa` or uppercase `CUMPA` consistently."
    - "Repository-local drafts and exports live only under `.cumpa/`; no runtime path, ignore rule, inventory exclusion, path guard, receipt, UI copy, or test recognizes the old `.cumpa/` namespace."
    - "All source callers, API/client types, tests, fixtures, scripts, root metadata, documentation, project skill files, and tracked planning history move together with no alias, fallback, compatibility shim, or deprecated spelling."
    - "A case-insensitive byte scan of every tracked path and tracked file reports no `cumpa` substring, except an exact documented third-party integrity/content occurrence that cannot be edited without corrupting upstream data."
  artifacts:
    - path: "package.json"
      provides: "Private package identity and the single `cumpa` bin mapping"
      contains: '"name": "cumpa"'
    - path: "package-lock.json"
      provides: "Lockfile root and workspace package identity synchronized to `cumpa`"
      contains: '"name": "cumpa"'
    - path: "src/contracts/request.ts"
      provides: "Strict `cumpa.review-request` request discriminator"
      contains: "cumpa.review-request"
    - path: "src/contracts/draft.ts"
      provides: "Strict `cumpa/export` discriminators for all export schema versions"
      contains: "cumpa/export"
    - path: "src/server/draft-loader.ts"
      provides: "`.cumpa/drafts` storage authority"
      contains: ".cumpa/drafts"
    - path: "src/server/export-store.ts"
      provides: "`.cumpa/exports` publication and receipt authority"
      contains: ".cumpa/exports"
    - path: "src/web/index.html"
      provides: "Cumpa browser title"
      contains: "<title>Cumpa</title>"
    - path: ".kimi-code/skills/spike-findings-cumpa/SKILL.md"
      provides: "Renamed tracked project skill with unchanged source-picker findings"
      contains: "name: spike-findings-cumpa"
    - path: ".planning/quick/260807-d9e-rename-the-full-project-as-cumpa-remove-/260807-d9e-PLAN.md"
      provides: "Quick-task execution contract, sanitized during execution so planning artifacts also pass the residual gate"
  key_links:
    - from: "package.json"
      to: "dist/bin/cumpa.mjs"
      via: "the sole npm bin entry and existing build/package scripts"
      pattern: '"cumpa".*dist/bin/cumpa\.mjs'
    - from: "src/contracts/request.ts"
      to: "src/cli/request.ts"
      via: "strict request parsing and matching CLI diagnostics"
      pattern: "cumpa\.review-request"
    - from: "src/contracts/draft.ts"
      to: "src/export/review-export.ts"
      via: "all canonical export builders and fixtures use the new discriminator"
      pattern: "cumpa/export"
    - from: "src/server/draft-loader.ts"
      to: "src/server/export-store.ts"
      via: "one `.cumpa` managed root for drafts, exports, receipts, ignore status, and path safety"
      pattern: "\.cumpa/(drafts|exports)"
    - from: "src/git/ignore-status.ts"
      to: "src/web/components/GitignoreStatus.vue"
      via: "Cumpa-named ignore status types, routes, client methods, and user copy"
      pattern: "CumpaIgnore|cumpaIgnore|/\.cumpa/"
    - from: "src/web/monaco/theme.ts"
      to: "src/web/monaco/diff-adapter.ts"
      via: "`CUMPA_THEME`, `CUMPA_THEME_ID`, `applyCumpaTheme`, and `cumpa-dark`"
      pattern: "CUMPA_THEME|applyCumpaTheme|cumpa-dark"
---

<objective>
Rename the entire tracked legacy project and product to Cumpa/cumpa as one clean cutover.

Purpose: Make brand, package, CLI-adjacent identifiers, runtime protocols, storage, UI, tests, documentation, and tracked project history agree on one name without preserving the legacy identity.
Output: One atomic runtime-and-test cutover, one tracked documentation/path cutover, and exhaustive build/smoke/residual proof.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.claude/CLAUDE.md
@package.json
@package-lock.json
@README.md
@.gitignore
@.kimi-code/skills/spike-findings-cumpa/SKILL.md
@src/cli/run.ts
@src/contracts/request.ts
@src/contracts/draft.ts
@src/domain/comparison-key.ts
@src/git/ignore-status.ts
@src/server/draft-loader.ts
@src/server/export-store.ts
@src/server/gitignore-capability.ts
@src/web/index.html
@src/web/monaco/theme.ts

<interfaces>
- `package.json` formerly used the legacy package name; its only bin remains `cumpa` at `dist/bin/cumpa.mjs`, and both package plus lockfile root identities are `cumpa`.
- Runtime machine surfaces use `cumpa.review-request`, `cumpa/export`, `cumpa:back-to-base`, Monaco `cumpa-dark` plus `CUMPA_THEME*`, `inmemory://cumpa`, `.cumpa/drafts`, `.cumpa/exports`, `/.cumpa/`, `.cumpa/.cumpa-ignore-probe`, `cumpa-*-key-v1` hash domains, `cumpa-patch-*` and other temp prefixes, and `CUMPA_*` launch/test/native-build variables.
- Named source callers use `CumpaIgnoreStatusSchema`/`CumpaIgnoreStatus`, `InspectCumpaIgnoreOptions`, `inspectCumpaIgnore`, `appendCumpaIgnoreRule`, client `getCumpaIgnoreStatus`, `cumpaRoot`/`cumpaIdentity`, theme `applyCumpaTheme`, and neutral ordering identifiers such as `orderExactPaths`, `orderUtf16CodeUnits`, `orderReviewExportComments`, `orderProjectedFiles`, `orderPathIdentity`, and `orderComments`.
- The persistence namespace is enforced across contracts, draft/export stores, Git ignore inspection/appending, inventory exclusion, exact-patch reserved-path checks, receipt schemas, API routes/capabilities, Vue copy, and source-control snapshot helpers. Those boundaries use only `.cumpa`; the legacy namespace is ordinary repository content and is never reserved, excluded, resumed, migrated, or read as fallback.
- Focused search found rename content across 46 source files and 53+ test files, plus root metadata/docs, `scripts/build-native-addon.mjs`, the tracked `.kimi-code` spike skill, and 100+ current/archived `.planning` artifacts. Treat these groups as the complete search domain, not as a sample; edit only tracked files/paths returned by the case-insensitive manifest.
- Move the tracked spike-skill and quick-task directories to their `...-cumpa...` paths with history preservation, then update every inbound reference.
- Generic English and ordering uses also contain the legacy token. Rewrite prose to `contrast`, `diff`, `review`, `matching`, or another accurate term; rename ordering helpers to `order*`/`sort*` and reuse their current deterministic byte/code-unit logic. Replace native comparator spellings with the neutral deterministic ordering helpers rather than allowing source exceptions.
</interfaces>
</context>

<constraints>
- Work directly on the current `main` checkout; `workflow.use_worktrees=false`. Do not create a branch or worktree.
- Clean cutover only: no alias package/bin, legacy environment-variable fallback, deprecated protocol kind, storage migration, dual-read/dual-write, symlink, re-export, compatibility constant, or explanatory legacy comment.
- Use `Cumpa` for product/user-facing text, `cumpa` for package/bin/protocol/storage/theme/temp identifiers, and `CUMPA_*` for environment variables/constants. Keep the already-correct `cumpa` CLI and `dist/bin/cumpa.mjs` paths.
- Preserve every existing behavior, schema version, security boundary, hash construction shape, path validation rule, Git semantics, UI interaction, and test intent except the requested identity cutover. Changing domain-separation labels intentionally changes derived keys; update vectors and fixtures rather than adding compatibility behavior.
- Use the current implementations and tests; add no dependency, abstraction, migration framework, or generated compatibility layer.
- Interpret “all references” as every tracked path and textual file: source, tests, fixtures, scripts, package metadata/lockfile, runtime-facing/generated names in source, README, project rules/skill, debug/forensics/intel records, current and archived milestone artifacts, quick plans/summaries, and this plan. Exclude immutable Git commit history and external GitHub repository settings.
- Do not edit vendored/generated ignored output such as `node_modules/`, `dist/`, or Playwright results. Rebuild `dist/` only for verification; it is not a tracked rename target.
- A third-party exception is allowed only when the exact bytes are inside an upstream integrity/content field and editing them would corrupt verification. The current lockfile search shows only editable root-package occurrences, so expect zero exceptions. If execution discovers one, document exact file, JSON field/line, upstream owner, and corruption reason in the summary and hard-code only that exact occurrence into the residual checker; never exclude a whole file, directory, API name, source symbol, test, or planning artifact.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Cut runtime, package, persistence, API, UI, and tests over atomically</name>
  <files>package.json, package-lock.json, scripts/build-native-addon.mjs, src/** matching the legacy token, tests/** matching the legacy token</files>
  <behavior>
    - Package/build: package and lockfile root identities are `cumpa`; exactly one bin named `cumpa` points to `dist/bin/cumpa.mjs`; packaged help identifies Cumpa/cumpa and exposes no alias.
    - Protocol: strict requests accept only `cumpa.review-request`; export schemas/builders emit only `cumpa/export`; every caller and fixture uses the new literals.
    - Persistence: drafts, exports, receipts, ignore checks/appends, path exclusions, and exact-patch reserved paths use only `.cumpa`; the previous directory is ordinary repository content, not a hidden compatibility store.
    - Runtime identifiers: all `CUMPA_*`, temp/domain/theme/Monaco/action IDs, product diagnostics, exported types/functions, client methods, and deterministic ordering helpers are renamed with every caller.
    - Tests retain their existing behavioral coverage and use Cumpa/cumpa fixtures, temp roots, environment keys, schema values, snapshots, and expected copy.
  </behavior>
  <action>Start from a fresh case-insensitive manifest of tracked paths and tracked file bytes, then implement the runtime and test cutover in the same task so no caller or fixture is left on an intermediate contract.

Update `package.json` package name to `cumpa` while retaining exactly the existing `cumpa: dist/bin/cumpa.mjs` bin. Regenerate only lockfile metadata with the repository’s npm tooling so both top-level and root-package `package-lock.json` names are `cumpa` and dependency versions/integrities do not churn.

In source, replace user-visible legacy-name text with `Cumpa`. Cut machine contracts to `cumpa.review-request`, `cumpa/export`, `cumpa:back-to-base`, `cumpa-dark`, `inmemory://cumpa`, `.cumpa/drafts`, `.cumpa/exports`, `/.cumpa/`, `.cumpa/.cumpa-ignore-probe`, `cumpa-*-key-v1`, `cumpa-patch-*`, and `CUMPA_*`. Rename all exported/internal callers together, including the discovered ignore-status schema/types/functions and API/client methods, managed-root fields, inventory/path guards, Monaco constants/function/action IDs, and every remaining identifier containing the legacy token. Rename generic comparator helpers to accurate `order*` names and reuse the existing byte/code-unit loops; replace native comparator spellings without changing deterministic ordering semantics.

Update every matching test/fixture in `tests/api`, `tests/cli`, `tests/e2e`, `tests/git`, `tests/helpers`, `tests/integration`, `tests/package`, `tests/performance`, and `tests/unit`: request/export literals, `.cumpa` paths and receipts, Git ignore rules/probes, temp directory prefixes, `CUMPA_*` environment keys, theme exports/IDs, product copy, package identity, expected hash vectors, and helper symbol imports. Preserve negative/security cases by constructing a legacy-namespace sample from character codes/base64 where a no-fallback assertion needs it, so tests prove that it is not recognized without reintroducing the literal into tracked text. Update every caller/fixture rather than weakening assertions.
  <verify>
    <automated>
      <command expected="0">npm run build:runtime</command>
      <command expected="0">npm run typecheck:web</command>
      <command expected="0">node scripts/run-focused-vitest.mjs tests/cli/request.test.ts tests/unit/comparison-key.test.ts tests/unit/draft-load.test.ts tests/unit/monaco-theme.test.ts tests/unit/review-export.test.ts tests/unit/review-markdown.test.ts</command>
      <command expected="0">node scripts/run-focused-vitest.mjs tests/git/inventory.test.ts tests/git/ignore-status.test.ts tests/git/exact-patch.test.ts tests/api/draft.test.ts tests/api/export.test.ts tests/api/gitignore.test.ts</command>
    </automated>
  </verify>
  <done>Runtime, package metadata/lockfile, strict protocols, persistence, API/client symbols, UI brand/theme, environment/temp identifiers, deterministic ordering helpers, and all existing test callers use only Cumpa/cumpa, with no compatibility path or weakened contract.</done>
</task>

<task type="auto">
  <name>Task 2: Rename tracked documentation, project skill, planning history, and paths</name>
  <files>.gitignore, README.md, .claude/CLAUDE.md, tracked spike-skill path move to .kimi-code/skills/spike-findings-cumpa/**, .planning/** including the tracked quick-task path move to ...change-the-cumpa-cli-command-and-gener/** and this PLAN.md</files>
  <action>Update all tracked non-runtime surfaces returned by the same case-insensitive manifest. Change `.gitignore` to `.cumpa/`. Rewrite README branding, package references, commands, request/export examples, persistence paths, ignore guidance, and lifecycle wording to Cumpa/cumpa while preserving technical meaning.

Move the tracked spike-skill directory to `.kimi-code/skills/spike-findings-cumpa/` and use its Cumpa frontmatter name plus `.claude/CLAUDE.md` skill reference; retain all spike requirements, evidence, source scripts, and performance semantics. Move the tracked quick-task directory to its `...change-the-cumpa-cli-command-and-gener/` path and update inbound links such as STATE. Rewrite every matching tracked `.planning` artifact—including PROJECT, ROADMAP, milestones, archived phases, audits, summaries, research, context, patterns, reviews, security/UI/verification reports, debug/forensics/intel files, previous quick artifacts, and this plan—using Cumpa/cumpa for identity and accurate neutral prose.

This plan initially contains the legacy spelling because it is the execution contract. After its instructions are loaded and the other files are cut over, sanitize this plan too: retain the requirements but refer to the former identity as “the legacy name/token/namespace,” update examples and discovered paths to their Cumpa forms, and keep the final residual command encoded so the plan cannot match itself. Ensure the future SUMMARY uses only Cumpa/cumpa and “legacy name”; do not reintroduce the old token while describing the work.</action>
  <verify>
    <automated>node --input-type=module -e 'import { readFileSync } from "node:fs"; import { spawnSync } from "node:child_process"; const needle=Buffer.from([99,111,109,112,97,114,101]).toString("ascii"); const listed=spawnSync("git",["ls-files","-z"],{encoding:"buffer"}); if(listed.status!==0) throw new Error(listed.stderr.toString()); const files=listed.stdout.toString("utf8").split("\0").filter(Boolean); const scoped=files.filter((path)=>path==="README.md"||path===".gitignore"||path.startsWith(".claude/")||path.startsWith(".kimi-code/")||path.startsWith(".planning/")); const hits=scoped.filter((path)=>path.toLowerCase().includes(needle)||readFileSync(path).toString("latin1").toLowerCase().includes(needle)); if(hits.length) throw new Error(`legacy docs/paths remain:\n${hits.join("\n")}`); for(const path of [".kimi-code/skills/spike-findings-cumpa/SKILL.md",".planning/quick/260729-ohr-change-the-cumpa-cli-command-and-gener/260729-ohr-PLAN.md"]) if(!files.includes(path)) throw new Error(`missing renamed path: ${path}`);'</automated>
  </verify>
  <done>Root docs/rules, the project skill, all tracked current and archived planning evidence, this plan, and both legacy-token path trees use only Cumpa/cumpa or accurate neutral terminology; path moves preserve contents and inbound references.</done>
</task>

<task type="auto">
  <name>Task 3: Prove package, CLI, behavior, and zero tracked residuals</name>
  <files>None (verification only; fix the owning file in Task 1 or Task 2 if any gate fails)</files>
  <action>Run the existing build and behavioral suites after both cutovers. Smoke the generated CLI help and production artifact contract, confirm the package/lockfile/bin tuple, then run one exhaustive case-insensitive byte scan across every `git ls-files` path and content. The residual gate must report zero paths and zero contents. Do not allowlist Node/browser API names, source symbols, tests, docs, or planning files; use the exact third-party exception procedure from constraints only if immutable upstream integrity/content bytes make zero impossible. Fix findings at their owning surface and rerun until clean. Do not edit ignored generated output to make the scan pass.</action>
  <verify>
    <automated>
      <command expected="0">npm run build</command>
      <command expected="0">npm run verify:production-artifacts</command>
      <command expected="0">node --input-type=module -e 'import { spawnSync } from "node:child_process"; const result=spawnSync(process.execPath,["dist/bin/cumpa.mjs","--help"],{encoding:"utf8"}); if(result.status!==0) throw new Error(result.stderr); const text=result.stdout+result.stderr; if(!/Usage:\s+cumpa\b/u.test(text)) throw new Error(text); const needle=Buffer.from([99,111,109,112,97,114,101]).toString("ascii"); if(text.toLowerCase().includes(needle)) throw new Error(text);'</command>
      <command expected="0">npm run test:unit</command>
      <command expected="0">npm run test:git</command>
      <command expected="0">npm run test:api</command>
      <command expected="0">npm run test:browser</command>
      <command expected="0">npm run test:package</command>
      <command expected="0">npm run test:package-export-safety</command>
      <command expected="0">npm run test:package-contract</command>
      <command expected="0">npm run test:performance</command>
      <command expected="0">node --input-type=module -e 'import { readFileSync } from "node:fs"; import { spawnSync } from "node:child_process"; const needle=Buffer.from([99,111,109,112,97,114,101]).toString("ascii"); const listed=spawnSync("git",["ls-files","-z"],{encoding:"buffer"}); if(listed.status!==0) throw new Error(listed.stderr.toString()); const files=listed.stdout.toString("utf8").split("\0").filter(Boolean); const pathHits=files.filter((path)=>path.toLowerCase().includes(needle)); const contentHits=files.filter((path)=>readFileSync(path).toString("latin1").toLowerCase().includes(needle)); if(pathHits.length||contentHits.length) throw new Error(`legacy residuals\npaths:\n${pathHits.join("\n")}\ncontent:\n${contentHits.join("\n")}`); const pkg=JSON.parse(readFileSync("package.json","utf8")); const lock=JSON.parse(readFileSync("package-lock.json","utf8")); if(pkg.name!=="cumpa"||Object.keys(pkg.bin??{}).join(",")!=="cumpa"||pkg.bin.cumpa!=="dist/bin/cumpa.mjs"||lock.name!=="cumpa"||lock.packages?.[""]?.name!=="cumpa") throw new Error("package/lock/bin identity mismatch");'</command>
    </automated>
  </verify>
  <done>The full build, production artifact check, generated CLI help smoke, existing unit/Git/API/browser/package/performance suites, package-lock consistency assertion, and exhaustive tracked path/content scan all pass with zero undocumented residuals.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Feature / requirement | Task | Status |
|---|---|---|---|---|
| GOAL | QUICK-260807-D9E | Rename the entire tracked project/product to Cumpa/cumpa | 1, 2, 3 | COVERED |
| REQ | BRAND | User-visible CLI/server/UI/export/docs brand is Cumpa | 1, 2 | COVERED |
| REQ | PACKAGE-CLI | Package and lockfile names are `cumpa`; sole existing bin remains `cumpa` at `dist/bin/cumpa.mjs` | 1, 3 | COVERED |
| REQ | PROTOCOL | Request/export/action/theme/Monaco/domain/temp/env identifiers cut over with every caller and no aliases | 1, 3 | COVERED |
| REQ | STORAGE | `.cumpa` exclusively owns drafts, exports, receipts, ignore behavior, inventory exclusion, and path protection | 1, 3 | COVERED |
| REQ | TESTS | All existing tests and fixtures update atomically without weakening behavior | 1, 3 | COVERED |
| REQ | DOCS-PLANNING | README, project rules/skill, debug/intel/forensics, quick tasks, and current/archived planning artifacts are renamed | 2, 3 | COVERED |
| REQ | PATHS | Both discovered tracked legacy-token path trees move and all inbound references follow | 2, 3 | COVERED |
| REQ | RESIDUAL | Exhaustive case-insensitive tracked path/content scan is zero, with only exact corruption-preventing third-party exceptions documented | 2, 3 | COVERED |
| RESEARCH | — | No external research or dependency choice; package metadata, focused tracked searches, existing source/tests, STATE, project rules, and tracked spike skill are authoritative | — | N/A |
| CONTEXT | CLEAN-CUTOVER | No compatibility alias, migration, fallback, deprecated command, or old storage namespace | 1, 2, 3 | COVERED |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Agent JSON stdin → strict request parser | Protocol identity changes must remain strict and must not accept both names. |
| Git repository paths → managed persistence | `.cumpa` must be the only excluded/reserved/writable product tree; old paths must not bypass or inherit product authority. |
| Browser/API → draft/export capability | Renamed schemas, clients, routes, receipts, and UI must agree without weakening authentication or validation. |
| Package metadata → generated executable | The private package and sole bin must resolve to the same existing Cumpa executable with no shadow alias. |
| Historical planning evidence → executor | Mechanical prose replacement must preserve technical meaning and must not alter immutable Git history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260807-D9E-01 | Spoofing | Request/export protocol | mitigate | Change strict discriminators and every fixture together; retain one accepted Cumpa identity and no alias. |
| T-260807-D9E-02 | Tampering | Draft/export managed roots | mitigate | Rename storage joins, schema regexes, receipts, ignore rules, inventory filters, reserved-path checks, and symlink/identity guards as one atomic boundary. |
| T-260807-D9E-03 | Information disclosure | Old repository-local data | accept | Clean cutover intentionally does not read or migrate the old directory; Cumpa treats it as ordinary repository content. |
| T-260807-D9E-04 | Denial of service | Runtime callers and test harness env | mitigate | Rename all environment keys, exported symbols, clients, fixtures, and generated-launch helpers atomically, then run the existing full suites. |
| T-260807-D9E-05 | Repudiation | Tracked historical artifacts | mitigate | Rename tracked planning evidence while preserving its claims; leave immutable Git history untouched and prove tracked residuals mechanically. |
| T-260807-D9E-SC | Tampering | npm lockfile | mitigate | Add no package; regenerate only root lock metadata, preserve dependency versions/integrities, and run package/build contracts. |
</threat_model>

<verification>
Task 1 proves the load-bearing schema, storage, Git, UI-theme, package, and fixture seams. Task 2 proves root docs/rules, tracked planning artifacts, and path moves are clean. Task 3 builds and smokes the real generated Cumpa CLI, runs every existing behavior/package/performance suite, asserts package-lock consistency, and scans every tracked path and byte case-insensitively.
</verification>

<success_criteria>
- Product copy uses Cumpa; machine/package/CLI/storage/protocol/theme/temp identifiers use cumpa/CUMPA consistently.
- `package.json` and both lockfile root identities are `cumpa`; exactly one `cumpa` bin points to `dist/bin/cumpa.mjs`.
- Strict request and export schemas use only `cumpa.review-request` and `cumpa/export`; all callers and fixtures match.
- `.cumpa/` is the sole product-managed repository directory across persistence, receipts, ignore behavior, inventory, path security, API/UI, helpers, and docs; no old-directory migration or fallback exists.
- Every discovered symbol, environment variable, theme/action/model ID, temp/domain label, test fixture, root document, project skill, archived/current planning artifact, and matching tracked path is renamed without semantic erosion.
- Build, production artifacts, CLI help, all existing test/package/performance commands, and lockfile assertions pass.
- The final encoded, case-insensitive scan returns no legacy substring in any tracked path or file; any permitted third-party exception is exact, unavoidable, and documented rather than broadly excluded.
</success_criteria>

<output>
Create `.planning/quick/260807-d9e-rename-the-full-project-as-cumpa-remove-/260807-d9e-SUMMARY.md` after execution. Use only Cumpa/cumpa and the phrase “legacy name/token/namespace” in the summary, record path moves, package/protocol/storage cutovers, suite results, and the final residual count. Rerun the encoded residual check after creating the summary (and after it is included in the tracked commit set) so the summary cannot reintroduce the old token.
</output>
