---
phase: quick
plan: 260808-lo1
type: execute
wave: 1
depends_on: []
files_modified:
  - .kimi-code/skills/cumpa/SKILL.md
  - package.json
  - README.md
autonomous: true
requirements:
  - QUICK-260808-LO1
must_haves:
  truths:
    - "The repository owns the approved /cumpa skill as a project-local skill artifact."
    - "The skill delegates review authority to Cumpa's installed native CLI, accepts only canonical completed review JSON, reports the feedback, and never edits reviewed code."
    - "A future published cumpa npm package contains the skill file without adding an installer, lifecycle hook, dependency, or packaging framework."
    - "README guidance lets source-checkout users and future published-package consumers copy the skill into their agent skill directory without implying the currently private package is already published."
  artifacts:
    - path: ".kimi-code/skills/cumpa/SKILL.md"
      provides: "Approved /cumpa coding-agent workflow and embedded protocol invariants"
      contains: "name: cumpa"
    - path: "package.json"
      provides: "Existing npm files allowlist extended to ship the skill"
      contains: ".kimi-code/skills/cumpa/"
    - path: "README.md"
      provides: "Source-checkout and conditional future-package skill installation guidance"
      contains: "/cumpa"
  key_links:
    - from: "package.json"
      to: ".kimi-code/skills/cumpa/SKILL.md"
      via: "the existing npm files allowlist includes the project-local skill directory"
      pattern: "\\.kimi-code/skills/cumpa"
    - from: "README.md"
      to: ".kimi-code/skills/cumpa/SKILL.md"
      via: "installation commands copy the checked-in or globally installed package artifact into the consumer's skill directory"
      pattern: "skills/cumpa/SKILL\\.md"
    - from: ".kimi-code/skills/cumpa/SKILL.md"
      to: "cumpa"
      via: "the skill launches the native CLI with a strict request on stdin and consumes canonical JSON from stdout"
      pattern: "cumpa\.review-request|cumpa/export"
---

<objective>
Ship the approved `/cumpa` skill as a repository-owned, npm-packaged artifact with direct installation guidance.

Purpose: Make the existing native coding-agent review protocol reusable as a slash skill while keeping Cumpa itself authoritative for review, canonical output, and Git safety.
Output: One project-local `SKILL.md`, one additive npm `files` entry, and one focused README installation section; no source implementation, helper script, dependency, lockfile, generated artifact, or automated-edit behavior.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@package.json
@README.md
@.kimi-code/skills/spike-findings-cumpa/SKILL.md
@skill://cumpa

<interfaces>
- `package.json` currently publishes only the `dist/` allowlisted tree, maps the `cumpa` bin to `dist/bin/cumpa.mjs`, and uses `prepack` for the existing product build. Extend the allowlist; do not introduce a second packaging mechanism.
- `README.md` currently describes a private `cumpa@0.0.0` source setup using npm and explicitly says the package is not published. Preserve that truth while making future-package instructions conditional on publication.
- `.kimi-code/skills/spike-findings-cumpa/SKILL.md` establishes the project-local skill location and the standard `SKILL.md` frontmatter shape.
- The approved `skill://cumpa` artifact is the content authority. It defines strict revision and exact-patch requests, supervised CLI lifetime, exit/result acceptance, canonical feedback reporting, and the prohibition on code or Git mutation.
</interfaces>
</context>

<constraints>
- D-01: The skill is only a gate to the installed native `cumpa` CLI; it must not recreate Cumpa's review logic or independently review a diff.
- D-02: Accept and report a review only after exit `0` plus one non-empty parseable JSON object with `kind: "cumpa/export"`; failures, cancellation, empty stdout, or invalid JSON produce no invented feedback.
- D-03: The skill performs no automatic edits, staging, commits, pushes, ref changes, index changes, or worktree mutation.
- D-04: Embed the approved request, process, result, exact-patch, and protocol essentials in `SKILL.md`; add no helper script or dependency.
- D-05: Reuse the existing `.kimi-code/skills/<name>/SKILL.md`, npm `files`, npm global-root, and README shell-guidance conventions. Do not add an installer, postinstall hook, new bin, packaging framework, or duplicated skill copy.
- D-06: Modify only `.kimi-code/skills/cumpa/SKILL.md`, `package.json`, and `README.md`. Do not modify `src/`, `tests/`, `scripts/`, `dist/`, `package-lock.json`, configuration, or milestone files during implementation.
- Run only the two task-local read-only Node/npm smoke commands. Do not run formatters, linters, builds, typechecks, Vitest, Playwright, package test suites, or project-wide validation.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Check in the approved native-CLI gate skill</name>
  <files>.kimi-code/skills/cumpa/SKILL.md</files>
  <action>Create `.kimi-code/skills/cumpa/SKILL.md` beside the existing project-local skill and copy the approved `skill://cumpa` artifact without weakening or extending it, per D-01 through D-04. Preserve its frontmatter name `cumpa` and approved description.

The skill body must remain self-contained and retain these protocol essentials: run from the reviewed repository; require installed Cumpa and Git; obtain an ordered base/head only when missing; resolve full immutable OIDs and use the merge base for PR semantics; treat a registered worktree as its committed `HEAD`; write unique request/result files outside the repository; send the strict schema-v1 revisions request with optional ordered pathspecs; run `env -u CMUX_WORKSPACE_ID cumpa` under the harness process supervisor with request stdin, result stdout, and loopback URL/diagnostics on stderr; keep the process alive until Finish and exit; apply the D-02 acceptance rule; report summary markdown, open comments grouped by file with anchor and verification state, counts, drift/acknowledgement state, and resolved count; report an empty review honestly; remove temporary files only after consumption.

Retain exact-patch mode only for a user-supplied or explicitly requested patch, including its strict request shape, repository/worktree target, non-empty NUL-free UTF-8 and total 1 MiB request constraints. Retain the invariants that piped stdin selects agent mode, stdout is empty until successful Finish, revisions use pinned full OIDs with ancestor base, dirty worktree bytes are excluded from revision review, readiness is not completion, and Cumpa never applies or otherwise mutates reviewed source. Do not add application logic, shell helpers, templates, examples unrelated to the approved protocol, or any instruction to implement feedback automatically.</action>
  <verify>
    <automated>node --input-type=module -e 'import { readFileSync } from "node:fs"; const text = readFileSync(".kimi-code/skills/cumpa/SKILL.md", "utf8"); for (const token of ["name: cumpa", "cumpa.review-request", "cumpa/export", "env -u CMUX_WORKSPACE_ID", "summary.markdown", "Native exact-patch mode", "Protocol invariants", "do not edit code"]) if (!text.includes(token)) throw new Error(`missing approved skill token: ${token}`);'</automated>
  </verify>
  <done>The checked-in skill matches the approved `/cumpa` native-CLI gate, embeds the complete invocation and canonical-result contract, reports review feedback without inventing it, and contains no code-editing or Git-mutation workflow.</done>
</task>

<task type="auto">
  <name>Task 2: Package the skill and document direct installation</name>
  <files>package.json, README.md</files>
  <action>Extend only the existing `package.json.files` allowlist with `.kimi-code/skills/cumpa/`, preserving `dist/`, the existing `bin`, `prepack`, scripts, dependency metadata, `private` state, and every other package field per D-05. Do not touch `package-lock.json`: a `files` allowlist change does not alter the dependency graph. Do not add an install script, lifecycle hook, second executable, generated copy, or package dependency.

Add one concise README subsection near the existing install guidance for installing the `/cumpa` coding-agent skill. Preserve the statement that `cumpa@0.0.0` is currently private and unpublished. Give a source-checkout command that creates `$HOME/.agents/skills/cumpa` and copies `.kimi-code/skills/cumpa/SKILL.md` there. Give a clearly conditional future-published-package path: after `npm install --global cumpa`, copy `$(npm root --global)/cumpa/.kimi-code/skills/cumpa/SKILL.md` to `$HOME/.agents/skills/cumpa/SKILL.md`, then reload/restart the coding-agent session so `/cumpa` is discovered. State that users of another compatible agent may substitute that agent's configured skill directory. Keep installation manual and transparent; do not claim npm automatically registers the skill or that the private package can already be installed from the registry.

Run the smoke command below with `--ignore-scripts` and `--dry-run`; it must inspect npm's actual publish manifest without building or creating a tarball. If the skill path is absent, correct only the `files` allowlist syntax until npm includes the exact checked-in artifact; do not solve it with a copy/build script.</action>
  <verify>
    <automated>node --input-type=module -e 'import { readFileSync } from "node:fs"; import { spawnSync } from "node:child_process"; const pkg = JSON.parse(readFileSync("package.json", "utf8")); if (!pkg.files.includes("dist/") || !pkg.files.includes(".kimi-code/skills/cumpa/")) throw new Error("package files allowlist is incomplete"); const readme = readFileSync("README.md", "utf8"); for (const token of ["/cumpa", ".kimi-code/skills/cumpa/SKILL.md", "npm root --global", "$HOME/.agents/skills/cumpa/SKILL.md"]) if (!readme.includes(token)) throw new Error(`missing install guidance token: ${token}`); const npm = process.platform === "win32" ? "npm.cmd" : "npm"; const result = spawnSync(npm, ["pack", "--json", "--dry-run", "--ignore-scripts"], { encoding: "utf8" }); if (result.status !== 0) throw new Error(result.stderr || "npm pack dry run failed"); const [packed] = JSON.parse(result.stdout); const paths = new Set(packed.files.map((file) =&gt; file.path)); for (const path of [".kimi-code/skills/cumpa/SKILL.md", "dist/bin/cumpa.mjs", "README.md", "package.json"]) if (!paths.has(path)) throw new Error(`missing packed artifact: ${path}`);'</automated>
  </verify>
  <done>The npm publish manifest contains the exact approved skill alongside the existing CLI artifacts, README users have truthful source and future-package copy instructions, and the change adds no installer, lifecycle behavior, dependency, lockfile update, helper, or implementation edit.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Feature / requirement | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | QUICK-260808-LO1 | Store the approved `/cumpa` skill in the repository and make it installable from future published releases | 1, 2 | COVERED | One canonical project-local artifact, npm allowlist wiring, and install guidance |
| REQ | SKILL-ARTIFACT | Project skill follows the existing local `SKILL.md` convention | 1 | COVERED | Uses `.kimi-code/skills/cumpa/SKILL.md` beside the existing project skill |
| REQ | PACKAGE-INSTALL | Future npm tarballs contain the skill and consumers receive a concrete installation path | 2 | COVERED | Existing `files` allowlist plus source/future-global copy commands |
| REQ | INSTALL-SMOKE | Verification defends actual package inclusion without a build or broad suite | 2 | COVERED | `npm pack --json --dry-run --ignore-scripts` manifest assertion |
| RESEARCH | — | No research, dependency selection, or packaging-framework choice | — | N/A | Locked quick-mode constraint; existing npm packaging is authoritative |
| CONTEXT | D-01 | Gate the native Cumpa CLI; do not recreate review logic | 1 | COVERED | Approved skill copied without extension |
| CONTEXT | D-02 | Consume and report canonical completed review JSON only | 1 | COVERED | Exit, parse, kind, and empty/failure behavior retained |
| CONTEXT | D-03 | Perform no automatic source or Git edits | 1 | COVERED | Explicit skill boundary and acceptance condition |
| CONTEXT | D-04 | No helper scripts; embed protocol essentials | 1, 2 | COVERED | One self-contained skill and direct npm allowlist |
| CONTEXT | D-05 | Reuse existing package/build/install conventions | 2 | COVERED | Additive `files` entry and README npm/manual-copy guidance |
| CONTEXT | D-06 | Planning/execution scope excludes source implementation and broad validation | 1, 2 | COVERED | Only three documentation/metadata artifacts; two focused smoke checks |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Skill instructions → native Cumpa process | The skill must not acquire review or repository-mutation authority beyond the installed CLI protocol. |
| Cumpa stdout → coding agent report | Only a successfully completed canonical JSON object may be treated as accepted feedback. |
| npm package → consumer skill directory | The installed file must be the checked-in approved skill, not generated or fetched during installation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-260808-LO1-01 | Elevation of privilege / Tampering | `.kimi-code/skills/cumpa/SKILL.md` | mitigate | Per D-01 and D-03, keep the skill a native-CLI gate and prohibit review logic, automatic edits, and Git mutation. |
| T-260808-LO1-02 | Spoofing / Tampering | Canonical stdout handoff | mitigate | Per D-02, require exit zero, one non-empty parseable object, and `kind: "cumpa/export"`; otherwise report no accepted review. |
| T-260808-LO1-03 | Tampering | npm package contents | mitigate | Assert the exact checked-in skill path in npm's dry-run publish manifest with scripts disabled. |
| T-260808-LO1-SC | Tampering | Package supply chain | accept | No package is installed, no dependency changes, and no lifecycle/helper script is added; npm only computes the local dry-run manifest. |
</threat_model>

<verification>
Run only the two task-local automated commands. The first checks the approved skill's required identity and protocol markers. The second checks the additive npm allowlist, README install paths, and actual dry-run publish manifest with lifecycle scripts disabled. Do not run a formatter, linter, build, typecheck, test suite, package test, browser scenario, or project-wide validation command.
</verification>

<success_criteria>
- `.kimi-code/skills/cumpa/SKILL.md` is the single repository-owned copy of the approved `/cumpa` skill and follows the existing project-local skill convention.
- The skill launches Cumpa's native protocol, waits for real completion, validates canonical JSON, reports the approved feedback fields, and never edits source or Git state.
- `package.json.files` preserves `dist/` and includes `.kimi-code/skills/cumpa/` without any helper, lifecycle hook, dependency, second bin, lockfile, or generated copy.
- README retains the current unpublished-package warning and provides executable source-checkout plus conditional future-global-package copy guidance.
- The focused npm dry-run manifest contains `.kimi-code/skills/cumpa/SKILL.md`, `dist/bin/cumpa.mjs`, `README.md`, and `package.json`; no build or test runs during execution.
</success_criteria>

<output>
Create `.planning/quick/260808-lo1-ship-the-cumpa-coding-agent-skill-with-t/260808-lo1-SUMMARY.md` after execution with the implementation commit hash and both focused smoke-check results.
</output>
