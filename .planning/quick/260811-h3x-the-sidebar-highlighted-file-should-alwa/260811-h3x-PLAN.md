---
phase: quick
plan: 260811-h3x
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/components/FileTree.vue
  - tests/integration/anchored-workspace.spec.ts
autonomous: true
requirements:
  - QUICK-260811-H3X
must_haves:
  truths:
    - "Clicking Next file changes the comparison in view and leaves exactly that file highlighted in the Changed files sidebar."
    - "Clicking Previous file changes the comparison in view and returns the sidebar highlight to that same file."
    - "Parent-driven file navigation synchronizes sidebar selection without replaying a child selection event or resetting the tree's focus and expanded directories."
  artifacts:
    - path: "src/web/components/FileTree.vue"
      provides: "Reactive parent-to-sidebar selection synchronization through the existing FileTreeModel transition"
      contains: "model.value.selectFile(fileId)"
    - path: "src/web/model/file-tree.ts"
      provides: "Existing non-emitting selection transition that preserves focus and expanded directory state"
      contains: "selectFile"
    - path: "tests/integration/anchored-workspace.spec.ts"
      provides: "Observable Next-file and Previous-file sidebar-highlight regression coverage in the existing two-file browser fixture"
      contains: "diff navigation and session state"
  key_links:
    - from: "src/web/App.vue"
      to: "src/web/components/FileTree.vue"
      via: "App's synchronous selectedFile update flows through initialSelectedFileId"
      pattern: "initial-selected-file-id=\"selectedFile\\?\\.fileId\""
    - from: "src/web/components/FileTree.vue"
      to: "src/web/model/file-tree.ts"
      via: "the prop watcher updates the current model with its existing selectFile transition"
      pattern: "watch[\\s\\S]*initialSelectedFileId[\\s\\S]*selectFile"
    - from: "tests/integration/anchored-workspace.spec.ts"
      to: "src/web/components/FileTree.vue"
      via: "Next file and Previous file clicks assert the visible heading and sole aria-selected tree item name the same file"
      pattern: "aria-selected"
---

<objective>
Keep the Changed files sidebar highlight synchronized with the file currently in view for both visible file-navigation buttons.

Purpose: Remove the split source-of-truth behavior in which App advances the viewed file while FileTree retains its earlier internal selection.
Output: One direct parent-to-child synchronization seam and focused bidirectional Playwright regression coverage.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/debug/resolved/next-file-sidebar-highlight.md
@package.json
@src/web/App.vue
@src/web/components/FileTree.vue
@src/web/model/file-tree.ts
@src/web/components/FileRow.vue
@tests/integration/anchored-workspace.spec.ts

<interfaces>
- `App.vue` is authoritative for the viewed file: workspace navigation emits `load-file`, `loadFile()` synchronously assigns `selectedFile`, and the sidebar receives `selectedFile?.fileId` through `initialSelectedFileId`.
- `FileTree.vue` owns a presentation model whose `selectedFileId` drives both `FileRow`'s `aria-selected` value and selected styling. Parent synchronization must update this model directly rather than emit `select` back to App.
- `FileTreeModel.selectFile(fileId)` already provides the smallest state transition: it changes selection while preserving `focusedRowId` and `expandedDirectoryIds`. Reuse it; do not add another state owner, event, composable, or navigation path.
- `tests/integration/anchored-workspace.spec.ts` already has a deterministic two-file `diff navigation and session state` scenario and the exact visible Next/Previous controls. Extend that scenario rather than creating a fixture or test file.
- `.planning/STATE.md` reports no active milestone blocker; this remains an isolated quick fix and does not alter milestone scope or ROADMAP.md.
</interfaces>
</context>

<constraints>
- Modify only `src/web/components/FileTree.vue` and `tests/integration/anchored-workspace.spec.ts` during execution; `App.vue`, `src/web/model/file-tree.ts`, `FileRow.vue`, contracts, styles, and navigation reducer behavior remain unchanged.
- Reuse `FileTreeModel.selectFile`; do not rename the prop, add a second selection ref, emit parent synchronization back through `select`, reset the tree model, move focus, expand directories, or add dependencies.
- Cover the visible Next file and Previous file buttons specifically. Preserve unrelated keyboard-navigation coverage.
- Run only the one focused Playwright scenario plus the described browser smoke during execution. Skip formatters, linters, typechecks, builds, broad suites, and unrelated cleanup.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Confirm the synchronization root cause and lock the bidirectional browser contract</name>
  <files>tests/integration/anchored-workspace.spec.ts</files>
  <behavior>
    - The initial first-file heading and the only `aria-selected="true"` file tree item identify `src/first.ts`.
    - Clicking the visible Next file button changes both the heading and the only selected sidebar file to `src/second.ts`.
    - Clicking the visible Previous file button changes both the heading and the only selected sidebar file back to `src/first.ts`.
  </behavior>
  <action>Before changing production code, trace the established path documented in the context: `nextFile()`/`previousFile()` dispatch workspace events, `load-file` calls `App.loadFile()`, `selectedFile` changes synchronously, and `FileTree` alone owns the stale internal `selectedFileId`. Confirm that the row's highlight and `aria-selected` both derive from that model so the fix belongs at the App-to-FileTree prop boundary, not in the reducer, async content loader, CSS, or individual navigation handlers.

Extend the existing `diff navigation and session state` scenario in place. Keep the current two-file fixture and visible Next file click. Assert after each navigation that the comparison heading and the sole file tree item with `aria-selected="true"` identify the same path. Replace the return shortcut in this local journey with the visible Previous file button so both reported controls are covered, while leaving keyboard coverage elsewhere intact. Do not create a separate fixture, component test harness, broad navigation matrix, or source-text assertion.</action>
  <verify>
    <automated>npx playwright test tests/integration/anchored-workspace.spec.ts --grep "diff navigation and session state"</automated>
    <expected>On the buggy baseline, the focused scenario fails after Next because the heading is `src/second.ts` while the sidebar still selects `src/first.ts`. The recorded failing-before evidence in `.planning/debug/resolved/next-file-sidebar-highlight.md` is acceptable when the current checkout already contains another in-progress synchronization edit; do not remove user work solely to recreate RED.</expected>
  </verify>
  <done>The existing focused scenario expresses the complete observable contract for initial selection, Next file, and Previous file, and the root cause is confirmed at the parent-prop-to-FileTree-model boundary before implementation is touched.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Synchronize FileTree selection from the viewed file</name>
  <files>src/web/components/FileTree.vue</files>
  <behavior>
    - Every defined `initialSelectedFileId` received from App becomes the FileTree model's selected file immediately and on later prop changes.
    - Parent synchronization changes only selection; it does not emit `select`, move tree focus, or discard expanded-directory state.
    - The focused browser scenario is green for both Next file and Previous file.
  </behavior>
  <action>At the existing prop boundary in `FileTree.vue`, keep or establish one immediate watcher for `props.initialSelectedFileId`. When the value is defined, replace the current model with `model.value.selectFile(fileId)`. Call the existing immutable model transition directly so the sidebar marker updates without `applyModel()`, which would emit `select` and risk a feedback loop or move focus. Do not reset the model with `createFileTreeModel`, change App/workspace navigation, duplicate `selectFile` in the component, or alter row rendering and CSS. If the watcher is already present from concurrent user work, retain the single implementation and make no duplicate production edit.</action>
  <verify>
    <automated>npx playwright test tests/integration/anchored-workspace.spec.ts --grep "diff navigation and session state"</automated>
    <expected>The one Chromium scenario passes: initial selection is first, Next selects and shows second, and Previous selects and shows first.</expected>
    <human-check>Smallest smoke: open any review with at least two reviewable files, click Next file once and Previous file once, and confirm after each click that the blue sidebar highlight follows the path shown in the comparison heading without moving keyboard focus unexpectedly.</human-check>
  </verify>
  <done>The FileTree model follows App's current file through its existing non-emitting selection transition, and the focused automated check plus two-click smoke prove synchronized highlighting in both directions.</done>
</task>

</tasks>

<source_audit>
| Source | Item | Covered by | Status |
|---|---|---|---|
| GOAL | Sidebar highlight always identifies the file currently in view | Task 2 direct prop-to-model synchronization and Task 1 observable assertions | COVERED |
| REQ | QUICK-260811-H3X: Next and Previous buttons keep the sidebar selection synchronized | Task 1 exercises both visible buttons; Task 2 fixes their shared parent-driven selection seam | COVERED |
| RESEARCH | No external research or package choice is needed | Level 0: current Vue watcher, immutable FileTree model, and Playwright fixture are sufficient | COVERED |
| CONTEXT | Reuse current-file, sidebar-selection, and workspace-navigation patterns from the current checkout and STATE.md | Tasks preserve App/workspace authority and reuse `FileTreeModel.selectFile` without milestone or ROADMAP changes | COVERED |
| ACCEPTANCE | Focused failing-before/fixed-after check and smallest smoke test | Task 1 records RED contract; Task 2 reruns it GREEN and performs one Next/Previous smoke | COVERED |
| ACCEPTANCE | Planning only changes the contracted PLAN.md | Planner validation only; product/test execution is deferred to the executor | COVERED |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| App current-file state → FileTree presentation model | The authoritative viewed file crosses into independently stored sidebar UI state. |
| Navigation control → accessibility selection state | A visible file change must not leave assistive technology reporting a different selected row. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-QUICK-260811-H3X-01 | Spoofing | FileTree selected row | mitigate | Synchronize from App's authoritative file ID and assert one `aria-selected="true"` row matches the heading after both controls. |
| T-QUICK-260811-H3X-02 | Denial of Service | FileTree focus and expansion | mitigate | Use the existing `selectFile` transition directly; do not rebuild the tree, emit a feedback event, or move focus. |
| T-QUICK-260811-H3X-SC | Tampering | package supply chain | accept | No dependency, package manifest, or lockfile change is allowed. |
</threat_model>

<verification>
1. Failing-before: use the recorded RED result or run `npx playwright test tests/integration/anchored-workspace.spec.ts --grep "diff navigation and session state"` before the synchronization edit; the buggy state shows the second heading while the first sidebar row remains selected.
2. Fixed-after: run the same focused command after synchronization; it must pass the initial, Next file, and Previous file heading/sidebar pairs.
3. Smoke only the real two-click journey described in Task 2. Do not run formatters, linters, typechecks, builds, broad tests, or unrelated checks.
4. Confirm execution changes only `src/web/components/FileTree.vue`, `tests/integration/anchored-workspace.spec.ts`, and generated quick-task bookkeeping; ROADMAP.md and unrelated product code remain untouched.
</verification>

<success_criteria>
- Next file shows and highlights the second file, with exactly one selected tree item.
- Previous file shows and highlights the first file, with exactly one selected tree item.
- Sidebar synchronization reuses `FileTreeModel.selectFile` and does not add state, events, dependencies, focus changes, expansion resets, or navigation-handler special cases.
- The same focused Playwright scenario supplies failing-before and passing-after evidence, and the smallest manual Next/Previous smoke agrees with it.
- ROADMAP.md is unchanged and the fix remains limited to the existing FileTree and browser-test seams.
</success_criteria>

<output>
Create `.planning/quick/260811-h3x-the-sidebar-highlighted-file-should-alwa/260811-h3x-SUMMARY.md` when execution completes.
</output>
