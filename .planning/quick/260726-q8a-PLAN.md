---
phase: quick
plan: 260726-q8a
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/main.ts
  - src/web/prototypes/Phase6DiffSemanticsPrototype.vue
autonomous: true
requirements:
  - QUICK-260726-Q8A
  - DIFF-01
  - DIFF-02
  - DIFF-03
  - DIFF-04
  - DIFF-05
must_haves:
  truths:
    - "Opening the normal Vite URL without `?prototype=phase6` preserves the existing Diff Review `App.vue` branch and does not render prototype content."
    - "Opening `?prototype=phase6&variant=quiet-rails`, `?prototype=phase6&variant=signed-gutters`, or `?prototype=phase6&variant=layered-overlap` renders a clearly throwaway Phase 6 mockup inside the existing Diff Review shell."
    - "The mockup presents three structurally different visual treatments for coordinated Monaco diff semantics while covering DIFF-01 through DIFF-05."
    - "Each variant implements D-01 through D-08 from Phase 06 context: restrained line fills, stronger intraline emphasis, non-color signed Base/Head cues, continuous multi-line bars, quiet unchanged/empty treatment, composable overlap states, selection edge, active-line edge, hover/comment affordance, persistent anchor rail, and separate focus ring."
    - "The floating switcher updates `?variant=`, supports previous/next click and ArrowLeft/ArrowRight keyboard cycling without intercepting editable controls, and remains development-only."
    - "The prototype has no persistence, no server mutations, no dependency changes, no tests, no formatter/linter/build requirement, and an explicit cleanup boundary to delete or absorb it after the visual decision."
  artifacts:
    - path: "src/web/main.ts"
      provides: "Development-only URL gate that mounts the throwaway prototype only for `?prototype=phase6`; otherwise mounts `App` exactly as before."
      contains: "import.meta.env.DEV"
    - path: "src/web/prototypes/Phase6DiffSemanticsPrototype.vue"
      provides: "Single-file Vue UI mockup with three shareable `?variant=` visual treatments and a floating switcher."
      contains: "PHASE 6 THROWAWAY MOCKUP"
  key_links:
    - from: "src/web/main.ts"
      to: "src/web/prototypes/Phase6DiffSemanticsPrototype.vue"
      via: "dynamic import only when `import.meta.env.DEV` and `prototype=phase6` are both true"
      pattern: "prototype.*phase6|import\.meta\.env\.DEV"
    - from: "src/web/prototypes/Phase6DiffSemanticsPrototype.vue"
      to: "src/web/styles.css"
      via: "reuse existing global shell classes and Phase 05 semantic CSS variables rather than introducing another palette"
      pattern: "var\(--surface-canvas\)|var\(--diff-addition-background\)|review-shell|diff-workspace"
    - from: "src/web/prototypes/Phase6DiffSemanticsPrototype.vue"
      to: "Phase 06 decisions D-01 through D-08"
      via: "variant annotations and visual state layers explicitly mapped to each decision"
      pattern: "D-01|D-02|D-03|D-04|D-05|D-06|D-07|D-08"
---

<objective>
Create a throwaway, browser-viewable Phase 6 Monaco diff semantics UI mockup that lets the user cumpa three requirement-compliant visual treatments on the existing Diff Review shell.

Purpose: De-risk the Phase 06 visual language before production Monaco theming by making DIFF-01 through DIFF-05 and decisions D-01 through D-08 visible, comparable, and shareable in a development-only URL.
Output: One dev-gated `main.ts` mount branch plus one throwaway Vue prototype component. Production behavior, persistence, dependencies, tests, builds, formatters, linters, and project-wide suites remain untouched during execution.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-semantic-dark-foundation/05-CONTEXT.md
@.planning/phases/06-monaco-diff-semantics/06-CONTEXT.md
@package.json
@vite.config.ts
@src/web/main.ts
@src/web/App.vue
@src/web/styles.css
@src/web/components/DiffWorkspace.vue
@src/web/prototypes/MonacoStabilityPrototype.vue

<interfaces>
- `package.json` has no `dev` script; use the existing Vite project dependency through `npm exec vite -- --host 127.0.0.1` for browser smoke verification. Do not add scripts.
- `vite.config.ts` sets the web root to `src/web`, so the browser entry is `src/web/main.ts` and `src/web/index.html` loads `/main.ts`.
- `src/web/main.ts` currently imports `App` and mounts it directly. Preserve that non-prototype behavior for every URL except the development-only `?prototype=phase6` branch.
- `src/web/App.vue` owns the current session shell, loading/unavailable branches, file drawer, active file strip, review toolbar, `DiffWorkspace`, comments rail, keyboard handling, and responsive media-query state. The prototype should echo this shell as static mock data rather than fetching API state or mutating drafts.
- `src/web/styles.css` already exposes Phase 05 semantic roles: `--surface-canvas`, `--surface-inset`, `--surface-panel`, `--surface-raised`, `--surface-interactive`, text roles, borders, focus ring, selection, diff addition/deletion/intraline, hunk, unchanged, empty, spacing, radii, and compact typography. The prototype must consume these roles and may add prototype-only scoped variables/classes, but must not create a competing token root.
- `src/web/components/DiffWorkspace.vue` exposes the visible production vocabulary to mirror: `.diff-workspace`, BASE/HEAD labels, glyph-gutter comment action, Monaco host, and unchanged-region context help. Preserve production behavior by not modifying it for this prototype.
- `src/web/prototypes/MonacoStabilityPrototype.vue` is the existing throwaway precedent: a Vue SFC under `src/web/prototypes/`, obvious prototype labeling, local fixture data, browser-viewable interaction, and cleanup-oriented code that is not a production integration point.
</interfaces>
</context>

<constraints>
- Execute this as a prototype only. Modify only `src/web/main.ts` and create `src/web/prototypes/Phase6DiffSemanticsPrototype.vue`; do not edit `App.vue`, `styles.css`, `DiffWorkspace.vue`, package metadata, lockfiles, tests, server code, contracts, generated output, or other planning artifacts.
- Keep the prototype development-only: `?prototype=phase6` must mount the prototype only when `import.meta.env.DEV` is true. In production builds and in normal development URLs, mount the existing `App` branch.
- Keep the implementation read-only and static. Do not call `createSessionClient`, mutate drafts, write persistence, add local storage, submit comments, or change review/export behavior.
- Do not add dependencies, package scripts, routes, test fixtures, Playwright specs, Vitest specs, formatters, linters, or build steps. Browser smoke uses Vite only.
- Clearly mark the component and rendered page as `PHASE 6 THROWAWAY MOCKUP — delete or absorb after visual decision`.
- Use three shareable variants controlled by `?variant=` and a floating bottom-center switcher per `skill://prototype/UI.md`. Variants must differ structurally, not merely by color.
- Reuse the existing shell density, BEM-like class hooks, and Phase 05 semantic tokens. Prototype-only scoped CSS is allowed for mock Monaco rows, labels, bars, overlays, and switcher styling.
- Preserve the milestone exclusions: no Monaco replacement decision, no production theme system, no light theme, no Primer/runtime dependency, no remote assets, no new review mechanics, no information-architecture redesign.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add the development-only Phase 6 prototype mount</name>
  <files>src/web/main.ts</files>
  <action>Update `src/web/main.ts` so the existing `App` remains the default mount for every normal URL. Before mounting, read `new URLSearchParams(window.location.search)`. If and only if `import.meta.env.DEV` is true and `prototype` equals `phase6`, dynamically import `./prototypes/Phase6DiffSemanticsPrototype.vue` and mount that component. Otherwise execute the existing `createApp(App).mount('#app')` path. Keep the import of `App` and the normal branch simple so production and non-prototype development behavior are visibly preserved. Do not add routing, package scripts, application state, persistence, or production feature flags.</action>
  <verify>
    <automated>Start the local Vite dev server with `npm exec vite -- --host 127.0.0.1` for browser smoke only; do not run build, test, lint, or format commands.</automated>
    <browser-smoke>After Task 2, open `/` and confirm it does not show `PHASE 6 THROWAWAY MOCKUP`; then open `/?prototype=phase6&variant=quiet-rails` and confirm the prototype renders.</browser-smoke>
  </verify>
  <done>`src/web/main.ts` mounts the prototype only for the dev-only `?prototype=phase6` URL and otherwise preserves the existing application mount.</done>
</task>

<task type="auto">
  <name>Task 2: Build three shareable Phase 6 visual treatments inside the existing shell</name>
  <files>src/web/prototypes/Phase6DiffSemanticsPrototype.vue</files>
  <action>Create `src/web/prototypes/Phase6DiffSemanticsPrototype.vue` as a single throwaway Vue SFC. At the top of the component and in the rendered page, state: `PHASE 6 THROWAWAY MOCKUP — delete or absorb after visual decision`. Import Vue Composition API helpers only as needed. Use local static fixture data for a small changed-file tree, active file strip, toolbar summary, faux side-by-side Monaco rows, context/reveal separators, inline comment anchor, selected text, active line, hover target, and comments rail. Include `<style src="../styles.css"></style>` so the prototype can reuse the existing shell classes when mounted without `App.vue`, plus a scoped style block for prototype-only mock Monaco rows and the switcher.

Render the mockup in the current Diff Review shape rather than a blank route: `.session-shell`, `.session-header` or a compact prototype header, `.review-shell`, `.review-files`, `.review-main`, `.active-file-strip`, `.review-toolbar`, `.diff-workspace`, `.diff-workspace__side-labels`, and `.comments-rail`. Use static controls and labels; buttons may be inert except the prototype variant switcher. The prototype can use faux Monaco markup instead of constructing a real editor because this task answers a visual-treatment question and must not change production Monaco behavior.

Implement exactly three `?variant=` values with names visible in the switcher and shareable URLs:
1. `quiet-rails` — restrained addition/deletion row fills, stronger intraline spans, continuous thin gutter bars, sparse `−`/`+` signs at block start/end, subtle unchanged canvas, and inset comment rails.
2. `signed-gutters` — stronger non-color Base/Head separation through signed gutter columns, line-number emphasis, side labels, hunk separators, and visible glyph-margin affordances while line fills remain quiet.
3. `layered-overlap` — overlap-focused treatment showing selection fill plus crisp edge, active-line full-width edge, hover-revealed comment affordance, persistent anchor rail, empty counterpart recession, and separate two-pixel focus ring.

For each variant, visibly cover DIFF-01 through DIFF-05 and cite the Phase 06 decisions D-01 through D-08 in a compact legend or checklist within the prototype. Ensure the visual states demonstrate: D-01 restrained whole-line fills with stronger intraline emphasis; D-02 Base/deletion and Head/addition distinguishable without red/green through signed `−`/`+` markers and labels; D-03 continuous multi-line gutter change bars with sparse signs; D-04 unchanged context close to normal canvas, empty counterpart recessed, hunk accent reserved for separators/reveal controls; D-05 diff meaning as persistent base layer while interactions use distinct geometry; D-06 restrained blue selection with visible edge; D-07 subtle active-line edge and stronger line-number emphasis with hover/comment affordance instead of row-fill replacement; D-08 persistent comment anchor rail and separate focus ring. Keep copy concise enough to cumpa visuals at desktop and narrow widths.</action>
  <verify>
    <automated>Use the same local Vite dev server command, `npm exec vite -- --host 127.0.0.1`, for manual browser smoke; do not run build, test, lint, or format commands.</automated>
    <browser-smoke>Open `/?prototype=phase6&variant=quiet-rails`, `/?prototype=phase6&variant=signed-gutters`, and `/?prototype=phase6&variant=layered-overlap`. Confirm each URL renders a different structural treatment, the throwaway banner is visible, all DIFF-01 through DIFF-05 and D-01 through D-08 labels are present, no server error appears in the prototype branch, and reloading preserves the selected variant.</browser-smoke>
  </verify>
  <done>`Phase6DiffSemanticsPrototype.vue` is a static, throwaway, development-only browser mockup with three structurally different Monaco diff semantics treatments that cover DIFF-01 through DIFF-05 and D-01 through D-08 without production behavior or persistence.</done>
</task>

<task type="auto">
  <name>Task 3: Add the floating switcher and finish with browser smoke verification</name>
  <files>src/web/prototypes/Phase6DiffSemanticsPrototype.vue</files>
  <action>In the same prototype component, add a bottom-center floating switcher that is visually distinct from the mock design. It must show left arrow, current variant key plus human-readable name, and right arrow. Clicking arrows cycles through the three variants with wraparound and updates the browser URL via `history.replaceState` or `history.pushState` so `prototype=phase6` remains present and `variant` changes without losing other search parameters. Add ArrowLeft and ArrowRight key handling for cycling, but return without handling when the focused target is an input, textarea, select, or contenteditable element. Initialize unknown or missing variants to `quiet-rails` and update the URL to that valid value. Register and remove key listeners with Vue lifecycle hooks.

Finish by browser-smoke verifying desktop and narrow widths. At desktop width, confirm the three-column shell, faux side-by-side Monaco area, signed gutter cues, intraline spans, hunk separators, active line, selection edge, focus ring, hover/comment affordance, anchor rail, and comments rail remain legible. At a narrow width around 390px, confirm the mockup remains usable without page-wide horizontal scrolling except the faux diff area may own localized horizontal scroll. Confirm normal `/` still follows the existing app branch and that `/?prototype=phase6&variant=quiet-rails` works only in the Vite dev server. Do not run formatters, linters, Vitest, Playwright, builds, or project-wide suites.</action>
  <verify>
    <automated>Use `npm exec vite -- --host 127.0.0.1` to serve the prototype for final browser smoke, then stop Vite; do not run build, test, lint, or format commands.</automated>
    <browser-smoke>Desktop: open `http://127.0.0.1:5173/?prototype=phase6&variant=quiet-rails`, use the floating arrows and keyboard ArrowRight/ArrowLeft to visit all three variants, reload each shareable URL, and confirm the variant persists. Narrow: resize to about 390px wide and repeat switcher checks; confirm the shell remains readable and only the mock diff area scrolls horizontally. Preservation: open `http://127.0.0.1:5173/` and confirm no prototype banner or switcher renders. Stop Vite after smoke verification.</browser-smoke>
  </verify>
  <done>Browser smoke verification at desktop and narrow widths proves the prototype is shareable by `?variant=`, keyboard/click switchable, visibly throwaway, development-only, and non-invasive to the normal app URL.</done>
</task>

</tasks>

<source_audit>

| Source | ID | Feature / requirement | Task | Status | Notes |
|---|---|---|---|---|---|
| GOAL | QUICK-260726-Q8A | Interactive Phase 6 Monaco diff semantics mockup with three visual treatments on the existing shell | 1, 2, 3 | COVERED | Dev-only mount, static shell mockup, three variants, floating switcher, browser smoke. |
| REQ | DIFF-01 | Coordinated dark semantic palette for Monaco canvas, syntax tokens, gutters, widgets, and surrounding workspace | 2 | COVERED | Reuses Phase 05 tokens and shell hooks; every variant shows canvas/gutter/widget/syntax treatment. |
| REQ | DIFF-02 | Added, deleted, intraline, hunk, unchanged, and empty regions remain distinguishable and readable | 2, 3 | COVERED | Each variant includes all region types, with desktop/narrow visual checks. |
| REQ | DIFF-03 | Base/deletion and Head/addition distinguishable without relying on red/green alone | 2 | COVERED | Signed `−`/`+` gutters, BASE/HEAD labels, side position, line-number/gutter structure. |
| REQ | DIFF-04 | Legible gutters, active-line emphasis, existing comment affordance without movement or gutter reflow | 2, 3 | COVERED | Faux Monaco rows show stable gutter columns, active-line edge, and comment affordance/rail without code movement. |
| REQ | DIFF-05 | Selection, active line, hover, comment anchors, focus, and diff meaning distinguishable when overlapping | 2, 3 | COVERED | `layered-overlap` makes overlap composition explicit; smoke checks all overlap states. |
| CONTEXT | D-01 | Restrained whole-line fills; stronger intraline emphasis | 2 | COVERED | All variants include quiet row fills plus stronger word spans. |
| CONTEXT | D-02 | Non-color Base/Head deletion/addition cues through signs and labels | 2 | COVERED | Signed gutters and BASE/HEAD labels are persistent. |
| CONTEXT | D-03 | Continuous multi-line gutter bars with sparse signs | 2 | COVERED | Variants render contiguous bars and start/end signs. |
| CONTEXT | D-04 | Unchanged/empty/hunk hierarchy stays quiet | 2 | COVERED | Recessed empty regions, normal-context canvas, hunk accent limited to separators/reveal controls. |
| CONTEXT | D-05 | Diff meaning remains base layer; interaction states use geometry | 2 | COVERED | Interaction edges/rails/outlines compose over diff fills. |
| CONTEXT | D-06 | Selection uses restrained blue fill and crisp edge | 2 | COVERED | Selection sample includes fill plus edge while signs/bars remain visible. |
| CONTEXT | D-07 | Active line and hover use edge/line-number/comment affordance rather than replacing row meaning | 2 | COVERED | Active-line edge and hover affordance are separate from diff row fill. |
| CONTEXT | D-08 | Comment anchor rail and focus ring remain simultaneously legible | 2 | COVERED | Persistent anchor rail plus separate two-pixel focus ring appear in overlap treatment. |
| CONTEXT | Phase 05 semantic dark tokens | Use one direct semantic-token root, no competing palette | 2 | COVERED | Prototype consumes existing CSS variables and shell classes. |
| PROTOTYPE | UI.md | Three structurally different variants on one route with floating switcher and URL state | 2, 3 | COVERED | `?variant=` values, bottom switcher, click/keyboard cycling, shareable URLs. |
| EXCLUSION | No production behavior changes, persistence, tests, formatter, linter, project-wide suites, dependency changes | 1, 2, 3 | COVERED | Dev-only branch and explicit smoke-only verification boundaries. |

</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| URL query → mounted Vue root | A prototype query parameter can change what component mounts in development. |
| Prototype fixture UI → production review workflow | Static mock visuals could be mistaken for supported review behavior. |
| Prototype CSS → global workspace styling | Throwaway styling could leak into normal app presentation if not scoped or dev-gated. |
| Browser smoke command → developer workstation | Verification starts a local Vite dev server and must not run builds, tests, formatters, linters, or persistence-mutating flows. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-QUICK-260726-Q8A-01 | Spoofing | Prototype mount | mitigate | Gate with `import.meta.env.DEV` and exact `prototype=phase6`; normal and production URLs mount `App`. |
| T-QUICK-260726-Q8A-02 | Tampering | Review data | mitigate | Use static fixtures only; no API client, draft mutation, export, local storage, or repository writes. |
| T-QUICK-260726-Q8A-03 | Information Disclosure | Fixture content | mitigate | Use synthetic code/file names and no repository secrets, object IDs, or local absolute paths. |
| T-QUICK-260726-Q8A-04 | Denial of Service | Key handling | mitigate | Scope ArrowLeft/ArrowRight listener to variant cycling and ignore editable/contenteditable focus targets. |
| T-QUICK-260726-Q8A-05 | Elevation of Privilege | UI affordances | mitigate | Keep prototype buttons inert except switcher; do not wire production review commands or server authority. |
| T-QUICK-260726-Q8A-SC | Tampering | Package supply chain | accept | No dependencies, package scripts, lockfiles, or install tasks are introduced. |
</threat_model>

<verification>
End execution with manual browser smoke only:
1. Start the existing Vite project dependency with `npm exec vite -- --host 127.0.0.1`.
2. Open `http://127.0.0.1:5173/` and confirm the existing app branch appears without the prototype banner or floating switcher.
3. Open `http://127.0.0.1:5173/?prototype=phase6&variant=quiet-rails`, `http://127.0.0.1:5173/?prototype=phase6&variant=signed-gutters`, and `http://127.0.0.1:5173/?prototype=phase6&variant=layered-overlap`; confirm each is reload-stable and visually distinct.
4. At desktop width, verify the mock Diff Review shell, faux side-by-side Monaco canvas, syntax/intraline samples, signed gutter bars, hunk separators, unchanged/empty regions, active line, selection edge, hover/comment affordance, anchor rail, focus ring, and comments rail are legible.
5. At a narrow width around 390px, verify the prototype remains usable and shareable; no page-wide horizontal scrolling appears except localized scrolling owned by the mock diff area.
6. Use switcher clicks and ArrowLeft/ArrowRight to cycle variants; focus a textarea/control and confirm arrow keys are not intercepted there.
7. Stop Vite. Do not run formatters, linters, Vitest, Playwright, builds, dependency installation, or project-wide suites.
</verification>

<success_criteria>
- `src/web/main.ts` keeps existing `App` behavior for every non-prototype URL and gates the throwaway mockup behind `import.meta.env.DEV` plus `?prototype=phase6`.
- `src/web/prototypes/Phase6DiffSemanticsPrototype.vue` exists, is clearly marked throwaway, uses existing shell classes and Phase 05 semantic tokens, and introduces no persistence or production review behavior.
- The prototype exposes exactly three shareable visual options: `quiet-rails`, `signed-gutters`, and `layered-overlap`.
- The floating switcher shows previous/current/next affordances, updates `?variant=`, wraps around, supports ArrowLeft/ArrowRight, and ignores editable/contenteditable targets.
- DIFF-01 through DIFF-05 and D-01 through D-08 are visibly covered in the mockup and cited in the variant legend/checklist.
- Desktop and narrow browser smoke verification pass through Vite; no formatter, linter, test, build, dependency install, package script addition, or project-wide suite is run.
- Cleanup boundary is explicit: delete the prototype branch/component after the visual decision, or absorb the chosen treatment into the real Phase 06 Monaco theme implementation and remove the losing variants/switcher.
</success_criteria>

<output>Create `.planning/quick/260726-q8a-SUMMARY.md` after execution with the chosen browser smoke URLs, desktop/narrow observations, and cleanup disposition. This planning task creates only `.planning/quick/260726-q8a-PLAN.md`.</output>
