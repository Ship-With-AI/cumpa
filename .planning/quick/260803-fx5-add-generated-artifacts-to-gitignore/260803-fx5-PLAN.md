---
phase: quick
plan: 260803-fx5
type: execute
wave: 1
depends_on: []
files_modified:
  - .gitignore
autonomous: true
requirements:
  - QUICK-260803-FX5
must_haves:
  truths:
    - "Generated shell, GSD runtime/cache, native build, and accidental local phase-index artifacts no longer appear in Git status."
    - "The Monaco source fix, its unit test, and the resolved debug report remain visible in Git status."
    - "Only targeted directory rules are added; durable planning/debug evidence is not broadly ignored."
  artifacts:
    - path: ".gitignore"
      provides: "Small general ignore rules for the five observed generated/runtime/build artifact categories"
      contains: ".planning/research/.cache/"
  key_links:
    - from: ".gitignore"
      to: ".bg-shell/, .gsd/runtime/, .planning/research/.cache/, build/, local:/"
      via: "one trailing-slash directory rule per observed artifact category"
      pattern: "(\\.bg-shell/|\\.gsd/runtime/|\\.planning/research/\\.cache/|build/|local:/)"
---

<objective>
Keep generated/runtime/build artifacts out of repository status without concealing the in-progress Monaco fix or durable GSD debug evidence.

Purpose: Restore a useful working-tree signal with the smallest rules consistent with the existing `.gitignore` directory-pattern style.
Output: One targeted `.gitignore` edit and scoped status proof.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.gitignore
@src/web/monaco/theme.ts
@tests/unit/monaco-theme.test.ts
@.planning/debug/resolved/diff-row-border-perimeter.md
</context>

<constraints>
- Work directly on `main`; do not create a branch or worktree.
- Change only `.gitignore` for product behavior. GSD may handle its plan, summary, and state evidence separately.
- Preserve `src/web/monaco/theme.ts`, `tests/unit/monaco-theme.test.ts`, and `.planning/debug/resolved/diff-row-border-perimeter.md` unchanged and visible.
- Do not add broad rules for `.planning/`, `.gsd/`, source, tests, Markdown, or hidden directories; do not delete generated artifacts.
- Run no formatter, linter, test, build, typecheck, or project-wide validation command. Verification is the scoped Git status command in Task 1 only.
</constraints>

<tasks>

<task type="auto">
  <name>Task 1: Add the five targeted directory ignore rules</name>
  <files>.gitignore</files>
  <action>Inspect the existing six directory/archive rules and append only rules not already present, preserving the established one-pattern-per-line trailing-slash convention. Add `.bg-shell/`, `.gsd/runtime/`, `.planning/research/.cache/`, `build/`, and `local:/` so the rules cover the observed manifest, runtime state, research cache JSON, native build tree, and accidental `local:/phase03-index.json` without naming individual generated files. Do not edit, stage, restore, or otherwise alter the two Monaco files or the resolved debug report, and do not broaden any rule to their parent source, test, `.planning/`, or `.gsd/` trees.</action>
  <verify>
    <automated>test "$(git status --short --untracked-files=all -- .bg-shell/ .gsd/runtime/ .planning/research/.cache/ build/ 'local:' src/web/monaco/theme.ts tests/unit/monaco-theme.test.ts .planning/debug/resolved/diff-row-border-perimeter.md)" = "$(printf '%s\n' ' M src/web/monaco/theme.ts' ' M tests/unit/monaco-theme.test.ts' '?? .planning/debug/resolved/diff-row-border-perimeter.md')"</automated>
  </verify>
  <done>The scoped status assertion passes: every observed generated/runtime/cache/build/local-index path is absent, while the modified Monaco source, modified Monaco unit test, and untracked resolved debug report remain visible with their original status.</done>
</task>

</tasks>

<source_coverage>
- GOAL: Task 1 removes only generated/runtime/build noise from status.
- REQ: QUICK-260803-FX5 is fully covered by the `.gitignore` edit and status assertion.
- RESEARCH: No external dependency or undocumented integration is involved; existing repository conventions are sufficient.
- CONTEXT: All five requested artifact categories and all three protected visible changes are named explicitly; no deferred item is introduced.
</source_coverage>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Working tree → Git status | Ignore rules decide which untracked repository evidence remains visible to developers. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-260803-FX5-01 | Tampering | `.gitignore` | mitigate | Use only the five exact directory rules and assert the protected source, test, and debug paths remain visible. |
| T-260803-FX5-02 | Repudiation | durable GSD evidence | mitigate | Do not ignore `.planning/` or `.planning/debug/`; keep the resolved debug report in the scoped status assertion. |
</threat_model>

<verification>
Run only the Task 1 scoped `git status` assertion. Its exact expected output contains the two modified Monaco files and the untracked resolved debug report, and contains none of the five generated artifact categories.
</verification>

<success_criteria>
- `.gitignore` is the only implementation file changed.
- The five smallest general directory rules cover every currently visible generated/runtime/cache/build/local-index artifact.
- The scoped status assertion passes while the Monaco source, Monaco unit test, and resolved debug report remain visible and unchanged.
- No formatter, linter, test, build, typecheck, broad validation, branch, or worktree operation is used.
</success_criteria>

<output>
Create `.planning/quick/260803-fx5-add-generated-artifacts-to-gitignore/260803-fx5-SUMMARY.md` when execution completes.
</output>
