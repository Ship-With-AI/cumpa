---
phase: quick
plan: 260731-ll9
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/App.vue
  - src/web/styles.css
  - tests/e2e/responsive-session.spec.ts
autonomous: true
requirements:
  - QUICK-260731-LL9
must_haves:
  truths:
    - "At desktop widths above the existing 1099px drawer breakpoint, the existing Files control collapses and restores the changed-files sidebar so the diff workspace gains its width."
    - "The comparison header consumes less vertical space and uses smaller, scoped typography without changing its Base, selected-file, Head, or toolbar information."
    - "Changed-file tree rows use smaller type while paths, counts, selection, focus, and availability cues remain readable."
    - "At 1099px and below, Files still opens the existing drawer, Close files and Escape still close it, and focus returns to the Files control."
  artifacts:
    - path: "src/web/App.vue"
      provides: "Mode-aware desktop Files collapse state while retaining the current narrow drawer state and focus flow"
      contains: "filesCollapsed"
    - path: "src/web/styles.css"
      provides: "Collapsed desktop grid plus comparison-header and Files-tree density rules scoped to the existing workspace classes"
      contains: "review-shell--files-collapsed"
    - path: "tests/e2e/responsive-session.spec.ts"
      provides: "Wide collapse/restore and narrow drawer/focus-return regression coverage"
      contains: "responsive keyboard and accessibility contract"
  key_links:
    - from: "src/web/App.vue"
      to: "src/web/styles.css"
      via: "review-shell--files-collapsed class changes only the desktop Files grid column"
      pattern: "review-shell--files-collapsed"
    - from: "src/web/App.vue"
      to: "nav.review-files"
      via: "the existing Files button exposes aria-controls and mode-aware aria-expanded state"
      pattern: "aria-(controls|expanded)"
    - from: "tests/e2e/responsive-session.spec.ts"
      to: "src/web/App.vue"
      via: "the same Files control is exercised at wide and narrow viewport widths"
      pattern: "getByRole\('button', \{ name: 'Files'"
---

<objective>
Maximize visible diff space with one small responsive workspace change.

Purpose: Let desktop users reclaim the Files column and reduce header/sidebar density without redesigning the review workspace or disturbing the existing mobile drawer.
Output: A desktop-collapsible Files sidebar, compact scoped header/sidebar styles, and focused wide/narrow Playwright coverage.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@package.json
@src/web/App.vue
@src/web/styles.css
@src/web/components/ReviewToolbar.vue
@tests/e2e/responsive-session.spec.ts

<interfaces>
- `App.vue` already owns `isFilesDrawer`, `filesOpen`, `openFiles()`, `closeFiles()`, opener capture, Escape handling, and the `max-width: 1099px` media-query transition. Keep that mobile drawer path authoritative.
- The current Files button is in `.review-context-header__file`; reuse that control at every viewport rather than adding a toolbar button or changing `ReviewToolbar.vue`.
- `ReviewToolbar.vue` owns only file/change navigation, review, and keyboard-help controls. Its props, emits, markup, and behavior remain unchanged.
- `styles.css` already owns the three-column desktop shell, the two-column 1100–1439px shell, the narrow drawer, comparison-header spacing, and tree-row typography. Extend those selectors rather than adding a component or JavaScript layout measurement.
- `responsive-session.spec.ts` already covers 1440/1280/1100 desktop geometry, the 1099px drawer boundary, the 320px keyboard journey, Files drawer accessibility, Escape focus return, and page-overflow checks.
</interfaces>
</context>

<constraints>
- Modify only `src/web/App.vue`, `src/web/styles.css`, and `tests/e2e/responsive-session.spec.ts` during execution.
- No dependency, persistence, routing, Monaco, toolbar-component, or information-hierarchy changes.
- Use one session-local desktop boolean; do not add local storage, resize observers, a composable, a new component, icons, animation, or a second Files control.
- Preserve the existing `filesOpen` drawer state, `openFiles()`/`closeFiles()` focus capture and return, Close files button, Escape behavior, breakpoints, and mobile overlay CSS.
- Keep typography and spacing changes scoped to `.review-context-header` and `.review-files`; do not change global type tokens or global button geometry.
- Run only the focused browser command in this plan; skip formatters, linters, typechecks, builds, broad suites, and unrelated cleanup.
</constraints>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Collapse desktop Files and compact the existing workspace chrome</name>
  <files>src/web/App.vue, src/web/styles.css, tests/e2e/responsive-session.spec.ts</files>
  <behavior>
    - At 1440px and representative 1100–1439px desktop widths, Files is visible, initially reports the sidebar expanded, collapses the Files column on activation, gives that width to `.review-main`, and restores the same sidebar on the next activation.
    - The desktop Files button has a stable `aria-controls` relationship to the changed-files navigation and reports its visible state through `aria-expanded`; collapsed tree content is absent from keyboard and accessibility navigation.
    - At 1099px and 320px, the same Files control continues to call the current drawer opening path; Close files and Escape close the overlay, and Escape returns focus to Files.
    - Header context/toolbar padding and gaps are tighter; the selected-file heading, endpoint names, and header controls use existing smaller type tokens without dropping or reordering content.
    - `.review-files .tree-row` uses the existing metadata-size typography tokens while row selection, focus outline, path truncation, line counts, and exceptional availability labels remain intact.
  </behavior>
  <action>Extend the existing `responsive keyboard and accessibility contract` first. Replace the desktop assertions that Files is absent at 1280, 1279, and 1100 with assertions for the reused visible control. At 1440 and at one 1100–1439 width, assert initial `aria-expanded="true"`, activate Files, assert `aria-expanded="false"`, confirm the changed-files navigation is hidden from accessibility/keyboard interaction, and cumpa shell geometry to prove `.review-main` expands into the released column; activate again and prove the original adjacent Files/main columns return with no page overflow. Retain and, where needed, strengthen the existing 1099px and 320px checks so the same control opens `.review-files--open`, exposes Close files, closes on Escape, and returns focus to Files.

In `App.vue`, add only `filesCollapsed` for desktop state and one mode-aware Files handler: at the current drawer breakpoint delegate to the unchanged `openFiles()` path; otherwise toggle `filesCollapsed`. Render the existing Files button at all widths, keep its accessible name exactly `Files`, give the changed-files `nav` a stable ID, and bind `aria-controls` plus `aria-expanded` to drawer visibility on narrow screens and sidebar visibility on desktop. Apply `review-shell--files-collapsed` only when not in drawer mode, and hide the desktop navigation from rendering/accessibility while collapsed. Do not merge desktop collapse into `filesOpen`, and do not alter `closeFiles()`, opener capture, Escape ordering, file selection, viewport breakpoints, or `ReviewToolbar.vue`.

In `styles.css`, collapse only the Files grid column for `review-shell--files-collapsed` in both existing desktop grid shapes (1440+ with the review rail and 1100–1439 with the overlaid review rail); leave the max-1099px drawer rules unchanged. Tighten only `.review-context-header__context` and its toolbar padding/gaps using existing spacing tokens. Override the selected-file heading and endpoint-name typography with existing smaller body/metadata tokens, and scope smaller control text to the comparison header without reducing focus outlines or the global 32px control height. Override only `.review-files .tree-row` to use the existing metadata font-size and line-height tokens. Do not change global tokens, tree geometry, breakpoints, review-toolbar structure, or Monaco dimensions.</action>
  <verify>
    <automated>npm run test:browser -- tests/e2e/responsive-session.spec.ts --grep "responsive keyboard and accessibility contract"</automated>
    <human-check>Open the real review workspace at 1440×640: use Files twice and confirm the sidebar disappears/restores, the diff visibly gains/returns the column width, focus stays visible, and the compact header/tree remain readable. Then use 320×640: open Files, confirm the existing drawer and Close files control, press Escape, verify focus returns to Files, and confirm there is no page-wide horizontal overflow.</human-check>
  </verify>
  <done>The focused Playwright contract passes and the browser check proves a reusable accessible Files control, real desktop width recovery, compact scoped header/sidebar typography, and unchanged narrow drawer/focus-return behavior.</done>
</task>

</tasks>

<source_audit>
| Source | Item | Covered by | Status |
|---|---|---|---|
| GOAL | Maximize visible diff workspace | Task 1 desktop column collapse and scoped density changes | COVERED |
| REQ | Desktop Files sidebar is collapsible | Task 1 mode-aware control, grid class, accessibility, and wide assertions | COVERED |
| REQ | Comparison header uses less vertical space and smaller type | Task 1 scoped header padding/gap/type rules and browser inspection | COVERED |
| REQ | Files sidebar uses smaller type | Task 1 `.review-files .tree-row` metadata typography | COVERED |
| CONTEXT | Reuse current Files control and drawer state | Task 1 keeps one control and delegates narrow activation to existing `openFiles()`/`filesOpen` | COVERED |
| CONTEXT | Preserve mobile drawer focus return and accessibility | Task 1 retains close/Escape functions and asserts 1099px/320px behavior | COVERED |
| ACCEPTANCE | Browser check at wide and narrow viewports | Task 1 automated responsive spec plus explicit 1440×640/320×640 human check | COVERED |
| RESEARCH | None | Quick mode Level 0; established Vue/CSS/Playwright patterns are sufficient | COVERED |
</source_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Viewport mode → Files control | The same control must select desktop collapse or mobile drawer behavior without exposing hidden navigation to keyboard or accessibility APIs. |
| Responsive shell → diff viewport | Releasing the Files column must expand only the main workspace and must not create document-wide overflow. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-QUICK-260731-LL9-01 | Denial of Service | responsive workspace access | mitigate | Preserve the existing narrow drawer path and prove the Files tree stays reopenable at desktop and mobile widths. |
| T-QUICK-260731-LL9-02 | Spoofing | Files expanded state | mitigate | Bind one Files control to the navigation with `aria-controls` and mode-aware `aria-expanded`; remove collapsed content from rendering/accessibility. |
| T-QUICK-260731-LL9-SC | Tampering | package supply chain | accept | No dependency, package, or lockfile changes are permitted. |
</threat_model>

<verification>
1. Run `npm run test:browser -- tests/e2e/responsive-session.spec.ts --grep "responsive keyboard and accessibility contract"`.
2. Perform the 1440×640 and 320×640 browser check from Task 1, including desktop width recovery, compact readable chrome, mobile Close files/Escape behavior, focus return, and no page-wide overflow.
3. Confirm execution touched only the three files listed in `files_modified` plus the generated quick-task summary/state bookkeeping.
</verification>

<success_criteria>
- Desktop Files collapses and restores through the existing Files control, with correct `aria-controls`/`aria-expanded` state and no inaccessible hidden tree.
- Collapsing Files measurably expands the diff workspace at wide desktop widths and restoring it returns the established grid geometry.
- The comparison header is shorter and uses smaller scoped type without losing Base, selected-file, Head, or toolbar content.
- Files tree rows use smaller scoped type without degrading focus, selection, counts, paths, or availability cues.
- The existing 1099px/320px drawer, Close files control, Escape handling, opener focus return, and localized overflow remain working.
- No new dependency, component, persistent preference, breakpoint, redesign, toolbar change, or unrelated cleanup is introduced.
</success_criteria>

<output>
Create `.planning/quick/260731-ll9-maximize-diff-space/260731-ll9-SUMMARY.md` when execution completes.
</output>
