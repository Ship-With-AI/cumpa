---
phase: 08-accessible-responsive-continuity
plan: "02"
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - src/web/styles.css
  - tests/e2e/responsive-session.spec.ts
autonomous: true
requirements:
  - A11Y-01
  - A11Y-02
  - A11Y-03
  - RESP-01
must_haves:
  truths:
    - "Normal text/control labels reach at least 4.5:1 and meaningful boundaries, icons, change signs/bars, selected/anchor/status rails, and focus indicators reach at least 3:1 against their final rendered composites without rounding failures upward."
    - "One root `--control-boundary: #8B949E` role identifies resting fields, checkboxes/radios, and any outlined control whose boundary is meaningful; quiet separator tokens do not masquerade as control boundaries."
    - "Every operable workspace target retains a persistent 2 CSS px focus perimeter; local inset fallbacks are limited to proven clipping ancestors and remain distinct from selected, lifecycle, anchor, destructive, hover, and diff channels."
    - "Forced colors preserve system-colored control boundaries, links, focus, selected rails, Base/Head labels, literal −/+, dashed Base versus solid Head bars, source anchor, comment selection, notice/status edges, and Monaco pane focus without a blanket author-color opt-out."
    - "Real application states—not injected class-only fixtures—prove contrast, focus geometry, and forced-color durability while Phase 08's outer fit/local-diff-overflow boundary remains intact."
  artifacts:
    - path: "src/web/styles.css"
      provides: "Shared control-boundary role, standards-aligned composite corrections, targeted focus fallbacks, and complete app-owned forced-color mappings"
    - path: "tests/e2e/responsive-session.spec.ts"
      provides: "Rendered source-over contrast, unclipped keyboard focus, real-state forced-color, and narrow containment browser evidence"
  key_links:
    - from: "src/web/styles.css:--control-boundary"
      to: "src/web/styles.css:input, select, textarea, checkbox/radio, outlined controls"
      via: "one canonical meaningful boundary role instead of repeated literals"
      pattern: "--control-boundary"
    - from: "tests/e2e/responsive-session.spec.ts:renderedContrastRatio"
      to: "src/web/styles.css:status/diff/selection/comment/control layers"
      via: "computed RGBA source-over composition and WCAG luminance thresholds"
      pattern: "renderedContrastRatio|compositeOver"
    - from: "tests/e2e/responsive-session.spec.ts:expectFocusIndicatorUnclipped"
      to: "src/web/styles.css::focus-visible and clipping-boundary fallbacks"
      via: "keyboard focus plus target/ancestor rectangle and computed perimeter assertions"
      pattern: "focus-visible|outlineOffset|boxShadow"
    - from: "src/web/styles.css:@media (forced-colors: active)"
      to: "Phase 06/07 public app-owned state classes"
      via: "Canvas/CanvasText/ButtonFace/ButtonText/ButtonBorder/LinkText/Highlight/HighlightText/GrayText mappings"
      pattern: "forced-colors: active"
---

<objective>
Make every meaningful Phase 05–07 visual channel survive actual compositing, clipping, grayscale-independent structure, and browser forced colors on top of the final responsive geometry from 08-01.

Purpose: Fulfill A11Y-01, A11Y-02, and A11Y-03 with centralized semantic CSS and real-browser evidence, without changing content, state, commands, or Monaco behavior.
Output: One shared control-boundary token, measured presentation corrections, targeted focus fallbacks, complete forced-color coverage, and extended packaged Chromium assertions.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-accessible-responsive-continuity/08-CONTEXT.md
@.planning/phases/08-accessible-responsive-continuity/08-RESEARCH.md
@.planning/phases/08-accessible-responsive-continuity/08-PATTERNS.md
@.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md
@.planning/phases/08-accessible-responsive-continuity/08-01-SUMMARY.md
@.planning/phases/05-semantic-dark-foundation/05-01-SUMMARY.md
@.planning/phases/06-monaco-diff-semantics/06-03-SUMMARY.md
@.planning/phases/07-github-familiar-review-surfaces/07-06-SUMMARY.md
@.planning/phases/07-github-familiar-review-surfaces/07-09-SUMMARY.md

<interfaces>
`styles.css` is the only visual authority. The direct `:root` semantic token set, global `:focus-visible` ring, public Phase 06 Monaco classes, Phase 07 badge/notice/comment/export classes, and terminal forced-colors media block are extended in place; no component token root or scoped stylesheet is allowed.

`responsive-session.spec.ts` already owns the packaged CLI/browser harness, `readStyles`, opaque `contrastRatio`, `assertNoPageOverflow`, real session/file route controls, reduced-motion setup, keyboard/tooltip checks, drawer geometry, and a synthetic forced-color fixture. Evolve these exact seams instead of adding a package, parallel harness, runtime accessibility subsystem, or source-text audit.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Measure real rendered states, then correct only proven contrast failures</name>
  <read_first>
    - `tests/e2e/responsive-session.spec.ts:1-203` — packaged CLI fixture, exact session files, and reusable browser prerequisites.
    - `tests/e2e/responsive-session.spec.ts:204-287` — `readStyles`, minimum-target assertion, opaque-only `contrastRatio`, `assertNoPageOverflow`, and Monaco line activation helpers.
    - `tests/e2e/responsive-session.spec.ts:328-416` — real session/file route controls that must remain request/response fixture authority.
    - `tests/e2e/responsive-session.spec.ts:453-719` — existing semantic palette, control, diff, tooltip, keyboard, and reduced-motion application journey.
    - `tests/e2e/responsive-session.spec.ts:720-1035` — current drawer/header geometry, focus checks, forced-color coverage, and 320px compact path.
    - `src/web/styles.css:1-102` — one semantic token root and global 2px/2px-offset focus contract.
    - `src/web/styles.css:318-445` — native and shared control rest/hover/active/disabled/checkbox/radio boundaries.
    - `src/web/styles.css:907-997` — shared notice and conflict text/icon/3px-edge anatomy over translucent backgrounds.
    - `src/web/styles.css:1306-1500` — conversation cards, form fields, badges, selected/status channels, and long-text containment.
    - `src/web/styles.css:1659-1860` — selected comments, recovery/export/receipt messages, full identifiers, and current horizontal overflow seams.
    - `tests/integration/monaco-anchor.spec.ts` — real overlapping diff/selection/active/anchor/focus analog; reuse expected public classes, never duplicate its adapter fixture.
    - `tests/e2e/review-panel-resolved.spec.ts` — real selected/resolved/stale/unavailable comment/status analog and single status-owner assertions.
    - `tests/integration/export-receipt-ui.spec.ts` — real conflict/progress/failure/receipt/gitignore state analog and authoritative exact labels.
    - `tests/integration/draft-recovery-ui.spec.ts` — real read-only/recovery/error/success state analog and authoritative exact labels.
    - `.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md:111-146` — exact 4.5:1/3:1 thresholds, control-boundary value, and required composite matrix.
    - `.planning/phases/08-accessible-responsive-continuity/08-RESEARCH.md:264-313,671-682` — rendered compositing algorithm and the resolved measure-before-correction decision.
  </read_first>
  <files>tests/e2e/responsive-session.spec.ts, src/web/styles.css</files>
  <action>Execute one explicit measure → correct → re-measure loop; no result from Task 2 is a prerequisite. First, before changing `src/web/styles.css`, replace the opaque-hex-only contrast assumption with test-only `RgbaColor`, `parseComputedColor`, `compositeOver`, and `renderedContrastRatio` symbols. Read browser-computed foreground, background, opacity, and relevant `::before`/`::after` layers; composite source-over from the canvas through every translucent ancestor/pseudo layer; convert final sRGB to relative luminance; and compare without rounding. Keep all helpers in this Playwright file—no runtime accessibility subsystem or dependency.

Drive actual workspace routes and controls to cover the applicable UI-SPEC composite matrix: canvas/panel/raised/control rest-hover-active-selected-disabled; addition/deletion/intraline plus selection/active/anchor/focus overlap; composer/accepted/selected/resolved/stale/unavailable comment states; field validation; notices; and representative recovery/drift/export conflict/progress/failure/receipt states through their existing state transitions or canonical mounted routes. Add retained assertions requiring at least 4.5:1 for normal/instruction/control text and at least 3:1 for meaningful boundaries/icons/signs/bars/rails/edges/focus.

Run the focused packaged command immediately with those assertions against the pre-correction CSS and capture each failing real target, pseudo/ancestor layer stack, foreground/background composite, and unrounded ratio. The diagnostic run is evidence selection, not task completion: do not whitelist, skip, round, weaken, or remove a failing assertion.

Only after that measured baseline exists, add exactly one direct-root semantic role `--control-boundary: #8B949E` and consume it for resting text fields, textareas, selects, checkbox/radio geometry, and existing outlined controls whose shape is required for recognition. Correct only retained browser-measured failures at a shared semantic role or by promoting required body/instruction text to existing `--text-primary`/`--text-secondary`; preserve semantic hue on the adjacent icon, heading label, sign, or 3px edge. Keep `--border-default`/`--border-strong` for decorative separation and interaction hierarchy. Do not scatter the literal boundary value, introduce component variables, repaint every separator, reduce font size, remove alpha layers, create ad hoc colors, or treat raw-token ratios as proof.

Re-run the same focused packaged command after every CSS correction and finish only when every retained real-state composite assertion passes at its threshold together with the inherited document-fit/local-diff-overflow checks. Do not modify route bodies, production state, APIs, persistence, export logic, commands, accessible names, live-region ownership, or test-only production hooks.</action>
  <verify>
    <automated>npm run test:package -- tests/e2e/responsive-session.spec.ts --grep "responsive keyboard and accessibility contract"</automated>
    <browser-check>Run the focused packaged case headed after the corrective loop; inspect the reported foreground, fully composited background, and unrounded ratio for representative control, overlapping diff/selection/anchor, comment lifecycle, notice, recovery, and export states.</browser-check>
  </verify>
  <acceptance_criteria>
    - `renderedContrastRatio` measures computed RGBA source-over composites through translucent ancestors and pseudo layers; normal text gates at 4.5 and meaningful indicators at 3 without rounding.
    - Real application routes and control transitions cover diff, selection, comment, notice, recovery/drift/export, and selected/status states; no synthetic class-only fixture substitutes for them.
    - `--control-boundary` exists once in the direct semantic root with `#8B949E` and is used by every meaningful resting field/checkbox/radio/outlined-control boundary.
    - Every retained baseline failure is corrected at a shared CSS role or affected selector, and the task's focused automated verification passes all measured composite assertions after correction.
    - Semantic labels/icons/signs/3px edges continue to carry state meaning; no runtime helper, dependency, state/API/persistence/export change, or test-only production hook is introduced.
  </acceptance_criteria>
  <done>The retained real-state measurements select the CSS corrections, and the same assertions prove every required rendered composite passes afterward.</done>
</task>

<task type="auto">
  <name>Task 2: Measure and correct unclipped focus and forced-color durability</name>
  <read_first>
    - `tests/e2e/responsive-session.spec.ts` — final real-state composite helpers, routes, and retained assertions from Task 1.
    - `src/web/styles.css:1-102` — direct semantic token root and global 2px/2px-offset focus contract.
    - `src/web/styles.css:2144-2220` — public signed change bars/signs, selection, source anchor, and Monaco pane focus classes.
    - `src/web/styles.css:2232-2304` — existing terminal forced-colors block and incomplete system-color mappings.
    - `src/web/components/ui/ReviewStateBadge.vue` and `src/web/components/InlineNotice.vue` — exact visible label/icon/role structures whose meaning CSS must preserve.
    - `src/web/components/ReviewPanel.vue` — selected-comment hook, Summary/Open/Resolved/Export order, and existing focus destinations.
    - `src/web/components/DiffWorkspace.vue` — final viewport/canvas clipping boundary from 08-01 and unchanged add-comment/editor focus targets.
    - `.planning/phases/08-accessible-responsive-continuity/08-UI-SPEC.md:202-259,401-427` — complete focus inventory, focus fallbacks, forced-color cues, and browser acceptance protocol.
    - `.planning/phases/08-accessible-responsive-continuity/08-RESEARCH.md:279-313,671-682` — local inset-focus, targeted forced-color patterns, and resolved Chromium blocking gate.
  </read_first>
  <files>tests/e2e/responsive-session.spec.ts, src/web/styles.css</files>
  <action>Extend the retained real-state case with test-only `expectFocusIndicatorUnclipped(locator)`. Use keyboard-produced focus, computed outline/inset geometry, target rectangles, and every clipping ancestor rectangle to prove a continuous at least 2px perimeter remains visible. Traverse the UI-SPEC focus inventory through existing Tab, Shift+Tab, shortcut, drawer, editor, composer, confirmation, tab, comment, recovery/export, receipt, Escape, and focus-return paths at desktop and 320px. Programmatic `.focus()` may isolate a geometry edge but cannot replace the keyboard path.

Run the focused case before focus CSS changes and retain every failing clipping assertion. Then keep the global opaque 2px `--focus-ring` outline with 2px offset, adding a 2px inset/negative-offset fallback only for a target whose recorded perimeter is clipped by a required 08-01 overflow ancestor: Monaco pane focus, diff comment action at the viewport edge, tree/review items at scroll edges, drawer close controls, inline composer/confirmation actions, Summary tabs/actions, and recovery/export/receipt actions. Add sufficient scroll padding where measured. Focus remains the outermost active channel, visibly separate from selected rails, source anchors, lifecycle badges, destructive borders, and diff fills; do not globally change outline offset or add a second tab stop.

Replace `[data-forced-colors-contract]` injected markup with representative rendered application states. First run the forced-color assertions against current CSS and retain every missing structural cue. Then extend only the existing `@media (forced-colors: active)` block with targeted public application selectors and CSS system colors: shells/content use `Canvas`/`CanvasText`; controls use `ButtonFace`/`ButtonText`/`ButtonBorder`; disabled uses `GrayText`; links/anchor rail use `LinkText`; focus and selected rails/perimeters use `Highlight`, with `HighlightText` only on forced selected fills. Preserve literal Base/Head and −/+; Base change bars are dashed and Head bars solid; explicitly map the source anchor, selected comment, state badges, inline notices, receipt/status edges, and public Base/Head Monaco pane focus classes. Retain icons, text labels, and 3px edges when tint/shadow disappears. Do not add a blanket author-color opt-out or private Monaco selector.

Restore forced colors to ordinary media in a guaranteed cleanup path. Re-run the focused command until all retained focus and forced-color assertions pass together with Task 1's composite assertions, reduced-motion checks, drawer inertness/Escape/focus return, ARIA relationships, document fit, and local diff overflow. Do not change behavior, ARIA/live-region ownership, route/state/API/persistence/export semantics, or production hooks.</action>
  <verify>
    <automated>npm run test:package -- tests/e2e/responsive-session.spec.ts --grep "responsive keyboard and accessibility contract"</automated>
    <browser-check>Run the focused case headed at desktop and 320px in ordinary and forced-color media; keyboard-traverse representative toolbar, drawer, editor, composer, comment, recovery, export, and receipt targets and confirm each retained perimeter and non-color cue is visibly present.</browser-check>
  </verify>
  <acceptance_criteria>
    - `expectFocusIndicatorUnclipped` validates the focused element, at least 2px perimeter, and every clipping ancestor after actual keyboard input at desktop and 320px.
    - The default 2px/2px-offset focus contract remains global; inset fallbacks are scoped only to measured clipping boundaries and remain distinct from other state rails.
    - Forced-color assertions use real application states and cover literal labels/signs, dashed/solid provenance bars, anchors, selections, statuses, control boundaries, and focus; the synthetic fixture is removed.
    - No ordinary workspace root/subtree opts out of forced colors, no private Monaco selector is added, and Chromium forced-color emulation passes as the blocking local gate.
    - Task 1's composite assertions plus reduced motion, document fit, local diff overflow, drawer inertness/Escape/focus return, ARIA relationships, and one live-region owner remain passing.
    - No behavior, runtime helper, dependency, state/API/persistence/export change, or test-only production hook is introduced.
  </acceptance_criteria>
  <done>Measured keyboard and forced-color failures select narrowly scoped CSS corrections, and the retained packaged assertions prove durable focus and structural meaning afterward.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New semantic symbol:** root CSS custom property `--control-boundary` with the approved `#8B949E` value.
- **New test-only symbols in `responsive-session.spec.ts`:** `RgbaColor`, `parseComputedColor`, `compositeOver`, `renderedContrastRatio`, and `expectFocusIndicatorUnclipped`.
- **Extended existing browser artifact:** `responsive keyboard and accessibility contract` gains real-state composite, focus-clipping, and forced-color steps; its injected `[data-forced-colors-contract]` fixture is retired.
- **New filesystem path, production component/class/state/prop/event, API/client method, request/result field, persistence/schema/export field, command, package, dependency, remote asset, environment variable, or human setup:** none.
- **Schema-push assessment:** not applicable; this plan modifies centralized CSS and test-only browser helpers/assertions.

<contribution_decisions>
- TDD mode is active, but CSS presentation and browser accessibility evidence are not eligible defined-I/O business logic; the plan remains `type: execute`. Task 1 contains the executable measure → correct → re-measure loop, so its passing verification proves the measured-composite acceptance criteria without depending on Task 2.
- This plan depends on 08-01 because focus clipping ancestors and final background/overflow composition must be stable before measurements are meaningful.
- The discretionary forced-color mapping uses only the UI-SPEC system colors, the existing direct token root, and public app-owned classes; Monaco's automatic high-contrast mode remains authoritative.
- Real Windows High Contrast is unavailable on this macOS workstation. Chromium forced-color emulation is the required automated gate; a real Windows pass, when available, is recorded as additional non-blocking evidence rather than replaced with a custom theme.
</contribution_decisions>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Required outcome | Plan/task | Status |
|--------|----|------------------|-----------|--------|
| GOAL | — | Review remains meaningful/operable in keyboard and forced-color contexts | 08-02/T1-T2; consolidated in 08-03 | COVERED |
| REQ | A11Y-01 | 4.5:1 text and 3:1 meaningful indicators after composition | 08-02/T1-T2 | COVERED |
| REQ | A11Y-02 | Persistent unclipped focus on every operable control/editor affordance | 08-02/T1-T2 | COVERED |
| REQ | A11Y-03 | Durable labels, markers, borders, and focus under forced colors | 08-02/T1-T2 | COVERED |
| REQ | RESP-01 | Accessibility fixes preserve no-page-overflow/local-diff ownership | 08-02/T1-T2 against 08-01 baseline | COVERED |
| RESEARCH | R-COMPOSITE | Browser-computed source-over contrast, not token-only ratios | 08-02/T1 | COVERED |
| RESEARCH | R-FOCUS | Global focus plus local inset fallback only at measured clipping boundaries | 08-02/T2 | COVERED |
| RESEARCH | R-FORCED | Targeted system-color mappings and no blanket opt-out | 08-02/T2 | COVERED |
| CONTEXT | D-01–D-03 | Final header/group geometry remains intact while focus is measured | inherited from 08-01 and asserted 08-02/T2 | COVERED |
| CONTEXT | D-04 | Complete paths/identities remain readable in ordinary/forced colors | inherited from 08-01 and asserted 08-02/T2 | COVERED |

Deferred ideas: none. Full zoom/grayscale/workflow/export/persistence continuity is assigned to 08-03, not omitted.
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository/draft text → visual accessibility layer | Wrapping/contrast changes must preserve safe Vue text rendering and exact content authority. |
| Browser/OS color override → app-owned semantic markers | Author CSS must retain structure while honoring user system colors. |
| Keyboard focus → clipping/scroll ancestors | Focus must remain visible without creating new destinations or mutation paths. |
| Browser test harness → production behavior | Evidence must drive real controls and must not add production hooks or synthetic state ownership. |

## STRIDE Threat Register — ASVS Level 1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-08-05 | Spoofing | high | low-contrast or color-only state | mitigate | Gate real rendered state composites at WCAG thresholds and preserve explicit labels/icons/signs/3px edges; block rather than whitelist failures. |
| T-08-06 | Denial of Service / Repudiation | high | clipped keyboard focus and destructive confirmations | mitigate | Traverse actual keyboard paths and compare full focus perimeter with clipping ancestors at desktop/320; keep confirmations/actions visible and focus-return behavior unchanged. |
| T-08-07 | Tampering | high | forced-color author override | mitigate | Use only targeted system-color mappings, assert no workspace `forced-color-adjust:none`, retain Monaco automatic high contrast, and verify non-color provenance/status structure. |
| T-08-08 | Tampering / Elevation of Privilege | high | untrusted text rendering | mitigate | Preserve Vue interpolation and exact route/state authorities; no `v-html`, generated untrusted CSS content, or test-only production hook may be added. |
| T-08-09 | Information Disclosure | medium | responsive test fixtures | mitigate | Keep repository-relative safe display values and existing loopback routes; do not expose absolute paths or new response data in diagnostics/screenshots. |
| T-08-SC | Tampering / Information Disclosure | low | package/asset supply chain | accept | No package, lockfile, registry, remote font/icon/asset, or network dependency is introduced. |

High-severity gate: T-08-05 through T-08-08 must be mitigated by passing rendered-state assertions; no high-severity threat may be accepted or transferred.
</threat_model>

<verification>
1. Run only the focused packaged `responsive-session.spec.ts` command; do not run formatters, linters, or project-wide suites.
2. Require source-over composite thresholds, keyboard focus perimeter geometry, real-state forced-color cues, reduced motion, and 08-01 overflow ownership to pass conjunctively.
3. Review the production diff: only `src/web/styles.css` changes, with one new root role and targeted selectors; no Vue/script/API/persistence/export/Monaco adapter changes.
4. Record headed ordinary/forced-color observations and any shared-role contrast corrections in `08-02-SUMMARY.md`.
</verification>

<success_criteria>
- A11Y-01 is proven over the actual required composite matrix at 4.5:1/3:1 thresholds without token-only shortcuts or rounding.
- A11Y-02 is proven by keyboard focus plus complete perimeter/clipping geometry over representative targets at desktop and 320px.
- A11Y-03 is proven in Chromium forced colors through system mappings and non-color labels/signs/patterns/edges with no blanket opt-out.
- RESP-01 remains true after accessibility corrections: the document fits and the diff viewport alone owns horizontal overflow.
- No API, state, persistence, command, Monaco mapping, export, package, remote asset, or live-region ownership semantics change.
- Every high-severity ASVS/STRIDE threat in this plan is concretely mitigated.
</success_criteria>

<output>
Create `.planning/phases/08-accessible-responsive-continuity/08-02-SUMMARY.md` when done.
</output>
