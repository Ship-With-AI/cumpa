---
phase: 05-semantic-dark-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/styles.css
  - scripts/verify-semantic-css.mjs
  - tests/e2e/responsive-session.spec.ts
  - tests/e2e/pinned-session.spec.ts
  - tests/integration/draft-recovery-ui.spec.ts
  - tests/integration/export-receipt-ui.spec.ts
autonomous: true
requirements:
  - VIS-01
  - VIS-02
  - VIS-03
must_haves:
  truths:
    - "VIS-01 / D-01 / D-03 / D-05 / D-06: the packaged browser resolves document, loading, empty, unavailable, recovery, workspace, drawer, overlay, and native-control presentation from one GitHub-dark-default-inspired semantic root without a light fallback or compatibility palette."
    - "VIS-02 / D-07 / D-08 / D-09: interface text uses the local system UI stack at the approved 12/16, 14/20, 16/24, and 20/28 roles with only 400 and 600 weights, while paths, object IDs, hashes, counts, and line-oriented metadata retain the local monospace stack."
    - "VIS-03 / D-04 / D-10 / D-11 / D-12: canvas, inset, panel, raised, and interactive regions are distinguishable through restrained surface steps, one-pixel boundaries, modest radii, and shadow only when an existing region becomes a true overlay."
    - "Existing selectors, Vue templates, local copy, ARIA/inert/focus behavior, state transitions, responsive drawer logic, and the 1439px, 1279px, 1099px, and 767px tiers remain structurally unchanged."
    - "Forced-colors mode retains explicit control and pane boundaries, selected rails, disabled text, links, and focus outlines through system colors without disabling ordinary browser forced-color adjustment."
    - "Phase 05 declares diff-facing semantic roles but does not call Monaco defineTheme/setTheme or alter Monaco models, tokens, diff layers, line mapping, gutters, view zones, hidden regions, geometry, or commands."
  artifacts:
    - path: "src/web/styles.css"
      provides: "Single dark semantic vocabulary, direct role consumers, explicit native-control states, responsive visual tiers, overlay-only elevation, and forced-colors fallback."
      contains: ":root"
    - path: "scripts/verify-semantic-css.mjs"
      provides: "Deterministic source/generated stylesheet clean-cutover audit for the exact canonical token root and complete retirement of both legacy vocabularies."
      contains: "dist/web/index.html"
    - path: "tests/e2e/responsive-session.spec.ts"
      provides: "Packaged Chromium computed-style contract for semantic tokens, typography, controls, status, focus, elevation, forced colors, and preserved responsive geometry."
      contains: "responsive keyboard and accessibility contract"
    - path: "tests/e2e/pinned-session.spec.ts"
      provides: "Packaged Chromium first-paint, loading, unavailable/error, and zero-file empty-state dark-surface evidence."
      contains: "identity session and empty states"
    - path: "tests/integration/draft-recovery-ui.spec.ts"
      provides: "Mounted recovery-surface dark panel, status, control, and focus evidence without changing recovery behavior."
      contains: "corrupt drafts remain read only"
    - path: "tests/integration/export-receipt-ui.spec.ts"
      provides: "Updated approved section-heading typography and static receipt-surface contract while retaining the complete receipt flow."
      contains: "renders only the confirmed receipt"
  key_links:
    - from: "src/web/App.vue"
      to: "src/web/styles.css"
      via: "the existing single global style import; no template or state-controller edits"
      pattern: "style src=\"./styles.css\""
    - from: "src/web/styles.css"
      to: "existing Vue semantic class hooks"
      via: "direct var(--surface-*), var(--text-*), var(--border-*), var(--interactive-*), var(--status-*), var(--focus-ring), var(--selection-*), and var(--diff-*) consumers"
      pattern: "var\\(--(?:surface|text|border|interactive|status|focus|selection|diff|destructive)-"
    - from: "tests/e2e/responsive-session.spec.ts"
      to: "generated package browser assets"
      via: "startGeneratedCli plus Chromium getComputedStyle assertions at the established viewport boundaries"
      pattern: "startGeneratedCli"
    - from: "scripts/verify-semantic-css.mjs"
      to: "src/web/styles.css and the stylesheet assets referenced by dist/web/index.html"
      via: "post-build source/generated token-root, declaration-allowlist, and retired definition/consumer audit"
      pattern: "verify-semantic-css"
    - from: "src/web/styles.css"
      to: "Phase 06 Monaco theming"
      via: "diff-facing custom properties only; no Phase 05 adapter edit or Monaco theme API call"
      pattern: "--diff-"
---

<objective>
Perform the single atomic Phase 05 semantic-dark clean cutover and prove it through the existing browser contracts.

Purpose: Satisfy VIS-01, VIS-02, and VIS-03 while implementing D-01 through D-12 exactly: replace both competing palettes rather than stacking another theme, preserve Diff Review identity and shipped behavior, and leave Monaco theme internals to Phase 06.
Output: One migrated production stylesheet, one deterministic source/generated stylesheet audit, plus focused packaged-Chromium and mounted-browser visual-contract updates.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/05-semantic-dark-foundation/05-CONTEXT.md
@.planning/phases/05-semantic-dark-foundation/05-RESEARCH.md
@.planning/phases/05-semantic-dark-foundation/05-PATTERNS.md
@.planning/phases/05-semantic-dark-foundation/05-UI-SPEC.md
@.planning/intel/API-SURFACE.md
@.planning/milestones/v1.0-phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-CONTEXT.md
@.planning/milestones/v1.0-phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-01-SUMMARY.md
@.planning/milestones/v1.0-phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-02-SUMMARY.md
@.planning/milestones/v1.0-phases/04-agent-ready-export/04-08-SUMMARY.md
@.planning/milestones/v1.0-phases/03-complete-review-draft/03-07-SUMMARY.md
@.planning/milestones/v1.0-phases/02-anchored-diff-review/02-10-SUMMARY.md

# Existing production and verification boundaries
@src/web/index.html
@src/web/App.vue
@src/web/styles.css
@src/web/components/ui/UiPrimitives.vue
@src/web/components/DiffWorkspace.vue
@tests/e2e/responsive-session.spec.ts
@tests/e2e/pinned-session.spec.ts
@tests/integration/draft-recovery-ui.spec.ts
@tests/integration/export-receipt-ui.spec.ts
</context>

<assumptions>
- The user explicitly approved and locked `05-UI-SPEC.md`; its stale `status: draft` and `Approval: pending` metadata are not implementation discretion and must not weaken the contract.
- `--text-muted` is retained as the canonical role name because the approved vocabulary requires it. Its presence is not a compatibility alias; every other competing Phase 1/Phase 2 palette name is removed, and there is only one root declaration.
- `src/web/index.html` already supplies `<meta name="color-scheme" content="dark">`; retain it byte-for-byte and add the CSS `color-scheme: dark` declaration in the single root. No HTML edit is required.
- Production scope is CSS-only. Test files may change to prove the new observable contract; Vue, API, persistence, export, and Monaco adapter files remain unchanged.
- Phase 05 supplies forced-colors hooks and representative evidence. Phase 08 still owns the exhaustive 320 CSS px, 400% zoom, grayscale, and overlap audit; Phase 05 must not claim that later proof.
</assumptions>

<artifacts_this_phase_produces>
## Artifacts this phase produces

### New paths
- `scripts/verify-semantic-css.mjs` — dependency-free source/generated clean-cutover and author-style audit.

### New CSS custom-property symbols in `src/web/styles.css`
- Surfaces: `--surface-canvas`, `--surface-inset`, `--surface-panel`, `--surface-raised`, `--surface-interactive`, `--surface-interactive-hover`, `--surface-interactive-active`.
- Text and boundaries: `--text-primary`, `--text-secondary`, canonical `--text-muted`, `--text-on-emphasis`, `--border-muted`, `--border-default`, `--border-strong`.
- Interaction: `--interactive-accent`, `--interactive-accent-emphasis`, `--focus-ring`, `--selection-background`, `--selection-border`, `--destructive-foreground`, `--destructive-emphasis`.
- Status: `--status-success-foreground`, `--status-success-background`, `--status-warning-foreground`, `--status-warning-background`, `--status-error-foreground`, `--status-error-background`, `--status-information-foreground`, `--status-information-background`, `--status-resolved-foreground`, `--status-resolved-background`, `--status-pending-foreground`, `--status-pending-background`, `--status-disabled-foreground`, `--status-disabled-background`.
- Diff-facing foundation: `--diff-addition-foreground`, `--diff-addition-background`, `--diff-addition-intraline-background`, `--diff-deletion-foreground`, `--diff-deletion-background`, `--diff-deletion-intraline-background`, `--diff-hunk-foreground`, `--diff-hunk-background`, `--diff-empty-background`, `--diff-unchanged-background`, `--diff-region-border`.
- Typography: `--font-ui`, `--font-mono`, `--font-size-metadata`, `--line-height-metadata`, `--font-size-body`, `--line-height-body`, `--font-size-section-heading`, `--line-height-section-heading`, `--font-size-page-heading`, `--line-height-page-heading`, `--font-weight-regular`, `--font-weight-semibold`.
- Shape/elevation: `--radius-compact`, `--radius-control`, `--radius-overlay`, `--radius-pill`, `--shadow-overlay`.
- Existing spacing symbols retained rather than recreated: `--space-xs`, `--space-sm`, `--space-md`, `--space-lg`, `--space-xl`, `--space-2xl`, `--space-3xl`.

### Other production symbols
- Vue components: none.
- TypeScript/JavaScript exports, functions, classes, or types: none.
- API routes, request/response fields, Zod schemas, persistence records, or export-contract fields: none.
- Monaco theme IDs, token rules, adapter methods, `defineTheme` calls, or `setTheme` calls: none.
- Dependencies, package scripts, fonts, icons, remote assets, registries, and environment variables: none.
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Atomically replace both visual vocabularies with one semantic dark contract</name>
  <files>src/web/styles.css, scripts/verify-semantic-css.mjs</files>
  <read_first>
    - `.planning/phases/05-semantic-dark-foundation/05-CONTEXT.md` — enforce D-01 through D-12 and later-phase ownership.
    - `.planning/phases/05-semantic-dark-foundation/05-UI-SPEC.md` — use the approved exact token, typography, spacing, shape, interaction, responsive, accessibility, and non-goal contracts.
    - `.planning/phases/05-semantic-dark-foundation/05-RESEARCH.md` — follow the atomic migration table, surface/elevation matrix, contrast findings, and raw-color audit.
    - `.planning/phases/05-semantic-dark-foundation/05-PATTERNS.md` — preserve direct semantic consumers, current cascade order, global class hooks, and breakpoint-driven overlay tiers.
    - `src/web/styles.css` — classify every legacy custom-property consumer by its actual visual responsibility before replacing it.
    - `src/web/index.html` — verify and retain the existing early dark color-scheme meta hint; do not edit it.
    - `src/web/App.vue`, `src/web/components/ui/UiPrimitives.vue`, and `src/web/components/DiffWorkspace.vue` — confirm existing class hooks and preserve templates, events, tooltip behavior, responsive state, and Monaco integration.
  </read_first>
  <action>
    Make one clean, internally consistent edit to `src/web/styles.css`; do not leave the file temporarily or finally dependent on two vocabularies.

    1. Replace the current top `:root` block and the later Phase 2 `:root` override with exactly one root declaration at the document foundation. Per D-01, D-03, D-05, and D-06, set `color-scheme: dark`, root foreground/background, and the role-based vocabulary listed in `Artifacts this phase produces`; delete the later root block, its palette-override comment, every compatibility mapping, every old definition, and every old `var(...)` consumer. Do not append a third root or duplicate token root.

    2. Use these exact approved values: canvas `#0D1117`; inset `#010409`; panel `#161B22`; raised/resting interactive `#21262D`; interactive hover `#292E36`; interactive active `#30363D`; primary text `#E6EDF3`; secondary text `#B1BAC4`; muted text `#8B949E`; emphasized text `#FFFFFF`; muted/default/strong borders `#21262D`/`#30363D`/`#484F58`; accent/accent-emphasis `#2F81F7`/`#1F6FEB`; focus and selection border `#58A6FF`; selection layer `rgb(56 139 253 / 35%)`; destructive foreground/emphasis `#F85149`/`#B62324`. Status foreground/background pairs are success `#3FB950`/`rgb(46 160 67 / 15%)`, warning `#D29922`/`rgb(187 128 9 / 15%)`, error `#F85149`/`rgb(248 81 73 / 15%)`, information `#58A6FF`/`rgb(56 139 253 / 15%)`, resolved `#A371F7`/`rgb(163 113 247 / 15%)`, pending `#B1BAC4`/`#21262D`, and disabled `#8B949E`/`#161B22`.

    3. Declare but do not apply through Monaco the diff-facing roles required by D-05: addition foreground/subtle/intraline `#3FB950`/`rgb(46 160 67 / 15%)`/`rgb(46 160 67 / 35%)`; deletion foreground/subtle/intraline `#F85149`/`rgb(248 81 73 / 15%)`/`rgb(248 81 73 / 35%)`; hunk foreground/subtle `#A371F7`/`rgb(163 113 247 / 15%)`; empty and unchanged backgrounds referencing `--surface-inset`; region border referencing `--border-default`. Do not add `.monaco-*` overrides or call/register a Monaco theme.

    4. Keep raw application color literals inside the single root declarations only, except the root-owned `--shadow-overlay: 0 8px 24px rgb(0 0 0 / 40%)` and forced-colors system keywords. Migrate all component rules to direct semantic consumers. Remove the old `--canvas`, `--panel`, `--accent`, `--destructive`, `--surface`, `--text`, `--rule`, addition/deletion/warning/info/error short names, and every `--color-*` definition/reference. Retain `--text-muted` only as the canonical approved role, not as an alias.

    5. Per D-07 through D-09, set `--font-ui` to `-apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif`, retain the existing local `ui-monospace, SFMono-Regular, Menlo, Consolas, monospace` stack as `--font-mono`, and enforce only 12px/16px metadata, 14px/20px body/control, 16px/24px section-heading, and 20px/28px page/state-heading roles with weights 400 and 600. Apply the 16px/24px/600 section role to `.comments-rail h2`, `.comments-rail h3`, `.active-file-strip h1`, `.export-section h3`, `.export-section h4`, and equivalent contained section headings; remove current 18px and default 700 results. Keep `.object-id`, `.worktree-path`, `.path-display`, `.metadata-value`, `.metadata-list dd code`, `.machine-reason`, `.diff-workspace__side-labels span`, hashes, paths, line counts, and line-oriented metadata on the mono stack. Preserve the 4/8 spacing tokens and existing 288px, 360px, and 640px layout dimensions.

    6. Per D-04, D-10, and D-11, map `:root`, `body`, `.loading-shell`, `.unavailable-shell`, `.session-shell`, `.review-shell`, `.review-main`, and `.file-metadata-pane` to canvas; recessed/code-adjacent wells and `.diff-workspace` surroundings to inset/canvas boundaries without theming Monaco internals; `.session-header`, `.review-files`, the static desktop `.comments-rail`, `.state-card`, `.draft-recovery__card`, and export/receipt/status groups to panel with one-pixel boundaries and no shadow. Use the raised role and `--shadow-overlay` only for `.identity-panel`, `.keyboard-help`, `.ui-tooltip__content` when separation is required, the comments rail when it is an overlay below 1440px, and the files drawer when it is an overlay below 1100px. Structural panes/editor hosts remain square; dense rows/tooltips use 4px radius; buttons/inputs/notices/contained cards use 6px; true overlays use 8px; only badges/statuses use 999px.

    7. Apply the full interaction matrix without changing behavior: `.identity-disclosure`, `.copy-button`, `.retry-button`, `.sheet-close-button`, `.view-tab`, `.ui-button`, `.ui-button--primary`, `.ui-button--destructive`, native buttons, text-like inputs/selects, `textarea`, checkbox/radio controls, placeholders, caret, and autofill states receive explicit dark rest, hover, active, focus-visible, disabled, pending, primary, and destructive presentation. Single-line controls are at least 32px high with 4px vertical and 8px horizontal padding. Disabled controls use the disabled foreground/background/border and no hover response; opacity is not the sole cue. Primary fill uses accent-emphasis with white text. Destructive rest stays neutral with destructive foreground/border; confirmed/hover uses destructive-emphasis, white text, and destructive border.

    8. Normalize `.dirty-badge`, `.status-badge--added`, `.status-badge--modified`, `.status-badge--deleted`, `.status-badge--moved`, `.line-counts__added`, `.line-counts__deleted`, `.availability-marker--unsupported`, `.availability-marker--unavailable`, `.inline-notice`, `.inline-notice--warning`, `.inline-notice--error`, `.draft-recovery__notice`, `.draft-recovery__notice--error`, and export/recovery progress or failure surfaces to the semantic status roles. Preserve existing labels, symbols, headings, roles, alerts, and rails. Use `--text-primary` for normal text inside tinted status containers rather than inheriting a semantic hue that research found can miss 4.5:1 after compositing.

    9. Use a single global `:focus-visible` rule with an opaque `2px solid var(--focus-ring)` outline and `2px` offset. Keep it outside hover/active state precedence and visible at pane edges. Preserve `.tree-row--selected` and `.view-tab[aria-selected="true"]` neutral selection plus the existing durable 3px accent rail/strong boundary and ARIA state; selection may not rely on fill alone. Preserve skip-link geometry and give its focused state accent-emphasis/white presentation above headers and overlays.

    10. Add one terminal `@media (forced-colors: active)` repair block, not a theme/token root. Use `Canvas`, `CanvasText`, `ButtonFace`, `ButtonText`, `ButtonBorder`, `LinkText`, `Highlight`, `HighlightText`, and `GrayText` on root/body, controls, links, disabled text, selected rails, focus outlines, pane/input/status boundaries, and highlighted selected text. Do not set `forced-color-adjust: none` on ordinary workspace elements. Retain the existing `prefers-reduced-motion: reduce` behavior and add no decorative animation.

    11. Preserve the existing `1439px`, `1279px`, `1099px`, and `767px` media-query boundaries, drawer transforms/widths, Vue-driven open classes, inert/aria-hidden ownership, focus restoration, wrapping, and review-main/Monaco-localized overflow. Do not add breakpoints, fixed page padding, or layout/state rules. At `>=1440px` all three regions are static and shadowless; at `1280–1439px` only the open comments rail is raised/shadowed; at `1100–1279px` the same overlay contract holds while header facts may wrap; at `768–1099px` both opened side regions are raised viewport-bound overlays; at `<=767px` preserve toolbar wrapping, constrained drawers, and the existing localized 640px review plane.

    12. Per D-02 and D-12, preserve every existing selector hook, Vue template, rendered string, DOM/ARIA relationship, event, responsive state transition, and Diff Review identity. Add no GitHub branding, theme selector, remote asset, font, icon, dependency, gradient, glow, glass, texture, static shadow, large soft card, new review mechanic, API/persistence/export change, or Monaco adapter change.

    13. Create `scripts/verify-semantic-css.mjs` as the focused deterministic clean-cutover gate and keep it dependency-free. After `npm run build:web`, read `src/web/styles.css`, resolve the emitted application stylesheet asset or assets from the stylesheet links in `dist/web/index.html`, and inspect the source and concatenated generated CSS separately. Require exactly one source `:root` and exactly one generated token-bearing `:root` that declares `--surface-canvas`; require the source root declaration names to equal the canonical token set in `Artifacts this phase produces` plus the retained spacing tokens, with no missing or extra/component-named declaration; and require every canonical name in the generated token root. Assert exactly one `--text-muted` declaration in each source and generated token root. Across the full source and generated CSS, fail on either a custom-property definition or a `var(...)` reference for `--canvas`, `--panel`, `--accent`, `--destructive`, `--surface`, `--text`, `--rule`, `--addition-bg`, `--addition-fg`, `--deletion-bg`, `--deletion-fg`, `--warning-bg`, `--warning-fg`, `--info-bg`, `--info-fg`, `--error-bg`, `--error-fg`, or any name matching `--color-*`. Fail when the built index has no application stylesheet link or when a linked asset is missing; report whether a failure came from source CSS or generated CSS.

    14. In the same audit, mechanically enforce the existing author-style acceptance claims against `src/web/styles.css`. Parse declarations by rule rather than relying on comments or broad substring counts. Permit hex/rgb color literals only as values of canonical custom-property declarations inside the single source token root, including the approved `--shadow-overlay`; reject raw hex/rgb/hsl literals in every component or media-query rule. Reject `@import`, remote `url(...)`, every linear/radial/conic/repeating gradient function, `backdrop-filter`, `text-shadow`, and `filter: drop-shadow(...)` so a remote import, gradient, glow, or glass treatment cannot pass. For each `box-shadow` declaration, allow `none` and existing inset selection/focus rails anywhere, but allow the non-inset `var(--shadow-overlay)` only on `.identity-panel`, `.keyboard-help`, and `.ui-tooltip__content`, on the open comments rail inside the existing `max-width: 1439px` overlay query, and on the open files drawer inside the existing `max-width: 1099px` overlay query; fail any literal, non-overlay, or static-pane exterior shadow. Keep forced-colors system keywords confined to the terminal `@media (forced-colors: active)` block and fail those keywords elsewhere.
  </action>
  <verify>
    <automated>npm run build:web &amp;&amp; node scripts/verify-semantic-css.mjs</automated>
  </verify>
  <acceptance_criteria>
    - `npm run build:web` exits 0; the deterministic audit reports exactly one `:root` in `src/web/styles.css` and exactly one canonical token-bearing `:root` across the generated stylesheet assets referenced by `dist/web/index.html`.
    - Separate full-source and full-generated scans report zero definitions and zero `var(...)` references for `--canvas`, `--panel`, `--accent`, `--destructive`, `--surface`, `--text`, `--rule`, every retired short diff/status name, and every `--color-*` name.
    - The source custom-property declaration set exactly matches the approved semantic/spacing allowlist, so no component-named or compatibility token can survive; source and generated token roots each declare canonical `--text-muted` exactly once. `src/web/styles.css` also contains no remote import, gradient, glow, glass effect, or static-pane shadow.
    - Root/body, loading, unavailable, empty, recovery, workspace, drawer, overlay, control, status, and export selectors resolve to approved dark roles without changing markup or copy.
    - All application color literals are confined to root role declarations and the approved overlay shadow; forced-colors uses only system color keywords for targeted repair.
    - No `.vue`, Monaco adapter, API, contract, persistence, export implementation, package, or HTML file is modified.
  </acceptance_criteria>
  <done>One atomic CSS-only production cutover implements VIS-01, VIS-02, VIS-03 and D-01 through D-12 with one root vocabulary and all existing consumers migrated.</done>
</task>

<task type="auto">
  <name>Task 2: Update focused Chromium contracts for every foundation state and preserved responsive tier</name>
  <files>tests/e2e/responsive-session.spec.ts, tests/e2e/pinned-session.spec.ts, tests/integration/draft-recovery-ui.spec.ts, tests/integration/export-receipt-ui.spec.ts</files>
  <read_first>
    - `src/web/styles.css` — treat the completed semantic contract as the computed-style authority; tests must exercise rendered assets rather than source substrings.
    - `.planning/phases/05-semantic-dark-foundation/05-UI-SPEC.md` sections Color, Typography, Shape and Elevation, Interaction State Contract, Responsive and Reflow Contract, and Accessibility Contract — translate every exact observable into focused assertions.
    - `.planning/phases/05-semantic-dark-foundation/05-RESEARCH.md` sections Verification Smoke Strategy, Contrast Risk, and Exact Likely Files/Symbols — use the recommended packaged and mounted-browser seams.
    - `.planning/phases/05-semantic-dark-foundation/05-PATTERNS.md` — retain `readStyles`, `contrastRatio`, `assertNoPageOverflow`, test-step grouping, and generated-package launch patterns.
    - `tests/e2e/responsive-session.spec.ts` — preserve the current packaged Chromium route fixture, semantic fixture, drawer/focus/inert checks, and viewport transitions.
    - `tests/e2e/pinned-session.spec.ts` — reuse `proveLoadingTransition` and the real `identity session and empty states` journey for loading, unavailable/error, stopped, and zero-file surfaces.
    - `tests/integration/draft-recovery-ui.spec.ts` — add visual assertions inside the real mounted corrupt-draft recovery flow without changing its request/byte-safety assertions.
    - `tests/integration/export-receipt-ui.spec.ts` — update only superseded style assertions while preserving receipt, copy, reveal, disclosure, narrow-width, and failed re-export behavior.
  </read_first>
  <action>
    Update the existing Playwright visual contracts; do not create static source-text tests or bypass production state transitions.

    1. In `tests/e2e/responsive-session.spec.ts`, extend `readStyles` only with computed properties needed for the approved contract, such as foreground color, caret color, background, borders, shadow, opacity, outline, typography, and transition. Keep `contrastRatio`, `assertNoPageOverflow`, Chromium prerequisite enforcement, generated CLI launch, route fixture, and `test.step` organization.

    2. Replace the old light-palette token expectations in the `exact palette, spacing, typography, contrast, motion tokens` step with the complete semantic root values from Task 1. Keep the runtime evidence complementary to, not a substitute for, `scripts/verify-semantic-css.mjs`: on `:root` and on every rendered child of the semantic fixture plus representative application components, query and assert empty computed values for `--canvas`, `--panel`, `--accent`, `--destructive`, `--surface`, `--text`, `--rule`, `--addition-bg`, `--addition-fg`, `--deletion-bg`, `--deletion-fg`, `--warning-bg`, `--warning-fg`, `--info-bg`, `--info-fg`, `--error-bg`, `--error-fg`, `--color-dominant`, `--color-secondary`, `--color-accent`, `--color-destructive`, `--color-text-primary`, `--color-text-secondary`, `--color-border`, `--color-hover`, `--color-added`, `--color-modified`, `--color-renamed`, and `--color-focus`. Assert canonical `--text-muted` remains non-empty. Assert root and body `color-scheme: dark`, `rgb(13, 17, 23)` canvas, and `rgb(230, 237, 243)` primary foreground.

    3. Update typography assertions to the local system UI declaration and exact 12px/16px, 14px/20px, 16px/24px, and 20px/28px roles with only 400/600 weights. Cover representative selectors from `.pin-cue`/`.availability-marker`, body/control rows, `.comments-rail h3`/`.active-file-strip h1`/export headings, and a page/state heading. Keep the `.path-display` monospace assertion and add an object-ID or hash representative so UI and mono roles cannot collapse. In the same deterministic semantic fixture, expose a `.state-card` and a standalone `.diff-workspace__gutter-action` with its existing `+` glyph and accessible name; at 1440px and 375px assert state-card padding is exactly 24px and 16px respectively, and assert the standalone icon target remains at least 32px by 32px with a 16px visible glyph and at least 8px measured separation from every target edge.

    4. Reuse the real page plus the existing injected semantic fixture for representative status/control composition: warning/error/information notices, recovery notice, neutral and primary buttons, destructive rest/confirmed state, enabled/disabled controls, `textarea`, placeholder/caret, and native checkbox. Assert rest `surface-interactive`, hover `surface-interactive-hover`, active `surface-interactive-active`, disabled semantic colors with no opacity-only cue or hover response, focus `rgb(88, 166, 255)` at 2px with 2px offset, primary accent-emphasis/white, destructive neutral rest then destructive-emphasis/white, and readable primary body text inside translucent status backgrounds. In the normal semantic palette, focus a real `.skip-link` and assert its established non-color link affordance includes an underline rather than relying on blue alone. Exercise the real `UiPrimitives` wrapper in the packaged review toolbar by pointer hover/mouse-leave and keyboard focus/focus-out or Escape: the same existing tooltip label must appear for hover and keyboard focus, and its visible `.ui-tooltip__content` must compute to 12px/16px text, 4px radius, a 1px boundary, raised dark surface, and exactly `0 8px 24px rgb(0 0 0 / 40%)` elevation while the wrapped control and static toolbar remain shadowless. Retain all existing labels, dismissal behavior, and non-color cues.

    5. Replace raw-token-only contrast checks with representative declared and composited pair checks: normal text at least 4.5:1 on canvas/panel/raised surfaces, muted/secondary text only where approved pairings meet 4.5:1, focus/selected rails and meaningful boundaries at least 3:1 against adjacent surfaces, emphasized text on primary/destructive fills at least 4.5:1, and status container body text against the actual dark/tinted computed background. Do not claim the Phase 06/08 overlap audit.

    6. Extend `review rail and drawers honor locked responsive geometry` without removing any current assertions, and call `assertNoPageOverflow` at every authoritative edge. At 1440px assert `.review-files`, `.comments-rail`, `.session-header`, `.review-main`, and representative static cards have no box shadow. At 1439px assert only the open `.comments-rail` has the exact overlay shadow and remains 360px/viewport-bound. Add the omitted 1280px/1279px pair: at 1280px assert the changed-files pane and diff remain two static columns, the Files trigger is absent, the desktop header remains unwrapped, the open comments rail is the sole raised/shadowed overlay, and document overflow is absent; at 1279px assert the same two static columns and comments overlay remain, the header facts take the approved wrapped arrangement without overlap or clipping, only the overlay is shadowed, and document overflow is absent. At 1100px retain static files plus comments overlay; at 1099px assert open files and comments are raised overlays while closed/inert/focus-restoration semantics remain; at 768px, 375px, and 320px retain wrapping, drawer width, local diff overflow, and no document-level overflow. Keep all 1440/1439/1280/1279/1100/1099/768/375/320 transitions and do not weaken target-size, keyboard, inert, or focus checks.

    7. Add a focused forced-colors sub-step with `page.emulateMedia({ forcedColors: 'active' })`. Assert root/body and ordinary controls remain legible, buttons/inputs retain explicit boundaries, selected/current rails and focus outlines remain visible through system colors, links remain distinguishable, and disabled text differs without opacity-only styling. Assert ordinary workspace components do not opt out of browser forced-color adjustment, then restore forced colors to `none`. Add a separate normal-palette reduced-motion sub-step with `page.emulateMedia({ reducedMotion: 'reduce' })`: exercise the comments rail, files drawer, and tooltip, assert their computed transition and animation durations are `0s`, root scroll behavior is `auto`, and open/closed transforms settle by the next animation frame with no 120ms transition interval; then restore `reducedMotion: 'no-preference'` before subsequent checks.

    8. In `tests/e2e/pinned-session.spec.ts`, add computed-style assertions to the existing packaged `identity session and empty states` journey: while `proveLoadingTransition` holds `/api/session`, the root/body and `.loading-shell` are dark with no light flash; the actual 403/unavailable page keeps `.unavailable-shell` and `.state-card` on canvas/panel with readable error text and no browser-default controls; the zero-file launch keeps the actual empty shell/card dark. Preserve every security, copy, stopped-session, identity, and empty-state behavior assertion.

    9. In `tests/integration/draft-recovery-ui.spec.ts`, add computed-style assertions inside `corrupt drafts remain read only until fingerprint-bound recovery response succeeds` for `.draft-recovery`, `.draft-recovery__card`, `.draft-recovery__badge`, `.draft-recovery__notice`, confirmation controls, disabled/pending state, and focused recovery action. Assert canvas/panel/semantic status colors, no static-card shadow, 6px contained radius, and 2px/2px focus treatment while retaining byte-preserving recovery and request assertions unchanged.

    10. In `tests/integration/export-receipt-ui.spec.ts`, update the confirmed receipt `h4` expectation from 18px to the approved 16px while retaining 600 weight and 24px line height. Add representative computed checks that `.export-receipt` and `.receipt-file-row` use panel/canvas steps, one-pixel boundaries, contained radii, readable text, and no static shadow at 768px/360px. Preserve the full receipt content, clipboard, reveal failure, disclosure, accessibility description, no-overflow, and re-export failure flow.

    11. Do not change Vue templates, copy, route/API fixtures beyond deterministic response timing already used by these tests, review mechanics, responsive state logic, or Monaco assertions. The packaged scenarios must drive the built CLI/browser bundle and real rendered states; mounted integration scenarios must retain their existing Vite/browser workflows.
  </action>
  <verify>
    <automated>npm run build &amp;&amp; npm run test:package -- tests/e2e/responsive-session.spec.ts &amp;&amp; npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "identity session and empty states" &amp;&amp; npm run test:browser -- tests/integration/draft-recovery-ui.spec.ts --grep "corrupt drafts remain read only" &amp;&amp; npm run test:browser -- tests/integration/export-receipt-ui.spec.ts --grep "renders only the confirmed receipt"</automated>
  </verify>
  <acceptance_criteria>
    - The exact automated command exits 0 in Chromium after building the production package assets.
    - Runtime token assertions on the root and rendered components complement the source/generated audit: they prove the complete canonical vocabulary and every explicitly enumerated retired property is empty, with the documented canonical `--text-muted` exception.
    - Packaged Chromium observes dark first paint/loading, ready workspace, unavailable/error, and zero-file empty states; mounted Chromium observes recovery and export surfaces.
    - Computed styles prove the approved four-size/two-weight typography, system UI/monospace split, explicit native-control states, semantic status body contrast, 2px/2px focus, static no-shadow surfaces, and breakpoint-only overlay shadow.
    - The responsive contract explicitly proves the 1280px unwrapped-header/two-column/comments-overlay state and the 1279px wrapped-nonoverlapping-header/two-column/comments-overlay state, including sole-overlay elevation and no document-level overflow, in addition to the existing 1440/1439/1100/1099/768/375/320 assertions.
    - At 1440px and 375px, the state card has exactly 24px and 16px padding respectively, and the standalone icon control has a minimum 32px square target, a 16px visible glyph, and at least 8px edge separation.
    - Normal-palette assertions prove a representative link retains an underline/non-color affordance, and real `UiPrimitives` pointer and keyboard-focus journeys prove the same visible label plus exact 12px/16px, 4px radius, 1px boundary, and conditional overlay-shadow tooltip contract.
    - Reduced-motion emulation proves transition/animation durations are zero, scrolling is not forced smooth, and drawer/rail/tooltip state changes settle effectively immediately; forced-colors assertions prove boundaries, focus, selection, links, and disabled states survive browser palette substitution without an ordinary-component opt-out.
    - Existing viewport, overflow, drawer, inert, focus-restoration, clipboard, recovery, receipt, and re-export assertions remain present and passing.
    - No test claims Monaco internal theming, detailed Phase 07 review-surface adaptation, or exhaustive Phase 08 accessibility/reflow completion.
  </acceptance_criteria>
  <done>Focused packaged and mounted Chromium contracts mechanically prove the full semantic dark foundation while preserving the shipped workflow and later-phase boundaries.</done>
</task>

</tasks>

<threat_model>
## Security posture

Security enforcement is active at ASVS Level 1 with a high-severity blocking threshold. This phase is presentation-only: it introduces no authentication, session, access-control, validation, persistence, serialization, filesystem, Git, or network authority. No high-severity residual threat is accepted.

## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local packaged CSS → Chromium rendering | Static project-owned CSS controls how trusted local workflow states and controls are perceived; it must not fetch remote resources or conceal consequential state. |
| Existing application state/ARIA → visual cues | Server-validated and reducer-owned states already cross into DOM labels, alerts, disabled states, selections, and controls; CSS must preserve rather than contradict those semantics. |
| OS/browser forced-colors palette → workspace paint | User-agent system colors may replace author colors and remove shadows; durable boundaries, labels, rails, and focus must survive that substitution. |
| Phase 05 CSS roles → Phase 06 Monaco consumer | Phase 05 exports presentation tokens only; it must not broaden Monaco or repository authority through unsupported internal overrides. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Spoofing | Product identity and dark adaptation | mitigate | Per D-01/D-02/D-12, retain Diff Review name/copy/IA and prohibit GitHub branding, remote assets, or a pixel-copy skin; packaged tests keep existing headings and workflow labels. |
| T-05-02 | Tampering | Destructive, disabled, pending, error, selected, and focused visual states | mitigate | Map each existing semantic/ARIA state to distinct neutral surface, text, boundary, rail, and label cues; retain event and confirmation behavior; computed-state tests cover deception-prone controls. |
| T-05-03 | Repudiation | Status and completion presentation | mitigate | Preserve explicit error/warning/pending/resolved labels, roles, live announcements, and receipt copy so hue never falsely communicates completion or suppresses a failure. |
| T-05-04 | Information Disclosure | CSS imports, fonts, images, analytics | mitigate | Add no `@import`, remote URL, font, asset, analytics, or dependency; all values remain local static CSS. |
| T-05-05 | Denial of Service | Responsive overflow, clipped focus, hidden controls | mitigate | Preserve established breakpoints, localized Monaco overflow, inert/focus management, minimum control geometry, and run exact 1440/1439/1100/1099/768/375/320 Chromium checks. |
| T-05-06 | Elevation of Privilege | API, persistence, Git, export, Monaco adapter authority | accept | No corresponding production file changes; CSS cannot grant capability. File scope and review verify only `src/web/styles.css` changes in production. |
| T-05-07 | Tampering | Forced-colors replacement removes shadow/fill-only cues | mitigate | Use system-color boundaries/rails/outlines, retain labels and symbols, and avoid `forced-color-adjust: none` on ordinary components; Chromium forced-colors checks cover the repair. |
| T-05-SC | Tampering | npm/pip/cargo installs | mitigate | No install or package mutation occurs; Package Legitimacy Audit is not applicable, and no `[ASSUMED]`, `[SUS]`, or `[SLOP]` package enters the phase. |
</threat_model>

<source_audit>
## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan/task | Status | Notes |
|--------|----|----------------------|-----------|--------|-------|
| GOAL | — | One coherent GitHub-dark-default-inspired visual foundation that retains Diff Review identity | 05-01 Tasks 1–2 | COVERED | One root CSS cutover plus packaged proof. |
| REQ | VIS-01 | Coherent dark hierarchy without light panels or browser-default flashes | 05-01 Tasks 1–2 | COVERED | Root first paint, actual state surfaces, explicit native controls. |
| REQ | VIS-02 | Compact GitHub-familiar typography/density with legible UI and code metadata | 05-01 Tasks 1–2 | COVERED | Exact type/weight/spacing roles and computed assertions. |
| REQ | VIS-03 | Restrained surface steps, borders, radii, overlay-only elevation | 05-01 Tasks 1–2 | COVERED | Selector matrix plus responsive static/overlay assertions. |
| CONTEXT | D-01 | Close dark-default semantic adaptation without branding | 05-01 Tasks 1–2 | COVERED | Exact approved palette; copy/identity preserved. |
| CONTEXT | D-02 | Identity remains name, local-first copy, IA, workflow | 05-01 Task 1 | COVERED | CSS-only production scope. |
| CONTEXT | D-03 | Dark root through loading/empty/error/controls without flashes | 05-01 Tasks 1–2 | COVERED | Root/meta/native controls and real state journeys. |
| CONTEXT | D-04 | Restrained canvas/inset/panel/raised/interactive hierarchy | 05-01 Tasks 1–2 | COVERED | Per-selector role/elevation matrix. |
| CONTEXT | D-05 | One role-named vocabulary including later diff-facing roles | 05-01 Task 1 | COVERED | Exhaustive artifact token list. |
| CONTEXT | D-06 | Replace both roots; no aliases/stacked layer | 05-01 Tasks 1–2 | COVERED | Exactly one root and runtime absence checks. |
| CONTEXT | D-07 | Local system UI plus existing mono stack; no remote font | 05-01 Tasks 1–2 | COVERED | Exact declarations and computed checks. |
| CONTEXT | D-08 | 14px baseline, 12px metadata, restrained headings | 05-01 Tasks 1–2 | COVERED | Four exact roles, 400/600 only. |
| CONTEXT | D-09 | Preserve 4/8 spacing and layout; comfortable controls | 05-01 Tasks 1–2 | COVERED | Existing space tokens and 32px controls; dimensions retained. |
| CONTEXT | D-10 | Modest 4/6/8/pill radii; square structural panes | 05-01 Tasks 1–2 | COVERED | Exact selector/radius matrix. |
| CONTEXT | D-11 | Borders/surface steps static; shadows only overlays | 05-01 Tasks 1–2 | COVERED | Exact responsive shadow assertions. |
| CONTEXT | D-12 | No gradients/glow/glass/textures/soft cards | 05-01 Task 1 | COVERED | Explicit negative boundary and raw-style cleanup. |
| RESEARCH | — | Atomic one-file production cutover and consumer migration | 05-01 Task 1 | COVERED | No parallel stylesheet plans. |
| RESEARCH | — | Raw-color removal, status composite handling, native controls, one focus rule | 05-01 Tasks 1–2 | COVERED | Direct roles plus computed/contrast evidence. |
| RESEARCH | — | Preserve responsive tiers and overlay-only shadow | 05-01 Tasks 1–2 | COVERED | Exact breakpoints and scenarios. |
| RESEARCH | — | Targeted forced-colors repair, no bespoke alternate palette | 05-01 Tasks 1–2 | COVERED | Terminal media block and browser emulation. |
| RESEARCH | — | Update responsive and receipt assertions; smoke all shell states | 05-01 Task 2 | COVERED | Four focused browser files and exact commands. |
| UI-SPEC | — | Spacing, typography, color, shape, interactions, component inventory, responsive, contrast/focus/forced-colors/motion, copy, registry, non-goals, guardrails | 05-01 Tasks 1–2 | COVERED | The source/generated CSS audit mechanically enforces the one-root clean cutover. Packaged Chromium explicitly covers 1280/1279 layout/header/overlay/overflow, reduced motion, normal-link non-color affordance, exact tooltip pointer/focus typography/shape/boundary/elevation, 24px/16px state-card padding, and 32px/16px/8px standalone icon geometry alongside the previously named contract checks. |
</source_audit>

<verification>
Run these focused checks in order; do not substitute formatters, linters, source-text-only tests, or the project-wide suite:

1. `npm run build` — compiles the Node runtime and the production Vite browser assets consumed by generated-package tests.
2. `node scripts/verify-semantic-css.mjs` — separately audits `src/web/styles.css` and the stylesheet assets referenced by `dist/web/index.html` for one canonical token root, the exact declaration allowlist, one `--text-muted`, and zero retired definitions or consumers; its source-only author-style pass also rejects raw component colors, imports/remote URLs, gradients/glow/glass constructs, misplaced forced-color keywords, and non-overlay exterior shadows.
3. `npm run test:package -- tests/e2e/responsive-session.spec.ts` — packaged Chromium proves exact tokens, typography, contrast, controls, normal-link affordance, pointer/keyboard tooltip contract, 24px/16px state-card density, 32px/16px/8px standalone icon geometry, reduced motion, forced colors, static/overlay elevation, all 1440/1439/1280/1279/1100/1099/768/375/320 responsive tiers, overflow, inert state, and focus restoration.
4. `npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "identity session and empty states"` — packaged Chromium proves dark first paint/loading, unavailable/error, stopped, ready, and zero-file empty states through actual application transitions.
5. `npm run test:browser -- tests/integration/draft-recovery-ui.spec.ts --grep "corrupt drafts remain read only"` — mounted Chromium proves the exact recovery panel/status/control/focus contract while retaining byte/recovery behavior.
6. `npm run test:browser -- tests/integration/export-receipt-ui.spec.ts --grep "renders only the confirmed receipt"` — mounted Chromium proves the exact receipt typography/surface/boundary/no-shadow contract and retained receipt flow.

Manual smoke, only after all commands pass: in the generated-package Chromium journey inspect 1440px static three-region layout; 1439px comments overlay; the 1280px unwrapped and 1279px wrapped-header two-column/comment-overlay boundary; 1099px files/comments drawers; 375px and 320px dark shell/focus/overflow; exact desktop/narrow state-card and icon-target density; delayed loading; unavailable/error; zero-file empty; recovery; textarea/checkbox/disabled/primary/destructive/status states; the normal-link underline; tooltip pointer/keyboard label and elevation; reduced motion; and forced colors. Judge Monaco host/surroundings only and explicitly record that Monaco internal theming remains Phase 06.
</verification>

<success_criteria>
- VIS-01, VIS-02, and VIS-03 are each covered by production actions and focused browser evidence.
- D-01 through D-12 and every approved UI-SPEC section have executable coverage, including deterministic source/generated clean-cutover proof and explicit mechanical coverage of every checker-identified responsive, motion, link, tooltip, and density observable.
- `src/web/styles.css` is the only modified production file; source and generated CSS each contain exactly one canonical token root, one canonical `--text-muted`, and no retired definition, stale consumer, compatibility alias, or duplicate palette.
- The deterministic stylesheet audit and all four focused Chromium commands pass after the production build.
- Existing Vue structure, copy, responsive state logic, workflow, API/persistence/export contracts, and Monaco adapter behavior remain unchanged.
- Phase 06 `defineTheme`/`setTheme` and Monaco internal token/diff-layer work are absent.
</success_criteria>

<output>
Create `.planning/phases/05-semantic-dark-foundation/05-01-SUMMARY.md` when done.
</output>
