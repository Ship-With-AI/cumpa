---
phase: 07-github-familiar-review-surfaces
plan: 05
type: execute
wave: 5
depends_on:
  - 07-04
files_modified:
  - src/web/components/DraftRecovery.vue
  - src/web/styles.css
  - tests/integration/draft-recovery-ui.spec.ts
autonomous: true
requirements:
  - REVW-04
must_haves:
  truths:
    - "Users see malformed, schema-invalid, and newer-schema drafts as explicitly Read only through an icon, visible label, and structural boundary rather than color alone (D-16)."
    - "Recovery preserves exact problem, version, repository-relative path, reveal/copy, focus, and Escape behavior without granting browser-selected filesystem authority."
    - "The destructive recovery action remains separately recognizable from focus and status, requires the existing explicit confirmation, and preserves backup-first/no-replacement semantics (D-13, D-14)."
    - "Only the acted-on recovery control shows the shared spinner, `Backing up existing draft…`, and aria-busy while pending; other disabled controls make no progress claim (D-15)."
    - "Failure remains Read only and explicitly says the draft was not replaced; success appears only after the existing verified recovered result and includes the verified backup path."
  artifacts:
    - path: "src/web/components/DraftRecovery.vue"
      provides: "Read-only/recovery badges, notices, destructive confirmation, pending, failure, and verified receipt presentation"
    - path: "src/web/styles.css"
      provides: "Recovery status, confirmation, busy-action, receipt, wrapping, reduced-motion, and focus-overlap styling"
    - path: "tests/integration/draft-recovery-ui.spec.ts"
      provides: "Real-browser malformed/schema-invalid/newer-schema, fixed-authority, destructive, pending, failure, and verified-recovery proof"
  key_links:
    - from: "src/web/components/DraftRecovery.vue"
      to: "src/web/model/review-draft-state.ts"
      via: "existing ReadOnlyDraftLoad discriminants remain the only recovery classification authority"
      pattern: "load.kind|ReadOnlyDraftLoad"
    - from: "src/web/components/DraftRecovery.vue"
      to: "src/contracts/api.ts"
      via: "fixed no-body reveal and fingerprint-bound recovery closures retain their existing request/result contracts"
      pattern: "recoverDraft|revealDraftFile|expectedFingerprint"
    - from: "src/web/components/DraftRecovery.vue"
      to: "src/web/styles.css"
      via: "read-only, destructive, pending, failure, and verified-result branches consume the Phase 07 status/control vocabulary"
      pattern: "draft-recovery__status|draft-recovery__action--busy|draft-recovery__receipt-status|ui-spinner"
    - from: "tests/integration/draft-recovery-ui.spec.ts"
      to: "src/web/components/DraftRecovery.vue"
      via: "production Vue/Vite browser harness exercises authoritative recovery branches and request shapes"
      pattern: "DraftRecovery|Backing up existing draft|Read only"
  prohibitions:
    - "No recovery API, persistence, fingerprint, byte-order, backup, replacement, reveal, copy, or continuation behavior change."
    - "No browser-selected path/ref/body authority, absolute-path expansion, automatic recovery, skipped confirmation, or claim that bytes were replaced or backed up before the existing verified result."
    - "No second live region, changed role/focus move, hidden semantic heading, icon-only status, color-only status, spinner-only pending state, pending InlineNotice tone, or invented pending UiIcon."
    - "No new breakpoint, shadowed static card, smooth motion, package, remote asset, schema contribution, or Phase 08 acceptance claim."
---

<objective>
Make the existing read-only draft recovery states explicit, safe, and locally busy using the Phase 07 status/control language without changing recovery authority or byte-safety behavior.

Purpose: Complete the recovery-specific REVW-04 slice after Plan 04 establishes notice, badge, spinner, and control presentation contracts.
Output: DraftRecovery state presentation, recovery-only CSS, and focused real-browser evidence in exactly three modified paths.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md
@.planning/phases/07-github-familiar-review-surfaces/07-RESEARCH.md
@.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md
@.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md
@.planning/phases/07-github-familiar-review-surfaces/07-04-SUMMARY.md

<interfaces>
Plan 01 provides fixed decorative `UiIcon` and shared reduced-motion-aware `.ui-spinner`; Plan 02 provides presentation-only `ReviewStateBadge`; Plan 04 provides the closed full-notice tone/icon/content anatomy and state-complete control styling. Reuse those contracts without adding another primitive or status vocabulary.

`DraftRecovery.vue` receives classified `ReadOnlyDraftLoad`, fixed no-authority `revealDraftFile()`, and fingerprint-bound `recoverDraft(expectedFingerprint)`. It owns confirmation, pending, failure, verified recovery result, exact copy/reveal actions, and focus. Those inputs, closures, transitions, and copy are authoritative.

`src/web/model/review-draft-state.ts` and `src/contracts/api.ts` are read-only authorities in this plan. No browser-selected filesystem authority, new request/result field, or alternate completion signal may be added.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make read-only recovery states explicit, safe, and locally busy</name>
  <read_first>
    - `src/web/components/DraftRecovery.vue` — modified read-only, reveal/copy, confirmation, pending, failure, and recovered receipt surface.
    - `src/web/styles.css` — modified recovery badge/notice/confirmation/receipt/busy styles and existing shared status/control vocabulary.
    - `tests/integration/draft-recovery-ui.spec.ts` — modified malformed, schema-invalid, newer-schema, fingerprint, backup, focus, and request-authority browser harness.
    - `src/web/components/ui/UiIcon.vue` — Plan 01 shared local fixed glyph source.
    - `src/web/components/ui/ReviewStateBadge.vue` — Plan 02 shared Read only/pending/success badge presentation.
    - `src/web/components/InlineNotice.vue` — Plan 04 closed full-notice tone/icon/content source; pending remains excluded.
    - `src/web/model/review-draft-state.ts` — no-change ReadOnlyDraftLoad classification source.
    - `src/contracts/api.ts` — no-change recovery result and reveal capability contracts.
    - `.planning/phases/07-github-familiar-review-surfaces/07-04-SUMMARY.md` — implemented notice, badge, spinner, and control hooks consumed here.
    - `.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md` — D-13 through D-16 and the recovery preservation boundary.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — Corrupt and Read-Only Draft State, recovery copy, action tiers, accessibility, motion, and verification item 20.
    - `.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md` — existing recovery presentation and no-backend-change boundary.
  </read_first>
  <files>src/web/components/DraftRecovery.vue, src/web/styles.css, tests/integration/draft-recovery-ui.spec.ts</files>
  <action>Retain all `DraftRecovery` computed classifications, fixed closures, expected fingerprint, async ordering, emits, exact copy, and focus behavior. Present `Read only` through `ReviewStateBadge kind="disabled" label="Read only"`; use warning/error/success notice anatomy for the recoverable problem, newer-schema blocker, reveal/copy outcomes, recovery failure, and verified recovery receipt as appropriate. Keep visible draft and backup repository-relative path values plus bounded details wrapping; do not invent an absolute path or expose request authority. This implements the non-color status contract per D-16.

In the destructive confirmation, keep exact `Start a new draft?`, byte-for-byte backup/no-replacement/no-undo text, `Keep existing draft`, and outlined destructive `Back up and start new`. Preserve the independent destructive, selected/focus, disabled, and busy channels per D-13 and D-14. While `pending`, localize the shared spinner, exact `Backing up existing draft…`, and `aria-busy="true"` to the acted-on destructive control/confirmation per D-15; preserve current disable rules and keep other controls disabled without progress claims. Pending must not use an InlineNotice tone, `.inline-notice--pending`, or pending `UiIcon`.

A failure stays Read only and explicitly says the existing draft was not replaced. Only the existing `recovered` result gets success/check structure, verified backup path, Copy backup path, and primary Open new draft. Do not synthesize recovery from a request start, optimistic state, reveal/copy result, or unverified response.

Extend the existing production Vue/Vite recovery browser harness at `tests/integration/draft-recovery-ui.spec.ts`; do not create a second harness or replace network evidence with source-text inspection. For malformed, schema-invalid, and newerUnsupported loads, assert Read only icon-label-boundary presentation, exact problem/upgrade details, fixed reveal/copy controls, no export or mutable workspace, confirmation focus/Escape, localized pending spinner/progressive label and stable bounds, failure no-replacement language, verified recovery success receipt, repository-relative path presentation, and one primary Open new draft continuation. Preserve request assertions that reveal carries no browser path authority and recovery sends only the existing fingerprint-bound request.</action>
  <acceptance_criteria>
    - Malformed, schema-invalid, and newer-schema surfaces all visibly say Read only with icon and structural boundary while retaining exact problem/path/version guidance.
    - Reveal/copy remain fixed-authority actions; recovery still requires explicit confirmation and the existing fingerprint-bound call.
    - Destructive, focused, disabled, and busy channels remain independently visible per D-13 and D-14 without changing control geometry or focus behavior.
    - Pending feedback is local `.ui-spinner` plus exact `Backing up existing draft…` plus aria-busy with stable bounds per D-15; no other control claims progress and InlineNotice still excludes pending.
    - Failure explicitly preserves read-only/no-replacement status; success/check presentation appears only after the existing verified recovered result and includes the backup path per D-16.
    - Focus, Escape, copy/reveal messages, byte-preservation order, backup verification, and Open new draft continuation remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/integration/draft-recovery-ui.spec.ts</automated>
    <browser-check>Run the focused recovery spec with `--headed`; inspect malformed, schema-invalid, newer-schema, confirmation, pending, failure, and recovered receipt states. Verify Read only and outcome meanings without hue, exact focus movement, no mutable review/export surface, pending expressed only by the shared spinner plus exact progressive text, and no success before verified backup.</browser-check>
  </verify>
  <done>REVW-04 recovery status communication and D-13 through D-16 are satisfied without changing recovery authority, request shape, byte ordering, backup verification, or continuation behavior.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Modified path 1:** `src/web/components/DraftRecovery.vue`; no new exported type, event, prop, closure, request, result, or business symbol.
- **Modified path 2:** `src/web/styles.css`; fixed recovery hooks are `.draft-recovery__status`, `.draft-recovery__action--busy`, and `.draft-recovery__receipt-status`, consuming existing `.ui-spinner`, notice, badge, focus, and reduced-motion contracts.
- **Modified path 3:** `tests/integration/draft-recovery-ui.spec.ts`; the existing production browser harness covers malformed, schema-invalid, newer-schema, confirmation, pending, failure, and verified recovery branches.
- **New visible presentation:** `Read only`, warning/error/success structure, and localized pending feedback; all product states, headings, body copy, actions, paths, and outcomes already exist.
- **New API endpoint/client method, recovery request/result field, fingerprint/persistence/Zod/schema/export field, reducer state, feature flag, package, dependency, registry block, remote asset, environment variable, or human setup:** none.

<contribution_decisions>
- **Schema contribution:** none. This plan modifies one Vue component, shared CSS, and one browser spec; no ORM/schema/migration file or live-schema operation exists, so no schema push is planned.
- **TDD contribution:** TDD mode is active, but this plan presents existing Vue state branches and proves observable behavior in Playwright. It introduces no defined-I/O business logic, API contract, or data transformation, so it remains `type: execute` rather than creating a synthetic unit seam.
- Existing roles, fixed closures, fingerprint binding, async ordering, focus targets, and copy are authoritative. Presentation consumes them and cannot broaden recovery capability.
- Icons are redundant visual reinforcement; exact headings, body copy, roles, progressive text, and verified result remain the semantic source.
- No package legitimacy checkpoint is needed because there is no install, upgrade, manifest, lockfile, registry, or remote asset change.
</contribution_decisions>

<source_coverage_audit>
## Recovery-Slice Multi-Source Coverage Audit

| Source | ID | Required outcome | Plan/task | Status |
|--------|----|------------------|-----------|--------|
| GOAL | — | Recovery remains operable within the GitHub-familiar review surface with clear hierarchy and complete interaction-state feedback | 07-05/T1 | COVERED |
| REQ | REVW-04 | Shared non-color status language covers read-only, destructive, pending, failure, and verified recovery states | 07-05/T1 | COVERED |
| CONTEXT | D-13 | Destructive recovery action remains visually distinct and outlined until interaction | 07-05/T1 | COVERED |
| CONTEXT | D-14 | Focus, destructive intent, disabled, and busy channels remain independent | 07-05/T1 | COVERED |
| CONTEXT | D-15 | Local spinner, exact progressive verb, and existing disable rules | 07-05/T1 | COVERED |
| CONTEXT | D-16 | Icon, visible label/heading, and structural boundary communicate status beyond color | 07-05/T1 | COVERED |
| RESEARCH | R-5 | Reuse shared local icon, badge, notice, and spinner primitives over existing state | 07-05/T1 | COVERED |
| RESEARCH | R-6 | Use targeted real-browser state and authority proof | 07-05/T1 | COVERED |
| RESEARCH | R-7 | Add no dependency, API, schema, persistence, Git, or recovery-service contribution | 07-05/T1 and artifact/contribution ledgers | COVERED |
| UI-SPEC | UI-3 | Preserve independent control states and reduced-motion busy meaning | 07-05/T1 | COVERED |
| UI-SPEC | UI-6 | Reuse full-notice and compact-badge semantics | 07-05/T1 | COVERED |
| UI-SPEC | UI-7 | Preserve corrupt/read-only recovery safety, exact copy, confirmation, failure, and verified receipt | 07-05/T1 | COVERED |
| UI-SPEC | UI-8 | Preserve inherited wrapping, keyboard, ARIA, focus, and Phase 08 boundary | 07-05/T1 | COVERED |
| UI-SPEC | NG-1–9 | Add no new mechanic, IA, API/schema, theme root, remote asset, breakpoint, click target, or automatic unsafe action | Frontmatter prohibitions and 07-05/T1 | COVERED |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Existing draft classification → recovery presentation | UI must represent the authoritative read-only/problem/result branch and never synthesize recovery or mutability. |
| Draft path/problem details → recovery DOM | Repository-local untrusted text must remain inert Vue text and must not become browser-selected filesystem authority. |
| Destructive confirmation → recovery capability | Presentation must preserve explicit consent, fingerprint binding, backup-first ordering, and no replacement on failure. |
| Existing result → success receipt | Success may appear only for the verified recovered result with its validated repository-relative backup path. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07-17 | Repudiation | high | recovery outcome | mitigate | Show pending until the existing result, retain failure no-replacement copy, and show success only for the verified recovered result with exact backup path/receipt assertions. |
| T-07-18 | Elevation of Privilege | high | reveal/recovery actions | mitigate | Preserve fixed no-body reveal and fingerprint-bound recovery closures; add no browser path/ref/body authority and retain request-shape assertions. |
| T-07-19 | Tampering | high | draft path/problem rendering | mitigate | Render safe validated strings as Vue text only, forbid HTML or repository-derived icon/class/path selection, and leave byte-oriented server behavior untouched. |
| T-07-20 | Denial of Service | medium | long recovery details/notices | mitigate | Use min-width zero and anywhere wrapping within existing scroll owners, retain bounded detail contracts, stable controls, and only the reduced-motion-aware shared spinner. |
| T-07-SC | Tampering | low | npm supply chain | accept | No install, upgrade, manifest, lockfile, registry, or remote asset change occurs. |

High-severity gate: T-07-17 through T-07-19 require the named verified-result, fixed-authority, inert-rendering, and real-browser mitigations; none may be accepted or transferred.
</threat_model>

<verification>
1. Run only `npm run test:browser -- tests/integration/draft-recovery-ui.spec.ts` for malformed, schema-invalid, newer-schema, confirmation, pending, failure, and verified recovery branches.
2. Repeat the focused file with `--headed` and reduced motion; inspect Read only, destructive/focus/disabled/busy overlap, exact progressive text, no-replacement failure, and verified receipt hierarchy.
3. Verify request evidence proves reveal has no browser-supplied path authority and recovery sends only the existing fingerprint-bound request after explicit confirmation.
4. Confirm only the three frontmatter paths change and no API, contract, persistence, recovery-service, package, lockfile, or Phase 08 file changes.
</verification>

<success_criteria>
- REVW-04 recovery coverage is complete for malformed, schema-invalid, newer-schema, destructive confirmation, pending, failure, and verified recovery states through text, icon, and structure beyond color.
- D-13 through D-16 and the approved UI-SPEC recovery copy, accessibility, keyboard, focus, responsive, reduced-motion, and fixed-authority contracts remain executable.
- Existing reveal, copy, fingerprint, backup-first, no-replacement, verified-result, and Open new draft behavior remains byte/transition compatible.
- T-07-17 through T-07-19 are mitigated and no backend/API/schema/persistence/package/remote asset or live-schema operation is introduced.
- The deterministic artifact inventory contains exactly the three paths declared in `files_modified`.
</success_criteria>

<output>
Create `.planning/phases/07-github-familiar-review-surfaces/07-05-SUMMARY.md` when done.
</output>
