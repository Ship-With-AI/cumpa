---
phase: 07-github-familiar-review-surfaces
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/web/components/ui/UiIcon.vue
  - src/web/components/ui/PathText.vue
  - src/web/components/PathDisplay.vue
  - src/web/components/ReviewToolbar.vue
  - src/web/App.vue
  - src/web/styles.css
  - tests/integration/anchored-workspace.spec.ts
autonomous: true
requirements:
  - VIS-04
  - REVW-01
must_haves:
  truths:
    - "Users see one compact bordered two-band header whose first band identifies Base, the active file, and Head and whose second band retains every existing diff navigation, Review, and Keyboard help action (D-01, D-02)."
    - "Users see quiet directory segments, strong filenames, and the complete old → new relationship for renamed or copied files without losing safe full path identity (D-03)."
    - "Previous/next file and change actions use local icons while retaining their exact names, shortcut tooltips, commands, and native disabled semantics; Review and Keyboard help remain visibly labeled (D-04)."
    - "Neutral, primary, destructive, icon, selected/current, focused, disabled, and busy controls have stable independent visual channels and unchanged 32px minimum geometry (D-13, D-14, D-15)."
    - "The header and toolbar wrap within the inherited breakpoints without introducing a new breakpoint, hiding an action, or transferring horizontal overflow away from the existing review-main/Monaco owner."
  artifacts:
    - path: "src/web/components/ui/UiIcon.vue"
      provides: "Closed local inline-SVG presentation vocabulary with decorative output only"
    - path: "src/web/components/ui/PathText.vue"
      provides: "Safe display-string directory/filename segmentation shared by header and later rail surfaces"
    - path: "src/web/components/PathDisplay.vue"
      provides: "Normal/deleted/renamed/copied path presentation using authoritative SessionFile values"
    - path: "src/web/components/ReviewToolbar.vue"
      provides: "Four icon-only navigation controls plus labeled Review/help controls with unchanged contracts"
    - path: "src/web/App.vue"
      provides: "Grouped Base/path/Head context band and toolbar band composed from existing session state"
    - path: "src/web/styles.css"
      provides: "Header, path, icon, selected, busy, and complete interaction-state visual hooks"
    - path: "tests/integration/anchored-workspace.spec.ts"
      provides: "Production-workspace Chromium proof for header identity, controls, state overlap, and inherited reflow boundaries"
  key_links:
    - from: "src/web/App.vue"
      to: "src/contracts/api.ts"
      via: "existing SessionResponse base/head and selected SessionFile values; no new fetch or ref resolution"
      pattern: "session.*base|session.*head|selectedFile"
    - from: "src/web/App.vue"
      to: "src/web/components/PathDisplay.vue"
      via: "selectedFile supplies old/new safe path objects and status"
      pattern: "PathDisplay.*selectedFile"
    - from: "src/web/components/ReviewToolbar.vue"
      to: "src/web/components/ui/UiIcon.vue"
      via: "fixed icon names inside existing native buttons while accessible names stay on the buttons"
      pattern: "UiIcon|aria-label"
    - from: "src/web/App.vue"
      to: "src/web/styles.css"
      via: "review-context-header and existing review-toolbar semantic classes"
      pattern: "review-context-header|review-toolbar"
  prohibitions:
    - "No new HTTP route/client method, persistence field, export field, workspace event, Git operation, ref resolution, or source-identity inference."
    - "No remote icon/font/asset, icon package, Primer/shadcn dependency, component token root, raw path reconstruction, or repository-derived SVG/class name."
    - "No action removal, overflow menu, label replacement for Review/help, shortcut change, disabled-rule change, focus-target change, or new breakpoint."
    - "No final Phase 08 claim for 320 CSS px, 400% zoom, forced colors, grayscale, or milestone-wide continuity."
---

<objective>
Create the GitHub-familiar grouped file context header and the shared local icon/control presentation contract while preserving every shipped action and state transition.

Purpose: Fulfill VIS-04 and the cross-surface REVW-01 control matrix first so later comment, rail, notice, summary, and export plans consume one established presentation vocabulary instead of inventing parallel conventions.
Output: Local icon and path primitives, grouped Base/path/Head plus toolbar composition, state-complete control CSS, and focused real-browser regression coverage.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md
@.planning/phases/07-github-familiar-review-surfaces/07-RESEARCH.md
@.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md
@.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md
@.planning/phases/05-semantic-dark-foundation/05-UI-SPEC.md
@.planning/phases/05-semantic-dark-foundation/05-01-SUMMARY.md
@.planning/phases/06-monaco-diff-semantics/06-03-SUMMARY.md
@.planning/intel/API-SURFACE.md

<interfaces>
Existing `App.vue` owns `session: ShallowRef&lt;SessionResponse | undefined&gt;`, `selectedFile: ShallowRef&lt;SessionFile | undefined&gt;`, the conditional Files drawer action, all toolbar callbacks, and the only workspace composition seam. `SessionResponse.base/head` already provide safe selector `label` plus full `oid`; the established display convention is the first seven OID characters.

Existing `PathDisplay.vue` receives one `SessionFile`, treats deleted files as old-path-owned, treats renamed/copied files as complete old → new relationships, and constructs the accessible `renamed/copied from … to …` label from server-provided safe `.display` values.

Existing `ReviewToolbar.vue` exposes props `atFirstFile`, `atLastFile`, `hasActiveFile`, `openCommentCount`, `resolvedCommentCount`, and `reviewExpanded`; it emits exactly `previousFile`, `nextFile`, `previousChange`, `nextChange`, `comments`, and `keyboardHelp` and owns the exact shortcut tooltip copy and Review ARIA relationships.

Existing `styles.css` has the sole semantic token root, global 2px/2px focus-visible ring, native disabled matrix, neutral/primary/destructive tiers, inherited breakpoints 1439/1279/1099/767, and no-shadow static-surface contract. Extend these hooks; do not create a second vocabulary.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Establish local icon, path typography, and state-complete control primitives</name>
  <read_first>
    - `src/web/components/ui/UiIcon.vue` — new modified file; verify it does not already exist before creation.
    - `src/web/components/ui/PathText.vue` — new modified file; shared safe-display presentation seam.
    - `src/web/components/PathDisplay.vue` — modified source of truth for effective and old/new path behavior.
    - `src/web/components/ReviewToolbar.vue` — modified owner of the six current toolbar actions and tooltip/ARIA contracts.
    - `src/web/styles.css` — modified sole semantic root and existing `.ui-button` rest/hover/active/focus/disabled/destructive matrix.
    - `src/web/components/ui/UiPrimitives.vue` — tooltip analog whose focus/hover/Escape behavior must stay unchanged.
    - `src/web/components/StatusBadge.vue` — existing fixed file-status badge analog; do not broaden it into review lifecycle semantics.
    - `.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md` — D-03, D-04, D-13, D-14, and D-15.
    - `.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md` — exact ReviewToolbar, PathDisplay, and native CSS analog assignments.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — Icon and Status Glyph Contract, Interaction State Contract, Typography, Spacing, and accessibility naming rules.
  </read_first>
  <files>src/web/components/ui/UiIcon.vue, src/web/components/ui/PathText.vue, src/web/components/PathDisplay.vue, src/web/components/ReviewToolbar.vue, src/web/styles.css</files>
  <action>Create presentation-only `UiIcon.vue` with a closed internal `UiIconName` union containing `previous-file`, `next-file`, `previous-change`, `next-change`, `open`, `check`, `warning`, `error`, `information`, `selected`, `lock`, and `disabled`. Render only fixed local 16×16 outline SVG paths using `currentColor`, 1.5px stroke, round caps/joins, and `aria-hidden="true"`; the component owns no label, focus, event, URL, behavior, or dynamic repository-derived path. Add `PathText.vue` with a required `display: string` prop and presentation-only last-slash segmentation into `.path-text__directory` and `.path-text__filename`; emit the exact input text in DOM order, never decode bytes, normalize separators, or construct identity. Update `PathDisplay.vue` to compose `PathText` for the existing effective path and each side of the existing old → new relationship while preserving `moveLabel`, deleted-file behavior, exact safe strings, and full-value accessibility.

Update only the four navigation button contents in `ReviewToolbar.vue` to use the corresponding `UiIcon` and add exact `aria-label` names `Previous file`, `Next file`, `Previous change`, and `Next change`; keep the existing focus/hover tooltips and shortcut strings byte-for-byte. Keep Review and Keyboard help visibly labeled, keep all six emits and disabled expressions, and bind the Review control to `.ui-button--selected` only when `reviewExpanded` is true while retaining `aria-expanded`, `aria-controls`, and the existing open/resolved screen-reader description. In `styles.css`, add `.ui-icon`, `.ui-button--icon`, `.ui-button--selected`, and `.ui-spinner` contracts: icon controls exactly 32×32px; selected uses a persistent accent boundary/rail plus text/icon meaning; spinner is 12×12px with a 2px current-color ring, one transparent segment, 600ms linear rotation, and stopped rotation under the existing reduced-motion media query. Deepen `.ui-button` states so hover changes surface/border, pressed uses the deeper active surface without shadow or movement, focus remains the existing outer ring, disabled has stable border/muted explicit treatment and no hover response, destructive stays outlined until hover/press, and busy geometry reserves inline space without changing bounds, per D-13 through D-15.</action>
  <acceptance_criteria>
    - `UiIcon` exposes only the closed fixed icon vocabulary, produces no accessible node or external request, and all icon paths are local `currentColor` geometry.
    - `PathText` and `PathDisplay` retain exact safe text and move labels while directory spans are muted and filenames are semibold primary text; normal, deleted, renamed, and copied identities remain complete.
    - The four navigation controls are 32×32px icon buttons whose accessible names, tooltip/shortcut strings, click emits, and boundary-disabled rules are unchanged; Review/help remain visible text.
    - Review selected/current treatment persists independently from the outer focus ring and existing `aria-expanded`; selected, focused, disabled+hover, destructive+focused, and busy+disabled channels remain distinguishable.
    - No new palette literal, component token root, shadow, gradient, remote asset, package, breakpoint, or product state is introduced.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "diff navigation and session state|Phase 07 header and control states"</automated>
    <browser-check>Run the same focused Playwright command with `--headed`. Tab through the four icon controls, Review, and Keyboard help; verify exact tooltips, a persistent selected Review boundary beside the separate 2px focus ring, no button movement at mouse-down, disabled controls ignoring hover, and stopped spinner motion under reduced motion.</browser-check>
  </verify>
  <done>The shared path/icon/control vocabulary implements D-03, D-04, and D-13 through D-15 without changing any toolbar behavior or authority boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Compose the compact two-band Base file Head header</name>
  <read_first>
    - `src/web/App.vue` — modified owner of session endpoints, selected file, Files drawer action, ReviewToolbar placement, and review-main composition.
    - `src/web/components/PathDisplay.vue` — modified Task 1 path relationship primitive consumed by the active header.
    - `src/web/components/ui/PathText.vue` — new Task 1 directory/filename presentation primitive.
    - `src/web/styles.css` — modified current `.review-main`, `.active-file-strip`, and `.review-toolbar` geometry plus inherited media queries.
    - `tests/integration/anchored-workspace.spec.ts` — modified production Vite/Monaco browser analog with session endpoint and navigation fixtures.
    - `src/web/components/IdentityHeader.vue` — no-change seven-character OID and control-safe endpoint-label analog.
    - `src/contracts/api.ts` — no-change SessionResponse and SessionFile data authority; no new API surface.
    - `src/domain/path-bytes.ts` — no-change control-safe display authority.
    - `.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md` — D-01 through D-04 and Phase 08 compatibility boundary.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — Grouped File Context Header, Toolbar, responsive, keyboard, and verification items 1–4 and 21–24.
    - `.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md` — current App active-file-strip/ReviewToolbar wrap analog.
  </read_first>
  <files>src/web/App.vue, src/web/styles.css, tests/integration/anchored-workspace.spec.ts</files>
  <action>In `App.vue`, replace the two unrelated visual siblings `.active-file-strip` and `ReviewToolbar` with one `.review-context-header` containing `.review-context-header__context` followed by `.review-context-header__toolbar`. Keep the existing `diff-review-heading`, quiet `Diff review` eyebrow, conditional Files button, `ReviewToolbar` props/emits, KeyboardHelp placement, and diff state order. Add local computed `baseShortOid` and `headShortOid` using exactly `session.value?.base.oid.slice(0, 7)` and `session.value?.head.oid.slice(0, 7)`; render visible BASE and HEAD blocks using only existing safe endpoint labels/full OIDs, with Base first/left and Head last/right. Render the active `selectedFile` through `PathDisplay` in the center identity; preserve the complete path through its accessible label/title and do not use `selectedPath` as the only rename/copy presentation. Do not re-resolve refs, request more metadata, infer Git identity, or move the conditional Files action out of the context band.

In `styles.css`, replace the fixed `48px auto minmax(0, 1fr)` review-main row assumption with an auto-sized grouped header plus `minmax(0, 1fr)` diff region. Give `.review-context-header` one 1px outer frame and 6px outer radius with square internal bands, `.review-context-header__context` the approved `minmax(144px, 1fr) minmax(200px, 2fr) minmax(144px, 1fr)` desktop grid expressed with `minmax(0, …)` and `min-width: 0`, and one divider between the 8px/16px context and toolbar bands. Use only the inherited spacing, typography, colors, borders, and 1439/1279/1099/767 breakpoints. Preserve Base-before-Head reading order, allow grouped actions to wrap without reordering or hiding, keep all values available when visually truncated, and leave Monaco as the sole localized two-dimensional scroll owner.

Extend `anchored-workspace.spec.ts` rather than creating another harness. Add a renamed-file fixture using authoritative old/new safe display values and a focused `Phase 07 header and control states` scenario that asserts one outer header/two bands, visible BASE/HEAD labels and seven-character OIDs, normal and old → new path anatomy, all six toolbar controls plus conditional Files behavior, exact icon names/tooltips/disabled states, Review selected/focus independence, stable control bounds across rest/hover/mousedown/disabled/busy fixture states, unchanged shortcuts, and no document-level horizontal overflow at 1440, 1280, 1100, 768, and 640px. Retain the existing Monaco BASE/HEAD, 50/50, gutter-action, paired-zone, and no-reflow assertions.</action>
  <acceptance_criteria>
    - Exactly one `.review-context-header` encloses the context and toolbar bands; no duplicate active-file bar or detached toolbar remains.
    - BASE and HEAD display the existing safe labels and exactly seven OID characters, align with left/right pane meaning, and expose no newly inferred identity.
    - A renamed/copied selected file visibly retains complete old → new identity with muted directories and strong filenames; deleted/ordinary files retain existing effective-path semantics.
    - Every shipped toolbar action remains in DOM/visual order with unchanged event, shortcut, tooltip, ARIA, focus, disabled, and drawer behavior.
    - At inherited representative widths the bands remain grouped, content/actions wrap or safely truncate with full values available, and `document.scrollWidth &lt;= document.clientWidth`; no new breakpoint or overflow menu exists.
    - Existing Monaco pane ratio, x-origin, gutter width, comment action, paired-zone tops/heights, keyboard commands, and file switching remain unchanged.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "diff navigation and session state|Phase 07 header and control states|no-reflow Monaco semantic channels"</automated>
    <browser-check>Run the focused file with `--headed`; inspect ordinary and renamed files at 1440, 1280, 1100, 768, and 640px. Confirm one framed two-band header, left BASE/center path/right HEAD hierarchy, complete rename identity, exact navigation tooltips, visible Review/help labels, independent selected/focus cues, no hidden action, and no page-wide horizontal scroll.</browser-check>
  </verify>
  <done>VIS-04 and the header/control portion of REVW-01 are proven in the production workspace without changing review mechanics, Monaco geometry, or responsive ownership.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New file:** `src/web/components/ui/UiIcon.vue`.
- **New local type/symbol:** `UiIconName` closed union with `previous-file`, `next-file`, `previous-change`, `next-change`, `open`, `check`, `warning`, `error`, `information`, `selected`, `lock`, and `disabled`; presentation-only `name` prop.
- **New file:** `src/web/components/ui/PathText.vue`.
- **New component contract:** required `display: string` prop and fixed `.path-text`, `.path-text__directory`, `.path-text__filename` spans; no identity transform/exported helper.
- **New App-local computed symbols:** `baseShortOid` and `headShortOid`, each a seven-character presentation of the already pinned OID.
- **New fixed CSS hooks:** `.ui-icon`, `.ui-button--icon`, `.ui-button--selected`, `.ui-button--busy`, `.ui-spinner`, `.path-text`, `.path-text__directory`, `.path-text__filename`, `.review-context-header`, `.review-context-header__context`, `.review-context-header__toolbar`, `.review-context-header__endpoint`, `.review-context-header__endpoint-label`, `.review-context-header__endpoint-oid`, and `.review-context-header__file`.
- **New product state, persisted field, workspace event/command, API endpoint/client method, Zod/schema field, ORM/migration, export field, feature flag, package, dependency, registry block, remote asset, environment variable, or human setup:** none.
- **Schema-push assessment:** not applicable. Every modified path is Vue, CSS, or Playwright; repository inspection found no ORM/schema/migration path in phase scope, so no live-schema push task is created.

<contribution_decisions>
- TDD mode is active, but both tasks are Vue presentation/CSS/composition and browser-contract work, not defined-I/O business logic, API behavior, or a data transformation contract; this plan therefore remains `type: execute` and adds focused Playwright proof after the UI work.
- The incomplete `.planning/intel/API-SURFACE.md` is only a hint. Direct source inspection establishes that existing SessionResponse/SessionFile data is sufficient, so no backend, API, persistence, or Git change is planned.
- `PathText` segments only an already safe display string for typography. It is not a path parser, normalizer, capability, or byte-identity transform.
- Phase 08 proof is not pulled forward: Phase 07 verifies representative inherited widths and avoids regressions, while final 320px/400%/forced-colors/grayscale/composite-contrast/end-to-end acceptance remains Phase 08.
</contribution_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository-derived safe labels/OIDs/paths → Vue header text | Pinned repository identity is untrusted display content and must stay text, never HTML, SVG path data, CSS, selector, or ref authority. |
| Fixed local icon vocabulary → native toolbar buttons | Decorative SVG must not replace button names, disabled semantics, shortcut help, or focus behavior. |
| Visual control state → reviewer interpretation | Selected, focus, disabled, destructive, and busy meaning can be spoofed if channels collapse or bounds move. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | high | header path/selector rendering | mitigate | Render only existing control-safe strings through Vue text bindings; forbid `v-html`, reconstructed path bytes, dynamic SVG paths/classes, and ref re-resolution; browser-test normal and renamed identity. |
| T-07-02 | Spoofing | high | Base/Head and toolbar semantics | mitigate | Keep visible BASE/HEAD labels, seven-character pinned IDs, side order, exact button names/tooltips/shortcuts, native disabled rules, and independent selected/focus channels under Playwright. |
| T-07-03 | Denial of Service | medium | long labels and paths | mitigate | Use `minmax(0, …)`, `min-width: 0`, safe truncation with full values available, wrapping groups, and document-overflow assertions at inherited widths. |
| T-07-04 | Information Disclosure | medium | repository identity | mitigate | Show only already authorized safe selector labels, pinned OIDs, and repository-relative safe paths; add no absolute path, worktree path, stderr, network request, or new endpoint. |
| T-07-05 | Information Disclosure | high | icons/assets | mitigate | Use fixed inline SVG and system fonts only; forbid remote URLs, packages, telemetry, and repository-derived icon content; assert no external asset contribution. |
| T-07-SC | Tampering | low | npm supply chain | accept | No install or upgrade occurs; package.json and lockfile are outside `files_modified`, and any dependency drift blocks execution. |

High-severity gate: T-07-01, T-07-02, and T-07-05 must be mitigated by the named source constraints and focused browser assertions. No high-severity finding may be accepted or transferred.
</threat_model>

<verification>
1. Run only the focused production-workspace Playwright cases: `npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "diff navigation and session state|Phase 07 header and control states|no-reflow Monaco semantic channels"`.
2. Repeat that focused command with `--headed` and inspect ordinary/renamed identity, every control tier/state overlap, tooltips, keyboard focus, reduced-motion spinner, and inherited widths 1440/1280/1100/768/640.
3. Confirm the changed-file list remains the seven declared Vue/CSS/Playwright paths and that package/API/schema/persistence/Monaco-adapter files are untouched.
</verification>

<success_criteria>
- VIS-04 and REVW-01 are satisfied by observable real-browser behavior, not source-text assertions.
- D-01 through D-04 and D-13 through D-15 are implemented exactly, with no toolbar action, label contract, command, ARIA relationship, focus target, disabled rule, or responsive owner lost.
- Header/path/control work consumes the Phase 05 semantic root and Phase 06 Monaco boundaries without adding a palette, shadow, breakpoint, remote asset, or review mechanic.
- Every high-severity ASVS L1 threat in this plan is mitigated; no absolute/repository-authority data or remote resource is introduced.
</success_criteria>

<output>
Create `.planning/phases/07-github-familiar-review-surfaces/07-01-SUMMARY.md` when done.
</output>
