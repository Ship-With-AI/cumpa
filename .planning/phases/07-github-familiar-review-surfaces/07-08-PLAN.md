---
phase: 07-github-familiar-review-surfaces
plan: 08
type: execute
wave: 7
depends_on:
  - 07-06
files_modified:
  - src/web/components/ExportSection.vue
  - src/web/components/ExportReadinessSummary.vue
  - tests/integration/export-receipt-ui.spec.ts
autonomous: true
gap_closure: true
requirements:
  - REVW-04
must_haves:
  truths:
    - "A revision conflict presents one error badge labeled `Review changed`; it never combines error structure with the false label `Ready` (D-16)."
    - "While ignore status is `null`, both Export readiness and GitignoreStatus describe an in-progress check; unavailable wording is reserved for the concrete `unavailable` result (D-15, D-16)."
    - "Every corrected status remains derived from the existing `ReviewExportState.phase` and `DiffReviewIgnoreStatus | null` authorities, with no export, reload, ignore mutation, or filesystem transition change."
    - "Focused Chromium evidence observes the conflict badge itself and holds the ignore-status request long enough to distinguish pending from unavailable."
  artifacts:
    - path: "src/web/components/ExportSection.vue"
      provides: "Truthful conflict badge label paired with the existing error kind"
    - path: "src/web/components/ExportReadinessSummary.vue"
      provides: "Exhaustive null/ignored/notIgnored/unavailable ignore-status presentation"
    - path: "tests/integration/export-receipt-ui.spec.ts"
      provides: "Delayed ignore-status and conflict-heading browser assertions over the production export surface"
  key_links:
    - from: "src/web/components/ExportSection.vue:stateLabel"
      to: "src/web/model/review-draft-state.ts:ReviewExportState.phase"
      via: "closed switch including the existing `conflict` branch"
      pattern: "case 'conflict'"
    - from: "src/web/components/ExportReadinessSummary.vue:ignoreStatusPresentation"
      to: "src/web/components/GitignoreStatus.vue"
      via: "matching pending, ignored, not-ignored, and unavailable semantics from the same prop"
      pattern: "ignoreStatus === null|kind === 'unavailable'"
---

<objective>
Close G-02a / WR-02 and G-02b / WR-03 by making export conflict and ignore-check status labels truthful, explicit, and consistent with their existing state authorities per D-15 and D-16.

Purpose: Restore the failed REVW-04 must-have without changing export, revision, drift, publication, receipt, or `.gitignore` behavior.
Output: Two exhaustive presentation mappings and focused production-browser assertions for the previously unobserved contradictory states.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-github-familiar-review-surfaces/07-CONTEXT.md
@.planning/phases/07-github-familiar-review-surfaces/07-RESEARCH.md
@.planning/phases/07-github-familiar-review-surfaces/07-PATTERNS.md
@.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md
@.planning/phases/07-github-familiar-review-surfaces/07-VERIFICATION.md
@.planning/phases/07-github-familiar-review-surfaces/07-REVIEW.md
@.planning/phases/07-github-familiar-review-surfaces/07-06-SUMMARY.md

<interfaces>
`ExportSection.vue:stateKind` already maps `conflict` to `error`; `stateLabel` omits that branch and reaches `default: 'Ready'`. The visible heading owns `ReviewStateBadge :kind="stateKind" :label="stateLabel"`.

`ExportReadinessSummary.vue:ignoreStatusPresentation` receives `DiffReviewIgnoreStatus | null`. `null` is the in-flight state already rendered by `GitignoreStatus.vue` as `Checking export directory ignore status`; the concrete `unavailable` discriminant is the only unavailable result.

`tests/integration/export-receipt-ui.spec.ts` already owns `Phase 07 explicit export and status states`, queued revision conflicts, request counts, real Vite/Chromium rendering, and the gitignore middleware. Extend that harness rather than adding component-source assertions.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make conflict and ignore-check status mappings exhaustive and observable</name>
  <read_first>
    - `src/web/components/ExportSection.vue:38-90` — current `stateLabel`, `stateKind`, heading badge, and revision-conflict branch.
    - `src/web/components/ExportReadinessSummary.vue:1-40` — current ignore-status projection that conflates `null` and `unavailable`.
    - `src/web/components/GitignoreStatus.vue:72-107` — authoritative user-facing meanings for null, ignored, unavailable, and notIgnored.
    - `src/contracts/api.ts:225-244` — strict `DiffReviewIgnoreStatus` discriminants.
    - `tests/integration/export-receipt-ui.spec.ts:1-120` — current ignore-status middleware and held-export fixture patterns.
    - `tests/integration/export-receipt-ui.spec.ts:337-402` — current Phase 07 export scenario, conflict alert assertion, and missing badge/null checks.
    - `.planning/phases/07-github-familiar-review-surfaces/07-VERIFICATION.md` — G-02a and G-02b failed truths.
    - `.planning/phases/07-github-familiar-review-surfaces/07-REVIEW.md` — WR-02 and WR-03 diagnoses.
    - `.planning/phases/07-github-familiar-review-surfaces/07-UI-SPEC.md` — exact conflict heading, status language, pending semantics, and export-transition prohibitions.
  </read_first>
  <files>src/web/components/ExportSection.vue, src/web/components/ExportReadinessSummary.vue, tests/integration/export-receipt-ui.spec.ts</files>
  <action>Add an explicit `case 'conflict': return 'Review changed'` to `ExportSection.vue:stateLabel`, preserving the existing `stateKind === 'error'`, conflict alert heading/body, focus move, `Reload latest`, and all phase transitions. Do not rename `ReviewExportState`, add presentation state, or infer readiness from receipt/drift values. The Export heading must therefore expose one error badge whose visible text is `Review changed` throughout the conflict branch per D-16.

In `ExportReadinessSummary.vue:ignoreStatusPresentation`, branch on `props.ignoreStatus === null` before inspecting discriminants and return `kind: 'pending'` with visible label `Checking ignore status`. Keep `ignored` as success and `notIgnored` as warning. Reserve `kind: 'disabled'` with `Ignore status unavailable` for the concrete `unavailable` discriminant only. Do not omit the readiness row, introduce a second polling state, change `GitignoreStatus.vue`, or alter export availability; this is an exhaustive presentation of the same authoritative value per D-15 and D-16.

Extend the existing export browser fixture with test-local `IgnoreStatusKind = 'ignored' | 'notIgnored' | 'unavailable'`, `heldIgnoreStatus: PromiseWithResolvers<void> | null`, `holdNextIgnoreStatus()`, and `completeHeldIgnoreStatus()` so a GET `/api/export/gitignore` response can be paused without changing production code. Reset the held state between tests. In `Phase 07 explicit export and status states`, assert the Export heading badge says `Review changed` while the revision-conflict alert is active and never contains `Ready`. Add a held initial ignore-status observation: while the GET is pending, assert the readiness row shows a pending `Checking ignore status` badge and `GitignoreStatus` simultaneously shows `Checking export directory ignore status`; then release a concrete `unavailable` response and assert both surfaces change to unavailable wording/structure. Retain all existing export request-body/count, reload-no-auto-export, progress, failure, and no-receipt assertions.</action>
  <acceptance_criteria>
    - Conflict maps to visible `Review changed` plus error kind and no `Ready` text exists in the conflict Export heading.
    - `null` maps to pending `Checking ignore status`, while only `{ kind: 'unavailable' }` maps to disabled `Ignore status unavailable`.
    - ExportReadinessSummary and GitignoreStatus describe the same authoritative value consistently in pending and unavailable states.
    - Focus, alert/status roles, explicit export action, revision conflict, reload, drift, receipt, publication, and ignore mutation behavior remain unchanged.
    - Real Chromium deterministically observes the pending interval and the later unavailable result; no source-text-only assertion substitutes for behavior.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:browser -- tests/integration/export-receipt-ui.spec.ts --grep "Phase 07 explicit export and status states"</automated>
    <browser-check>Run only the same case with `--headed`; pause on conflict and on held ignore status. Confirm the error badge says Review changed, pending readiness and GitignoreStatus agree, and the unavailable result replaces pending wording without triggering export or `.gitignore` mutation.</browser-check>
  </verify>
  <done>G-02a / WR-02 and G-02b / WR-03 are closed by exhaustive state mappings and deterministic production-browser evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

- **Modified existing production mappings:** `ExportSection.vue:stateLabel` and `ExportReadinessSummary.vue:ignoreStatusPresentation`; no new exported production symbol.
- **New test-local type:** `IgnoreStatusKind = 'ignored' | 'notIgnored' | 'unavailable'` in `tests/integration/export-receipt-ui.spec.ts`.
- **New test-local held-response symbols:** `heldIgnoreStatus`, `holdNextIgnoreStatus()`, and `completeHeldIgnoreStatus()` in the existing export browser harness.
- **Extended existing browser artifact:** `Phase 07 explicit export and status states` gains conflict-badge and pending→unavailable ignore-status assertions.
- **New source path, component, prop, event, API/client method, request/result field, persistence/schema/export field, feature flag, package, dependency, remote asset, environment variable, or human setup:** none.
- **Schema-push assessment:** not applicable; only existing Vue and Playwright files are modified, with no ORM/schema/migration path.

<contribution_decisions>
- TDD mode is active, but these are existing Vue computed presentation mappings and browser glue, not new business logic, API behavior, or a data transformation contract; the plan remains `type: execute`.
- The existing state machine remains the only authority. No UI fallback may claim readiness, unavailability, success, or completion without the matching discriminant.
- This plan is independent of 07-07 and 07-09 at closure wave 7: it shares no modified path with either and consumes the executed post-07-06 baseline.
</contribution_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `ReviewExportState.phase` → heading badge | A contradictory label can spoof export readiness even when error styling and the alert are correct. |
| Async ignore-status lifecycle → readiness language | `null` is pending, not failure; collapsing it with unavailable misrepresents system state. |
| Browser fixture timing → verification claim | An immediately resolved GET can skip the pending UI and produce false confidence. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-07-G04 | Spoofing | high | `ExportSection.stateLabel` | mitigate | Map `conflict` explicitly to `Review changed`, keep error kind and conflict alert, and assert the heading badge itself never says Ready. |
| T-07-G05 | Spoofing | high | `ExportReadinessSummary.ignoreStatusPresentation` | mitigate | Distinguish null pending from concrete unavailable, reuse visible progressive/disabled labels, and assert consistency with GitignoreStatus. |
| T-07-G06 | Repudiation | high | export browser fixture | mitigate | Hold and release the real middleware response so Chromium observes both states; retain request counts proving no passive state triggers export or ignore mutation. |
| T-07-G07 | Elevation of Privilege | medium | export/ignore capabilities | mitigate | Change presentation only; preserve fixed client closures and add no request field, path authority, automatic export, reload, acknowledgement, or append behavior. |
| T-07-SC | Tampering | low | npm supply chain | accept | No install, upgrade, manifest, lockfile, package, registry, or remote asset change occurs. |

High-severity gate: T-07-G04 through T-07-G06 require the explicit mappings and held-response browser assertions; none may be accepted or transferred.
</threat_model>

<verification>
1. Run only the named focused Playwright case; do not run formatters, linters, builds, unit suites, or project-wide suites.
2. Observe conflict, held pending ignore status, and released unavailable status in real Chromium while preserving existing export request counts and no-auto-action evidence.
3. Confirm the three modified paths are exactly those declared and no API, state-machine, persistence, schema, filesystem, package, or Phase 08 file changes.
</verification>

<success_criteria>
- REVW-04 and roadmap criterion 5 are no longer blocked by contradictory conflict or ignore-status labels.
- D-15 and D-16 accurately represent pending, disabled, and error states through visible text, icon, and structure without color-only or fallback meaning.
- Existing export/drift/revision/publication/receipt/gitignore behavior remains transition-compatible.
- Every high-severity threat is concretely mitigated with no backend, schema, dependency, or filesystem-authority scope.
</success_criteria>

<output>
Create `.planning/phases/07-github-familiar-review-surfaces/07-08-SUMMARY.md` when done.
</output>
