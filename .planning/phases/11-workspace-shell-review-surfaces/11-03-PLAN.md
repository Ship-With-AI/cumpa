---
phase: 11-workspace-shell-review-surfaces
plan: 03
type: execute
wave: 3
depends_on: ["11-01", "11-02"]
files_modified:
  - src/web/components/DetailsDialog.vue
  - src/web/components/IdentityPanel.vue
  - src/web/components/FileMetadataPane.vue
  - src/web/components/KeyboardHelp.vue
  - src/web/App.vue
  - src/web/styles.css
  - tests/e2e/pinned-session.spec.ts
  - tests/e2e/responsive-session.spec.ts
  - tests/integration/anchored-workspace.spec.ts
autonomous: true
requirements: [REV-03]

must_haves:
  truths:
    - "A reviewer opens Details from the header and reads, in order: the comparison identities, the scope and provenance facts, the selected file's metadata, and the keyboard help — all in one modal surface."
    - "On an exact-patch session Details shows preimage/postimage, patch digest, review key, changed-file count, validation target, and frozen-snapshot provenance — and no fabricated commit or merge-base fields."
    - "Details is a modal dialog at every viewport width, traps Tab, makes the review shell inert, closes on Escape, and returns focus to the Details trigger."
    - "Selected-file metadata in Details is fetched live for the selected file, with loading, error, retry, and stale-response handling — the pane is no longer orphaned."
    - "The keyboard-help surface no longer lives inside the main review region, and both the toolbar trigger and the ? shortcut still reach it."
  artifacts:
    - path: "src/web/components/DetailsDialog.vue"
      provides: "Details modal composing identity, file metadata, and keyboard help inside ModalDialog"
      min_lines: 60
      exports: ["default"]
    - path: "src/web/components/IdentityPanel.vue"
      provides: "Always-content identity/scope rows with its dual-mode root and local focus trap removed"
      min_lines: 40
    - path: "src/web/components/FileMetadataPane.vue"
      provides: "Selected-file metadata content with a non-landmark root and a live data path"
      contains: "aria-label=\"File details\""
  key_links:
    - from: "src/web/App.vue"
      to: "src/web/api/client.ts"
      via: "selected-file metadata fetch through sessionClient.getFileMetadata, which has zero callers today"
      pattern: "getFileMetadata"
    - from: "src/web/components/DetailsDialog.vue"
      to: "src/web/components/ui/ModalDialog.vue"
      via: "Details renders inside the shared modal primitive from plan 01"
      pattern: "ModalDialog"
    - from: "src/web/components/IdentityHeader.vue"
      to: "src/web/components/DetailsDialog.vue"
      via: "Details trigger opens the dialog and receives focus back on close"
      pattern: "aria-haspopup"
---

<objective>
REV-03: replace the permanent metadata toolbar with a Details dialog holding comparison identities, commit and merge-base data, selected-file metadata, and keyboard help.

Two of the three content sources are not simply movable. `IdentityPanel.vue` ships a dual-mode root that must collapse to always-modal. `FileMetadataPane.vue` is app-orphaned with no data path at all — `sessionClient.getFileMetadata` exists and has zero callers — so reviving it is fetch wiring, not a mount.

Purpose: the header becomes an identity strip only; everything it used to carry permanently now lives one keystroke away.
Output: one Details dialog, one revived metadata pane with a real data path, one keyboard-help surface outside the main region.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md
@.planning/phases/11-workspace-shell-review-surfaces/11-PATTERNS.md
@.planning/phases/11-workspace-shell-review-surfaces/11-01-SUMMARY.md
@.planning/phases/11-workspace-shell-review-surfaces/11-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Revive FileMetadataPane — give it a data path and a non-landmark root</name>
  <files>src/web/components/FileMetadataPane.vue, src/web/App.vue</files>
  <read_first>
    - `src/web/components/FileMetadataPane.vue:162-176` — `focusHeading`, `getScrollPosition`, `setScrollPosition`, and the `defineExpose` on line 176. These accessors are the pane's public API and the Details dialog will use `focusHeading`.
    - `src/web/components/FileMetadataPane.vue:179-181` — the template opens at 179; line 180 is `<main ref="paneElement" class="file-metadata-pane" aria-label="File details">`, and the heading with `tabindex="-1"` follows. The root is a second `main` landmark.
    - `src/web/App.vue:1181` — `<main class="review-main" …>`, the page's real `main`. Nesting a second `main` inside a `role="dialog"` alongside it is an accessibility defect that no current spec catches, because all three `getByRole('main', …)` locators in `tests/e2e/pinned-session.spec.ts` (`:896`, `:1387`, `:1410`) are name-scoped and therefore stay unique under Playwright strict mode.
    - `src/web/api/client.ts:80` — the `getFileMetadata(fileId: string): Promise<FileMetadataResponse>` declaration. `src/web/api/client.ts:271-275` — its implementation. `grep -rn "getFileMetadata" src/ tests/` returns only these two lines: the method has zero callers.
    - `src/web/App.vue:292-318` (`loadFile`) — the fetch analog to copy: clear the error, clear the previous value, bump a monotonic `requestVersion`, set loading, `await`, discard the response when `version !== requestVersion` or the id does not match, map `SessionClientError` to its message with a shared fallback constant, and clear loading in `finally` only for the current version.
    - `tests/e2e/pinned-session.spec.ts:55-98` — the only mount of this component today: a Playwright harness rendering four `FileMetadataPane` instances with literal props. It is the existing proof of the pane's rendering contract and must keep passing.
  </read_first>
  <behavior>
    Write these first and watch them fail:
    - Test 1: selecting a file populates the pane with that file's status, reviewability or availability explanation, old and new path, signed addition and deletion counts, and old and new modes.
    - Test 2: while the fetch is in flight the pane shows its loading state; on failure it shows the error message and a retry control that re-issues the request.
    - Test 3: selecting file A then file B before A resolves leaves B's metadata displayed — the stale response for A is discarded.
    - Test 4: the pane's root is not a `main` landmark, and the page still exposes exactly one `main`.
    - Test 5: the harness at `tests/e2e/pinned-session.spec.ts:55-98` still passes with only the root element name changed in its expectations, if at all.
  </behavior>
  <action>
    Change the pane's root element from a landmark to a non-landmark container, keeping `aria-label="File details"`, keeping `ref="paneElement"` so the scroll accessors keep working, keeping the `tabindex="-1"` heading, and keeping the `defineExpose` set unchanged.

    Add the metadata fetch to `App.vue` alongside the existing file-content load. Model it exactly on `loadFile`: its own monotonic request counter, its own loading and error refs, the same `SessionClientError` mapping, the same stale-response discard, the same `finally` guard. Trigger it when the selected file changes, and expose a retry that re-issues the current request. Bind the result, the loading flag, and the error message to the pane's existing props and wire its retry event.

    Do not fetch metadata for every file up front, and do not fetch on dialog open only — the pane must reflect the currently selected file whether Details is open or not, so the reviewer never sees another file's metadata after switching.
  </action>
  <acceptance_criteria>
    - `grep -c '<main' src/web/components/FileMetadataPane.vue` is `0`.
    - `grep -c 'aria-label="File details"' src/web/components/FileMetadataPane.vue` is exactly `1`.
    - `grep -c 'defineExpose({ focusHeading, getScrollPosition, setScrollPosition })' src/web/components/FileMetadataPane.vue` is exactly `1`.
    - `grep -c 'getFileMetadata' src/web/App.vue` is at least 1 — the method had zero callers before this task.
    - `npm run build:web && npx playwright test tests/e2e/pinned-session.spec.ts` passes, including the four-instance harness at `:55-98`.
    - `npm run typecheck:web` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web &amp;&amp; npm run build:web &amp;&amp; npx playwright test tests/e2e/pinned-session.spec.ts</automated>
  </verify>
  <done>Behaviours 1-5 hold, `sessionClient.getFileMetadata` has a real caller, and the page exposes exactly one `main` landmark.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build DetailsDialog and collapse IdentityPanel to content-only</name>
  <files>src/web/components/DetailsDialog.vue, src/web/components/IdentityPanel.vue, src/web/components/KeyboardHelp.vue, src/web/styles.css</files>
  <read_first>
    - `src/web/components/ui/ModalDialog.vue` — the primitive from plan 01. Read its prop and slot contract before composing against it.
    - `src/web/components/IdentityPanel.vue:76-87` — the dual-mode root: `:role="modal ? 'dialog' : 'region'"`, `:aria-modal="modal ? 'true' : undefined"`, `:class="{ 'identity-panel--modal': modal }"`, `@keydown="containFocus"`, and a `v-if="modal"` close button whose copy branches across `Close patch scope` / `Close review scope` / `Close comparison identities`.
    - `src/web/components/IdentityPanel.vue:50-58` — the local `containFocus` copy, whose `querySelectorAll` on line 56 covers `button:not(:disabled)` only. Plan 01's primitive replaces it; delete it rather than porting it.
    - `src/web/components/IdentityPanel.vue:5, 22-32` — the existing `controlSafeDisplay` usage on displayed identities.
    - `src/web/components/KeyboardHelp.vue:12` — `<section v-if="open" class="keyboard-help" aria-labelledby="keyboard-actions-heading">`, and `:14` the `Keyboard actions` heading with `tabindex="-1"`.
    - `src/web/styles.css:675-681` — `.identity-panel` and `.keyboard-help` share the overlay-shadow rule group with `.ui-tooltip__content`. Both selectors are in `overlayAllowlist` at `scripts/verify-semantic-css.mjs:230-233`; removing a selector from the stylesheet without removing its allowlist entry, or the reverse, changes what the gate enforces.
    - `src/web/styles.css:2415-2433` (as retargeted by plan 01) — `.identity-panel { width: min(480px, calc(100vw - 32px)) }`, now superseded by `--dialog-width`.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md` — the Details (REV-03) section, which fixes the trigger copy, the heading, the close copy and accessible name, initial focus, and the full content order for both session kinds.
  </read_first>
  <behavior>
    Write these first and watch them fail:
    - Test 1: the Details dialog's heading is `Details`, its close control's visible copy is `Close` with accessible name `Close details`, and initial focus lands on that close control.
    - Test 2: content appears in the order comparison, scope and provenance, selected file, keyboard help — verified by DOM order of the section headings, not by visual position.
    - Test 3: on a pinned comparison, Details exposes ordered Base and Head selector labels and types, full Base and Head commit IDs with their existing copy actions, the merge-base commit ID, ordered Git pathspecs and commit IDs where a scoped range exists, and dirty/source-availability facts.
    - Test 4: on an exact-patch session, Details exposes preimage and postimage identity, patch digest, review key, changed-file count, validation target, and frozen-snapshot provenance, and contains no merge-base field and no Base/Head wording.
    - Test 5: full OIDs, paths, hashes, and digests wrap or scroll inside their value cell; the document gains no horizontal overflow with the dialog open.
    - Test 6: Tab and Shift+Tab stay inside the dialog, the review shell carries `inert`, Escape closes, and focus returns to the `Details` trigger.
  </behavior>
  <action>
    Create `DetailsDialog.vue` rendering inside `ModalDialog.vue` with the exact heading, close copy, and accessible name from the UI-SPEC, and composing three existing content sources in the specified order: `IdentityPanel.vue` for comparison and scope, `FileMetadataPane.vue` for the selected file, `KeyboardHelp.vue` for the commands. The dialog is the only owner of dialog semantics and focus; the three children contribute content and headings only.

    Collapse `IdentityPanel.vue` to content. Delete `containFocus` and its `@keydown` binding, delete the `role`, `aria-modal`, and modal-class bindings on its root, and delete the `v-if="modal"` close button — the primitive owns close. Once the `modal` prop is gone, the `identityModal` computed at `src/web/App.vue:156-158` has no consumer; delete it and both branches that read it, and delete `isCompact` if that leaves it unused. Keep every `controlSafeDisplay` call: repository-controlled labels still render here.

    Strip dialog framing from `KeyboardHelp.vue` the same way — it becomes a titled content section inside Details, keeping its heading id and `tabindex="-1"` so focus can be moved to it. Its `open` prop and `close` event become the dialog's concern; remove them and migrate `src/web/App.vue:1240`'s bindings in Task 3.

    In the stylesheet, drop the now-superseded `.identity-panel` width override in favour of the shared modal panel rule consuming `--dialog-width` and `--dialog-max-height`. Keep `.identity-panel` and `.keyboard-help` in the overlay-shadow group only if they still carry an overlay shadow of their own; if the shared panel rule now provides it, remove both the declarations and their `overlayAllowlist` entries in the same change so the gate's allowlist keeps matching the stylesheet exactly.

    Do not build a generic dialog framework or a state-card abstraction. `DetailsDialog.vue` is a thin composition over the primitive plan 01 already extracted.
  </action>
  <acceptance_criteria>
    - `src/web/components/DetailsDialog.vue` exists and `grep -c 'ModalDialog' src/web/components/DetailsDialog.vue` is at least 1.
    - `grep -c 'Close details' src/web/components/DetailsDialog.vue` is at least 1.
    - `grep -c 'containFocus' src/web/components/IdentityPanel.vue` is `0`.
    - `grep -c 'aria-modal' src/web/components/IdentityPanel.vue` is `0` and `grep -c "role=\"dialog\"" src/web/components/IdentityPanel.vue` is `0`.
    - `grep -c 'identity-panel--modal' src/web/components/IdentityPanel.vue src/web/styles.css` totals `0`.
    - `grep -c 'identityModal' src/web/App.vue` is `0`.
    - `grep -c 'controlSafeDisplay' src/web/components/IdentityPanel.vue` is at least 2 — unchanged from today.
    - `grep -c 'Comparison identities' src/web` is `0`.
    - Every selector that appears in `overlayAllowlist` at `scripts/verify-semantic-css.mjs:230-233` also appears in `src/web/styles.css`, and vice versa for every rule declaring `box-shadow: var(--shadow-overlay)`. `npm run verify:semantic-css` exits 0.
    - `npm run typecheck:web` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web &amp;&amp; npm run verify:semantic-css</automated>
  </verify>
  <done>Behaviours 1-6 hold once Task 3 mounts the dialog; `IdentityPanel.vue` and `KeyboardHelp.vue` carry no dialog semantics or focus logic; the gate allowlist and the stylesheet still agree.</done>
</task>

<task type="auto">
  <name>Task 3: Mount Details, move keyboard help out of the main region, re-point specs</name>
  <files>src/web/App.vue, tests/e2e/pinned-session.spec.ts, tests/e2e/responsive-session.spec.ts, tests/integration/anchored-workspace.spec.ts</files>
  <read_first>
    - `src/web/App.vue:1146` — the current `IdentityPanel` mount, a sibling of `.review-shell`. `src/web/App.vue:1361-1374` — the canonical overlay sibling band. Every overlay mounts here, never inside `.review-main`.
    - `src/web/App.vue:1240` — `<KeyboardHelp :open="keyboardHelpOpen" @close="keyboardHelpOpen = false" />`, currently **inside** `.review-main` (which opens at `:1181`). It is the lone violator of the sibling rule.
    - `src/web/App.vue:88` — `const keyboardHelpOpen = ref(false)`. `:862-863` — Escape closes it from the central chain. `:875` — the `?` shortcut opens it. `:1236` — `@keyboard-help="keyboardHelpOpen = true"` on `ReviewToolbar`.
    - `src/web/components/ReviewToolbar.vue:92-93` — the `Keyboard help` tooltip and button emitting `keyboardHelp`. The UI-SPEC keeps ReviewToolbar's navigation commands and disabled rules unchanged, so this trigger stays and simply routes to Details.
    - `src/web/App.vue:839-842` (`closeIdentity`) and `src/web/App.vue:827, 834` — the current identity open/close handlers and their `identityModal` reads.
    - `src/web/App.vue:844-856` (`handleKeydown`) — line 856 returns early for input, textarea, and select targets, so the central Escape chain cannot close a dialog whose focus sits in a form control. Details has no form control today, but its close must not depend on that chain; the primitive owns Escape locally.
    - `tests/e2e/pinned-session.spec.ts:442, 902, 905, 982, 987, 1161, 1164, 1202` — eight sites keyed on the old trigger copy and on `getByRole('region', { name: 'Comparison identities' })`.
    - `tests/e2e/responsive-session.spec.ts:1115, 1119` — asserts the `Keyboard actions` heading visible then absent as a standalone surface. `:1579-1600` — the narrow identity-sheet block, including the `aria-modal`, `.review-shell[inert]`, tight-Tab-wrap, Escape, and focus-return assertions at `:1586-1599` that the Details dialog must now satisfy at every width, not only when narrow.
    - `tests/integration/anchored-workspace.spec.ts:545` — `getByRole('button', { name: 'Keyboard help', exact: true })`; this toolbar trigger keeps its name.
    - `tests/integration/anchored-workspace.spec.ts:1203-1208` — the `.review-main` overflow canary. Moving `KeyboardHelp` out of `.review-main` can only help it; adding anything back in can break it.
  </read_first>
  <action>
    Replace the `IdentityPanel` mount and the `KeyboardHelp` mount with one `DetailsDialog` mounted in the overlay sibling band next to `SupportDialog`. Collapse the three separate open/close paths into one: the header's `Details` trigger, the `ReviewToolbar` `Keyboard help` button, and the `?` shortcut all open the same dialog; the toolbar and shortcut entry points additionally move focus to the keyboard-help heading inside it, using the heading's existing `tabindex="-1"`.

    Delete `keyboardHelpOpen` and its three read sites once the dialog owns open state, and delete the identity-specific Escape branch from `handleKeydown` — the primitive closes locally, which is the only form that works once a dialog contains a form control. Focus return to the invoking trigger is the primitive's job; do not re-add a third bespoke focus-return shape in `App.vue`.

    Re-point the spec sites named in read_first: the trigger and region locators move to the new trigger copy and to `getByRole('dialog', { name: 'Details' })`; the standalone keyboard-help assertions become assertions about that heading inside the dialog. The trap block at `responsive-session.spec.ts:1586-1599` keeps all five of its assertions — they are the primitive's contract and must now hold at desktop width too, so widen the block rather than narrowing it. Leave `anchored-workspace.spec.ts:545` unchanged.
  </action>
  <acceptance_criteria>
    - `grep -c 'keyboardHelpOpen' src/web/App.vue` is `0`.
    - `grep -c 'KeyboardHelp' src/web/App.vue` is `0` — it is composed by `DetailsDialog.vue`, not mounted by `App.vue`.
    - `grep -c 'IdentityPanel' src/web/App.vue` is `0`.
    - `grep -c 'DetailsDialog' src/web/App.vue` is at least 2 (import plus mount).
    - Reading `src/web/App.vue` after the edit shows the `DetailsDialog` mount as a direct child of `.session-shell`, in the sibling band with `SupportDialog`, and **not** inside `.review-main` or `.review-shell`.
    - `grep -c "getByRole('button', { name: 'Keyboard help', exact: true })" tests/integration/anchored-workspace.spec.ts` is at least 1 — unchanged.
    - `npm run build:web && npx playwright test tests/e2e/pinned-session.spec.ts tests/e2e/responsive-session.spec.ts tests/integration/anchored-workspace.spec.ts` passes.
    - `npm run build:web && npx playwright test tests/integration/monaco-anchor.spec.ts tests/e2e/file-tree.spec.ts` passes — Phase 09 and Phase 10 contracts do not regress.
    - `npm run verify:semantic-css` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run verify:semantic-css &amp;&amp; npm run build:web &amp;&amp; npx playwright test tests/e2e/pinned-session.spec.ts tests/e2e/responsive-session.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/monaco-anchor.spec.ts tests/e2e/file-tree.spec.ts</automated>
  </verify>
  <done>One Details dialog in the overlay sibling band serves the header trigger, the toolbar trigger, and the `?` shortcut; no overlay remains inside `.review-main`; all five named specs pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repository → Details rows | Details is where full commit IDs, merge bases, pathspecs, blob identities, and old/new paths surface. Every one of these is repository-controlled. |
| server → metadata fetch | `sessionClient.getFileMetadata` gains its first caller; its responses now reach the DOM. |
| dialog → background shell | Details makes the review shell inert; a leaky trap lets a keyboard reviewer operate suppressed controls. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03-01 | Spoofing | `IdentityPanel.vue`, `FileMetadataPane.vue`, `DetailsDialog.vue` rows | mitigate | Control bytes and bidirectional overrides in paths, labels, or modes can reorder or disguise identity rows. `controlSafeDisplay` from `src/domain/path-bytes.ts` stays on every displayed identity in `IdentityPanel.vue` (asserted at two or more call sites) and is applied to the newly surfaced metadata rows. |
| T-11-03-02 | Spoofing | Exact-patch Details content | mitigate | Rendering fabricated commit or merge-base fields for an exact-patch session, or labelling frozen content as current, misrepresents what the reviewer is approving. Task 2 Test 4 asserts the exact-patch branch contains no merge-base field and no Base/Head wording. |
| T-11-03-03 | Elevation of privilege | Details focus containment | mitigate | Details inherits plan 01's widened focusable query and Shift+Tab containment, closing the `IdentityPanel.vue:56` button-only leak. Asserted by the trap block at `responsive-session.spec.ts:1586-1599`, which Task 3 widens to desktop width. |
| T-11-03-04 | Information disclosure | metadata fetch responses | mitigate | Errors surface through the existing `SessionClientError` message mapping with a shared fallback constant, matching `loadFile` at `src/web/App.vue:292-318`. No raw exception, stack, or absolute path is rendered. `tests/e2e/pinned-session.spec.ts:1419` continues to assert the repository root appears nowhere on the page. |
| T-11-03-05 | Tampering | stale metadata response | mitigate | A late response for a previously selected file would attribute one file's status, counts, and modes to another. The monotonic `requestVersion` discard from `loadFile` is copied exactly and asserted by Task 1 Test 3. |
| T-11-03-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install occurs. `DetailsDialog.vue` composes existing in-repo components. Verified by `git diff --stat package.json package-lock.json` being empty. |
</threat_model>

<verification>
- `npm run typecheck:web` and `npm run verify:semantic-css` exit 0.
- `npm run build:web && npx playwright test tests/e2e/pinned-session.spec.ts tests/e2e/responsive-session.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/monaco-anchor.spec.ts tests/e2e/file-tree.spec.ts` passes.
- `git diff --stat package.json package-lock.json` is empty.
- Every `box-shadow: var(--shadow-overlay)` rule in `src/web/styles.css` has a matching `overlayAllowlist` entry and no allowlist entry is orphaned.

**Known-red window:** Task 2 strips dialog semantics from `IdentityPanel.vue` and `KeyboardHelp.vue` before Task 3 remounts them inside `DetailsDialog`. Between those tasks `App.vue` still references the removed props and `typecheck:web` fails. Run Task 2 and Task 3 back to back. Everything is green at the end of Task 3.

**Not in scope here:** the `Review notes` trigger that plan 02 added stays inert until plan 04. Do not wire it to Details to make it look finished — the two dialogs hold different content and the UI-SPEC forbids Details becoming the only owner of summary or export state.
</verification>

<success_criteria>
- `DetailsDialog.vue` exists, composes identity, file metadata, and keyboard help in that order inside `ModalDialog.vue`, and is mounted as a `.session-shell` sibling.
- `IdentityPanel.vue` carries no `role="dialog"`, no `aria-modal`, no `containFocus`, and no modal-mode class or prop; `identityModal` is gone from `App.vue`.
- `FileMetadataPane.vue` has a non-landmark root and `sessionClient.getFileMetadata` has a real caller with loading, error, retry, and stale-response handling.
- No overlay remains mounted inside `.review-main`.
- The header trigger, the `Keyboard help` toolbar button, and the `?` shortcut all open the one Details dialog.
- The five named specs pass and the drift gate exits 0.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Produced by |
|---|---|---|
| `src/web/components/DetailsDialog.vue` | new component | Task 2 |
| Live selected-file metadata data path through `sessionClient.getFileMetadata` | wiring | Task 1 |
| `IdentityPanel.vue` collapsed to content-only, second focus-trap copy deleted | refactor | Task 2 |
| `FileMetadataPane.vue` landmark defect fixed, component un-orphaned | bug fix | Task 1 |
| `KeyboardHelp.vue` relocated out of `.review-main` into Details | refactor | Tasks 2-3 |
| Re-pointed Details/keyboard-help locators in `tests/e2e/pinned-session.spec.ts` and `tests/e2e/responsive-session.spec.ts` | test | Task 3 |

<output>
Create `.planning/phases/11-workspace-shell-review-surfaces/11-03-SUMMARY.md` when done.
</output>
