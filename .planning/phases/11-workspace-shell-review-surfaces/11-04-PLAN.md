---
phase: 11-workspace-shell-review-surfaces
plan: 04
type: execute
wave: 4
depends_on: ["11-01", "11-02", "11-03"]
files_modified:
  - src/web/components/ReviewNotesDialog.vue
  - src/web/components/ReviewPanel.vue
  - src/web/App.vue
  - src/web/styles.css
  - tests/e2e/agent-ready-export-safety.spec.ts
  - tests/e2e/complete-review-draft.spec.ts
  - tests/e2e/review-panel-resolved.spec.ts
  - tests/integration/selector-drift-ui.spec.ts
  - tests/integration/export-receipt-ui.spec.ts
autonomous: true
requirements: [REV-02, REV-04]

must_haves:
  truths:
    - "The comments rail holds open and resolved comment history and nothing else; summary, export, receipts, .gitignore consent, and attached completion are not reachable from it."
    - "Opening Review notes never changes the diff canvas width — the dialog overlays the workspace instead of taking a grid column."
    - "A reviewer can write a summary, read export readiness, run an export, acknowledge drift, read a receipt, and finish an attached session from inside Review notes, with the same buffers, revisions, and confirmations as before."
    - "Closing the rail returns focus to the Review trigger; a closed rail is unrendered or inert, not merely translated off-screen."
    - "The comments rail adopts the mockup's review-surface language: the heading band and its counts stay pinned while the card list scrolls, comment groups stay bordered cards with hairline-separated rows, the selected card keeps its accent edge, and every restyled declaration resolves a canonical token."
  artifacts:
    - path: "src/web/components/ReviewNotesDialog.vue"
      provides: "Summary, export readiness/progress/receipt/drift-acknowledgement, .gitignore consent, and attached completion inside a modal"
      min_lines: 120
      exports: ["default"]
    - path: "src/web/components/ReviewPanel.vue"
      provides: "Comments rail: heading, counts, open group, resolved group"
      contains: "review-heading"
  key_links:
    - from: "src/web/components/IdentityHeader.vue"
      to: "src/web/components/ReviewNotesDialog.vue"
      via: "the Review notes trigger plan 02 left inert now opens this dialog"
      pattern: "aria-haspopup"
    - from: "src/web/components/ReviewNotesDialog.vue"
      to: "src/web/components/ReviewPanel.vue"
      via: "Review stale feedback scrolls and focuses a comment card that lives in the rail, routed through App.vue"
      pattern: "staleAnchors"
    - from: "src/web/components/ReviewNotesDialog.vue"
      to: "src/web/components/ui/ModalDialog.vue"
      via: "Review notes renders inside the shared modal primitive"
      pattern: "ModalDialog"
---

<objective>
REV-04 splits `ReviewPanel.vue` in two: the rail keeps comment history, and a Review-notes dialog takes summary, export, and attached completion off the grid so the diff stays full width. REV-02 then restyles what remains of the rail — the surface a reviewer reads history in — without touching its lifecycle. REV-01's inline surfaces and the two dead components move to plan 07, which runs in wave 5 immediately after this plan; this plan is already the largest in the phase.

The split is not a clean cut. One handler spans both halves, both halves need the comment set, one heading id can only belong to one of them, and two Playwright harnesses mount `ReviewPanel.vue` directly.

Purpose: the diff canvas stops competing with review notes for horizontal space.
Output: one new dialog, a rail reduced to history and restyled, and five specs re-pointed.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md
@.planning/phases/11-workspace-shell-review-surfaces/11-PATTERNS.md
@mockups/02-review-stream.html
@.planning/phases/11-workspace-shell-review-surfaces/11-02-SUMMARY.md
@.planning/phases/11-workspace-shell-review-surfaces/11-03-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Split ReviewPanel into rail and Review-notes dialog</name>
  <files>src/web/components/ReviewNotesDialog.vue, src/web/components/ReviewPanel.vue, src/web/App.vue, src/web/styles.css</files>
  <read_first>
    - `src/web/components/ReviewPanel.vue:342` — the root `<section ref="root" class="review-panel" aria-labelledby="review-heading" @keydown.escape="closeFromEscape">`, closing at `:818`.
    - `src/web/components/ReviewPanel.vue:343-351` — the heading header: `<h2 id="review-heading" ref="heading" tabindex="-1">Review</h2>`, the two `ReviewStateBadge` counts, and the `Close review` button. Both halves show counts; only one may own `id="review-heading"`.
    - Section boundaries, all verified against the file: conflict alert `:354-373` (its `role="alert"` is on `:357`); operation-failure alert `:374-389` (`role="alert"` on `:378`); summary section `:390-403` with `<SummarySection>` at `:391`; open-comments group `:405-550`; resolved-comments group `:551-653`; export section `:654-673` with `<ExportSection>` at `:655`; attached-completion section `:675-817`.
    - `src/web/components/ReviewPanel.vue:150-157` — `focusStaleFeedback`, invoked from the button at `:740` inside the attached-completion block. It reads `props.comments` and moves focus to a comment card. The button moves to the dialog; the card stays in the rail. This handler spans the cut.
    - `src/web/components/ReviewPanel.vue:131-137` — `attached`, `attachedHasUnsavedText`, `attachedBlockedByUnsavedText`. The dialog half derives readiness from `props.comments` and per-comment buffers, so both halves need the comment set and the buffer map. Split the prop surface; do not narrow it.
    - `src/web/components/ReviewPanel.vue:441` and `:587` — the stale/orphan badge labels `Stale anchor` and `Anchor unavailable`; `:497` `Exact path bytes`; `:502` `Inspect recorded file`, conditional on the recorded file still being available; `:503` `Copy anchor details`. This copy already matches the UI-SPEC — preserve it verbatim.
    - `src/web/App.vue:1304-1312` — the rail `<aside v-show="commentsOpen" id="review-panel" ref="commentsDrawer" class="comments-rail" :inert="!commentsOpen" :aria-hidden="…" aria-labelledby="review-heading">`, with the `ReviewPanel` mount at `:1314-1357` passing roughly twenty-five props.
    - `src/web/App.vue:256-260` (`openComments`) — captures the opener, then focuses `#review-heading` inside the drawer. `:262` (`closeComments`).
    - `src/web/App.vue:1128` — the third skip link targets `#review-heading`.
    - `src/web/components/ui/ModalDialog.vue` — the primitive from plan 01. Its Escape is local, which matters here: `src/web/App.vue:856` returns early for input, textarea, and select targets, so the central chain cannot close a dialog whose focus is in the summary textarea.
    - `src/web/styles.css` `.comments-rail` rules, lifted to top level by plan 01 — the rail overlays the right edge at every width with `--shadow-overlay` when open and does not occupy a grid column.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md` — Review notes (REV-04) content order and copy, and Comments rail (REV-02).
  </read_first>
  <behavior>
    Write these first and watch them fail:
    - Test 1: with Review notes open, the diff canvas bounding width is identical to its width with Review notes closed, at desktop and at the compact cutoff.
    - Test 2: the rail exposes the `Review` complementary region, the level-2 `Review` heading, and the open and resolved groups — and exposes no summary editor, no export control, no receipt, no `.gitignore` control, and no attached-completion control.
    - Test 3: Review notes content order is summary, then counts and export readiness, then export action and drift acknowledgement and progress and receipts, then attached completion.
    - Test 4: an unsaved summary buffer typed in Review notes survives closing and reopening the dialog; reopening does not export, save, reload, acknowledge drift, or finish the review.
    - Test 5: `Review stale feedback` inside Review notes scrolls to and focuses the affected comment card in the rail.
    - Test 6: Escape closes Review notes while focus is inside the summary textarea.
    - Test 7: closing the rail returns focus to the `Review` trigger, and the closed rail has zero tabbable descendants.
  </behavior>
  <action>
    Keep `ReviewPanel.vue` as the rail. It retains the root section, the heading header including `id="review-heading"` and the two counts, the open-comments group, and the resolved-comments group. The skip link at `App.vue:1128`, the rail's `aria-labelledby`, and the initial-focus target in `openComments` all point at that id, so the rail is the only half that can own it.

    Create `ReviewNotesDialog.vue` rendering inside `ModalDialog.vue` and taking, in UI-SPEC order: the summary section, the counts and export readiness, the export section with drift acknowledgement, progress, `.gitignore` consent and receipts, and the attached-completion section. It shows the same open and resolved counts as the rail but must not declare a second `id="review-heading"`. Give the two alert sections a home: the revision-conflict and operation-failure alerts belong with the mutations that raise them, so they move to the dialog.

    Promote `focusStaleFeedback` to `App.vue`. The dialog emits the intent; `App.vue` resolves the affected comment id against the comment set and drives the rail's existing focus path. Do not duplicate the comment set into the dialog just to keep the handler local.

    Split the prop surface rather than narrowing it: both halves receive `comments` and `commentBuffers`, because the dialog's readiness computeds at `:131-137` read them. Mount the dialog in the `.session-shell` overlay sibling band next to `SupportDialog` and `DetailsDialog` — not inside `.review-main`, where `tests/e2e/pinned-session.spec.ts:1418` forbids the summary textarea, and not inside `.review-shell`, where it would take a grid column.

    Wire the `Review notes` trigger that plan 02 left inert. Per the UI-SPEC one-shell-overlay rule, opening Review notes may close the rail, but must preserve the rail's selection and focus-return paths so `Review stale feedback` still lands on a card.
  </action>
  <acceptance_criteria>
    - `src/web/components/ReviewNotesDialog.vue` exists and `grep -c 'ModalDialog' src/web/components/ReviewNotesDialog.vue` is at least 1.
    - `grep -c 'id="review-heading"' src/web/components/ReviewPanel.vue` is exactly `1` and `grep -c 'id="review-heading"' src/web/components/ReviewNotesDialog.vue` is `0`.
    - `grep -c 'SummarySection' src/web/components/ReviewPanel.vue` is `0` and `grep -c 'ExportSection' src/web/components/ReviewPanel.vue` is `0` and `grep -c 'attached-completion' src/web/components/ReviewPanel.vue` is `0`.
    - `grep -c 'SummarySection' src/web/components/ReviewNotesDialog.vue` is at least 1, `grep -c 'ExportSection' src/web/components/ReviewNotesDialog.vue` is at least 1, and `grep -c 'attached-completion' src/web/components/ReviewNotesDialog.vue` is at least 1.
    - `grep -c 'focusStaleFeedback' src/web/components/ReviewPanel.vue` is `0` and `grep -c 'staleAnchors' src/web/App.vue` is at least 1.
    - `grep -c 'Stale anchor' src/web/components/ReviewPanel.vue` is at least 2 and `grep -c 'Anchor unavailable' src/web/components/ReviewPanel.vue` is at least 2 and `grep -c 'Copy anchor details' src/web/components/ReviewPanel.vue` is at least 1 and `grep -c 'Inspect recorded file' src/web/components/ReviewPanel.vue` is at least 1 — the rail keeps all unverified-anchor copy verbatim.
    - Reading `src/web/App.vue` after the edit shows the `ReviewNotesDialog` mount as a direct child of `.session-shell`, in the sibling band with `SupportDialog` and `DetailsDialog`.
    - `npm run typecheck:web` exits 0.
    - `npm run verify:semantic-css` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web &amp;&amp; npm run verify:semantic-css</automated>
  </verify>
  <done>Behaviours 1-7 hold once Task 2 re-points the specs; the rail holds only history; the dialog holds summary, export, and completion; one cross-half handler lives in `App.vue`.</done>
</task>

<task type="auto">
  <name>Task 2: Re-point every spec the split moves — including the four the ROADMAP missed</name>
  <files>tests/e2e/agent-ready-export-safety.spec.ts, tests/e2e/complete-review-draft.spec.ts, tests/e2e/review-panel-resolved.spec.ts, tests/integration/selector-drift-ui.spec.ts, tests/integration/export-receipt-ui.spec.ts</files>
  <read_first>
    - `tests/e2e/agent-ready-export-safety.spec.ts:18` — imports `ReviewPanel.vue` into a harness. `:36` mounts it with about twenty literal props. `:151` locates `getByRole('region', { name: 'Finish attached review' })`; `:172, 176, 190, 197, 202, 206` drive and assert the attached-completion flow through that region. After the split `ReviewPanel.vue` no longer renders any of it, so the harness must mount `ReviewNotesDialog.vue`. **This file is not on the ROADMAP's test-impact list.**
    - `tests/e2e/complete-review-draft.spec.ts:141-144` — the `ensureReviewOpen` helper: it clicks the `Review` button and asserts `aria-expanded`. It is the rail toggle. `:325-329` calls it and then immediately asserts the `Summary Saved` region and the `No summary yet` text — surfaces that move to the dialog. **`:328-329` is not on the ROADMAP's list.**
    - `tests/integration/selector-drift-ui.spec.ts:342-360` — opens the rail via the `Review` disclosure, then drives the whole summary Edit / fill / focus-preservation path: `:348-349` the `Summary` disclosure, `:350-351` the `Edit` tab, `:354-355` `getByLabel('Review summary (Markdown)')` and `fill`. This path proves an unsaved buffer survives a drift poll — that is the point of the test and it must keep proving it, now from inside the dialog. **Not on the ROADMAP's list.**
    - `tests/integration/export-receipt-ui.spec.ts:184` — `page.getByRole('button', { name: 'Review' }).click()` before exercising receipt states. Receipts move to the dialog.
    - `tests/e2e/review-panel-resolved.spec.ts:15` — imports `ReviewPanel.vue` into a harness for resolved-comment behaviour. Resolved comments stay in the rail, so this import stays. `:17` imports `SelectorDriftNotice`; leave the drift block at `:326-353` alone — plan 06 owns it.
    - `tests/integration/complete-review-panel.spec.ts` — 33 lines, zero `page.` or `getByRole` occurrences. It is a pure `createReviewDraftState` model test. **The ROADMAP's instruction to "update presentation selectors" in this file is wrong; do not edit it.**
    - Other `ensureReviewOpen` definitions, all rail toggles: `tests/e2e/anchored-review.spec.ts:131`, `tests/integration/anchored-workspace.spec.ts:428`, `tests/e2e/agent-ready-export.spec.ts:245`, `tests/e2e/public-support-states.spec.ts:195`, `tests/e2e/marketplace-review.spec.ts:28`.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-CONTEXT.md` — records that `tests/e2e/marketplace-review.spec.ts` and `tests/e2e/public-support-states.spec.ts` fail today for missing environment markers only and are out of scope.
  </read_first>
  <action>
    Re-point each site to the surface that now owns it. `ensureReviewOpen` stays the rail toggle wherever a spec is asserting comment history; where a spec is asserting summary, export, receipts, drift acknowledgement, or attached completion, it must open `Review notes` instead. Do not widen a locator to match both surfaces — an assertion that passes against either half proves nothing about the split.

    Re-point the `agent-ready-export-safety.spec.ts` harness import and mount from `ReviewPanel.vue` to `ReviewNotesDialog.vue`, carrying across only the props the dialog half actually takes. Keep the `Inline composer lock harness` section and its two `CommentComposer` mounts as they are — the composer mutation-lock contract is unchanged by this plan.

    Leave `tests/integration/complete-review-panel.spec.ts` untouched. Leave the drift-notice assertions in `review-panel-resolved.spec.ts` untouched. Leave the two environment-blocked specs untouched.
  </action>
  <acceptance_criteria>
    - `grep -c 'components/ReviewPanel.vue' tests/e2e/agent-ready-export-safety.spec.ts` is `0` and `grep -c 'components/ReviewNotesDialog.vue' tests/e2e/agent-ready-export-safety.spec.ts` is exactly `1`.
    - `grep -c 'components/ReviewPanel.vue' tests/e2e/review-panel-resolved.spec.ts` is exactly `1` — unchanged.
    - `git diff --name-only` does not list `tests/integration/complete-review-panel.spec.ts` or `tests/integration/anchored-workspace.spec.ts` — the first is a pure model test, and the second reaches no moved surface (`grep -c 'Summary\|Export review\|Finish attached\|Review summary' tests/integration/anchored-workspace.spec.ts` is `0` today), so both are regression runs only.
    - `git diff --name-only` lists `tests/e2e/complete-review-draft.spec.ts`, `tests/integration/selector-drift-ui.spec.ts`, `tests/integration/export-receipt-ui.spec.ts`, and `tests/e2e/agent-ready-export-safety.spec.ts`.
    - `grep -c 'Review notes' tests/integration/selector-drift-ui.spec.ts` is at least 1 and `grep -c "getByLabel('Review summary (Markdown)')" tests/integration/selector-drift-ui.spec.ts` is at least 1 — the unsaved-buffer proof survives the move.
    - `npm run build:web && npx playwright test tests/e2e/agent-ready-export-safety.spec.ts tests/e2e/complete-review-draft.spec.ts tests/e2e/review-panel-resolved.spec.ts tests/integration/selector-drift-ui.spec.ts tests/integration/export-receipt-ui.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/agent-ready-export-states.spec.ts` passes.
    - `npx vitest run tests/integration/complete-review-panel.spec.ts` passes with that file unmodified.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:web &amp;&amp; npx playwright test tests/e2e/agent-ready-export-safety.spec.ts tests/e2e/complete-review-draft.spec.ts tests/e2e/review-panel-resolved.spec.ts tests/integration/selector-drift-ui.spec.ts tests/integration/export-receipt-ui.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/agent-ready-export-states.spec.ts</automated>
  </verify>
  <done>Every moved surface is asserted against its new owner, the two harness imports point at the right halves, the pure model test is untouched, and all seven named specs pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REV-02 — restyle the rail that remains</name>
  <files>src/web/components/ReviewPanel.vue, src/web/styles.css</files>
  <read_first>
    - `src/web/styles.css:1986-1992` — `.review-panel`: a flex column with `overflow-y: auto`. The rail owns its own scroll container, which is what makes a sticky heading band possible without a second scroll owner.
    - `src/web/styles.css:2007-2011` — `.review-panel__heading`: `justify-content: space-between`, `padding: var(--space-4)`, `border-bottom: 1px solid var(--border-default)`. It scrolls away with the card list today. `grep -c 'position: sticky' src/web/styles.css` is `0` — nothing in the shipped stylesheet is sticky yet.
    - `src/web/styles.css:1451-1456` — `.comments-rail`: `background: var(--surface-panel)`, `border-color: var(--border-default)`. A sticky heading must sit on that same surface token or the card list will show through it.
    - `src/web/styles.css:2153-2213` — the card rules that stay: `.review-panel__comments` padding at `:2153`; the bordered rounded group card at `:2157`; the `--surface-panel` group header at `:2164`; the `+ .review-panel__comment` hairline using `--border-gap` at `:2174`; `.review-panel__comment` on `--surface-canvas` at `:2178`; `.review-panel__comment--selected` at `:2199` with `border-inline-start: 3px solid var(--selection-border)`.
    - `mockups/02-review-stream.html:52` — `.file-heading { position:sticky; top:79px; z-index:2; … }`, and `:17` `.site-header { position:sticky; top:0; z-index:4 }`. The normative pattern: a review surface that scrolls keeps its identifying band pinned.
    - `mockups/02-review-stream.html:60-61` — `.hunk` / `.hunk-title`: a bordered rounded card whose header band sits on a quieter surface than its body. `:86-87` — `.notes-list` / `.notes-list li`: hairline-separated rows with no per-row border or radius. `:38` — `.file-index a[aria-current="true"]`: a left accent edge plus a tinted row for the selected entry. The shipped rail rules above already match this shape; the restyle tightens it rather than replacing it.
    - `scripts/verify-semantic-css.mjs` — the gate that rejects any colour literal outside the canonical `:root`. Every declaration this task writes resolves an existing token; the task adds no token.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-SPEC.md` — Comments rail (REV-02): groups stay `Open` then `Resolved`, each card keeps path identity, side and line, body, state badge, verification badge, selected state, and existing actions; `Show comment` behaviour and focus return are unchanged; the rail always overlays the right edge with `--shadow-overlay`.
  </read_first>
  <behavior>
    Re-author these assertions first and watch them fail:
    - Test 1: with enough comments to overflow the rail, scrolling the card list leaves the `Review` heading and both counts visible at the rail's top edge. They scroll out of view today.
    - Test 2: the pinned heading band is opaque — the card scrolling underneath it is not visible through it.
    - Test 3: the heading band does not introduce a second scroll owner. `.review-panel` remains the only scrolling element inside the rail, and the rail itself still does not scroll the document.
    - Test 4: the selected card still carries its accent edge and raised surface, and the group header still reads on a quieter surface than the card body.
    - Test 5 (regression): every rail behaviour named in the UI-SPEC still holds — group order, card content, `Show comment` selection and focus return, close returning focus to the `Review` trigger — and the rail still overlays the right edge without changing the diff bounds.
  </behavior>
  <action>
    Pin the rail's heading band. Give `.review-panel__heading` `position: sticky` at the top of its scroll container with the rail's own `--surface-panel` background and a stacking order above the card list, so the `Review` heading and both counts stay readable while history scrolls. The scroll container is `.review-panel` itself — do not add a second one, and do not move the scroll to `.comments-rail`, which is `overflow: hidden` by design.

    Tighten the card list onto the mockup's shape using existing tokens only: the group stays one bordered, rounded, clipped card; the group header stays on the quieter panel surface; rows stay separated by the `--border-gap` hairline with no per-row border or radius; the selected card keeps its accent edge and raised surface. Adjust spacing, weight, and hierarchy where the shipped rules diverge from the mockup; do not introduce a colour literal, a gradient, a shadow outside the allowlisted overlay group, or a new token.

    Change no markup that carries meaning: no heading level, `id`, `aria-labelledby`, badge label, action name, or group order moves. This is REV-02 presentation on the surface that survived Task 1 — the rail's lifecycle, selection, and focus paths are untouched.
  </action>
  <acceptance_criteria>
    - `grep -c 'position: sticky' src/web/styles.css` is at least `1` — it is `0` today, so this fails until the heading band is pinned. Reading the `.review-panel__heading` rule after the edit shows the sticky declaration, a `--surface-panel` background, and a stacking order.
    - `grep -c 'review-panel__group' src/web/styles.css` is at least `5` — the group card, its header, the header heading, and the row-separator rule all survive the restyle rather than being flattened into an undifferentiated list. It is exactly 5 today.
    - `grep -c 'var(--selection-border)' src/web/styles.css` is at least 1 and reading `.review-panel__comment--selected` after the edit shows both the accent edge and the raised surface.
    - `grep -c 'id="review-heading"' src/web/components/ReviewPanel.vue` is exactly `1` and `grep -Ec '<h2|<h4|<h5' src/web/components/ReviewPanel.vue` is unchanged from its pre-task value — the restyle moves no heading level.
    - `npm run verify:semantic-css` exits 0 — no colour literal, no unallowlisted overlay shadow, no orphan token.
    - `npm run build:web && npx playwright test tests/e2e/review-panel-resolved.spec.ts tests/e2e/responsive-session.spec.ts` passes, including the rail geometry and resolved-history assertions.
    - `npm run typecheck:web` exits 0.
  </acceptance_criteria>
  <verify>
    <automated>npm run typecheck:web &amp;&amp; npm run verify:semantic-css &amp;&amp; npm run build:web &amp;&amp; npx playwright test tests/e2e/review-panel-resolved.spec.ts tests/e2e/responsive-session.spec.ts</automated>
  </verify>
  <done>Behaviours 1-5 hold; the rail's heading band is pinned on its own surface, the card list matches the mockup's group-and-hairline shape using canonical tokens only, and no rail semantics or copy changed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer intent → export publication | Review notes is where a reviewer triggers an irreversible-looking publication and acknowledges drift. Misreported state here has real consequences. |
| repository → comment cards | Comment bodies, paths, and recorded anchor bytes are repository- and draft-derived and render in both halves. |
| draft file → `.gitignore` consent | The `.gitignore` append is the one filesystem mutation reachable from this surface. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-04-01 | Repudiation | `ReviewNotesDialog.vue` export progress and receipt | mitigate | A failed or pending export must never render a current success receipt, and a previous receipt must be labelled as previous. The export, progress, receipt, and failure sections move verbatim with their existing state machine; `tests/integration/export-receipt-ui.spec.ts` and `tests/integration/agent-ready-export-states.spec.ts` are run in Task 2 to prove no state collapsed during the move. |
| T-11-04-02 | Tampering | `.gitignore` consent flow | mitigate | The consent controls, confirmation heading, and append-only semantics move unchanged; no automatic append is introduced. Covered by the export specs run in Task 2. |
| T-11-04-03 | Spoofing | unverified-anchor cards | mitigate | Stale and orphaned records must stay rail-only, read-only, and never be recoloured as verified or reattached. The `Stale anchor` / `Anchor unavailable` / `Exact path bytes` / `Copy anchor details` / `Inspect recorded file` copy is asserted present and unchanged in `ReviewPanel.vue`, and `Inspect recorded file` keeps its availability condition. |
| T-11-04-04 | Elevation of privilege | Review notes focus containment and Escape | mitigate | Review notes contains a textarea, so `src/web/App.vue:856`'s early return means the central Escape chain cannot close it. The dialog inherits plan 01's local Escape and widened focusable query, asserted by Task 1 Test 6. |
| T-11-04-05 | Information disclosure | mount position | mitigate | Mounting the summary textarea inside `.review-main` would break `tests/e2e/pinned-session.spec.ts:1418` and put an editable control inside the diff region. The dialog mounts in the `.session-shell` overlay sibling band, asserted by reading `App.vue` after the edit. |
| T-11-04-06 | Tampering | rail card identity under restyle | mitigate | A restyle that drops a badge, flattens the selected-card accent, or hides the pinned counts lets a reviewer misread which comment applies to which line and how much history is outstanding. Task 3 changes declarations only — no heading level, `id`, badge label, action name, or group order — and is asserted against the UI-SPEC rail contract plus `tests/e2e/review-panel-resolved.spec.ts`. Inline card containment moves to plan 07 with the inline surfaces. |
| T-11-04-SC | Tampering | npm/pip/cargo installs | mitigate | No package-manager install occurs; one component is composed from existing markup and one stylesheet is edited. Verified by `git diff --stat package.json package-lock.json` being empty. |
</threat_model>

<verification>
- `npm run typecheck:web` and `npm run verify:semantic-css` exit 0.
- `npm run build:web && npx playwright test tests/e2e/agent-ready-export-safety.spec.ts tests/e2e/complete-review-draft.spec.ts tests/e2e/review-panel-resolved.spec.ts tests/e2e/responsive-session.spec.ts tests/integration/selector-drift-ui.spec.ts tests/integration/export-receipt-ui.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/agent-ready-export-states.spec.ts` passes.
- `npx vitest run tests/integration/complete-review-panel.spec.ts` passes with the file unmodified.
- `git diff --stat package.json package-lock.json` is empty.

**Known-red window:** Task 1 removes the summary, export, and attached-completion sections from `ReviewPanel.vue` before Task 2 re-points the specs that reach them through the rail. Between those tasks, `agent-ready-export-safety.spec.ts`, `complete-review-draft.spec.ts:328-329`, `selector-drift-ui.spec.ts:342-360`, and `export-receipt-ui.spec.ts:184` are red. Run Task 1 and Task 2 back to back.

**Correcting the ROADMAP:** the ROADMAP's test-impact list omits `tests/e2e/agent-ready-export-safety.spec.ts`, `tests/e2e/complete-review-draft.spec.ts`, and `tests/integration/selector-drift-ui.spec.ts:342-360`, and wrongly lists `tests/integration/complete-review-panel.spec.ts`, which is a 33-line model test with zero DOM selectors. Record this in the plan summary.

**Deliberately deferred to plan 06:** the drift-notice live-owner assertions in `review-panel-resolved.spec.ts:326-353` and the notice locator in `selector-drift-ui.spec.ts:336`. Do not touch either here.
</verification>

<success_criteria>
- `ReviewNotesDialog.vue` exists, renders inside `ModalDialog.vue`, is mounted in the `.session-shell` overlay sibling band, and holds summary, export, receipts, drift acknowledgement, `.gitignore` consent, and attached completion.
- `ReviewPanel.vue` holds only the heading, counts, open group, and resolved group, and is the sole owner of `id="review-heading"`.
- Opening Review notes leaves the diff canvas width unchanged.
- `focusStaleFeedback` lives in `App.vue` and still lands on a rail comment card.
- The comments rail's heading band is pinned on the rail surface, the card list matches the mockup's group-and-hairline shape, and `npm run verify:semantic-css` exits 0 with no new token and no colour literal.
- Inline comment surfaces, their copy, and the two dead components are untouched here — plan 07 owns them.
- All specs named in the verification block pass, and the pure model test is unmodified.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Produced by |
|---|---|---|
| `src/web/components/ReviewNotesDialog.vue` | new component | Task 1 |
| `ReviewPanel.vue` reduced to the comments rail | refactor | Task 1 |
| `focusStaleFeedback` promoted to `App.vue` as a cross-surface handler | refactor | Task 1 |
| Pinned rail heading band and restyled comment-group cards | component | Task 3 |
| Re-pointed harness mounts and surface locators across five specs | test | Task 2 |

<output>
Create `.planning/phases/11-workspace-shell-review-surfaces/11-04-SUMMARY.md` when done.
</output>
