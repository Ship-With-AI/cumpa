---
phase: 04-agent-ready-export
plan: 06
type: execute
wave: 5
depends_on: [04-04, 04-05]
files_modified:
  - src/web/api/client.ts
  - src/web/model/review-draft-state.ts
  - src/web/components/ExportSection.vue
  - src/web/components/GitignoreStatus.vue
  - src/web/components/ExportReceipt.vue
  - src/web/components/ReceiptFileRow.vue
  - src/web/styles.css
  - tests/integration/agent-ready-export-receipt.spec.ts
autonomous: true
requirements: [EXP-01, EXP-05, EXP-06, EXP-08, SAFE-04]
must_haves:
  truths:
    - "A receipt appears only after server-confirmed complete-pair publication and exposes accepted revision, one timestamp, drift state, exactly two relative paths, full exact-byte SHA-256 hashes, byte counts, and accessible copy/reveal actions (D-15, D-16)."
    - "Reveal calls the Plan 04-03 fixed export-directory capability without browser path/body/query authority; failure preserves the confirmed receipt and selectable relative values (D-16)."
    - "Ignore status remains independent of export; two-step explicit consent is required before the Plan 04-04 fixed append, while decline/failure keeps a persistent warning and never blocks export (D-13, D-14)."
    - "Receipt, ignore, copy, and reveal states meet UI-SPEC keyboard/focus/live-region/responsive/zoom/reduced-motion contracts and offer no source-control action (D-17)."
  prohibitions:
    - "No client-computed path/hash/timestamp/drift, generic reveal path, file picker, absolute path, temporary name, token, raw OS error, per-file success, third artifact, success green/confetti, or apply/stage/commit/push action."
    - "No second API client/state owner/Review panel/modal/toast/design system; this plan serially extends Plan 04-05 owners."
  artifacts:
    - path: "src/web/components/ExportReceipt.vue"
      provides: "Server-confirmed exact two-file receipt and fixed reveal action"
    - path: "src/web/components/GitignoreStatus.vue"
      provides: "Independent effective status and two-step fixed append consent"
    - path: "tests/integration/agent-ready-export-receipt.spec.ts"
      provides: "Focused receipt/copy/reveal/ignore/accessibility evidence"
  key_links:
    - from: "src/web/api/client.ts"
      to: "Plan 04-03 secured fixed export-directory reveal route"
      via: "exact no-body/no-query method; no path parameter"
      pattern: "revealExportDirectory"
    - from: "src/web/components/ExportReceipt.vue"
      to: "Plan 04-03 ExportReceipt result"
      via: "display/copy server values without recomputation"
      pattern: "sha256"
---

<objective>
Complete the separate receipt, copy, fixed reveal, and explicit ignore-consent UI slice on top of the core export state.

Purpose: Make D-13/D-14/D-16 reachable and accessible without overloading the readiness/drift/progress plan.
Output: Serial client/state/section extensions, focused receipt/ignore components/styles, and real-browser tests.
</objective>

## Phase Goal

**As a** developer with a completed pinned review, **I want to** explicitly export one accepted revision as a stable agent-ready pair, **so that** an applying agent can act on precise feedback without Diff Review modifying source control.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-agent-ready-export/04-CONTEXT.md
@.planning/phases/04-agent-ready-export/04-UI-SPEC.md
@.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json
@.planning/phases/04-agent-ready-export/04-03-SUMMARY.md
@.planning/phases/04-agent-ready-export/04-04-SUMMARY.md
@.planning/phases/04-agent-ready-export/04-05-SUMMARY.md

<interfaces>
Validate the ledger. Consume the exact Plan 04-03 export receipt and fixed reveal result plus Plan 04-04 ignore status/append algebra. Extend the Plan 04-05 API client, canonical state, Export section, and styles serially; never create a second owner.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement explicit ignore status and two-step append consent</name>
  <files>src/web/api/client.ts, src/web/model/review-draft-state.ts, src/web/components/ExportSection.vue, src/web/components/GitignoreStatus.vue, src/web/styles.css, tests/integration/agent-ready-export-receipt.spec.ts</files>
  <behavior>
    - Ignored state states Git already ignores fixed directory and offers no append; unignored state says export can continue and internal comparison exclusion remains unconditional.
    - First Add to .gitignore performs no request and opens inline confirmation focused on safe action; only second Append ignore rule invokes no-field capability; Escape chooses Keep unchanged.
    - Decline sends no request, never blocks/cancels export, and leaves persistent unignored warning.
    - Append success announces exact rule, rechecks effective status, and claims no stage/commit; failure keeps warning/export/receipt and bounded retry/decline actions.
  </behavior>
  <action>Add failing browser cases first. Extend the existing client with the exact no-field Plan 04-04 status/append methods and canonical state slots. Compose `GitignoreStatus` into existing Export section. Implement exact UI-SPEC two-step safe DOM order, Escape/focus behavior, copy, status independence, success recheck, and nonblocking failure. Do not fold append into Export or persist consent into canonical JSON/draft. Reuse inherited primitives and exact copy.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-06-receipt-ignore-ui'];if(!c)throw Error('missing 04-06-receipt-ignore-ui');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>Ignore status and exact append consent are accessible, separate, explicit, and nonblocking.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render exact receipt and connect the secured fixed reveal capability</name>
  <files>src/web/api/client.ts, src/web/model/review-draft-state.ts, src/web/components/ExportSection.vue, src/web/components/ExportReceipt.vue, src/web/components/ReceiptFileRow.vue, src/web/styles.css, tests/integration/agent-ready-export-receipt.spec.ts</files>
  <behavior>
    - Receipt exists only for server-confirmed final pair and shows accepted revision, shared UTC timestamp, none-observed/acknowledged drift, full pinned identities disclosure, review.json then review.md.
    - Each row shows full repository-relative POSIX path, complete lowercase `sha256:` 64-hex, byte count, and Copy path; exactly two rows and no receipt file/per-file completion.
    - Copy all details contains revision/timestamp/drift and both complete relative path/hash/byte records only. Clipboard failure leaves full selectable values and adjacent recovery.
    - Reveal request sends exact method with no body/query/path; server derives target. Reveal failure retains successful receipt and instructs copy relative path/open from repository root without leaking absolute identity.
    - Previous confirmed receipt remains distinctly labeled when a later re-export fails or is unsupported; it is never relabeled as current attempt success.
  </behavior>
  <action>Add failing confirmed/failed/previous/copy/reveal/security-projection/browser cases first. Extend the existing client with the strict fixed reveal call and no path argument. Render only validated Plan 04-03 receipt DTO; never calculate values in browser. Use exactly two fixed rows and full selectable safe values. Connect `Reveal export directory` to the server capability and handle bounded failure without invalidating receipt. Apply UI-SPEC neutral completion surface, focus/live-region/keyboard/32px target/semantic order/narrow drawer/200% zoom/screen-reader/reduced-motion rules. No success green, confetti, truncation of copied data, temporary names, partial status, or source-control controls.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-06-receipt-ignore-ui'];if(!c)throw Error('missing 04-06-receipt-ignore-ui');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
    <human-check>At phase-end verification, inspect keyboard-only ignore consent and receipt at wide/narrow/200% zoom with reduced motion; verify full selectable paths/hashes, screen-reader labels, copy/reveal recovery, and no receipt on failed/unsupported attempts.</human-check>
  </verify>
  <done>The UI displays and copies the exact server-confirmed pair and reveals only its fixed server-derived directory.</done>
</task>

</tasks>

<acceptance_criteria>
- Plan modifies eight files and is sequential after both shared UI and ignore server owners.
- Receipt is independently verifiable, accessible, relative-only, and shown only after complete publication.
- Fixed reveal has a concrete client→secured route→platform adapter key link and no browser path authority.
- Ignore decline/failure remains nonblocking and append requires two explicit activations.
</acceptance_criteria>

## Artifacts this phase produces

- Serial extensions to existing API client/canonical state/Export section.
- Focused ignore-status and receipt/file-row responsibilities.
- Fixed no-path reveal client call tied to Plan 04-03 route.
- Focused real-browser ignore/receipt/copy/reveal/accessibility test owner.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Server receipt → browser | UI must not invent or truncate verification facts. |
| Browser → ignore/reveal actions | Events could seek arbitrary file authority. |
| Receipt → clipboard/DOM | Unsafe/absolute values could leak or spoof success. |

## STRIDE Threat Register — ASVS L1, high threats block

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-27 | Spoofing/Repudiation | receipt | High | mitigate/block | Strict server DTO after final pair only; exact two rows and previous-receipt separation. |
| T-04-28 | Elevation | reveal | High | mitigate/block | No-argument client to secured fixed server-derived capability. |
| T-04-29 | Elevation/Tampering | ignore consent | High | mitigate/block | No-field action after two explicit activations; decline is no request. |
| T-04-30 | Information Disclosure | UI/clipboard | High | mitigate/block | Relative values only; no absolute/temp/token/error internals. |
| T-04-31 | Denial of Service | copy/reveal failure | Medium | mitigate | Preserve receipt and selectable values with bounded recovery. |
| T-04-SC | Tampering | UI supply chain | High | mitigate/block | Existing primitives only; no install. |
</threat_model>

<verification>Run only `04-06-receipt-ignore-ui` after ledger validation plus phase-end human inspection. No installs, formatters, linters, broad suites/builds, or Git commands.</verification>

<success_criteria>
- EXP-01/05/06/08, SAFE-04, and D-13–D-17 are fully reachable in the existing Review panel.
- Receipt/reveal/ignore behavior preserves fixed authority, accessibility, and complete-pair truth.
</success_criteria>

<output>Create `.planning/phases/04-agent-ready-export/04-06-SUMMARY.md` with predecessor interfaces, RED/GREEN command, two-step ignore matrix, receipt values, client-to-route reveal proof, copy/reveal failures, and accessibility/responsive observations.</output>
