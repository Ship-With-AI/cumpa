---
phase: 04-agent-ready-export
plan: 05
type: execute
wave: 4
depends_on: [04-03]
files_modified:
  - src/web/api/client.ts
  - src/web/model/review-draft-state.ts
  - src/web/components/ReviewPanel.vue
  - src/web/components/ExportSection.vue
  - src/web/components/ExportReadinessSummary.vue
  - src/web/components/DriftExportAcknowledgement.vue
  - src/web/components/ExportProgress.vue
  - src/web/styles.css
  - tests/integration/agent-ready-export-states.spec.ts
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EXP-04, EXP-06, EXP-07, EXP-08]
must_haves:
  truths:
    - "The existing Review panel, API client, and canonical browser state owner gain one Export section for explicit accepted-revision export; no second browser authority exists (D-02, EXP-01)."
    - "Readiness shows accepted revision, actionable/attention/resolved counts, summary presence, effective ignore status, and unsaved-text exclusion; zero actionable and empty summary remain exportable (D-06, D-09, D-11, D-12)."
    - "Drift consent displays full pinned/current identities, submits only the latest opaque token, resets when observation changes, and never refreshes the pinned comparison (D-10)."
    - "Progress and failure are pair-level only; revision/read-only/recovery state priorities preserve canonical draft and every unsaved buffer and never auto-retry (D-02, D-15)."
    - "Keyboard, focus, live-region, responsive drawer, 200% zoom, contrast, target size, and reduced-motion contracts are observable for readiness/drift/progress/failure."
  prohibitions:
    - "No autosave/on-close/background export, automatic retry, force/selective export, stale token reuse, local-buffer inclusion, browser serialization/hash/path authority, or per-file success."
    - "No new page/modal/toast/store/design system/icon/package/file picker/source-control action; stale/orphaned/resolved records never become actionable."
  artifacts:
    - path: "src/web/components/ExportSection.vue"
      provides: "Readiness, drift, progress, conflict/read-only, and pair failure composition"
    - path: "src/web/model/review-draft-state.ts"
      provides: "Export operation slots in the one canonical browser state owner"
    - path: "tests/integration/agent-ready-export-states.spec.ts"
      provides: "Focused real-browser core export-state and accessibility evidence"
  key_links:
    - from: "src/web/model/review-draft-state.ts"
      to: "src/web/api/client.ts"
      via: "expected revision plus optional opaque drift token only"
      pattern: "expectedRevision"
    - from: "src/web/components/ExportSection.vue"
      to: "src/web/model/review-draft-state.ts"
      via: "single canonical state machine"
      pattern: "driftAcknowledgementToken"
---

<objective>
Implement the core visible export flow—readiness, explicit initiation, drift acknowledgement, revision/read-only states, pair progress, and failure—inside the existing Review panel.

Purpose: Make Plan 04-03 export reachable without combining receipt/ignore/reveal work into the same execution surface.
Output: Extended client/state/panel owners, focused core export components/styles, and real-browser state/accessibility tests.
</objective>

## Phase Goal

**As a** developer with a completed pinned review, **I want to** explicitly export one accepted revision as a stable agent-ready pair, **so that** an applying agent can act on precise feedback without Diff Review modifying source control.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/04-agent-ready-export/04-CONTEXT.md
@.planning/phases/04-agent-ready-export/04-UI-SPEC.md
@.planning/phases/04-agent-ready-export/04-PATTERNS.md
@.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json
@.planning/phases/04-agent-ready-export/04-03-SUMMARY.md

<interfaces>
Validate the ledger. Resolve exactly one API client, canonical browser state owner, Review panel, component/style primitives, and browser runner. This plan owns the first Phase 4 writes to those UI files. Plan 04-06 depends on it and extends the same owners serially for ignore/receipt/reveal.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire accepted-revision readiness and explicit export into canonical state</name>
  <files>src/web/api/client.ts, src/web/model/review-draft-state.ts, src/web/components/ReviewPanel.vue, src/web/components/ExportSection.vue, src/web/components/ExportReadinessSummary.vue, tests/integration/agent-ready-export-states.spec.ts</files>
  <behavior>
    - Export is enabled only for readable current-schema accepted revision with no draft mutation, conflict, or export pending; corrupt/schema-invalid/newer state uses inherited read-only recovery and no export escape hatch.
    - Readiness shows exact accepted revision, open verified actionable count, open stale/orphaned attention count, resolved count, summary included/No summary provided, and informational ignore status; counts cannot select a subset.
    - Unsaved summary/comment/add buffers remain exact and excluded; CTA stays enabled with exact warning and Review unsaved text action.
    - Zero actionable, empty summary, and fully empty accepted revision remain valid with exact UI-SPEC copy and explicit Export review.
    - Activation sends only displayed expectedRevision; success/failure never changes canonical draft, comment state, anchors, comparison, or local buffers.
  </behavior>
  <action>Add failing real-browser/state tests first. Extend the reconciled client with the strict Plan 04-03 export call. Extend the one canonical browser owner with export pending/result/previous-confirmed-receipt placeholder/current observation/ignore-status slots without a second draft or classification cache. Add one Export section after Resolved. Derive readiness from canonical accepted state and existing actionability projection; preserve all attempted buffers. Implement exact CTA/supporting/unsaved/zero/empty/read-only copy and eligibility. Reuse inherited primitives/tokens; no page/modal/toast/store. Receipt rendering, ignore consent, copy, and reveal are explicitly deferred to dependent Plan 04-06, not stubbed here.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-05-export-ui-states'];if(!c)throw Error('missing 04-05-export-ui-states');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>The existing Review panel exposes accurate accepted-revision readiness and explicit export without duplicating or mutating canonical/local state.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement drift, conflict, pair progress, and failure transitions accessibly</name>
  <files>src/web/model/review-draft-state.ts, src/web/components/ExportSection.vue, src/web/components/DriftExportAcknowledgement.vue, src/web/components/ExportProgress.vue, src/web/styles.css, tests/integration/agent-ready-export-states.spec.ts</files>
  <behavior>
    - Moved/unavailable drift shows full untruncated base/head pinned/current or unavailable identity and unchecked consent; confirm sends latest opaque token only.
    - Changed observation before commit publishes nothing, resets checkbox, updates identities, focuses/announces stale state, and never refreshes pinned comparison.
    - Revision conflict shows expected/latest revision and explicit Reload latest preserving every buffer; reload never exports automatically.
    - Progress phrases are exactly Preparing accepted revision, Validating export pair, Publishing export pair in one polite region; no file independently succeeds.
    - Publication/recovery failure is one alert, says no new pair published and previous complete pair unchanged; retry is a fresh explicit preflight with no stale consent.
    - State priority, focus, keyboard, 32px targets, responsive drawer, 200% zoom, reduced motion, contrast, and one-time announcements match UI-SPEC.
  </behavior>
  <action>Add failing transition/accessibility/browser cases first. Implement UI-SPEC priority: session authority; corrupt/newer; revision conflict; drift required/stale; publication/recovery failure; pending; confirmed result placeholder; ready. Keep navigation/reading/local editing available during pending and disable only duplicate/conflicting initiation. Render full drift identities and local checkbox bound to observation ID; submit only current opaque token; clear on observation change. Use inherited Reload latest behavior preserving buffers. Keep previous confirmed receipt data distinct after later failure for Plan 04-06 to render. Apply only inherited spacing/type/palette/focus/live-region/responsive/reduced-motion tokens and exact copy. Never show a path/hash/receipt or per-file success before final server result.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-05-export-ui-states'];if(!c)throw Error('missing 04-05-export-ui-states');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
    <human-check>At phase-end verification, inspect core export states at wide/drawer/narrow/200% zoom with keyboard and reduced motion; confirm full identities, focus order, live announcements, no buffer loss, and no partial/per-file language.</human-check>
  </verify>
  <done>Drift, conflict, pair progress, and failure transitions are accessible, explicit, pinned, and non-mutating.</done>
</task>

</tasks>

<acceptance_criteria>
- Core UI plan modifies nine files and is structurally separate from ignore/receipt/reveal Plan 04-06.
- Existing client/state/panel owners are singular; downstream shared UI writes depend on this plan.
- Readiness, actionability, empty states, drift no-refresh, conflict, progress, failure, accessibility, and buffer preservation pass focused browser tests.
</acceptance_criteria>

## Artifacts this phase produces

- Existing API client export call and canonical browser export-operation slots.
- Existing Review panel with one core Export section.
- Focused readiness, drift acknowledgement, and progress responsibilities, folded into fewer files if reconciliation convention prefers.
- Focused real-browser state and accessibility tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Canonical server state → browser | UI must not invent revision, identity, or actionability. |
| Browser events → export route | Events could replay stale drift or broaden authority. |
| Result → visible status | Partial or failed publication could be misrepresented. |

## STRIDE Threat Register — ASVS L1, high threats block

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-22 | Spoofing | readiness/actionability | High | mitigate/block | Canonical accepted state/shared projection only; no cache or client serialization. |
| T-04-23 | Tampering | revision/drift request | High | mitigate/block | Expected revision and latest opaque token only; reset on observation change. |
| T-04-24 | Tampering | unsaved buffers | High | mitigate/block | Single canonical/local state owner; buffers preserved and excluded. |
| T-04-25 | Spoofing/Repudiation | progress/failure | High | mitigate/block | Pair-level server result only; no per-file or premature receipt state. |
| T-04-26 | Denial of Service | duplicate actions | Medium | mitigate | Disable duplicate initiator; bounded transition announcements. |
| T-04-SC | Tampering | UI supply chain | High | mitigate/block | Reuse existing primitives; no package/install. |
</threat_model>

<verification>Run only `04-05-export-ui-states` after ledger validation plus phase-end human checks. Do not install packages or run formatters, linters, broad browser matrices, project-wide suites, builds, or Git commands.</verification>

<success_criteria>
- EXP-01/02/03/04/06/07/08 and D-02/D-05–D-12/D-15 are visible without duplicated browser authority.
- Core UI is ready for the dependent exact receipt/ignore/reveal slice.
</success_criteria>

<output>Create `.planning/phases/04-agent-ready-export/04-05-SUMMARY.md` with resolved owners, RED/GREEN command, state transition/priority matrix, buffer-preservation evidence, and accessibility/responsive observations.</output>
