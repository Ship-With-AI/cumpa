---
phase: 04-agent-ready-export
plan: 07
type: tdd
wave: 5
depends_on: [04-03, 04-04]
files_modified:
  - tests/helpers/git-fixture.ts
  - tests/helpers/source-control-snapshot.ts
  - tests/helpers/export-fault-runner.ts
  - tests/e2e/agent-ready-export-safety.spec.ts
  - tests/package/agent-ready-export-safety.test.ts
autonomous: true
requirements: [EXP-05, EXP-06, EXP-08, SAFE-04]
must_haves:
  truths:
    - "Dirty real-Git fixtures and independent before/after snapshots detect HEAD, ref, remote, index, tracked source/mode, staged/unstaged, untracked, and command-audit mutation, excluding only `.diff-review/**` and an explicitly approved exact gitignore suffix (D-17, D-18)."
    - "Supported-platform child-process interruption proves stable continuously resolves to the old or new complete pair across native exchange; no test accepts recoverable stable-path absence (D-03, D-15, D-18)."
    - "Unsupported/probe-failed re-export proves typed refusal before any stable metadata/content change; first export remains allowed only from validated candidate when stable absent (D-03)."
    - "Final files independently reparse/reproject/hash to the exact relative-path receipt; API/security/source-control audits exercise generated artifacts, not mocks or source-text assertions (EXP-05, EXP-06, EXP-08)."
  prohibitions:
    - "No clean-only fixture, broad exception, mocked production boundary, dev-server-only evidence, one-format proof, stable-to-backup recovery assumption, or portable fallback re-export."
    - "No product Git mutation/network command, repository executable/hook/package script/shell string, source write, or unapproved `.gitignore` mutation."
  artifacts:
    - path: "tests/helpers/source-control-snapshot.ts"
      provides: "Independent binary-safe source-control before/after evidence"
      exports: [captureSourceControlSnapshot, compareSourceControlSnapshots]
    - path: "tests/helpers/export-fault-runner.ts"
      provides: "Native exchange/refusal child-process interruption harness"
      exports: [runExportInterruptionMatrix]
    - path: "tests/package/agent-ready-export-safety.test.ts"
      provides: "Generated-package native/fault/non-mutation boundary proof"
  key_links:
    - from: "tests/helpers/export-fault-runner.ts"
      to: "Plan 04-03 native exchange publication"
      via: "real child-process observer and restart states"
      pattern: "reExportUnsupported|exchange"
---

<objective>
Build focused packaged safety harnesses for real-Git non-mutation and the literal native atomic-exchange/refusal guarantee.

Purpose: Give final journey tests reusable independent evidence without combining all acceptance behavior into one plan.
Output: Dirty fixtures, source-control snapshots, interruption/fault runner, and focused generated-package safety tests.
</objective>

## Phase Goal

**As a** developer with a completed pinned review, **I want to** explicitly export one accepted revision as a stable agent-ready pair, **so that** an applying agent can act on precise feedback without Diff Review modifying source control.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/04-agent-ready-export/04-CONTEXT.md
@.planning/phases/04-agent-ready-export/04-RESEARCH.md
@.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json
@.planning/phases/04-agent-ready-export/04-03-SUMMARY.md
@.planning/phases/04-agent-ready-export/04-04-SUMMARY.md

<interfaces>
Validate the ledger and extend the actual Phase 1–3 fixture/package owners. Use generated package artifacts, production route/store/Git/native adapter, and existing fault seams; do not create alternate fixture vocabulary or product command runner.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend dirty real-Git fixtures and independent non-mutation snapshots</name>
  <files>tests/helpers/git-fixture.ts, tests/helpers/source-control-snapshot.ts, tests/e2e/agent-ready-export-safety.spec.ts, tests/package/agent-ready-export-safety.test.ts</files>
  <behavior>
    - Four ordered branch→branch, branch→worktree, worktree→branch, worktree→worktree fixtures include unusual safe paths, unsupported content, internal-path crossings, moved/unavailable selectors, and distinct dirty staged/unstaged/untracked/mode states.
    - Snapshot records full HEAD/ref/remotes, raw worktree-specific index bytes/hash, tracked identities/content/modes, binary-safe staged/unstaged deltas with external diff/textconv disabled, untracked identities/bytes, and product command audit.
    - Comparison permits only `.diff-review/**`; only after explicit append, root `.gitignore` may equal original bytes plus exact allowed suffix. No arbitrary untracked/source/index/ref/remote/audit exclusion exists.
    - Deliberate mutation controls prove each forbidden class is detected before relying on snapshots around export.
  </behavior>
  <action>Add failing helper/package controls first. Extend the actual disposable fixture and seed distinguishable data for every mutation class. Implement an independent trusted test-harness snapshot rather than using product reporting. Record the complete facts above and exact narrow exceptions. Audit forbidden product command families: add/apply/am/commit/merge/rebase/reset/restore/checkout/switch/update-index/update-ref/push; network; hooks; editor/pager; shell; repository binary; package script. Any occurrence fails. Do not use a clean fixture or ignore all untracked files.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-07-source-snapshot'];if(!c)throw Error('missing 04-07-source-snapshot');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>Dirty fixtures and binary-safe snapshots independently detect every prohibited repository/source-control mutation with only exact permitted outputs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Exercise native exchange, refusal fallback, faults, and interruptions in generated processes</name>
  <files>tests/helpers/export-fault-runner.ts, tests/e2e/agent-ready-export-safety.spec.ts, tests/package/agent-ready-export-safety.test.ts</files>
  <behavior>
    - Every candidate generation boundary fails once: creation; both file open/write/sync/close; reread; parse/schema/projection equality; hashes; directory sync; revision/drift rechecks; exchange; parent sync; final reread; displaced-old cleanup.
    - With old stable pair, every failure before native exchange leaves identical old stable bytes/hashes and continuous path visibility; failure after exchange leaves complete new stable pair or typed unconfirmed state without removing stable.
    - Child-process readers sample stable through interruption immediately before, during, and after one-call exchange and around parent sync/final verification/old-remnant cleanup; every observation is exactly old complete pair or new complete pair, never missing/mixed/one-file.
    - Unsupported adapter, failed runtime probe, cross-device, and undeclared platform return reExportUnsupported before any stable inode/metadata/content change. First-export fault leaves stable absent; successful first export appears as one complete directory rename.
    - Restart validates stable first; ambiguous non-stable remnants are preserved and cannot trigger a stable-removal restoration sequence.
  </behavior>
  <action>Write the exact approved guarantee cases before harness implementation. Launch the real generated server and production recovery in child processes using the reconciled fault hooks. Observe stable from a separate process at high-frequency synchronization barriers controlled by test hooks, not probabilistic sleeps. Cover all awaited boundaries and supported declared OS primitives. Assert inode/content/hash/name facts and source-control snapshots. For re-export, the invariant is strict: stable is never renamed away and every observer sees old or new complete pair. For unsupported cases, snapshot stable metadata/content before call and prove byte/inode/name equality after typed refusal. Independently reread final exactly-two files, validate JSON, rerender/compare Markdown, and recompute receipt SHA-256/bytes/relative paths. Never word a recoverable stable-path absence as acceptable.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-07-real-fs-recovery'];if(!c)throw Error('missing 04-07-real-fs-recovery');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>Generated-process tests prove continuous stable-path atomic replacement on supported platforms and refusal-before-touch elsewhere, with exact receipt and non-mutation evidence.</done>
</task>

</tasks>

<acceptance_criteria>
- D-03 interruption tests assert continuous stable visibility as old-or-new complete pair; no temporary absence is an approved state.
- Unsupported native state refuses re-export before any stable touch; first export is the only portable rename path and requires stable absence.
- Dirty source-control snapshots and command audit fail on plausible mutations and permit only exact outputs/approved append.
- Focused generated-package tests use real Git/filesystem/API/native boundaries.
</acceptance_criteria>

## Artifacts this phase produces

- Extended real-Git fixture owner and independent source-control snapshot helper.
- Native exchange/refusal fault and interruption harness.
- Focused generated-package safety E2E/package tests.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated package → real repository | Product has local access but must remain closed/non-mutating. |
| Fixture → non-mutation assertion | Weak fixtures or exceptions could hide destructive behavior. |
| Native exchange → external observer | Interruption could expose missing/mixed stable state. |

## STRIDE Threat Register — ASVS L1, high threats block

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-32 | Tampering | source/index/refs | High | mitigate/block | Dirty binary-safe independent snapshots and exact exceptions. |
| T-04-33 | Elevation | product commands | High | mitigate/block | Fixed command audit forbids mutations/network/repository execution. |
| T-04-34 | Tampering/Repudiation | interrupted exchange | High | mitigate/block | Synchronized external observer proves continuous old-or-new complete stable. |
| T-04-35 | Tampering | unsupported fallback | High | mitigate/block | Stable inode/content snapshot and typed refusal before touch. |
| T-04-36 | Repudiation | receipt vs disk | High | mitigate/block | Independent final reread, schema/reprojection and exact hashes. |
| T-04-SC | Tampering | package graph | High | mitigate/block | Existing generated artifacts; no install. |
</threat_model>

<verification>Run only `04-07-source-snapshot` and `04-07-real-fs-recovery` after ledger validation. No installs, formatters, linters, broad builds/suites, or Git mutations.</verification>

<success_criteria>
- D-03/D-15/D-17/D-18 and EXP-05/06/08/SAFE-04 have real generated-process safety evidence.
- Final acceptance can reuse trustworthy fixtures without reimplementing safety assertions.
</success_criteria>

<output>Create `.planning/phases/04-agent-ready-export/04-07-SUMMARY.md` with fixture identities, deliberate mutation controls, before/after snapshots, exact native availability, continuous-path interruption matrix, refusal-before-touch evidence, final receipt rereads, and command audit.</output>
