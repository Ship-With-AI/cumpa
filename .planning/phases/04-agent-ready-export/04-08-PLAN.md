---
phase: 04-agent-ready-export
plan: 08
type: execute
wave: 6
depends_on: [04-06, 04-07]
files_modified:
  - tests/e2e/agent-ready-export.spec.ts
  - tests/package/agent-ready-export.test.ts
  - src/server/app.ts
  - src/web/App.vue
autonomous: true
requirements: [EXP-01, EXP-02, EXP-03, EXP-04, EXP-05, EXP-06, EXP-07, EXP-08, SAFE-04]
must_haves:
  truths:
    - "Generated packaged CLI/server/browser assets complete real-Git export/re-export for all ordered branch/worktree combinations and write exactly the stable full-launch-OID pair (D-01, D-03, EXP-01)."
    - "On-disk canonical exact bytes, reparsed Markdown, accepted revision, drift acknowledgement, actionability truth table, relative paths, hashes, receipt, and fixed reveal agree end to end (D-02, D-04–D-12, D-16)."
    - "A generated packaged resume journey persists one accepted revision in a disposable real Git repository, closes browser and server, relaunches the packaged CLI for the same ordered pinned pair, recovers the exact revision/summary/comments/anchors, exports that resumed state, and independently validates canonical JSON, reparse-derived Markdown, relative receipt, and hashes; a different ordered pair remains separate (Roadmap Phase 4 success criterion 5, D-02, D-18)."
    - "Supported native re-export keeps stable continuously old-or-new complete; unsupported re-export refuses before touch; failure never weakens D-03 or exposes partial success (D-03, D-15, EXP-06)."
    - "Ignore decline/append, unconditional comparison exclusion, strict API authority, source-control non-mutation, accessible receipt/copy/reveal, and package/lock no-install contracts pass generated-package tests (D-13, D-14, D-17, D-18, EXP-08, SAFE-04)."
  prohibitions:
    - "No dev-server/fake DTO/mock-only/source-text/clean-only/one-format evidence, broad suite substitution, package install, project-wide build, or weakened portable re-export fallback."
    - "Integration corrections may only connect already tested owners; they may not introduce a second store/schema/API/client/state/route/capability/native adapter or new authority."
  artifacts:
    - path: "tests/e2e/agent-ready-export.spec.ts"
      provides: "Complete generated CLI/server/real-Git/real-browser journey including close/relaunch persisted resume and different-pair separation"
    - path: "tests/package/agent-ready-export.test.ts"
      provides: "Release boundary, packaged resume/export, package/lock, native, security, exact-byte, and non-mutation proof"
  key_links:
    - from: "tests/e2e/agent-ready-export.spec.ts"
      to: "generated packaged CLI/server/browser"
      via: "authenticated real browser plus on-disk pair reread"
      pattern: "review.json|review.md"
    - from: "tests/package/agent-ready-export.test.ts"
      to: "generated packaged CLI persisted draft and export boundary"
      via: "browser/server close, same ordered pinned comparison relaunch, exact accepted-state recovery, then independent on-disk export validation"
      pattern: "resume|acceptedRevision|review.json|review.md"
    - from: "tests/package/agent-ready-export.test.ts"
      to: "Plan 04-07 safety helpers"
      via: "independent source-control and native interruption evidence"
      pattern: "captureSourceControlSnapshot|runExportInterruptionMatrix"
---

<objective>
Close Phase 4 with the focused generated-package browser-to-Git journey and complete source/decision/threat audit.

Purpose: Prove the entire locked export outcome end to end after all pure, server, safety, UI, and harness owners exist.
Output: Final E2E/package matrices and only the integration corrections demanded by failing packaged behavior.
</objective>

## Phase Goal

**As a** developer with a completed pinned review, **I want to** explicitly export one accepted revision as a stable agent-ready pair, **so that** an applying agent can act on precise feedback without Diff Review modifying source control.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-agent-ready-export/04-CONTEXT.md
@.planning/phases/04-agent-ready-export/04-RESEARCH.md
@.planning/phases/04-agent-ready-export/04-UI-SPEC.md
@.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json
@.planning/phases/04-agent-ready-export/04-02-SUMMARY.md
@.planning/phases/04-agent-ready-export/04-03-SUMMARY.md
@.planning/phases/04-agent-ready-export/04-04-SUMMARY.md
@.planning/phases/04-agent-ready-export/04-05-SUMMARY.md
@.planning/phases/04-agent-ready-export/04-06-SUMMARY.md
@.planning/phases/04-agent-ready-export/04-07-SUMMARY.md

<interfaces>
Validate the ledger. Extend only the existing generated-package E2E owners and make minimal app/server wiring corrections proven necessary by failing tests. All behavior and authority belongs to predecessor plans; do not redesign it here.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Prove the complete generated browser-to-Git export journey</name>
  <files>tests/e2e/agent-ready-export.spec.ts, tests/package/agent-ready-export.test.ts, src/server/app.ts, src/web/App.vue</files>
  <behavior>
    - Generated CLI/server/browser completes explicit export for branch→branch, branch→worktree, worktree→branch, worktree→worktree; same pair re-export uses native exchange or typed refusal according to declared supported target.
    - The `packaged-resume-after-relaunch` case creates and accepts a persisted revision with a nonempty summary and distinguishable verified, stale/orphaned, and resolved comments/anchors in a disposable real Git repository; it closes the browser, stops the generated server, and relaunches the packaged CLI on the exact same ordered pinned full-OID comparison.
    - After relaunch, the real browser/server must recover the exact accepted revision, summary bytes, comment IDs/bodies/states/timestamps, and complete side/path/line/blob/selected-text/context/hash anchors before export. Launching a deliberately different ordered full-OID pair in the same repository must not recover that state, and returning to the original pair must recover it unchanged.
    - Export from the resumed original pair, then independently read canonical JSON bytes, parse and validate them, derive Markdown only from that reparsed value, compare exact Markdown bytes, recompute both SHA-256 hashes/lengths, and match exactly two relative receipt paths plus accepted revision to the recovered state.
    - Stable path uses full launch base/head OIDs and contains exactly review.json/review.md. Strict JSON, total ordering, deterministic timestamp-only delta, exact accepted revision, and reparsed Markdown compare independently.
    - Open verified is actionable; open stale/orphaned is attention-only; resolved is JSON history/count-only; empty summary and zero actionable remain valid; agent instructions require pinned commit/blob/path/side/text/context hash and forbid guessing.
    - Revision conflict, corrupt/newer draft, drift required/stale acknowledgement, unsupported content, wrong token/Host/Origin/method/body/query/authority, native unsupported, and publication failure publish nothing new and show bounded safe UI.
    - Fixed reveal security tests prove no body/query/path authority and zero adapter invocation on denial; success derives full-OID directory server-side; failure retains receipt.
    - Ignore decline exports with persistent warning; two-step append preserves exact bytes; generated records remain absent from comparison regardless of ignore state.
    - Receipt values and independently reread exact bytes/hashes/lengths agree; copy/reveal, keyboard, focus, live regions, responsive drawer, 200% zoom, contrast, targets, and reduced motion match UI-SPEC.
    - Independent dirty snapshots and command audit remain equal under success, failure, refusal, append, reveal, and recovery; package/lock graph shows no Phase 4 install.
  </behavior>
  <action>Add the full failing generated-package journey matrix first, including one named `packaged-resume-after-relaunch` case, then make only necessary wiring corrections in the existing app/server composition owners. Launch the actual packaged CLI in disposable dirty real repositories, authenticate the real browser, and use production Vue/Monaco, Fastify routes, persisted draft store, Git runner, native adapter, filesystem, and Plan 04-07 safety helpers. For the resume case, create a pinned ordered pair; accept a revision whose summary and verified/stale-orphaned/resolved comment records have distinguishable exact identities; capture the accepted revision and every summary/comment/state/timestamp/anchor field; close the browser completely and terminate the generated server/CLI process; relaunch the packaged CLI against the same repository and exact same ordered full launch-OID pair; and assert the relaunched browser/server exposes byte-for-byte/equality-exact accepted revision, summary, comments, states, timestamps, and full anchors before any export. In the same disposable repository, launch a deliberately different ordered full-OID pair and assert it has a separate draft and cannot expose the original accepted state, then relaunch the original pair and assert its state remains intact. Export only after this recovered-state proof. Independently read the resulting on-disk JSON bytes, strict-parse/validate them, derive Markdown only from the reparsed canonical value and compare exact bytes, recompute exact SHA-256 and byte lengths, require exactly two regular files and repository-relative receipt paths, and require receipt revision/hash/path values to match the recovered revision and files. Exercise every other behavior above. For supported re-export, invoke the synchronized external observer matrix and assert no missing stable path; for unsupported policy assert typed refusal and stable inode/content equality. Test export and reveal token/Host/Origin/method/body/query/unknown authority with zero downstream calls. Inspect generated dependency/lock graph and product command audit. Do not substitute same-session re-export, ledger/report claims, a dev server, fake DTO, mocked storage/Git/native boundary, source-text assertion, broad suite, or prior plan for the close/relaunch resume evidence.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-08-packaged-export'];if(!c)throw Error('missing 04-08-packaged-export');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
    <human-check>Inspect one generated-package run at wide/narrow/200% zoom with keyboard and reduced motion. Verify full identities, no-refresh drift consent, pair progress, exact receipt paths/hashes, two-step ignore, copy/reveal recovery, no source-control controls, and source snapshot report.</human-check>
  </verify>
  <done>The generated package passes the complete locked Phase 4 matrix, including exact persisted accepted-state recovery after browser/server closure and packaged CLI relaunch, different ordered-pair separation, and independent export validation from the resumed original pair.</done>
</task>

<task type="auto">
  <name>Task 2: Record complete source, decision, requirement, and threat disposition evidence</name>
  <files>tests/package/agent-ready-export.test.ts</files>
  <action>Make the focused package test emit a bounded machine-readable coverage result mapping every EXP-01–EXP-08, SAFE-04, D-01–D-18, Roadmap criterion, UI-SPEC state family, and ASVS L1/high threat to at least one concrete executed focused test result from Plans 04-02–04-08. Require exact command/package artifact identities and reject missing, duplicate, or unexecuted references. Map Roadmap Phase 4 success criterion 5 only to the executed `packaged-resume-after-relaunch` result, whose evidence must identify real Git fixture and generated package, original ordered full-OID pair, accepted revision/state fingerprint, browser/server termination, new packaged process identity, exact recovered revision/summary/comments/anchors, different ordered-pair separation assertion, subsequent original-pair export, canonical JSON validation, reparse-derived Markdown equality, relative receipt, exact hashes/lengths, and source-control snapshots. Reject criterion-5 mapping to reconciliation ledger, plan prose, same-session re-export, or any result missing closure/relaunch/recovery/export evidence. Record D-03 precisely from observed Plan 04-03/04-07 target results: target-specific native one-call exchange, continuous old-or-new stable observation, unsupported re-export refusal before stable touch, and first-export single rename only from fully validated candidate when stable absent. Record no required install and package/lock agreement. This is evidence aggregation, not a source-text test or substitute for predecessor behavior.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),x=JSON.parse(readFileSync('.planning/phases/04-agent-ready-export/04-01-RECONCILIATION.json','utf8')),c=x.commands['04-08-packaged-export'];if(!c)throw Error('missing 04-08-packaged-export');const r=spawnSync(c.executable,c.argv,{cwd:c.cwd,stdio:'inherit'});process.exit(r.status??1)"</automated>
  </verify>
  <done>Every requirement, locked decision, UI contract family, and blocking threat has concrete executed evidence; Roadmap Phase 4 criterion 5 is satisfied only by the named packaged close/relaunch/recover/export case with different-pair separation.</done>
</task>

</tasks>

<acceptance_criteria>
- Focused generated-package command proves exact bytes/reparse-derived Markdown, same snapshot/revision, full-OID pair, hashes, actionability, drift no-refresh, append-only ignore, unconditional exclusion, accessible receipt/reveal, source-control non-mutation, and the named browser/server close plus packaged CLI relaunch resume/export journey.
- Native atomic exchange/refusal fallback satisfies literal D-03 under interruption; no test/report accepts stable-path absence during re-export.
- Security tests cover export, ignore, and reveal token/Host/Origin/method/body/query/unknown authority with zero invocation.
- Roadmap Phase 4 success criterion 5 maps only to executed `packaged-resume-after-relaunch` evidence: exact persisted accepted state after process relaunch, different ordered-pair separation, and independently verified export from the resumed original pair.
- All requirements and D-01–D-18 have executed evidence; package/lock/no-install and all ASVS L1/high mitigations pass.
</acceptance_criteria>

## Artifacts this phase produces

- Complete generated-package E2E and package acceptance matrices, including explicit close/relaunch resume/export and different-pair separation.
- Minimal existing app/server composition corrections only when demanded by a failing packaged behavior.
- Machine-readable complete coverage and threat-disposition evidence from real test results.

## Multi-Source Coverage Audit

| Source | ID | Feature / invariant | Plan(s) | Status |
|---|---|---|---|---|
| GOAL | — | Explicit stable agent-ready pair without source-control modification | 03, 05, 06, 07, 08 | COVERED |
| ROADMAP | Phase 4 success criterion 5 | Packaged real-Git browser/server close, packaged CLI relaunch on same ordered pinned pair, exact accepted-state resume, different-pair separation, resumed export, independent bytes/Markdown/receipt/hash validation, and source-control non-mutation | 08 `packaged-resume-after-relaunch` only | COVERED |
| REQ | EXP-01 | Explicit repository-local JSON+Markdown pair | 03, 05, 06, 08 | COVERED |
| REQ | EXP-02 | Strict versioned canonical identities/history/anchors | 02, 05, 08 | COVERED |
| REQ | EXP-03 | Markdown from reparsed canonical JSON/actionability | 02, 05, 08 | COVERED |
| REQ | EXP-04 | Pinned commit/blob/text/context verification/no guessing | 02, 05, 08 | COVERED |
| REQ | EXP-05 | Relative paths and exact-byte hashes | 02, 03, 06, 07, 08 | COVERED |
| REQ | EXP-06 | Atomic pair-only failure semantics | 03, 05, 06, 07, 08 | COVERED |
| REQ | EXP-07 | Deterministic except explicit timestamp | 02, 05, 08 | COVERED |
| REQ | EXP-08 | No apply/stage/commit/push/source execution | 03, 04, 05, 06, 07, 08 | COVERED |
| REQ | SAFE-04 | Unconditional exclusion plus optional exact append | 04, 06, 07, 08 | COVERED |
| CONTEXT | D-01 | Stable full-launch-OID directory, exactly two files | 03, 08 | COVERED |
| CONTEXT | D-02 | One accepted revision/snapshot, revalidated before publish | 02, 03, 05, 08 | COVERED |
| CONTEXT | D-03 | Native atomic exchange; refusal-before-touch fallback | 03, 07, 08 | COVERED |
| CONTEXT | D-04 | Exact total ordering/canonical bytes/LF policy | 02, 08 | COVERED |
| CONTEXT | D-05–D-09 | Markdown structure, actionability/history/full anchors | 02, 05, 08 | COVERED |
| CONTEXT | D-10 | Explicit current drift acknowledgement; no refresh | 03, 05, 08 | COVERED |
| CONTEXT | D-11–D-12 | Optional summary/zero actionable valid | 02, 05, 08 | COVERED |
| CONTEXT | D-13–D-14 | Exact append/decline/unconditional exclusion | 04, 06, 08 | COVERED |
| CONTEXT | D-15 | Validated/flushed candidate then atomic publish | 03, 05, 06, 07, 08 | COVERED |
| CONTEXT | D-16 | Revision/time/relative paths/hashes/copy/fixed reveal | 02, 03, 06, 08 | COVERED |
| CONTEXT | D-17 | Closed writes/no source-control or repo execution | 03, 04, 05, 06, 07, 08 | COVERED |
| CONTEXT | D-18 | Dirty packaged before/after, close/relaunch resume, and fault/interruption proof | 07, 08 | COVERED |
| RESEARCH | — | Strict grounded reconciliation, singular owners/commands | 01 | COVERED |
| RESEARCH | — | Native supported-target build/runtime exchange probe | 03, 07 | COVERED |
| RESEARCH | — | Fixed reveal adapter → route → client/UI | 01, 03, 06, 08 | COVERED |
| UI-SPEC | — | Readiness/drift/progress/conflict/read-only | 05, 08 | COVERED |
| UI-SPEC | — | Ignore/receipt/copy/reveal/accessibility/responsive | 06, 08 | COVERED |

No Phase 4 deferred idea exists. Alternate output paths, subsets, source mutation, forge delivery, fuzzy relocation, refreshed comparisons, extra artifacts, and new design/dependency systems remain prohibited rather than unplanned.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Generated package → dirty repository | Real product access must remain fixed and non-mutating. |
| Persisted accepted draft → relaunched packaged process | Resume must recover the exact same ordered-pair state and isolate every different ordered pair before export. |
| Browser → all secured capabilities | Untrusted events may seek path/content/Git authority. |
| Native publication → stable pair | Atomic guarantee must survive observed interruption. |
| On-disk pair → API/UI receipt | Product claims require independent exact-byte proof. |

## STRIDE Threat Register — ASVS L1, high threats block

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04-37 | Tampering | source/index/refs | High | mitigate/block | Dirty independent snapshots and exact permitted exceptions. |
| T-04-38 | Elevation | routes/capabilities | High | mitigate/block | Real token/Host/Origin/method/body/query/authority zero-invocation matrix. |
| T-04-39 | Tampering/Repudiation | native re-export | High | mitigate/block | Supported one-call exchange and external continuous observer; refusal before touch otherwise. |
| T-04-40 | Repudiation | receipt vs disk | High | mitigate/block | Independent parse/reprojection/SHA-256/byte/path/name comparison. |
| T-04-41 | Information Disclosure | UI/API/package output | High | mitigate/block | No absolute/temp/token/command/stderr/raw draft values. |
| T-04-42 | Elevation | repository execution | High | mitigate/block | Product command audit; no hooks/shell/package scripts/repository binaries/network mutation. |
| T-04-43 | Tampering/Repudiation | packaged resume identity | High | mitigate/block | Terminate browser/server, relaunch a new packaged process on the same full-OID ordered pair, compare exact revision/summary/comments/anchors, assert different-pair separation, then independently validate resumed export bytes/receipt/hashes. |
| T-04-SC | Tampering | package graph | High | mitigate/block | Reconciliation/package test proves manifest-lock agreement and zero install. |
</threat_model>

<verification>Run only `04-08-packaged-export` after ledger validation plus the specified phase-end human inspection. Earlier focused commands remain referenced evidence. No package installs, formatters, linters, broad builds/suites, project-wide commands, or Git mutations.</verification>

<success_criteria>
- EXP-01–EXP-08, SAFE-04, D-01–D-18, Roadmap criteria, UI-SPEC, and ASVS L1/high threats are complete and grounded; criterion 5 points only to executed packaged close/relaunch resume/export evidence.
- The shipped guarantee is exact: native atomic re-export only on target-specific probe-verified packaged targets; explicit no-touch refusal elsewhere; atomic first rename only when stable is absent.
</success_criteria>

<output>Create `.planning/phases/04-agent-ready-export/04-08-SUMMARY.md` with focused command/artifact identities, full journey matrix, named packaged close/relaunch resume evidence and different-pair separation, exact recovered accepted revision/summary/comments/anchors, resumed export canonical bytes/reparse-derived Markdown/relative receipt/hashes, native target support/refusal proof, security/reveal matrix, source snapshots, accessibility observations, package/lock/no-install evidence, and complete source/threat audit.</output>
