---
phase: 04-exact-runtime-tarball
plan: "04"
task: 1
status: blocked
last_updated: 2026-09-08
---

# Phase 04 checkpoint — protected configuration required

## Current state

Plans 04-01, 04-02 and 04-03 are complete, verified and committed on `main`. Resume **04-04 Task 1**, not an earlier implementation plan. No final release candidate or `04-ARTIFACT-EVIDENCE.json` / `04-ARTIFACT-APPROVAL.md` has been created.

The approved canonical support origin was absent from `CUMPA_RELEASE_SUPPORT_SERVICE_URL` and `SUPABASE_PROJECT_REF` in the executor environment. The documented local environment/configuration locations had no available value. This is the plan's human-input gate. Ask for the path to an existing protected local configuration, or resume in an executor environment containing the approved value. Do not ask the user to paste the origin into chat, and do not contact a remote provider or invent a value to bypass the gate.

## Completed work and proof

- Runtime/legal-only package contract, metadata-authoritative `--version`, strict 18-release prerequisite gate and explicit one-build/one-pack producer.
- Supplied-archive verifier and every live caller migrated; no verifier build/repack or runtime upload path.
- Isolated global installation, actual browser/assets/relaunch, V2/V3 Finish, optional-peer validation, source-control/cleanup checks and bounded parent-owned acceptance report.
- Installed testing found the real V3 add/delete canonicalization bug. `cd92a8c` reproduces it; `a111260` conditionally omits absent paths without weakening canonical JSON.
- Full offline fixture pipeline passed the real acceptance command: two Vitest tests including seven Chromium scenarios, 167 resolved dependency relationships and actual Darwin ARM64 re-export.
- Final credential-free build, all 57 ordinary Vitest files / 438 tests, web typecheck and strict harness TypeScript check passed. The two additional ordinary browser suites passed 10 tests. The full local-security gate passed 12 commands including Deno and two database cycles.
- Independent source review is clean, with Main's post-review V3 correction documented. Security finds no implementation gap; nine final lifecycle gates (eight HIGH) remain open.

See `04-01-SUMMARY.md`, `04-02-SUMMARY.md`, `04-03-SUMMARY.md`, `04-REVIEW.md` and `04-SECURITY.md` for details. Final phase-goal verification is deliberately deferred until actual artifact approval.

## Critical Anti-Patterns

| Anti-pattern | severity | How it could recur | Structural prevention |
|---|---|---|---|
| Treating a synthetic-origin fixture as the release artifact | blocking | An offline fixture used candidate-purpose data to test the complete acceptance command; passing that test is not real configuration or human approval. | Disposable fixture custody is removed; no final artifact records exist. Start a new 04-04 cycle with the operator-provided approved origin and require the actual digest/length approval. |
| Reusing Phase 3 authorization for new remote mutations | blocking | Source and review commits made during this phase are newer than Phase 3's explicitly authorized source. | Perform no push, upload, publication, visibility/retention change or provider mutation. Phase 5 requires its separate authorization and evidence. |

## Resume procedure

1. Obtain the already-approved canonical origin from protected local input. Validate it in memory and pass it only via `CUMPA_RELEASE_SUPPORT_SERVICE_URL`; retain only its configured flag/fingerprint.
2. Read 04-04-PLAN.md completely. Reconcile current source/package/lock/LICENSE/notices/native facts and confirm clean committed source/index. Use absolute producer/verifier evidence paths even where illustrative plan commands show a relative path.
3. Produce one new candidate in a unique durable outside-checkout custody leaf. Run the supplied-archive verifier and installed acceptance against exactly those same bytes. No repack, repair or substitution is allowed.
4. Make that archive read-only and independently rehash SHA-256, npm SHA-1, SHA-512 SRI and byte length. Record verified—not accepted-local—evidence and the planned limitations.
5. Present the actual digest/length and require the plan's exact attributable approval statement. Never auto-approve, use placeholders or accept a generic approval. Only then record accepted-local and the Phase 5 handoff.
6. Complete remaining security/goal verification and phase tracking only after the final real artifact and human gate pass.

## Resume safety

This current Phase 04 starts at `17dbecf`; older commits carrying `04-01` through `04-04` labels belong to earlier milestones. Current implementation summaries are present. There are no current 04-04 execution commits to reconcile.

The checkout was rebuilt configured-absent after fixture verification. The pre-existing local Supabase database container was not stopped. Existing `.gsd/` user work and the separately retained legacy artifacts/backups were not removed.
