---
phase: 04-exact-runtime-tarball
plan: "04"
task: 3
status: blocked
last_updated: 2026-09-09
---

# Phase 04 checkpoint — actual artifact approval required

## Current state

Plans 04-01 through 04-03 are complete. Plan 04-04 Tasks 1 and 2 are now complete: the real production-configured archive was built once, packed once, inspected, installed, exercised and made read-only. Resume **04-04 Task 3**. Do not rebuild or repeat production.

`04-ARTIFACT-EVIDENCE.json` is the authoritative bounded record and has status `verified`, not `accepted-local`. `04-ARTIFACT-APPROVAL.md` does not exist. No attributable artifact approval has been received.

## Existing configuration reused

The user confirmed that production configuration already exists in GitHub and rejected unnecessary local duplication. Main read `SUPABASE_PROJECT_REF` from the `Ship-With-AI/cumpa` GitHub `production` environment, validated the canonical form in memory, and confirmed its fingerprint appears in the prior publication review. The derived origin was passed only through `CUMPA_RELEASE_SUPPORT_SERVICE_URL`. No origin/project reference was logged or copied to a local configuration file.

No source push, workflow dispatch, hosted-support mutation, upload, publication, retention change or transport authorization occurred.

## Completed final tasks

| Task | Evidence commit | Result |
|---|---|---|
| 04-04 Task 1: produce and verify | `a616366` | One real configured candidate; all independent archive hashes, package/legal/native identities and 95-asset browser graph passed. |
| 04-04 Task 2: install and secure custody | `57f0578` | Seven installed Chromium scenarios, 167 dependency relationships, V2/V3 Finish, unavailable support/dismissal, real Darwin ARM64 native re-export, cleanup and source-control checks passed. Archive mode 0444 and rehashes agree. |

Before packing, required build-generated helper attributions were added to `THIRD_PARTY_NOTICES.md`. All prior grants and Monaco's upstream notice remain intact; Cumpa's approved MIT LICENSE bytes are unchanged. Final evidence records the old/new notice digests and the additive reconciliation.

## Actual archive identity

- Package: `@shipwithai/cumpa@1.5.0`
- Basename: `shipwithai-cumpa-1.5.0.tgz`
- SHA-256: `e7766d43f7f804b138e694b298480cdec62ebfc16960f86d4c1e3c7959cf1dca`
- Byte length: `3513998`
- Custody label: `accepted-candidate`
- Read-only: verified

The archive is in one unique leaf under the conventional outside-checkout `~/.local/share/cumpa/release-candidates/1.5.0/` directory. Its private absolute path is intentionally not stored in records. Locate by basename and match the recorded hashes—not by assuming the newest file. No backup is required or created.

## Critical Anti-Patterns

| Anti-pattern | severity | How it could recur | Structural prevention |
|---|---|---|---|
| Rebuilding or substituting the ready archive | blocking | A rebuild with the same version could change the approved bytes. | Reuse only the hash-bound read-only archive above; independently rehash before and after actual assent. |
| Treating fixture results or generic approval as artifact approval | blocking | Earlier tests used a synthetic origin; a generic approval does not bind these real bytes. | Fixture custody was removed. Require the exact current SHA-256/length/limitations statement and keep evidence `verified` until it is received. |
| Reusing Phase 3 or artifact approval for remote publication | blocking | Those approvals have narrower scopes than new pushes or registry actions. | Perform no remote mutation. Phase 5 requires its own authorization and independently rehashes these same bytes. |

## Required human response — not yet received

```text
EXACT RUNTIME ARTIFACT APPROVED: @shipwithai/cumpa@1.5.0 sha256 e7766d43f7f804b138e694b298480cdec62ebfc16960f86d4c1e3c7959cf1dca bytes 3513998; limitations acknowledged
```

Limitations: local artifact acceptance only; bounded disclosure scanning; native observation limited to Darwin ARM64 with explicit fallback elsewhere; no Cumpa registry availability/equality, public-source alignment, provenance, upload/publication, source push, transport authorization or hosted-support deployment claim. Phase 5 must locate and rehash this exact archive, never rebuild it.

## After valid assent

1. Reopen the candidate, verify mode/read-only state, and independently compare SHA-256, npm SHA-1, SHA-512 SRI and byte length with final evidence.
2. Atomically set evidence `accepted-local` and record actual approver identity/time, exact approved SHA-256/length and limitations acknowledgement.
3. Create `04-ARTIFACT-APPROVAL.md` with the attributable verbatim response, archive identities, evidence digest and unchanged-byte Phase 5 obligations. Do not store a private path or origin.
4. Close the remaining approval-related security gates, perform final goal verification and create 04-04-SUMMARY.md before marking Phase 4 complete.

Existing `.gsd/` user work, the pre-existing local database container, and the separately retained legacy artifacts/backups were not removed.
