---
phase: 10-diff-reading-surface
plan: 05
type: execute
wave: 4
depends_on: ['10-02', '10-04']
files_modified:
  - src/web/styles.css
  - tests/e2e/responsive-session.spec.ts
autonomous: true
requirements: [DIFF-02, DIFF-03]

must_haves:
  truths:
    - "Both side labels render their full metadata line without clipping at every supported width"
    - "The label row still sits inside the same locally scrollable canvas as the editor, so horizontal movement cannot separate them"
    - "Under forced colors every diff cue a reviewer needs survives: side names, signed text, dashed and solid rails, glyph signs, and group boundaries"
    - "No element outside the diff viewport owns horizontal overflow at any supported width"
    - "The 640px canvas minimum and the paired comment-zone alignment are intact"
  artifacts:
    - path: "src/web/styles.css"
      provides: "Side-label padding at the bounded diff-anatomy exception with the canvas row track resized to match"
      contains: "9px 18px"
    - path: "tests/e2e/responsive-session.spec.ts"
      provides: "Label-row geometry assertion and forced-colors coverage extended to the group boundaries"
      contains: "diff-workspace__side-labels"
    - path: "tests/integration/anchored-workspace.spec.ts"
      provides: "Re-verified positional label selectors and the untouched outer-overflow canary"
      contains: "outerOverflowOwners"
  key_links:
    - from: "src/web/styles.css"
      to: "src/web/components/DiffWorkspace.vue"
      via: "the canvas grid row track sized to the label row's content box, both inside .diff-workspace__viewport"
      pattern: "grid-template-rows"
    - from: "tests/e2e/responsive-session.spec.ts"
      to: "src/web/monaco/diff-semantics.ts"
      via: "forced-colors assertions on the boundary decoration classes"
      pattern: "monaco-diff-hunk"
---

<objective>
Land the side-label row's bounded padding exception together with the canvas row track it requires, then close the phase by proving the four ROADMAP success criteria on a running browser and leaving every gate green.

Purpose: the UI-SPEC grants the side labels a bounded padding exception taken directly from the normative mockup, and that padding does not fit the shipped row track — metadata line height plus the exception's vertical padding exceeds the current track by four pixels, so applying one without the other clips the primary non-colour side cue. The phase also needs one place where the assembled result is demonstrated rather than assumed: the four requirements landed across four plans, and the success criteria are about the whole surface.
Output: the label row carries the exception padding on a track sized to hold it; the forced-colors step gains coverage for the group boundaries added in plan 10-04; the phase-wide gate and suite run is recorded green.

Out of scope: no Monaco option, theme key, decoration, or component change — every one of those landed in plans 10-01 through 10-04 and this plan must not revisit them. No shell-level responsive behaviour and no comment-card restyling.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
@GSD core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/10-diff-reading-surface/10-UI-SPEC.md
@.planning/phases/10-diff-reading-surface/10-RESEARCH.md
@.planning/phases/10-diff-reading-surface/10-PATTERNS.md
@.planning/phases/10-diff-reading-surface/10-02-SUMMARY.md
@.planning/phases/10-diff-reading-surface/10-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Apply the bounded label padding with the canvas track resized in the same edit</name>
  <files>src/web/styles.css, tests/e2e/responsive-session.spec.ts</files>
  <read_first>
    - src/web/styles.css:1682-1715 (`.diff-workspace__viewport` overflow ownership, `.diff-workspace__canvas` with its 640px minimum and row track, `.diff-workspace__side-labels` with its column gap and separator background, and the label span rule carrying the current token padding)
    - src/web/styles.css:96-130 (the typography and spacing tokens, specifically `--font-size-metadata`, `--line-height-metadata`, `--space-2`, and `--space-4`)
    - src/web/components/DiffWorkspace.vue:286-300 (the section, viewport, canvas, and the label row's `div` greater-than `span` greater-than `span` nesting that the integration selectors depend on)
    - tests/integration/anchored-workspace.spec.ts:1195-1235 (the outer-overflow canary expecting an empty list, the 640px canvas minimum, and the two positional label selectors)
    - tests/integration/anchored-workspace.spec.ts:550-560 (the 32 by 32 gutter-affordance assertion on an adjacent but unrelated control)
    - tests/e2e/responsive-session.spec.ts:585-600 (the shipped label-row text assertion)
    - tests/e2e/responsive-session.spec.ts:55-65 (the width matrix these assertions run across)
    - .planning/phases/10-diff-reading-surface/10-UI-SPEC.md:59-77 (the spacing scale and the two named exceptions, including the label padding) and :177-196 (side identity and the never-stacks, never-truncates rule)
    - .planning/phases/10-diff-reading-surface/10-RESEARCH.md:286-292 (Pitfall 6 — the arithmetic that makes the padding and the track a single edit) and :525-533 (Q5 and its rejected alternative)
    - .planning/phases/10-diff-reading-surface/10-PATTERNS.md:282-322 (the geometry block quoted verbatim with the same arithmetic)
    - mockups/01-quiet-workspace.html:9 (the normative source for the padding values)
  </read_first>
  <behavior>
    - Each side-label span computes to the mockup's vertical and horizontal padding.
    - The label row's rendered height equals the metadata line height plus both vertical paddings, with no clipping of either label's text.
    - Both labels remain fully readable at every width in the shipped matrix, including the narrowest.
    - The diff canvas still reports at least 640px of width.
    - No element outside `.diff-workspace__viewport` gains horizontal overflow.
  </behavior>
  <action>
Write the failing assertion first. In `tests/e2e/responsive-session.spec.ts`, add an assertion that reads a side-label span's computed padding and the label row's rendered height, and pin them to the exception values and the resulting sum. Place it beside the shipped label-row text assertion so all label contract checks sit together. Run it and confirm it fails against the current token padding.

Then make both edits in `src/web/styles.css` in one commit.

Replace the label span's padding with the mockup's literal vertical and horizontal values. The UI-SPEC authorizes this as a bounded diff-anatomy exception, explicitly not a reusable spacing token, so do not introduce a token for it and do not round either value to the nearest spacing step. The drift gate audits colour literals, not length literals, so this creates no gate exposure.

In the same edit, raise the canvas grid's first row track from its current value to the label row's content-box height: the metadata line height plus both vertical paddings. Applying the padding without the track is the failure this task exists to prevent — the row would clip, and the labels are the primary non-colour side cue that the requirement's without-colour-alone guarantee rests on. Applying the track without the padding would leave a taller row with the old spacing, which the UI-SPEC's spacing scale does not authorize. Neither half is independently correct.

Change nothing else in the geometry block. The 640px canvas minimum, the viewport's overflow ownership, the label row's two equal columns, its one-pixel column gap, and its separator background are all pinned continuity values or plan 10-03's work; the canvas full-width and full-height sizing must stay so the labels and the editor keep sharing one locally scrollable canvas.

Do not touch `src/web/components/DiffWorkspace.vue`. The integration spec selects both labels through positional child selectors that depend on the exact `div` greater-than `span` greater-than `span` nesting, and that spec is not on the ROADMAP's expected-impact list — it must keep passing without being edited. Since this task changes no markup, those selectors resolve unchanged; the verification below proves it rather than assuming it.
  </action>
  <verify>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run test:browser -- tests/integration/anchored-workspace.spec.ts` exits zero, including the outer-overflow canary that expects an empty owner list, the at-least-640px canvas assertion, both positional label selectors, and the 32 by 32 gutter-affordance assertion.
    - `git diff --stat tests/integration/anchored-workspace.spec.ts` reports no change for this task — the spec proved the geometry without being re-pinned.
    - `git diff --stat src/web/components/DiffWorkspace.vue` reports no change.
    - `npm run test:browser -- tests/e2e/responsive-session.spec.ts` exits zero, including the new padding and row-height assertion and the shipped label-row text assertion.
    - `grep -c "9px 18px" src/web/styles.css` returns 1.
    - `grep -c "min-width: 640px" src/web/styles.css` returns 1 — the canvas minimum survived the geometry edit.
    - `npm run verify:semantic-css` exits zero.
  </acceptance_criteria>
  <done>The label row carries the authorized exception padding on a track sized to hold it, both labels render unclipped at every supported width, and the markup-dependent integration spec passes without being edited.</done>
</task>

<task type="auto">
  <name>Task 2: Extend forced-colors coverage to the group boundaries and record the phase green</name>
  <files>tests/e2e/responsive-session.spec.ts</files>
  <read_first>
    - tests/e2e/responsive-session.spec.ts:1190-1240 (the forced-colors step: the media emulation, the rail style and sign glyph assertions, the side-label text assertion, the non-colour state cue helper, and the restoring `finally` block)
    - src/web/styles.css:2905-2941 (the terminal forced-colors repair including the arms plan 10-04 added for both boundary classes)
    - src/web/monaco/diff-semantics.ts:50-100 (the emission of both boundary classes, so the locators target what is actually rendered)
    - .planning/phases/10-diff-reading-surface/10-UI-SPEC.md:252-267 (the accessibility contract: redundant non-colour channels, the forced-colors requirement, and the prohibition on depending on canonical RGB values in that mode)
    - .planning/ROADMAP.md:84-103 (the four success criteria this plan closes and the expected test impact list)
    - .planning/phases/10-diff-reading-surface/10-04-SUMMARY.md (the boundary class names and the forced-colors arms as landed)
  </read_first>
  <action>
Extend the existing forced-colors step rather than adding a new one. Inside it, beside the rail-style and sign-glyph assertions, locate the first element carrying each boundary class and assert that the relevant border colour resolves to a real colour under forced colors. Use the same shape the neighbouring assertions use — a resolved colour pattern rather than a canonical token value — because canonical RGB values are precisely what forced colors overrides, and pinning one would assert the opposite of the contract.

This closes the last gap in the without-colour-alone guarantee. That step already proves the side names, the signed removed and added text, the dashed and solid rails, and the minus and plus glyphs survive forced colors. Group boundaries were added after it was written, so without this they are the one cue in the requirement's list with no forced-colors evidence.

Keep the step's structure intact: the media emulation, the surrounding focus and link assertions, the non-colour state cue helper call, and the `finally` block that closes the files drawer and restores the media state must all remain exactly as they are. A boundary assertion placed after the `finally` or outside the emulation would silently assert nothing.

Then run the phase's full verification set and record the result. Run the drift gate, the web typecheck, the three unit suites the phase touches plus the line-mapping suite, and the browser suites named by the ROADMAP's expected-impact list together with the two integration specs this phase extended. The browser command rebuilds the web bundle first, so this is a genuine end-to-end confirmation rather than a check against a stale build.

Two known environment failures are out of scope and must not be chased: the marketplace and public-support-state browser specs fail for missing environment markers, as recorded in `10-CONTEXT.md`. Record them as pre-existing and unrelated rather than attempting a fix inside this phase.

Confirm each of the four ROADMAP success criteria against observed evidence and name the evidence in the summary. Monaco remaining the sole diff authority is shown by the absence of any new diff computation, line mapper, or syntax renderer across the phase's changed files, together with the passing anchoring and line-mapping suites. The quiet reading treatment communicating meaning without colour alone is shown by the forced-colors step as extended here. The preserved expandable context, side-by-side geometry, paired comment alignment, and 640px canvas with localized overflow are shown by the integration spec's canary and canvas assertions plus the paired-zone assertion. Readability across desktop, full-desktop, and mobile without document-level horizontal overflow is shown by the width matrix and the responsive density assertions from plan 10-02.
  </action>
  <verify>
    <automated>npm run verify:semantic-css</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:semantic-css` exits zero.
    - `npm run typecheck:web` exits zero.
    - `npx vitest run tests/unit/monaco-theme.test.ts tests/unit/monaco-diff-adapter.test.ts tests/unit/monaco-diff-semantics.test.ts tests/unit/line-mapping.test.ts` exits zero.
    - `npm run test:browser -- tests/e2e/responsive-session.spec.ts tests/e2e/anchored-review.spec.ts tests/e2e/complete-review-draft.spec.ts tests/integration/monaco-anchor.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/selector-drift-ui.spec.ts` exits zero.
    - The forced-colors step asserts a resolved border colour for both boundary classes, and both assertions sit inside the media emulation, before the restoring `finally` block.
    - `git diff --stat` for this task lists exactly one file, `tests/e2e/responsive-session.spec.ts` — the phase's final task changes no source.
    - The summary names, for each of the four ROADMAP success criteria, the specific command and assertion that evidences it.
  </acceptance_criteria>
  <done>Every diff cue including the group boundaries is proven to survive forced colors, the full phase verification set passes, and each ROADMAP success criterion is tied to named evidence.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| canonical `:root` and mockup exception values → rendered label geometry | Length values enter the stylesheet outside the token scale under an explicit contract exception |
| browser forced-colors mode → rendered diff cues | A user-agent mode that overrides every authored colour, leaving only structure and text as carriers of meaning |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-14 | Information disclosure | Side-label row clipping | mitigate | The padding and the row track land in one commit and a browser assertion pins the resulting row height, so the primary non-colour side cue cannot be silently truncated into an unreadable strip. |
| T-10-15 | Spoofing | Diff cues under forced colors | mitigate | The forced-colors step asserts side names, signed text, dashed and solid rails, glyph signs, and now group boundaries, so no cue that distinguishes removed from added or one group from the next depends on colour alone. |
| T-10-16 | Denial of service | Document-level horizontal overflow | mitigate | The integration canary asserts an empty list of overflow owners outside the diff viewport at every matrix width, so a geometry change cannot push the whole document into horizontal scrolling. |
| T-10-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install task exists in this phase; `10-RESEARCH.md` records the Package Legitimacy Audit as not applicable. |

ASVS L1 scope for this plan is V5. No new input path, sink, or network origin is introduced; the changes are a length-value edit and a test extension. No high-severity finding expected.
</threat_model>

<verification>
- `npm run verify:semantic-css` exits zero.
- `npm run typecheck:web` exits zero.
- `npx vitest run tests/unit/monaco-theme.test.ts tests/unit/monaco-diff-adapter.test.ts tests/unit/monaco-diff-semantics.test.ts tests/unit/line-mapping.test.ts` exits zero.
- `npm run test:browser -- tests/e2e/responsive-session.spec.ts tests/e2e/anchored-review.spec.ts tests/e2e/complete-review-draft.spec.ts tests/integration/monaco-anchor.spec.ts tests/integration/anchored-workspace.spec.ts tests/integration/selector-drift-ui.spec.ts` exits zero.
- **Known-red window, intra-task:** task 1's RED step leaves the new padding assertion failing until the stylesheet edit lands. Closed inside task 1. The padding and the row track are a single commit, so no intermediate clipped-label state is committed.
- Two browser specs fail for missing environment markers and are out of scope for this phase, as recorded in `10-CONTEXT.md`: `tests/e2e/marketplace-review.spec.ts` and `tests/e2e/public-support-states.spec.ts`.
- **This is the phase's final plan and it ends with every in-scope gate and suite green.**
</verification>

<success_criteria>
- Both side labels render unclipped at every supported width on a row track sized to the authorized padding exception, with the 640px canvas minimum and the viewport's local overflow ownership intact.
- The markup-dependent integration spec passes without being edited, proving the label nesting was preserved.
- Forced colors preserve side names, signed removed and added text, dashed and solid rails, minus and plus glyphs, and group boundaries.
- The full phase verification set passes, and each of the four ROADMAP success criteria is tied to a named command and assertion.
- DIFF-01, DIFF-02, and DIFF-03 are each closed by evidence recorded across the phase's five summaries.
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

`src/web/styles.css` — geometry changes
- `.diff-workspace__side-labels > span` padding replaced with the mockup's bounded `9px 18px` diff-anatomy exception
- `.diff-workspace__canvas` first grid row track raised from `32px` to the label row's content-box height, in the same commit

`tests/e2e/responsive-session.spec.ts` — new assertions
- Side-label computed padding and label-row rendered height
- Forced-colors border-colour assertions for `monaco-diff-hunk-start` and `monaco-diff-hunk-end`, inside the existing forced-colors step

`tests/integration/anchored-workspace.spec.ts` — re-verified without edit
- The outer-overflow canary, the at-least-640px canvas assertion, and both positional side-label selectors pass unchanged, which is the evidence that the label markup nesting was preserved

Phase-wide verification recorded
- `npm run verify:semantic-css`, `npm run typecheck:web`, the four unit suites, and the six browser specs named above

No new canonical token, no spacing-scale change, no component change, no new file, no dependency change.
</artifacts>

<output>
Create `.planning/phases/10-diff-reading-surface/10-05-SUMMARY.md` when done. Record: the padding values and the computed row-track value with the arithmetic that produced it; the measured label-row height and confirmation that neither label clipped at the narrowest matrix width; confirmation that `tests/integration/anchored-workspace.spec.ts` and `src/web/components/DiffWorkspace.vue` were not modified; the forced-colors evidence for both boundary classes; the full command-by-command result of the phase verification set, with the two out-of-scope environment-marker failures listed separately as pre-existing; and, for each of the four ROADMAP success criteria, the specific command and assertion that evidences it, so the phase verification step can consume this without re-deriving the mapping.
</output>
