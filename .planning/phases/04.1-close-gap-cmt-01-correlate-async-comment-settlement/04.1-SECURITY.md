---
phase: 04.1
slug: close-gap-cmt-01-correlate-async-comment-settlement
status: verified
threats_total: 11
threats_closed: 11
threats_open: 0
unique_threat_ids: 11
register_authored_at_plan_time: true
asvs_level: 1
block_on: open
created: 2026-07-24
last_audited: 2026-07-24
---

# Phase 04.1 — Close CMT-01 Async Comment Settlement Security

This is a mitigation-verification audit, not a new-vulnerability scan. It evaluates the eleven unique threats declared in the two plan-time `<threat_model>` registers: `T-04.1-01` through `T-04.1-10` plus the shared `T-04.1-SC`. The phase declares ASVS L1 and blocks high threats; all high-threat mitigations are source- and focused-test-grounded.

## Audit Result

| Metric | Count |
|---|---:|
| Unique plan-time threats | 11 |
| Closed | 11 |
| Open | 0 |
| Accepted risks | 1 |
| Unregistered threat flags | 0 |
| ASVS level | 1 |

## Verification Executed

| Focused check | Observed result | Threats supported |
|---|---|---|
| `npm run test:unit -- tests/unit/workspace-state.test.ts` | 18 files passed; 108 tests passed | T-04.1-01, T-04.1-02, T-04.1-04 |
| `npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "async comment settlement"` | 4 passed | T-04.1-06, T-04.1-07, T-04.1-10 |
| `npm run test:api -- tests/api/draft.test.ts tests/api/draft-atomicity.test.ts` | 14 files passed; 98 tests passed | T-04.1-03, T-04.1-08 |
| `git show --format= --name-only e4bcc78 da7415c 43ba512 a788727` | Only the phase model/UI/test and verification/audit artifacts were listed; no `package.json`, lockfile, or dependency manifest appeared | T-04.1-SC |

## Threat Register

| Plan(s) | Threat ID | Category | Component | Disposition | Status | Implementation and test evidence |
|---|---|---|---|---|---|---|
| 01 | T-04.1-01 | Spoofing/Tampering | settlement origin | mitigate/block | CLOSED | `src/web/model/workspace-state.ts:288-296` creates a pending composer and emits its opaque `fileId` and controller-generated `requestId`; `:222-243` rejects a missing/non-pending/nonmatching request, file, side, or line before changing state and focuses only an active origin. `tests/unit/workspace-state.test.ts:307-337` proves A settles while B remains byte-for-byte unchanged with no focus command. |
| 01 | T-04.1-02 | Tampering | stale same-anchor result | mitigate/block | CLOSED | `src/web/model/workspace-state.ts:449-460` allocates monotonic controller-local IDs; `:230-235`, `:301-302`, and `:316-317` return the unchanged transition for every nonmatching success, duplicate, or failure. `tests/unit/workspace-state.test.ts:372-424` creates a replacement same-anchor request, asserts distinct IDs, and proves all three stale outcomes are no-ops. |
| 01 | T-04.1-03 | Tampering/Elevation | canonical accepted draft | mitigate/block | CLOSED | The post-review controller identity guard in `src/web/App.vue:507-519` discards a callback from a replaced `WorkspaceController` before draft or reducer mutation. For the retained controller, `:535-560` accepts only an `accepted` server result, constructs the workspace comment from that result, calls `acceptReviewDraft(result.draft, undefined, workspaceComment)`, and only then dispatches correlated success. The focused atomicity API run above passed. |
| 01 | T-04.1-04 | Denial of Service | failure/conflict callback | mitigate | CLOSED | `src/web/App.vue:521-533` propagates the origin `fileId` and `requestId` on revision conflict and now supplies `result.expectedRevision` to `reviewState.conflict`; `:562-573` does the same origin correlation on rejected promise. `src/web/model/workspace-state.ts:313-323` addresses `event.fileId`, requires that exact pending ID, and restores the existing composer fields as retryable with the supplied message. `tests/integration/anchored-workspace.spec.ts:612-653` holds A, advances canonical B, then proves the A conflict reports originating revision 0 and latest revision 1 while retaining A's retry composer. |
| 01 | T-04.1-05 | Information Disclosure | recoverable error | accept | CLOSED (accepted risk) | The residual is explicitly logged as AR-04.1-01 below. The recoverable composer messages at `src/web/App.vue:525,536,540,566` are fixed and bounded; the live announcement adds only the already rendered review file display path, side, and line (`:509,532,561,573`), never raw server, request, or error-object detail. |
| 01, 02 | T-04.1-SC | Tampering | package graph | mitigate/block | CLOSED | The actual phase commit file manifest inspected above contains no dependency manifest or lockfile. The phase's changed runtime/test surfaces reuse existing imports (`src/web/App.vue:1-25`; `tests/integration/anchored-workspace.spec.ts:1-10`) and the focused commands ran against the existing locked graph. |
| 02 | T-04.1-06 | Tampering | browser response gate | mitigate/block | CLOSED | `tests/integration/anchored-workspace.spec.ts:162-185` parses JSON then requires `DraftMutationRequestSchema.safeParse` success and `addComment` before exposing/awaiting the gate. `:187-226` releases only `DraftMutationResultSchema.parse`-validated accepted, persistence-failure, or revision-conflict responses. The four passing browser cases use real UI submission and never dispatch a reducer event. |
| 02 | T-04.1-07 | Spoofing | browser success evidence | mitigate/block | CLOSED | The accepted journey in `tests/integration/anchored-workspace.spec.ts:541-575` submits A through the inline composer, switches and keeps B visible, releases the HTTP response, deterministically waits for the App-produced polite live-region message, then returns to A and asserts the canonical comment with no composer. The failure journey in `:577-610` repeats the B-active wait and proves A's exact body, anchor label, recoverable alert, and enabled retry button; the conflict journey in `:612-653` proves the same ownership when B advances the canonical draft. `src/web/App.vue:80,131-134,906-908` keys each live-region child with a monotonically incremented announcement version; `tests/integration/anchored-workspace.spec.ts:655-684` proves identical consecutive failure text produces distinct DOM updates. All four passed in the focused browser run. |
| 02 | T-04.1-08 | Tampering | atomic acceptance guarantee | mitigate/block | CLOSED | The App source-order control for T-04.1-03 retains server-result ownership; the focused `draft.test.ts` and `draft-atomicity.test.ts` API execution passed 14 files / 98 tests, directly confirming the preserved atomic acceptance boundary required by this plan-time threat. |
| 02 | T-04.1-09 | Repudiation | Phase 2/audit bookkeeping | mitigate/block | CLOSED | The bookkeeping artifact itself is the mitigation: `04.1-VERIFICATION.md:64-72` records exact focused commands and their observed counts; `04.1-02-SUMMARY.md:65-90` records the date, named browser scenarios, and T-04.1-06 through T-04.1-10 dispositions. This audit independently re-ran the three focused commands above rather than relying on those records alone. |
| 02 | T-04.1-10 | Denial of Service | leaked test gate | mitigate | CLOSED | `tests/integration/anchored-workspace.spec.ts:128-139` creates a fresh resolver pair for one gate; `:142-153` clears the gate, outcome, canonical comments, and content-request state. `:532-539` performs that reset before and after each case. The passing four-case browser run confirms no case inherited an unresolved gate. |

## Summary Threat Flags

Neither `04.1-01-SUMMARY.md` nor `04.1-02-SUMMARY.md` contains a literal `## Threat Flags` section. Their explicit threat annotations map directly to declared IDs: `04.1-01-SUMMARY.md:90-96` maps T-04.1-01 through T-04.1-05 and T-04.1-SC; `04.1-02-SUMMARY.md:86-90` maps T-04.1-06 through T-04.1-10 and T-04.1-SC. Therefore no `unregistered_flag` warning exists.

## Accepted Risks Log

| Risk ID | Threat | Decision | Boundary and evidence |
|---|---|---|---|
| AR-04.1-01 | T-04.1-05 | Accepted | Recoverable composer errors disclose a fixed user-facing outcome (`src/web/App.vue:525,536,540,566`). The associated polite announcement may include the already rendered review file display path, side, and line (`:509,532,561,573`), but never server, request, or raw-error data. The plan explicitly accepts this bounded residual. |

## Security Audit Trail

| Audit Date | Threats Total | Closed | Open | Auditor | Scope and result |
|---|---:|---:|---:|---|---|
| 2026-07-24 | 11 | 11 | 0 | SecureClosurePhase | Loaded both plan registers, summaries, context, review, verification, and only the declared model/UI/test seams. Verified source controls plus focused unit, browser, and atomicity test execution; implementation remained read-only. |

### Audit Protocol

- Used only the plan-time register; no unrelated threat surface was added.
- Treated every mitigation as absent until the cited code or test seam established it.
- Verified the post-review `WorkspaceController` identity guard, deterministic callback-produced live-region browser waits, and versioned live-region child replacement for repeated identical failure announcements in actual source and focused test evidence.
- Created only this audit artifact; no implementation, test, dependency, or package file was modified.

## Sign-Off

- [x] Every unique declared threat has a CLOSED or documented accepted-risk disposition.
- [x] Every high threat is CLOSED with implementation/test evidence.
- [x] No unregistered summary threat flag was found.
- [x] `threats_open: 0` is justified for the locked Phase 04.1 register.

**Status:** verified
