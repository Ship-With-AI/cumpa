---
phase: 04.1-close-gap-cmt-01-correlate-async-comment-settlement
plan: 02
type: execute
wave: 2
depends_on: [04.1-01]
files_modified:
  - tests/integration/anchored-workspace.spec.ts
  - .planning/phases/02-anchored-diff-review/02-VERIFICATION.md
  - .planning/v1.0-MILESTONE-AUDIT.md
autonomous: true
gap_closure: true
requirements: [CMT-01]
must_haves:
  truths:
    - "A real browser can submit on A, switch to B before the response settles, and remain on B when accepted canonical success arrives; returning to A shows the accepted comment and no pending composer (D-05, D-07)."
    - "A real browser can submit on A, switch to B before failure settles, and remain on B; returning to A shows the exact original text/anchor as retryable with the existing recoverable error (D-04, D-07)."
    - "Browser evidence crosses the production composer → WorkspaceState command → SessionClient promise → App.vue callback → reducer transition path; the test gate pauses only HTTP completion and never dispatches reducer events directly (D-07)."
    - "Fresh focused Phase 2 evidence reconciles CMT-01, DIFF-07, DRFT-01, CR-001, WR-001, and WR-002 without claiming unrelated Phase 3 ledger or orphan-route cleanup (D-08)."
  artifacts:
    - path: "tests/integration/anchored-workspace.spec.ts"
      provides: "Deterministic production-transition browser regressions for delayed success and failure across file switching"
      contains: "async comment settlement"
    - path: ".planning/phases/02-anchored-diff-review/02-VERIFICATION.md"
      provides: "Fresh Phase 2 requirement and CR/WR disposition grounded in focused commands"
      contains: "WR-002"
    - path: ".planning/v1.0-MILESTONE-AUDIT.md"
      provides: "Reconciled INT-B01/F04/CMT-01/DIFF-07 milestone status while retaining unrelated debt"
      contains: "INT-B01"
  key_links:
    - from: "tests/integration/anchored-workspace.spec.ts"
      to: "src/web/App.vue"
      via: "real browser add button and delayed /api/draft/mutations response"
      pattern: "api/draft/mutations|Add comment"
    - from: "src/web/App.vue"
      to: "src/web/model/workspace-state.ts"
      via: "production dispatchWorkspace settlement event after the gated promise resolves"
      pattern: "dispatchWorkspace"
    - from: ".planning/phases/02-anchored-diff-review/02-VERIFICATION.md"
      to: "focused command results"
      via: "current dated CMT-01/DIFF-07/DRFT-01 and CR-001/WR-001/WR-002 evidence"
      pattern: "CMT-01|DIFF-07|DRFT-01|CR-001|WR-001|WR-002"
---

<objective>
Prove WR-002 closed through the mounted production browser transitions, then rerun and reconcile the affected Phase 2 and milestone evidence.

Purpose: Convert the pure reducer fix into deterministic CMT-01 user-flow proof while ensuring DIFF-07, real Monaco, server-derived anchors, and atomic persistence remain intact.
Output: Hermetic delayed-success/failure browser regressions plus current Phase 2 and milestone audit dispositions for CR-001, WR-001, WR-002, INT-B01, F04, CMT-01, and DIFF-07.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/v1.0-MILESTONE-AUDIT.md
@.planning/phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-CONTEXT.md
@.planning/phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-PATTERNS.md
@.planning/phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-01-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-VERIFICATION.md
@tests/integration/anchored-workspace.spec.ts
@tests/integration/monaco-anchor.spec.ts
@tests/api/draft.test.ts
@tests/api/draft-atomicity.test.ts
@tests/e2e/anchored-review.spec.ts

<interfaces>
The browser harness may add only test-owned response controls around its existing schema-validated `/api/draft/mutations` middleware: one resettable promise gate and one accepted-or-failure outcome selected before submit. The browser must still use the real Vue composer, WorkspaceState, SessionClient, App.vue promise callback, Monaco adapter, and reducer. Production API contracts and server persistence are not replaced or modified. Phase 2 verification is reconciled only from commands actually rerun in this task.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Prove delayed success and failure through the production browser transition path</name>
  <files>tests/integration/anchored-workspace.spec.ts</files>
  <read_first>
    - `.planning/phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-01-SUMMARY.md` — implemented request identity and focused reducer evidence; stop if the dependency summary is absent or reports a gate violation.
    - `tests/integration/anchored-workspace.spec.ts:1-190` — current hermetic session/draft middleware and schema-validated accepted result.
    - `tests/integration/anchored-workspace.spec.ts:274-303` — real Monaco inline submit and resumed accepted-comment assertions.
    - `tests/integration/anchored-workspace.spec.ts:350-465` — existing anchored gap-closure browser style.
    - `src/web/App.vue:492-553` — production callback path the browser test must traverse without direct reducer dispatch.
    - `.planning/phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-PATTERNS.md` — deterministic response-gate and no-bypass constraints.
  </read_first>
  <action>Extend the existing hermetic middleware with a resettable, per-test response gate after `DraftMutationRequestSchema` validation and before response completion, plus a bounded accepted/failure outcome. Preserve `DraftMutationResultSchema.parse` for accepted and conflict payloads and reset all gate/outcome/canonical state between cases so parallel or later tests cannot inherit a blocked response. Add two focused tests whose titles contain `async comment settlement`: in both, open file A in real Monaco, activate a visible line, fill the real inline composer, click Add comment, wait until the mutation is received but withheld, switch through the actual file tree to file B, and only then release the response. For success, return a canonical accepted draft, assert B remains active after settlement and no off-screen focus/navigation occurs, then return to A and assert the accepted comment is rendered and the pending composer is gone. For failure, complete the request with the existing bounded failure path, assert B remains active and untouched, return to A, and assert the same side/line composer retains the exact text with its recoverable message and can be retried. Do not call workspace dispatch/reducer helpers from the test, intercept the App callback, replace Monaco, or assert only request payloads; the evidence must traverse production UI transitions per D-07. Run only the two named cases and keep all existing browser cases unchanged.</action>
  <verify>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "async comment settlement"</automated>
  </verify>
  <acceptance_criteria>
    - Two deterministic browser cases cover delayed accepted success and delayed failure after A→B using real composer/file-tree/Monaco interactions.
    - The response is observably withheld until B is active, then released; neither case relies on timing sleeps.
    - Success leaves B active, accepts only server-returned canonical state, and shows the accepted comment after returning to A without a pending composer.
    - Failure leaves B active and returns A's exact side/line/text to retryable state with the established message.
    - Test-only controls are reset and cannot leak; production source, API schemas, server store, and package graph are not changed by this task.
  </acceptance_criteria>
  <done>The two focused mounted-browser cases pass through the real production transition chain and demonstrate correct off-screen success and failure settlement.</done>
</task>

<task type="auto">
  <name>Task 2: Re-verify and reconcile Phase 2 and milestone closure evidence</name>
  <files>.planning/phases/02-anchored-diff-review/02-VERIFICATION.md, .planning/v1.0-MILESTONE-AUDIT.md</files>
  <read_first>
    - `.planning/phases/02-anchored-diff-review/02-VERIFICATION.md` — stale `gaps_found` report and CR-001/WR-001/WR-002 dispositions to reconcile.
    - `.planning/v1.0-MILESTONE-AUDIT.md:1-64` — current gap scores and Phase 2 stale-artifact diagnosis.
    - `.planning/v1.0-MILESTONE-AUDIT.md:142-235` — INT-B01, F04, route debt, blocking finding, and fresh prior checks.
    - `.planning/REQUIREMENTS.md:39-68` — DIFF-07, CMT-01, and DRFT-01 acceptance language and current checkbox state.
    - `package.json:16-31` — exact focused test scripts; do not substitute broad suites.
    - `.planning/phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-CONTEXT.md` — D-08 scope fence excluding metadata-route and Phase 3 ledger cleanup.
  </read_first>
  <action>Run the focused commands below against the final Plan 01/Task 1 state and record exact command, date, pass counts, and relevant scenario names. Reconcile `02-VERIFICATION.md` in place: mark CMT-01 verified only when the new unit and mounted-browser A→B success/failure cases pass; mark DIFF-07 verified only when the existing real-Monaco 10-case gate passes; retain DRFT-01 as verified only when focused draft and atomicity API tests pass; change CR-001 and WR-001 to closed/superseded using the fresh Monaco result and current source; change WR-002 to closed using request-correlated unit/browser evidence. Recompute the Phase 2 status, score, requirement score, blocker list, goal-backward tables, finding disposition, integration wiring, and exact-gaps section so the artifact has no internal stale contradiction. Then reconcile `.planning/v1.0-MILESTONE-AUDIT.md` from the fresh evidence: CMT-01 becomes satisfied, DIFF-07 satisfied, INT-B01 wired/closed, F04 complete, X12 wired, requirement/integration/flow scores and verdict reflect closure, and prior-finding disposition records CR-001/WR-001/WR-002 closed. Preserve Phase 2 orphan metadata route as deferred debt and preserve all Phase 3 ledger/UI debt without editing REQUIREMENTS.md or Phase 3 artifacts. Do not claim a focused command that was not executed, and do not convert unrelated debt into closure scope.</action>
  <verify>
    <automated>npm run test:unit -- tests/unit/workspace-state.test.ts &amp;&amp; npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "async comment settlement" &amp;&amp; npm run test:browser -- tests/integration/monaco-anchor.spec.ts &amp;&amp; npm run test:api -- tests/api/draft.test.ts tests/api/draft-atomicity.test.ts &amp;&amp; npm run test:package -- tests/e2e/anchored-review.spec.ts --grep "packaged anchored gap closure"</automated>
  </verify>
  <acceptance_criteria>
    - Every listed focused command passes and its exact result is recorded; no formatter, linter, project-wide suite, or unrelated package test is run.
    - Phase 2 verification has one coherent current status with CMT-01, DIFF-07, and DRFT-01 supported by fresh evidence and CR-001/WR-001/WR-002 explicitly reconciled.
    - Milestone audit marks X12/INT-B01/F04 and CMT-01/DIFF-07 consistently with the same evidence and recomputed scores.
    - Orphan metadata-route cleanup remains deferred; Phase 3 requirement-ledger and UI cleanup remain untouched and explicitly retained as debt.
    - No source, tests, REQUIREMENTS.md checkboxes, API contracts, or package files are modified during bookkeeping.
  </acceptance_criteria>
  <done>Fresh focused evidence closes WR-002 without regressing DIFF-07 or DRFT-01, and both Phase 2 verification and milestone audit are internally consistent with the closure.</done>
</task>

</tasks>

<acceptance_criteria>
- Mounted browser evidence deterministically covers A-pending→B-active accepted success and failure through production Vue/Monaco/client/callback/reducer transitions.
- Success settles only A, accepts server-derived canonical state, leaves B active, and never forces off-screen focus/navigation.
- Failure settles only A, preserves its exact body/anchor and recoverable message, and leaves B untouched.
- Fresh unit, browser, Monaco, API atomicity, and packaged anchored-review gates pass without broad-suite substitution.
- Phase 2 and milestone artifacts consistently report CMT-01, DIFF-07, DRFT-01, CR-001, WR-001, WR-002, INT-B01/X12, and F04 from executed evidence.
- Excluded orphan-route and Phase 3 ledger/UI cleanup remain out of scope.
</acceptance_criteria>

## Artifacts this phase produces

- Hermetic mounted-browser regressions for delayed async comment success/failure across A→B navigation.
- Fresh focused Phase 2 evidence spanning reducer, production browser, real Monaco, atomic persistence, and packaged anchored review.
- Reconciled `02-VERIFICATION.md` with current CMT-01/DIFF-07/DRFT-01 and CR-001/WR-001/WR-002 disposition.
- Reconciled milestone audit status for INT-B01/X12, F04, CMT-01, and DIFF-07 while retaining unrelated debt.

## Multi-Source Coverage Audit

| Source | ID | Feature / invariant | Plan(s) | Status |
|---|---|---|---|---|
| GOAL | Phase 04.1 | Exact origin composer settles across file switching and closure is verified | 01, 02 | COVERED |
| REQ | CMT-01 | User adds a precise comment without async cross-file corruption | 01, 02 | COVERED |
| REQUIREMENT GUARD | DIFF-07 | File switching and Monaco layout retain correct comment placement | 01 preserves; 02 re-verifies | COVERED |
| REQUIREMENT GUARD | DRFT-01 | Accepted UI state follows atomic server persistence | 01 preserves; 02 re-verifies | COVERED |
| AUDIT | INT-B01/X12 | Async result returns to originating composer | 01 implements; 02 proves/reconciles | COVERED |
| AUDIT | F04/WR-002 | A submit → B switch → A success/failure return journey | 01, 02 | COVERED |
| AUDIT | CR-001/WR-001 | Existing Monaco fixes receive fresh current disposition | 02 | COVERED |
| CONTEXT | D-01–D-06 | Origin/request state-machine semantics and regressions | 01 | COVERED |
| CONTEXT | D-07 | Hermetic browser evidence; preserve real-Monaco/package gates | 02 | COVERED |
| CONTEXT | D-08 | Re-run/reconcile affected Phase 2 verification and audit | 02 | COVERED |
| PATTERNS | Deterministic browser seam | Pause only schema-validated HTTP completion and traverse production transitions | 02 | COVERED |
| RESEARCH | — | No RESEARCH.md exists; no package or external integration is introduced | — | N/A |

Deferred orphan metadata-route cleanup and unrelated Phase 3 requirement-ledger/UI cleanup are explicit exclusions, not missing items.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser interaction → delayed HTTP result | The active file changes while a response is pending; test must prove the real callback preserves request origin. |
| Hermetic middleware → production client | Fixture responses must remain schema-valid and must not bypass client/App/reducer behavior. |
| Focused command output → verification/audit claims | Bookkeeping must cite only freshly executed results and preserve unrelated debt truthfully. |
| Server mutation → accepted browser state | Atomic/server-derived acceptance remains a separately rerun guard, not inferred from a mocked success. |

## STRIDE Threat Register — ASVS L1, high threats block

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-04.1-06 | Tampering | browser response gate | High | mitigate/block | Pause only after request schema validation; release a schema-validated response through the real SessionClient/App callback. |
| T-04.1-07 | Spoofing | browser success evidence | High | mitigate/block | Assert canonical accepted draft/comment after response, active B retention, and A state after return; no direct reducer dispatch. |
| T-04.1-08 | Tampering | atomic acceptance guarantee | High | mitigate/block | Rerun focused draft plus atomicity API tests and retain `acceptReviewDraft` server-result ownership from Plan 01. |
| T-04.1-09 | Repudiation | Phase 2/audit bookkeeping | High | mitigate/block | Record exact focused commands, dates, counts, and named scenarios; reconcile all related tables/scores together. |
| T-04.1-10 | Denial of Service | leaked test gate | Medium | mitigate | Reset/release gate and fixture outcome per test; no sleeps or shared unresolved promise. |
| T-04.1-SC | Tampering | package graph | High | mitigate/block | No dependency/package mutation; execute existing locked focused scripts only. |
</threat_model>

<verification>Run only the task-scoped Playwright cases first, then the exact five-command focused closure chain in Task 2. Evidence is invalid if a response gate leaks, a new case bypasses production transitions, or any command fails. Skip formatters, linters, broad builds, project-wide suites, unrelated Phase 3 checks, and package installs.</verification>

<success_criteria>
- Browser success/failure race evidence passes deterministically with B active at settlement and exact A state after return.
- Focused reducer, Monaco, atomic persistence, and packaged Phase 2 gates pass.
- CMT-01 is satisfied; DIFF-07 and DRFT-01 remain satisfied; CR-001, WR-001, WR-002, INT-B01/X12, and F04 have consistent current dispositions.
- Phase 2 verification and milestone audit contain no stale contradiction about these findings.
- Deferred orphan-route and Phase 3 cleanup remain explicitly out of scope.
</success_criteria>

<output>Create `.planning/phases/04.1-close-gap-cmt-01-correlate-async-comment-settlement/04.1-02-SUMMARY.md` with browser gate design, named success/failure scenarios, exact focused commands/pass counts, Phase 2 score/status changes, milestone score/status changes, reconciled finding table, retained deferred debt, changed artifacts, and threat dispositions.</output>
