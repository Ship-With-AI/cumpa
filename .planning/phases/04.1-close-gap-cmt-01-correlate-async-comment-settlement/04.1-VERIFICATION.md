---
phase: 04.1-close-gap-cmt-01-correlate-async-comment-settlement
verified: 2026-07-24T11:56:02Z
status: passed
score: 9/9 must-haves verified
behavior_unverified: 0
overrides_applied: 0
---

# Phase 04.1: Close CMT-01 Async Comment Settlement Verification Report

**Phase Goal:** A reviewer can switch files while an add-comment request is pending, and success or failure settles only its originating composer without weakening canonical atomic draft acceptance or anchored-diff stability.

**Verified:** 2026-07-24T11:56:02Z  
**Status:** passed  
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
| --- | --- | --- | --- |
| 1 | A request from file A settles accepted success on only its originating pending composer while file B remains active; B is untouched and off-screen settlement emits no navigation/focus command. | ✓ VERIFIED | `workspace-state.ts:222-244` resolves `files[fileId]`, requires the exact pending `requestId`, and emits focus only when `fileId === activeFileId`. Unit test `settles an accepted add on its origin file without focusing when another file is active` asserts A is cleared, B is byte-for-byte unchanged, and commands are `[]`. The named mounted-browser success test passed with B visible at release and A accepted after return. |
| 2 | A failure or revision conflict from file A makes only A retryable with its original text and anchor while B remains active and untouched. | ✓ VERIFIED | `workspace-state.ts:313-323` addresses `event.fileId`, removes only the matching request ID, and preserves composer fields while adding the supplied error. `App.vue:520-530` sends a correlated `add-failed` for a revision conflict; `App.vue:559-570` does the same for rejected promises. Unit and mounted-browser failure regressions assert A's body, `Head · line 10` anchor header, recoverable message, enabled retry, and B remaining active. |
| 3 | A stale success, duplicate, or failure cannot settle a replacement composer at the same file, side, and line. | ✓ VERIFIED | `workspace-state.ts:230-235`, `301-302`, and `316-317` require a pending composer and exact `requestId` before mutation. Unit test `ignores every stale settlement for a replacement request at the same anchor` creates a new request ID and asserts each stale success/duplicate/failure leaves state and commands unchanged. |
| 4 | The App callback rejects a result from a replaced workspace controller before canonical-draft or reducer mutation. | ✓ VERIFIED | In `App.vue:507-519`, `originWorkspace` is captured when `persist-comment` starts and `if (workspace !== originWorkspace) return` precedes revision-conflict state, `acceptReviewDraft`, and `dispatchWorkspace`. The same guard precedes the catch-path failure dispatch at `App.vue:559-562`. This is a direct source-order check, not a summary assertion. |
| 5 | Accepted UI state remains derived from the atomically persisted server canonical draft; correlation does not create client-side persistence authority. | ✓ VERIFIED | `App.vue:551-557` calls `acceptReviewDraft(result.draft, undefined, workspaceComment)` before correlated `add-succeeded`. `src/server/routes.ts:215-275` derives a durable anchor then delegates to `draftStore.mutate`; `src/server/draft-store.ts:249-278` serializes by draft, rejects revision conflicts, commits before returning `{ kind: 'accepted', draft }`, and returns `persistenceFailure` on commit error. Focused draft/atomicity API suite passed. |
| 6 | A real browser can submit on A, switch to B before a withheld accepted response settles, remain on B, then return to an accepted A with no pending composer. | ✓ VERIFIED | `anchored-workspace.spec.ts:530-564` drives the real inline composer and tree, waits for schema-validated request receipt, asserts the response is unsettled while B is active, releases it, observes the App live message while B remains visible, then verifies canonical A comment/no composer. The named Playwright case passed. |
| 7 | A real browser can submit on A, switch to B before failure settles, remain on B, then return to A's exact retryable composer. | ✓ VERIFIED | `anchored-workspace.spec.ts:566-599` uses the same production interaction path and delayed HTTP seam, releases a 500 only after B is visible, then checks the exact A body, line-10 header, recoverable alert, and enabled add button. The named Playwright case passed. |
| 8 | The browser proof traverses production composer → workspace command → SessionClient promise → App callback → reducer, without reducer-event injection or timing sleeps. | ✓ VERIFIED | The test fixture at `anchored-workspace.spec.ts:162-219` validates `DraftMutationRequestSchema`, pauses only completion, and returns `DraftMutationResultSchema` payloads. The test interacts only through Monaco/tree DOM controls; production `App.vue:492-570` consumes the command and dispatches settlement. No direct reducer dispatch or sleep appears in either race case. |
| 9 | CMT-01 closure preserves anchored-diff stability and has current requirement/finding traceability. | ✓ VERIFIED | Verifier reran real-Monaco `monaco-anchor.spec.ts` successfully (10/10). `02-VERIFICATION.md` and `v1.0-MILESTONE-AUDIT.md` record CMT-01/WR-002/INT-B01/X12/F04 closed and DIFF-07/DRFT-01 verified, while keeping unrelated orphan-route and Phase 3 debt deferred. Both plans declare the only owned requirement, `CMT-01`; `REQUIREMENTS.md` defines it as adding one comment on any visible base/head line, including unchanged context. |

**Score:** 9/9 truths verified (0 present but behavior-unverified)

## Required Artifacts

| Artifact | Expected | Status | Details |
| --- | --- | --- | --- |
| `src/web/model/workspace-state.ts` | Origin/request-correlated commands, settlement events, and exact pending-composer guard | ✓ VERIFIED | Substantive reducer/controller implementation: request IDs are controller-local and monotonic (`:446-475`), outbound `persist-comment` snapshots origin (`:287-295`), and all settlement variants are guarded. Wired from `App.vue`. |
| `src/web/App.vue` | Callback returns command origin/request identity and accepts canonical draft before settlement | ✓ VERIFIED | Imports/creates the workspace controller (`:23-25`); `runCommands` maps the reducer command to `SessionClient.mutate`, guards controller replacement, accepts canonical result, then dispatches a correlated event. |
| `tests/unit/workspace-state.test.ts` | State-machine regression coverage for cross-file and stale settlement | ✓ VERIFIED | Substantive, behavior-oriented assertions cover off-screen success, failure text/anchor retention, stale success/duplicate/failure, and active-origin focus. The focused command passed 18 files / 108 tests. |
| `tests/integration/anchored-workspace.spec.ts` | Deterministic delayed-success/failure browser regressions through production transitions | ✓ VERIFIED | Schema-valid middleware gate is test-only and reset before/after each case; two real-browser cases prove both settlement outcomes without timing sleeps. The focused command passed 2/2. |
| `.planning/phases/02-anchored-diff-review/02-VERIFICATION.md` | Reconciled Phase 2 CMT-01, DIFF-07, DRFT-01, CR-001, WR-001, and WR-002 evidence | ✓ VERIFIED | Current artifact names the exact commands and disposition of all affected Phase 2 findings. It does not claim ownership of unrelated Phase 3 work. |
| `.planning/v1.0-MILESTONE-AUDIT.md` | Reconciled INT-B01/X12 and F04 closure while retaining unrelated debt | ✓ VERIFIED | Current audit marks X12/INT-B01 wired/closed and F04 complete, and identifies only the fresh focused evidence for this closure. |

## Key Link Verification

| From | To | Via | Status | Details |
| --- | --- | --- | --- | --- |
| Workspace reducer | `App.vue` | `persist-comment` command carrying `fileId` and `requestId` | ✓ WIRED | `App.vue:492-570` processes the discriminated command and echoes its identity into every settlement branch. |
| `App.vue` | Workspace reducer | `dispatchWorkspace` after the SessionClient promise settles | ✓ WIRED | `dispatchWorkspace` at `App.vue:588-592` dispatches to the current controller and recursively executes resulting commands. Controller-replacement guards run before this path. |
| Browser race test | Production transition path | real composer/tree DOM plus delayed `/api/draft/mutations` completion | ✓ WIRED | Middleware validates the request and only delays its response; it neither replaces the client nor dispatches reducer events. |
| Server mutation route | Atomic draft store | durable-anchor construction then serialized `mutate` | ✓ WIRED | Route derives server-side anchor and calls `draftStore.mutate`; store returns accepted draft only after `commit(next)` succeeds. |

## Data-Flow Trace (Level 4)

| Artifact | Data / event | Source | Produces real data | Status |
| --- | --- | --- | --- | --- |
| `App.vue` | `WorkspaceCommand` and settlement event | Real workspace controller, then `SessionClient.mutate` response | Yes — command snapshots composer origin; accepted result is server canonical draft | ✓ FLOWING |
| `workspace-state.ts` | Per-file composer/comment state | `state.files[event.fileId]` and exact pending request | Yes — immutable reducer transition updates the actual origin-file record | ✓ FLOWING |
| Browser race fixture | Mutation response | Schema-validated test HTTP middleware | Yes — it uses the real request payload and schema-validated accepted/failure response, not hardcoded reducer state | ✓ FLOWING |

## Behavioral Spot-Checks

| Behavior | Command | Result | Status |
| --- | --- | --- | --- |
| Cross-file origin settlement, off-screen no-focus, failure retention, and stale replacement no-op | `npm run test:unit -- tests/unit/workspace-state.test.ts` | 18 files, 108 tests passed | ✓ PASS |
| A-pending → B-active delayed accepted/failure settlement through mounted production UI | `npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "async comment settlement"` | 2 passed | ✓ PASS |
| Anchored Monaco placement through resize, switching, and recomputation | `npm run test:browser -- tests/integration/monaco-anchor.spec.ts` | 10 passed | ✓ PASS |
| Atomic persisted draft acceptance and failure behavior | `npm run test:api -- tests/api/draft.test.ts tests/api/draft-atomicity.test.ts` | 14 files, 98 tests passed | ✓ PASS |

### Supplementary Orchestrator Evidence

The orchestrator independently observed on 2026-07-24: Git 8 files/35 tests passed, the canonical configured packaged Playwright suite 54/54 passed, and Vite production build succeeded with 1,443 modules. These results support regression confidence but are not needed to inflate the focused verification score. The attempted bare `npm run test:browser` is explicitly excluded: its `--config=tests` invocation collected Vitest files without a Playwright file filter, whereas the filtered focused commands above and canonical `npm run test:package` are valid.

## Requirements Coverage

| Requirement | Source plan | Description | Status | Evidence |
| --- | --- | --- | --- | --- |
| CMT-01 | `04.1-01-PLAN.md`, `04.1-02-PLAN.md` | User can add one comment to any visible base/head line, including unchanged context, without cross-file async settlement corruption. | ✓ SATISFIED | Reducer request correlation and mounted A→B success/failure proof directly close WR-002; Phase 2 verification and milestone audit record CMT-01 closed. |
| DIFF-07 (guard, not Phase 04.1-owned) | Both closure plans | File switching, resize, and context expansion preserve inline placement/side alignment. | ✓ PRESERVED | Focused real-Monaco suite passed 10/10; no Monaco production code changed in this phase. |
| DRFT-01 (guard, not Phase 04.1-owned) | Both closure plans | UI confirms accepted mutations only after atomic persistence. | ✓ PRESERVED | Route/store/App ordering plus focused draft and atomicity suite passed 14 files / 98 tests. |

**Requirement ownership:** Both plans declare `CMT-01`; no additional requirement is mapped to Phase 04.1 in `REQUIREMENTS.md`, so there are no orphaned Phase 04.1 requirement IDs. `CMT-01` remains historically mapped to Phase 2; this inserted phase is its audited closure, not a duplicate requirement ownership claim.

## Anti-Patterns Found

| File | Line | Pattern | Severity | Impact |
| --- | --- | --- | --- | --- |
| — | — | No `TBD`, `FIXME`, `XXX`, `TODO`, `HACK`, placeholder, or equivalent debt marker in the four changed source/test artifacts. | — | No blocker or warning. |

## Residual Gaps and Human Verification

None for the Phase 04.1 goal. The Playwright tests exercise both runtime settlement outcomes while B is active; reducer tests cover the request-ordering invariant; and the controller-replacement guard is directly established by the pre-mutation early returns in the production callback. No deferred `<human-check>` blocks were found in either plan.

The explicitly retained, out-of-scope debt is the orphan metadata route/client surface and Phase 3 requirement-ledger/UI polish; neither affects CMT-01 settlement and neither is represented as closed by this report.

## Deterministic Next Action

Proceed to the milestone's next planned verification or closure workflow. Do not reopen CMT-01 unless a reproduction violates the tested sequence: submit on A, switch to B before callback completion, release success or failure, then return to A.

---

_Verified: 2026-07-24T11:56:02Z_  
_Verifier: the agent (gsd-verifier)_
