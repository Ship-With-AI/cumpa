---
phase: 01-pinned-local-comparison
plan: 04
type: tdd
wave: 4
depends_on: [01-03]
files_modified:
  - src/domain/source.ts
  - src/git/candidates.ts
  - src/cli/picker.ts
  - src/cli/confirm.ts
  - src/cli/run.ts
  - src/contracts/comparison.ts
  - tests/git/candidates.test.ts
  - tests/cli/selection.test.ts
autonomous: true
requirements: [SEL-03, SEL-04, SEL-07]
must_haves:
  truths:
    - "A developer searches one list grouped into Local branches and Worktrees, including detached and unavailable registered worktrees and separate source rows that share a commit."
    - "The same picker runs base first and head second; base is always explicit, while only the current checkout may be marked as the suggested head."
    - "Every worktree row exposes path, detached state, short committed HEAD, and truthful clean/dirty/unavailable state; dirty copy says committed HEAD is reviewed and all staged, unstaged, and untracked bytes are ignored."
    - "Confirmation preserves ordered labels, full identities, selected worktree paths, dirty warnings, and the pinned-does-not-follow-refs statement."
  artifacts:
    - path: src/git/candidates.ts
      provides: "Native-Git branch/worktree/dirty discovery"
      exports: [discoverSourceCandidates]
    - path: src/cli/picker.ts
      provides: "Reusable grouped searchable base/head prompt"
      exports: [pickOrderedSources]
    - path: src/cli/confirm.ts
      provides: "Identity-rich launch confirmation"
      exports: [confirmPinnedComparison]
  key_links:
    - from: src/git/candidates.ts
      to: src/cli/picker.ts
      via: "stable source candidate IDs remain distinct from commit OIDs"
      pattern: "SourceCandidate"
    - from: src/cli/picker.ts
      to: src/cli/run.ts
      via: "ordered base/head result feeds frozen comparison creation"
      pattern: "pickOrderedSources"
---

<objective>
Replace the skeleton selection port with truthful native-Git discovery and the complete two-step searchable picker.

Purpose: Deliver SEL-03, SEL-04, and SEL-07 per D-01–D-07 and D-09 without changing comparison authority or moving selection into the browser.
Output: Source candidate model, branch/worktree discovery, grouped picker, confirmation adapter, and focused real-Git/CLI tests.
</objective>

## Phase Goal

**As a** developer working in a local Git repository, **I want to** choose an ordered base and head and open their pinned PR-style comparison in my browser, **so that** I can verify committed local changes without publishing branches or reading dirty worktree bytes.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-RESEARCH.md
@.planning/phases/01-pinned-local-comparison/01-PATTERNS.md
@.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md
@.planning/phases/01-pinned-local-comparison/01-03-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify truthful searchable source selection</name>
  <read_first>
    <file>.planning/phases/01-pinned-local-comparison/01-CONTEXT.md</file>
    <file>.planning/phases/01-pinned-local-comparison/01-RESEARCH.md</file>
    <file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file>
    <file>tests/helpers/git-fixture.ts</file>
    <file>src/cli/run.ts</file>
  </read_first>
  <files>tests/git/candidates.test.ts, tests/cli/selection.test.ts</files>
  <behavior>
    - "Local branches and every registered linked/detached worktree appear in separate groups and remain separate when commit OIDs match."
    - "Search covers type, label, short ID, path, detached, dirty, clean, and unavailable text; zero matches keeps both separators and focused query with no selectable placeholder."
    - "Base is explicitly selected before head; only head has the current-checkout suggestion; dirty and unavailable copy matches the verified UI contract."
  </behavior>
  <action>Create disposable repositories with branches, linked and detached worktrees, duplicate OIDs, staged/unstaged/untracked dirt, and an unavailable/prunable registration. Write tests before production code. Assert discovery uses worktree list --porcelain -z, for-each-ref explicit fields, and status --porcelain=v1 -z; no human-line parsing or filesystem-byte comparison. Assert exact prompt/recovery copy and keyboard filter behavior from 01-UI-SPEC.md, including Escape clearing only search. Require a named behavioral RED and commit tests first.</action>
  <verify><automated><command expected="nonzero" classification="behavioral-red">npm run test:git -- tests/git/candidates.test.ts</command><command expected="nonzero" classification="behavioral-red">npm run test:unit -- tests/cli/selection.test.ts</command></automated></verify>
  <acceptance_criteria>Both focused suites reach the candidate/picker assertions and fail because discovery and selection behavior are not implemented, not because fixture or prompt setup failed.</acceptance_criteria>
  <done>The truthful candidate and picker contract is committed in RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN/REFACTOR — discover and confirm ordered source identities</name>
  <read_first>
    <file>tests/git/candidates.test.ts</file>
    <file>tests/cli/selection.test.ts</file>
    <file>src/git/runner.ts</file>
    <file>src/cli/run.ts</file>
    <file>src/contracts/comparison.ts</file>
  </read_first>
  <files>src/domain/source.ts, src/git/candidates.ts, src/cli/picker.ts, src/cli/confirm.ts, src/cli/run.ts, src/contracts/comparison.ts</files>
  <action>Model source identity separately from commit identity. discoverSourceCandidates must use the existing bounded argument-array runner and byte-safe Git protocols, retain all local branch and registered worktree records, probe each usable worktree's committed HEAD and dirty state, and retain unreachable/prunable registrations as disabled rows with an exact reason rather than omitting or calling them clean. Implement one @inquirer/search source used twice with Local branches and Worktrees separators, role-specific prompt copy, base-first state, current-source head suggestion, searchable identifying fields, no-match behavior, and explicit back transitions. Preserve same-OID rows as truthful candidates; equal-commit launch blocking belongs to Plan 03. confirmPinnedComparison must render the D-09 ordered identities, applicable paths, dirty warning, and exact pin statement/launch labels from 01-UI-SPEC.md. Replace the skeleton selection port in runCli without adding browser selection or guessed bases. Refactor only with identical tests green.</action>
  <verify><automated><command expected="0">npm run test:git -- tests/git/candidates.test.ts</command><command expected="0">npm run test:unit -- tests/cli/selection.test.ts</command></automated></verify>
  <acceptance_criteria>All candidate identity, grouping, search, base/head order, suggestion, unavailable, dirty, and confirmation cases pass; the valid ordered result still feeds the existing pinned-session skeleton.</acceptance_criteria>
  <done>Ordered searchable source selection is GREEN end to end.</done>
</task>
</tasks>

## Artifacts this phase produces

- `src/domain/source.ts` — `SourceCandidate`, `BranchCandidate`, `WorktreeCandidate`, `CandidateAvailability`, stable source IDs.
- `src/git/candidates.ts` — `discoverSourceCandidates` and byte-safe candidate parsers.
- `src/cli/picker.ts` — `pickOrderedSources` and reusable grouped search source.
- `src/cli/confirm.ts` — `confirmPinnedComparison`.
- `tests/git/candidates.test.ts` — real-Git candidate matrix.
- `tests/cli/selection.test.ts` — ordered picker/search/confirmation contract.
- Updated `src/cli/run.ts` and `src/contracts/comparison.ts` — complete source identity wiring and schemas.

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-05 | Spoofing | candidate labels/paths | mitigate | Control-safe presentation, separate immutable source IDs, no display string as authority. |
| T-01-06 | Tampering | candidate Git probes | mitigate | Argument arrays and NUL protocols only; no shell interpolation or line-oriented path parsing. |
| T-01-07 | Information disclosure | dirty state | mitigate | Expose only collapsed dirty/clean/unavailable status; never read or transmit dirty content. |
| T-01-SC | Tampering | dependencies | mitigate | Use the Plan 01 approved lockfile; no new installs. |
</threat_model>

<verification>
Run only the two focused test files. Record real-Git rows for duplicate OIDs, detached and unavailable worktrees, dirty classes, search/no-match behavior, ordered selection, and confirmation. Do not run formatters, linters, or broad suites.
</verification>

<success_criteria>
- SEL-03, SEL-04, and SEL-07 are delivered once in this plan and observable before launch.
- D-01–D-07 and D-09 copy/state rules are implemented without browser authority or guessed base selection.
- Candidate identity, exact path representation, and commit identity remain separate throughout selection.
</success_criteria>

<output>Create `.planning/phases/01-pinned-local-comparison/01-04-SUMMARY.md` with RED/GREEN evidence, fixture matrix, prompt copy, artifacts/symbols, and authority-boundary proof.</output>
