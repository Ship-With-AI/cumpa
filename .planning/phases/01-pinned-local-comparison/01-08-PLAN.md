---
phase: 01-pinned-local-comparison
plan: 08
type: tdd
wave: 8
depends_on: [01-07]
files_modified:
  - src/contracts/api.ts
  - src/server/app.ts
  - src/server/security.ts
  - src/server/capabilities.ts
  - src/server/routes.ts
  - src/web/api/client.ts
  - src/web/App.vue
  - tests/api/security.test.ts
  - tests/api/session.test.ts
autonomous: true
requirements: [SAFE-02, SAFE-03]
must_haves:
  truths:
    - "Only requests with the per-process bearer token, exact bound Host, and absent-or-exact bound Origin can read the frozen session or file metadata."
    - "Authorization and strict request validation run before route, capability, object, or Git work; denials reveal no token, path, object, command, stderr, or capability-existence detail."
    - "The browser can request only the frozen session and an opaque launch-scoped file ID; no API grammar selects repository, ref, object, path, Git option, export destination, mutation, or blob text."
    - "The token travels in the initial URL fragment, is removed immediately, remains in memory only, and never appears in logs, referrers, persistence, API bodies, or copy UI."
  artifacts:
    - path: src/server/security.ts
      provides: "Exact Host/Origin/Bearer gate and security headers"
      exports: [registerSessionSecurity]
    - path: src/server/capabilities.ts
      provides: "Frozen opaque file capability registry"
      exports: [createCapabilityRegistry]
    - path: src/server/routes.ts
      provides: "Narrow read-only session and file-metadata routes"
      exports: [registerSessionRoutes]
    - path: src/web/api/client.ts
      provides: "Fragment bootstrap and in-memory authenticated client"
      exports: [createSessionClient]
  key_links:
    - from: src/server/routes.ts
      to: src/server/capabilities.ts
      via: "fileId is the only file lookup input"
      pattern: "fileId"
    - from: src/web/api/client.ts
      to: src/server/security.ts
      via: "in-memory Authorization bearer with same-origin requests"
      pattern: "Authorization"
---

<objective>
Close the working browser session behind a strict process-scoped network and capability boundary.

Purpose: Deliver SAFE-02 and SAFE-03 plus D-16/D-18 non-leaking post-launch behavior without exposing general Git, filesystem, mutation, or content APIs.
Output: Strict API schemas, exact request gate, security headers, frozen registry/routes, fragment client, and Fastify inject/socket tests.
</objective>

## Phase Goal

**As a** developer working in a local Git repository, **I want to** choose an ordered base and head and open their pinned PR-style comparison in my browser, **so that** I can verify committed local changes without publishing branches or reading dirty worktree bytes.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>
<context>
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-RESEARCH.md
@.planning/phases/01-pinned-local-comparison/01-PATTERNS.md
@.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md
@.planning/phases/01-pinned-local-comparison/01-07-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify the closed loopback capability surface</name>
  <read_first>
    <file>.planning/phases/01-pinned-local-comparison/01-RESEARCH.md</file>
    <file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file>
    <file>src/server/app.ts</file>
    <file>src/contracts/comparison.ts</file>
    <file>src/web/App.vue</file>
  </read_first>
  <files>tests/api/security.test.ts, tests/api/session.test.ts</files>
  <behavior>
    - "Missing/wrong bearer, hostile Host, and unexpected present Origin are rejected before handler work; exact Host/token with absent Origin and exact same Origin succeed."
    - "Traversal-like/unknown file IDs, extra authority fields, unknown routes/methods, and malformed JSON are generic externally and make zero Git/object calls."
    - "Fragment token is removed into memory and sent only as Authorization; responses carry no-store/no-referrer/nosniff/frame/base/CSP protections and no CORS."
  </behavior>
  <action>Write Fastify inject tests first plus one focused real-socket browser bootstrap check. Cover missing/wrong token, length mismatch, hostile Host, wrong/same/absent Origin, traversal and unknown file IDs, extra repository/ref/commit/blob/path/Git/export/mutation fields, wrong methods/routes, malformed payloads, object disappearance, and logger redaction. Assert denials execute before route/capability/object spies and all errors use the 01-UI-SPEC.md generic security or post-launch recovery copy with a terminal-only correlation detail. Assert static and API responses have the required security headers and no permissive CORS. Commit a named behavioral RED before implementation.</action>
  <verify><automated><command expected="nonzero" classification="behavioral-red">npm run test:api -- tests/api/security.test.ts tests/api/session.test.ts</command></automated></verify>
  <acceptance_criteria>The focused API command reaches authorization/capability assertions and fails only because the closed surface is absent.</acceptance_criteria>
  <done>The closed loopback capability contract is committed in RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN/REFACTOR — authorize frozen session metadata only</name>
  <read_first>
    <file>tests/api/security.test.ts</file>
    <file>tests/api/session.test.ts</file>
    <file>src/server/app.ts</file>
    <file>src/contracts/comparison.ts</file>
    <file>src/git/inventory.ts</file>
    <file>src/web/App.vue</file>
  </read_first>
  <files>src/contracts/api.ts, src/server/app.ts, src/server/security.ts, src/server/capabilities.ts, src/server/routes.ts, src/web/api/client.ts, src/web/App.vue</files>
  <action>Generate at least 32 random bytes per process, encode base64url, compare provided tokens in constant time after safe length handling, and redact them from all diagnostics. Freeze expectedHost and expectedOrigin only after the 127.0.0.1:0 bind resolves. Require exact Host, reject any present non-exact Origin, allow absent Origin only with exact Host and token, and do not enable CORS. Apply no-store, no-referrer, nosniff, frame-ancestors none, base-uri none, and same-origin-only CSP compatible with packaged assets. Define strict GET /api/session and GET /api/files/:fileId responses; reject unknown/extra request authority before handlers. Close routes over createCapabilityRegistry(Map<fileId, frozen metadata>) and never accept repository/ref/object/path/export/blob text or mutations. In createSessionClient, read token from fragment, immediately replace browser history, retain token only in closure memory, and attach bearer to same-origin calls. Keep UI error copy non-leaking per 01-UI-SPEC.md and terminal diagnostics correlated without token. Refactor only after the identical focused command and socket case pass.</action>
  <verify><automated><command expected="0">npm run test:api -- tests/api/security.test.ts tests/api/session.test.ts</command><command expected="0">npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "fragment token protects loopback API"</command></automated></verify>
  <acceptance_criteria>The full Host/Origin/token matrix, security headers, opaque capability constraints, zero-work denials, token lifecycle, non-leaking errors, and valid same-session reads pass.</acceptance_criteria>
  <done>The token, origin, host, and opaque capability boundary is GREEN.</done>
</task>
</tasks>

## Artifacts this phase produces

- `src/contracts/api.ts` — strict `SessionResponseSchema`, `FileMetadataResponseSchema`, `ApiErrorSchema`, inferred types.
- `src/server/security.ts` — `registerSessionSecurity`, constant-time token checker, headers/error mapper.
- `src/server/capabilities.ts` — `createCapabilityRegistry`, frozen `FileCapability` lookup.
- `src/server/routes.ts` — `registerSessionRoutes` for exactly two read-only GET capabilities.
- `src/web/api/client.ts` — `createSessionClient`, fragment-token bootstrap, authenticated request helpers.
- `tests/api/security.test.ts` — authorization/header/non-leak/zero-work matrix.
- `tests/api/session.test.ts` — strict route and opaque capability contract.
- Updated `src/server/app.ts`, `src/web/App.vue` — secured factory wiring and validated client consumption.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Public browser origin → loopback | A hostile page may probe a local TCP service. |
| Browser DTO → capability registry | Untrusted request values attempt to influence repository/object access. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-18 | Spoofing | loopback request | mitigate | Per-process 256-bit+ bearer, exact Host, exact-if-present Origin, no CORS. |
| T-01-19 | Information disclosure | token/errors | mitigate | Fragment bootstrap, immediate removal, memory-only token, redaction, generic browser errors. |
| T-01-20 | Elevation | request grammar | mitigate | Strict two-route schemas and opaque file IDs; no repository/ref/object/path/export/blob authority. |
| T-01-21 | Tampering | capability map | mitigate | Freeze launch-scoped map before serving; no request mutation path. |
| T-01-SC | Tampering | dependencies | mitigate | No new installs. |
</threat_model>

<verification>
Run only the focused API files and named packaged token smoke. Record authorization matrix, zero-work spies, security headers, strict route grammar, token removal/redaction, generic browser copy, and terminal correlation. Do not run formatters, linters, or broad suites.
</verification>
<success_criteria>
- SAFE-02 and SAFE-03 are delivered once in this plan.
- Loopback, token, origin, host, capability, and response-policy controls fail closed without broad fallback.
- Browser input cannot select any repository, ref, object, path, command option, export target, mutation, or blob content.
</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-08-SUMMARY.md` with RED/GREEN evidence, artifacts/symbols, authorization matrix, zero-work proof, security headers, token lifecycle, socket smoke, and non-leak evidence.</output>
