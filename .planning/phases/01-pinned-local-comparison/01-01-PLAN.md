---
phase: 01-pinned-local-comparison
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - tsconfig.json
  - vite.config.ts
  - scripts/build-bin.mjs
  - src/web/index.html
  - src/web/main.ts
  - src/web/App.vue
  - tests/e2e/package-assets.spec.ts
autonomous: false
requirements: []
must_haves:
  truths:
    - "Every installed release is queried independently by exact package name and version and receives an explicit human approve, replace, or reject disposition before installation."
    - "The generated executable and production Vue asset bundle are present in the packed artifact and can be launched through the package bin boundary."
  artifacts:
    - path: package.json
      provides: "One-package Node 24 ESM scripts, exact dependencies, and diff-review bin mapping"
    - path: scripts/build-bin.mjs
      provides: "Deterministic generated executable wrapper"
    - path: tests/e2e/package-assets.spec.ts
      provides: "Packaged executable and production-asset smoke contract"
  key_links:
    - from: package.json
      to: scripts/build-bin.mjs
      via: "build and pack scripts generate the declared bin"
      pattern: "build-bin"
---

<objective>
Create the audited package/build/generated-bin execution unit that the walking skeleton depends on.

Purpose: Isolate supply-chain approval and release-shape proof from Git and server behavior while preserving the first vertical proof through the real packed executable and production Vue assets. This supporting plan adds no requirement ownership; Phase 1's 23 requirement IDs remain owned exactly once by the behavior plans.
Output: Approved exact dependency graph, build configuration, generated bin, production Vue bootstrap, and focused packaged-asset smoke.
</objective>

## Phase Goal

**As a** developer working in a local Git repository, **I want to** choose an ordered base and head and open their pinned PR-style comparison in my browser, **so that** I can verify committed local changes without publishing branches or reading dirty worktree bytes.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-RESEARCH.md
@.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md
@.planning/phases/01-pinned-local-comparison/SKELETON.md
</context>

<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Approve every exact package release independently</name>
  <read_first>
    <file>.planning/phases/01-pinned-local-comparison/01-RESEARCH.md</file>
  </read_first>
  <files>package.json, package-lock.json</files>
  <action>Before any install, run each exact-release query below as its own npm invocation. Record version, maintainers, publisher when returned, repository, publication time, engines, peer dependencies, lifecycle scripts, distribution integrity/signatures/provenance fields when returned, and deprecation for every row. Check the Fastify/static and Vite/Vue/plugin/TypeScript/vue-tsc compatibility groups. Present one row per release with evidence and an explicit human disposition of approve, replace with a separately queried exact release, or reject. Any SUS/ASSUMED row remains blocked until disposed by the human; any SLOP row is forbidden. Install only the resulting approved exact releases and copy the complete table and dispositions into 01-01-SUMMARY.md. Do not install Monaco or packages excluded by research.</action>
  <verify>
    <automated>
      <command expected="0">npm view commander@15.0.0 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view @inquirer/search@4.2.1 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view fastify@5.10.0 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view @fastify/static@9.3.0 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view zod@4.4.3 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view open@11.0.0 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view vue@3.5.39 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view vite@8.1.4 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view @vitejs/plugin-vue@6.0.7 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view typescript@7.0.2 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view @types/node@24.11.1 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view vue-tsc@3.3.7 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view vitest@4.1.10 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
      <command expected="0">npm view @playwright/test@1.61.1 version maintainers repository time engines peerDependencies scripts dist deprecation</command>
    </automated>
    <human-check>For every exact row, explicitly approve it, name an independently audited replacement, or reject it; approve both compatibility groups before installation.</human-check>
  </verify>
  <acceptance_criteria>All fourteen proposed releases have a valid independent metadata result and explicit disposition, compatibility sets are approved, no SLOP release is accepted, and installation begins only after the gate clears.</acceptance_criteria>
  <done>The exact install set and human dispositions are auditable before package-lock creation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED — specify the packed executable and production assets</name>
  <read_first>
    <file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file>
    <file>.planning/phases/01-pinned-local-comparison/SKELETON.md</file>
  </read_first>
  <files>package.json, package-lock.json, tsconfig.json, vite.config.ts, scripts/build-bin.mjs, tests/e2e/package-assets.spec.ts</files>
  <behavior>
    - "npm pack contains the generated diff-review bin and production-built Vue entry/assets, not TypeScript source or a dev proxy."
    - "Executing the packed bin reaches its production entry and reports the expected not-yet-wired launch boundary rather than resolving an undeclared source path."
  </behavior>
  <action>Install only the approved exact set. Establish Node 24 ESM, Node/shared compilation, Vite production build rooted at src/web, deterministic executable generation, package-script entry points for the focused test categories configured by Plans 02–03, and a pack inspection helper. Write the packaged smoke first; classify build/browser/package failures as prerequisites and require the named behavioral assertion to fail because the production Vue bootstrap is not yet present.</action>
  <verify><automated><command expected="0" classification="prerequisite">npm run verify:prerequisites</command><command expected="nonzero" classification="behavioral-red">npm run test:package -- tests/e2e/package-assets.spec.ts --grep "packed executable contains production Vue assets"</command></automated></verify>
  <acceptance_criteria>The named test reaches its packaged-runtime assertion and fails only for missing production bootstrap behavior.</acceptance_criteria>
  <done>The release-shape contract is RED after all prerequisites pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: GREEN — generate the bin and minimal production Vue asset</name>
  <read_first>
    <file>tests/e2e/package-assets.spec.ts</file>
    <file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file>
  </read_first>
  <files>src/web/index.html, src/web/main.ts, src/web/App.vue, scripts/build-bin.mjs, tests/e2e/package-assets.spec.ts</files>
  <action>Create the production entry reached by the generated bin and the smallest App.vue bootstrap; main.ts must import and mount that exact App.vue production root so Plan 03 extends an already wired component rather than creates a parallel entry. Vite must emit the real packaged index and assets. Apply the verified UI contract's system sans fallback, dark surface, visible focus token, and loading h1/status. Keep Git, listener, session composition, and Monaco absent until their dependency-ordered units.</action>
  <verify><automated><command expected="0">npm run test:package -- tests/e2e/package-assets.spec.ts --grep "packed executable contains production Vue assets"</command><command expected="0">npm pack --dry-run</command></automated></verify>
  <acceptance_criteria>The packed executable resolves the production entry and package contents include only required runtime output and Vue assets.</acceptance_criteria>
  <done>The package/bin/asset walking-skeleton unit is GREEN.</done>
</task>
</tasks>

<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-SC | Tampering | npm/browser supply chain | mitigate | Independent exact-release metadata queries plus blocking per-row human disposition before install. |
| T-01-01 | Tampering | packed executable | mitigate | Deterministic generated wrapper and pack-content assertion; no source-path fallback. |
</threat_model>

<verification>Run only the exact metadata commands, prerequisite smoke, named packaged test, and dry-run pack audit above. Do not run formatters, linters, project-wide tests, or implementation from later units.</verification>
<success_criteria>The approved package can generate and exercise its real executable and production Vue asset boundary without crossing the 15-file blocker threshold.</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-01-SUMMARY.md` with all release evidence/dispositions, RED/GREEN evidence, scripts, generated-bin path, and pack inventory.</output>
