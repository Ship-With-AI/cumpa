---
phase: 01-pinned-local-comparison
plan: 03
type: tdd
wave: 3
depends_on: [01-02]
files_modified:
  - src/cli/run.ts
  - src/server/app.ts
  - src/server/lifecycle.ts
  - src/web/App.vue
  - src/web/styles.css
  - tests/e2e/pinned-session.spec.ts
  - playwright.config.ts
requirements: [SEL-08, SAFE-01, SAFE-05]
autonomous: true
must_haves:
  truths:
    - "The generated CLI freezes comparison data before binding Fastify only to 127.0.0.1 on an OS-assigned port and opens the production Vue page afterward."
    - "The actual URL and Ctrl+C instruction print before browser-open attempt; opener failure leaves the URL usable."
    - "One interrupt idempotently aborts active Git work and closes the listener exactly once."
  artifacts:
    - path: src/server/app.ts
      provides: "Fastify factory for one frozen session"
      exports: [createSessionApp]
    - path: src/server/lifecycle.ts
      provides: "Idempotent loopback shutdown controller"
      exports: [createShutdownController]
    - path: tests/e2e/pinned-session.spec.ts
      provides: "Generated-bin lifecycle and production Vue proof"
  key_links:
    - from: src/cli/run.ts
      to: src/server/app.ts
      via: "frozen comparison enters app factory before listen"
      pattern: "createSessionApp"
    - from: src/server/app.ts
      to: src/web/App.vue
      via: "production asset path renders frozen identities"
      pattern: "api/session"
---
<objective>
Complete the initial walking skeleton from frozen Git comparison through loopback lifecycle to the production Vue page.

Purpose: Deliver SEL-08, SAFE-01, and SAFE-05 per D-08, D-10, and D-16 without mixing package scaffolding or Git-core implementation into this unit.
Output: Focused packaged RED/GREEN test, loopback app/lifecycle, CLI wiring, and minimal verified-contract identity page.
</objective>
<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md
@.planning/phases/01-pinned-local-comparison/SKELETON.md
@.planning/phases/01-pinned-local-comparison/01-02-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: RED — specify loopback production-session lifecycle</name>
  <read_first><file>tests/git/pinned-comparison.test.ts</file><file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file><file>src/cli/run.ts</file></read_first>
  <files>playwright.config.ts, tests/e2e/pinned-session.spec.ts</files>
  <behavior>
    - "The generated bin binds 127.0.0.1:0 after freezing identities and opens the actual production Vue URL."
    - "Printed URL and shutdown instructions precede opener invocation; opener rejection does not stop the server."
    - "A terminal interrupt closes the listener and active Git children exactly once."
  </behavior>
  <action>Configure packaged Playwright execution on the Plan 01 build/package scripts and write the named cases before implementation, using the approved packed executable and real-Git fixture from Plans 01–02. Classify build, package, Git, and exact Chromium identity as prerequisites; RED must reach a browser/lifecycle assertion rather than fail setup.</action>
  <verify><automated><command expected="0" classification="prerequisite">npm run verify:prerequisites</command><command expected="nonzero" classification="behavioral-red">npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "generated CLI opens immutable pinned session|interrupt closes loopback session once"</command></automated></verify>
  <acceptance_criteria>Both named cases reach missing lifecycle/UI behavior and fail for that reason only.</acceptance_criteria>
  <done>The loopback packaged contract is RED.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: GREEN — serve the frozen comparison and drain once</name>
  <read_first><file>tests/e2e/pinned-session.spec.ts</file><file>src/git/comparison.ts</file><file>src/cli/run.ts</file><file>src/web/main.ts</file><file>.planning/phases/01-pinned-local-comparison/01-UI-SPEC.md</file></read_first>
  <files>src/cli/run.ts, src/server/app.ts, src/server/lifecycle.ts, src/web/App.vue, src/web/styles.css</files>
  <action>Freeze the Plan 02 comparison before createSessionApp; bind explicitly to host 127.0.0.1 and port 0, obtain the actual address, generate a 256-bit-or-stronger process token for the later security gate, print the URL and exact fallback/shutdown copy before invoking the injectable opener, and keep serving if the opener rejects. Extend the App.vue already mounted by Plan 01's main.ts and serve only that production Vite path, rendering the validated labels/full IDs/loading transition with verified UI tokens; do not create or modify a second browser entry. Register one idempotent shutdown promise that removes signal handlers, aborts tracked Git children, awaits Fastify close, and preserves the first exit status.</action>
  <verify><automated><command expected="0">npm run test:package -- tests/e2e/pinned-session.spec.ts --grep "generated CLI opens immutable pinned session|interrupt closes loopback session once"</command></automated></verify>
  <acceptance_criteria>The actual production page shows independent frozen Git identities from an ephemeral IPv4-loopback URL; opener fallback and single-drain shutdown pass.</acceptance_criteria>
  <done>The complete initial walking skeleton is GREEN.</done>
</task>
</tasks>
<threat_model>
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-01-04 | Spoofing | listener | mitigate | Explicit 127.0.0.1:0, process token seed, and actual-address URL. |
| T-01-05 | Denial of service | shutdown | mitigate | One idempotent drain promise aborts children and awaits server close. |
| T-01-SC | Tampering | dependencies | mitigate | Reuse Plan 01 lockfile; no installs. |
</threat_model>
<verification>Run only the prerequisite check and named generated-package cases above.</verification>
<success_criteria>All three owned requirements are proven through the real generated executable and production browser path within seven modified files.</success_criteria>
<output>Create `.planning/phases/01-pinned-local-comparison/01-03-SUMMARY.md` with RED/GREEN evidence, bound address, opener order/fallback, identity proof, and shutdown counts.</output>
