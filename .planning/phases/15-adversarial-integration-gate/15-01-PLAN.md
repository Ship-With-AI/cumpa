---
phase: 15-adversarial-integration-gate
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/cli/run.ts
  - src/server/app.ts
  - src/server/capabilities.ts
  - src/server/draft-loader.ts
  - src/server/draft-store.ts
  - src/server/export-store.ts
  - tests/api/draft.test.ts
  - tests/api/exact-patch.test.ts
  - tests/api/export-publication.test.ts
  - tests/api/attached-completion.test.ts
  - tests/api/attached-completion-coordinator.test.ts
  - tests/cli/request.test.ts
  - tests/e2e/agent-ready-export.spec.ts
autonomous: true
requirements:
  - HAND-06
task_waves:
  - wave: 1
    tasks: [15-01-01]
    depends_on: []
    gate: "Task 1 writes the failing seam tests, implements the named production seam, and leaves its focused command green before Task 2 starts."
  - wave: 2
    tasks: [15-01-02]
    depends_on: [15-01-01]
    gate: "Begin only after Task 1 is committed and its focused command passes; Task 2 adds test-only regression evidence and may not change production files."
  - wave: 3
    tasks: [15-01-03]
    depends_on: [15-01-02]
    gate: "Begin only after Task 2 is committed and the combined Wave 1-2 API/CLI gate passes."
must_haves:
  truths:
    - "HAND-06 / criterion 1: Equivalent range or exact-patch submissions retain equal deterministic source reviewKey provenance while every attached invocation owns a distinct server-created draft, queue, export, reveal, and recovery namespace."
    - "HAND-06 / criterion 2: An attached invocation over the same revisions as an interactive review starts with an empty attached draft and cannot resume, overwrite, reveal, publish, or recover through the interactive comparison identity."
    - "HAND-06 / criterion 3: Success, delivery failure, signal cancellation, and duplicate or concurrent Finish affect only their own coordinator, browser session, stdout descriptor, listener, shutdown controller, patch snapshot, and durable storage scope."
    - "Every successful attached process emits exactly one complete canonical V2 or V3 document with no trailing bytes; every failed or signalled process emits zero success bytes and cannot report exit 0."
    - "Interactive draft filenames and export directories remain byte-for-byte compatible, and existing interactive data requires no migration."
  prohibitions:
    - "Never randomize or replace comparisonKey(), rangeReviewKey(), exact-patch reviewKey, DraftComparison provenance, ExportPublicationIdentity provenance, or canonical V2/V3 fields."
    - "Never accept a storage identity from stdin, request JSON, URL, browser state, bearer token, environment launch options, or any other client-controlled input."
    - "Never use an unprefixed nonce, silently fall back after an invalid attached scope, independently allocate draft and export identities, or let their receipt paths disagree."
    - "Never add a dependency, database, lock service, daemon, global request registry, wrapper output protocol, serializer, request schema field, browser route, test runner, fake UI/server, or second package/browser harness."
    - "Never weaken stable-name validation, managed-root and symlink checks, atomic replace/publication, revision fencing, canonical provenance validation, response-settlement ordering, or loopback/token isolation."
  artifacts:
    - path: src/cli/run.ts
      provides: "Single trusted attached storage-scope allocation at launchAttachedSession() and unchanged per-invocation lifecycle composition"
    - path: src/server/draft-loader.ts
      provides: "Validated attached storage override for draftPaths() while classifyDraft() continues checking deterministic source provenance"
    - path: src/server/draft-store.ts
      provides: "One attached key shared by canonical draft path, temporary file names, and runSerialized() queue ownership"
    - path: src/server/export-store.ts
      provides: "Separated publication storage name and ExportPublicationIdentity provenance for publish/recovery"
    - path: src/server/capabilities.ts
      provides: "Same attached storage scope wired to range/patch draft, Export, reveal, and Finish boundaries"
    - path: tests/e2e/agent-ready-export.spec.ts
      provides: "Packaged Chromium evidence for equivalent ranges, equivalent exact patches, interactive coexistence, process failure, exact files, and exact stdout"
  key_links:
    - from: src/cli/run.ts
      to: src/server/app.ts
      via: "launchAttachedSession() creates one domain-prefixed storage scope and passes it inside the existing AttachedCompletionOptions used by both range and patch app factories"
      pattern: "launchAttachedSession|attachedCompletion|storageScope"
    - from: src/server/app.ts
      to: src/server/draft-store.ts
      via: "createAppDraftStore() and createExactPatchCapabilityRegistry() pass the same optional attached scope to createDraftStore()"
      pattern: "createAppDraftStore|createDraftStore|storageScope"
    - from: src/server/draft-store.ts
      to: src/server/draft-loader.ts
      via: "createDraftStore() passes one scope to createDraftLoader(); DraftPaths.key drives canonicalPath, temporary names, and queueKey"
      pattern: "createDraftLoader|queueKey|canonicalPath"
    - from: src/server/capabilities.ts
      to: src/server/export-store.ts
      via: "range and exact-patch Export/reveal use the same attached scope for storage while publishReviewExport() still validates canonical bytes against deterministic identity"
      pattern: "publishReviewExport|revealExportDirectory|storageScope"
    - from: tests/e2e/agent-ready-export.spec.ts
      to: dist/bin/cumpa.mjs
      via: "existing startGeneratedCli(), startAttachedCli(), loopback, file-descriptor, and Chromium helpers exercise the extracted npm package"
      pattern: "startGeneratedCli|startAttachedCli|openSession|parseCanonicalReviewExport"
---

<objective>
Close HAND-06 by separating mutable attached-invocation storage ownership from immutable source provenance, then prove the separation through the current API, CLI, packaged browser, filesystem, stdout, and shutdown paths.

Purpose: Equivalent agent submissions must remain canonically comparable without sharing drafts, exports, browser/process state, or terminal delivery; attached work must never occupy an interactive identity.
Output: One trusted dual-identity production seam, focused adversarial API/CLI regressions, and packaged Chromium evidence covering all three Phase 15 roadmap criteria.
Plan shape: This intentionally remains one shared-seam/evidence plan because every task creates or consumes the same attached composition and draft harness; strict Wave 1 → Wave 2 → Wave 3 execution prevents overlap while preserving the contracted smallest plan set.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-adversarial-integration-gate/15-CONTEXT.md
@.planning/phases/15-adversarial-integration-gate/15-RESEARCH.md
@.planning/phases/15-adversarial-integration-gate/15-PATTERNS.md
@.planning/phases/15-adversarial-integration-gate/15-VALIDATION.md
@.planning/phases/14-attached-lifecycle-canonical-completion/14-01-SUMMARY.md
@.planning/phases/14-attached-lifecycle-canonical-completion/14-02-SUMMARY.md
@.planning/phases/14-attached-lifecycle-canonical-completion/14-03-SUMMARY.md
</context>

<execution_guard>
This file is a plan only. Do not run Vitest, Playwright, package builds, linters, formatters, or project-wide tests while planning. The executor runs every RED/GREEN and evidence command below only during implementation. Reuse the existing installed toolchain and harnesses; no install step is authorized.
</execution_guard>

<interfaces>
The production change is one dual-identity seam, not a second workflow:

- `AttachedCompletionOptions` gains one readonly internal `storageScope` created exactly once by `launchAttachedSession()`. Use `agent-${randomBytes(16).toString('hex')}`: fixed `agent-` domain prefix plus 32 lowercase hexadecimal characters. The value is unrelated to the bearer token and never enters a request or browser contract.
- The existing range and exact-patch app factories receive that same `AttachedCompletionOptions`; no second allocator is permitted in app, capability, draft, export, or patch-snapshot code.
- `createDraftStore()` / `createDraftLoader()` / `draftPaths()` accept the optional trusted scope. When present, it is the sole `DraftPaths.key`, so the canonical file, temporary siblings, and process serialization queue agree. The persisted `DraftComparison` and `sameComparison()` checks remain source-provenance checks.
- `publishReviewExport()` and `recoverReviewExport()` accept the optional trusted scope as the stable storage name while `ExportPublicationIdentity`, `matchesPublicationIdentity()`, and canonical V2/V3 documents keep the deterministic range/patch `reviewKey`. Invalid attached names fail explicitly before any filesystem join; they never fall back to the provenance identity.
- `createCapabilityRegistry()` and `createExactPatchCapabilityRegistry()` use the same scope for draft, Export, reveal, and recovery paths. Ordinary interactive sessions omit it and retain current `<comparisonKey>.json` and `<baseOid>..<headOid>` storage.
</interfaces>

<phase_coverage_matrix>
| Source item | Named task coverage | Final evidence |
|---|---|---|
| GOAL: attached and interactive scopes remain isolated through repeated equivalent submissions and failures | Tasks 1–3 | Focused API/CLI suites plus extracted-package Chromium process, filesystem, and stdout assertions |
| HAND-06 | Tasks 1–3 | All three roadmap criteria are asserted in `tests/e2e/agent-ready-export.spec.ts`; focused seams localize failures |
| Criterion 1: identical Git inputs keep independent request identities, drafts, exports, and feedback | Task 1 establishes and API-tests dual identity; Task 3 runs equivalent range and patch children | Equal canonical `reviewKey`; different `agent-*` draft/export paths and raw feedback; independent complete stdout documents |
| Criterion 2: attached cannot resume, overwrite, or publish through interactive identity | Task 2 tests attached-versus-interactive API/CLI ownership; Task 3 runs both packaged modes over the same revisions | Attached starts missing/revision 0; interactive raw files and rendered feedback remain exact after attached Export and Finish |
| Criterion 3: completing/failing one scope changes no peer and emits no duplicate/partial success | Task 2 gates coordinator/CLI success, failure, signal, and duplicate Finish; Task 3 proves multi-process liveness and signal isolation | One-delivery counters, zero failure stdout, exact one-document success stdout, exit codes, peer liveness, and unchanged peer files/browser |
| RESEARCH: deterministic provenance and server-created attached storage are separate | Task 1 | Equal range/patch `reviewKey` survives storage separation; canonical schemas and source hashes remain unchanged |
| RESEARCH scenarios S1–S4 and validation threats T-15-01..T-15-05 | Tasks 1–3 | API cross-instance race cases plus packaged range, interactive coexistence, and exact-patch failure cases |
| CONTEXT boundary: repeated equivalent attached and interactive scopes stay isolated | Tasks 1–3 | No deferred item exists; no future AGENT-04/PATCH-06 behavior is introduced |
| PATTERNS: reuse current app, coordinator, lifecycle, package, loopback, Chromium, and Git fixture seams | Tasks 1–3 | No parallel infrastructure or dependency is created |
</phase_coverage_matrix>

## Artifacts this phase produces

| Artifact | Observable proof |
|---|---|
| Trusted `agent-<32 lowercase hex>` attached storage scope in `src/cli/run.ts` | Two launches receive different scopes; strict request input has no field capable of selecting either value |
| Draft ownership override across `src/server/app.ts`, `src/server/draft-store.ts`, and `src/server/draft-loader.ts` | Equal-provenance attached apps start independently, serialize on different keys, persist different raw bytes, and do not touch legacy interactive paths |
| Export/reveal/recovery ownership override across `src/server/capabilities.ts` and `src/server/export-store.ts` | Receipt and reveal paths share each invocation's attached component while canonical bytes still validate against the deterministic source identity |
| Focused API/CLI adversarial regressions in the six existing test files | Task 1 RED/GREEN establishes the seam; Task 2 adds test-only success/failure/signal/duplicate-Finish evidence for invocation-local ownership. |
| Packaged Chromium adversarial gate in `tests/e2e/agent-ready-export.spec.ts` | Real extracted CLI children, real loopback servers, Chromium pages, `.compare` bytes, exit status, and entire stdout files prove criteria 1–3 |

<tasks>

<task id="15-01-01" wave="1" type="auto" tdd="true">
  <name>Task 1: TDD the trusted attached storage seam without changing source provenance</name>
  <files>tests/api/draft.test.ts, tests/api/exact-patch.test.ts, tests/api/export-publication.test.ts, src/cli/run.ts, src/server/app.ts, src/server/capabilities.ts, src/server/draft-loader.ts, src/server/draft-store.ts, src/server/export-store.ts</files>
  <read_first>
    - `.planning/phases/15-adversarial-integration-gate/15-RESEARCH.md`: “Dual identity,” “Exact collision chain,” S1, and minimal plan Task 1.
    - `.planning/phases/15-adversarial-integration-gate/15-PATTERNS.md`: one trusted value ownership fence and prohibited divergent patterns.
    - `src/cli/run.ts`: `launchAttachedSession()`, `launchExactPatchSession()`, `runOrdinaryAction()`, and the current `randomBytes()` token allocation.
    - `src/server/app.ts`: `CreateSessionAppOptions`, `CreateExactPatchSessionAppOptions`, `createAppDraftStore()`, `createSessionApp()`, and `createExactPatchSessionApp()`.
    - `src/server/capabilities.ts`: `CapabilityRegistryOptions`, `AttachedCompletionOptions`, `createCapabilityRegistry()`, `createExactPatchCapabilityRegistry()`, both `revealExportDirectory()` implementations, and both `publishReviewExport()` call sites.
    - `src/server/draft-loader.ts`: `DraftPaths`, `sameComparison()`, `draftPaths()`, and `createDraftLoader()`.
    - `src/server/draft-store.ts`: `runSerialized()`, `createDraftStore()`, `queueKey`, and temporary/canonical path use.
    - `src/server/export-store.ts`: `ExportPublicationIdentity`, `PublishReviewExportInput`, `stableNameFor()`, `matchesPublicationIdentity()`, `publishReviewExport()`, and `recoverReviewExport()`.
    - Existing helpers in `tests/api/draft.test.ts` (`range()`, `comparison()`, `buildApp()`), `tests/api/exact-patch.test.ts` (`grounded()`, `buildApp()`, `setSummary()`, `exportReview()`), and `tests/api/export-publication.test.ts` (`candidatePair()`, `publishReviewExport()`, `recoverReviewExport()`).
  </read_first>
  <behavior>
    - Two range apps with identical pinned OIDs/pathspecs have equal `range.reviewKey` but different attached draft keys, queue ownership, export directories, and raw feedback.
    - Two exact-patch apps built from the same `GroundedExactPatch` have equal digest/`reviewKey` and canonical V3 provenance but different attached draft/export paths; closing one does not remove or corrupt the other's snapshot or files.
    - Attached draft and export components match `^agent-[0-9a-f]{32}$`; traversal, bare 40/64-hex values, pair-shaped names, malformed prefixes, and any invalid override are rejected before joining paths.
    - Interactive calls with no attached scope keep existing deterministic draft/export paths and recover existing bytes unchanged.
    - Draft/export receipts use the same scope component, but `classifyDraft()`, `matchesPublicationIdentity()`, V2/V3 schemas, and canonical output continue to validate deterministic source provenance.
  </behavior>
  <action>
RED first: extend only the three named API files with real stores/session apps and temporary repository roots. Assert equal source provenance, distinct structurally prefixed attached paths, distinct raw draft/export bytes, same-scope publish/reveal/recovery, legacy interactive compatibility, and explicit invalid-name rejection. Run the task's focused command and record that it fails because attached storage still aliases deterministic `reviewKey`; a test that passes without the new seam is vacuous and must be strengthened before production edits.

GREEN/REFACTOR: in `launchAttachedSession()`, allocate exactly one `agent-${randomBytes(16).toString('hex')}` scope beside the existing coordinator/token and place it on `AttachedCompletionOptions`. Thread that unchanged object through the current range and exact-patch factory callbacks. Do not read or derive the value from `AgentReviewRequest`, `ExactPatchRequest`, URL fragments, browser payloads, `COMPARE_LAUNCH_OPTIONS`, source OIDs, pathspecs, patch digest, `reviewKey`, or the bearer token.

Extend the existing draft options/path seam so a present attached scope is strictly validated and becomes `DraftPaths.key`; one key must drive `canonicalPath`, `relativePath`, temporary siblings, and `queueKey`. Keep `DraftComparison`, `sameComparison()`, and saved draft provenance unchanged. Omission must preserve every interactive path; invalid presence must throw/fail, never fall back.

Extend the existing export input/recovery seam with the same optional scope. `stableNameFor()` selects and validates the attached storage name only for filesystem ownership; `matchesPublicationIdentity()` continues validating the canonical document against the deterministic `ExportPublicationIdentity`. Both capability registries pass the exact same value to publish and reveal calculations. Preserve atomic publication, managed-root/symlink guards, exact-byte re-read, native exchange behavior, receipts, and all canonical builders.

Run the same command to GREEN, refactor only duplication introduced by this seam, then run it once more. No schema, UI, dependency, migration, alternate lock, or alternate allocator is allowed.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run --no-file-parallelism tests/api/draft.test.ts tests/api/exact-patch.test.ts tests/api/export-publication.test.ts</automated>
    <expected>During implementation: RED fails only on missing attached namespace separation; after production work, all three focused API files pass with equal provenance, distinct storage, invalid-name rejection, and unchanged interactive paths.</expected>
  </verify>
  <acceptance_criteria>
    - HAND-06 criterion 1 is proven for both range and exact-patch API paths with exact files, not object-only equality or mtimes.
    - The allocator has one owner (`launchAttachedSession()`), one structural namespace, and no client-controlled input.
    - Range and patch `reviewKey` values and canonical V2/V3 provenance remain deterministic and equal for equivalent submissions.
    - Attached draft, queue, export, reveal, and recovery use one matching storage scope; interactive behavior and stored data remain unchanged.
  </acceptance_criteria>
  <done>The focused API suite is green and the only production change is the smallest dual-identity seam across the six named production files.</done>
  <commit>After GREEN verification, create one atomic commit containing this task only: `feat(15-01): isolate attached draft and export storage`.</commit>
</task>

<task id="15-01-02" wave="2" depends_on="15-01-01" type="auto">
  <name>Task 2: Prove cross-session lifecycle, interactive coexistence, and one-shot delivery isolation</name>
  <files>tests/api/draft.test.ts, tests/api/attached-completion.test.ts, tests/api/attached-completion-coordinator.test.ts, tests/cli/request.test.ts</files>
  <read_first>
    - Task 1's green diff and focused evidence; use its one `AttachedCompletionOptions.storageScope`, never create another identity.
    - `src/server/attached-completion.ts`: `AttachedCompletionCoordinator.status()`, `finish()`, `runDelivery()`, `delivery`, `responseSettled`, `markResponseSettled()`, and `cancel()`.
    - `src/server/lifecycle.ts`: `createShutdownController()` signal ownership, close ordering, exit status, and idempotence.
    - `src/cli/run.ts`: `writeStdout()`, `waitForAttachedOutcome()`, `launchAttachedSession()`, and both `runOrdinaryAction()` mode branches.
    - `src/server/capabilities.ts`: range and exact-patch `attachedCompletion.finish()` closures and their `draftStore.settle()` / canonical delivery boundaries.
    - `tests/api/attached-completion.test.ts`: authenticated `inject()` route harness for range and exact-patch Finish.
    - `tests/api/attached-completion-coordinator.test.ts`: concurrent Finish gate, delivery count, retryable/terminal states, and mutation fencing.
    - `tests/cli/request.test.ts`: `ordinary action request ownership`, injected `stdout`, `signalSource`, app factories, exit status, cancellation-at-delivery, exact-patch failure, and prelaunch silence cases.
    - `tests/api/draft.test.ts`: current interactive comparison-local persistence helpers, now plus Task 1's attached storage assertions.
  </read_first>
  <behavior>
    - Attached and interactive apps over the same revisions use disjoint paths; attached starts `missing` at revision 0 and never displays, mutates, reveals, exports, or recovers the interactive feedback.
    - Two attached coordinators can Finish concurrently without sharing status, delivery, response-settlement, draft queue, or shutdown state.
    - Concurrent or duplicate Finish on one coordinator coalesces one in-flight operation and delivers once; after completion it returns `alreadyCompleted` without a second stdout write.
    - Delivery failure is terminal only for its coordinator and produces zero successful stdout bytes; the peer remains waiting/operable and can later complete exactly once.
    - SIGINT/SIGTERM cancellation and listener shutdown apply only to their launch; the cancelled CLI exits with signal-derived nonzero status and empty stdout while its sibling remains alive.
    - Range and exact-patch CLI launches receive server-created distinct scopes even for byte-equivalent strict requests; neither request can select the scope.
  </behavior>
  <action>
Start from Task 1's committed GREEN seam and add regression evidence only; Task 2 may modify exactly the four files in its `<files>` list and no production file. In `draft.test.ts`, run one ordinary interactive app and one attached range app over the same OIDs: persist/export distinct feedback, capture exact interactive draft/export bytes, verify attached begins missing and uses `agent-*`, then prove attached mutation/Export/Finish/reveal leaves the interactive bytes and content unchanged.

In `attached-completion.test.ts`, compose two real authenticated session apps with equal provenance, different Task 1 scopes, separate coordinators, and separate delivery sinks. Gate A while B saves/exports/finishes; assert each status, response-settlement promise, persisted feedback, canonical returned bytes, and reveal path belongs only to its app. Cover both a range app and an exact-patch app across the matrix without duplicating the route harness.

In `attached-completion-coordinator.test.ts`, extend the existing `Promise.withResolvers()` gate pattern to two coordinator instances. Prove A's concurrent/duplicate Finish increments A's delivery once, A terminal delivery failure cannot transition B, and B can subsequently complete once. Assert coordinator state and delivery counts directly; do not introduce a new coordinator abstraction.

In `request.test.ts`, reuse `runOrdinaryAction()` dependency injection and `EventEmitter` signal sources to launch equivalent attached requests with independent stdout arrays and exit collectors. Capture the factory options to prove distinct `agent-*` scopes are created after strict request parsing and are absent from request input. Exercise success in one launch, delivery failure and signal cancellation in another, and duplicate Finish delivery attempts. Require failure stdout length zero, success exactly one unchanged canonical byte array, one shutdown/exit decision, and no peer liveness/state change.

Run the task command as an evidence gate. If any added coverage exposes a production defect, stop Task 2 without committing it, return to Task 1, revise only Task 1's named production sources at the existing composition/ownership seam, rerun Task 1's focused command, and amend the Task 1 commit before restarting Task 2. Do not make a conditional production edit in Task 2 or replace the already per-instance `AttachedCompletionCoordinator`, `createShutdownController()`, token, listener, stdout sink, or patch snapshot. Once the evidence passes, refactor only test duplication through existing local helpers.
  </action>
  <verify>
    <automated>./node_modules/.bin/vitest run --no-file-parallelism tests/api/draft.test.ts tests/api/attached-completion.test.ts tests/api/attached-completion-coordinator.test.ts tests/cli/request.test.ts</automated>
    <expected>After Task 1 is green, this test-only command passes attached-versus-interactive isolation, two-coordinator races, delivery failure, signals, duplicate Finish, exact output count, and request non-authority; any production failure routes back to Task 1 before Task 2 proceeds.</expected>
  </verify>
  <acceptance_criteria>
    - HAND-06 criterion 2 is proven with unchanged raw interactive files and unchanged interactive API content after attached Export and Finish.
    - HAND-06 criterion 3 is proven for concurrent success, terminal delivery failure, SIGINT/SIGTERM cancellation, and duplicate/concurrent Finish.
    - No successful path writes a prefix, partial document, delimiter, diagnostic, or second document; no failed path writes success bytes or exits 0.
    - Coordinators, browser/session apps, tokens, listeners, stdout sinks, shutdown controllers, and patch snapshots remain per invocation and are not redesigned.
  </acceptance_criteria>
  <done>The Wave 2 API/CLI evidence is green and every storage/lifecycle/output authority is demonstrably invocation-local without a Task 2 production change.</done>
  <commit>After evidence verification, create one atomic test-only commit containing exactly `tests/api/draft.test.ts`, `tests/api/attached-completion.test.ts`, `tests/api/attached-completion-coordinator.test.ts`, and `tests/cli/request.test.ts`: `test(15-01): prove attached lifecycle isolation`.</commit>
</task>

<task id="15-01-03" wave="3" depends_on="15-01-02" type="auto">
  <name>Task 3: Prove equivalent range and exact-patch isolation through the packaged Chromium path</name>
  <files>tests/e2e/agent-ready-export.spec.ts</files>
  <read_first>
    - Task 1's Wave 1 commit, Task 2's Wave 2 commit, and both green focused command outputs; do not begin this task unless the combined Wave 1-2 gate passes.
    - `.planning/phases/15-adversarial-integration-gate/15-RESEARCH.md`: scenarios S1–S3, observable assertions, complete-stdout rule, and exact existing harness list.
    - `tests/e2e/agent-ready-export.spec.ts`: `beforeAll()` package/extract setup; `startGeneratedCli()`, `startAttachedCli()`, `waitForAttachedLoopbackUrl()`, `waitForAttachedExit()`, `waitForLoopbackUrl()`, `stopGeneratedCli()`, `openSession()`, `ensureReviewOpen()`, `addHeadComment()`, `saveSummary()`, and the existing attached V2 test.
    - `tests/helpers/git-fixture.ts`: `createDirtyGitFixture()` and its real repository/worktree refs; reuse it rather than creating a new fixture framework.
    - `src/contracts/request.ts`: current strict revision and exact-patch request shapes; use them as input to the existing child stdin.
    - `src/export/review-export.ts`: `parseCanonicalReviewExport()` and canonical byte behavior; parse entire stdout files without trim or prefix extraction.
    - `src/server/lifecycle.ts`: expected SIGINT exit 130 and per-app cleanup behavior.
  </read_first>
  <behavior>
    - Two packaged attached range children receive equivalent revision requests and equal canonical V2 `reviewKey`, but distinct browser URLs/tokens, `agent-*` draft/export components, raw feedback, stdout files, and completion lifetimes.
    - A packaged interactive child and successful attached range child over the same revisions keep legacy and `agent-*` storage disjoint; attached starts empty, and its Export/Finish leaves the interactive rendered feedback, exact draft/export bytes, page, listener, and process unchanged.
    - While that same-revision interactive review remains live, signalling a separate same-revision attached child produces zero attached stdout and exit 130/nonzero without changing the interactive rendered-feedback snapshot, exact files, page, listener, or process.
    - Two packaged attached exact-patch children receive the same real already-applied patch and equal canonical V3 digest/`reviewKey`; their draft/export paths and feedback remain distinct.
    - Signalling exact-patch child A exits 130 with an empty stdout file and cleans only A's listener/snapshot; B stays live, its browser and files remain exact, then B exits 0 with one complete canonical V3 document.
    - Every successful stdout file equals the canonical serialization of its full parsed V2/V3 document, has no trailing newline or extra byte, and contains only that child's feedback; every failed stdout file is zero bytes.
  </behavior>
  <action>
After the sequential Wave 1 and Wave 2 gates pass, extend only the existing packaged-browser spec. Generalize `startAttachedCli()` to accept the existing strict request object rather than synthesizing revisions internally; retain its separate stdin/stdout/stderr descriptors, marker file, extracted `dist/bin/cumpa.mjs`, environment cleanup, and real loopback browser handoff. Do not add a second pack step, server launcher, browser fixture, fake UI, or process manager.

Add one bounded adversarial scenario group using the existing package created in `beforeAll()`:

1. Launch two attached range children with byte-equivalent revision input. Open separate Chromium pages, save distinct summaries/comments, invoke ordinary Export so durable export trees exist, and snapshot exact draft/export bytes. Finish A; assert A exits 0 with only A's complete canonical V2 bytes while B remains live with empty stdout and unchanged page/files. Finish B and assert the symmetric result. Their source `reviewKey` values must be equal; their `agent-*` components and feedback must differ.
2. Launch the existing packaged interactive path with `startGeneratedCli()` and an attached range child over the same revisions. Persist/export interactive feedback and snapshot its rendered feedback as exact UTF-8 bytes, exact draft JSON and export JSON/Markdown bytes, legacy path names, loopback address, and process state. Confirm the attached child begins empty/revision 0, save/export/Finish different feedback, then require its exit 0 and one complete canonical V2 stdout document. Revisit the retained interactive page and require its rendered-feedback snapshot and all file bytes/path names to remain byte-for-byte exact while its listener and process remain live. Next launch a separate same-revision attached child while retaining that interactive review, signal the attached child with SIGINT, and require zero attached stdout bytes plus exit 130/nonzero and closure of only the attached listener. Reassert byte-for-byte equality of the interactive rendered feedback and draft/export files, the same legacy names, a responsive page, and unchanged interactive listener/process liveness; exercise one existing read-only UI action after the signal so a stale screenshot cannot satisfy the proof.
3. Produce one real already-applied patch from `createDirtyGitFixture()` using the Git CLI already available to the harness, then launch two children with the same strict exact-patch request. Save/export distinct feedback and snapshot B. Send SIGINT to A and require exit 130, zero stdout bytes, and only A's loopback/snapshot cleanup. Prove B remains operable and byte-identical, then Finish B and require exit 0 with one canonical V3 document containing only B's feedback. Equal patch digest/`reviewKey` must coexist with different `agent-*` paths.

Read whole stdout files as `Buffer`; never trim, split, search for a JSON prefix, or accept a first parseable document. For each success, parse the entire bytes with `parseCanonicalReviewExport()`, re-canonicalize and require byte equality, assert no trailing newline, correct schema/source provenance/summary/comments, one exit, and no second write. For failure, require a zero-length file and nonzero/signal exit. Compare file contents/hashes and directory components, not mtimes.

Run the existing package command once for the owning spec, then the existing package-contract gate. Close every page, descriptor, child, and fixture through current helpers in `finally` paths so a failure cannot strand a process.
  </action>
  <verify>
    <automated>npm run test:package -- tests/e2e/agent-ready-export.spec.ts &amp;&amp; npm run test:package-contract</automated>
    <expected>During implementation only: the extracted package and Chromium scenarios pass for equivalent ranges, attached-success interactive coexistence, signalled-failure interactive coexistence, equivalent exact patches, SIGINT isolation, exact durable/rendered bytes, complete one-document success stdout, zero-byte failure stdout, and package contents.</expected>
  </verify>
  <acceptance_criteria>
    - HAND-06 and roadmap criteria 1–3 pass through the actual packed CLI, loopback Fastify servers, Chromium UI, repository-local files, stdout descriptors, and child exits.
    - Both range and exact-patch equivalent submissions retain equal deterministic provenance while their attached mutable ownership is distinct.
    - The interactive legacy identity, rendered feedback, exact files, page, listener, and process remain unchanged after both attached Export/Finish success and a same-revision attached child's signalled failure.
    - No success is partial or duplicate; no failure is mistaken for success; all resources are closed by the owning invocation.
  </acceptance_criteria>
  <done>The Wave 3 packaged adversarial gate is green and supplies final observable evidence for every Phase 15 criterion.</done>
  <commit>After both evidence commands pass, create one atomic commit containing this task only: `test(15-01): add packaged adversarial integration gate`.</commit>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Strict stdin request → CLI launcher | Untrusted revision/patch input is validated before the trusted process allocates an attached storage scope; input has no namespace authority. |
| CLI launcher → session app | One trusted scope, token, coordinator, listener, stdout sink, and shutdown controller are composed for exactly one invocation. |
| Session capability → draft/export filesystem | Mutable state crosses into `.compare`; validated attached names must be structurally disjoint from deterministic interactive and source-provenance identities. |
| Canonical source identity → mutable storage identity | Deterministic `reviewKey` proves reviewed source; `agent-*` proves invocation ownership. Neither may substitute for the other. |
| Browser/Fastify → coordinator → stdout | Authenticated Finish may irreversibly deliver one canonical document; duplicate, partial, failed, or cancelled delivery must not cross sessions. |
| One child/app → concurrent child/app | Signals, listener close, snapshot disposal, response settlement, durable mutation, and exit status must remain instance-local. |
| Extracted npm package → Chromium/repository | Final evidence must exercise shipped files and real browser/filesystem behavior, not a mock implementation. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan / Evidence |
|---|---|---|---|---|---|
| T-15-01 | Tampering / Information Disclosure | Cross-attached draft/export storage collision | HIGH | mitigate | Task 1 creates one validated `agent-*` scope per launch; API and packaged range/patch tests require distinct exact files with equal source provenance. |
| T-15-02 | Tampering / Repudiation | Canonical provenance drift | HIGH | mitigate | Storage override never changes `comparisonKey`, range/patch `reviewKey`, `DraftComparison`, `ExportPublicationIdentity`, or V2/V3 validation; tests require equal keys for equivalent inputs. |
| T-15-03 | Elevation of Privilege / Tampering | Interactive namespace takeover or path traversal | HIGH | mitigate | No client field; fixed prefix plus lowercase-hex grammar; invalid presence fails before `join()`; attached-versus-interactive raw-byte tests prove no resume/overwrite/reveal/publish/recovery. |
| T-15-04 | Denial of Service / Tampering | Cross-coordinator settlement, signal, or shutdown | HIGH | mitigate | Existing coordinator/controller remain per launch; Task 2 gates two instances and Task 3 proves one child can finish, fail, or signal without changing an attached or interactive peer's liveness, exact files, browser, listener, or process. |
| T-15-05 | Tampering / Repudiation | Duplicate or partial stdout accepted as success | HIGH | mitigate | One-delivery counter, awaited response settlement, whole-file canonical byte equality, no trailing byte, zero-byte failure, and explicit exit assertions cover duplicate/concurrent Finish and signals. |
| T-15-SC | Tampering | Supply chain | LOW | accept | No package install or dependency change; reuse Node crypto/filesystem/process APIs, Fastify, Vitest, Playwright, and the existing packed-browser harness. |

ASVS references: V3 Session Management for per-invocation token/app/coordinator/scope/listener/controller; V4 Access Control for server-only storage authority and authenticated reveal/Finish; V5 Validation for strict request and storage-name grammar; V6 Cryptography for Node `randomBytes()` rather than client input or custom randomness. Every HIGH threat requires the applicable focused Wave 1-2 evidence and packaged Wave 3 assertion; none may be accepted, deferred, or transferred.
</threat_model>

<verification>
Execution-time order only; none of these commands runs during planning:

1. Task 1 RED then GREEN: `./node_modules/.bin/vitest run --no-file-parallelism tests/api/draft.test.ts tests/api/exact-patch.test.ts tests/api/export-publication.test.ts`.
2. Task 2 test-only regression evidence after Task 1 GREEN: `./node_modules/.bin/vitest run --no-file-parallelism tests/api/draft.test.ts tests/api/attached-completion.test.ts tests/api/attached-completion-coordinator.test.ts tests/cli/request.test.ts`.
3. Wave 2 combined gate after Tasks 1 and 2: `./node_modules/.bin/vitest run --no-file-parallelism tests/api/draft.test.ts tests/api/exact-patch.test.ts tests/api/export-publication.test.ts tests/api/attached-completion.test.ts tests/api/attached-completion-coordinator.test.ts tests/cli/request.test.ts`.
4. Only after step 3 passes, Wave 3 packaged evidence: `npm run test:package -- tests/e2e/agent-ready-export.spec.ts`.
5. Package contents contract after packaged behavior passes: `npm run test:package-contract`.
6. Inspect evidence, not source text: equal source keys plus distinct `agent-*` paths; interactive rendered feedback and exact draft/export bytes unchanged after attached success and signalled failure; interactive page/listener/process liveness; one complete canonical success file/no newline; zero-byte failed file; correct 0/130/nonzero exits; sibling liveness; one-delivery duplicate Finish; no HIGH threat without its named assertion.
</verification>

<success_criteria>
- HAND-06 is green in focused API/CLI tests and the packaged Chromium gate.
- Criterion 1: equivalent range and exact-patch requests have equal deterministic `reviewKey` provenance, distinct server-created attached storage scopes, distinct draft/export bytes, and feedback returned only to the owning child.
- Criterion 2: same-revision attached work starts empty and cannot alter, reveal, publish, or recover through the interactive identity; legacy interactive paths and exact bytes remain unchanged.
- Criterion 3: completing, delivery-failing, or signalling A leaves B and interactive state unchanged; concurrent/duplicate Finish delivers once; failure stdout is empty; successful stdout is exactly one canonical V2/V3 document with no extra byte.
- Draft path, queue key, temporary files, export publication, reveal, and recovery all consume the same invocation scope; no client input can choose it.
- Existing interactive storage, request/API/browser contracts, canonical schemas, source provenance, security, atomicity, revision fencing, and response-settlement ordering remain compatible.
- No new dependency, infrastructure, schema version, migration, alternate serializer, or parallel harness exists.
- All three task commits are atomic and ordered by strict dependency: Wave 1 Task 1, Wave 2 Task 2, then Wave 3 Task 3; the executor creates no commit before its task verification is green.
- Every HIGH threat T-15-01 through T-15-05 has focused and end-to-end observable evidence; no blocker, warning, or open question remains.
</success_criteria>

<output>
Create `.planning/phases/15-adversarial-integration-gate/15-01-SUMMARY.md` when execution finishes, including Task 1 RED/GREEN outcomes, Task 2/3 evidence command outcomes, task commit hashes, exact packaged evidence, criteria/threat disposition, and any deviation. Planning itself creates no summary and makes no commit.
</output>

## PLANNING COMPLETE
