---
phase: 11-production-performance-gate
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - tests/performance/production-picker.mjs
  - package.json
  - src/cli/run.ts
  - src/git/repository.ts
  - src/git/candidates.ts
  - src/cli/picker.ts
autonomous: true
requirements:
  - PERF-01
  - PERF-02
must_haves:
  truths:
    - "PERF-01: across five serial measured production processes after one discarded warmup, median time from the parent immediately before spawning dist/bin/cumpa.mjs until the exact rendered eager current-branch row is at most 400 ms."
    - "PERF-02: in those same five production processes, median time from the parent immediately before writing branch-09999 until the exact rendered matching branch row is at most 500 ms."
    - "The gate proves a fresh fixture has exactly 10,000 local refs/heads entries, all represented in packed-refs and none remaining as loose refs/heads files, before any warmup or measured process starts."
    - "Every sample traverses the compiled dist/bin/cumpa.mjs Commander, repository discovery, staged source discovery, real @inquirer/search renderer, native Git search, and rendered candidate row without injected CLI, picker, or Git seams."
    - "A failed budget identifies its segment with raw unrounded samples and bounded diagnostics; production code changes only after measured diagnosis, with zero edits when both budgets pass and at most one smallest correction for each independently proven failing root cause, then the unchanged gate passes both fixed budgets."
    - "Task 1 machine-classifies PASS, valid budget-only RED, and fatal infrastructure/protocol/lifecycle failure; only PASS or a RED backed by the proven fixture, one completed warmup, five completed measured invocations, and successful cleanup advances to Task 2."
    - "The repository's existing Node 24 CI runner owns npm run test:performance as a separate serial acceptance job and records Node/Git/platform/CPU/load/CI metadata with every result."
  artifacts:
    - path: "tests/performance/production-picker.mjs"
      provides: "Self-contained black-box packed-ref fixture, production child driver, parent-clock measurements, deterministic cleanup, raw diagnostics, and absolute median assertions"
    - path: "package.json"
      provides: "test:performance command that builds and runs the compiled production picker gate"
      contains: "npm run build:runtime && node tests/performance/production-picker.mjs"
    - path: "dist/bin/cumpa.mjs"
      provides: "Generated production executable actually spawned by every warmup and measured invocation"
    - path: "src/cli/run.ts"
      provides: "Conditional PERF-01 correction surface; may be one member of the minimum correction set only when raw stage evidence identifies a distinct root cause in top-level imports, run(), or runCli()"
    - path: "src/git/repository.ts"
      provides: "Conditional PERF-01 correction surface; may be one member of the minimum correction set only when raw stage evidence identifies a distinct root cause in discoverGitRepository() or probeMachineProtocols()"
    - path: "src/git/candidates.ts"
      provides: "Conditional PERF-01/PERF-02 correction surface; may be one member of the minimum correction set only when raw stage evidence identifies a distinct discovery or search root cause"
    - path: "src/cli/picker.ts"
      provides: "Conditional PERF-02 correction surface; may be one member of the minimum correction set only when raw stage evidence identifies a distinct source/render-handoff root cause"
  key_links:
    - from: "package.json"
      to: "tests/performance/production-picker.mjs"
      via: "test:performance first runs build:runtime and then the standalone gate"
      pattern: "test:performance.*build:runtime.*production-picker\\.mjs"
    - from: "tests/performance/production-picker.mjs"
      to: "dist/bin/cumpa.mjs"
      via: "spawn(process.execPath, [absoluteExecutablePath]) with cwd set to the disposable repository, shell:false, and no launch bypass environment"
      pattern: "spawn\\(process\\.execPath"
    - from: "dist/bin/cumpa.mjs"
      to: "src/cli/run.ts"
      via: "generated package bin imports the compiled production run() entry"
      pattern: "run"
    - from: "src/cli/run.ts"
      to: "src/git/candidates.ts"
      via: "runCli() creates the real SourceDiscovery before invoking ordered selection"
      pattern: "discoverSourceCandidates"
    - from: "src/cli/picker.ts"
      to: "src/git/candidates.ts"
      via: "sourceForPrompt forwards the entered term to SourceDiscovery.searchBranches and renders candidateItem().name"
      pattern: "searchBranches"
---

<objective>
Prove PERF-01 and PERF-02 through the shipped compiled picker in a proven 10,000-packed-local-branch repository, and make the smallest measured production correction only if that immutable gate first fails.

Purpose: Close v1.2 with objective user-visible timing evidence rather than injected unit seams or spike measurements. The gate is the specification: one discarded warmup, five serial fresh processes, exact rendered-row stops, parent monotonic clocks, fixed absolute medians, and raw bounded diagnostics.
Output: One standalone production performance harness, one package command, passing raw evidence for both budgets, and the minimum evidence-backed production correction set—zero edits on initial pass, otherwise at most one smallest correction per independently proven failing root cause.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/11-production-performance-gate/11-CONTEXT.md
@.planning/phases/11-production-performance-gate/11-RESEARCH.md
@.planning/phases/11-production-performance-gate/11-PATTERNS.md
@.planning/phases/09-immediate-source-picker/09-VERIFICATION.md
@.planning/phases/10-on-demand-branch-search/10-01-SUMMARY.md
@.planning/phases/10-on-demand-branch-search/10-VERIFICATION.md
@package.json
@scripts/build-bin.mjs
@src/cli/run.ts
@src/cli/picker.ts
@src/git/repository.ts
@src/git/candidates.ts
@tests/cli/help.test.ts
@tests/cli/selection.test.ts
@tests/cli/errors.test.ts
@tests/git/candidates.test.ts
@.planning/spikes/001-large-repo-startup-baseline/support.mjs

<interfaces>
- The only acceptance entry is repository-root `dist/bin/cumpa.mjs`, spawned as `spawn(process.execPath, [executablePath], { cwd: fixtureRepository, env, shell: false, stdio: ['pipe', 'pipe', 'pipe'] })`.
- The production flow remains `dist/bin/cumpa.mjs` → Commander `run()` → `runCli()` → `discoverSourceCandidates()` → `pickOrderedSources()` → `sourceForPrompt()` → `SourceDiscovery.searchBranches()` → real Inquirer rendering.
- The exact readiness marker is `[Branch] main · ${mainShortOid}`. The exact entered-term marker is `[Branch] branch-09999 · ${targetShortOid}`. Compute both abbreviations from the fixture through native Git before measurement.
- The package command is exactly `"test:performance": "npm run build:runtime && node tests/performance/production-picker.mjs"`; add no benchmark, terminal, statistics, Git, or profiling dependency.
- Conditional production surfaces are limited to the pattern/research map: `src/cli/run.ts` (`run`, `runCli`, top-level imports), `src/git/repository.ts` (`discoverGitRepository`, `probeMachineProtocols`), `src/git/candidates.ts` (`discoverSourceCandidates`, `searchBranches`), and `src/cli/picker.ts` (`sourceForPrompt`, `mergedCandidates`).
</interfaces>
</context>

<feature>
  <name>Black-box production picker performance budgets</name>
  <files>tests/performance/production-picker.mjs, package.json, and only conditionally the four mapped production source files</files>
  <behavior>
    - A new disposable repository contains exactly `main` plus `branch-00001` through `branch-09999`, for exactly 10,000 local branches. Native Git logical enumeration, direct packed-refs parsing, and recursive loose-head inspection independently agree on the exact expected set before timing.
    - One complete process is discarded as warmup. Five complete measured processes run serially against the unchanged packed fixture; no retry, outlier deletion, adaptive sample count, concurrency, or platform multiplier is allowed.
    - PERF-01 starts with parent `performance.now()` on the statement immediately before `spawn()` and ends at the first exact decoded readiness row. PERF-02 starts with parent `performance.now()` immediately before one `child.stdin.write('branch-09999')` and ends at the first exact decoded target row; do not append Enter.
    - Each marker has a 10,000 ms operational watchdog distinct from the 400/500 ms acceptance budgets. Early exit, spawn error, missing marker, or cleanup failure fails the invocation.
    - Both five-value arrays are retained unrounded. Median is the middle value of a numerically sorted copy. Only displayed values may be rounded; acceptance compares raw medians to `<= 400` and `<= 500`.
    - An initial budget failure is RED evidence. Diagnose each failing segment on the same compiled child path, remove all temporary measurement probes, and retain no production edit unless raw stage evidence maps it to an independently proven root cause. The correction set is empty when both budgets pass and may contain one smallest correction per distinct failing root cause when needed; the unchanged gate must then pass both budgets.
  </behavior>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: GATE FIRST — build the black-box harness and capture the initial RED-or-pass result</name>
  <files>tests/performance/production-picker.mjs, package.json</files>
  <read_first>
    - `.planning/phases/11-production-performance-gate/11-RESEARCH.md` — implement its fixture proof, exact markers, one-plus-five sampling, watchdog, diagnostic, and absolute-budget contracts without weakening them.
    - `.planning/phases/11-production-performance-gate/11-PATTERNS.md` — reuse the one-stream fast-import and fixed argument-array process patterns; do not import either spike into acceptance.
    - `package.json` and `scripts/build-bin.mjs` — preserve existing scripts and use the generated package bin path.
    - `src/cli/picker.ts` — confirm candidateItem() display text used by the exact rendered markers; do not add a readiness hook.
    - `src/cli/run.ts` — confirm and remove inherited launch-bypass variables from the child environment.
  </read_first>
  <action>
Create `tests/performance/production-picker.mjs` using Node 24 standard-library modules only. Resolve repository root and `dist/bin/cumpa.mjs` to absolute paths and fail before fixture work if the executable is absent after the package build. Create a unique harness-owned root with `mkdtemp(join(tmpdir(), 'compare-production-picker-'))`; initialize one repository with fixed local identity and one commit on `main`. Use one native `git fast-import` stdin stream to create `branch-00001` through `branch-09999` at the fixture commit, then run `git pack-refs --all` before warmup. Every Git/setup invocation must use an executable plus argument array, `shell: false`, non-interactive Git environment, drained bounded output, a finite setup timeout, and checked exit/signal.

Before spawning Compare, construct the exact expected full-ref set and prove all three invariants: `git for-each-ref --format=%(refname) refs/heads` returns exactly those 10,000 unique refs; recursive inspection of `<git-common-dir>/refs/heads` finds zero files, treating an absent directory as zero; and parsing `<git-common-dir>/packed-refs` while ignoring comments, blank lines, and peeled records yields exactly the same 10,000 refs. Abort before timing on any mismatch. Resolve `mainShortOid` and `targetShortOid` through Git for the exact row strings.

Implement one child driver reused by warmup and samples. Build a sanitized copy of the environment that explicitly deletes `COMPARE_LAUNCH_OPTIONS` and `CMUX_WORKSPACE_ID`, sets deterministic terminal dimensions and no-color flags plus Git non-interactive flags, but preserves the real `PATH` and never substitutes Git or picker dependencies. Attach stdout/stderr listeners immediately. Incrementally decode stdout with `StringDecoder('utf8')`, retain a rolling tail of at most 64 KiB per stream, and match only the literal contiguous rendered marker strings; do not strip ANSI/control bytes into a potentially forged marker. Fixture labels are fixed ASCII and color is disabled. Start the readiness parent clock on the line immediately before spawning `process.execPath` with `[absoluteExecutablePath]`. After the readiness row, start the search clock immediately before exactly one `stdin.write('branch-09999')`, with no Enter, and stop on the exact target row.

Use independent 10-second marker watchdogs. On success, terminate the still-open prompt, await close for a short fixed cleanup interval, escalate to `SIGKILL` if needed, and await final close. Apply the same termination path for spawn error, early exit, or watchdog failure. Run one complete discarded warmup and then exactly five complete samples serially. Use `try/finally` so every child is settled and only the captured harness-owned temp root is recursively removed.

Emit one structured record per raw sample and one terminal line prefixed `COMPARE_PERF_RESULT=` whose JSON payload has `classification` (`PASS`, `BUDGET_RED`, or `HARD_FAILURE`), fixture logical/packed/loose counts plus exact-set booleans, completed warmup/measured counts, both raw sample arrays and requirement medians/budgets/pass flags, child/temp cleanup booleans, Node/Git/platform/arch/CPU/load/CI metadata, and sanitized environment facts. Emit the terminal classification only after child settlement and temp cleanup are known. Classify any fixture/setup/build/spawn error, early exit, marker/protocol/watchdog failure, incomplete one-plus-five invocation set, child-settlement failure, or temp-cleanup failure as `HARD_FAILURE` and exit 1. Classify only a fully proven, fully sampled, fully cleaned run whose raw median exceeds either fixed budget as `BUDGET_RED` and exit 2 by default; classify a fully valid run within both budgets as `PASS` and exit 0. Support `--accept-budget-red` solely for Task 1 verification: it changes the process status for a valid `BUDGET_RED` from 2 to 0 while retaining that JSON classification, but it must never convert `HARD_FAILURE` to success. Add exactly `"test:performance": "npm run build:runtime && node tests/performance/production-picker.mjs"` to `package.json` without changing other scripts or metadata.

Run `npm run test:performance -- --accept-budget-red` once in the repository's existing Node 24 CI runner as a separate serial job with no project tests co-scheduled, and preserve its complete raw output for the SUMMARY. The command may return success only for `PASS` or a machine-validated `BUDGET_RED`; the latter is the required RED checkpoint for Task 2, not permission to alter thresholds or the harness. Any `HARD_FAILURE`, missing/unparseable terminal record, unproven fixture, incomplete warmup or measured set, or failed lifecycle/cleanup remains fatal and blocks Task 2.
  </action>
  <verify>
    <automated>npm run test:performance -- --accept-budget-red</automated>
    <expected>Exit 0 with a parseable `COMPARE_PERF_RESULT=` classification of either `PASS`, or `BUDGET_RED` only after exact fixture proof, one completed discarded warmup, five completed serial measured invocations, and successful child/temp cleanup. Exit nonzero for `HARD_FAILURE`, missing/unparseable classification, setup/build/spawn failure, early exit, marker/protocol/watchdog failure, incomplete invocation counts, or lifecycle/cleanup failure.</expected>
  </verify>
  <done>The first-run checkpoint advances only on a valid pass or a machine-proven budget-only RED; every infrastructure, protocol, marker, lifecycle, and cleanup failure remains fatal, and the permanent default gate still exits nonzero for either failed budget.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED→GREEN — conditionally diagnose and remove only independently measured production root causes</name>
  <files>src/cli/run.ts, src/git/repository.ts, src/git/candidates.ts, src/cli/picker.ts</files>
  <read_first>
    - Task 1 raw report — identify whether PERF-01, PERF-02, or both failed; do not infer a bottleneck from spike numbers.
    - `.planning/phases/11-production-performance-gate/11-PATTERNS.md` — use only its mapped conditional symbols and preserve the production flow.
    - `src/cli/run.ts`, `src/git/repository.ts`, `src/git/candidates.ts`, `src/cli/picker.ts` — trace the failing segment before changing it.
    - `tests/cli/help.test.ts`, `tests/cli/selection.test.ts`, `tests/cli/errors.test.ts`, `tests/git/candidates.test.ts` — preserve existing CLI, staged picker, error, Git protocol, selection, cancellation, and recovery contracts.
  </read_first>
  <action>
Branch only on Task 1's terminal classification. `PASS` requires zero production edits and proceeds directly to Task 3. `BUDGET_RED` permits diagnosis only for the requirement segments whose raw medians failed. `HARD_FAILURE` or any absent/inconsistent classification must already have stopped Task 1. Keep `tests/performance/production-picker.mjs`, its fixture, sample policy, markers, classifications, and budgets byte-for-byte unchanged for the remainder of the plan.

For a PERF-01 failure, measure the compiled child around the mapped startup boundaries only: `src/cli/run.ts` top-level import/startup, `run()`, and `runCli()`; `src/git/repository.ts` `discoverGitRepository()` and `probeMachineProtocols()`; and `src/git/candidates.ts` `discoverSourceCandidates()`. For a PERF-02 failure, measure only `src/cli/picker.ts` `sourceForPrompt()`/`mergedCandidates()` and `src/git/candidates.ts` `searchBranches()` including its filtered listing, abbreviation batch, and render handoff. If both fail, diagnose the two intervals separately. Use temporary monotonic duration probes that emit stage names and raw durations to stderr through the same compiled command, run exactly `npm run test:performance` to collect them, and remove every temporary probe before the lasting edit. The diagnostic run is evidence only and cannot satisfy acceptance.

Write each diagnosis and its raw stage measurements into the eventual SUMMARY before changing production behavior. Partition the evidence into distinct failing root causes: if both requirement failures share one measured cause, make one correction; if evidence proves independent causes, permit at most one smallest correction for each; never force a production edit merely because a segment failed. Every retained edit must name its mapped production symbol and raw before/after stage evidence, and any edit lacking that mapping must be reverted. Preserve Commander and real Inquirer, required Git/version/protocol validation, exact-ID authority, cancellation, literal local-branch semantics, terminal escaping, Base/Head order, and fresh recovery. A readiness correction may defer only genuinely post-selection work or safely reduce/parallelize measured independent prerequisite work. A search correction may remove only measured redundant work in the existing filtered/two-process path. Do not add a cache, persistent index, background enumeration, debounce, fuzzy search, automatic packing, remote search, cap, new abstraction, new exported interface, dependency, benchmark-only production hook, or scaled budget.

Rebuild only through the unchanged default `npm run test:performance` package command and iterate per evidenced root cause until both raw medians pass. The original `BUDGET_RED` output is RED; the unchanged default gate exiting 0 is GREEN. Revert every proposed edit that does not improve its mapped failing stage or is not needed once the complete minimum correction set is evaluated; do not accumulate speculative or duplicate optimizations.
  </action>
  <verify>
    <automated>npm run test:performance</automated>
    <expected>The unchanged default gate exits 0 with both raw medians inside the fixed budgets. A Task 1 `PASS` leaves all four conditional source files untouched. A Task 1 `BUDGET_RED` leaves only the minimum correction set: at most one smallest edit per independently proven root cause, every retained edit mapped to raw stage evidence, and no diagnostic probe remaining.</expected>
  </verify>
  <done>Both PERF segments are GREEN through the same compiled rendered-row gate, with zero production changes on initial pass or the smallest independently justified correction set when needed; unsupported, duplicate, speculative, and diagnostic edits are absent.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: REFACTOR/PROOF — preserve behavior and rerun the unchanged final gate</name>
  <files>tests/performance/production-picker.mjs, package.json, src/cli/run.ts, src/git/repository.ts, src/git/candidates.ts, src/cli/picker.ts</files>
  <read_first>
    - Task 1 and Task 2 raw output — retain initial and final evidence and confirm the gate contract did not change after RED.
    - The actual diff — remove temporary diagnostic code and unnecessary production edits; do not refactor a passing no-source-change result.
  </read_first>
  <action>
If Task 2 changed production source, run the focused existing regression command exactly as listed below and fix any regression at the same root-cause surface without weakening existing behavior. If no production source changed, the focused command still provides non-regression evidence for the shipped staged picker. In both cases, require `tests/git/comparison.test.ts` Git version/protocol prerequisite assertions to pass before the production performance gate starts. Do not add tests for implementation details; the standalone gate is the new observable performance contract.

Then audit the actual diff against the raw stage report: zero production edits are allowed after an initial `PASS`; after `BUDGET_RED`, every retained production edit must map to one distinct failing root cause and its before/after stage evidence, with no more than one smallest correction per cause. Remove any unsupported, duplicate, probe-only, or no-longer-needed edit. Run the default `npm run test:performance` one final time without changing the harness after Task 1. Preserve the complete output. Require terminal classification `PASS`, exactly five raw measured readiness values and five raw search values after one discarded warmup, fixture logical/packed counts of 10,000 and loose count zero, both raw medians within fixed budgets, compiled executable identity, complete Node/Git/platform/CPU/load/CI metadata from the existing Node 24 runner, and successful child/temp cleanup. Confirm no launch bypass variable, injected dependency, source import, spike path, retry, outlier removal, concurrent sample, platform multiplier, or loose-ref acceptance can produce success.
  </action>
  <verify>
    <automated>npm exec -- vitest run tests/git/candidates.test.ts tests/git/comparison.test.ts tests/cli/selection.test.ts tests/cli/errors.test.ts tests/cli/help.test.ts &amp;&amp; npm run test:performance</automated>
    <expected>The focused staged-picker regressions pass, including `tests/git/comparison.test.ts` Git version/protocol prerequisite assertions, before the unchanged default compiled-production gate starts and exits 0 with classification `PASS`, exact fixture/invocation/cleanup proofs, and both fixed raw median budgets passing; the retained diff is empty on initial pass or contains only independently evidenced minimum corrections.</expected>
  </verify>
  <done>Final objective evidence proves the Git version/protocol prerequisites remain valid before PERF-01 and PERF-02 pass through dist/bin/cumpa.mjs, all existing picker contracts remain green, all children/temp paths are cleaned, and every retained production edit is the smallest correction for one independently proven root cause.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Harness → Git/Node subprocesses | Commands and repository paths cross an OS execution boundary and must remain fixed executable-plus-argv calls with no shell interpolation. |
| Harness-owned temp root → filesystem cleanup/packing | `pack-refs` and recursive removal are destructive operations permitted only inside the exact temp root created by this process. |
| Child terminal bytes → marker parser/diagnostics | Stdout/stderr can contain ANSI, control bytes, invalid split UTF-8, and repository-derived text; only exact decoded safe-ASCII row markers establish timing, while diagnostics must not replay active terminal controls. |
| Parent timers → child-rendered outcomes | Timing authority stays in one parent monotonic clock; child timestamps, prompt titles, Git completion, injected callbacks, and echoed input are not acceptance boundaries. |
| Parent → long-lived interactive child | The real prompt remains open after the two markers, so watchdog, signal escalation, stream draining, and awaited close are required to prevent hangs or leaked processes. |
| Captured process output → CI logs/SUMMARY | Diagnostic evidence must retain raw numeric samples and useful tails without allowing unbounded memory/log growth or terminal-control spoofing. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering / Elevation of Privilege | Git and Node invocation | mitigate | Use fixed executables and argument arrays with `shell: false`; pass fast-import data and picker term through stdin; never concatenate a shell command. Verify the child argv targets the absolute `dist/bin/cumpa.mjs`. |
| T-11-02 | Tampering / Denial of Service | Fixture packing and cleanup | mitigate | Create the repository beneath one `mkdtemp` root captured by the harness; allow `pack-refs` only with that repository as cwd and recursive removal only for that captured root in `finally`. Never pack or remove the caller's repository. |
| T-11-03 | Spoofing / Tampering / Information Disclosure | Terminal marker and diagnostic parsing | mitigate | Generate only fixed ASCII branch labels, disable color, incrementally decode with `StringDecoder`, and require literal contiguous complete row markers without ANSI stripping. JSON-escape bounded stdout/stderr tails before emission so hostile control bytes cannot forge a marker or execute in logs. |
| T-11-04 | Denial of Service / Repudiation | Child lifecycle | mitigate | Attach/drain both pipes immediately; use independent 10-second marker watchdogs; treat spawn error/early exit as failure; terminate after markers, await close, escalate to `SIGKILL` after a fixed grace period, and await final settlement before the next serial sample or temp cleanup. |
| T-11-05 | Denial of Service / Information Disclosure | Diagnostics | mitigate | Retain at most 64 KiB tail per stream, emit all raw numeric samples and bounded escaped tails plus phase/exit/environment facts, never unbounded child output or repository contents. |
| T-11-06 | Tampering | Picker bypass/environment | mitigate | Delete `COMPARE_LAUNCH_OPTIONS` and `CMUX_WORKSPACE_ID` from the child environment, preserve real PATH/Git, and reject any run that reaches session launch before both picker markers. |
| T-11-07 | Tampering / Repudiation | Performance acceptance | mitigate | Keep one discarded warmup and exactly five serial samples; compare unrounded medians to fixed 400/500 ms values; prohibit retries, dropped outliers, concurrency, adaptive counts, platform scaling, and post-RED gate edits. |
| T-11-SC | Tampering | Package supply chain | accept | No package install or dependency change occurs; the harness uses Node built-ins, installed Git, the current lockfile, and the existing production picker dependencies. |
</threat_model>

## Artifacts this phase produces

| Path / output | New or conditional artifact | Contract |
|---------------|-----------------------------|----------|
| `tests/performance/production-picker.mjs` | New permanent acceptance harness | Creates and proves exactly 10,000 packed local branches with zero loose heads; drives one warmup plus five serial compiled-production children; measures exact rendered rows with parent monotonic clocks; enforces fixed raw medians; cleans up deterministically; emits bounded raw diagnostics. |
| `package.json` | New `test:performance` script only | Runs `npm run build:runtime && node tests/performance/production-picker.mjs`; no dependency or unrelated metadata change. |
| `dist/bin/cumpa.mjs` | Generated, executed artifact | Built by `build:runtime` and spawned as the only acceptance product entry; not replaced by source imports, injected seams, or spikes. |
| Command output from `npm run test:performance` | Objective execution evidence | Records machine-readable PASS/BUDGET_RED/HARD_FAILURE classification, fixture proofs, one-plus-five completion, raw values and aggregates, fixed budgets, host metadata, exact compiled path, lifecycle/cleanup status, and bounded diagnostics. |
| `src/cli/run.ts` | Conditional existing production surface | Untouched on initial pass; otherwise changed at most once as part of the minimum correction set when raw startup-stage evidence proves a distinct cause in imports, `run()`, or `runCli()`. |
| `src/git/repository.ts` | Conditional existing production surface | Untouched on initial pass; otherwise changed at most once as part of the minimum correction set when raw startup-stage evidence proves a distinct cause in `discoverGitRepository()` or `probeMachineProtocols()`. |
| `src/git/candidates.ts` | Conditional existing production surface | Untouched on initial pass; otherwise changed at most once as part of the minimum correction set when raw discovery/search-stage evidence proves a distinct cause in `discoverSourceCandidates()` or `searchBranches()`. |
| `src/cli/picker.ts` | Conditional existing production surface | Untouched on initial pass; otherwise changed at most once as part of the minimum correction set when raw search/render-stage evidence proves a distinct cause in `sourceForPrompt()` or `mergedCandidates()`. |

No new exported symbol, dependency, benchmark framework, PTY wrapper, Git abstraction, cache, index, background service, production timing hook, fixture helper module, threshold configuration, or spike-based acceptance artifact is created.

## Multi-Source Coverage Audit

| Source | ID | Feature / constraint | Plan | Status | Notes |
|--------|----|----------------------|------|--------|-------|
| GOAL | — | Fast readiness and branch-search results through the same production path in a packed 10,000-branch repository. | 11-01 | COVERED | The compiled executable and exact rendered outcomes are the only acceptance path. |
| REQ | PERF-01 | Picker usable within 400 ms of process start. | 11-01 | COVERED | Parent-before-spawn clock, exact eager row, one warmup, five serial raw samples, fixed median. |
| REQ | PERF-02 | Matching local branch result within 500 ms of entering a term. | 11-01 | COVERED | Parent-before-write clock, fixed term, exact target row, same five children, fixed median. |
| RESEARCH | — | Fresh fixture has exactly 10,000 packed local refs and no loose heads. | 11-01 | COVERED | Exact expected-set comparison across Git enumeration, packed-refs, and recursive loose storage. |
| RESEARCH | — | Production-only compiled Commander/Inquirer/native-Git path; no injection or spike acceptance. | 11-01 | COVERED | Absolute dist/bin spawn, sanitized bypass environment, real streams and rendered row markers. |
| RESEARCH | — | One discarded warmup plus five serial processes and absolute raw medians. | 11-01 | COVERED | Harness hard-codes the sample policy and reports every value without retry/scaling. |
| RESEARCH | — | Deterministic watchdog, signal cleanup, bounded diagnostics, and host metadata. | 11-01 | COVERED | Per-marker watchdogs, awaited TERM/KILL close, 64 KiB tails, finally cleanup, JSON evidence. |
| RESEARCH | — | Gate-first YAGNI and measured conditional optimization. | 11-01 | COVERED | Task 1 measures first; Task 2 touches only a failing mapped segment; Task 3 reruns the unchanged gate. |
| CONTEXT | — | Users receive both fast outcomes through the same production path; no numbered locked or deferred decisions. | 11-01 | COVERED | Plan implements the boundary directly and adds no deferred/future requirement. |

<verification>
1. On the repository's existing Node 24 CI runner, run `npm run test:performance -- --accept-budget-red` alone in a serial job; preserve the complete first-run output and require exit 0 with machine-readable `PASS` or valid budget-only `BUDGET_RED`, while every `HARD_FAILURE` remains nonzero and blocks execution.
2. Confirm setup evidence reports logical refs `10000`, packed local refs `10000`, loose local-head files `0`, and exact expected-set equality before warmup.
3. Confirm every invocation names the absolute `dist/bin/cumpa.mjs` path and sanitized environment, with no source import, `runCli` dependency injection, prompt fake, Git wrapper, spike script, or launch bypass.
4. Confirm the report contains one explicitly discarded complete warmup and exactly five serial raw readiness/search sample pairs, with no retry, omission, concurrent child, or threshold scaling.
5. Confirm PERF-01 starts immediately before each spawn and stops at `[Branch] main · ${mainShortOid}`; PERF-02 starts immediately before writing `branch-09999` and stops at `[Branch] branch-09999 · ${targetShortOid}`.
6. If the baseline is `BUDGET_RED`, retain raw segment/stage diagnosis, allow at most one smallest correction for each independently proven root cause, record the evidence mapping for every retained edit, remove every probe/unsupported edit, and run the unchanged default `npm run test:performance` until both budgets pass. If it is `PASS`, confirm all four conditional production files remain unchanged.
7. Run `npm exec -- vitest run tests/git/candidates.test.ts tests/git/comparison.test.ts tests/cli/selection.test.ts tests/cli/errors.test.ts tests/cli/help.test.ts && npm run test:performance` as the final acceptance command; require every focused suite, including `tests/git/comparison.test.ts` Git version/protocol prerequisite assertions, to pass before the unchanged production performance gate starts.
8. From that combined command, preserve the complete final default `npm run test:performance` output on the same Node 24 runner and require classification `PASS`, exact fixture/one-plus-five/lifecycle/cleanup proofs, raw median readiness `<= 400` ms, and raw median search `<= 500` ms with exit 0.
</verification>

<success_criteria>
- PERF-01 and PERF-02 are both proven by five-sample raw medians through the compiled production picker, not mocks, injected seams, source imports, or spikes.
- The fixture is freshly created, exactly 10,000 local branches, fully packed, zero loose heads, and independently proven before measurement.
- Exact rendered branch rows bound both parent monotonic intervals; prompt text, Git completion, child clocks, and synthetic events cannot stop a timer.
- The gate uses one discarded warmup, five serial measured processes, unrounded fixed absolute medians, deterministic timeouts/cleanup, and raw bounded diagnostics.
- Initial `PASS` produces no production change. Initial `BUDGET_RED` produces raw segment/stage evidence and the minimum correction set—at most one smallest correction per independently proven root cause, every edit mapped to evidence—followed by GREEN evidence from the unchanged default gate.
- Focused picker/Git/CLI regressions, including the Git version/protocol prerequisite assertions in `tests/git/comparison.test.ts`, pass before the final unchanged `npm run test:performance` command exits 0; the final evidence reports both budgets passing.
</success_criteria>

<output>
Create `.planning/phases/11-production-performance-gate/11-01-SUMMARY.md` with: initial machine classification and raw gate output; exact fixture proofs; completed warmup and five raw sample pairs; raw min/median/max and fixed budgets; existing Node 24 runner plus compiled executable/runtime/Git/platform/CPU/load/CI identity; bounded hard-failure diagnostics if applicable; each distinct measured root cause and temporary probe evidence if applicable; an evidence mapping for every retained production edit or confirmation of zero production edits; focused regression output proving `tests/git/comparison.test.ts` Git version/protocol prerequisite assertions passed before the gate; final unchanged default gate `PASS` output; TDD budget-only-RED-or-initial-PASS → GREEN → proof sequence; and explicit PERF-01/PERF-02 coverage.
</output>
