---
phase: 03-distribution-contract-legal-boundary
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - .planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md
autonomous: false
requirements: [PKG-06, PKG-07, REL-04, REL-05]
must_haves:
  truths:
    - "The exact existing Ship-With-AI/cumpa repository identity, final intended remote refs/content/history, and every applicable GitHub exposure surface are reviewed after 03-02; a clean HEAD or npm scan is never treated as publication clearance (D-12, D-13)."
    - "All advertised remote refs and GitHub branch/tag/PR relationships are reviewed; a private main sync is a separately authorized fast-forward whose exact old OID is enforced by an explicit lease as well as an ancestry check, followed by a fresh actual-remote snapshot (D-12, D-13, D-14)."
    - "Issues, PRs/reviews/comments, releases/assets, discussions, wiki, Pages, Actions workflows/runs/jobs/logs/artifacts, deployments/environments, and other enabled repository surfaces are exhaustively paginated and reviewed or explicitly marked inaccessible and blocking; reports contain no raw secret (D-13, D-14)."
    - "Every applicable branch, tag, and push ruleset plus branch protection, repository, environment, deployment, fork, and Actions protection is inventoried. The approved disposition explicitly accounts for GitHub disabling ALL PUSH RULESETS on private-to-public conversion rather than promising impossible unchanged settings (D-13, D-14)."
    - "Both licensors' exact-text approvals remain valid for the current LICENSE, rights/exposure findings are resolved, any private sync and supported protection disposition are approved, and FINAL PUBLICATION AUTHORIZATION binds the actual remote snapshot immediately before conversion (D-09, D-11, D-14, D-15)."
    - "After a no-drift recheck, execution changes only the existing Ship-With-AI/cumpa repository's visibility and verifies unauthenticated public source/LICENSE/Issues access, the approved main OID, and the approved post-public protection state (D-12, D-17, D-19)."
    - "The public transition preserves GitHub-required platform permissions and disclosed D.4/D.5/D.8 consequences without calling Cumpa open source; making the repository private again is never represented as retracting forks, clones, caches, or copies (D-01, D-06, D-12, D-14)."
    - "D-20 reconciliation is consumed unchanged; D-21 provenance remains a Phase 5 evidence claim, D-22 npm/tarball/OIDC work remains later, and D-23 application/skill/support behavior remains untouched (D-20, D-21, D-22, D-23)."
  prohibitions:
    - "HIGH: Do not change visibility while either licensor approval is missing/stale, any rights/security/exposure finding is unresolved, an applicable GitHub surface is inaccessible or incompletely paginated, private sync is unapproved, or protection disposition is unapproved (D-13, D-14)."
    - "HIGH: No unconditional force, non-fast-forward update, mirror, deletion, unapproved ref, or history rewrite is allowed. An exact-old-OID --force-with-lease guard is permitted only after proving that same approved old OID is an ancestor of the approved target; the lease is compare-and-swap protection, never rewrite authority. A guardrail that forbids the flag must not be bypassed (D-14, D-15)."
    - "HIGH: Do not promise all protection settings survive. GitHub disables every push ruleset during private-to-public conversion; block unless the human-approved disposition accepts that consequence or establishes supported replacement protections before final authorization."
    - "Do not treat inability to inspect a surface as absence, record raw secrets/tokens/log payloads in evidence, or use a scanner as legal certification."
    - "Do not bind authorization to 03-PUBLICATION-REVIEW.md's future commit OID or to later approval/post-change/SUMMARY commits. Those local records are not part of the authorized remote source snapshot and must not be automatically pushed."
    - "Do not invent an atomic GitHub compare-and-change visibility API. Re-query and compare immediately before mutation; drift blocks and requires a refreshed review/authorization."
    - "HIGH: All remote mutations use an operator-verified, short-lived credential with write authority limited to the approved repository identity. Never fall back to broad ambient OAuth/SSH credentials or an all-repositories token if the slug changes, access fails, or scoped authentication is unavailable."
    - "Do not create another source/issue repository, publish npm, add a release workflow/dependency/approval service, alter license text/application/UI/API/schema/AI/skill behavior, redo D-20 reconciliation, or claim provenance/registry availability (D-12, D-15, D-20, D-21, D-22, D-23)."
  artifacts:
    - path: ".planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md"
      provides: "Redacted final source/GitHub exposure inventory, approved remote snapshot and protection disposition, attributable publication authorization, and observed public verification"
      contains: "FINAL PUBLICATION AUTHORIZATION"
  key_links:
    - from: ".planning/phases/03-distribution-contract-legal-boundary/03-RIGHTS-REVIEW.md"
      to: ".planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md"
      via: "Final 03-02 delta and actual remotely exposed history/surfaces refresh the rights review; changed legal text/material returns to 03-01 approval"
      pattern: "rights|history|unresolved|LICENSE"
    - from: ".planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md"
      to: "approved remote source snapshot"
      via: "Both named approvals bind current LICENSE digest; the remote snapshot binds the source target without self-referencing later local records"
      pattern: "Alessandro Magionami|Manuel Salvatore Martone|SHA-256"
    - from: "captured pre-review source-target OID"
      to: "refs/heads/main on Ship-With-AI/cumpa"
      via: "Approved private fast-forward of only main, guarded by the same old-OID ancestry check and exact lease, then actual remote resnapshot"
      pattern: "source_target_oid|remote_main_before|remote_main_after"
    - from: "approved protection disposition"
      to: "post-public GitHub protection state"
      via: "Explicit branch/tag/push ruleset and repository/environment/Actions comparison, including expected push-ruleset disablement"
      pattern: "push ruleset|branch protection|environment|Actions"
    - from: "FINAL PUBLICATION AUTHORIZATION"
      to: "existing repository visibility mutation"
      via: "Repository ID, canonical remote snapshot digest, expected main OID, exposure digest, and protection-disposition digest are rechecked immediately before one visibility-only API mutation"
      pattern: "FINAL PUBLICATION AUTHORIZATION|visibility|public"
    - from: "public Ship-With-AI/cumpa"
      to: "README source and Issues links"
      via: "Unauthenticated repository/ref/content API checks at the approved main OID, including the exact LICENSE SHA-256"
      pattern: "github.com/Ship-With-AI/cumpa/issues"
---

<objective>
Review the exact post-03-02 remote publication candidate and every applicable GitHub exposure/protection surface, obtain all blocking human authorizations, then change only the existing repository's visibility and verify the actual public result.

Purpose: Publish the selected proprietary source/history without silently exposing unreviewed refs or GitHub data, weakening protections by assumption, or relying on stale/self-referential approval evidence.
Output: One redacted publication-review record containing the exact authorized remote snapshot, protection disposition, human authorization, visibility observation, and public source/Issues proof.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-distribution-contract-legal-boundary/03-CONTEXT.md
@.planning/phases/03-distribution-contract-legal-boundary/03-RESEARCH.md
@.planning/phases/03-distribution-contract-legal-boundary/03-PATTERNS.md
@.planning/phases/03-distribution-contract-legal-boundary/03-01-SUMMARY.md
@.planning/phases/03-distribution-contract-legal-boundary/03-02-SUMMARY.md
@.planning/phases/03-distribution-contract-legal-boundary/03-RIGHTS-REVIEW.md
@.planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md
@LICENSE
@THIRD_PARTY_NOTICES.md
@package.json
@package-lock.json
@README.md
@docs/distribution-operations.md
@docs/support-service-operations.md
@.github/workflows/deploy-supabase-production.yml
</context>

<interfaces>
- Target identity: the existing GitHub repository `Ship-With-AI/cumpa`, verified by immutable repository/node ID as well as owner/name.
- Mutation authority: an existing repository-scoped short-lived credential, or an owner-provided expiring fine-grained token selecting only the approved repository, with only the permissions required for the authorized changes. Operator-verified scope must be bound to the selected repository identity; a name-rebound replacement repository must not gain write access. No new GitHub App or standing credential service is required. Token values never enter arguments, URLs, reports, or logs; supply them only through the approved secure credential mechanism and revoke temporary authorization when finished.
- Source target: capture the exact local `main` OID containing completed 03-01 and 03-02 outputs before creating or amending `03-PUBLICATION-REVIEW.md`; this is the only candidate main update, not a later local approval/report/SUMMARY commit.
- Canonical remote snapshot: deterministically sorted reviewed ref/OIDs, relevant content digests, surface IDs/statuses, and protection values. Record the field projection before hashing; omit transport headers, request/capture timestamps, counters, and local approval/post-change fields, but never omit security-relevant content or settings. Require all exposure-producing Actions/deployments to be terminal and the operator to maintain a no-write/no-rename/no-transfer window through final authorization and conversion. New content, status changes, or lost quiescence invalidates the snapshot.
- Private-preparation authorization: attributable approval of the exact optional old-main-OID -> source-target-OID fast-forward, its ancestry-plus-explicit-lease guard, the repository-scoped mutation credential, and any exact supported protection changes. It is not visibility authorization.
- Final gate: `FINAL PUBLICATION AUTHORIZATION: Ship-With-AI/cumpa snapshot <sha256> main <40-hex-oid> protections <sha256>` supplied by the authorizer only after reviewing the actual post-sync remote snapshot and consequences.
- Mutation: the supported visibility-only CLI/REST operation resolves the exact owner/name and verifies the immutable ID, while repository-scoped write authority prevents a rebound slug from authorizing another repository. GitHub's live UpdateRepositoryInput has no visibility field; do not invent a GraphQL visibility mutation or PATCH-by-ID endpoint.
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inventory the exact private publication candidate without remote mutation</name>
  <files>.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md</files>
  <read_first>
    - `.planning/phases/03-distribution-contract-legal-boundary/03-01-SUMMARY.md`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-02-SUMMARY.md`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-RIGHTS-REVIEW.md`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md`
    - `LICENSE`
    - `THIRD_PARTY_NOTICES.md`
    - `package.json`
    - `package-lock.json`
    - `README.md`
    - `docs/distribution-operations.md`
    - `docs/support-service-operations.md`
    - `.github/workflows/deploy-supabase-production.yml`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-CONTEXT.md` decisions D-01, D-06, and D-09 through D-23
  </read_first>
  <action>Keep `Ship-With-AI/cumpa` PRIVATE throughout this task. Before writing the review record, capture the clean local `main` OID containing completed 03-01/03-02 outputs as `source_target_oid`; later local review/approval/SUMMARY commits are deliberately outside this candidate and must not be pushed automatically. Recompute the LICENSE digest and validate two separate exact-text approvals from Alessandro Magionami and Manuel Salvatore Martone plus the blocker-free 03-RIGHTS-REVIEW. Review the complete 03-02 delta and the entire candidate remote exposure; a change to LICENSE text returns to both exact-text approvals, and a change to refs, material content, rights findings, or protection state invalidates the affected review/approval (D-09, D-11, D-13, D-14).

Create `03-PUBLICATION-REVIEW.md` as a redacted evidence record, not an approval service. Bind immutable repository ID, current private visibility, `source_target_oid`, local/current-upstream relationship, and the then-current remote state. Enumerate deterministic sorted remote refs/OIDs with native Git's advertised `refs/*` plus exhaustive GitHub branch/tag APIs and PR head/base relationships; include remote-only refs and do not assume local refs define exposure. Review every object/path/blob and commit message reachable from each intended exposed ref, including deleted/historical content and the tracked planning history. Record scanner coverage and human classification separately; neither npm/package scanning nor a clean current tree certifies history or rights (D-12, D-13).

Using authenticated GitHub reads with exhaustive pagination/cursor traversal, inventory repository settings/features; Issues/comments; PRs/reviews/review comments/head/base refs; releases/assets; enabled discussions/comments, wiki refs/history, and Pages; Actions workflows/runs/jobs/logs/artifacts; deployments; environment protections/branch rules; and private-infrastructure references. Before downloading raw logs/artifacts, create a mode-0700 temporary directory under umask 077 and register finally/trap cleanup, including normal errors, blockers and SIGINT/SIGTERM. Never extract an untrusted archive outside that directory or execute its contents. Store only bounded redacted findings/digests in the durable report. Confirm cleanup on success and failure; an interruption or hard termination with unconfirmed cleanup leaves a blocker to resolve before publication. An applicable surface that cannot be fully listed/read, uncertain pagination, or a HIGH rights/credential/confidential-material finding blocks; it is not evidence of absence (D-13, D-14).

Inventory all effective repository and inherited rulesets by target/type (branch, tag, and push), enforcement/bypass conditions, classic branch protection, repository security/settings, fork policy, Actions allowed-actions/default-workflow/fork-PR permissions, environment reviewers/wait/deployment branches, and relevant deployment protection. Explicitly record GitHub's documented private-to-public consequence that ALL PUSH RULESETS are disabled. Propose a supported disposition: accept that exact loss with informed approval, or—before final authorization—apply separately approved supported branch/tag/environment/Actions replacement protections. Never promise an unsupported push ruleset remains enabled. Block if required protection cannot be preserved acceptably.

Determine the actual remote state rather than reusing planning-time OIDs. No-sync is valid only when remote main already equals source_target_oid. Otherwise propose one exact private fast-forward from remote_main_before to source_target_oid, plus any precisely enumerated supported protection changes. Prove that approved old OID is an ancestor of the target, review every intervening commit/byte, and specify the exact-old-OID lease that must reject ref drift at the update itself. Include operator-confirmed repository-scoped mutation authentication; broad ambient credentials are not an acceptable fallback. This task only inventories and proposes: it performs no push, protection mutation, or visibility change. Keep the proposal independent of later human statements and its own future commit OID.</action>
  <verify>
    <human-check>Confirm the recorded proposal identifies the immutable repository, reviewed source target, exact optional old-main to target transition, complete redacted exposure findings, and supported protection disposition, while the repository and refs remain unchanged and private.</human-check>
  </verify>
  <acceptance_criteria>The redacted proposal covers every applicable exposed surface and identifies all unresolved blockers and exact proposed changes without performing them.</acceptance_criteria>
  <done>The reviewed private-preparation proposal is ready for a separate top-level human checkpoint.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Authorize only the exact private preparation changes</name>
  <files>.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md</files>
  <read_first>
    - `.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md`
    - `LICENSE`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-CONTEXT.md`
  </read_first>
    <what-built>A redacted complete private exposure inventory, exact reviewed source target, proposed optional main fast-forward, and supported protection disposition including automatic push-ruleset disablement.</what-built>
    <instructions>
      1. Confirm the repository immutable ID is the existing Ship-With-AI/cumpa and it is still private.
      2. Confirm both exact LICENSE approvals match current bytes and all source/history, GitHub-surface, rights, notice, credential, confidential-material, and inaccessible-surface findings are resolved.
      3. Confirm refs and all enabled GitHub surfaces were exhaustively paginated/reviewed and the evidence is redacted.
      4. Confirm the proposed protection disposition explicitly handles every branch/tag/push ruleset and repository/environment/Actions control, including GitHub disabling all push rulesets.
      5. If main sync is needed, approve the exact old OID -> target OID fast-forward with BOTH the ancestry check and explicit expected-old lease; no unconditional force, non-fast-forward, mirror, deletion, rewrite, or other ref is authorized.
      6. Confirm a short-lived mutation credential selects only the approved repository, with the required permissions and any organization approval. If unavailable, complete that human-only authentication step through a secure channel; never paste a token into the record or silently reuse broad ambient credentials.
    </instructions>
    <resume-signal>Type `PRIVATE PREPARATION AUTHORIZED: Ship-With-AI/cumpa review &lt;sha256&gt;` using the displayed review digest after scoped authentication is ready, or identify the exact blocker. This does not authorize public visibility.</resume-signal>
  <action>The human authorizer must approve the exact reviewed preparation proposal and confirm repository-scoped authentication. This assent and credential authorization are non-automatable human actions, not deferred verification. Reject missing, stale, agent-authored or broader authorization; no push, settings mutation, or visibility change occurs in this checkpoint.</action>
  <verify>
    <human-check>Compare the human statement with the exact current proposal, repository identity, old and target main OIDs, and every proposed protection change. Confirm it expressly excludes public visibility and does not waive unresolved findings.</human-check>
  </verify>
  <verification>Verify the actual human statement, approved old/target OIDs, guard and protection change set, operator-confirmed credential scope, and successful read access to the same immutable repository. Record only attributable approval and non-secret scope evidence. Task 3 stays blocked if authentication, attribution, or scope is unresolved.</verification>
  <acceptance_criteria>A distinct attributable private-preparation authorization covers the exact proposed change set; without it Task 3 cannot start.</acceptance_criteria>
  <done>The bounded private preparation is authorized, but has not been applied and public visibility is still unauthorized.</done>
</task>

<task type="auto">
  <name>Task 3: Apply authorized private preparation and snapshot actual remote exposure</name>
  <files>.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md</files>
  <read_first>
    - `.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md`
    - `LICENSE`
    - `.github/workflows/deploy-supabase-production.yml`
    - `docs/distribution-operations.md`
  </read_first>
  <action>Validate Task 2's actual approval, unchanged proposal, repository identity and scoped mutation credential. Re-query private visibility, remote refs and protections; drift returns to Task 1. Load and validate the exact approved 40-hex REMOTE_MAIN_BEFORE and SOURCE_TARGET_OID values; do not substitute a later local HEAD. Apply only authorized supported protection changes. For a required push, first require `git merge-base --is-ancestor "$REMOTE_MAIN_BEFORE" "$SOURCE_TARGET_OID"` to succeed, then use the exact expected-old lease `--force-with-lease=refs/heads/main:$REMOTE_MAIN_BEFORE` with the single refspec `$SOURCE_TARGET_OID:refs/heads/main`. Use the verified HTTPS repository URL and only the scoped credential, without changing stored origin/global authentication. The ancestry check and lease must use the SAME immutable approved old OID; together they allow only that fast-forward, never a non-fast-forward rewrite. No generic --force, mirror, delete, other ref, refreshed implicit lease, or silent retry is permitted. If a tool/organizational guardrail forbids the lease flag, stop rather than bypass it. On any mismatch or unexpected effect, stop and refresh review. Whether pushed or no-sync, require actual remote main to equal SOURCE_TARGET_OID and verify the remote LICENSE/metadata/notices/README correspond to the reviewed post-03-02 source tree before proceeding. Wait for exposure-producing runs/deployments to become terminal; review their final logs/artifacts and establish the operator's no-write/no-rename window. Then repeat the complete deterministic paginated captures, resolve all findings and cleanup blockers, and record the actual snapshot/protection digests for Task 4. Later local evidence commits remain excluded. This task does not authorize or perform public visibility.</action>
  <verify>
    <automated>git merge-base --is-ancestor "$REMOTE_MAIN_BEFORE" "$SOURCE_TARGET_OID" &amp;&amp; git ls-remote --symref origin 'refs/*' &amp;&amp; gh api repos/Ship-With-AI/cumpa --jq '{id,node_id,full_name,private,visibility,default_branch,has_issues,has_discussions,has_wiki,has_pages}'</automated>
    <human-check>Compare the post-preparation canonical snapshot to every reviewed inventory and approval; all surfaces are accessible and complete, remote main equals the reviewed source target where sync was approved, and the actual protection state matches the approved pre-publication disposition.</human-check>
  </verify>
  <acceptance_criteria>One redacted record binds the actual private remote ref/content/surface/protection state that would become public; all blockers are resolved and no unapproved ref or protection mutation occurred.</acceptance_criteria>
  <done>The exact remotely exposed candidate—not later local evidence commits—is ready for separate FINAL PUBLICATION AUTHORIZATION.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Grant final authorization for the actual remotely exposed state</name>
  <files>.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md</files>
  <read_first>
    - `.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md`
    - `LICENSE`
    - `docs/distribution-operations.md`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-CONTEXT.md` decisions D-06 and D-12 through D-23
  </read_first>
  <what-built>The final post-sync canonical remote refs/content/GitHub-surface snapshot, resolved findings, valid dual-licensor exact-text approval, and approved supported post-public protection disposition.</what-built>
  <instructions>
    1. Review the canonical remote snapshot digest, exact expected main OID, all ref OIDs, and immutable repository ID; these—not the local review record's future commit—are the content authorized for visibility.
    2. Confirm every applicable Git/GitHub surface is reviewed with exhaustive pagination, every inaccessible surface and HIGH finding is resolved, and evidence contains no raw secret.
    3. Confirm Alessandro's and Manuel's distinct approvals still bind the exact current LICENSE and that any final content/ref/material change received the affected renewed review/approval.
    4. Approve the actual supported protection disposition, explicitly acknowledging all push rulesets will be disabled and confirming the expected surviving/replacement branch, tag, environment, and Actions controls.
    5. Acknowledge public-repository GitHub platform/public-access consequences, including applicable Terms D.4, D.5, and D.8, public Actions history/log exposure, forking/copying, and that returning to private cannot retract copies.
    6. Confirm remote main equals the reviewed source target, any push passed the exact-old lease plus ancestry guard, exposure-producing runs are terminal, temporary evidence is cleaned up, scoped mutation authentication remains valid, and the no-write/no-rename window is in effect.
    7. Confirm this authorization permits only the existing repository's visibility transition, not npm publication, future pushes, protection drift, application changes, or later local evidence commits.
  </instructions>
  <action>The human authorizer must supply FINAL PUBLICATION AUTHORIZATION for the exact actual remote snapshot, 40-hex main OID and protection digest after reviewing the instructions. This is non-automatable assent: human-action remains blocking under auto/end-of-phase settings. A generic approved, agent-authored statement, stale or conditional response, or proxy license assent cannot satisfy it.</action>
  <verify>
    <automated>git ls-remote --symref origin 'refs/*' &amp;&amp; gh api repos/Ship-With-AI/cumpa --jq '{id,node_id,full_name,private,visibility,default_branch}'</automated>
    <human-check>Approve only the exact actual remote snapshot and protection disposition after checks 1–7; verify the repository remains private at authorization time.</human-check>
  </verify>
  <verification>After receiving the actual statement, recompute the same projected snapshot/protection digests and check repository identity, exact remote source target, dual-license approval, scoped authentication, quiescence and cleanup. The executor records assent separately from the snapshot it names; no future approval-file commit OID is required. Any mismatch returns to Task 1 without visibility mutation.</verification>
  <acceptance_criteria>An attributable FINAL PUBLICATION AUTHORIZATION is stored separately from the canonical snapshot it names and grants only the one bounded visibility mutation.</acceptance_criteria>
  <done>The actual remote publication candidate, resolved rights/exposure review, dual exact-license approval, and supported protection state are explicitly authorized.</done>
  <resume-signal>Type exactly `FINAL PUBLICATION AUTHORIZATION: Ship-With-AI/cumpa snapshot &lt;sha256&gt; main &lt;40-hex-oid&gt; protections &lt;sha256&gt;` with the values displayed in the record, or identify the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 5: Recheck, change only visibility, and prove the observed public state</name>
  <files>.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md</files>
  <read_first>
    - `.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md`
    - `.planning/phases/03-distribution-contract-legal-boundary/03-LICENSE-APPROVAL.md`
    - `LICENSE`
    - `README.md`
    - `docs/distribution-operations.md`
  </read_first>
  <action>Load the authorized repository ID, canonical snapshot digest, expected main OID, and protection digest from the human response. Immediately before mutation, rerun the complete deterministic remote-ref, GitHub-surface, repository-setting, and protection captures with exhaustive pagination and compare every digest/value—not only HEAD—to the authorized snapshot. Also recheck private visibility and current LICENSE digest/dual approval. GitHub exposes no invented atomic compare-and-change visibility primitive: this immediate comparison narrows but does not erase the race. Any drift, permission loss, new/inaccessible surface, changed ref/content/license/protection, unresolved finding, or stale approval blocks; return to Task 1 and obtain a new FINAL PUBLICATION AUTHORIZATION (D-13, D-14).

If the comparison is exact, the no-write/no-rename window still holds, and owner/name resolves to the approved immutable ID, run `gh repo edit Ship-With-AI/cumpa --visibility public --accept-visibility-change-consequences` with no other setting flags and ONLY the verified repository-scoped credential. Its write authority must exclude any replacement repository if the slug is rebound; missing/expired/unsupported scoped authentication blocks, with no fallback to broad OAuth, SSH or all-repositories tokens. GitHub's live GraphQL UpdateRepositoryInput has no visibility field: use no invented GraphQL visibility or PATCH-by-ID operation. Do not push later local commits, change refs/settings, create a repository, publish npm, or alter application/skill/license behavior. Push-ruleset disablement is expected only under the approved disposition.

Observe rather than infer the result. Provide APPROVED_MAIN_OID, APPROVED_LICENSE_SHA256 and APPROVED_REPOSITORY_ID from the exact approval record as non-secret environment values for the check below. Its Node HTTP requests set no Authorization header and use no Git credential helper or stored CLI authentication. Require public repository identity, main ref equality, approved LICENSE bytes at that OID, and Issues access. Authenticated reads then compare actual protections with the approved disposition. Record bounded observations/digests, not raw credentials. This establishes source-publication facts only; registry availability and provenance remain Phase 5 evidence (D-17, D-19, D-21).

On a mutation error, record the redacted error and leave private state untouched where the API did not change it. On any verification failure after GitHub reports public, record that the repository has already been exposed, stop all further mutation/push, and escalate for explicit remediation; do not automatically toggle private or claim copies/forks/caches were retracted. Any future push—including this local review record or the later GSD SUMMARY—changes public content and requires fresh applicable review/authorization.</action>
  <verify>
    <automated>env -u GH_TOKEN -u GITHUB_TOKEN -u GITHUB_AUTH_TOKEN -u NODE_OPTIONS node --input-type=module -e 'import assert from "node:assert/strict"; import {createHash} from "node:crypto"; const {APPROVED_MAIN_OID:oid,APPROVED_LICENSE_SHA256:digest,APPROVED_REPOSITORY_ID:id}=process.env; assert.match(oid??"",/^[a-f0-9]{40}$/); assert.match(digest??"",/^[a-f0-9]{64}$/); assert.match(id??"",/^[0-9]+$/); const base="https://api.github.com/repos/Ship-With-AI/cumpa"; const headers={"User-Agent":"cumpa-public-verifier","Accept":"application/vnd.github+json"}; const get=async path=>{const response=await fetch(base+path,{headers,redirect:"error"}); assert.equal(response.status,200,path); return response.json()}; const repo=await get(""); assert.equal(String(repo.id),id); assert.equal(repo.private,false); assert.equal(repo.visibility,"public"); assert.equal(repo.has_issues,true); const ref=await get("/git/ref/heads/main"); assert.equal(ref.object.sha,oid); const license=await get("/contents/LICENSE?ref="+oid); assert.equal(license.type,"file"); assert.equal(license.encoding,"base64"); assert.equal(createHash("sha256").update(Buffer.from(license.content,"base64")).digest("hex"),digest); await get("/issues?per_page=1"); console.log("Anonymous repository, approved main, LICENSE digest and Issues checks passed");'</automated>
    <human-check>Open source and Issues in a fresh unauthenticated browser context. Compare authenticated post-public branch/tag/push rulesets and repository/environment/Actions controls with the exact approved disposition, including expected push-ruleset disablement. Confirm cleanup and revoke any temporary mutation credential; do not claim success if public or protection checks fail.</human-check>
  </verify>
  <acceptance_criteria>The existing repository is observably public at the approved main OID, exact approved LICENSE and Issues are accessible without private credentials, actual protections match the supported disposition, and failures are recorded without false rollback claims.</acceptance_criteria>
  <done>Ship-With-AI/cumpa—not a substitute repository—is publicly readable in the exact authorized state with honest redacted evidence.</done>
</task>

</tasks>

<threat_model>
## Enforcement
ASVS Level 1 applies. Every HIGH-severity finding is blocking before publication; no automatic/end-of-phase setting may waive the private-preparation or FINAL PUBLICATION AUTHORIZATION gates.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Local main -> private remote main | Only one reviewed source-target OID may fast-forward main after exact human authorization; later local evidence commits are excluded. |
| Git/GitHub private surfaces -> public internet | Refs/history and non-Git surfaces, including Actions logs/artifacts, cross an irreversible disclosure boundary. |
| GitHub protections -> visibility conversion | Push rulesets are disabled by the platform; surviving/replacement controls must follow an approved supported disposition. |
| Human evidence -> visibility mutation | Dual exact-license approval, resolved findings, private prep, and final snapshot authorization must be attributable and current. |
| Authenticated mutation -> unauthenticated observation | Success is public source/Issues and expected OID/protection evidence, not a successful command exit. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-03-03-SR | Information Disclosure | Git history and GitHub surfaces | HIGH | mitigate | Complete reachable-history plus exhaustively paginated surface review; inaccessible surfaces and unresolved redacted findings block. |
| T-03-03-RG | Tampering | remote refs/private sync | HIGH | mitigate | Require the same approved old-OID ancestry check and exact expected-old lease; reject stale or non-fast-forward updates, unconditional force/mirror/delete/other refs, and resnapshot actual remote. |
| T-03-03-AP | Spoofing / Repudiation | licensor/publication authorization | HIGH | mitigate | Recompute LICENSE/snapshot/protection digests; require both named license approvals and exact attributable FINAL PUBLICATION AUTHORIZATION. |
| T-03-03-DR | Tampering | stale snapshot race | HIGH | mitigate | Repeat canonical captures immediately before mutation; any mismatch returns to review. Acknowledge no atomic visibility CAS exists. |
| T-03-03-PR | Elevation of Privilege | rulesets and protections | HIGH | mitigate | Inventory branch/tag/push rulesets and repository/environment/Actions controls; approve supported post-public state accounting for all push rulesets being disabled. |
| T-03-03-RV | Information Disclosure | evidence record | HIGH | mitigate | Store only bounded redacted findings and digests; keep raw logs/artifacts temporary and protected; never commit secret contents. |
| T-03-03-ID | Spoofing | target repository | HIGH | mitigate | Bind immutable ID plus owner/name, and use short-lived write authority restricted to that repository so a rebound slug cannot authorize another target; block absent scoped authentication. |
| T-03-03-PF | Repudiation | claimed public success | HIGH | mitigate | Unauthenticated repository/Issues/Git/LICENSE checks plus authenticated protection comparison and observed timestamps. |
| T-03-03-RB | Information Disclosure | failed rollback assumption | HIGH | mitigate | Record that public copies may persist; never auto-private or claim retraction after exposure. |
</threat_model>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- `.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md` — redacted final exposure/right/protection record, exact remote snapshot, private-preparation approval, attributable final visibility authorization, and observed public verification.
- External observed state: the existing immutable `Ship-With-AI/cumpa` repository is public at the approved remote main OID; this is a bounded GitHub visibility mutation, not a new repository or a local evidence commit.
</artifacts_this_phase_produces>

<verification>
- Use native Git for reviewed refs and scoped private preparation, supported GitHub APIs/CLI for mutations, and credential-free Node HTTP for public observations; install no dependency or scanner/approval/release framework.
- Require exhaustive pagination/cursor traversal and explicit enabled/disabled/not-applicable status for every named GitHub surface; inaccessible is blocking.
- Verify main sync is the exact approved fast-forward with matching ancestry and expected-old lease checks; no-sync requires remote main already equal the source target. Rebuild the actual remote snapshot afterward.
- Recompute the exact canonical remote/exposure/protection state immediately before the one visibility-only mutation; drift returns to review and final authorization.
- Verify actual unauthenticated source/LICENSE/Issues access, expected main OID, immutable repository identity, and approved protection state after conversion.
- Record failure honestly: no automatic privacy toggle, no retraction claim, no package/provenance claim, and no automatic push of local review/SUMMARY commits.
</verification>

<success_criteria>
- PKG-06: the publicly readable repository carries the exact dual-approved proprietary LICENSE and required notices without overriding GitHub-required/statutory/third-party rights or claiming open source.
- PKG-07: the existing Ship-With-AI/cumpa and reviewed history become public only after final exhaustive exposure review, resolved findings, approved private sync/protection disposition, and exact final authorization.
- REL-04: the README's exact source and Issues surfaces are proved usable without private access at the approved source OID; no homepage or substitute tracker/repository exists.
- REL-05: public-source eligibility exists for Phase 5 to assess, but this plan makes no npm/provenance claim and preserves the actual-evidence policy.
- D-12 through D-23 execution boundaries and failure behavior are explicit; D-20 remains completed, and D-21/D-22/D-23 later-phase/application boundaries remain intact.
</success_criteria>

<output>Create `.planning/phases/03-distribution-contract-legal-boundary/03-03-SUMMARY.md` when complete. Do not push the local publication-review or SUMMARY commit without a new review/authorization of that additional public content.</output>
