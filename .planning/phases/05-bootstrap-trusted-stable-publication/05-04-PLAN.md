---
phase: 05-bootstrap-trusted-stable-publication
plan: 04
type: execute
wave: 3
depends_on: [05-01, 05-03]
files_modified:
  - .planning/phases/05-bootstrap-trusted-stable-publication/05-SOURCE-PUBLICATION-REVIEW.md
  - .planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-EVIDENCE.json
  - .planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-APPROVAL.md
  - .planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-PUBLICATION.json
  - .planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-AUTH-REVOCATION.md
autonomous: false
requirements: [REL-01]
must_haves:
  truths:
    - "Per D-01 and D-04, @shipwithai/cumpa@1.5.0-bootstrap.0 is built anew from the exact reviewed Phase 5 source tip, fully scanned and installed, actually approved by digest and published publicly under bootstrap without assigning latest."
    - "Per D-02, the old e776… Phase 4 archive and its sealed evidence/approval remain immutable history and are neither candidate input nor publication fallback; the source package and lock remain stable 1.5.0."
    - "Per D-03 and D-08, future phase execution permits bounded local preparation; source push, artifact approval, temporary npm authentication and irreversible publication each require distinct actual scoped authorization."
    - "Per D-05, the exact reviewed source SHA/tree containing completed Phase 5 source/workflow/policy changes is published to public main through a dedicated [skip ci] boundary commit, and observation proves the existing Supabase deployment workflow did not deploy as a side effect."
    - "Per D-06, the bootstrap build uses the canonical configured production resource obtained from the existing GitHub production environment in process memory, matches fingerprint 89485617b2d50d4778542ebedc3817a3e3fcddb6520a4a9c3a66e37c3a9c6cdf, and never persists cleartext origin or credential material."
    - "Per D-07, acceptance scans the actual runtime/legal-only tarball, installs the exact bytes with scripts disabled, and exercises generated binary, browser/worker/codicon/review/support-dismissal/V2/V3 and real Darwin ARM64 native re-export behavior before approval."
    - "Per D-09, durable evidence distinguishes observed source control, local acceptance, public registry equality, interactive bootstrap publication and lack of CI trusted-publisher provenance without overstating any assurance."
    - "After every attempted or ambiguous npm publication, the temporary npm credential is actually logged out/revoked, only plan-owned isolated auth/config/cache/prefix state is removed, and a bounded revocation record blocks stable setup until completion."
  prohibitions:
    - "HIGH / D-01 / D-02: Never publish, repack, relabel, delete or use the old Phase 4 e7766d43f7f804b138e694b298480cdec62ebfc16960f86d4c1e3c7959cf1dca archive as bootstrap or fallback; never alter Phase 04 evidence/approval."
    - "HIGH / D-03: Never let generic assent, an earlier planning decision or one gate authorize another resource/credential/irreversible action; no implicit publish on login and no source credential reused for npm."
    - "HIGH / D-04: Never publish a directory, rebuild/repack after approval, silently choose another prerelease, overwrite/unpublish an occupied identity, create a reservation package, or assign latest."
    - "HIGH / D-05: Never push current HEAD, an unreviewed SHA, a different tree, a force update or a source tip lacking [skip ci]; never disable/edit the Supabase workflow or weaken branch/ruleset checks to make the push pass."
    - "HIGH / D-06: Never write a local .env, duplicate the configured origin into npm-release, print/persist the origin, persist GitHub/npm tokens, login URLs, raw npm config, private custody paths or raw sensitive logs."
    - "HIGH / D-07: Never approve fixture/source/mock checks in place of installed exact-byte behavior or treat native fallback as success on the actual Darwin ARM64 producer host."
    - "HIGH / D-08: Never pre-fill approval, accept placeholders or generic approval, edit approval after sealing, or continue when identity/time/permission/2FA facts are missing."
    - "HIGH / D-09: Never call the interactive bootstrap publish trusted publishing/provenance, claim server-side revocation from local deletion alone, or equate configured-origin fingerprinting with public disclosure of the origin."
    - "Never blindly retry an ambiguous publish. Query the exact registry identity first; absent requires fresh scoped authorization, present requires exact-byte comparison, and wrong bytes are a blocking incident rather than an automatic unpublish."
  artifacts:
    - path: ".planning/phases/05-bootstrap-trusted-stable-publication/05-SOURCE-PUBLICATION-REVIEW.md"
      provides: "Exact reviewed source tip/tree, public ref precondition, [skip ci] contract, source authorization and no-Supabase-side-effect observations"
    - path: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-EVIDENCE.json"
      provides: "Producer-authored exact bootstrap archive/source/projection identities with no cleartext configured origin or custody path"
      contains: "cumpa.runtime-artifact-evidence/v1"
    - path: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-APPROVAL.md"
      provides: "Actual human digest/evidence-bound bootstrap acceptance and limitations acknowledgment"
    - path: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-PUBLICATION.json"
      provides: "Observed npm authority/2FA, irreversible authorization, publication outcome, dist-tags, registry byte equality and usability result"
    - path: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-AUTH-REVOCATION.md"
      provides: "Actual logout/revocation attempts, local credential absence, plan-owned cleanup and precisely bounded server-side assurance"
  key_links:
    - from: "reviewed Phase 5 source tree"
      to: "public refs/heads/main"
      via: "Dedicated [skip ci] boundary commit and explicit full-SHA refspec after exact remote/ruleset authorization"
      pattern: "reviewedSourceSha|reviewedTreeSha|skip ci"
    - from: "reviewed source SHA"
      to: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-EVIDENCE.json"
      via: "Clean configured --purpose bootstrap producer cycle records the same source head/tree and stable source manifest hash"
      pattern: "bootstrap|source"
    - from: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-EVIDENCE.json"
      to: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-APPROVAL.md"
      via: "Approval repeats actual package/version, archive SHA-256, byte length and evidence SHA-256 after installed acceptance"
      pattern: "1.5.0-bootstrap.0|sha256|byteLength"
    - from: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-APPROVAL.md"
      to: "npm publish exact tarball"
      via: "Fresh publication authorization repeats the sealed archive identity, bootstrap tag and observed account authority/2FA"
      pattern: "bootstrap"
    - from: "npm registry downloaded bytes"
      to: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-PUBLICATION.json"
      via: "Registry packument and downloaded tarball are independently hashed/length-checked against the approved archive before usability success"
      pattern: "registry|distTags|sha256"
    - from: "attempted npm publication"
      to: ".planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-AUTH-REVOCATION.md"
      via: "finally-path logout/revocation and isolated auth/config/cache/prefix cleanup execute for success, failure and ambiguity"
      pattern: "logout|revocation|cleanup"
---

<objective>
Publish the exact reviewed Phase 5 source and one newly built, usable MIT bootstrap package through separate explicit gates, then revoke temporary npm authority and seal honest evidence.

Purpose: Complete REL-01 with a real public bootstrap that validates package-name ownership, account/2FA authority, exact-byte approval/publication, installed behavior and cleanup before any stable release is attempted.
Output: Exact source publication/no-deployment review, bounded bootstrap evidence, digest-bound approval, registry publication/equality record and credential revocation/cleanup record.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-CONTEXT.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-RESEARCH.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-PATTERNS.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-01-SUMMARY.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-03-SUMMARY.md
@.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md
@.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-APPROVAL.md
@docs/distribution-operations.md
@.github/workflows/deploy-supabase-production.yml
@scripts/pack-runtime.mjs
@scripts/verify-production-artifacts.mjs
@tests/package/agent-ready-export.test.ts
@package.json
@package-lock.json
</context>

<interfaces>
- Public bootstrap identity is fixed: package `@shipwithai/cumpa`, version `1.5.0-bootstrap.0`, dist-tag `bootstrap`, access `public`. Root source manifests remain `1.5.0`. Vacancy is a precondition, not an invitation to choose another version.
- Historical public-main baseline comes from the reviewed Phase 3 record and must be re-observed at execution. The new publication target is a dedicated reviewed boundary commit whose message contains `[skip ci]`; its tree is the completed Phase 5 source/workflow/policy tree and its SHA is computed at execution.
- Source push uses the explicit `<reviewed-full-sha>:refs/heads/main` refspec only after the observed remote SHA, fast-forward relation, rulesets, required checks, workflow-write permission and skip behavior are reviewed. If policy makes that route unavailable or would leave required checks unresolved, stop for owner decision.
- Configured bootstrap production uses `--purpose bootstrap`; scanning uses `--profile bootstrap`; installed acceptance uses `CUMPA_RUNTIME_PROFILE=bootstrap`. These are Plan 05-01's closed interfaces.
- The canonical support origin is derived in process memory from the existing GitHub `production` environment variable `SUPABASE_PROJECT_REF`, normalized only to the configured service URL expected by the producer, validated for canonical form and compared to SHA-256 fingerprint `89485617b2d50d4778542ebedc3817a3e3fcddb6520a4a9c3a66e37c3a9c6cdf`. Durable files record only fingerprint/configured booleans.
- Bootstrap registry equality uses public npm/Node reads directly against `05-BOOTSTRAP-EVIDENCE.json`; `scripts/verify-npm-release.mjs` remains stable-only and is not generalized. Compare exact packument name/version, `dist-tags.bootstrap`, absence of a bootstrap-induced `latest` change, downloaded tarball SHA-256/SHA-1/SHA-512/integrity/byte length and installed generated-bin version.
- Durable records use execution-derived values only: full source/tree SHAs, archive/evidence/legal hashes and lengths, public account/scope role facts, 2FA mode observation, run/attempt IDs, timestamps, approval text, publication response/packument identities and bounded revocation/cleanup booleans. They never contain secrets, auth URLs, raw configs/logs or private paths.
</interfaces>

<bootstrap_auth_lifetime>
- Before npm login, create an opaque-ID directory under `~/.local/share/cumpa/release-operations/bootstrap/` with mode 0700 and an atomic mode-0600 `operation.json`. Record only fixed relative owned HOME/userconfig/cache/prefix names, approved artifact/evidence digests, target/purpose, timestamps, guard identity and revocation state. No token is copied into this receipt and no actual private location enters public evidence. On resume, inspect nonterminal receipts for this exact package/repository before any new session; multiple or unowned receipts require operator resolution.
- Login through final publication/rejection must be one live, bounded, credential-owning guarded transaction. Prefer an operator-owned native terminal with EXIT/HUP/INT/TERM logout/owned-cleanup traps and an explicit bounded idle/deadline policy; keep that guard active across every authentication, authority-check and publication decision. A disposable guard procedure is operational scratch, not a new permanent credential manager or approval framework.
- Arm revocation before login can create a session. Every canceled/failed login, failed authority check, rejection, timeout, normal exit and signal enters the same supported logout/revocation path. Do not relay unfiltered live npm login/profile output through captured harness logs. A guard cannot claim to handle SIGKILL, machine loss or an unreachable provider automatically; those cases are explicitly unresolved and use the persisted recovery receipt.
- On any resumed execution, recover this exact receipt and resolve/revoke its isolated session before another login or stable setup. Validate private-root ownership, no symlinks/path escape and fixed relative children; never kill an unverified process or touch the user's pre-existing npm/GitHub state. If ownership or server invalidation cannot be established, stop for the operator rather than guessing or claiming cleanup.
- The guard owns the authentication lifetime even while the agent waits for human input. Actual publication authority remains a distinct exact-value decision; it is never inferred from starting the guarded login. It may be entered in that private operator transaction and preserved verbatim as a bounded authorization receipt, without exposing credentials.
- Keep the receipt until actual revocation and owned cleanup are confirmed. If logout/provider access fails, preserve a private blocked recovery state as needed for targeted revocation; do not destroy the only recovery route and then report server success. Completion still requires the correctly labeled real revocation result.
- Before handling real credentials, exercise the chosen disposable guard with harmless stand-in commands for canceled login, authority failure, rejection, idle timeout, EXIT/INT/TERM and resumed recovery. These simulations validate cleanup mechanics only, never publication or real account authority.
</bootstrap_auth_lifetime>

<tasks>

<task type="auto">
  <name>Task 1: Prepare the configured bootstrap before persisting source-review records</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-SOURCE-PUBLICATION-REVIEW.md, .planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-EVIDENCE.json</files>
  <read_first>
    - `05-01-SUMMARY.md` and `05-03-SUMMARY.md` — completed code/workflow/policy and actual verification
    - `05-CONTEXT.md` — D-01 through D-09 and the unchanged reviewed origin fingerprint
    - `.planning/phases/03-distribution-contract-legal-boundary/03-PUBLICATION-REVIEW.md` — source review convention and historical baseline
    - `scripts/pack-runtime.mjs`, `scripts/verify-production-artifacts.mjs`, `tests/package/agent-ready-export.test.ts`, `tests/helpers/runtime-artifact.ts` — actual bootstrap command/environment contracts
    - `.github/workflows/deploy-supabase-production.yml`, `package.json`, `package-lock.json`, `LICENSE`, `THIRD_PARTY_NOTICES.md` — source, legal and collateral-deployment boundaries
  </read_first>
  <action>Only during authorized phase execution, first finish/commit plan-owned bookkeeping and require a clean main containing completed Plans 05-01 through 05-03. Inspect the actual public main, repository identity, fast-forward relationship, branch/ruleset/check restrictions and every unpublished changed path/history object; do not assume the old Phase 3 SHA is still current. Apply the existing source-rights/sensitive-material review to this exact range. Check the fixed bootstrap version is authoritatively absent and record the current latest tag or authoritative absence. Errors are not vacancy.

Keep preliminary review data and all temporary output outside the checkout in caller-held mode-0700 scratch. Do not write/commit the canonical review/evidence files yet. Create a dedicated `[skip ci]` boundary commit if the current verified tip lacks the marker; an empty boundary commit changes no reviewed tree bytes. Capture this actual clean source SHA P and tree T. No checkout/reset/rebase/branch/worktree operation is permitted. From this point through the end of producer and installed acceptance, make no Git commits or tracked-source changes.

After creating or selecting P, extend the review to its actual commit object and final unpublished range: author/committer identities and timestamps, message, parents, signature metadata, newly exposed object reachability and sensitive/rights findings. Reuse already reviewed unchanged tree objects by their hashes, but do not treat tree equality as approval of new commit metadata. Compute the final reviewed-object/range digest including P before any source authorization; record that post-P result in the eventual source review.

Capture the existing GitHub production variable in process memory, require canonical form and fingerprint `89485617b2d50d4778542ebedc3817a3e3fcddb6520a4a9c3a66e37c3a9c6cdf`, and pass its derived URL only through child environment `CUMPA_RELEASE_SUPPORT_SERVICE_URL`. A changed value stops for a separate configuration decision. Do not create `.env`, copy the value into another environment, or display it.

Choose new outside-checkout custody and evidence destinations that do not exist. Execute one `pack-runtime.mjs --purpose bootstrap --custody-dir ... --evidence ...` cycle. Inspect with `verify-production-artifacts.mjs --profile bootstrap --archive ... --expected-sha256 ... --evidence ...`; these are the only scanner arguments. Execute the existing argument-free `npm run accept:runtime-artifact` with caller-held `CUMPA_RUNTIME_CUSTODY_DIR`, `CUMPA_RUNTIME_ARCHIVE_BASENAME`, `CUMPA_RUNTIME_ARCHIVE_SHA256`, `CUMPA_RUNTIME_EVIDENCE`, `CUMPA_RUNTIME_ACCEPTANCE_REPORT`, `CUMPA_RUNTIME_PROFILE=bootstrap` and the protected support URL in process environment. No invented archive/hash CLI options are forwarded to Vitest. Require exact source P/T, unchanged source package/lock, version-only projection, legal/dist/browser/worker/codicon coverage, V2/V3 completion, unavailable-support dismissal, cleanup and real Darwin ARM64 native re-export. Observe the actual binary; no fixture result authorizes an operational artifact.

Make the actual custody archive read-only and independently compare SHA-256, SHA-1, SHA-512 SRI and length again. Only after all source-sensitive preparation is finished, persist the bounded `05-BOOTSTRAP-EVIDENCE.json` with unchanged producer core plus verified scanner/acceptance observations, and `05-SOURCE-PUBLICATION-REVIEW.md` naming P/T, actual pre-push remote state, reviewed paths/history, legal/input hashes, vacancy and planned literal refspec. Preserve object ordering and hash the final evidence. Remove created scratch/report state, not the candidate. Subsequent bookkeeping commits may advance local main; P remains the candidate source and sole proposed public push target. Never return/reset the checkout to P.</action>
  <verify>
    <automated>With the private caller environment populated as described: node scripts/verify-production-artifacts.mjs --profile bootstrap --archive "$BOOTSTRAP_ARCHIVE" --expected-sha256 "$BOOTSTRAP_SHA256" --evidence "$BOOTSTRAP_PRODUCER_EVIDENCE", followed by npm run accept:runtime-artifact. Capture outputs privately and report only bounded results; independently assert source P/T, final hashes/length and read-only custody before writing canonical records.</automated>
  </verify>
  <acceptance_criteria>A real newly built bootstrap passes the existing scanner/installed contracts from clean P/T before any review-file commit can change source HEAD. Canonical records preserve P/T and actual byte identities; the current checkout is never reset or switched, and no publication or authentication has occurred.</acceptance_criteria>
  <done>The private read-only candidate and exact source review exist for separate owner decisions.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 2: Authorize the exact source publication action</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-SOURCE-PUBLICATION-REVIEW.md</files>
  <read_first>`05-SOURCE-PUBLICATION-REVIEW.md`; actual current public ref/ruleset observations; D-03 and D-05.</read_first>
  <action>Display actual P/T, observed public-main precondition, complete reviewed range, source-review digest, literal P-to-main refspec, existing credential capability and `[skip ci]` collateral-workflow result expected. Stop until the owner authorizes precisely that source push. Existing authorized GitHub authentication may be used; do not demand or create another token merely to satisfy a planning assumption.</action>
  <what-built>The reviewed source and bootstrap candidate exist locally. No remote mutation has occurred.</what-built>
  <how-to-verify>Review the displayed source exposure and target. This authorization covers only this exact push; it does not authorize npm login, artifact acceptance or registry publication.</how-to-verify>
  <verify><human>Require the actual source SHA/tree, observed public-main SHA and review digest in the attributable authorization. Generic or mismatched assent does not resume.</human></verify>
  <acceptance_criteria>Actual owner authority binds the one reviewed source target and no broader action.</acceptance_criteria>
  <resume-signal>Reply with the executor-rendered exact source authorization or stop; no template token qualifies.</resume-signal>
  <done>The actual source-push authority is recorded, or the plan remains stopped without remote mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Push the recorded source SHA without collateral deployment</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-SOURCE-PUBLICATION-REVIEW.md</files>
  <read_first>`05-SOURCE-PUBLICATION-REVIEW.md`; Task 2 response; `.github/workflows/deploy-supabase-production.yml`.</read_first>
  <action>Re-observe the remote/ref/ruleset conditions and require the authorized precondition still holds. Push only the literal recorded P to `refs/heads/main`, without force, current-HEAD shorthand, another ref or a policy change. If current protection prevents the safe route, stop for owner resolution. Local evidence commits after P are not pushed implicitly.

Observe public commit/tree/file identity and Actions/environment deployment state for this target. Corroborate documented `[skip ci]` suppression with actual empty/skipped/no-deployment observations; do not cancel or modify unrelated runs. Record the actual outcome and bounded timestamps/identities. Preserve pre-existing GitHub login/keys. Revoke or remove only credentials/state explicitly created for this plan and owned by it; do not log out the user's existing GitHub session.</action>
  <verify><automated>Privately compare observed public main/tree with recorded P/T and inspect Actions/deployment results for P. Require zero collateral Supabase deployment for this push and preserve the candidate/evidence hashes.</automated></verify>
  <acceptance_criteria>The exact reviewed P/T is public, no Supabase mutation was caused, no broader ref was pushed and no unrelated credential was removed.</acceptance_criteria>
  <done>The candidate's actual source is publicly available without authorizing npm operations.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Approve the actual bootstrap artifact</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-APPROVAL.md</files>
  <read_first>`05-BOOTSTRAP-EVIDENCE.json`; completed source publication record; independently rehashed read-only custody.</read_first>
  <action>Display the actual package/version, all archive hashes and length, evidence digest, source P/T, legal hashes, configured-origin fingerprint, installed results, native observation and limitations. Stop for exact attributable artifact approval. Do not pre-fill an approval record or infer publication authority.</action>
  <what-built>One real configured read-only `1.5.0-bootstrap.0` candidate passed complete installed acceptance.</what-built>
  <how-to-verify>Compare the actual displayed identities with the archive and evidence. This is local bootstrap-byte approval, not `latest`, CI provenance, npm login or publication.</how-to-verify>
  <verify><human>Require actual package/version, SHA-256, byte length, evidence digest and explicit limitations acknowledgement.</human></verify>
  <acceptance_criteria>Only the displayed actual bytes receive approval; a generic or mismatched response leaves the gate closed.</acceptance_criteria>
  <resume-signal>Reply with the executor-rendered BOOTSTRAP ARTIFACT APPROVED sentence containing real values and limitations, or identify the blocker.</resume-signal>
  <done>The actual bootstrap byte identity and limitations receive attributable approval, or authentication remains prohibited.</done>
</task>

<task type="auto">

  <name>Task 5: Seal approval and prepare empty isolated npm state</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-APPROVAL.md</files>
  <read_first>`05-BOOTSTRAP-EVIDENCE.json`; Task 4's actual response; the existing isolated-environment pattern in `tests/helpers/runtime-artifact.ts`.</read_first>
  <action>Rehash the same archive/evidence after assent and confirm read-only mode. Write the approval once with actual identities, source/legal facts, bounded acceptance, approver/time and verbatim response; seal it without changing archive or evidence. Create the fresh private relative auth HOME/userconfig/cache/prefix and instantiate the live guarded transaction plus persisted recovery receipt from `<bootstrap_auth_lifetime>` before login. Keep its EXIT/HUP/INT/TERM handling and bounded idle/deadline policy active across subsequent human waits. Do not copy user npmrc, inherit publication credentials or expose private paths/config. No login or publish happens yet; an in-memory cleanup promise is insufficient.</action>
  <verify><automated>Privately assert the sealed approval binds the unchanged read-only archive/evidence and the new isolated npm state is empty and plan-owned.</automated></verify>
  <acceptance_criteria>Approval is attributable and sealed before authentication; isolated state cannot reuse or delete unrelated user credentials.</acceptance_criteria>
  <done>The candidate is approved and the empty authentication context is ready for a separate user action.</done>
</task>

<task type="checkpoint:human-action" gate="blocking-human">

  <name>Task 6: Authorize and complete isolated interactive npm login</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-PUBLICATION.json</files>
  <read_first>`05-BOOTSTRAP-APPROVAL.md`; the caller-held isolated environment description and cleanup obligation.</read_first>
  <action>Authorize this one temporary interactive npm authentication only, inside the already active credential-owning guarded transaction. The operator completes native browser/2FA without posting URLs, codes, tokens or raw config into chat/evidence. No publication is queued. A lost/unresponsive guard or ambiguous recovery state requires targeted recovery/revocation first. Every cancellation or failure enters the guard's owned cleanup path, not an unguarded agent subprocess.</action>
  <what-built>The artifact is approved and a fresh isolated authentication context is prepared.</what-built>
  <how-to-verify>Confirm expected account/scope/purpose and complete the provider's interactive flow. Login authorizes no publication.</how-to-verify>
  <verify><human>Record successful scoped authentication without retaining its credential or login URL; otherwise perform owned cleanup and stop.</human></verify>
  <acceptance_criteria>A short-lived isolated session, not an automation token or inherited login, is used only for the bootstrap process.</acceptance_criteria>
  <resume-signal>Report the completed isolated authentication and public account identity, never a token, code or URL.</resume-signal>
  <done>The isolated interactive session is established under its own authority, or its created state is revoked/cleaned and work stops.</done>
</task>

<task type="auto">
  <name>Task 7: Observe account authority, 2FA and current vacancy privately</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-PUBLICATION.json</files>
  <read_first>`05-BOOTSTRAP-APPROVAL.md`; Task 6 response; current supported npm identity/profile/organization commands.</read_first>
  <action>Capture authenticated npm identity, relevant organization/package-write authority and publication-compatible 2FA through supported CLI/API/UI reads inside the isolated context. Filter results in memory to the target account/scope and needed booleans; never print raw `npm profile get --json`, organization membership or config output. Successful login alone is not package-creation authority. Re-observe exact version vacancy and prior latest state, then rehash the approved identities. A missing privilege, collision, changed bytes, error, stop or interruption enters owned-session revocation/cleanup rather than leaving authentication alive.</action>
  <verify><automated>Use captured supported npm reads and report only the observed target account, target write/2FA/vacancy booleans, unchanged identities and prior latest. Require every necessary precondition; do not substitute shell tests of unpopulated variables for observations.</automated></verify>
  <acceptance_criteria>Actual account/scope/2FA and vacancy are established privately; the approved bytes remain unchanged and no mutation has occurred.</acceptance_criteria>
  <done>Only the irreversible publication decision remains before a bootstrap attempt.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 8: Authorize one exact non-latest bootstrap publication</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-PUBLICATION.json</files>
  <read_first>`05-BOOTSTRAP-EVIDENCE.json`; sealed approval; actual account/2FA/vacancy/latest observations.</read_first>
  <action>Display the actual fixed package/version, archive SHA-256/length, evidence/approval digests, public source, target account/authority, `bootstrap` tag, public access and one-attempt/revocation policy. Stop for a separate exact publication authorization. Rejection immediately revokes/cleans the plan-owned login and leaves the registry untouched.</action>
  <what-built>The exact artifact is approved and current authenticated preconditions were observed.</what-built>
  <how-to-verify>Authorize only the approved `.tgz` with `--tag bootstrap --access public --ignore-scripts` against public npm. No directory, different version/tag, latest assignment, blind retry or unpublish is authorized.</how-to-verify>
  <verify><human>Require the actual package/version, SHA-256, byte length, tag/access and observed account/authority in the attributable response.</human></verify>
  <acceptance_criteria>One irreversible attempt is explicitly authorized; artifact approval or login is not reused as permission.</acceptance_criteria>
  <resume-signal>Reply with the executor-rendered exact BOOTSTRAP PUBLICATION AUTHORIZED statement or stop and revoke.</resume-signal>
  <done>One exact bootstrap publication attempt is authorized, or the owned session is revoked without publication.</done>
</task>


<task type="auto">
  <name>Task 9: Attempt publication once, revoke, and verify public bytes without credentials</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-PUBLICATION.json, .planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-AUTH-REVOCATION.md</files>
  <read_first>Sealed bootstrap evidence/approval; Task 8 response; operative distribution policy; the supported npm logout/revocation mechanism.</read_first>
  <action>Immediately compare actual identities and current preconditions with the authorization. Within the live guarded authentication lifetime and outside any source repository, execute exactly one `npm publish` of the approved absolute `.tgz` with `--tag bootstrap --access public --ignore-scripts --fetch-retries=0 --registry https://registry.npmjs.org/` and the isolated userconfig. Capture only bounded outcome facts and notify the credential-owning guard of the terminal result so revocation starts immediately, before public consumer verification.

Use a finally path to run supported revocation/logout for this created session and remove only owned auth/config/cache/prefix state after the attempt, including network error, interruption or ambiguity. Preserve candidate/evidence/approval. Never log out or delete unrelated user state. Record actual provider result, local absence and any unresolved revocation outcome without promoting local deletion into server proof.

Then use separate credential-free temporary consumers for public reconciliation. Resolve the exact public version and tag, download its actual registry `.tgz`, and compare length/SHA-256/SHA-1/SHA-512 with the approved bootstrap. Require bootstrap points to the fixed version and latest remains unchanged. Perform a normal exact global install from the public registry into a fresh isolated prefix and observe its generated `cumpa --version` as `1.5.0-bootstrap.0`; do not hide an install-lifecycle failure with a local tarball fallback. Clean created consumer/download state.

For an ambiguous attempt, inspect exact public state read-only: matching published bytes can be verified, authoritative absence leaves a blocked attempt requiring new authority and a fresh session, and different bytes/metadata is a blocking incident. Never automatically retry publication, unpublish or rebuild. Bounded publication/revocation records distinguish all outcomes.</action>
  <verify><automated>Observe exact public packument/tag, independently hashed downloaded bytes, generated global binary output, provider revocation/logout result and owned-state absence. All evidence is captured through isolated private processes; no raw auth/profile/log payload is printed.</automated></verify>
  <acceptance_criteria>A success requires real registry equality and usability with latest unchanged; every attempted or abandoned session reaches scoped revocation/cleanup, including ambiguous/failing paths.</acceptance_criteria>
  <done>The actual bootstrap outcome is known or safely blocked, and no temporary session is silently carried into stable setup.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 10: Confirm the actual temporary authorization is revoked</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-AUTH-REVOCATION.md</files>
  <read_first>Bounded provider logout/revocation and local cleanup observations; public bootstrap outcome.</read_first>
  <action>Present actual logout/provider results, owned local absence and any unresolved status. Ask the owner to confirm revocation through the provider's supported security surface without sharing credential details. Record operator confirmation separately from independently observed server proof; any surviving session or unresolved cleanup blocks stable setup.</action>
  <what-built>The attempt and public reconciliation are recorded; owned-session cleanup was attempted on every path.</what-built>
  <how-to-verify>Confirm only what was actually observed. Local file deletion alone is not server-side revocation.</how-to-verify>
  <verify><human>Require explicit confirmation tied to this bootstrap session/purpose and accurate observation level, or retain a blocking failure.</human></verify>
  <acceptance_criteria>The temporary bootstrap authorization is actually revoked, with no inflated assurance claim.</acceptance_criteria>
  <resume-signal>Confirm the executor-rendered revocation facts and observation level, or identify any remaining session/state.</resume-signal>
  <done>Actual scoped revocation is confirmed at the recorded assurance level, or stable setup remains blocked.</done>
</task>

<task type="auto">
  <name>Task 11: Seal the actual bootstrap outcome and revocation record</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-PUBLICATION.json, .planning/phases/05-bootstrap-trusted-stable-publication/05-BOOTSTRAP-AUTH-REVOCATION.md</files>
  <read_first>Collected public equality/usability facts; Task 10 response; actual source publication and sealed artifact approval records.</read_first>
  <action>Validate the collected cross-record identities and append the exact attributable revocation response/time, preserving the distinction between provider results, local cleanup, operator confirmation and independent server observation. Do not rerun checks merely to contradict or reconfirm the operator's report. Seal the bounded publication/revocation records and record their digests in the summary. Mark REL-01 successful only when exact public bytes/tag/usability, no collateral source deployment and actual revocation/cleanup all succeeded. Otherwise retain the real blocker; no stable setup, replacement credential or remote repair is implied.</action>
  <verify><automated>Check bounded record/schema and identity consistency, exact approval bindings, observed successful public outcome and completed revocation/owned cleanup. Preserve all immutable candidates and historical records.</automated></verify>
  <acceptance_criteria>The completed records support REL-01 without fictitious provenance, credential reuse, hidden retries or server-revocation overclaims.</acceptance_criteria>
  <done>The usable public bootstrap is complete and its temporary authorization is gone, or stable work remains explicitly blocked.</done>
</task>

</tasks>

<threat_model>
## Enforcement
ASVS Level 1 applies. Every HIGH finding blocks completion or forces an accurately recorded terminal outcome; approval, publication success or schedule cannot waive a HIGH control.

## Trust Boundaries
| Boundary | Description |
|---|---|
| Reviewed local tree -> public GitHub main | One exact authorized commit/tree crosses a public irreversible boundary and ordinarily triggers production workflows. |
| GitHub production variable -> local build process | Protected configuration is used in process memory; only its approved fingerprint may cross into durable evidence. |
| Read-only bootstrap custody -> human approval | Scanner/install observations and exact digests cross from automated evidence into an explicit human artifact decision. |
| Temporary npm authentication -> registry mutation | Account/scope/2FA authority must be observed before one exact irreversible publish attempt. |
| Public npm registry -> publication evidence | Registry metadata/downloads are untrusted until independently compared with approved local bytes and fixed tags. |
| Attempted publication -> revocation/cleanup | Success, failure and ambiguity all enter the same mandatory logout and owned-state cleanup path. |

## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-05-04-ST | Spoofing / Tampering | source publication target | HIGH | mitigate | Full SHA/tree review, explicit refspec, remote precondition, no current HEAD/force and public post-push object comparison. |
| T-05-04-SD | Tampering / Denial of Service | Supabase production workflow | HIGH | mitigate | Dedicated [skip ci] tip, preflight rules/checks, no workflow edits, and actual Actions/deployment observation before/after push. |
| T-05-04-CF | Information Disclosure | production support configuration | HIGH | mitigate | Retrieve authorized variable in process memory, canonical validation/fingerprint match, redacted output and no `.env`/npm-release copy. |
| T-05-04-AS | Spoofing / Tampering | bootstrap artifact approval | HIGH | mitigate | Read-only custody, repeated hashes/length/evidence digest, exact rendered human response and immutable approval before npm login. |
| T-05-04-HF | Tampering | historical Phase 4 artifact | HIGH | mitigate | Explicitly excluded from producer/publish inputs; immutable history is neither fallback nor deletion target. |
| T-05-04-NC | Spoofing | npm package/version vacancy | HIGH | mitigate | Public checks before source boundary, build, login and publish; collision stops for owner resolution without bump/unpublish. |
| T-05-04-CR | Information Disclosure / Elevation of Privilege | GitHub and npm authentication | HIGH | mitigate | Use GitHub authentication only within actual authorized capability and preserve pre-existing sessions; npm uses a fresh isolated plan-owned session. Never print credentials, raw profiles or login URLs; revoke/clean only owned credentials on every terminal path. |
| T-05-04-AU | Spoofing / Elevation of Privilege | npm identity, role and 2FA | HIGH | mitigate | Authenticated whoami/profile/org/package-creation authority checks; login alone cannot authorize publish. |
| T-05-04-PA | Repudiation / Tampering | human authorizations | HIGH | mitigate | Distinct execution-time gates repeat actual SHA/tree/digests/identity/action; placeholders/generic assent/mismatch fail. |
| T-05-04-RT | Tampering | bootstrap dist-tags | HIGH | mitigate | Explicit bootstrap tag/public access, snapshot and require latest unchanged, never publish directory or allow arbitrary version. |
| T-05-04-AR | Tampering / Repudiation | ambiguous npm publish | HIGH | mitigate | No automatic retry; exact registry query then absent->new authorization, present->download equality, mismatch->blocking incident. |
| T-05-04-RV | Elevation of Privilege / Repudiation | Temporary npm authorization across human waits | HIGH | mitigate | Credential-owning guarded transaction is armed before login and persists across decisions; private recovery receipt, bounded waits and EXIT/HUP/INT/TERM handling; cleanup-first recovery after hard loss, no false automatic-revocation claim, and separate operator/server observations. |
| T-05-04-EV | Information Disclosure | durable operational records | HIGH | mitigate | Bounded schemas, public/fingerprinted identities only, no credentials/origin/private paths/raw logs and post-write secret scan. |
| T-05-04-NR | Tampering | installed runtime/native behavior | HIGH | mitigate | Exact archive scan and full installed browser/runtime contract; Darwin ARM64 requires observed native re-export before approval. |
</threat_model>

<artifacts_this_phase_produces>
## Files
- `05-SOURCE-PUBLICATION-REVIEW.md`: `reviewedSourceSha`, `reviewedTreeSha`, `observedPublicMainSha`, changed-path/object hashes, legal/source-manifest hashes, exact refspec, branch/ruleset/check observations, source authorization attribution/time, public post-push equality, Actions run/attempt/deployment observations and source-credential revocation/cleanup booleans.
- `05-BOOTSTRAP-EVIDENCE.json`: unchanged Plan 05-01 producer core plus bounded verified scanner/acceptance observations for the actual bootstrap; preserves source/projection/archive identities and origin fingerprint, with no human approval claim before assent.
- `05-BOOTSTRAP-APPROVAL.md`: exact archive/evidence/source/legal/acceptance values, approver/time/verbatim rendered decision, limitations and explicit no-provenance/no-latest scope.
- `05-BOOTSTRAP-PUBLICATION.json`: bounded schema for authenticated public account/scope role/2FA, prior tags/vacancy, exact one-attempt authorization, npm outcome, ambiguity reconciliation, final packument/download identities, tag/latest result and generated-bin usability.
- `05-BOOTSTRAP-AUTH-REVOCATION.md`: logout/revocation action and time, local auth/config/cache/prefix absence, plan-owned cleanup count, operator observation and separately labeled independent server invalidation result.
- Private execution-only bootstrap recovery receipt and disposable guard: opaque operation ID, fixed relative owned paths, approved identities, guard/deadline and revocation/cleanup state; no credential copy, public upload, repository commit or new permanent auth framework.

## Consumed commands and flags
- Producer: `scripts/pack-runtime.mjs --purpose bootstrap`.
- Scanner: `scripts/verify-production-artifacts.mjs --profile bootstrap`.
- Acceptance: existing `accept:runtime-artifact` entry point with `CUMPA_RUNTIME_PROFILE=bootstrap`.
- Publication: one exact archive operand with `npm publish`, explicit `--tag bootstrap --access public --ignore-scripts --registry https://registry.npmjs.org/`, isolated userconfig/HOME and a working directory outside any source repository.
- Registry verification: supported npm public metadata/download/install commands plus Node/standard checksum reads against the approval; `scripts/verify-npm-release.mjs` remains stable-only.
</artifacts_this_phase_produces>

<verification>
- Public main resolves to the exact authorized reviewed SHA/tree, and source/legal/package/lock bytes match the source review.
- Actual GitHub Actions/environment/deployment observations prove the source push did not execute the Supabase production deployment workflow; [skip ci] text alone is insufficient.
- `05-BOOTSTRAP-EVIDENCE.json`, tarball and approval recompute to the exact execution-derived hashes/length and remain read-only.
- The bootstrap scanner and full installed acceptance run against the exact newly produced archive and actual configured origin; Darwin ARM64 native behavior is observed.
- Authenticated npm account/scope role, package creation authority, 2FA and version vacancy are observed before a distinct exact publication authorization.
- Public packument/tag/download bytes and generated binary equal the approved bootstrap; `latest` remains the pre-publish value.
- Logout/revocation executes for all attempted/ambiguous outcomes, isolated credentials/config/cache/prefix and plan-owned temporary paths are absent, and server invalidation is not overstated.
- All five durable artifacts pass bounded-schema/private-data scans and preserve Phase 04 bytes untouched.
</verification>

<success_criteria>
- REL-01 is complete: one complete public MIT `@shipwithai/cumpa@1.5.0-bootstrap.0` is usable under `bootstrap`, exact-byte approved/published/verified, and does not occupy `latest`.
- The exact completed Phase 5 source tip is public without collateral Supabase deployment, and later local evidence commits never alter the pushed target.
- D-01 through D-09 are evidenced by actual observations and separate resource/identity/permission/irreversible gates, not planning assertions.
- Temporary npm authority and any separately created plan-owned GitHub authority are revoked/cleaned at their boundary; pre-existing user GitHub sessions are preserved. Incomplete revocation or any HIGH finding blocks stable setup.
- No new version, retry, unpublish, workflow bypass, generic release abstraction, credential/config value, private custody path or optimistic provenance claim appears.
</success_criteria>

<output>Create `.planning/phases/05-bootstrap-trusted-stable-publication/05-04-SUMMARY.md` after execution with exact artifact file digests, source/publication run observations and unresolved blockers. Do not claim REL-01 or proceed to stable unless every success criterion, including revocation, is actually observed.</output>
