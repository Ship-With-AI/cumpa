---
phase: 05-bootstrap-trusted-stable-publication
plan: 05
type: execute
wave: 4
depends_on: [05-04]
files_modified:
  - .planning/phases/05-bootstrap-trusted-stable-publication/05-CI-ARTIFACT-APPROVAL.md
autonomous: false
requirements: [REL-02]
must_haves:
  truths:
    - "Per D-03/D-08, actual npm scope/package authority, 2FA, direct trusted-publisher relation, and native protected `npm-release` environment are verified and configured only after explicit setup authorization."
    - "Per D-05, only the exact reviewed public `main` commit from 05-04 may be dispatched; the temporary expected-source variable and the actual run/head observation both bind it."
    - "Per D-06, the authoritative production project reference is read in memory, fingerprint-checked, converted to the canonical origin, installed only as an absent-only temporary masked production secret, and never exposed or duplicated."
    - "Per D-01/D-07, the authorized run actually produces, scans, installs, seals, and uploads one new GitHub-hosted Darwin ARM64 candidate before any human artifact approval."
    - "Per D-08, after the candidate job terminates both owned temporary production values are deleted and proven absent; the exact artifact ID is downloaded, transport/evidence/archive identities are independently checked, local custody is read-only, and attributable assent binds the actual new bytes and limitations."
    - "Per D-02, approval supersedes only the old Phase 4 archive's prospective publication designation; Phase 4 archive/evidence/approval remain immutable history and never become fallback."
  prohibitions:
    - "HIGH: No registry, trusted-publisher, environment-protection, production secret/variable, workflow-dispatch, artifact-download, or approval mutation without its named execution-time authority and verified actor/resource scope."
    - "HIGH: Never overwrite a pre-existing `CUMPA_RELEASE_BUILD_ORIGIN` secret or `CUMPA_RELEASE_SOURCE_SHA` variable, delete unrelated configuration, alter `SUPABASE_PROJECT_REF`, persist the origin/project reference, or copy either temporary value into `npm-release`."
    - "HIGH: Never log/display raw origin/project reference, place it on argv/history/evidence, create `.env`, store private custody paths, or treat late masking as sufficient after interpolation."
    - "HIGH: Artifact approval does not approve npm publication, the protected environment, another run/attempt/candidate, a rebuild, or registry/transparency mutation."
    - "No `npm publish`, environment deployment approval, automatic retry/rerun, manual artifact construction, upload replacement, old Phase 4 fallback, or source/config/protection weakening in this plan."
    - "No rewrite, deletion, annotation, or digest change to Phase 4 archive/evidence/approval or existing `.gsd`/legacy artifacts."
  artifacts:
    - path: ".planning/phases/05-bootstrap-trusted-stable-publication/05-CI-ARTIFACT-APPROVAL.md"
      provides: "Attributable exact new candidate/evidence/source/run/attempt/native-artifact approval with limitations and no stable publication authority"
      contains: "CI CANDIDATE APPROVED"
  key_links:
    - from: "05-04 reviewed public source SHA"
      to: "CUMPA_RELEASE_SOURCE_SHA and workflow run"
      via: "absent-only temporary variable, read-back, input-free dispatch, first-step guard, and actual run head check"
      pattern: "CUMPA_RELEASE_SOURCE_SHA|sourceSha"
    - from: "production SUPABASE_PROJECT_REF"
      to: "build-candidate producer step"
      via: "in-memory canonical derivation and absent-only masked transient `CUMPA_RELEASE_BUILD_ORIGIN` secret"
      pattern: "89485617b2d50d4778542ebedc3817a3e3fcddb6520a4a9c3a66e37c3a9c6cdf"
    - from: "build-candidate upload outputs"
      to: "05-CI-ARTIFACT-APPROVAL.md"
      via: "exact artifact-ID download plus transport, sealed-evidence, archive, source, run and attempt checks"
      pattern: "artifact-id|evidence-sha256|archive-sha256"
---

<objective>
Establish the exact protected trusted-publishing boundary, authorize and supervise one input-free fresh-candidate run, clean its owned temporary production transports, and approve the actual downloaded new artifact without yet authorizing stable publication.

Purpose: Turn the fixed source/workflow design into one inspected digest-bound candidate while keeping configuration, CI, artifact approval, and irreversible registry authority separate per D-03 and D-08.
Output: Verified remote trusted-publisher/environment configuration, one pending same-run publish job, cleaned temporary production configuration, one read-only local copy of the exact artifact payload, and `05-CI-ARTIFACT-APPROVAL.md`.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-CONTEXT.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-RESEARCH.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-PATTERNS.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-01-SUMMARY.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-02-SUMMARY.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-03-SUMMARY.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-04-SUMMARY.md
@.planning/phases/04-exact-runtime-tarball/04-04-SUMMARY.md
@.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-EVIDENCE.json
@.planning/phases/04-exact-runtime-tarball/04-ARTIFACT-APPROVAL.md
@.github/workflows/publish-npm.yml
@scripts/verify-npm-release.mjs
@docs/distribution-operations.md
</context>

<interfaces>
- Fixed trusted publisher is npm package/scope `@shipwithai/cumpa`, GitHub owner/repository `Ship-With-AI/cumpa`, workflow filename `publish-npm.yml`, and environment `npm-release`. The connection must explicitly allow direct `npm publish`; staging-only default is not acceptable.
- Before configuration, verify actual authenticated npm/GitHub identities, scope/package rights, account 2FA, public repository ID `1327753770`, exact stable version `1.5.0` absence, bootstrap outcome/revocation/cleanup, workflow source on public `main`, and current native environment-protection capabilities. Missing identity/permission/resource information creates a dynamic authentication/action gate; it never becomes an assumption.
- `npm-release` must require a named authorized reviewer for the publish deployment, restrict deployment to `main`, prevent administrative bypass when available, and grant no support-production secret. Record actual supported settings; if native review protection is unavailable or cannot gate this exact job, stop for owner action rather than replace it with a file/label/comment convention.
- Configuration-setup authority is separate from CI build/upload authority. Both authorizations name exact resources and changes; neither grants stable publication.
- Exact source `P` comes from 05-04's separately authorized, reviewed, public-main tip. Confirm remote `main === P`, required checks/history match the source review, and no collateral Supabase deployment occurred before configuring or dispatching CI.
- Before CI dispatch, prove both `production` names `CUMPA_RELEASE_BUILD_ORIGIN` and `CUMPA_RELEASE_SOURCE_SHA` absent. Read authoritative `SUPABASE_PROJECT_REF` into local process memory without terminal/log persistence, require canonical project-reference form, derive `https://<ref>.supabase.co`, and require SHA-256 `89485617b2d50d4778542ebedc3817a3e3fcddb6520a4a9c3a66e37c3a9c6cdf`. A changed fingerprint stops for separate owner resolution.
- Before creating either value, require no queued, pending, waiting or in-progress run of this workflow and no unresolved earlier operation journal. Create the absent source variable first with the native environment-variable POST create endpoint, not an upsert; its name is a narrow admission lease. Read back P. Recheck secret absence under that lease, then send the origin by stdin to native `gh secret set`. Never overwrite a pre-existing value or use the workflow token for administration.
- Dispatch once on `main` with no inputs. Resolve exactly one new authorized run by workflow/event/actor/time/P, then require actual run ID, attempt 1 and first source guard. Monitor for competitors until owned transports are gone. Unexpected runs or ambiguous ownership stop all artifact/publication approval; do not select the newest run, silently retry, or cancel unrelated work.
- Cleanup follows the recovery contract below, including partial creation, no-run/no-job failure, ambiguous dispatch, competing runs, cancellation and context loss. It is not conditioned on a nonexistent job becoming terminal. The authoritative `SUPABASE_PROJECT_REF` is never changed.
- Successful build outputs bind `artifact-id`, `artifact-digest`, `evidence-sha256`, `archive-sha256`, and `archive-byte-length`. Require the `publish` job to remain pending on `npm-release`, not started/bypassed.
- Download the native artifact by exact numeric ID into a new private outside-checkout custody leaf. Validate the downloaded transport digest, exactly two payload files, sealed evidence SHA-256, archive byte length/SHA-256/SHA-1/SHA-512 SRI, fixed package/version, producer/scanner/acceptance/source/run/attempt/platform/arch facts, source public alignment, and every passed check. Make both payload files read-only and rehash again. Keep the private custody path out of evidence/approval.
- Approval phrase: `CI CANDIDATE APPROVED: @shipwithai/cumpa@1.5.0 sha256 <actual-64-hex> bytes <actual-integer>; evidence sha256 <actual-64-hex>; source <actual-40-hex>; run <actual-id> attempt <actual-integer>; artifact <actual-id> digest <actual-64-hex>; limitations acknowledged; stable publication not authorized`.
- Record actual run `createdAt`, artifact expiry and pending-deployment timing when available. Derive a conservative approval deadline no later than run creation + 30 days and a total-run deadline at creation + 35 days; record the basis rather than inventing an API-provided expiry. Validity ends at the earliest provider/artifact bound. Before candidate or stable assent, show remaining validity and require enough time for the configured 15-minute publish job. Expiry stops this attempt and needs a newly authorized candidate cycle, not a rerun.
</interfaces>

<recovery_contract>
- Before any remote write, create an opaque-ID directory under `~/.local/share/cumpa/release-operations/ci/` with mode 0700 and atomically persist mode-0600 `operation.json`. Record repository/workflow/P, authorizer reference, fixed resource names, each effect's intent/response/ownership and metadata times, dispatch time, run/attempt/job IDs and deadlines. No token, origin/project reference, actual custody path or raw response is retained. Resume by inspecting this exact repository's nonterminal receipts; multiple/ambiguous receipts block new setup. This is one private recovery receipt, not a generic state-machine framework.
- Persist intent before each remote effect and the observed response/ownership immediately afterward. Arm EXIT/INT/TERM cleanup as soon as a resource can have been created. Use a supervised finite operation or operator-owned guarded terminal; context loss must not leave ownership only in model/kernel memory.
- For partial setup or a definitively failed dispatch with no possible run/job, immediately remove each value proven created by this operation and verify absence. No nonexistent job is awaited. If a request may have succeeded but its identity/ownership is uncertain, reconcile read-only; otherwise stop for owner resolution and never guess ownership from a matching name alone.
- With an exact active candidate job, retain its required origin until the job terminates, then remove only owned values. For abort/competition, remove the owned source guard first when ownership is certain to fence later admissions; preserve the origin until all already-running candidate consumers are terminal. Queued-only runs are not active consumers and must not cause an infinite terminal-job wait. Do not approve a pending publication while competition is unresolved.
- Preflight and continuously observe competing workflow runs while transports exist. Fixed native non-canceling concurrency serializes normal runs; it does not replace authorization. An unexpected run stops success, records the breach and requires quiescence/owner resolution. Only an explicitly authorized exact owned prepublication run may be canceled; never cancel unrelated runs or replay publication.
- Cleanup re-reads resource identity/value or metadata and removes only proven operation-owned state. A changed/ambiguous resource is not deleted. Provider/network failure leaves a recoverable blocked journal, not a false cleanup-success claim.
- On resume after any interruption, discover the operation by its opaque receipt in the private conventional area, validate ownership/permissions/relative paths, and complete cleanup/reconciliation before any new setup, dispatch, candidate approval or publication. Retain unresolved receipts privately; reduce them to bounded observed outcome metadata only after cleanup is proven.
- Verify the procedure with disposable, non-publishing stand-ins covering first-write failure, second-write failure, lost response, dispatch rejection, no job, ambiguous/multiple runs, queued competitor, running-job cancellation, EXIT/INT/TERM and recovered journal. Real execution still requires actual provider observations and authority.
</recovery_contract>

<tasks>

<task type="checkpoint:human-action" gate="blocking-human">
  <name>Task 1: Verify release authority and configure the exact protected trusted publisher</name>
  <files>Remote npm trusted-publisher and GitHub `npm-release` environment configuration only; no repository file</files>
  <read_first>
    - `05-CONTEXT.md` D-03, D-04, D-05, and D-08
    - `05-04-SUMMARY.md` actual bootstrap publication/revocation and reviewed public-source outcome
    - `.github/workflows/publish-npm.yml` exact trusted-publisher subject and pending-environment contract
    - `docs/distribution-operations.md` authority separation and public/irreversible implications
    - Official npm trusted-publisher setup/current direct-publish permission documentation and GitHub deployment-environment protection API documentation
  </read_first>
  <what-built>The exact source workflow and bootstrap proof exist; this gate establishes only the real npm/GitHub identities, rights, trusted-publisher relation, and protected deployment control required to produce a stable candidate safely.</what-built>
  <how-to-verify>
    1. Using read-only native queries first, display authenticated npm/GitHub actor names, `@shipwithai`/`@shipwithai/cumpa` write authority, account 2FA status, exact `1.5.0` vacancy, public repository ID, bootstrap token revocation/temporary-state cleanup, and actual GitHub environment protection capabilities. Never display credential values or login URLs in durable output.
    2. Present the exact proposed mutations: create/update only `npm-release` with required reviewer/main restriction/no bypass when supported, and create exact npm trusted publisher `Ship-With-AI/cumpa` + `publish-npm.yml` + `npm-release` with direct publishing explicitly enabled. No production/support secret is copied and no unrelated protection/retention setting changes.
    3. Obtain explicit authorization naming these remote resources and mutations. Generic Phase 5 approval or prior bootstrap authority is insufficient.
    4. Configure GitHub through its native API/CLI where supported. Complete only npm dashboard steps that have no supported API, using the authenticated owner account and the exact values above.
    5. Query both systems again and present bounded observed configuration. Stop if identity, rights, 2FA, version vacancy, exact publisher subject/direct permission, required review, branch restriction, or no-bypass protection is absent or unverifiable.
  </how-to-verify>
  <action>Do not mutate anything until actual state and the exact requested setup authority have been presented. After attributable authorization, automate native GitHub changes and guide only unavoidable npm dashboard interaction. Re-read authoritative state after writes rather than inferring success from a UI/CLI exit. Record no credential/config values and do not create the candidate approval file yet. Per D-03, this setup does not authorize CI dispatch or stable publication.</action>
  <verify>
    <automated>gh api repos/Ship-With-AI/cumpa/environments/npm-release &amp;&amp; node --input-type=module -e 'import assert from "node:assert/strict"; const response=await fetch("https://registry.npmjs.org/%40shipwithai%2Fcumpa/1.5.0"); assert.equal(response.status,404,"stable version must remain absent before publication");'</automated>
    <human-check>Confirm the npm trusted-publisher page shows exactly `Ship-With-AI/cumpa`, `publish-npm.yml`, `npm-release`, direct publication enabled, and the GitHub environment shows the authorized reviewer/main/no-bypass settings actually supported.</human-check>
  </verify>
  <acceptance_criteria>Actual identities/rights/2FA/vacancy are known and the exact native trusted-publisher plus protected-environment relation is configured under separate attributable setup authority without storing credentials or changing unrelated settings.</acceptance_criteria>
  <done>The release boundary can gate one pending publish job; no workflow run or npm stable publication is authorized or started.</done>
  <resume-signal>Provide exact release-setup authorization and complete any identified human-only npm dashboard step, or state the identity/permission/capability blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Authorize one source-bound candidate run and clean owned temporary production transports</name>
  <files>Remote `production` secret/variable and one GitHub Actions run only; no repository file</files>
  <read_first>
    - `05-04-SUMMARY.md` exact reviewed public main SHA `P`, source-publication authority, and collateral deployment proof
    - `05-03-SUMMARY.md` committed workflow jobs/actions/tool versions and source-guard behavior
    - `.github/workflows/publish-npm.yml` exact input-free dispatch, first-step guard, build outputs, and protected publish job
    - `docs/distribution-operations.md` D-03/D-05/D-06/D-07/D-08 authority and cleanup requirements
    - Native GitHub environment secret/variable, workflow dispatch, jobs, artifacts, and pending deployment API documentation
  </read_first>
  <what-built>An exact protected workflow is publicly present at reviewed main commit `P`; this gate permits only temporary production transport creation and one input-free CI candidate build/upload for that SHA.</what-built>
  <how-to-verify>
    1. Display public `P`, remote-main equality, reviewed source/history/checks, absence of collateral Supabase deployment, exact workflow identity, and current stable-version vacancy.
    2. Prove reserved production secret `CUMPA_RELEASE_BUILD_ORIGIN` and variable `CUMPA_RELEASE_SOURCE_SHA` are absent; confirm authoritative `SUPABASE_PROJECT_REF` exists without displaying it. In memory, validate canonical form, derived origin, and expected fingerprint.
    3. Present exact changes and cleanup: create the two absent-only temporary values, dispatch one input-free run at `P`, wait for `build-candidate` termination, then delete only those two values and verify absence even on failure/cancellation. Obtain explicit CI-run authorization; setup/artifact/bootstrap/source authority is insufficient.
    4. Create/read back the source variable and create the masked origin secret through stdin, dispatch once, and resolve actual run ID/attempt/head SHA. If the existing `production` environment requires deployment review, use this exact CI-run authorization to approve only the verified candidate-build job for `P`; do not alter that environment or approve any Supabase deployment. Show the first source guard passed before checkout.
    5. Apply `<recovery_contract>` on every partial/failed/ambiguous path. For a successful exact run, wait for the candidate job independently of the waiting workflow, then remove and verify both owned values. For no-run/no-job failures, clean immediately; for competition or uncertainty, fence known-owned future admission and reconcile/quiesce without guessing or deleting unrelated state.
    6. On success, show exact build outputs and prove the publish job is pending only on `npm-release`, with no OIDC/publish attempt yet.
  </how-to-verify>
  <action>Per D-05/D-06, bind only 05-04's exact P and the actual authorized actor; never dispatch whichever main is current. Establish the private persisted recovery receipt and arm supervised cleanup before creating resources. Require no existing workflow run or unresolved lease, acquire the source-variable admission lease with create-only semantics, recheck secret absence, and perform the one authorized setup/dispatch while atomically journaling each observed effect. Monitor actual source/run/attempt and competitors, not just a selected run.

Execute the explicit partial/no-job/active-job/ambiguity/context-loss branches in `<recovery_contract>`. In-memory flags alone are insufficient. Never log the protected value or copy it into another environment. Do not call uncertain creation or deletion successful. Complete targeted cleanup or retain a blocked recoverable state before further work; no candidate or stable authority can waive an unresolved resource.

Per D-01/D-07/D-08, require the real successful Darwin ARM64 producer/scanner/acceptance/seal/upload results from this exact run/attempt. Record and show provider/artifact validity bounds before asking for artifact approval. Do not approve npm-release or invoke publication here; expiration, unexpected run, failed check or unresolved cleanup stops the cycle without automatic rerun.</action>
  <verify>
    <automated>Capture native run/jobs/pending-deployment/resource metadata privately. Verify the exact run/source/attempt, all partial/no-job/concurrent/recovered cleanup branches and absence of only the two reserved owned names, plus preservation of the authoritative variable. Emit bounded status/identity/deadline comparisons, not full production-secret or variable inventories.</automated>
    <human-check>Authorize only the displayed exact `P`, temporary value names, one input-free run, artifact upload, and mandatory targeted cleanup; confirm observed run/attempt/head and pending protected publish job.</human-check>
  </verify>
  <acceptance_criteria>One authorized run at exact `P` completes the candidate job successfully, both temporary production values are proven absent afterward, and the unapproved publish job waits without registry access.</acceptance_criteria>
  <done>A real new candidate exists in one native artifact; it is neither locally approved nor authorized for stable publication.</done>
  <resume-signal>Type `AUTHORIZE CI CANDIDATE RUN FOR &lt;displayed-P&gt; WITH TEMPORARY SOURCE/ORIGIN TRANSPORT AND TARGETED CLEANUP` or identify the blocker.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 3: Inspect and approve the actual new same-run candidate</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-CI-ARTIFACT-APPROVAL.md</files>
  <read_first>
    - Actual Task 2 run/jobs/artifact outputs and cleaned-production observations
    - Downloaded sealed `cumpa.runtime-artifact-evidence/v1` from the exact native artifact ID
    - `05-02-SUMMARY.md` prepublish validation and bounded CI evidence contract
    - `05-04-SUMMARY.md` exact public source SHA and source review
    - `04-ARTIFACT-EVIDENCE.json` and `04-ARTIFACT-APPROVAL.md` as immutable history only, never a candidate source
    - `docs/distribution-operations.md` actual candidate approval limitations and separate stable authority
  </read_first>
  <what-built>One exact same-run configured stable tgz plus sealed producer/scanner/acceptance/CI evidence has been uploaded after a successful Darwin ARM64 build and downloaded by native artifact ID after temporary configuration cleanup.</what-built>
  <how-to-verify>
    1. Show run ID/attempt/head SHA/workflow ref, native artifact ID/digest, exact two-file payload, evidence SHA-256, archive basename/byte length/SHA-256/npm SHA-1/SHA-512 SRI, and independent post-read-only rehash.
    2. Show bounded passed facts for package/version, public-source alignment, one build/pack, legal/inventory/support fingerprint, scanner, complete installed browser/review/Finish behavior, cleanup/source control, and actual Darwin ARM64 native re-export plus other-target fallback limitation.
    3. Confirm the expected-source variable and origin secret are absent, authoritative production variable is untouched, publish job remains pending, stable `1.5.0` remains absent, and no npm publication/provenance event occurred.
    4. Acknowledge limitations: bounded scanning; native observation is Darwin ARM64 with explicit fallback elsewhere; artifact transport digest supplements inner hashes; source/build/publisher provenance is not complete until npm attestation is cryptographically verified; registry/global/npx proofs have not occurred.
    5. Confirm approval is only for this actual evidence/archive/source/run/attempt/artifact identity and is not stable publication authority.
  </how-to-verify>
  <action>Download through the native GitHub artifact endpoint by exact ID, compare the transport digest and reject extra/missing payloads. Validate sealed evidence and tgz independently, compare actual run/workflow/source through GitHub read APIs, make the local payload read-only outside checkout, rehash, and display only bounded safe values. Per D-01/D-08, generic, prospective, stale, proxy, agent-authored, or mismatched assent fails; a changed/repaired/repacked candidate requires a new separately authorized run and new approval, never fallback.

After the user supplies the exact actual-value phrase from `<interfaces>`, rehash once more and create `05-CI-ARTIFACT-APPROVAL.md` with verbatim assent, approver/timestamp, archive/evidence/transport identities, source/workflow/run/attempt, passed check summary, read-only custody label without path, cleanup observations, limitations, and explicit `stablePublicationAuthorized: false`. State that this new approval supersedes only Phase 4's publication designation; do not edit old files. Do not approve `npm-release`, publish, retry, or mutate the sealed payload.</action>
  <verify>
    <automated>On the real local host, independently hash the downloaded transport, sealed evidence and archive, check read-only/containment, and compare sealed CI facts with actual GitHub run/job/artifact API observations. Do not set or impersonate GITHUB_*, RUNNER_* or another CI identity to make the live-publish verifier accept local inspection. The workflow's later verify-candidate command runs only in its actual CI context.</automated>
    <human-check>Approve only by repeating every displayed actual identity and limitations in the exact phrase; confirm stable publication remains unauthorized.</human-check>
  </verify>
  <acceptance_criteria>An attributable human approves the actual read-only new CI candidate bound to its evidence/source/run/attempt/artifact/limitations after targeted cleanup, with no stable registry authorization.</acceptance_criteria>
  <done>The exact candidate is locally approved and the same workflow's protected publish job remains pending for separate 05-06 authority.</done>
  <resume-signal>Use the exact `CI CANDIDATE APPROVED: ...; stable publication not authorized` phrase populated with displayed actual values, or identify the blocker.</resume-signal>
</task>

</tasks>


<artifacts_this_phase_produces>
## Artifacts this phase produces
Private execution-only artifact: a bounded recovery journal with operation ID, public P/resource names, per-effect ownership/receipt metadata, run/job identities, deadlines and cleanup state. It contains no secret or private custody path and is not committed or uploaded; it exists solely to make this one authorized operation recoverable.
- Remote npm trusted-publisher relation: `@shipwithai/cumpa` <- `Ship-With-AI/cumpa` / `publish-npm.yml` / `npm-release`, direct publish enabled.
- Remote GitHub `npm-release` environment: main-restricted native required-review gate, no support-production secret.
- Temporary remote production values, created absent-only and deleted after candidate job termination: secret `CUMPA_RELEASE_BUILD_ORIGIN`, variable `CUMPA_RELEASE_SOURCE_SHA`.
- One actual GitHub Actions run/attempt with successful `build-candidate`, native artifact ID/digest, and `publish` pending on the protected environment.
- Outside checkout: one private read-only local download of exact sealed evidence and `shipwithai-cumpa-1.5.0.tgz`; paths never become durable evidence.
- `.planning/phases/05-bootstrap-trusted-stable-publication/05-CI-ARTIFACT-APPROVAL.md`: actual assent, approver/time, archive/evidence/source/workflow/run/attempt/artifact identities, checks, cleanup, limitations, `stablePublicationAuthorized: false`, actual run creation/artifact expiry observations, conservative approval/workflow deadline bounds and their basis.
</artifacts_this_phase_produces>

<threat_model>
## ASVS Level 1 / HIGH Threat Register

| Threat ID | STRIDE | Boundary | Risk | Disposition | Specific mitigation and proof |
|---|---|---|---|---|---|
| T-05-05-01 | Spoofing | Authenticated actor -> npm/GitHub authority | HIGH | mitigate | Read-only identity/rights/2FA/resource checks and attributable resource-specific setup authorization before mutation. |
| T-05-05-02 | Elevation of privilege | Trusted publisher/environment setup -> stable publish | HIGH | mitigate | Exact publisher subject plus native required-review environment; setup and CI authorities explicitly exclude protected-job approval. |
| T-05-05-03 | Information disclosure | Production variable -> temporary build secret | HIGH | mitigate | In-memory canonical validation/fingerprint, stdin secret creation, no tracing/argv/durable value, origin mapped only to candidate producer/scanner/acceptance steps that require configured-origin validation. |
| T-05-05-04 | Tampering | Moving main -> candidate source | HIGH | mitigate | Exact public `P`, absent-only source variable/read-back, workflow first-step equality gate, and independent actual run-head check. |
| T-05-05-05 | Tampering | Temporary configuration cleanup | HIGH | mitigate | Persist effect intent/ownership before writes; create-only source-variable lease; explicit partial/no-job/active-job/ambiguous/context-loss branches; remove only proven owned state and verify actual absence without changing the authoritative variable. |
| T-05-05-06 | Tampering | Native artifact -> local candidate approval | HIGH | mitigate | Exact artifact-ID download, transport plus independent inner hashes, two-file allowlist, sealed context checks, read-only custody and rehash. |
| T-05-05-07 | Repudiation | Automated results -> human approval | HIGH | mitigate | Display actual new identities/limits and require attributable exact-value assent; record `stablePublicationAuthorized: false`. |
| T-05-05-08 | Denial of service | Failed/cancelled candidate -> fallback/retry | HIGH | mitigate | Cleanup still runs, approval blocks, and no automatic rerun/rebuild/old-local fallback exists. |
| T-05-05-SC | Tampering | npm/GitHub configuration clients | HIGH | mitigate | Native APIs/CLI and official dashboard only; read back exact configuration; no new package or credential broker. |
| T-05-05-CC | Tampering / Repudiation | Competing dispatches and shared temporary configuration | HIGH | mitigate | No nonterminal run before setup, fixed non-canceling workflow serialization, continuous competitor observation, owned admission fencing and quiescence/owner stop on unexpected runs; never approve publication under contention. |
| T-05-05-RC | Information disclosure / Repudiation | Executor loss and uncertain remote side effects | HIGH | mitigate | Private persisted operation receipt, atomic intent/response updates, ownership reconciliation and cleanup-first recovery; uncertain ownership never authorizes deletion or publication. |
| T-05-05-DL | Denial of service | Approval/run/artifact expiry | MEDIUM | mitigate | Record actual creation/expiry observations and conservative 30/35-day bounds, reserve the 15-minute publish window, recheck pending state and stop for a new authorized cycle when expired. |
</threat_model>

<verification>
- Actual remote npm/GitHub state—not intended settings—proves rights, direct trusted publishing, required review, and exact identities.
- Actual run/job/artifact APIs prove source/run/attempt, first guard, build success, native artifact outputs, and still-pending publish job.
- Both owned temporary production values are absent before artifact approval; authoritative variable remains unchanged.
- Candidate approval binds actual values and explicitly withholds stable publication authority.
- Disposable non-mutating stand-ins cover partial resource creation, lost responses, no run/job, competing/queued runs, interruption and recovered journals; actual execution still requires real provider ownership/cleanup observations.
- Candidate/stable approval is solicited only inside the recorded live validity window; an expired attempt never triggers automatic rerun.
</verification>

<success_criteria>
- A protected token-free publication relation exists for the one fixed workflow without unrelated configuration changes.
- One new exact-source Darwin ARM64 candidate passes every check, is transferred once, downloaded/read-only, and actually approved.
- Temporary source/origin transports are removed before publication; Phase 4 remains immutable history.
</success_criteria>

<output>
Create `.planning/phases/05-bootstrap-trusted-stable-publication/05-05-SUMMARY.md` when done. Preserve the pending workflow run ID/attempt and exact artifact/evidence/archive identities for 05-06; do not approve its environment.
</output>
