---
phase: 05-bootstrap-trusted-stable-publication
plan: 06
type: execute
wave: 5
depends_on: [05-05]
files_modified:
  - .planning/phases/05-bootstrap-trusted-stable-publication/05-RELEASE-EVIDENCE.json
autonomous: false
requirements: [PKG-01, PKG-02, REL-02]
must_haves:
  truths:
    - "Per D-03/D-08, an attributable stable-publication authorization names the already approved archive/evidence/source/run/attempt/artifact and irreversible registry/transparency effects before the exact pending `npm-release` job is approved."
    - "Per D-01/D-08, the protected job revalidates the unchanged same-run candidate, makes one direct OIDC `npm publish` attempt, and never builds, packs, substitutes, falls back, or automatically retries."
    - "Per D-09, exact public version metadata and downloaded registry bytes match the approved new candidate by byte length, SHA-256, npm SHA-1, and SHA-512 SRI."
    - "Per D-08/D-09, current npm cryptographically verifies the signature/attestation bundle and decoded policy checks bind the exact package subject to repository, workflow, main ref, source SHA, GitHub-hosted runner, run ID, and attempt."
    - "Per D-09, credential-free clean global installation exposes the generated `cumpa` bin and a separate empty-context literal `npx --yes @shipwithai/cumpa@1.5.0 --version` resolves exact `1.5.0`."
    - "Per D-02/D-06/D-07, release evidence names only the new Darwin ARM64 CI candidate, confirms owned temporary configuration remained absent, preserves bounded legal/native/privacy limitations, and leaves Phase 4 history untouched."
  prohibitions:
    - "HIGH: No stable publish/environment approval before exact candidate approval plus separate actual-value stable authority; no generic, stale, proxy, or agent-authored assent."
    - "HIGH: No second publish command, automatic job rerun, candidate rebuild/repack, different run/attempt/artifact, dist-tag workaround, unpublish, or Phase 4 fallback after any success, failure, timeout, collision, or ambiguous response."
    - "HIGH: No success from npm exit code, metadata, badge, OIDC exchange, signature presence, or provenance presence alone; registry bytes, npm cryptography, exact claims, global bin, and npx must all pass."
    - "HIGH: No npm token/credential, inherited auth/userconfig/HOME/cache/prefix, live login URL, origin/project reference, private custody path, raw npm/GitHub payload, or unbounded logs in durable release evidence."
    - "No SLSA-level, exhaustive secret-scan, every-platform native, full public browser flow, source-input completeness, or retrospective Phase 4 CI provenance claim."
    - "No mutation of trusted-publisher/environment/production configuration, deployment workflow, source, Phase 4 files, existing `.gsd`, or legacy artifacts except targeted cleanup of temporary consumer state created here."
  artifacts:
    - path: ".planning/phases/05-bootstrap-trusted-stable-publication/05-RELEASE-EVIDENCE.json"
      provides: "Bounded actual authorization/publish/registry/attestation/global/npx/cleanup evidence for the new stable candidate"
      contains: "cumpa.npm-release-evidence/v1"
  key_links:
    - from: "05-CI-ARTIFACT-APPROVAL.md"
      to: "pending npm-release deployment"
      via: "separate exact-value stable authority followed by approval of only the named pending job"
      pattern: "stable publication not authorized|AUTHORIZE STABLE PUBLICATION"
    - from: "same-run publish job"
      to: "public @shipwithai/cumpa@1.5.0"
      via: "verify-candidate then one direct trusted-publisher npm publish of the exact tgz"
      pattern: "verify-candidate|npm publish"
    - from: "public registry tarball and npm audit output"
      to: "05-RELEASE-EVIDENCE.json"
      via: "independent hashes, npm cryptography, decoded exact claims, isolated global and npx observations"
      pattern: "verify-public|npm audit signatures|include-attestations"
---

<objective>
Obtain separate exact stable-publication authority, approve only the named pending protected job, observe its single publish attempt, and prove the actual public bytes, attestation claims, global install, and npx resolution before recording success.

Purpose: Satisfy PKG-01, PKG-02, and REL-02 with public evidence rather than planned intent while making ambiguous/failed irreversible operations stop honestly without retry.
Output: One public stable release when every gate passes and bounded `.planning/phases/05-bootstrap-trusted-stable-publication/05-RELEASE-EVIDENCE.json` describing the actual outcome.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-CONTEXT.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-RESEARCH.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-PATTERNS.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-02-SUMMARY.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-03-SUMMARY.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-04-SUMMARY.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-05-SUMMARY.md
@.planning/phases/05-bootstrap-trusted-stable-publication/05-CI-ARTIFACT-APPROVAL.md
@.planning/phases/04-exact-runtime-tarball/04-04-SUMMARY.md
@.github/workflows/publish-npm.yml
@scripts/verify-npm-release.mjs
@docs/distribution-operations.md
@package.json
</context>

<interfaces>
- Stable authority phrase: `AUTHORIZE STABLE PUBLICATION: @shipwithai/cumpa@1.5.0 sha256 <actual-64-hex> bytes <actual-integer>; evidence sha256 <actual-64-hex>; source <actual-40-hex>; run <actual-id> attempt <actual-integer>; artifact <actual-id> digest <actual-64-hex>; approve npm-release job <actual-job-id>; public registry and transparency-log mutation acknowledged; no retry authorized`.
- Before presenting that gate, independently rehash read-only local candidate/evidence, compare 05-05 approval, query exact run/job/artifact/pending-deployment state, require both temporary production names absent and authoritative variable untouched, recheck stable `1.5.0` absence, and read back exact trusted-publisher/environment protections. Any drift stops; do not ask the owner to approve around it.
- Check the recorded conservative environment-approval deadline, total workflow deadline and artifact expiry before soliciting stable authority. Require the exact run/job is still pending and at least the configured 15-minute publish-job window remains before the earliest bound. Expiry or insufficient time stops; a newly authorized candidate cycle and actual new approval are required, never a job rerun or old-archive fallback.
- Stable authority grants only approval of the named pending `npm-release` deployment in the named run/attempt. If current actor cannot approve due native reviewer/self-review policy, the actual configured reviewer performs that exact GitHub action after seeing the same authorization; do not weaken protection or approve another job.
- The workflow invokes one direct `npm publish <absolute-exact-tgz> --tag latest --access public --ignore-scripts --fetch-retries=0 --registry https://registry.npmjs.org/` from isolated outside-checkout npm state after same-run stdlib verification. It retains real GitHub OIDC context but no static npm credential or inherited project/user config. Automatic provenance remains enabled; no lifecycle, staging, second attempt or auto-rerun is permitted.
- Pass `--fetch-retries=0` on that single mutating npm invocation; an ambiguous outcome leads only to read-only reconciliation, never a hidden client upload retry.
- Classify the attempt as `succeeded`, `failed-absent`, `ambiguous-absent`, `collision-different`, or `registry-identical-after-ambiguous-response`. Any non-success/timeout first performs only read-only exact-version registry reconciliation. Absent or different bytes stop; identical bytes may continue public verification while preserving the ambiguous client result in evidence. Never execute another publish for the same version.
- Public metadata is exact-version `@shipwithai/cumpa@1.5.0`: record bounded `name`, `version`, `dist.tarball`, `dist.shasum`, `dist.integrity`, signature/attestation presence/counts. Download only the HTTPS registry tarball under the registry host allowlist and require approved byte length/SHA-256/SHA-1/SHA-512 SRI.
- Run exact npm `11.19.1` in a new credential-free scripts-disabled audit consumer tree and require `npm audit signatures --json --include-attestations` success. Store bounded verified bundle/claim comparisons, not raw full output. Decode the verified DSSE payload with Node standard library and require: in-toto Statement v1; one `pkg:npm/%40shipwithai/cumpa@1.5.0` subject with approved tarball SHA-512 hex; SLSA GitHub Actions workflow build type; exact GitHub repository/path/ref; resolved source `gitCommit`; `workflow_dispatch`; repository/owner IDs when provided by npm's current predicate; GitHub-hosted builder/runner; exact run/attempt invocation URL. Missing or mismatched fields fail the provenance success path.
- The audit consumer must actually contain installed exact `@shipwithai/cumpa@1.5.0` plus its matching package-lock entry before audit. Use scripts-disabled exact public installation, compare target lock integrity with approved bytes and require a verified bundle for that target from the same npm audit result. CLI success on an empty dependency tree is failure, not provenance evidence.
- Global proof uses a fresh mode-0700 HOME/userconfig/cache/prefix with publication credentials stripped, exact registry, and normal exact global install. Invoke the generated prefix `cumpa` binary directly and require exit 0/stdout `1.5.0`.
- npx proof uses a second fresh HOME/userconfig/cache/prefix, begins with no global install and empty cache, runs literal `npx --yes @shipwithai/cumpa@1.5.0 --version`, and requires exit 0/stdout `1.5.0`. These are separate observations; Phase 7 owns full public browser review behavior.
- Final evidence schema is `kind: "cumpa.npm-release-evidence/v1"`, `status: "published-verified"`, with bounded top-level `package`, `candidate`, `authorizations`, `configuration`, `publication`, `registry`, `attestation`, `consumers`, `cleanup`, and `limitations`. Failed/ambiguous execution may write the same kind with exact non-success status and observed safe facts, but it cannot satisfy the plan.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Authorize the exact stable mutation and approve only its pending protected job</name>
  <files>Remote pending `npm-release` deployment only; no repository file</files>
  <read_first>
    - `05-CI-ARTIFACT-APPROVAL.md` exact actual candidate assent and explicit withheld stable authority
    - `05-05-SUMMARY.md` actual run/job/artifact, trusted-publisher/environment, temporary cleanup, and local read-only custody observations
    - `.github/workflows/publish-npm.yml` exact prepublish command and one direct publish step
    - `docs/distribution-operations.md` irreversible registry/transparency implications, no-retry policy, and exact evidence requirements
    - Official GitHub pending-deployment approval API and npm trusted-publishing/provenance documentation
  </read_first>
  <what-built>One approved read-only candidate is bound to one exact source/run/attempt/artifact and its same workflow publish job is waiting at the protected `npm-release` environment without registry authority.</what-built>
  <how-to-verify>
    1. Display immediate independent archive/evidence rehashes and exact candidate approval, source/workflow/run/attempt/artifact/job identities; verify the job remains pending and no publish step has started.
    2. Show actual exact trusted-publisher relation/direct permission, required reviewer/main/no-bypass environment state, stable version absence, and both temporary production names absent while the authoritative variable remains unchanged.
    3. Explain that approval initiates one irreversible public package mutation and eligible public transparency-log/provenance entries; no retry, unpublish, replacement, fallback, or claim before public verification is authorized.
    4. Require the exact stable authority phrase from `<interfaces>` populated with displayed values. Generic release approval, prior setup/CI/artifact assent, or values for another job fail.
    5. After valid authority, approve only the named pending `npm-release` job through the native deployment API/current configured reviewer. Observe that the protected job starts; do not issue any direct local publish command.
  </how-to-verify>
  <action>Per D-03/D-08, perform every read-only drift check before requesting assent. Preserve the exact user statement, approver identity and timestamp for Task 2. If candidate, cleanup, repository, trusted publisher, protection, stable vacancy, run, artifact, or pending-job state changed, stop rather than amend approval targets. After exact authority, use the native pending-deployment approval for this run/job only; do not bypass/relax environment policy. The workflow then owns its one publish attempt. Do not treat environment approval, OIDC acquisition, or command start as success.</action>
  <verify>
    <automated>gh run view "$CUMPA_RELEASE_RUN_ID" --repo Ship-With-AI/cumpa --json databaseId,attempt,headSha,jobs,status,conclusion,url &amp;&amp; gh api repos/Ship-With-AI/cumpa/actions/runs/"$CUMPA_RELEASE_RUN_ID"/pending_deployments</automated>
    <human-check>Authorize only with the exact actual-value phrase and confirm only its named pending `npm-release` job was approved.</human-check>
  </verify>
  <acceptance_criteria>Separate attributable stable authority binds the actual approved candidate and irreversible effects, then only the exact protected pending job begins its one workflow-owned publish attempt.</acceptance_criteria>
  <done>One direct publish attempt is authorized and running/terminal; no outcome or public proof is inferred yet.</done>
  <resume-signal>Use the exact `AUTHORIZE STABLE PUBLICATION: ...; no retry authorized` phrase populated with displayed actual values, or identify the blocker.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Reconcile the single attempt and prove public registry, attestation, global, and npx behavior</name>
  <files>.planning/phases/05-bootstrap-trusted-stable-publication/05-RELEASE-EVIDENCE.json</files>
  <read_first>
    - Exact Task 1 authorization and the terminal workflow/job/step observations for its one publish attempt
    - `05-CI-ARTIFACT-APPROVAL.md` and the still-read-only local candidate/evidence
    - `05-02-SUMMARY.md` `verify-public` command, bounded result, registry/attestation/global/npx contract
    - `05-03-SUMMARY.md` workflow tool versions, one-attempt semantics, and same-run bindings
    - `05-PATTERNS.md` registry readback, environment isolation, and bounded evidence patterns
    - Official npm exact-version metadata, `npm audit signatures --json --include-attestations`, trusted-publisher provenance, and GitHub Actions SLSA predicate documentation
  </read_first>
  <action>Observe the exact workflow/job/steps to terminal state and record one publish invocation's exit/timing/conclusion without raw logs or secrets. On nonzero, timeout, cancellation, or ambiguous client response, query exact version read-only before deciding. If absent, record honest stopped status and do not retry. If present, download and compare bytes before distinguishing identical registry success from a collision. A different identity is a permanent blocker; never unpublish, mutate dist-tags, approve/rerun a job, or dispatch/build again.

For an exact present version, create a private temporary tool root with fresh HOME/userconfig/cache/prefix, install exact supported `npm@11.19.1` there from the fixed public registry without inherited credentials, assert both that npm and its matching npx versions, and prepend only that installation's bin directory while invoking `verify-public` against the still-read-only sealed evidence and exact artifact metadata. Per D-09, require all algorithm-specific registry identities, npm's successful cryptographic signature/attestation audit, and decoded exact subject/repository/workflow/ref/source/run/attempt/runner claims. Then require the helper's two distinct credential-free consumer roots and actual generated global bin plus literal npx command. Do not let local checkout/dist/archive paths, shared caches, inherited npm config, metadata-only checks, or the bootstrap install satisfy either consumer proof.

Atomically create bounded `05-RELEASE-EVIDENCE.json` from the verifier result plus actual candidate approval and stable authorization references, protected-job/publish outcome, trusted-publisher/environment snapshot, confirmed temporary-production cleanup, and temporary consumer cleanup. Use exact schema in `<interfaces>`; include safe expected/observed/pass tables and limitations, not raw provider output, values, tokens, URLs containing credentials, absolute private paths, or claims beyond evidence. Compute and report its SHA-256 after close. Per D-02/D-07, name the new CI candidate and Darwin ARM64/fallback limitation; never import the old Phase 4 digest as current release authority. A non-success record is evidence of the stop, not plan completion.</action>
  <verify>
    <automated>node scripts/verify-npm-release.mjs verify-public --evidence "$CUMPA_CI_CUSTODY_DIR/candidate-evidence.json" --expected-evidence-sha256 "$CUMPA_RELEASE_EVIDENCE_SHA256" --artifact-id "$CUMPA_RELEASE_ARTIFACT_ID" --artifact-digest "$CUMPA_RELEASE_ARTIFACT_DIGEST" --output "$CUMPA_RELEASE_VERIFICATION_REPORT"</automated>
  </verify>
  <acceptance_criteria>One and only one publish attempt is reconciled; exact public bytes, npm-verified and exact-claim attestation, clean generated global bin, and separate exact-version npx all pass before bounded evidence says `published-verified`.</acceptance_criteria>
  <done>`@shipwithai/cumpa@1.5.0` is publicly consumable and source/run-attested with exact-byte evidence, or the phase stops with honest non-success evidence and no retry.</done>
</task>

</tasks>

<artifacts_this_phase_produces>
## Artifacts this phase produces
- One separately authorized native approval of the exact pending `npm-release` job.
- One observed direct trusted-publisher `npm publish` attempt for the approved same-run tgz; no local publish or retry command.
- One exact-version public registry tarball verification by byte length, SHA-256, SHA-1, and SHA-512 SRI.
- One npm `11.19.1` `audit signatures --json --include-attestations` cryptographic result plus decoded exact-claim comparison table.
- One credential-free generated global-bin `cumpa --version` proof and one separate fresh-context literal `npx --yes @shipwithai/cumpa@1.5.0 --version` proof.
- `.planning/phases/05-bootstrap-trusted-stable-publication/05-RELEASE-EVIDENCE.json`
  - fields: `kind`, `status`, `package`, `candidate`, `authorizations`, `configuration`, `publication`, `registry`, `attestation`, `consumers`, `cleanup`, `limitations`
  - identities: archive length/SHA-256/SHA-1/SHA-512, sealed evidence SHA-256, source/workflow/run/attempt, artifact ID/digest, registry metadata/download, provenance subject/source, global/npx results
</artifacts_this_phase_produces>

<threat_model>
## ASVS Level 1 / HIGH Threat Register

| Threat ID | STRIDE | Boundary | Risk | Disposition | Specific mitigation and proof |
|---|---|---|---|---|---|
| T-05-06-01 | Repudiation | Candidate approval -> stable authority | HIGH | mitigate | New exact-value irreversible-effects statement and approver/time before native deployment approval; old/generic assent rejected. |
| T-05-06-02 | Elevation of privilege | Pending deployment -> npm registry | HIGH | mitigate | Approve only named protected job after read-back; job-scoped OIDC and exact trusted-publisher subject; no npm token/local publish. |
| T-05-06-03 | Tampering | Approved artifact -> published package | HIGH | mitigate | Same-run prepublish seal/hash gate, direct supplied tgz publish, then exact downloaded registry hashes across four identities. |
| T-05-06-04 | Denial of service | Ambiguous npm response -> duplicate attempt | HIGH | mitigate | One-attempt invariant and read-only exact-version reconciliation; absent/different state stops without rerun/retry/unpublish. |
| T-05-06-05 | Spoofing | Registry signature/badge -> provenance claim | HIGH | mitigate | npm cryptographically verifies full bundle; Node policy then matches exact subject/repository/workflow/source/run/attempt/runner. |
| T-05-06-06 | Spoofing | Local cache/install -> public usability | HIGH | mitigate | Separate credential-free empty global and npx roots; generated bin and literal exact-version command must report `1.5.0`. |
| T-05-06-07 | Information disclosure | Provider/audit/consumer output -> release record | HIGH | mitigate | Allowlisted bounded fields, stripped auth/config environment, no raw logs/private paths/origin/token, protected temp cleanup. |
| T-05-06-08 | Tampering | Evidence composition -> final success claim | HIGH | mitigate | Exact schema, expected/observed/pass rows, atomic new file, post-close digest, and success only when every independent observation passes. |
| T-05-06-SC | Tampering | Verification tool/package supply chain | HIGH | mitigate | Fixed exact package/registry, Node stdlib, current supported npm's verifier, existing lock/toolchain; no new package or hand-rolled cryptography. |
</threat_model>

<verification>
- GitHub readback proves the exact protected job, one publish invocation, terminal outcome, source/run/attempt/artifact identity, and no automatic rerun.
- `verify-public` performs the actual external registry/cryptographic/global/npx scenario; source-text checks do not substitute for it.
- Final evidence contains only bounded actual observations and authorization references, has a post-close SHA-256, and reports success only if every REL-02/PKG-01/PKG-02 gate passed.
</verification>

<success_criteria>
- The exact new CI candidate is the byte-identical public `@shipwithai/cumpa@1.5.0` after one separately authorized publish attempt.
- npm cryptography and decoded policy fields prove the expected repository/workflow/source/run/attempt subject without overstating provenance.
- Fresh credential-free global and npx consumers each resolve and execute exact `1.5.0`.
- Cleanup and bounded evidence preserve privacy, Phase 4 history, and no-retry semantics.
</success_criteria>

<output>
Create `.planning/phases/05-bootstrap-trusted-stable-publication/05-06-SUMMARY.md` only after `05-RELEASE-EVIDENCE.json` status is `published-verified`; otherwise preserve the stop record and report the exact blocker without advancing Phase 5.
</output>
