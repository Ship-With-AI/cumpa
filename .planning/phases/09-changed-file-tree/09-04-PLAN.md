---
phase: 09-changed-file-tree
plan: 04
type: execute
wave: 3
depends_on: [09-01, 09-03]
files_modified:
  - src/web/components/FileTree.vue
  - src/web/components/DirectoryRow.vue
  - src/web/components/FileRow.vue
  - src/web/styles.css
autonomous: true
requirements: [TREE-01, TREE-02, TREE-03, TREE-04, TREE-05]

must_haves:
  truths:
    - "The changed-files area shows a header, a filter field, and a scrolling tree with a keyboard hint below it"
    - "Typing in the filter narrows the tree to matching files with their ancestor directories visible and expanded"
    - "A filter matching nothing shows a heading, an explanation, and a button that returns the full tree"
    - "Clearing the filter restores the previous tree, keeps the open file selected, and scrolls it into view without stealing focus from the filter"
    - "Exactly one visible tree row is reachable by Tab, and at load that row is the selected file"
  artifacts:
    - path: "src/web/components/FileTree.vue"
      provides: "Header composition, native search filter with a clear button, scroller, no-match recovery state, keyboard hint, query wiring, projected-tree rendering, tab-stop drilling"
      contains: "setQuery"
    - path: "src/web/components/DirectoryRow.vue"
      provides: "tabbableRowId prop replacing focusedRowId, drilled to both recursive branches"
      contains: "tabbableRowId"
    - path: "src/web/components/FileRow.vue"
      provides: "tabbable prop replacing focused, bound to tabindex"
      contains: "tabbable"
    - path: "src/web/styles.css"
      provides: "Pane row-track layout that hands scrolling to the inner scroller, and retirement of the tree's negative focus offset"
  key_links:
    - from: "src/web/components/FileTree.vue"
      to: "src/web/model/file-tree.ts"
      via: "the search input's value flows into model.setQuery by direct model assignment, never through applyModel, so DOM focus stays in the field"
      pattern: "setQuery"
    - from: "src/web/components/FileTree.vue"
      to: "src/web/components/FileRow.vue"
      via: "model.tabbableRowId drilled through the root loop and both DirectoryRow branches to become the single tabindex=\"0\""
      pattern: "tabbable"
    - from: "src/web/components/FileTree.vue"
      to: "src/web/styles.css"
      via: "the scroller element the pane's second grid track sizes and the scrollbar rules target"
      pattern: "file-tree-pane__scroller"
---

<objective>
Compose the changed-files area the mockup shows — header, filter, scroller, recovery state, hint — wire the filter to the model's query transition, and make the model's single derived tab stop the only reachable row.

Purpose: this is the plan that turns plan 09-01's model capability into TREE-04 and TREE-05 behaviour and closes the TREE-03 roving-tabindex defect that has been red since before this milestone. It is also the plan that gives the pane its row tracks, so the header and filter stay put while only the tree scrolls.
Output: `FileTree.vue` owns the query, renders the projected tree, and drills `tabbableRowId`; `DirectoryRow.vue` and `FileRow.vue` bind `tabindex` to the tab stop instead of to the focused row; the pane hands scrolling to an inner scroller; `tests/e2e/file-tree.spec.ts:331` turns green with no edit.

Out of scope: the Files trigger, the drawer lifecycle, sidebar hide and restore, the narrow file-then-Base-then-Head ordering, and `--sidebar-width` — all Phase 11. New browser coverage for the filter, the recovery state, and the contract viewports is plan 09-05.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
@GSD core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/09-changed-file-tree/09-UI-SPEC.md
@.planning/phases/09-changed-file-tree/09-RESEARCH.md
@.planning/phases/09-changed-file-tree/09-PATTERNS.md
@.planning/phases/09-changed-file-tree/09-01-SUMMARY.md
@.planning/phases/09-changed-file-tree/09-02-SUMMARY.md
@.planning/phases/09-changed-file-tree/09-03-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Compose the header, filter, scroller, recovery state, and hint in FileTree.vue</name>
  <files>src/web/components/FileTree.vue</files>
  <read_first>
    - src/web/components/FileTree.vue (whole file — `focusModelRow` at :28-39, `applyModel` at :41-51, `focusRow`/`activateFile`/`toggleDirectory` at :53-69, `handleKeydown` at :71-97, the four exposed functions at :100-130, the mount emit at :132-136, both watchers at :138-157, the template at :160-200)
    - src/web/model/file-tree.ts (the surface after plan 09-01: `query`, `setQuery`, `projectedTree`, `tabbableRowId`)
    - src/web/components/DriftExportAcknowledgement.vue:20-30 and :55-65 (the repository's only local-ref-bound form control; the closest analog for a local query ref)
    - src/web/components/SummarySection.vue:185-200 (the label-wrapped control convention)
    - src/web/App.vue:1160-1200 (the `nav#changed-files` host, the `FileTree` call site with no template ref, the zero-files `empty-state` branch, and the `Files` button's `aria-controls`)
    - src/web/App.vue:380-393 (`initializeReviewDraft` auto-loading the first *reviewable* file, which is why the seeded focus and the selection diverge)
    - src/web/styles.css (the chrome classes authored by plan 09-02: `__top`, `__count`, `__eyebrow`, `__filter`, `__filter-glyph`, `__clear`, `__scroller`, `.tree-empty`, `.tree-hint`; also `.visually-hidden` at :901-911 and `.ui-button`)
    - .planning/phases/09-changed-file-tree/09-UI-SPEC.md:150-172 (composition, filter field), :226-243 (activation, reconciliation), :247-266 (exact copy), :297-314 (structure, names, roving tabindex), :327-338 (component ownership)
    - .planning/phases/09-changed-file-tree/09-RESEARCH.md:259-273 (Pitfall 5, the heading that must keep its accessible name), :279-287 (Pitfalls 7 and 8), :535-552 (open questions on host widths, toggling during filter, hint placement, empty-state level)
    - mockups/01b-quiet-workspace-tree.html:42 (the sidebar header and filter markup), :48 (the narrow host markup), :125-143 (the filter and tab-stop behaviour)
    - tests/e2e/pinned-session.spec.ts:490-497 and :873-900 (the tree text, heading name, and tree name assertions pinned unchanged for the whole phase)
    - tests/e2e/responsive-session.spec.ts:1030-1055 (the skip-link target and the first-file focus-ring check)
  </read_first>
  <action>
**Header.** Replace the heading's single text node with four spans while keeping the element an `h2`, keeping `id="changed-files-heading"`, and keeping `tabindex="-1"`. Render `Files` and the bare count and the `Changed` eyebrow as three spans hidden from accessibility APIs, and put the exact string `Changed files ({N})` in a fourth span carrying the existing `visually-hidden` class. This is load-bearing, not cosmetic: `tests/e2e/pinned-session.spec.ts:877-879` asserts the heading's accessible name is exactly `Changed files (3)` and is pinned unchanged for the whole phase; `tests/e2e/file-tree.spec.ts:325` asserts the same shape; the tree's own name derives from this heading through `aria-labelledby`, which `pinned-session.spec.ts:494` and `:881` depend on; and the heading is the skip-link target asserted at `tests/e2e/responsive-session.spec.ts:1034`. Hiding the visible composition and exposing the sentence is what satisfies `09-UI-SPEC.md:156` without touching any of those.

Wrap the heading and the filter in the top block plan 09-02 styled, so the header inset and the header-to-filter separation come from CSS rather than from markup spacing.

**Filter.** Build it from a native search input, not a combobox. Use a plain container as the positioning context with four siblings: a decorative search glyph hidden from accessibility APIs, a visually-hidden label associated to the input by `id`, the input itself, and the clear button. Do not nest the button inside a `label` — a label may not contain interactive content other than its own control, and an invalid nesting here would also make the click target ambiguous.

Give the input `type="search"`, `autocomplete="off"`, the accessible label `Filter files`, and the placeholder `Find file…`. Take the placeholder from `09-UI-SPEC.md:252`, which is the exact-copy authority; `mockups/01b-quiet-workspace-tree.html:42` renders a longer phrasing and the spec's copy wins. The placeholder is not the accessible name.

Bind the input to a local `ref` holding the empty string. Do not add a store: `09-UI-SPEC.md:338` places query and restoration state with the existing model and component.

Render the clear button only while the query is non-empty, with the accessible name `Clear file filter` and an `aria-hidden` multiplication-sign glyph. This is a **deliberate interaction departure** from `01b`: a native search field's built-in clear affordance is not dependably keyboard-reachable, and `09-UI-SPEC.md:170` requires an explicit target. Clicking it must clear the query, must not activate or change the open file, and must return focus to the input.

**Query wiring.** Push the query into the model by assigning `model.value` directly from `model.value.setQuery(...)`. Do **not** route it through `applyModel`: that helper calls `focusModelRow`, which focuses a row on the next tick and would rip DOM focus out of the field on every keystroke. The `initialSelectedFileId` watcher at `FileTree.vue:138-146` already assigns `model.value` directly — clone that shape, not the `toggleDirectory` shape. `09-UI-SPEC.md:313` requires the tab stop to be reassigned without DOM focus moving unexpectedly, and the model's derived tab stop already does that work.

Filter immediately on every keystroke. Do not debounce: one filter pass over 5,000 files measured 0.32 ms, the changed-file inventory is capped near 8,000 records by `DEFAULT_STDOUT_LIMIT` at `src/git/runner.ts:4`, and a debounce would also make whole-tree Playwright locators race.

**Rendering the projection.** Change the root loop to iterate the model's projected tree instead of `tree`. This is what makes the filtered view, the filtered `aria-level` values, and plan 09-03's directory descendant counts all correct at once, because a projected directory's `children` are already only the survivors. Keep the `:key` expression, the `:level="1"` seed, and every existing event binding.

**Recovery state.** When the projection has no visible rows, render a recovery block inside the scroller, as a sibling *after* the tree list — not in place of it. Keeping the list element mounted preserves the `role="tree"`, its `aria-labelledby` target, and the `getByRole('tree', …)` assertions in `pinned-session.spec.ts`. This is a **deliberate departure** from `mockups/01b-quiet-workspace-tree.html:129`, which swaps the tree out.

Use the exact copy from `09-UI-SPEC.md:255-256`: heading `No matching files`, body `Clear the filter to show all changed files.`, and a button reading `Clear filter` that performs the same transition as the trailing clear affordance. Reuse the existing shared button class rather than authoring a second button style. Render the heading as an `h3` with its own id; never reuse `changed-files-heading`, which is the skip-link target, the tree's label source, and an `h2`.

A comparison with no changed files at all is not a filter result. `App.vue:1175-1178` already owns that case with its own explanation, and `FileTree` is not even mounted then — do not mislabel it.

**Hint.** Render the hint as a paragraph sibling of the list, inside the scroller, carrying the text `↑↓ move · → open · ← close · Home/End jump · Enter/Space select`. Never place it inside the list element: a paragraph is an invalid child of a `role="tree"` list and it would pollute the `getByRole('tree')` text assertions at `tests/e2e/pinned-session.spec.ts:494-496`. The Home, End, and Space mentions are a **deliberate copy expansion** of `01b:135`, required because TREE-03 promises those keys.

**Scroller.** Wrap the list, the recovery block, and the hint in the scroller element plan 09-02 styled, so the header and filter stay fixed while only the tree scrolls.

**Clearing restores and reveals.** After the query clears, scroll the selected file's row into nearest view and do not focus it, because focus belongs to whichever control the reviewer used. Find the row by iterating the list's `[role="treeitem"]` elements and comparing their `fileId` data attribute, the same way `focusModelRow` at `FileTree.vue:34-36` already does. Do not build a selector by interpolating the file id into a string.

**Prop replacement reconciles the query.** The `props.files` watcher at `:149-157` rebuilds the model from scratch, which resets the query to empty while the field still shows the reviewer's text. Re-apply the current query ref immediately after rebuilding so the field and the projection agree. Selection reconciliation stays exactly as it is: by opaque file id, never by visible text, which is what keeps two display-colliding paths distinct.

**Delete the dead exposed API.** Remove the `defineExpose` block and the four functions behind it. They have no consumer — `App.vue:1174` renders this component with no template ref — and two of them read and write `scrollTop` on the pane element, which stops being the scroller in task 3. Re-grep the repository for all four names before deleting, and remove the now-unused heading and pane template refs too. This is cleanup that this plan's own change obsoletes, so it is in scope. Keep `treeElement`; the focus lookup and the reveal-on-clear both need it.

Introduce no `v-html` and no `innerHTML`. The query and every path are rendered through text interpolation only.
  </action>
  <verify>
    <automated>npm run typecheck:web &amp;&amp; npm run test:browser -- tests/e2e/pinned-session.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:web` exits zero.
    - `npm run test:browser -- tests/e2e/pinned-session.spec.ts` exits zero, and `git diff --stat tests/e2e/pinned-session.spec.ts` shows no change.
    - `grep -c 'defineExpose\|focusSelectedFile\|getScrollPosition\|setScrollPosition\|focusHeading' src/web/components/FileTree.vue` returns 0.
    - `grep -rc 'focusSelectedFile\|getScrollPosition\|setScrollPosition' src/web src/cli 2>/dev/null` reports no match outside components that define their own same-named local functions; the deleted names have no remaining `FileTree` caller.
    - `grep -c 'model.value.setQuery' src/web/components/FileTree.vue` returns at least 1, and `grep -c 'applyModel(model.value.setQuery' src/web/components/FileTree.vue` returns 0.
    - `grep -c 'projectedTree' src/web/components/FileTree.vue` returns 1.
    - `grep -c 'Changed files (' src/web/components/FileTree.vue` returns 1 and that string sits inside a `visually-hidden` span.
    - `grep -c 'Filter files\|Find file…\|Clear file filter\|No matching files\|Clear the filter to show all changed files.\|Clear filter' src/web/components/FileTree.vue` returns 6.
    - `grep -c 'v-html\|innerHTML\|setTimeout\|debounce' src/web/components/FileTree.vue` returns 0.
    - The hint paragraph and the recovery block are siblings of the `ul[role="tree"]`, not descendants; `npm run test:browser -- tests/e2e/pinned-session.spec.ts` is the mechanical proof, since its tree text assertions fail otherwise.
  </acceptance_criteria>
  <done>The changed-files area renders header, filter, clear affordance, scroller, tree, recovery state, and hint; the query reaches the model without moving DOM focus; the projected tree is what renders; the dead exposed API is gone; every pinned `pinned-session` assertion is still green.</done>
</task>

<task type="auto">
  <name>Task 2: Bind tabindex to the model's single tab stop through both recursive branches</name>
  <files>src/web/components/FileTree.vue, src/web/components/DirectoryRow.vue, src/web/components/FileRow.vue</files>
  <read_first>
    - src/web/components/FileTree.vue:160-200 as changed by task 1 (the root loop passing `:focused-row-id` and `:focused`)
    - src/web/components/DirectoryRow.vue (whole file — the props at :9-15, the `tabindex` binding at :41, the recursive `DirectoryRow` branch at :55-65, the `FileRow` branch at :66-75)
    - src/web/components/FileRow.vue (whole file — the `focused` prop at :11, the `tabindex` binding at :49)
    - src/web/model/file-tree.ts (the `tabbableRowId` derivation and the `selectFile` re-homing from plan 09-01)
    - .planning/phases/09-changed-file-tree/09-UI-SPEC.md:309-314 (roving tabindex contract) and :241 (tab-stop precedence under a filter)
    - .planning/phases/09-changed-file-tree/09-RESEARCH.md:352-382 (the two-part fix and why the derived value is needed on top of the re-homing) and :423-439 (the root-cause chain)
    - .planning/phases/09-changed-file-tree/09-PATTERNS.md:1003-1007 (the three prop-drilling sites)
    - tests/e2e/file-tree.spec.ts:328-331 and :385-418 (the selected-row tab stop and the keyboard matrix)
  </read_first>
  <action>
Replace the focus-derived `tabindex` with the tab-stop-derived one at all three drilling sites, and delete the props that become unused. A clean cutover: no alias prop, no both-props-accepted transition.

In `FileRow.vue`, replace the `focused` boolean prop with a `tabbable` boolean prop and bind `tabindex` to it. In `DirectoryRow.vue`, replace the `focusedRowId` prop with a `tabbableRowId` prop of the same nullable string type, bind its own row's `tabindex` by comparing that value to its row id, pass the same value down to the nested `DirectoryRow`, and compute `tabbable` for each child `FileRow` from it. In `FileTree.vue`, pass the model's tab stop to the root `DirectoryRow` and compute `tabbable` for each root `FileRow` from it.

Keep the `focus` handlers exactly as they are. A row focused by Tab still emits its row id, which sets `focusedRowId`, which is the first branch of the tab-stop precedence — so the stop follows the reviewer after they enter the tree. This is also how a focus anchor that a filter removed self-heals without any reconciliation code.

Keep `selectedFileId` on `DirectoryRow` and `selected` on `FileRow`: they drive `aria-selected` and the selection styling, which are a different axis from the tab stop. Keep `aria-level`, `aria-expanded`, `aria-selected="false"` on directories, `data-row-id`, `data-file-id`, the inline indent property, and the click handlers untouched.

This binding, together with plan 09-01's `selectFile` re-homing, is the whole TREE-03 fix. The defect was never a component bug: `createFileTreeModel` seeds focus on the first tree *leaf* while `App.vue:387-391` auto-loads the first *reviewable* file, and in the packaged fixture those differ because the first leaf is the unsupported `binary.dat`. The re-homing makes the two agree at load; the derived stop covers the two cases re-homing cannot see — a filter removing the focused row, and an empty projection with no stop at all. Expect `tests/e2e/file-tree.spec.ts:331` to pass with no edit to that file; if it still fails, the defect is not fixed and the assertion must not be touched.
  </action>
  <verify>
    <automated>npm run typecheck:web &amp;&amp; npm run test:browser -- tests/e2e/file-tree.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run typecheck:web` exits zero.
    - `npm run test:browser -- tests/e2e/file-tree.spec.ts` exits zero with no failures — including the assertion at `tests/e2e/file-tree.spec.ts:331`, which turns green without being edited.
    - `git diff --stat tests/e2e/file-tree.spec.ts` shows no change in this plan.
    - `grep -c 'focusedRowId' src/web/components/DirectoryRow.vue` returns 0 and `grep -c 'tabbableRowId' src/web/components/DirectoryRow.vue` returns 4 or more, covering the prop declaration, its own `tabindex`, the recursive pass-down, and the child `FileRow` computation.
    - `grep -c 'focused' src/web/components/FileRow.vue` returns 0 and `grep -c 'tabbable' src/web/components/FileRow.vue` returns 2 or more.
    - `grep -c 'tabbable' src/web/components/FileTree.vue` returns 2 or more.
    - `grep -c 'aria-selected="false"' src/web/components/DirectoryRow.vue` returns 1 and `grep -c 'data-file-id' src/web/components/FileRow.vue` returns 1, both unchanged.
  </acceptance_criteria>
  <done>Exactly one visible row carries `tabindex="0"`, it is the selected row at load, the keyboard matrix is unchanged, the obsolete focus props are gone, and the phase's inherited red assertion is green.</done>
</task>

<task type="auto">
  <name>Task 3: Hand scrolling to the inner scroller and restore the contract focus ring</name>
  <files>src/web/styles.css</files>
  <read_first>
    - src/web/styles.css:160-166 (the global `:focus-visible` outline width and offset)
    - src/web/styles.css (the `.file-tree-pane` rule and the `.review-files .tree-row:focus-visible` rule as they stand after plan 09-02)
    - src/web/styles.css:1289-1310 (`.review-files` as the outer scroll owner and the pane width override)
    - src/web/styles.css:2264-2272 and :2306-2346 (the responsive host widths and the absolutely positioned files drawer, which has a definite block size)
    - src/web/components/FileTree.vue as changed by task 1 (the pane now has exactly two children: the top block and the scroller)
    - tests/e2e/responsive-session.spec.ts:382-435 (`expectFocusIndicatorUnclipped`, which walks overflow ancestors and fails a clipped ring) and :1046-1053 (its first-file call site)
    - tests/e2e/responsive-session.spec.ts:1455-1472 (the `.review-files` computed `overflow-y` and `box-shadow` assertions)
    - .planning/phases/09-changed-file-tree/09-RESEARCH.md:279-283 (Pitfall 7, the two nested scroll owners)
    - .planning/phases/09-changed-file-tree/09-UI-SPEC.md:150-159 (composition order) and :220 (the focus ring and the scroller inset it needs)
  </read_first>
  <action>
Give `.file-tree-pane` two row tracks — the top block sized to content and the scroller taking the remaining space with a zero floor — and change its overflow from vertical auto to hidden. Keep the full height and sidebar surface that plan 09-02 added. The pane now has exactly the two children task 1 produced, so the tracks line up one-to-one.

Leave `.review-files` completely alone. It must keep computing `overflow-y: auto`, which `tests/e2e/responsive-session.spec.ts:1459` asserts, and `box-shadow: none` outside its drawer query, which `:1460` asserts. Two nested scroll owners is the shipped arrangement; this change only makes the inner one the element that actually scrolls at desktop widths. In the drawer at 1099 pixels and below, `.review-files` is absolutely positioned with a definite block size, so the pane's full height still resolves and the inner scroller still works.

Delete the `.review-files .tree-row:focus-visible` rule that negates the outline offset. That rule existed for a full-bleed row with no radius; with the 5px-radius row and the scroller inset plan 09-02 authored, the tree row should use the contract's 2px outline at 3px offset from the global `:focus-visible` rule, as `09-UI-SPEC.md:220` specifies. `--focus-outline-width` keeps its global consumer, so no orphan results.

This retirement is why plan 09-02's scroller carries block-start padding: the helper at `tests/e2e/responsive-session.spec.ts:382-435` fails any focusable element whose ring perimeter crosses an overflow ancestor's box, and a positive offset needs five pixels of clearance above the first row. If the responsive run reports a clipped ring, increase the scroller's block-start inset rather than reintroducing a negative offset — a negative offset draws the ring inside the row and defeats the contract.

Touch nothing else. Do not change the `.review-shell` grid tracks at desktop or at 1439 pixels, do not change the drawer geometry, and do not introduce `--sidebar-width`. Host sizing is Phase 11's, locked by `09-CONTEXT.md:22`. The UI-SPEC's `294px` and `248px` figures are mockup host widths; this phase verifies tree-interior fluidity at the *shipped* `288px` and `256px` hosts and records the mockup figures as Phase 11 input.
  </action>
  <verify>
    <automated>npm run verify:semantic-css &amp;&amp; npm run test:browser -- tests/e2e/responsive-session.spec.ts tests/e2e/file-tree.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm run verify:semantic-css` exits zero.
    - `npm run test:browser -- tests/e2e/responsive-session.spec.ts tests/e2e/file-tree.spec.ts` exits zero with no failures.
    - `grep -c 'review-files .tree-row:focus-visible' src/web/styles.css` returns 0.
    - `grep -n 'grid-template-columns: 288px minmax(0, 1fr) 360px' src/web/styles.css` and `grep -n 'grid-template-columns: 256px minmax(0, 1fr)' src/web/styles.css` each still return a match, proving the shell tracks are untouched.
    - Comment-filtered, no shell sizing token was introduced: `grep -v '^\s*/\*' src/web/styles.css | grep -v '^\s*//' | grep -c -- '--sidebar-width'` returns 0, and the same filtered pipeline over `scripts/css-token-contract.mjs` returns 0. Filter the comment lines: this plan's own rationale names that token, so an unfiltered count could trip on a comment rather than on a declaration.
    - The `.file-tree-pane` rule declares `overflow: hidden` and two row tracks, and no `.review-files` declaration changed; `git diff src/web/styles.css` confirms the diff is confined to the pane rule and the deleted focus-offset rule.
  </acceptance_criteria>
  <done>The header and filter stay fixed while the tree scrolls, the tree row's focus ring uses the contract width and offset without clipping, the outer scroll owner and the shell grid are untouched, and both browser suites are fully green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| reviewer keystrokes → DOM | The filter query is untrusted text that is stored in component state and re-rendered |
| repository content → DOM lookups | Opaque file ids derived from repository content are used to locate rows |
| shell host → tree interior | The pane is mounted inside a host whose inert and hidden state Phase 11 owns |

## STRIDE Threat Register

ASVS level 1; blocking threshold `high`. This plan adds no endpoint, no request, no persisted data, and no file write, so only V5 (input validation / output encoding) applies — which is also the one category `09-RESEARCH.md:565` marks as applicable for the phase.

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-13 | Tampering | the filter input and the recovery message in `FileTree.vue` | mitigate | Render the query and every message through Vue text interpolation and attribute binding, which escape by default. Never introduce `v-html` or assign `innerHTML`, and specifically never add match highlighting that injects markup around the query — that is the one change that would turn this field into a reflected DOM cross-site-scripting sink. Task 1's grep for `v-html` and `innerHTML` is the gate. |
| T-09-14 | Tampering | row lookup for reveal-on-clear | mitigate | Iterate `[role="treeitem"]` elements and compare their `fileId` data attribute, the pattern already at `FileTree.vue:34-36`. Do not interpolate an id into a selector string: the string-built selector at the deleted `FileTree.vue:110-112` was only safe because `OpaqueFileIdSchema` at `src/contracts/api.ts:313` constrains the id shape, and dataset iteration removes the dependence on that invariant entirely. |
| T-09-15 | Spoofing | the heading's accessible name | mitigate | Keep the exact `Changed files ({N})` sentence in an accessibility-exposed span while the visible composition is hidden from accessibility APIs. A screen-reader reviewer must not be told a different count than a sighted reviewer sees. Both counts come from the same `files.length`. |
| T-09-16 | Repudiation | filtering versus opening a file | mitigate | `setQuery` never changes `selectedFileId`, the clear affordances never activate a file, and only an explicit click or focus opens one. A filter keystroke that silently changed which file the reviewer is reviewing would make their review comments attributable to a file they never chose. |
| T-09-17 | Denial of service | per-keystroke filtering with no debounce | accept | Measured 0.32 ms per pass at 5,000 files, with the inventory capped near 8,000 records by `DEFAULT_STDOUT_LIMIT` at `src/git/runner.ts:4`. A debounce would add a race to every whole-tree browser locator for no measured benefit. |
| T-09-18 | Information disclosure | the retired exposed component API | mitigate | Deleting `defineExpose` removes four unused entry points into the component's focus and scroll state. Keeping them would leave methods that silently target the wrong element after this task moves the scroller. |
| T-09-SC | Tampering | npm/pip/cargo installs | mitigate | Not applicable — this plan installs nothing. |
</threat_model>

<verification>
- `npm run typecheck:web` green.
- `npm run verify:semantic-css` green.
- `npm run test:browser -- tests/e2e/file-tree.spec.ts` fully green, including `:331`, with that file byte-unchanged in this plan.
- `npm run test:browser -- tests/e2e/responsive-session.spec.ts` fully green, including the first-file focus-ring check and the `.review-files` overflow and shadow assertions.
- `npm run test:browser -- tests/e2e/pinned-session.spec.ts` green with that file byte-unchanged.
- `npm run test:unit` green — the model contract from plan 09-01 is unchanged by this plan.
- No `v-html`, no `innerHTML`, no debounce, no virtualization, no store, and no string-interpolated DOM selector anywhere in the three changed components.
- `.review-shell` grid tracks and the drawer geometry byte-unchanged; no `--sidebar-width`.
</verification>

<success_criteria>
- The changed-files area reads header, filter, scrolling tree, then keyboard hint, with the header and filter fixed while only the tree scrolls (TREE-01).
- Typing narrows the tree to files whose full path matches, case-insensitively, with ancestor directories rendered and expanded (TREE-04).
- Directory counts reflect surviving descendants while a filter is active, because the projected tree is what renders (TREE-02).
- A no-match filter shows `No matching files`, `Clear the filter to show all changed files.`, and a `Clear filter` button, and never claims the comparison itself is empty (TREE-05).
- Clearing the filter restores the previous tree and expansion state, leaves the open file selected, scrolls it into nearest view, and does not steal focus from the filter or clear control (TREE-05).
- Exactly one visible row carries `tabindex="0"`; at load it is the selected row; an empty projection has none; the full keyboard matrix still works (TREE-03).
- Nesting, compaction, byte-exact ordering, the all-expanded default, expand and collapse, and opaque selection are unchanged (TREE-03).
</success_criteria>

<artifacts>
## Artifacts this phase produces (this plan)

`src/web/components/FileTree.vue`
- `query` — local `ref<string>` holding the reviewer's verbatim filter text
- The query handler assigning `model.value` from `model.value.setQuery(...)` without `applyModel`
- `clearQuery` — clears the query, returns focus to the input, and reveals the selected row
- The reveal-on-clear helper iterating `[role="treeitem"]` by `fileId` data attribute and calling nearest-block scroll into view
- Header spans: `Files`, the visually-hidden `Changed files ({N})` sentence, `.file-tree-pane__count`, `.file-tree-pane__eyebrow`
- `.file-tree-pane__top` wrapper, `.file-tree-pane__filter` container, `.file-tree-pane__filter-glyph`, the visually-hidden `Filter files` label, the search input with its `id`, `.file-tree-pane__clear`
- `.file-tree-pane__scroller` wrapper
- `.tree-empty` recovery block with its own `h3` id, body paragraph, and `Clear filter` button
- `.tree-hint` paragraph
- Root loop switched to `model.projectedTree`
- `props.files` watcher re-applies the current query after rebuilding
- Removed: `defineExpose`, `focusHeading`, `focusSelectedFile`, `getScrollPosition`, `setScrollPosition`, the heading ref, the pane ref

`src/web/components/DirectoryRow.vue`
- `tabbableRowId: string | null` prop replacing `focusedRowId`, drilled to the recursive branch and used to compute each child's `tabbable`

`src/web/components/FileRow.vue`
- `tabbable: boolean` prop replacing `focused`, bound to `tabindex`

`src/web/styles.css`
- `.file-tree-pane` two row tracks and `overflow: hidden`
- Removed: `.review-files .tree-row:focus-visible` negative outline offset

Exact copy strings introduced: `Filter files`, `Find file…`, `Clear file filter`, `No matching files`, `Clear the filter to show all changed files.`, `Clear filter`, and the hint line `↑↓ move · → open · ← close · Home/End jump · Enter/Space select`.

No new file. No new CSS token. No model, contract, API, server, or Git change.
</artifacts>

<output>
Create `.planning/phases/09-changed-file-tree/09-04-SUMMARY.md` when done. Record: confirmation that `tests/e2e/file-tree.spec.ts:331` is now green with that file unedited, closing the phase's inherited red window; the deleted exposed API and the re-grep evidence that it had no consumer; the two deliberate departures from `01b` — the always-mounted tree beside the recovery block, and the explicit clear button — with their rationales; the placeholder copy taken from the UI-SPEC rather than the mockup; the decision to keep `toggleDirectory` writing the reviewer's expansion set while a filter forces display expansion; and the observed focus-ring clearance above the first row.
</output>
