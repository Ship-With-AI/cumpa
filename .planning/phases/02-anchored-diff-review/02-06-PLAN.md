---
phase: 02-anchored-diff-review
plan: 06
type: execute
wave: 4
depends_on: [02-03, 02-05]
files_modified:
  - src/web/api/client.ts
  - src/web/App.vue
  - src/web/styles.css
  - src/web/components/DiffWorkspace.vue
  - src/web/components/CommentComposer.vue
  - src/web/components/CommentsRail.vue
  - tests/integration/anchored-workspace.spec.ts
autonomous: true
requirements: [DIFF-02, DIFF-03, DIFF-04, DIFF-05, DIFF-07, CMT-01, CMT-02, CMT-08, DRFT-01, DRFT-02, DRFT-03]
must_haves:
  truths:
    - "The inline side-specific composer shows safe path, Base/Head, and line; blur preserves, non-empty cancel confirms, pending/failure retains exact text/anchor, and only an atomically persisted canonical response renders Saved locally."
    - "D-04: Add comment and Command/Ctrl+Enter are the only acceptance triggers; moving focus or blurring the composer preserves the unfinished text and never submits it."
    - "D-05: Cancel/Discard draft or Escape closes an empty composer immediately, while non-empty unaccepted text remains until the reviewer explicitly confirms discard."
    - "The comments rail orders accepted records deterministically, navigates verified records by reveal-before-focus, and keeps stale/orphan records visible with recorded details and no inline marker, relocation, or Phase 3 lifecycle actions."
    - "Same-pair draft resume and different-pair isolation are visible while scroll/focus/context/composer remain session-only through switching and responsive resize."
    - "Pointer and keyboard comment activation use one transition, send only fileId/side/line/body, and never accept client-derived path, blob, context, comparison, or persistence authority."
  artifacts:
    - path: src/web/components/CommentComposer.vue
      provides: "Locked-anchor explicit-persistence composer"
    - path: src/web/components/CommentsRail.vue
      provides: "Verified/stale/orphan navigation and presentation"
    - path: tests/integration/anchored-workspace.spec.ts
      provides: "Focused real-browser inline persistence, resume, and anchor-state contracts"
  key_links:
    - from: src/web/components/CommentComposer.vue
      to: src/web/api/client.ts
      via: "strict four-field add request and canonical response before accepted render"
      pattern: "addComment"
    - from: src/web/components/CommentsRail.vue
      to: src/web/model/workspace-state.ts
      via: "verified reveal/focus or stale/orphan inspect/copy commands"
      pattern: "showComment"
    - from: src/web/App.vue
      to: src/server/draft-store.ts
      via: "authenticated closed draft API resumes only the selected comparison"
      pattern: "draft"
---

<objective>
Add exact inline comment persistence and deterministic comments-rail resume/navigation to the completed review shell.

Purpose: Deliver D-01–D-06/D-12 and the persistence-facing UI-SPEC without reopening Plan 05 shell/navigation or pulling Phase 3 comment lifecycle and reattachment behavior forward.
Output: Inline composer, canonical acceptance flow, comments rail, same-pair resume/different-pair isolation, stale/orphan presentation, and two focused real-browser interaction contracts.
</objective>

## Phase Goal
**As a** developer reviewing a pinned local comparison, **I want to** inspect immutable text side by side and attach precise comments that remain stable, **so that** I can resume trustworthy feedback without publishing branches or relocating anchors.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-UI-SPEC.md
@.planning/phases/02-anchored-diff-review/02-01-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-02-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-03-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-04-SUMMARY.md
@.planning/phases/02-anchored-diff-review/02-05-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Wire the exact inline comment persistence interaction</name>
  <files>src/web/api/client.ts, src/web/App.vue, src/web/styles.css, src/web/components/DiffWorkspace.vue, src/web/components/CommentComposer.vue, tests/integration/anchored-workspace.spec.ts</files>
  <read_first>Read D-01–D-06/D-12, UI-SPEC Gutter affordance, Inline geometry, Composer, Discard, Persistence states, the canonical add API in 02-02-SUMMARY.md, atomic draft behavior in 02-03-SUMMARY.md, workspace transitions in 02-04-SUMMARY.md, and the actual integrated shell identifiers in 02-05-SUMMARY.md.</read_first>
  <action>Extend the completed Plan 05 DiffWorkspace without changing its Phase 1 identity, responsive shell, navigation, or adapter ownership. Expose one 24×24 side-specific gutter action only for the hovered/focused visible model line and give it exact `Add comment to base/head line N` or `Focus comment on ...` accessible naming plus Option/Alt+Enter tooltip. Pointer and keyboard dispatch the same state action. Insert the composer directly below the selected side/model line with the proven paired-zone/full-width fallback and immutable header `{safe side path} · Base|Head · line N` plus fixed-anchor tooltip. Include labeled Comment textarea, exact placeholder/support copy, Add comment, and Discard draft. Whitespace shows the exact validation message. Per D-04, only the explicit Add comment button or Command/Ctrl+Enter invokes submission; blur and ordinary focus movement preserve unfinished text and never submit. Per D-05, Cancel/Discard draft or Escape closes an empty composer immediately, but non-empty unaccepted text opens the inline Keep writing/Discard draft confirmation; Escape from that confirmation keeps writing, and only confirmed Discard draft removes the text. Pending disables edit/actions and announces adding without showing accepted state. Send only fileId/side/line/body. On failure/session end preserve text/anchor and show exact recoverable alert; on duplicate focus the canonical existing record; only strict canonical 201 after atomic persistence replaces the composer with a Saved locally accepted surface and announcement/focus. Maintain one composer per file and explicit confirmation before moving non-empty text. Test base/head, changed/revealed unchanged/deleted/inserted lines, rename-side headers, pointer/keyboard parity, duplicate focus, D-04 blur/non-submission and both explicit acceptance paths, D-05 empty immediate discard and non-empty confirmation paths, faulted persistence retention, and success-only acceptance in focused Chromium per D-01–D-06/D-12.</action>
  <verify><automated><command expected="0">npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "inline comment persistence"</command></automated></verify>
  <done>D-04 is browser-observable: Add comment and Command/Ctrl+Enter accept, while blur never submits. D-05 is browser-observable: Cancel/Discard draft or Escape immediately removes an empty composer and requires explicit confirmation before discarding non-empty unaccepted text. D-01–D-03/D-06/D-12 remain browser-observable; no unsuccessful request appears accepted, no duplicate/thread is created, and Plan 05 shell/navigation behavior remains intact.</done>
</task>

<task type="auto">
  <name>Task 2: Present resume and exact verified, stale, and orphan comment navigation</name>
  <files>src/web/api/client.ts, src/web/App.vue, src/web/styles.css, src/web/components/DiffWorkspace.vue, src/web/components/CommentsRail.vue, tests/integration/anchored-workspace.spec.ts</files>
  <read_first>Read UI-SPEC Draft load/resume, Comments rail, Show comment sequence, Stale and Orphan Contract, copywriting, and later-phase boundary; read 02-03-SUMMARY.md draft view response, 02-04-SUMMARY.md reveal transitions, 02-05-SUMMARY.md shell/drawer/focus seams, and Task 1 canonical accepted-record presentation.</read_first>
  <action>Load the active comparison's strict draft after authenticated session bootstrap. Show exact loading, resumed, new-pair, and safe load-failure copy; never overwrite invalid existing state or merge another comparison. Compose a Comments rail ordered by deterministic file tree, Base before Head, then line, with path, side-line, two-line body preview, and Saved/Stale anchor/Anchor unavailable badge. Verified `Show comment` must save outgoing state, select opaque fileId, wait models/diff, reveal hidden line through the proven public path, rebuild zones, center, focus inline heading, announce, and return focus to the initiating rail action on Escape. Stale is rail-only with exact `has not been moved` copy, recorded safe path/side/line/short blob, conditional Inspect recorded file that never places it, and Copy anchor details. Orphan is likewise rail-only with no fabricated target; neither has gutter/inline marker. Keep body readability and copy/action access independent of color. Add no edit/delete/resolve/reopen/filter/summary/revision conflict/selector drift/export/reattach/reply/thread/theme controls. Focused real-browser cases assert same-pair resume copy, different-pair empty copy/isolation, exact reveal sequence, stale/orphan visible details and absence of relocated inline markers, empty rail copy, and responsive comments drawer focus per D-03/D-06/D-12.</action>
  <verify><automated><command expected="0">npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "draft resume and anchor states"</command></automated></verify>
  <done>Same/different pair UX and verified/stale/orphan presentation are complete, accessible, consume the Plan 05 shell seam, and remain strictly within Phase 2.</done>
</task>
</tasks>

<acceptance_criteria>
- Inline comments use immutable side/model-line display and server-derived canonical anchors, with accepted state only after atomic success.
- Same pair resumes, a different pair remains isolated, and verified records reveal before focus through the existing shell state machine.
- Stale/orphan records remain visible without relocation, inline decoration, fabricated target, or mutation.
- The slice adds no Phase 3/later edit/delete/resolve/summary/conflict/drift/export/reattachment/reply/thread/theme behavior.
</acceptance_criteria>

## Artifacts this phase produces
- `src/web/components/CommentComposer.vue` — exact locked-anchor persistence UI.
- `src/web/components/CommentsRail.vue` — deterministic resume and verified/stale/orphan navigation UI.
- `tests/integration/anchored-workspace.spec.ts` — focused real-browser persistence/resume contracts extending Plan 05 coverage.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser composer → closed API | UI event/state cannot add repository, path, object, comparison, anchor-derived, or draft authority. |
| API response → rendered comment | Only strict validated canonical DTOs render; repository text and comment body are untrusted display text. |
| Comments rail → editor navigation | Recorded state cannot relocate or fabricate a file/line target. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-34 | Spoofing | session/draft UI | mitigate | Reuse in-memory bearer client and strict same-origin DTO validation; never display another pair's data. |
| T-02-35 | Tampering | composer request | mitigate | Four-field request only; immutable header is display and server remains anchor authority. |
| T-02-36 | Information Disclosure | UI errors/copy | mitigate | Safe copy omits token, absolute path, object command/stderr, attempted authority, blob text, and internal correlation details. |
| T-02-37 | Elevation of Privilege | rendered text | mitigate | Vue escapes comment/path text; Monaco remains read-only; no dynamic HTML, link execution, repository code execution, or writable command. |
| T-02-38 | Denial of Service | comment/body/layout | mitigate | Server/client body bounds, one composer per file, bounded rail previews, and proven adapter lifecycle. |
| T-02-39 | Repudiation | accepted visual state | mitigate | Pending/failure remain distinct; accepted surface uses canonical server record only after atomic success. |
| T-02-40 | Tampering | stale/orphan navigation | mitigate | Rail-only exact states; no inline decoration, nearby search, path invention, line rewrite, or persistence mutation. |
| T-02-SC | Tampering | UI supply chain | mitigate | No packages or remote component registry; the approved locked Monaco and local Vue components only. |
</threat_model>

<verification>Run only the two grep-selected focused real-browser contracts for inline persistence and draft resume/anchor states. They test observable behavior in real Monaco/Vue, not source text or private internals. Do not run formatter, linter, aggregate browser suite, API/unit sweep, or project-wide tests.</verification>

<success_criteria>
- D-01–D-06/D-12 and all comment/draft requirement-facing UI contracts are browser-observable.
- Accessibility, pending/failure/success focus behavior, same-pair resume, different-pair isolation, and stale/orphan exactness are proven.
- The slice consumes Plans 02–05 and leaves the packaged acceptance plan a single downstream dependency.
</success_criteria>

<output>Create `.planning/phases/02-anchored-diff-review/02-06-SUMMARY.md` with focused command evidence, inline state transitions, canonical acceptance proof, resume/isolation observations, stale/orphan navigation evidence, focus/accessibility matrix, and later-phase exclusion audit.</output>
