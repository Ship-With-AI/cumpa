---
phase: 02-anchored-diff-review
plan: 09
type: execute
wave: 7
depends_on:
  - 02-08
files_modified:
  - src/web/monaco/diff-adapter.ts
  - src/web/model/workspace-state.ts
  - src/web/components/DiffWorkspace.vue
  - src/web/components/CommentComposer.vue
  - src/web/components/CommentsRail.vue
  - src/web/App.vue
  - src/web/styles.css
  - tests/unit/workspace-state.test.ts
  - tests/integration/anchored-workspace.spec.ts
autonomous: true
gap_closure: true
requirements:
  - DIFF-07
  - CMT-01
  - CMT-08
  - DRFT-02
must_haves:
  truths:
    - "A developer can hover or focus any visible base or head model line—including a revealed unchanged non-line-1 line—and activate a comment anchor on that exact side/model line (DIFF-07, CMT-01, D-01, D-12)."
    - "Exactly one textarea-backed composer exists, mounted in the Monaco view zone directly below the selected line, and it remains anchored through resize, context reveal, and file switching (CMT-01, D-02, D-07, D-11)."
    - "Moving away from a non-empty composer requires a deliberate move confirmation that either preserves the original draft or discards it and opens the requested target; the confirmation is never a no-op (D-05)."
    - "Composer focus, responsive drawers, and stale/orphan actions work at wide, 1100–1439px drawer, and narrow widths without exposing inactive navigation or inventing inline positions (CMT-08, DRFT-02)."
    - "Stale/orphan rail entries show copyable recorded anchor facts and offer recorded-file inspection only when exact reconciliation grants that capability; focus remains on the rail record and no relocation occurs (CMT-08, D-12)."
  artifacts:
    - path: "src/web/monaco/diff-adapter.ts"
      provides: "Model-position pointer/focus affordance events and viewport presentation coordinates"
      exports:
        - AnchorAffordanceTarget
    - path: "src/web/model/workspace-state.ts"
      provides: "Pending-target composer move confirmation transition"
      contains: "confirm-move"
    - path: "src/web/components/DiffWorkspace.vue"
      provides: "Single Monaco-zone composer and exact per-line affordance composition"
    - path: "src/web/components/CommentsRail.vue"
      provides: "Recorded anchor details, copy action, and capability-gated file inspection"
    - path: "tests/integration/anchored-workspace.spec.ts"
      provides: "Arbitrary-line, single-composer, drawer, focus, and stale/orphan browser regressions"
  key_links:
    - from: "src/web/monaco/diff-adapter.ts"
      to: "src/web/components/DiffWorkspace.vue"
      via: "AnchorAffordanceTarget reports side/model line while `top` is used only for transient placement"
      pattern: "AnchorAffordanceTarget"
    - from: "src/web/components/DiffWorkspace.vue"
      to: "src/web/components/CommentComposer.vue"
      via: "Vue render mounts one composer into `.monaco-anchor-zone--composer` and focuses that zone's textarea"
      pattern: "monaco-anchor-zone--composer"
    - from: "src/web/components/CommentComposer.vue"
      to: "src/web/model/workspace-state.ts"
      via: "distinct `confirmMove` event dispatches `confirm-move` with the preserved pending target"
      pattern: "confirm-move"
    - from: "src/web/components/CommentsRail.vue"
      to: "src/web/App.vue"
      via: "explicit copy-recorded-anchor and inspect-recorded-file events consume Plan 02-08 capability"
      pattern: "inspect-recorded-file"
---

<objective>
Replace line-1-only controls and duplicate composer rendering with one exact Monaco-anchored interaction, repair non-empty composer movement, and finish responsive/focus/stale-record behavior.

Purpose: DIFF-07 and CMT-01 remain blocked because pointer controls do not derive real model positions and two composer surfaces exist. Related state, rail, and drawer defects prevent the UI from honoring D-01, D-02, D-05, D-11, and D-12 consistently.
Output: Public Monaco pointer/focus affordance plumbing, a single zone composer, deliberate move confirmation, actionable recorded-anchor rail controls, corrected drawer inert/focus behavior, and focused browser coverage.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-RESEARCH.md
@.planning/phases/02-anchored-diff-review/02-UI-SPEC.md
@.planning/phases/02-anchored-diff-review/02-VERIFICATION.md
@.planning/phases/02-anchored-diff-review/02-08-SUMMARY.md
@src/web/monaco/diff-adapter.ts
@src/web/model/workspace-state.ts
@src/web/components/DiffWorkspace.vue
@src/web/components/CommentComposer.vue
@src/web/components/CommentsRail.vue
@src/web/App.vue
@src/web/styles.css
@tests/unit/workspace-state.test.ts
@tests/integration/anchored-workspace.spec.ts
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Make composer movement a real, loss-safe state transition</name>
  <files>src/web/model/workspace-state.ts, src/web/components/CommentComposer.vue, tests/unit/workspace-state.test.ts</files>
  <read_first>
    - src/web/model/workspace-state.ts — preserve the current per-file composer state, explicit persistence commands, and one-comment-per-side-line invariant.
    - src/web/components/CommentComposer.vue — preserve the UI-SPEC's shared discard prompt and controls for both confirmation states; route only the destructive action according to whether a pending move exists.
    - tests/unit/workspace-state.test.ts — extend the established transition/command assertions rather than testing component plumbing by source text.
    - .planning/phases/02-anchored-diff-review/02-CONTEXT.md — implement D-03, D-05, D-06, and D-11 exactly.
  </read_first>
  <behavior>
    - "Activating a different side/model line with non-whitespace composer text stores the requested target and enters `confirm-move` without changing the original anchor/text."
    - "Keep writing exits confirmation and retains the original anchor/text; Escape has the same loss-safe outcome."
    - "Confirm move discards the old text and opens an empty ready composer at the stored target, updates focused line, and rebuilds annotations."
    - "Cancel/discard confirmation still discards only the current composer; it cannot accidentally execute a pending move."
    - "A pending persistence request cannot move or discard the composer until the API outcome resolves."
    - "Both confirmation states render the exact shared prompt `Discard this comment draft? Your text hasn’t been saved.` with `Keep writing` and `Discard draft`; `Discard draft` dispatches `confirm-move` only when a pending move exists and `confirm-discard` for ordinary closure."
  </behavior>
  <action>
Write failing state tests first, then add an explicit pending move target to `WorkspaceComposer` (or an equally typed per-file state field) and a distinct `confirm-move` event. Update `activate-line`, keep-writing/Escape, cancel, and confirmation transitions so the requested target is never lost and destructive movement occurs only after explicit confirmation, per D-05 and D-11. Keep the exact shared UI-SPEC confirmation presentation for both `confirm-discard` and `confirm-move`: prompt `Discard this comment draft? Your text hasn’t been saved.`, safe action `Keep writing`, and destructive action `Discard draft`. In `CommentComposer`, route that same `Discard draft` control to `confirmMove` only when status is `confirm-move` with a pending target; route it to `confirmDiscard` for ordinary draft closure. Keep D-03's existing-comment focus behavior and D-06's pending/error text retention unchanged. Preserve D-04's explicit Add Comment/Ctrl-or-Cmd+Enter submission contract; blur must never submit or discard.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/workspace-state.test.ts</automated>
    <expected>The focused state suite passes new target-preservation, keep-writing, confirm-move, confirm-discard separation, and pending-save cases.</expected>
  </verify>
  <acceptance_criteria>
    - `confirm-move` has a distinct event and UI emission path.
    - The requested side/model line is retained while confirmation is visible.
    - Keep writing and Escape preserve original text/anchor; confirm move alone discards and relocates.
    - Pending save and explicit Add Comment/Ctrl-or-Cmd+Enter semantics remain unchanged.
    - Both movement and ordinary closure use the exact shared UI-SPEC prompt and `Keep writing` / `Discard draft` labels; no move-specific visible copy is introduced.
  </acceptance_criteria>
  <done>Non-empty composer movement is deliberate, observable, reversible before confirmation, and no longer a no-op.</done>
</task>

<task type="auto">
  <name>Task 2: Deliver exact per-line affordances, one zone composer, and safe rail/drawer actions</name>
  <files>src/web/monaco/diff-adapter.ts, src/web/components/DiffWorkspace.vue, src/web/components/CommentsRail.vue, src/web/App.vue, src/web/styles.css, tests/integration/anchored-workspace.spec.ts</files>
  <read_first>
    - src/web/monaco/diff-adapter.ts — reuse original/modified editors, `activateAnchor`, paired view zones, public mouse/cursor/scroll events, and current lifecycle disposal.
    - src/web/components/DiffWorkspace.vue — remove the fixed line-1 controls and declarative composer while retaining command/expose contracts used by App.
    - src/web/components/CommentsRail.vue — replace the inert generic Inspect action for degraded records with recorded facts and explicit capabilities.
    - src/web/App.vue — align media-query state with CSS drawer thresholds and preserve exact Plan 02-08 reconciliation.
    - src/web/styles.css — retain the dense three-column visual language while making affordance placement and 1100–1439px drawer controls coherent.
    - .planning/phases/02-anchored-diff-review/02-UI-SPEC.md — implement Pointer/Floating Controls, Focus Management, Responsive, and stale/orphan specifications.
    - tests/integration/anchored-workspace.spec.ts — before production edits, create the focused `anchored gap closure` scenario from the UI-SPEC contract so Task 2's browser verification always has a collected test.
  </read_first>
  <action>
Before changing production code, create the focused Playwright scenario named `anchored gap closure` in `tests/integration/anchored-workspace.spec.ts` as a RED contract. Cover a non-line-1 visible base target, a non-line-1 visible head target, exactly one textarea-backed zone composer, the shared discard prompt/control labels, move preservation and relocation, the 1440px/1200px/900px drawer and focus states, and recorded stale/orphan actions. Run the focused command once to prove Playwright collects the named scenario and that it fails on the current fixed-line or duplicate-composer behavior rather than with “no tests found”; then implement the production changes below and rerun it to GREEN.
Extend the public adapter with `AnchorAffordanceTarget` carrying `side`, positive model `line`, and a transient viewport `top`. Derive it from original/modified `onMouseMove`, cursor/focus, scroll, layout, and context-reveal events; clear it when no valid visible model position exists; dispose every new listener. The model side/line is the only activation identity under D-01/D-12; `top` may position the 24×24 control but must never be persisted or converted into a diff-row anchor. Replace both fixed `activateAnchor(side, 1)` buttons with an affordance whose accessible label/data attributes contain the actual side/line and whose click invokes `activateAnchor(target.side, target.line)`. Keyboard Alt+Enter must continue through the same activation method.

Make `.monaco-anchor-zone--composer` the sole composer mount. Remove the declarative composer below the editor and remove the adapter's provisional native textarea/heading so Vue `render(CommentComposer)` owns the only textarea. Re-render into the current zone after diff recomputation, D-08 collapsed/all-context reveal changes, resize/layout, file switch/restore, and composer state changes; unmount stale zone roots; then focus the zone textarea when activation/focus-comment commands request it. Keep paired spacer geometry and D-07's one-active-file rule.

Use the exact recorded anchor and file capability from Plan 02-08 in `CommentsRail`: render escaped recorded display path plus exact-byte identifier, side, line, blob OID, selected text/context, verification reason; add an explicit copy action; show `Inspect recorded file` only when an exact current file capability exists. App may switch to that exact file but must leave focus on the originating rail record, must not reveal/place an inline anchor, and must not change stale/orphan status. Clipboard writes occur only after the explicit copy control and failures announce without losing focus.

Split responsive state at the real CSS thresholds: comments drawer through 1439px and files drawer through 1099px. Show each Close control at its own threshold, mark each closed off-canvas navigation subtree inert/aria-hidden, move focus into the opened drawer, and return it to the opener on close. Do not make the main review shell inert merely because a non-modal drawer is open. Preserve desktop side-by-side rendering and D-10 shortcuts.
  </action>
  <verify>
    <automated>npm run test:unit -- tests/unit/workspace-state.test.ts</automated>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "anchored gap closure"</automated>
    <expected>State tests pass and the focused browser matrix proves arbitrary-line base/head activation, one textarea, restored anchoring, recorded-anchor actions, and drawer/focus behavior.</expected>
  </verify>
  <acceptance_criteria>
    - Pointer activation uses a real Monaco model position and works for non-line-1 unchanged base/head lines after reveal.
    - The page contains exactly one composer textarea and its header/zone identify the selected path, side, and model line.
    - Resize, context reveal/collapse, diff update, and file switching do not create a second composer or detach the first from its model line.
    - `Inspect recorded file` is capability-gated and never relocates stale/orphaned evidence inline.
    - At 1200px the comments drawer has a working Close control; at narrower widths inactive drawers are inert and focus movement/return is deterministic.
  </acceptance_criteria>
  <done>The anchored workspace closes the DIFF-07/CMT-01 blockers and the associated CMT-08/DRFT-02 interaction defects using public Monaco and exact identity APIs.</done>
</task>

<task type="auto">
  <name>Task 3: Lock the cross-width anchored interaction as a browser contract</name>
  <files>tests/integration/anchored-workspace.spec.ts</files>
  <read_first>
    - tests/integration/anchored-workspace.spec.ts — consolidate new cases under the existing mock session without weakening prior scenarios.
    - src/web/components/DiffWorkspace.vue — target stable accessible names/data attributes produced by Task 2.
    - src/web/components/CommentsRail.vue — assert user-visible recorded facts and action availability, not internal Vue state.
    - src/web/styles.css — use 1440px, 1200px, and 900px viewports that straddle actual breakpoints.
  </read_first>
  <action>
Complete the focused `anchored gap closure` browser scenario. At 1440px, reveal all context, hover and click a non-line-1 unchanged base line, assert one textarea in the correct line zone, type text, attempt to move to a non-line-1 head line, verify the exact `Keep writing` safe action, then activate the exact `Discard draft` destructive action and prove its internal `confirm-move` transition opens the new target. Repeat activation with keyboard cursor+Alt+Enter. Resize to 1200px and 900px while the composer is open, toggle context, switch away/back, and assert the single composer restores to the exact side/model line and receives focus. Exercise comments/files drawer open-close, inert/aria-hidden transitions, focus entry/return, stale/orphan recorded detail copy, capability-gated file inspection, and the absence of inline relocation. Use web-first assertions and deterministic model-line data attributes; do not rely on fixed pixel clicks, text duplication counts, or line 1.
  </action>
  <verify>
    <automated>npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "anchored gap closure"</automated>
    <expected>The single focused Playwright scenario passes at all three widths and demonstrates both base and head non-line-1 activation paths.</expected>
  </verify>
  <acceptance_criteria>
    - Both pointer and keyboard activation share the exact target contract.
    - The textarea count is one before and after every resize/context/file transition.
    - The move-confirmation path proves both preservation and explicit relocation.
    - Drawer and degraded-record controls are checked by focus, inert state, visible data, and command effect.
  </acceptance_criteria>
  <done>The repaired behavior is protected by a deterministic cross-width user-flow regression rather than source-text checks.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Monaco pointer/cursor event → durable anchor request | Viewport events select immutable side/model-line identity. |
| composer UI → workspace state/persistence | User text and destructive movement cross an explicit state transition before persistence. |
| recorded draft facts → rail/clipboard/file capability | Untrusted recorded content is displayed/copied; exact reconciliation controls file access. |
| responsive navigation → focus tree | Hidden off-canvas controls must not remain keyboard-operable. |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-G09-01 | Tampering / Elevation | High | Monaco affordance activation | mitigate — blocking | Derive side/line from public model-position events, validate positive line against the selected model, and pass only that immutable identity to `activateAnchor`; non-line-1 base/head browser checks must pass. |
| T-02-G09-02 | Tampering | High | composer move confirmation | mitigate — blocking | Preserve original text/anchor and requested target in typed state; only the distinct confirm-move event can discard and relocate. Unit and browser confirmation checks must pass. |
| T-02-G09-03 | Spoofing / Information disclosure | Medium | stale/orphan rail actions | mitigate | Escape all recorded values, gate file inspection on the exact capability, never issue reveal/center for degraded records, and copy only after explicit user action. |
| T-02-G09-04 | Elevation of privilege | Medium | hidden drawers | mitigate | Apply inert and aria-hidden to closed off-canvas navigation, keep modal semantics off non-modal drawers, and test focus entry/return at each threshold. |
| T-02-G09-05 | Denial of service | Medium | Monaco listener/zone lifecycle | mitigate | Dispose pointer/scroll listeners and stale Vue zone roots; retain one paired zone and one active composer through recomputation. |
| T-02-G09-SC | Tampering | Low | package supply chain | accept | This plan installs no package and uses existing Monaco/Vue/Playwright APIs from the lockfile. |

High-severity gate: T-02-G09-01 and T-02-G09-02 are blocking. Plan completion requires their focused unit and browser mitigations to pass.
</threat_model>

<verification>
1. `npm run test:unit -- tests/unit/workspace-state.test.ts`
2. `npm run test:browser -- tests/integration/anchored-workspace.spec.ts --grep "anchored gap closure"`
3. Smoke scenario: at 1440px reveal unchanged context, pointer-activate non-line-1 base then head, and verify the sole composer header/zone identity.
4. Smoke scenario: at 1200px and 900px resize with text present, confirm one anchored textarea, usable Close controls, inactive-drawer inertness, and focus return.
5. Smoke scenario: inspect/copy stale and orphaned records, verifying exact facts remain in the rail and no inline relocation occurs.
6. Confirm no formatter, linter, project-wide test, package install, or schema command runs.
</verification>

<acceptance_criteria>
- DIFF-07 and CMT-01 are no longer blocked: arbitrary visible model lines have real pointer/focus affordances and one exact zone composer.
- D-05 movement confirmation has distinct, tested preserve/discard outcomes.
- CMT-08 degraded records are actionable only through safe recorded-fact controls.
- DRFT-02 exact recovered records retain correct file capability across responsive transitions.
- All focused automated checks and explicit viewport smoke scenarios pass.
</acceptance_criteria>

<success_criteria>
A developer can select any visible base/head model line, use one durable inline composer without accidental loss, navigate responsive drawers safely, and inspect degraded recorded anchors without false relocation.
</success_criteria>

## Artifacts this phase produces

- `src/web/monaco/diff-adapter.ts`
  - `AnchorAffordanceTarget`
  - model-position affordance callback/lifecycle
- `src/web/model/workspace-state.ts`
  - pending composer move target
  - `WorkspaceEvent` variant `confirm-move`
- `src/web/components/CommentComposer.vue`
  - `confirmMove` emission from the shared `Discard draft` control only when a pending move exists
  - exact UI-SPEC prompt plus `Keep writing` and `Discard draft` labels shared by ordinary closure and movement
- `src/web/components/DiffWorkspace.vue`
  - exact-side/line affordance data attributes
  - sole Monaco-zone composer mount/focus path
- `src/web/components/CommentsRail.vue`
  - `copy-recorded-anchor` event
  - `inspect-recorded-file` event
  - recorded anchor detail panel
- `src/web/App.vue`
  - exact recorded-file inspection and clipboard handlers
  - separate comments/files drawer media state and focus return
- `src/web/styles.css`
  - model-line affordance and sole Monaco-zone composer presentation
  - 1439px comments-drawer and 1099px files-drawer visibility, inertness, and focus-state styling
- `tests/unit/workspace-state.test.ts`
  - pending move target, shared-confirmation routing, preservation, relocation, and pending-save transition contracts
- `tests/integration/anchored-workspace.spec.ts`
  - `anchored gap closure` browser contract

<output>
Create `.planning/phases/02-anchored-diff-review/02-09-SUMMARY.md` when done.
</output>
