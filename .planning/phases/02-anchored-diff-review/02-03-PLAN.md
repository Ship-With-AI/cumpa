---
phase: 02-anchored-diff-review
plan: 03
type: tdd
wave: 3
depends_on: [02-02]
files_modified:
  - src/contracts/draft.ts
  - src/domain/comparison-key.ts
  - src/server/draft-store.ts
  - src/server/routes.ts
  - tests/unit/comparison-key.test.ts
  - tests/api/draft.test.ts
  - tests/api/draft-atomicity.test.ts
autonomous: true
requirements: [CMT-01, CMT-02, CMT-08, DRFT-01, DRFT-02, DRFT-03]
must_haves:
  truths:
    - "The repository-local draft key is a deterministic domain-separated hash of the full resolved selected base commit OID followed by full resolved selected head commit OID; changing either endpoint isolates the draft even when merge base/head would match."
    - "One strict schema-versioned canonical JSON draft stores comparison identities and accepted open comments with exact canonical anchors; Phase 2 exposes no edit/delete/resolve/summary-mutation or client revision-conflict behavior."
    - "Each accepted add is serialized per repository/comparison, validates the complete next document, writes a unique restrictive same-directory temp, syncs, closes, renames over canonical without delete/truncate, syncs the directory where supported, and returns success only after the persistence boundary succeeds."
    - "Same-pair relaunch resumes accepted comments; another base or head opens an isolated draft; duplicate side-line adds return a constrained conflict and no failed write replaces or corrupts the prior canonical draft."
  artifacts:
    - path: src/domain/comparison-key.ts
      provides: "Ordered selected-base/selected-head comparison key"
    - path: src/server/draft-store.ts
      provides: "Strict repository-local load and serialized atomic mutation"
    - path: tests/api/draft-atomicity.test.ts
      provides: "Filesystem fault-injection and concurrent-add behavior contract"
  key_links:
    - from: src/server/routes.ts
      to: src/server/draft-store.ts
      via: "canonical server-built comment is accepted only after atomic add returns"
      pattern: "addComment"
    - from: src/server/draft-store.ts
      to: src/domain/comparison-key.ts
      via: "server-owned active pinned comparison chooses the fixed drafts filename"
      pattern: "comparisonKey"
---

<objective>
Implement comparison-keyed repository-local draft resume and atomic accepted-comment persistence through RED→GREEN→REFACTOR.

Purpose: Make D-03/D-06 and DRFT-01–DRFT-03 true at the filesystem/API boundary before the browser can show an accepted comment.
Output: Deterministic comparison identity, strict v1 draft store, closed draft routes, duplicate enforcement, and focused fault-injected API tests.
</objective>

## Phase Goal
**As a** developer reviewing a pinned local comparison, **I want to** inspect immutable text side by side and attach precise comments that remain stable, **so that** I can resume trustworthy feedback without publishing branches or relocating anchors.

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-RESEARCH.md
@.planning/phases/02-anchored-diff-review/02-02-SUMMARY.md
</context>

<feature>
  <name>Atomic comparison-local comment drafts</name>
  <files>src/contracts/draft.ts, src/domain/comparison-key.ts, src/server/draft-store.ts, src/server/routes.ts, tests/unit/comparison-key.test.ts, tests/api/draft.test.ts, tests/api/draft-atomicity.test.ts</files>
  <behavior>
    - Identical full selected base/head OIDs produce one key; changing base or head produces a distinct key; labels, refs, merge base alone, paths, and short IDs do not influence the key.
    - GET draft uses only the active launch comparison and returns a strict empty-or-resumed view with exact verified/stale/orphaned presentation states.
    - POST add accepts only canonical server-derived anchors, rejects an existing exact side/path/blob/line uniqueness key with 409, and emits 201 only after atomic replacement succeeds.
    - Open/write/sync/close/rename failures preserve the last valid canonical bytes and never expose accepted success; temp cleanup never removes canonical; simultaneous adds serialize without losing either distinct comment.
    - Invalid existing JSON/schema is refused and preserved, not overwritten; recovery UI belongs to Phase 3, but safe failure is mandatory now.
  </behavior>
  <implementation>Extend the actual reconciled Fastify route registration and active pinned comparison object. Keep repository root and comparison key entirely server-owned. Use Node 24 filesystem primitives; add no atomic-write/locking package.</implementation>
</feature>

<tasks>
<task type="auto">
  <name>Task 1: RED — specify comparison isolation, resume, duplicate, and atomic failure semantics</name>
  <files>tests/unit/comparison-key.test.ts, tests/api/draft.test.ts, tests/api/draft-atomicity.test.ts</files>
  <read_first>Read the actual Phase 1 app/session shutdown seam from 02-01-SUMMARY.md, the canonical anchor/API contracts from 02-02-SUMMARY.md, Research Patterns 6–8, and DRFT-01–DRFT-03. Reuse the implemented temporary-repository and Fastify inject conventions.</read_first>
  <action>Write tests before production code. Key cases use full opaque SHA-1/SHA-256-length OIDs and prove ordered base/head framing, same merge-base with changed selected base isolation, changed head isolation, and label/ref irrelevance. API cases prove strict empty draft, same-pair resume across app/store recreation, different-pair empty isolation, canonical accepted records, exact duplicate 409, stale/orphan view classification, authorization before store work, rejection of client comparison/draft/path/revision/anchor fields, and preservation/refusal of invalid existing JSON. Fault-injection cases independently fail temp mkdir/open(`wx`)/write/sync/close/rename/directory-sync boundaries, assert response never reports accepted before the defined boundary, canonical prior bytes remain schema-valid and unchanged on pre-rename failures, temp-only cleanup, restrictive mode, and two near-simultaneous distinct adds both survive serialized mutation. Account for documented unsupported directory sync without treating it as partial-write success/failure ambiguity. RED must be missing store behavior, not filesystem/platform/test setup. Commit `test(02-03): add failing atomic draft contract`.</action>
  <verify><automated>
    <command expected="nonzero" classification="behavioral-red">npm run test:unit -- tests/unit/comparison-key.test.ts</command>
    <command expected="nonzero" classification="behavioral-red">npm run test:api -- tests/api/draft.test.ts tests/api/draft-atomicity.test.ts</command>
  </automated></verify>
  <done>Focused key/API/fault contracts reach named behavioral RED and the test commit precedes implementation.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — persist one canonical draft per pinned comparison atomically</name>
  <files>src/contracts/draft.ts, src/domain/comparison-key.ts, src/server/draft-store.ts, src/server/routes.ts</files>
  <read_first>Read the RED tests, reconciled active-comparison/repository-root dependency injection, and canonical anchor builder. Preserve existing strict error/response/security conventions.</read_first>
  <action>Compute a lowercase SHA-256 comparison key from a fixed domain plus unsigned-64-bit-length-framed full selected base then head OIDs. Store under the fixed repository-root-relative `.diff-review/drafts/&lt;hex-key&gt;.json`; never accept any path/key/comparison selector from the browser. Define strict ReviewDraftV1 with schemaVersion 1, full selected base/head/merge-base identities, internal revision, an initialized empty summary only if the canonical schema requires it (no Phase 2 summary route/UI), and comments containing immutable ID, open state, body, canonical DurableAnchorV1, createdAt/updatedAt. Load and validate existing bytes before mutation and refuse invalid/unsupported documents without replacing them. Serialize mutations per repository/comparison, reject duplicate canonical anchor key, validate the complete next document, create a unique same-directory `wx` temp with restrictive permissions, write all bytes, file-sync, close, rename directly over canonical without deleting/truncating it, and directory-sync where supported. Clean only the temp on failure; return the canonical comment/201 only after the defined persistence boundary. Implement authenticated `GET /api/draft` and complete `POST /api/draft/comments`, with exact verification classification on views and safe generic errors/correlation. Do not claim cross-process or multi-tab optimistic concurrency, active selector drift, comment lifecycle, summary editing, export, or corrupt-file recovery controls. Run identical commands to GREEN and commit `feat(02-03): implement atomic comparison drafts`.</action>
  <verify><automated>
    <command expected="0">npm run test:unit -- tests/unit/comparison-key.test.ts</command>
    <command expected="0">npm run test:api -- tests/api/draft.test.ts tests/api/draft-atomicity.test.ts</command>
  </automated></verify>
  <done>Same-pair resume, pair isolation, duplicate rejection, exact view states, and every atomicity invariant are GREEN.</done>
</task>

<task type="auto">
  <name>Task 3: REFACTOR — isolate validated documents from filesystem commit mechanics</name>
  <files>src/contracts/draft.ts, src/domain/comparison-key.ts, src/server/draft-store.ts, src/server/routes.ts</files>
  <read_first>Read only the GREEN store/route implementation and current focused output.</read_first>
  <action>Separate pure next-document validation/keying from the serialized atomic replacement boundary where this improves fault injection. Keep one canonical write path, no delete-first branch, no browser authority, and no extra dependency. Rerun identical commands unchanged. Commit `refactor(02-03): separate draft mutation and atomic commit` only if production changes.</action>
  <verify><automated>
    <command expected="0">npm run test:unit -- tests/unit/comparison-key.test.ts</command>
    <command expected="0">npm run test:api -- tests/api/draft.test.ts tests/api/draft-atomicity.test.ts</command>
  </automated></verify>
  <done>Post-refactor tests remain GREEN and the TDD sequence is recorded.</done>
</task>
</tasks>

<acceptance_criteria>
- Accepted means the server's atomic persistence boundary completed; no early response or optimistic canonical state exists.
- Same full selected base/head pair resumes the same validated repository-local draft, while either changed selected endpoint isolates it.
- Duplicate exact side-specific anchors cannot create a second comment; failed adds preserve both the composer contract for Plan 04 and prior canonical bytes.
- Invalid existing data is preserved and safely refused without pulling Phase 3 recovery UI or concurrency behavior into scope.
</acceptance_criteria>

## Artifacts this phase produces
- `src/domain/comparison-key.ts` — deterministic selected-base/selected-head key.
- `src/server/draft-store.ts` — strict load, serialized mutation, and atomic replacement.
- `tests/api/draft.test.ts` and `tests/api/draft-atomicity.test.ts` — resume/isolation/security/fault contracts.

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Browser → draft routes | Browser is untrusted and cannot select repository, comparison, key, path, anchor facts, or revision. |
| Draft store → repository filesystem | Only server-computed fixed-root hex paths may be opened or replaced. |
| Existing JSON → application | Repository-local data is untrusted and strictly validated before use or mutation. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-11 | Tampering | comparison identity | mitigate | Domain-separated ordered hash over full selected base/head OIDs; store and verify both identities. |
| T-02-12 | Tampering | canonical draft | mitigate | Strict full-document validation and synced same-directory temp→rename; never delete/truncate canonical first. |
| T-02-13 | Elevation of Privilege | draft path | mitigate | Fixed repository-local root, server-computed hex filename, random temp, `wx`, restrictive mode, no client naming. |
| T-02-14 | Denial of Service | concurrent mutations | mitigate | Per-repository/comparison in-process serialization and bounded body/document schemas. |
| T-02-15 | Information Disclosure | draft errors/logs | mitigate | Correlation/generic browser error; do not log tokens, bodies, blob text, absolute repository paths, or document contents. |
| T-02-16 | Repudiation | accepted state | mitigate | Canonical IDs/timestamps and success only after atomic replacement boundary; focused fault evidence. |
| T-02-SC | Tampering | dependencies | mitigate | Node 24 filesystem/crypto only; no package install. |
</threat_model>

<verification>The two focused commands demonstrate behavioral RED, GREEN, and post-refactor GREEN. Tests assert observable canonical bytes/responses rather than only mocked syscall order. No formatter, linter, project-wide suite, or browser test runs.</verification>

<success_criteria>
- DRFT-01–DRFT-03 and persistence aspects of D-03/D-06 are complete at the API/store boundary.
- TDD gate commits exist in order.
- Phase 3 conflict, lifecycle, summary mutation, schema-recovery UI, and selector drift remain absent.
</success_criteria>

<output>Create `.planning/phases/02-anchored-diff-review/02-03-SUMMARY.md` with RED/GREEN/REFACTOR evidence, comparison-key vectors, canonical schema, atomic fault matrix, concurrency result, same/different pair evidence, security denials, and commits.</output>
