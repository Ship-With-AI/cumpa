---
phase: 06-monaco-diff-semantics
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/web/styles.css
  - scripts/verify-semantic-css.mjs
  - src/web/monaco/theme.ts
  - tests/unit/monaco-theme.test.ts
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
  - DIFF-05
must_haves:
  truths:
    - "The one stable diff-review-dark Monaco theme is typed, idempotent, and ready to be selected before editor construction."
    - "Monaco canvas, syntax, diff, gutter, widget, selection, hunk, empty-region, and focus roles use the Phase 05 semantic dark contract with exact approved values."
    - "Every syntax foreground remains readable over normal, whole-line, and intraline diff layers, and selected text has an explicit white foreground contract."
  artifacts:
    - path: "src/web/monaco/theme.ts"
      provides: "Typed Monaco theme identity, data, and idempotent registration/selection helper"
      exports: ["DIFF_REVIEW_THEME_ID", "DIFF_REVIEW_THEME", "applyDiffReviewTheme"]
    - path: "tests/unit/monaco-theme.test.ts"
      provides: "RED/GREEN exact-value, parity, completeness, and call-order contract"
    - path: "src/web/styles.css"
      provides: "Five approved syntax semantic roles in the sole root token contract"
    - path: "scripts/verify-semantic-css.mjs"
      provides: "Source/generated CSS allowlist and exact-value coverage for the syntax roles"
  key_links:
    - from: "src/web/monaco/theme.ts"
      to: "src/web/styles.css"
      via: "byte-for-byte theme/root semantic-role parity asserted by tests"
      pattern: "DIFF_REVIEW_THEME|--syntax-"
    - from: "scripts/verify-semantic-css.mjs"
      to: "src/web/styles.css"
      via: "canonicalTokens and expectedValues audit the only semantic root"
      pattern: "canonicalTokens|expectedValues"
  prohibitions:
    - "No second token root, compatibility alias, component palette, runtime CSS-token scraping, or silent vs-dark fallback."
    - "No new dependency, remote asset, extra theme, theme switch, Primer runtime, icon package, or font package."
    - "No API, schema, persistence, Git, CLI, server, export, or review-mechanics change."
---

<objective>
Establish the exact typed Monaco theme contract that later editor integration consumes, implementing the restrained whole-line/strong intraline hierarchy and independent selection/focus channels required by D-01, D-04, D-05, D-06, D-07, and D-08.

Purpose: Prevent a competing palette or base-theme first paint by making the approved Phase 05-to-Monaco mapping complete, deterministic, typed, and unit-tested before adapter construction is touched.
Output: Five canonical syntax roles, an extended semantic CSS audit, `src/web/monaco/theme.ts`, and a focused Vitest contract.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-monaco-diff-semantics/06-CONTEXT.md
@.planning/phases/06-monaco-diff-semantics/06-RESEARCH.md
@.planning/phases/06-monaco-diff-semantics/06-PATTERNS.md
@.planning/phases/06-monaco-diff-semantics/06-UI-SPEC.md
@.planning/phases/05-semantic-dark-foundation/05-CONTEXT.md
@.planning/phases/05-semantic-dark-foundation/05-01-SUMMARY.md
@.planning/intel/API-SURFACE.md

<interfaces>
Existing semantic root: `src/web/styles.css :root` is the only color-token authority. Existing Monaco analog: `src/web/monaco/configure.ts` exports synchronous, idempotent browser configuration. Existing unit analog: `tests/unit/line-mapping.test.ts` uses direct Vitest behavior assertions and `.js` ESM imports.

New theme interface: `DIFF_REVIEW_THEME_ID` is exactly `diff-review-dark`; `DIFF_REVIEW_THEME` satisfies `monaco.editor.IStandaloneThemeData`; `applyDiffReviewTheme(editorApi?)` calls `defineTheme` and then `setTheme` for that stable identifier. The optional narrow editor API seam exists only to test ordering without mocking Monaco internals.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend the sole semantic root and its generated-CSS audit</name>
  <read_first>
    - `src/web/styles.css` — modified file and Phase 05 canonical root.
    - `scripts/verify-semantic-css.mjs` — modified file and exact allowlist/value audit.
    - `.planning/phases/06-monaco-diff-semantics/06-UI-SPEC.md` — exact Direct Semantic-Token Contract and syntax table.
    - `.planning/phases/06-monaco-diff-semantics/06-CONTEXT.md` — D-01 and Phase 05 reuse boundary.
    - `.planning/phases/05-semantic-dark-foundation/05-CONTEXT.md` — D-05/D-06 single role-based vocabulary source of truth.
    - `.planning/phases/05-semantic-dark-foundation/05-01-SUMMARY.md` — established semantic audit analog and generated-CSS gate.
  </read_first>
  <files>src/web/styles.css, scripts/verify-semantic-css.mjs</files>
  <action>Add only `--syntax-keyword-foreground: #D2A8FF`, `--syntax-string-foreground: #A5D6FF`, `--syntax-number-foreground: #F2CC60`, `--syntax-type-foreground: #79C0FF`, and `--syntax-invalid-foreground: #FFA198` to the existing single `:root`. Extend both `canonicalTokens` and `expectedValues` with those exact names and bytes. Preserve every existing token, direct-color confinement rule, forced-colors exception, root uniqueness rule, typography value, spacing value, and breakpoint. Do not add aliases, a Monaco/component token block, or component raw colors; the five roles are the approved narrow accessibility extension needed because former interactive/status hues fail over the locked intraline composites.</action>
  <acceptance_criteria>
    - The source root contains exactly one declaration for each of the five approved syntax roles with the exact uppercase hex value.
    - `canonicalTokens` and `expectedValues` require all five roles in both source CSS and Vite-emitted CSS.
    - The semantic audit continues to reject a second root, missing/extra tokens, mismatched values, legacy aliases, and paint-bearing raw colors outside the canonical root.
    - No existing semantic role, font metric, spacing scale, responsive rule, or forced-colors repair changes.
  </acceptance_criteria>
  <verify>
    <automated>npm run build:web &amp;&amp; node scripts/verify-semantic-css.mjs</automated>
  </verify>
  <done>The Phase 05 single-root contract contains and audits the five Phase 06 syntax roles without creating another palette.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: RED/GREEN the complete typed diff-review-dark theme</name>
  <read_first>
    - `src/web/monaco/theme.ts` — modified new file; confirm it is absent before RED.
    - `tests/unit/monaco-theme.test.ts` — modified new file; confirm it is absent before RED.
    - `src/web/styles.css` — modified prerequisite and byte-parity source of truth.
    - `src/web/monaco/configure.ts` — synchronous idempotent configuration analog.
    - `tests/unit/line-mapping.test.ts` — focused Vitest/ESM analog.
    - `.planning/phases/06-monaco-diff-semantics/06-UI-SPEC.md` — exact Monaco roles, syntax scopes, alpha bytes, typography, and first-paint contract.
    - `.planning/phases/06-monaco-diff-semantics/06-RESEARCH.md` — pinned Monaco 0.55.1 public API and theme recommendations.
  </read_first>
  <files>src/web/monaco/theme.ts, tests/unit/monaco-theme.test.ts</files>
  <behavior>
    - RED: the theme ID is `diff-review-dark`, data has `base: 'vs-dark'` and `inherit: true`, and a fake editor API records `defineTheme` before `setTheme` on every call.
    - RED: theme rules cover default/source, comment, keyword/storage/control, string, number/constants, type/class/interface/namespace, function/method/identifier/variable/property, operator/delimiter/punctuation, tag/attribute, and invalid scopes with the exact approved regular styles; invalid alone is underlined.
    - RED: theme/root parity is exact for `E6EDF3`, `B1BAC4`, `FFFFFF`, and all five new syntax bytes.
    - RED: every required Monaco color role is present with the exact UI-SPEC value.
  </behavior>
  <action>First write `tests/unit/monaco-theme.test.ts`, run it, and commit the failing RED gate as `test(06-01): add failing Monaco theme contract`. Then create `src/web/monaco/theme.ts` importing `monaco-editor`, export `DIFF_REVIEW_THEME_ID`, `DIFF_REVIEW_THEME`, and `applyDiffReviewTheme`, and commit the GREEN gate as `feat(06-01): define semantic Monaco theme`. The theme must satisfy `monaco.editor.IStandaloneThemeData` and explicitly map: canvas/text/gutter/line-number/cursor/whitespace/indent roles to `#0D1117`, `#E6EDF3`, `#010409`, `#8B949E`, `#E6EDF3`, `#58A6FF`, `#484F5880`, `#21262D`, `#30363D`; focused/inactive selection to `#388BFD59`/`#388BFD40` with `editor.selectionForeground: #FFFFFF`; transparent active-row fill `#00000000`, active edge `#484F58`, and overview border `#30363D`; inserted/removed lines `#2EA04326`/`#F8514926`, inserted/removed text `#2EA04359`/`#F8514959`, intraline borders and overview cues `#3FB950`/`#F85149`, matching gutter layers, and center border `#30363D`; `diffEditor.diagonalFill: #010409`; unchanged-region background/foreground/shadow `#161B22`/`#A371F7`/`#00000000`; `editorLink.activeForeground: #A371F7`; widget/hover surfaces `#21262D`, text `#E6EDF3`, borders `#30363D`; `focusBorder: #58A6FF`; scrollbar rest/hover/active `#30363D80`, `#484F58A6`, `#484F58`. Token rules set no background, bold, or italics and add no font-metric or letter-spacing changes. `applyDiffReviewTheme` synchronously invokes `defineTheme(DIFF_REVIEW_THEME_ID, DIFF_REVIEW_THEME)` then `setTheme(DIFF_REVIEW_THEME_ID)` on every call; repeated calls remain safe and do not generate IDs or silently fall back. Refactor only after GREEN and keep the focused test green.</action>
  <acceptance_criteria>
    - The RED test is observed failing before `theme.ts` exists and the RED and GREEN commits use the required `test(06-01)` then `feat(06-01)` prefixes.
    - `DIFF_REVIEW_THEME` is statically checked as `IStandaloneThemeData`, contains every UI-SPEC role and scope, and contains no unapproved palette value.
    - Theme values are byte-identical to canonical CSS roles, including all five syntax roles and `editor.selectionForeground === '#FFFFFF'`.
    - Two consecutive `applyDiffReviewTheme` calls each record `defineTheme` before `setTheme` with the same stable identifier.
    - No runtime DOM/CSS-token extraction, network access, package addition, or theme fallback path exists.
  </acceptance_criteria>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/unit/monaco-theme.test.ts &amp;&amp; npm run build:web &amp;&amp; node scripts/verify-semantic-css.mjs</automated>
  </verify>
  <done>The typed theme and exact parity contract are GREEN and ready for the Wave 2 adapter to select before construction.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New files:** `src/web/monaco/theme.ts`; `tests/unit/monaco-theme.test.ts`.
- **New exported symbols:** `DIFF_REVIEW_THEME_ID`, `DIFF_REVIEW_THEME`, `applyDiffReviewTheme`.
- **New private/internal symbols:** none.
- **New CSS fields:** `--syntax-keyword-foreground`, `--syntax-string-foreground`, `--syntax-number-foreground`, `--syntax-type-foreground`, `--syntax-invalid-foreground`.
- **New API endpoints/client methods:** none.
- **New persistence/export/Zod schema fields:** none.
- **New package scripts or user commands:** none; verification invokes existing `build:web`, Vitest, and semantic-audit entry points.
- **New dependencies, registries, remote assets, environment variables, or human setup:** none.

<contribution_decisions>
- External/package coverage: no install or upgrade; existing `monaco-editor@0.55.1` and `@playwright/test@1.61.1` remain lockfile-pinned. The research `SUS` recency label does not trigger an install checkpoint because no package-manager operation occurs.
- API and schema coverage: no route, client, Zod, draft, export, CLI, server, or persistence contribution; `.planning/intel/API-SURFACE.md` is incomplete and its silence is not used as absence evidence.
- Assumption delta: none at plan time. If execution finds a visible inherited Monaco role outside the mandatory map, map it to the nearest existing semantic role, add a focused assertion, and record the delta in `06-01-SUMMARY.md`; do not mint a color.
- TDD contribution: Task 2 is a dedicated RED→GREEN→optional REFACTOR feature contract. Task 1 is configuration/audit prerequisite and is not behavior logic.
- Drift decision: if installed or locked Monaco is not exactly 0.55.1, stop before implementation and re-check theme roles and pinned DOM hooks; do not silently adapt to another version.
- UI contribution: yes—typed palette/syntax contract only. Browser first-paint, geometry, overlap, and real Monaco proof are mandatory in dependent Plan 03, not claimed here.
</contribution_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Canonical CSS root → bundled typed Monaco theme | Duplicate literals could drift and misrepresent additions, deletions, selections, or focus. |
| Local source → Vite-emitted CSS | Build transformation must retain the one-root allowlist and exact semantic values. |
| Dependency graph → browser bundle | Existing pinned Monaco/Playwright code is consumed locally; this plan performs no install or network fetch. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering | high | `theme.ts` semantic mapping | mitigate | Exact role completeness and byte-parity tests plus the source/generated CSS audit block a theme that can invert or erase diff meaning. |
| T-06-02 | Information Disclosure | high | theme/font/icon sourcing | mitigate | Keep all theme data, Codicons, and fonts local; add no URL, remote asset, dependency, telemetry, or runtime token service. |
| T-06-03 | Spoofing | medium | theme identity/registration | mitigate | Use only stable `diff-review-dark`; assert `defineTheme` then `setTheme` on every call and forbid generated IDs or fallback. |
| T-06-SC | Tampering | low | npm supply chain | accept | No package install/upgrade occurs; pinned packages were audited in research. Any unexpected lockfile/package change stops execution. |

High-severity gate: execution is blocked until T-06-01 and T-06-02 are demonstrably mitigated by the focused gates above; an unmitigated high finding cannot be accepted.
</threat_model>

<verification>
1. `./node_modules/.bin/vitest run tests/unit/monaco-theme.test.ts` passes after an observed RED gate.
2. `npm run build:web &amp;&amp; node scripts/verify-semantic-css.mjs` proves source/generated root parity and author-style confinement.
3. Review the diff to confirm only the four declared files changed and no package, API, schema, or runtime behavior was added.
</verification>

<success_criteria>
- DIFF-01 has a complete typed semantic theme prerequisite, and DIFF-02/DIFF-05 exact layer values are contractually available for integration.
- D-01 and D-04 restraint/hunk hierarchy plus D-05–D-08 independent state channels are encoded without changing editor geometry or behavior.
- The theme is complete, idempotent, pinned to Monaco 0.55.1 public APIs, and cannot drift silently from Phase 05 roles.
</success_criteria>

<output>
Create `.planning/phases/06-monaco-diff-semantics/06-01-SUMMARY.md` when complete, including RED/GREEN commits, exact verification results, any inherited-role assumption delta, and confirmation that API/schema/package contributions remain none.
</output>
