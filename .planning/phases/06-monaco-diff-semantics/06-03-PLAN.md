---
phase: 06-monaco-diff-semantics
plan: 03
type: execute
wave: 2
depends_on:
  - 06-01
  - 06-02
files_modified:
  - src/web/monaco/diff-adapter.ts
  - src/web/styles.css
  - src/web/prototypes/MonacoStabilityPrototype.vue
  - tests/integration/monaco-anchor.spec.ts
  - tests/integration/anchored-workspace.spec.ts
autonomous: true
requirements:
  - DIFF-01
  - DIFF-02
  - DIFF-03
  - DIFF-04
  - DIFF-05
must_haves:
  truths:
    - "Users see Monaco canvas, tokens, gutters, widgets, scrollbars, separators, hidden regions, and the Phase 05 workspace as one coordinated dark interface from first paint."
    - "Users can distinguish quiet whole-line changes, stronger intraline spans, unchanged code, collapsed/revealed hunks, and flat recessed empty counterparts while code remains readable."
    - "Users can identify Base/deletion and Head/addition through BASE/HEAD labels, side position, continuous bars, and sparse literal minus/plus signs without hue recognition."
    - "Users retain legible line numbers, active-line emphasis, and the existing 32px comment affordance without code, gutter, sash, scroll, or paired-zone reflow."
    - "Users can simultaneously distinguish diff meaning, selection, active line, hover target, persistent anchor, and focused pane in every required overlap state."
  artifacts:
    - path: "src/web/monaco/diff-adapter.ts"
      provides: "Theme-before-construction integration and bounded independent diff/selection decoration lifecycles"
    - path: "src/web/styles.css"
      provides: "Non-geometric Monaco hooks for signed bars, selection edge/foreground, empty/hunk regions, anchor rail, and pane focus"
    - path: "src/web/prototypes/MonacoStabilityPrototype.vue"
      provides: "Deterministic TypeScript/JSON, range-density, empty-side, selection, focus, anchor, and recompute fixtures"
    - path: "tests/integration/monaco-anchor.spec.ts"
      provides: "Real-Chromium theme, semantic layer, contrast, overlap, geometry, accessibility, and bounded-lifecycle proof"
    - path: "tests/integration/anchored-workspace.spec.ts"
      provides: "Production workspace BASE/HEAD, exact comment action, localized overflow, and paired-zone no-reflow proof"
  key_links:
    - from: "src/web/monaco/diff-adapter.ts"
      to: "src/web/monaco/theme.ts"
      via: "applyDiffReviewTheme immediately before monaco.editor.createDiffEditor"
      pattern: "applyDiffReviewTheme.*createDiffEditor"
    - from: "src/web/monaco/diff-adapter.ts"
      to: "src/web/monaco/diff-semantics.ts"
      via: "onDidUpdateDiff passes getLineChanges and actual model line counts into stable per-side collections"
      pattern: "onDidUpdateDiff|buildDiffDecorations|getLineChanges"
    - from: "src/web/monaco/diff-adapter.ts"
      to: "src/web/styles.css"
      via: "fixed phase-owned pane, bar, sign, selection, and anchor class hooks"
      pattern: "monaco-diff-|monaco-selection-|monaco-anchor-line"
    - from: "tests/integration/monaco-anchor.spec.ts"
      to: "src/web/prototypes/MonacoStabilityPrototype.vue"
      via: "real Vite/Chromium fixture and public adapter diagnostics"
      pattern: "monaco-stability|__monacoStabilityPrototype"
  prohibitions:
    - "No adapter replacement, model mutation, second diff algorithm, line-mapping change, new view zone/widget, private Monaco service, or custom renderer."
    - "No `renderSideBySide`/inline-breakpoint/hidden-region constant/accessibility-label/keyboard-command/comment lifecycle/persistence behavior change."
    - "No opaque interaction row fill, repeated long-block signs, injected text, dynamic repository-derived class, gutter width/padding, font-metric change, page animation, or diagonal empty texture."
    - "No detailed Phase 07 review-surface restyle or Phase 08 responsive, 400% zoom, forced-colors, or milestone-wide continuity claim."
---

<objective>
Integrate the Wave 1 typed theme and semantic decoration builder into the existing public Monaco adapter, then prove every required first-paint, diff-language, overlap, no-reflow, accessibility, and lifecycle contract in real Chromium.

Purpose: Complete all five Phase 06 roadmap outcomes while preserving the shipped editor geometry, immutable review mechanics, and Phase 05 semantic authority.
Output: Bounded adapter visual-state collections, phase-owned global CSS hooks, deterministic browser fixtures, and focused Monaco plus real-workspace Playwright evidence.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-monaco-diff-semantics/06-CONTEXT.md
@.planning/phases/06-monaco-diff-semantics/06-RESEARCH.md
@.planning/phases/06-monaco-diff-semantics/06-PATTERNS.md
@.planning/phases/06-monaco-diff-semantics/06-UI-SPEC.md
@.planning/phases/05-semantic-dark-foundation/05-CONTEXT.md
@.planning/phases/05-semantic-dark-foundation/05-01-SUMMARY.md
@.planning/phases/06-monaco-diff-semantics/06-01-SUMMARY.md
@.planning/phases/06-monaco-diff-semantics/06-02-SUMMARY.md
@.planning/intel/API-SURFACE.md

<interfaces>
From Plan 01: `DIFF_REVIEW_THEME_ID`, `DIFF_REVIEW_THEME`, and `applyDiffReviewTheme` in `src/web/monaco/theme.ts`.
From Plan 02: `buildDiffDecorations(changes, side, modelLineCount)` in `src/web/monaco/diff-semantics.ts`.
Existing adapter: `PublicMonacoDiffAdapter` owns one `IStandaloneDiffEditor`, original/modified editors, immutable models, thirteen current static listeners/actions, `onDidUpdateDiff`, separate anchor collections, paired zones, file view state, and diagnostics. Existing update order is diff update → `rebuildAnchoredLayout()` → `refreshAnchorAffordance()` → `onChange()`.
Existing workspace: `DiffWorkspace.vue` preserves the section label, exact BASE/HEAD copy, absolutely positioned 32px native gutter action and Option+Enter title, Monaco host, composer lifecycle, and unchanged-context guidance; it is a no-change source of truth.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Select the theme before construction and integrate bounded signed diff layers</name>
  <read_first>
    - `src/web/monaco/diff-adapter.ts` — modified construction, update, model, anchor, and disposal lifecycle.
    - `src/web/styles.css` — modified sole semantic root consumer and current workspace geometry.
    - `src/web/prototypes/MonacoStabilityPrototype.vue` — modified deterministic real-Monaco fixture.
    - `tests/integration/monaco-anchor.spec.ts` — modified real-browser analog and ten-update lifecycle contract.
    - `tests/integration/anchored-workspace.spec.ts` — modified production workspace BASE/HEAD/comment-action/overflow analog.
    - `src/web/monaco/theme.ts` — Plan 01 theme source of truth.
    - `src/web/monaco/diff-semantics.ts` — Plan 02 decoration builder source of truth.
    - `src/web/components/DiffWorkspace.vue` — no-change lifecycle, copy, ARIA, side-label, host, and gutter-action authority.
    - `src/web/monaco/line-mapping.ts` — no-change durable counterpart mapping authority.
    - `.planning/phases/06-monaco-diff-semantics/06-CONTEXT.md` — D-01 through D-04 and adapter-preservation boundary.
    - `.planning/phases/06-monaco-diff-semantics/06-UI-SPEC.md` — exact theme, range, region, copy, responsive ownership, and verification items 1–8 and 12–16.
    - `tests/e2e/responsive-session.spec.ts` — source-over/WCAG and no-page-overflow browser-test analog.
  </read_first>
  <files>src/web/monaco/diff-adapter.ts, src/web/styles.css, src/web/prototypes/MonacoStabilityPrototype.vue, tests/integration/monaco-anchor.spec.ts, tests/integration/anchored-workspace.spec.ts</files>
  <behavior>
    - First mounted Monaco frame already uses `diff-review-dark`; repeated adapter construction uses the same theme and never flashes an inherited light or uncoordinated dark role.
    - Canvas, gutter, line numbers, divider, scrollbars, hover/context widgets, unchanged/hunk controls, and flat empty counterparts compute to the approved semantic roles.
    - Whole-line additions/deletions retain exact `#26` alpha and intraline spans exact `#59` alpha plus solid semantic edges; no diff decoration sets foreground.
    - Every populated 1-/3-/4+-line range has one continuous 2px bar and the Plan 02 sign density; Base renders literal `−`, Head literal `+`; empty counterparts render no sign.
    - Ten diff updates and A→B→A switches replace semantic decorations atomically and retain bounded models, listeners, zones, composers, bars, and signs.
  </behavior>
  <action>Extend the prototype fixtures and `monaco-anchor.spec.ts` to cover TypeScript and JSON token samples, 1-line, 3-line, 4+-line, intraline replacement, collapsed/revealed unchanged context, normal unchanged code, an added file, and a deleted file. Preserve existing fixture text assertions while making Previous/Next traverse the expanded deterministic `FILES` sequence. Add browser assertions for theme-before-first-frame, exact computed canvas/gutter/line/diff/widget/hunk/empty values, `.diagonal-fill` with `background-image: none`, bars/signs, literal glyphs, and ten-update/A→B→A replacement counts.

Import `applyDiffReviewTheme` and invoke it on the immediately preceding construction path before `monaco.editor.createDiffEditor`; retain every existing editor option and add only `renderIndicators: false`. Import `buildDiffDecorations`; add stable private `originalDiffDecorations` and `modifiedDiffDecorations` collections after public side-editor acquisition. Add private `refreshDiffDecorations()` that calls `getLineChanges()`, passes each current model line count, and atomically `.set()`s both collections in the existing `onDidUpdateDiff` callback before anchor layout/affordance/onChange. Clear diff collections during outgoing model disposal and final disposal without sharing `originalDecorations`/`modifiedDecorations`, which remain anchor-owned.

In global CSS, consume existing variables to render `.monaco-diff-change-bar--base/head` as continuous 2px red/green bars and `.monaco-diff-change-sign--base/head` as borderless/surface-less 12px/16px semibold monospace signs through CSS pseudo-content in the already allocated margin lane; never use Monaco decoration `before`/`after`. Pin `.diagonal-fill` to no background image and `var(--diff-empty-background)`, and style the installed Monaco 0.55.1 hidden-region center/boundary hooks with panel/border defaults and purple hunk foreground only. Do not add raw component colors, width, padding, line height, margin, zones, widgets, transitions, or media rules.</action>
  <acceptance_criteria>
    - `applyDiffReviewTheme()` executes before the first `createDiffEditor()` call, `renderIndicators` alone changes editor options, and all prior options retain their exact values.
    - Real Chromium observes `#0D1117` canvas from the first mounted editor frame, `#010409` gutter/flat empty regions, Phase 05 panel/widget/border roles, and purple-only context controls without a base-theme flash or diagonal texture.
    - Whole-line/intraline fills retain exact `#26`/`#59` alpha bytes, stronger spans lead quieter rows, and no diff foreground override exists.
    - Every continuous bar is 2px; 1–3 lines have one endpoint sign, 4+ have first/last only, Base is U+2212 `−`, Head is ASCII `+`, and empty sides have zero phantom signs.
    - Static signs/bars expose no role, label, tab stop, pointer action, tooltip, injected model text, or dynamic repository-derived class.
    - Recompute and A→B→A assertions show exact current decoration counts, two live models, no duplicate zones/composers, and a constant listener count at this task boundary.
  </acceptance_criteria>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/unit/monaco-theme.test.ts tests/unit/monaco-diff-semantics.test.ts &amp;&amp; npm run build:web &amp;&amp; node scripts/verify-semantic-css.mjs &amp;&amp; npm run test:browser -- tests/integration/monaco-anchor.spec.ts</automated>
    <browser-check>Run `npm run test:browser -- tests/integration/monaco-anchor.spec.ts --headed`; inspect TypeScript, JSON, added-file, and deleted-file fixtures. The first visible editor paint is dark; exact changed words lead quiet changed rows; BASE/deletion signs are `−`, HEAD/addition signs are `+`; long blocks show endpoint signs only; collapsed context is neutral/purple; empty counterparts are flat recessed neutral with no hatch.</browser-check>
  </verify>
  <done>The typed theme and sparse semantic diff layers render from first paint through repeated file/update lifecycles without changing review behavior or geometry.</done>
</task>

<task type="auto">
  <name>Task 2: Compose selection, active line, anchor, hover, and focus without reflow</name>
  <read_first>
    - `src/web/monaco/diff-adapter.ts` — modified stable collection/listener/focus-container lifecycle.
    - `src/web/styles.css` — modified selection, active, anchor, focus, pinned Monaco hook, and no-reflow rules.
    - `src/web/prototypes/MonacoStabilityPrototype.vue` — modified overlap-state fixture controls/data only.
    - `tests/integration/monaco-anchor.spec.ts` — modified contrast/composite/overlap/geometry/accessibility assertions.
    - `tests/integration/anchored-workspace.spec.ts` — modified exact production comment action, labels, zone geometry, and localized-scroll assertions.
    - `src/web/components/DiffWorkspace.vue` — no-change exact section/button/zone/copy/position source of truth.
    - `src/web/monaco/theme.ts` — selected/active/focus exact values.
    - `src/web/monaco/diff-semantics.ts` — persistent base diff channel.
    - `tests/e2e/responsive-session.spec.ts` — `srgbChannel`, contrast, target-size, and page-overflow analog.
    - `.planning/phases/06-monaco-diff-semantics/06-CONTEXT.md` — D-05 through D-08 precedence and geometry decisions.
    - `.planning/phases/06-monaco-diff-semantics/06-UI-SPEC.md` — overlap order, measured matrices, exact names/roles, responsive boundaries, and verification items 4 and 9–18.
  </read_first>
  <files>src/web/monaco/diff-adapter.ts, src/web/styles.css, src/web/prototypes/MonacoStabilityPrototype.vue, tests/integration/monaco-anchor.spec.ts, tests/integration/anchored-workspace.spec.ts</files>
  <behavior>
    - Every non-empty selection in each pane has exactly one phase-owned white-foreground decoration; empty selections have none, multi-selections replace exactly, and inactive selections retain white text after external focus.
    - Selection fill/edge, transparent active-line edge/number, persistent anchor rail, hover comment button, focused-pane perimeter, and underlying diff bar/sign remain simultaneously visible in every required overlap.
    - Text reaches at least 4.5:1 and meaningful edge/bar/sign/rail/focus boundaries at least 3:1 against actual source-over composites, not raw theme literals.
    - Semantic states do not move content x-origin, gutter, line height, pane bounds, 50/50 split, sash, scroll dimensions, comment action, or paired zones.
    - Exact ARIA/copy, keyboard commands, read-only models, file-state restoration, hidden-anchor reveal, and localized overflow remain unchanged.
  </behavior>
  <action>Extend the prototype only with deterministic state exposure needed to select ranges, move focus externally, focus each pane, activate/reveal anchors, hover the production-equivalent target, and replay recomputation; do not move production behavior into the fixture. Extend `monaco-anchor.spec.ts` with the seven mandatory overlap cases: deletion+intraline+active; addition+selection+active; deletion+selection+anchor; addition+active+hover; anchored selected focused Base; anchored selected focused Head; inactive selection after external focus. Parse computed `rgb/rgba`, source-over alpha in the required order, and unrounded WCAG sRGB luminance. Iterate every required TypeScript/JSON token foreground over normal, add/delete `#26`, add/delete `#59`, focused selection over all four diff states, and inactive selection over all four. Require text ratio `>= 4.5`, meaningful boundaries `>= 3`, selected tokens `rgb(255, 255, 255)`, and exact class count equal to current non-empty selections. Record baseline and overlap-state content x-origin, gutter width, line height, editor/pane bounds, 50/50 split, sash, scroll width/height, action position, and paired-zone top/height. Extend `anchored-workspace.spec.ts` to prove the exact section/editor labels, BASE/HEAD text, 32×32 action, exact aria-label/title/`+`, Option+Enter path, decorative sign non-interactivity, paired-zone alignment, and no document-level horizontal overflow at 1440, 1280, 1100, 768, and 640px while Monaco/review-main retains localized two-dimensional ownership.

Add stable private `originalSelectionDecorations` and `modifiedSelectionDecorations` collections, plus one `onDidChangeCursorSelection` and one `onDidChangeModel` listener per side. Add private `refreshSelectionDecorations()` that mirrors every non-empty public `getSelections()` range using only `inlineClassName: 'monaco-selection-contrast-foreground'` and `inlineClassNameAffectsLetterSpacing: false`; call it on both event families and after file/view-state restoration, replace via `.set()`, clear on model disposal, and keep it independent from diff and anchor collections. The four stable listeners raise the existing exact diagnostics baseline from 13 to 17 and remain constant over recomputation. Add fixed `monaco-diff-pane--base` and `monaco-diff-pane--head` classes through each public `getContainerDomNode()` once; use their `:focus-within` state for the focused-pane perimeter without new focus state or listeners.

In global CSS, set `.monaco-selection-contrast-foreground` only to `color: var(--text-on-emphasis) !important`; pin Monaco 0.55.1 `.selected-text` to a 1px inset `var(--selection-border)` edge without foreground or geometric properties; style `.monaco-anchor-line` as a 3px inset `var(--interactive-accent)` rail with no row fill; and give only the focused Base/Head container a non-layout 2px inset `var(--focus-ring)` perimeter. Keep the active row background transparent through the theme, active line number primary, existing absolute gutter action surface-backed/blue/bordered, and all overlap layers independent. Add no opacity on code/panes, forced-color suppression, animation, row hover fill, width, padding, margin, outline expansion, or document overflow.</action>
  <acceptance_criteria>
    - Each pane owns separate diff, selection, and anchor collections; anchor rebuild never clears diff/selection cues, and model/file disposal leaves no stale class.
    - Listener diagnostics are exactly 17 after the four required stable selection/model listeners and remain 17 through ten recomputations and A→B→A restoration.
    - Focused and inactive selections over all four diff composites retain exactly the current non-empty selection classes and computed white foreground; clearing/multi-selection/file switching removes stale ranges.
    - All seven overlap cases preserve the bar/sign, blue selection edge, active edge/number where applicable, 3px anchor rail, 32px hover action where applicable, and 2px focused-pane perimeter.
    - Every token/state composite meets 4.5:1; each meaningful bar/sign/selection edge/anchor rail/focus boundary meets 3:1 using actual layered computed colors.
    - In grayscale, BASE/HEAD labels, side position, U+2212/plus signs, and continuous bars remain sufficient to distinguish deletion/addition without hue.
    - Baseline versus overlap values for content x-origin, gutter width, line height, pane ratio, sash, scroll dimensions, and action position are unchanged; paired zones have equal heights and tops within 1px, with only existing zone insertion/ordinary scrolling exempt.
    - Existing immutable text, read-only language models, F7/Shift+F7, Option+Enter, Escape/focus handoff, hidden reveal, A→B→A state, section/editor/button/zone/spacer names, and comments remain behaviorally unchanged.
    - At 1440/1280/1100/768/640px the document does not gain horizontal overflow; no new media rule or inline/unified diff appears, and Monaco/review-main remains the localized scroll owner.
  </acceptance_criteria>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/unit/monaco-theme.test.ts tests/unit/monaco-diff-semantics.test.ts &amp;&amp; npm run build:web &amp;&amp; node scripts/verify-semantic-css.mjs &amp;&amp; npm run test:browser -- tests/integration/monaco-anchor.spec.ts tests/integration/anchored-workspace.spec.ts</automated>
    <browser-check>Run `npm run test:browser -- tests/integration/monaco-anchor.spec.ts tests/integration/anchored-workspace.spec.ts --headed`. Inspect and capture the seven overlap states at 1280×760 and repeat Base/Head in grayscale: diff fill/sign/bar, white selected text with blue edge, active edge/number, anchor rail, blue bordered comment action, and focused-pane perimeter must coexist. At 1440, 1280, 1100, 768, and 640px confirm the page itself does not scroll horizontally, side-by-side 50/50 ownership remains, and toggling selection/active/anchor/focus causes no visible content, gutter, sash, action, or zone movement.</browser-check>
  </verify>
  <done>All required semantic and interaction layers compose legibly in real Monaco with exact contrast, non-color, accessibility, lifecycle, and no-reflow evidence.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New files in this plan:** none; this plan integrates the new Wave 1 files.
- **New exported symbols:** none.
- **New private adapter fields:** `originalDiffDecorations`, `modifiedDiffDecorations`, `originalSelectionDecorations`, `modifiedSelectionDecorations`.
- **New private adapter methods:** `refreshDiffDecorations`, `refreshSelectionDecorations`.
- **New fixed CSS classes emitted/owned:** `monaco-diff-pane--base`, `monaco-diff-pane--head`, `monaco-diff-change-bar--base`, `monaco-diff-change-bar--head`, `monaco-diff-change-sign--base`, `monaco-diff-change-sign--head`, `monaco-selection-contrast-foreground`.
- **Existing pinned/style hooks newly consumed:** `.selected-text`, `.diagonal-fill`, Monaco hidden-region center/boundary hooks, `.monaco-anchor-line`; these are not new symbols and must be browser-guarded for Monaco 0.55.1.
- **New public adapter fields/methods:** none.
- **New Vue components, product states, copy, controls, commands, or workflows:** none.
- **New API endpoints/client methods:** none.
- **New persistence/export/Zod schema fields:** none.
- **New package scripts, dependencies, registry blocks, remote assets, environment variables, or human setup:** none; all commands invoke existing focused Vitest, Vite, semantic-audit, and Playwright entry points.

<contribution_decisions>
- External/package coverage: no install, upgrade, SDK, service, database, env var, registry block, remote font, icon, or asset. Existing Monaco 0.55.1 and Playwright 1.61.1 stay pinned.
- API/schema coverage: none. This browser-only phase does not alter HTTP routes/clients, Zod contracts, draft JSON, export JSON/Markdown, Git authority, CLI, Fastify, persistence, or security token handling. The incomplete API-SURFACE hint is not treated as proof of nonexistence; direct source boundaries establish no change.
- Assumption delta: research records none. The one implementation verification loop concerns visible inherited Monaco roles: if Chromium exposes one outside the mandatory map, map it to the nearest existing semantic role, add a focused computed-style assertion, and record it in `06-03-SUMMARY.md`; do not add a new palette role. Any product/architecture ambiguity stops execution rather than being guessed.
- Execute contribution: both UI styling and adapter integration tasks retain their behavior blocks and focused browser assertions as standard execute work; Wave 1 unit TDD remains part of every final focused gate.
- Drift decision: pinned private DOM styling is limited to Monaco 0.55.1 `.selected-text`, `.diagonal-fill`, and hidden-region hooks and must be covered by existence/computed-style assertions. A Monaco version drift blocks execution for renewed research. Schema drift is not applicable because schemas are unchanged.
- UI contribution: yes. This plan fulfills the approved Phase 06 UI contract only; Phase 07 still owns detailed header/comment/review surfaces and Phase 08 owns final forced-colors, 400% zoom, responsive restructure, and milestone continuity proof.
- Security contribution: ASVS L1 applies only to browser content-preservation and local-asset boundaries. High-severity misrepresentation or content-injection findings block completion.
</contribution_decisions>

<source_coverage_audit>
## Multi-Source Coverage Audit

| Source | ID | Required outcome | Plan/task | Status |
|--------|----|------------------|-----------|--------|
| GOAL | — | Accurate side-by-side Monaco interpretation under one dark semantic contract | 01–03 | COVERED |
| ROADMAP | SC-1 | Canvas, syntax, gutters, widgets, and workspace coordinate | 01; 03/T1 | COVERED |
| ROADMAP | SC-2 | Addition, deletion, intraline, hunk, unchanged, and empty regions remain readable | 01; 02; 03/T1–T2 | COVERED |
| ROADMAP | SC-3 | Base/deletion versus Head/addition has non-color cues | 02; 03/T1–T2 | COVERED |
| ROADMAP | SC-4 | Line numbers, active line, comment affordance stay legible without reflow | 03/T2 | COVERED |
| ROADMAP | SC-5 | Selection, active, hover, anchor, focus, and diff overlap remain distinct | 03/T2 | COVERED |
| REQ | DIFF-01 | One coordinated Monaco/workspace palette | 01; 03/T1 | COVERED |
| REQ | DIFF-02 | Every diff/context/empty layer legible | 01; 03/T1–T2 | COVERED |
| REQ | DIFF-03 | Base/Head and deletion/addition not color-only | 02; 03/T1–T2 | COVERED |
| REQ | DIFF-04 | Gutters/active/action legible without movement | 02; 03/T2 | COVERED |
| REQ | DIFF-05 | Required interaction overlap remains distinguishable | 01; 03/T2 | COVERED |
| CONTEXT | D-01 | Quiet changed lines, stronger changed spans | 01/T2; 03/T1 | COVERED |
| CONTEXT | D-02 | Persistent `−` Base and `+` Head cues | 02/T1; 03/T1–T2 | COVERED |
| CONTEXT | D-03 | Continuous bars and sparse endpoint signs | 02/T1; 03/T1 | COVERED |
| CONTEXT | D-04 | Neutral unchanged/empty; purple only hunk reveal/separator | 01/T2; 03/T1 | COVERED |
| CONTEXT | D-05 | Diff remains base layer; interaction uses other geometry | 01/T2; 03/T2 | COVERED |
| CONTEXT | D-06 | Blue selection fill/edge preserves signed gutter meaning | 01/T2; 03/T2 | COVERED |
| CONTEXT | D-07 | Active edge/number; hover emphasizes existing action only | 01/T2; 03/T2 | COVERED |
| CONTEXT | D-08 | Anchor rail and 2px focused-pane perimeter coexist | 01/T2; 03/T2 | COVERED |
| RESEARCH | R-1 | Theme registration before construction and first-paint proof | 01/T2; 03/T1 | COVERED |
| RESEARCH | R-2 | Independent diff/selection/anchor collections and bounded listeners | 03/T1–T2 | COVERED |
| RESEARCH | R-3 | Public `getLineChanges()` only; empty-side filtering/merge/clamp | 02/T1; 03/T1 | COVERED |
| RESEARCH | R-4 | Selection foreground decoration plus pinned `.selected-text` edge | 03/T2 | COVERED |
| RESEARCH | R-5 | Remove diagonal fill image and guard hidden-region hooks | 03/T1 | COVERED |
| RESEARCH | R-6 | Actual source-over contrast, no-reflow geometry, grayscale proof | 03/T2 | COVERED |
| RESEARCH | R-7 | No package/API/schema/persistence/network contribution | 01–03 ledgers | COVERED |
| UI-SPEC | UI-1 | No library/registry/remote asset; preserve four-point spacing, typography, 32px labels/action, Monaco metrics | 01; 03/T1–T2 | COVERED |
| UI-SPEC | UI-2 | Exact root syntax roles, theme roles/scopes, alpha bytes, and idempotent stable ID | 01/T1–T2 | COVERED |
| UI-SPEC | UI-3 | No new Vue workflow; preserve adapter/component inventory and side ownership | 03/T1–T2 | COVERED |
| UI-SPEC | UI-4 | Whole/intraline language, sparse bars/signs, hidden/unchanged/hunk/empty regions | 02/T1; 03/T1 | COVERED |
| UI-SPEC | UI-5 | Exact eight-layer overlap order and seven executable overlap states | 03/T2 | COVERED |
| UI-SPEC | UI-6 | Exact section/editor/button/zone/spacer semantics, names, and keyboard paths | 03/T2 | COVERED |
| UI-SPEC | UI-7 | 4.5:1 text, 3:1 meaningful cues, actual composites, grayscale/non-color meaning | 03/T2 | COVERED |
| UI-SPEC | UI-8 | Preserve side-by-side breakpoint, min-width, local scroll owner, resize/zone path, no new media/layout | 03/T2 | COVERED |
| UI-SPEC | UI-9 | Preserve all existing copy and leave empty counterparts unlabeled | 03/T1–T2 | COVERED |
| UI-SPEC | V-1–4 | Theme order/parity/first paint/full token-state loop | 01/T2; 03/T1–T2 | COVERED |
| UI-SPEC | V-5–8 | Fixture matrix, visual layers, sparse cues, repeated-update replacement | 02/T1; 03/T1 | COVERED |
| UI-SPEC | V-9–11 | Overlap captures, computed composites, grayscale | 03/T2 | COVERED |
| UI-SPEC | V-12–15 | Geometry, existing behavior, exact action, localized overflow | 03/T2 | COVERED |
| UI-SPEC | V-16–18 | Accessibility names/roles, unclipped focus, contrast thresholds | 03/T2 | COVERED |
| UI-SPEC | NG/G-1–10 | All non-goals and implementation guardrails preserved | frontmatter prohibitions; 03/T1–T2 | COVERED |
</source_coverage_audit>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Repository-controlled text → Monaco immutable models/token renderer | Untrusted file bytes must remain text, never become phase-owned HTML, CSS, selector, or executable widget content. |
| Monaco public diff/selection events → fixed visual classes | Numeric ranges and closed Base/Head identity may affect decorations; repository strings may not. |
| Pinned Monaco DOM → global CSS hooks | Three version-specific visual hooks are permitted only with real-browser existence/computed-style guards. |
| Visual semantics → reviewer decision | A missing/overpainted cue can spoof addition/deletion or anchor/focus meaning even without data mutation. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-06-07 | Tampering | high | repository text rendering | mitigate | Preserve immutable Monaco models/tokenization; forbid `innerHTML`, custom content widgets, injected decoration text, dynamic selectors, and model mutation; retain exact-text/read-only browser assertions. |
| T-06-08 | Spoofing | high | addition/deletion/anchor/focus cues | mitigate | Require persistent labels, side position, literal signs, bars, separate overlap channels, grayscale proof, and actual composite thresholds before completion. |
| T-06-09 | Denial of Service | high | listeners/decorations/models over updates | mitigate | Allocate concern-specific collections/listeners once, replace with `.set()`, clear on model/dispose, and assert exact listener/model/zone/decoration bounds over ten updates and file restoration. |
| T-06-10 | Information Disclosure | high | assets/theme/runtime | mitigate | Keep theme, CSS, fonts, Codicons, fixtures, and tests local; add no URL, remote asset, telemetry, dependency, API, or network call. |
| T-06-11 | Tampering | medium | pinned Monaco DOM hooks | mitigate | Limit hooks to installed 0.55.1 `.selected-text`, `.diagonal-fill`, and hidden-region boundaries; guard existence and computed effect in Chromium and block on dependency drift. |
| T-06-SC | Tampering | low | npm supply chain | accept | No install/upgrade occurs; existing packages remain pinned and any lockfile drift blocks execution. |

High-severity gate: T-06-07 through T-06-10 must be mitigated by the named focused browser/unit gates. No high-severity finding may be accepted or transferred for phase completion.
</threat_model>

<verification>
1. Unit contracts: `./node_modules/.bin/vitest run tests/unit/monaco-theme.test.ts tests/unit/monaco-diff-semantics.test.ts`.
2. Source/generated semantic gate: `npm run build:web &amp;&amp; node scripts/verify-semantic-css.mjs`.
3. Real Monaco and real workspace: `npm run test:browser -- tests/integration/monaco-anchor.spec.ts tests/integration/anchored-workspace.spec.ts`.
4. End-of-phase browser review: run the same two explicit files with `--headed`, inspect/capture all seven overlap states and grayscale, and verify no-reflow/localized overflow at 1440/1280/1100/768/640px. Do not run unfiltered `test:browser` or unrelated project-wide suites.
</verification>

<success_criteria>
- All five DIFF requirement IDs and all five roadmap success criteria are objectively proven by focused unit and real-Chromium evidence.
- D-01 through D-08 and every required UI-SPEC theme, syntax, diff, overlap, accessibility, geometry, copy, and lifecycle contract are implemented without silent deferral.
- No review mechanics, immutable data, API/schema/persistence/export/Git/CLI/server boundary, side-by-side geometry, command, ARIA name, copy, responsive breakpoint, or Phase 07/08 surface changes.
- No high-severity ASVS L1 threat remains unmitigated, and pinned Monaco DOM/version drift is explicit rather than silent.
</success_criteria>

<output>
Create `.planning/phases/06-monaco-diff-semantics/06-03-SUMMARY.md` when complete. Include exact unit/CSS/Chromium commands and results, browser capture/geometry/contrast evidence, listener baseline, source-audit confirmation, any inherited-role assumption delta, pinned DOM/version confirmation, and explicit none statements for API/schema/package contributions.
</output>
