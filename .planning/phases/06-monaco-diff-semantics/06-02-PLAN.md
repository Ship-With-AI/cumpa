---
phase: 06-monaco-diff-semantics
plan: 02
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/web/monaco/diff-semantics.ts
  - tests/unit/monaco-diff-semantics.test.ts
autonomous: true
requirements:
  - DIFF-03
  - DIFF-04
must_haves:
  truths:
    - "Base/deletion and Head/addition ranges yield persistent structural cues that do not depend on red versus green."
    - "One continuous change bar covers each contiguous valid block, with one sign for 1–3 lines and signs only at the first and last lines for blocks of 4 or more."
    - "Pure insertion/deletion empty counterparts never receive a phantom marker, and every returned range stays within the current immutable model."
  artifacts:
    - path: "src/web/monaco/diff-semantics.ts"
      provides: "Pure public-ILineChange to fixed-class Monaco decoration transformation"
      exports: ["buildDiffDecorations"]
    - path: "tests/unit/monaco-diff-semantics.test.ts"
      provides: "RED/GREEN sparse-sign, contiguous-range, empty-side, and clamping contract"
  key_links:
    - from: "src/web/monaco/diff-semantics.ts"
      to: "monaco.editor.ILineChange"
      via: "reads only Monaco-authoritative original/modified line-number fields"
      pattern: "originalStartLineNumber|modifiedStartLineNumber"
    - from: "src/web/monaco/diff-semantics.ts"
      to: "src/web/monaco/diff-adapter.ts"
      via: "Wave 2 passes getLineChanges(), DiffSide, and current model line count"
      pattern: "buildDiffDecorations"
  prohibitions:
    - "No second textual diff algorithm, line-mapping rewrite, source-text mutation, private Monaco service, DOM query, or view zone."
    - "No `before`/`after` injected decoration text, dynamic selector/class from repository content, repeated sign on every long-block row, or marker on an empty counterpart."
    - "No gutter width, padding, margin, font metric, line height, content widget, ARIA role, pointer target, or tab stop is introduced."
---

<objective>
Specify and implement the pure semantic-decoration transformation for the locked signed gutter language in D-02 and D-03, while preserving the no-reflow comment/gutter contract required by DIFF-04.

Purpose: Make sparse signs and continuous bars deterministic and independently testable before they enter the stateful Monaco adapter lifecycle.
Output: `buildDiffDecorations` and a focused Vitest RED/GREEN contract covering every public line-range boundary.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-monaco-diff-semantics/06-CONTEXT.md
@.planning/phases/06-monaco-diff-semantics/06-RESEARCH.md
@.planning/phases/06-monaco-diff-semantics/06-PATTERNS.md
@.planning/phases/06-monaco-diff-semantics/06-UI-SPEC.md
@src/web/monaco/line-mapping.ts
@tests/unit/line-mapping.test.ts

<interfaces>
Existing side identity: `DiffSide = 'base' | 'head'` from `src/web/monaco/line-mapping.ts`. Existing range authority: `monaco.editor.ILineChange` supplies original and modified start/end line numbers; an end less than start is an empty side, not model line 1. Existing test fixtures in `tests/unit/line-mapping.test.ts` encode insertion as original `4..3` and deletion as modified `2..1`.

New interface: `buildDiffDecorations(changes: readonly monaco.editor.ILineChange[] | null, side: DiffSide, modelLineCount: number): monaco.editor.IModelDeltaDecoration[]`. It emits fixed `linesDecorationsClassName` and `glyphMarginClassName` values only; CSS supplies visible sign pseudo-content in Plan 03.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN Monaco-authoritative continuous bars and sparse signs</name>
  <read_first>
    - `src/web/monaco/diff-semantics.ts` — modified new file; confirm it is absent before RED.
    - `tests/unit/monaco-diff-semantics.test.ts` — modified new file; confirm it is absent before RED.
    - `src/web/monaco/line-mapping.ts` — pure-transform and `DiffSide` source of truth.
    - `tests/unit/line-mapping.test.ts` — exact insertion/deletion `ILineChange` analog fixtures and Vitest convention.
    - `src/web/monaco/diff-adapter.ts` — downstream `getLineChanges()` and immutable-model lifecycle consumer.
    - `.planning/phases/06-monaco-diff-semantics/06-CONTEXT.md` — D-02/D-03 locked non-color and sparse-marker language.
    - `.planning/phases/06-monaco-diff-semantics/06-UI-SPEC.md` — exact bar/sign geometry, glyph, accessibility, empty-side, and no-reflow contract.
    - `.planning/phases/06-monaco-diff-semantics/06-RESEARCH.md` — pinned public API, merging, clamping, and collection-replacement guidance.
  </read_first>
  <files>src/web/monaco/diff-semantics.ts, tests/unit/monaco-diff-semantics.test.ts</files>
  <behavior>
    - Null/empty changes or a non-positive model line count produce no decorations.
    - Base reads original ranges and owns `monaco-diff-change-bar--base` plus `monaco-diff-change-sign--base`; Head reads modified ranges and owns the corresponding `--head` classes.
    - A valid 1-, 2-, or 3-line contiguous block produces one full-block bar and one sign on its first line.
    - A valid block of 4 or more lines produces one full-block bar and signs only on its first and last lines.
    - Touching or overlapping populated ranges merge before marker density is decided, so the output has one bar and no repeated internal boundary sign.
    - Ranges clamp to `1..modelLineCount`; an empty counterpart whose end is less than start is rejected before clamping and never becomes a line-1 marker.
  </behavior>
  <action>Write the focused test first using the exact insertion/deletion shapes from `line-mapping.test.ts`, plus unchanged, 1-line, 3-line, 4+-line, touching, overlapping, out-of-bounds, and zero-line-count cases. Assert observable returned ranges and fixed classes, not helper implementation or source strings. Run the test and commit the failing RED gate as `test(06-02): add failing diff semantics contract`. Then create `src/web/monaco/diff-semantics.ts`, import Monaco types and `DiffSide`, and export only `buildDiffDecorations`. Select original fields for `base` and modified fields for `head`; reject `end &lt; start` before clamping; clamp populated ranges to actual model lines; discard ranges empty after clamping; sort and merge touching/overlapping ranges. For each merged range emit one `isWholeLine: true` bar decoration spanning the full range with only `linesDecorationsClassName: monaco-diff-change-bar--{side}`. Emit sign decorations at the first line for lengths 1–3 and at first plus last for lengths 4+, with only `glyphMarginClassName: monaco-diff-change-sign--{side}`. Do not use `before`, `after`, `className`, content widgets, view zones, model reads, DOM data, or a diff algorithm. Commit GREEN as `feat(06-02): derive sparse semantic diff decorations`; refactor only with tests green.</action>
  <acceptance_criteria>
    - The RED test is observed failing before production implementation and RED/GREEN commits use the required `test(06-02)` then `feat(06-02)` prefixes.
    - Exact insertion and deletion fixtures produce decorations only on their populated side; no synthetic line-1 cue exists on the empty side.
    - 1–3 line blocks have exactly one sign; 4+ blocks have exactly two signs at the endpoints; touching/overlapping blocks are coalesced before this count is calculated.
    - Every range is within `1..modelLineCount`, all classes are fixed from the closed `DiffSide` union, and output is deterministic for identical inputs.
    - Decoration options contain no injected text, interactive semantics, foreground override, spacing, width, or layout-affecting option.
  </acceptance_criteria>
  <verify>
    <automated>./node_modules/.bin/vitest run tests/unit/monaco-diff-semantics.test.ts</automated>
  </verify>
  <done>The pure Monaco-authoritative decoration builder is GREEN for all sparse-marker and empty-counterpart boundaries and is ready for atomic adapter collections.</done>
</task>

</tasks>

## Artifacts this phase produces

- **New files:** `src/web/monaco/diff-semantics.ts`; `tests/unit/monaco-diff-semantics.test.ts`.
- **New exported symbols:** `buildDiffDecorations`.
- **New private/internal symbols:** implementation-local range normalization/merge helpers only; no additional exported contract.
- **New CSS fields/classes:** none in this plan; it emits fixed class names `monaco-diff-change-bar--base`, `monaco-diff-change-bar--head`, `monaco-diff-change-sign--base`, and `monaco-diff-change-sign--head` for Plan 03 to style.
- **New API endpoints/client methods:** none.
- **New persistence/export/Zod schema fields:** none.
- **New package scripts or user commands:** none; verification invokes the installed focused Vitest binary.
- **New dependencies, registries, remote assets, environment variables, or human setup:** none.

<contribution_decisions>
- External/API/schema contribution: none. The transformation consumes in-browser Monaco data and neither crosses nor changes HTTP, persistence, export, Git, CLI, or server boundaries.
- Assumption delta: none. Empty-side and boundary semantics are grounded in current `ILineChange` fixtures and pinned Monaco 0.55.1 declarations; if the dependency version differs, stop and revalidate rather than reinterpret ranges.
- TDD contribution: this whole plan is one eligible pure input→output contract and must complete RED→GREEN→optional REFACTOR with separate RED/GREEN commits.
- Drift decision: `getLineChanges()` remains authoritative; any mismatch between the builder and Monaco output is a failing contract, never a reason to introduce another diff algorithm.
- UI contribution: structural non-color data only. Literal visible glyphs, colors, no-reflow, grayscale, and browser accessibility are integrated and proved in dependent Plan 03.
</contribution_decisions>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Monaco `ILineChange` → phase decoration ranges | Public numeric ranges become browser presentation cues and must remain bounded and side-correct. |
| Closed `DiffSide` → CSS class names | Only fixed Base/Head classes may cross into selectors; repository text is never a selector source. |

## STRIDE Threat Register

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|-----------|----------|----------|-----------|-------------|-----------------|
| T-06-04 | Tampering | high | range normalization | mitigate | Reject empty sides before clamping, clamp populated ranges, merge deterministically, and test insertion/deletion/out-of-bounds cases so false cues cannot misrepresent review content. |
| T-06-05 | Elevation of Privilege | high | decoration content path | mitigate | Emit fixed class names and numeric ranges only; prohibit `innerHTML`, content widgets, injected decoration text, repository-derived selectors, pointer handlers, roles, and tab stops. |
| T-06-06 | Denial of Service | medium | repeated/overlapping changes | mitigate | Sort/merge once and emit bounded endpoint markers rather than per-row signs or update-time listeners. |
| T-06-SC | Tampering | low | npm supply chain | accept | No install/upgrade occurs and Monaco remains pinned; dependency drift blocks execution for revalidation. |

High-severity gate: execution is blocked until T-06-04 and T-06-05 pass their focused RED/GREEN tests; high-severity visual misrepresentation or content-injection findings cannot be accepted.
</threat_model>

<verification>
1. Observe the focused test fail before implementation.
2. `./node_modules/.bin/vitest run tests/unit/monaco-diff-semantics.test.ts` passes after GREEN and after any refactor.
3. Inspect the exported surface: only `buildDiffDecorations` is new, with no DOM, network, model-text, persistence, or API dependency.
</verification>

<success_criteria>
- DIFF-03 has deterministic Base/Head signed structural cues implementing D-02 and D-03 without color-only meaning.
- DIFF-04 receives only existing-lane, non-interactive decorations and therefore no new geometry or comment-affordance surface.
- Every Monaco empty-side, clamping, contiguity, and marker-density boundary is protected by an observable behavior test.
</success_criteria>

<output>
Create `.planning/phases/06-monaco-diff-semantics/06-02-SUMMARY.md` when complete, including RED/GREEN commits, exact test output, exported interface, and confirmation that API/schema/package contributions remain none.
</output>
