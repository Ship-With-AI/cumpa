---
phase: 03-public-disclosure-and-gpl-readiness
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - .cumpa/tools/gitleaks/8.30.1/darwin-arm64/gitleaks
  - .cumpa/tools/git-lfs/3.8.0/darwin-arm64/git-lfs
  - .cumpa/publication/toolchain-provenance.json
  - .cumpa/publication/disclosure-evidence.json
autonomous: false
requirements: [SRC-02, LIC-03]
must_haves:
  truths:
    - "Exact official Gitleaks 8.30.1 and Git LFS 3.8.0 binaries are available from ignored owner-only paths with verified publisher release, checksum-manifest, binary digest, platform, and version provenance."
    - "Maintainers inspect complete redacted pre-push evidence while the repository remains private, including byte-level review of every historic intended-ref LFS payload."
    - "Every intended ref, privacy candidate, secret finding, permission gap, inaccessible object, LFS payload, and rights issue receives an exact disposition; incomplete or unreviewable evidence cannot authorize remediation or push."
    - "Any real credential is revoked or rotated through its provider before finding-specific deletion, rewrite, purge, or retention approval, with non-secret proof and affected-surface recollection required."
    - "Plan 03 performs no push: its summary/planning metadata must be committed before Plan 04 obtains exact old/new OID and deployment-consequence authorization for the final private synchronization."
  artifacts:
    - path: ".cumpa/publication/toolchain-provenance.json"
      provides: "Ignored strict provenance for the exact pinned scanner binaries and invocation paths"
    - path: ".cumpa/publication/disclosure-evidence.json"
      provides: "Ignored redacted immutable pre-remediation full-history, historic LFS payload, and GitHub-surface evidence"
  key_links:
    - from: "official publisher release/checksum manifests"
      to: "toolchain-provenance.json"
      via: "HTTPS origin, selected asset/checksum entry, manifest digest, binary digest, platform, path, and reported version verification"
      pattern: "gitleaks_binary_sha256|git_lfs_binary_sha256"
    - from: "toolchain-provenance.json"
      to: "disclosure-evidence.json"
      via: "collector invokes only the exact recorded binary paths and embeds the provenance digest"
      pattern: "toolchain_provenance_sha256"
    - from: "historic LFS pointer/object map"
      to: "disclosure-evidence.json"
      via: "fetched payload OID verification plus per-object secret and private-data scan results"
      pattern: "lfs_objects|payload_review"
    - from: "Plan 03 blocking finding decision"
      to: "Plan 04 fresh collection and exact one-action/final authorization loop"
      via: "03-03-SUMMARY records exact dispositions and requested remediation scope, but grants no mutation, ref, or deployment authority"
      pattern: "remediation|03-03-SUMMARY"
  prohibitions:
    - "HIGH: Do not push any ref in this plan; commit_docs=true makes the later 03-03 summary/metadata part of the state Plan 04 must collect and authorize."
    - "HIGH: Do not treat LFS fsck or a binary inventory as payload review; every fetched payload must receive both required scans or block."
    - "HIGH: Do not delete/expire hosted data, rewrite/push history, purge records/LFS, or rotate/revoke credentials in this plan; Plan 04 requires a fresh strict one-action authorization before each consequence."
    - "Never make the repository public, create/push tags or releases, publish npm, broaden GitHub OAuth scopes automatically, install tools system-wide, or print protected values."
---

<objective>
Provision deterministic disclosure tools, collect the complete private surface including historic LFS payload bytes, and obtain exact finding-specific remediation decisions without pushing.

Purpose: Exercise SRC-02 against the real private repository while preserving a reachable synchronization order after GSD commits this plan's summary.
Output: Ignored toolchain provenance and complete pre-remediation evidence plus a canonical decision handoff for Plan 04.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-CONTEXT.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-RESEARCH.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-PATTERNS.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/COVERAGE.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-01-SUMMARY.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-02-SUMMARY.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md
@scripts/verify-public-disclosure.mjs
@.github/workflows/deploy-supabase-production.yml
@docs/support-service-operations.md
</context>

<interfaces>
- Provision: `node scripts/verify-public-disclosure.mjs --provision-tools --tools-root .cumpa/tools --gitleaks-version 8.30.1 --git-lfs-version 3.8.0 --output .cumpa/publication/toolchain-provenance.json`.
- Provenance check: `node scripts/verify-public-disclosure.mjs --check-toolchain .cumpa/publication/toolchain-provenance.json`.
- Collect: `node scripts/verify-public-disclosure.mjs --collect --output .cumpa/publication/disclosure-evidence.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --toolchain-provenance .cumpa/publication/toolchain-provenance.json`.
- Evidence check: `node scripts/verify-public-disclosure.mjs --check-evidence .cumpa/publication/disclosure-evidence.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --toolchain-provenance .cumpa/publication/toolchain-provenance.json --require-current`.
- `--provision-tools` may download only exact current-platform assets and official checksum manifests from publisher-owned `gitleaks/gitleaks` and `git-lfs/git-lfs` release origins into owner-only ignored paths; it may not use npm, package managers, `sudo`, or ambient binaries. Missing or unverifiable upstream provenance blocks.
- Collector completeness includes the exact `refs/heads/main` intended ref, all other local/remote ref classes as review surfaces, reachable objects/metadata, every historic LFS pointer mapped to fetched digest-verified payload bytes and both required scans, all authoritative GitHub surfaces, repository/environment/organization credential metadata, rights/license inputs, and stable before/after snapshots.
- A completed scanner finding produces redacted `blocked` evidence for review. Tool/API/provenance/pagination/download/LFS/scan failure or an unsupported, skipped, oversized, binary-unreviewable, missing, or corrupt payload is incomplete and non-waivable.
</interfaces>

## Artifacts this phase produces

Ignored runtime artifacts:
- `.cumpa/publication/toolchain-provenance.json`
- `.cumpa/publication/disclosure-evidence.json`

The canonical Task 3 decision is recorded in `03-03-SUMMARY.md` for Plan 04 to execute only after that summary/metadata commit exists. Raw release downloads, LFS payloads, logs, artifacts, release assets, attachments, API bodies, and scan reports remain in owner-only temporary storage and are deleted in `finally`. This plan creates no push, deployment, pre-push authorization, or attestation.

<tasks>

<task type="auto">
  <name>Task 1: Provision and verify the exact official disclosure toolchain</name>
  <files>.cumpa/publication/toolchain-provenance.json, .cumpa/tools/gitleaks/8.30.1/darwin-arm64/gitleaks, .cumpa/tools/git-lfs/3.8.0/darwin-arm64/git-lfs</files>
  <read_first>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-RESEARCH.md</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-02-SUMMARY.md</file>
    <file>scripts/verify-public-disclosure.mjs</file>
  </read_first>
  <action>Run the provision interface before any live collection. Select only the exact Gitleaks 8.30.1 and Git LFS 3.8.0 assets matching the executor OS/architecture from their publisher-owned official GitHub releases. Require HTTPS final origins, official upstream checksum-manifest entries, manifest SHA-256, asset/binary SHA-256, reported versions, executable owner-only files, and ignored paths. Record only non-secret provenance and exact invocation paths in the strict self-digesting artifact. Do not install globally, invoke npm/system package managers, use `sudo`, broaden privileges, fall back to ambient `PATH`, or proceed when upstream provenance cannot be verified.</action>
  <verify>
    <automated>node scripts/verify-public-disclosure.mjs --check-toolchain .cumpa/publication/toolchain-provenance.json</automated>
  </verify>
  <acceptance_criteria>The provenance verifier proves exact official publishers, versions, platform assets, checksum-manifest and binary digests, file modes, ignored storage, and invocation paths for both required binaries.</acceptance_criteria>
  <done>Live collection has deterministic, pinned, non-privileged scanner prerequisites instead of a guaranteed missing-tool stop.</done>
</task>

<task type="auto">
  <name>Task 2: Collect the complete private pre-remediation disclosure surface</name>
  <files>.cumpa/publication/disclosure-evidence.json</files>
  <read_first>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-01-SUMMARY.md</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-02-SUMMARY.md</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/COVERAGE.md</file>
    <file>.cumpa/publication/toolchain-provenance.json</file>
    <file>scripts/verify-public-disclosure.mjs</file>
    <file>.github/workflows/deploy-supabase-production.yml</file>
  </read_first>
  <action>Keep the repository private and run collection with the verified provenance artifact so the collector uses only the recorded Gitleaks and Git-LFS paths. Require exact repository identity, `refs/heads/main` as the sole intended ref, classified local refs plus remote heads/tags/pull refs, reachable objects and author/tag metadata, Git and project-private-data scans, all `COVERAGE.md` GitHub surfaces, repository/environment/organization credential and variable metadata, and stable before/after manifests. For historic LFS, enumerate all intended-ref pointers, fetch every mapped object into owner-only temporary storage, verify pointer OID/size against payload, scan each payload with Gitleaks and the project-private-data policy, and persist only a redacted per-object result/non-text inventory. Missing, corrupt, binary-unreviewable, unsupported, skipped, oversized, or otherwise incomplete payloads block and cannot receive a human waiver. Download and scan every workflow attempt log, artifact, release asset, collaboration body, Discussion/wiki body, attachment, and other required payload; redact before persistence and remove temporary bytes in `finally`.</action>
  <verify>
    <automated>node scripts/verify-public-disclosure.mjs --check-evidence .cumpa/publication/disclosure-evidence.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --toolchain-provenance .cumpa/publication/toolchain-provenance.json --require-current</automated>
  </verify>
  <acceptance_criteria>The strict evidence is provenance-bound, redacted, self-digesting, private-repository-bound, stable, and complete across Git, GitHub, settings, credentials, downloads, and byte-level historic LFS review; it is blocked only by individually reviewable findings, never collection gaps.</acceptance_criteria>
  <done>Maintainers have complete real private-repository evidence before deciding remediation; no ref or hosted/provider state changed.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 3: Decide every finding disposition and identify required remediation</name>
  <files>.cumpa/publication/toolchain-provenance.json, .cumpa/publication/disclosure-evidence.json</files>
  <read_first>
    <file>.cumpa/publication/toolchain-provenance.json</file>
    <file>.cumpa/publication/disclosure-evidence.json</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-RESEARCH.md</file>
  </read_first>
  <decision>Select the exact disposition for every complete evidence finding and identify each required finding-specific remediation, while granting no provider, cleanup, rewrite, push, deployment, or visibility authority in Plan 03.</decision>
  <context>Each real credential requires provider-native revoke/rotate proof before cleanup. Provider remediation, destructive Git/GitHub cleanup, support purge, history rewrite, and any retained public occurrence are separate finding-specific decisions. Because the 03-03 summary commit changes the snapshot, Plan 04 must recollect and obtain a strict current one-action authorization before executing any consequence.</context>
  <action>Present the current evidence, rights, and toolchain digests with every redacted finding and the three options. Record only exact per-finding dispositions, desired remediation scope/order, proof requirements, and consequences in the Plan 03 summary. Stop on `block-unresolved`; do not convert this decision into executable authority.</action>
  <options>
    <option id="resolve-without-mutation">
      <name>Resolve or exactly accept every finding without mutation</name>
      <pros>Provides reviewer identity/time plus an individual `resolved` rationale/evidence or exact-location `accepted-public` scope for every finding, including the exact-scope revoked-credential retention rule where applicable.</pros>
      <cons>Invalid for an active credential, incomplete surface, unreviewable LFS payload, blanket scope, or finding that requires removal.</cons>
    </option>
    <option id="request-named-remediation">
      <name>Identify separately named finding-specific remediation</name>
      <pros>For each finding, names provider/credential non-secret ID, required revoke-or-rotate-first step, desired deletion/rewrite/purge/support target, action order, proof, consequences, and required affected-surface recollection so Plan 04 can construct one exact current action request.</pros>
      <cons>Grants no action, ref, OID, force-with-lease, deployment, support, or hosted-data authority; Plan 04 must collect current state and obtain a new canonical decision plus verifier-valid artifact for each action.</cons>
    </option>
    <option id="block-unresolved">
      <name>Keep publication and remediation blocked</name>
      <pros>Identifies incomplete evidence or unresolved findings without converting uncertainty into approval.</pros>
      <cons>Plan 04 cannot authorize a consequential action or synchronize main.</cons>
    </option>
  </options>
  <verify>
    <automated>node scripts/verify-public-disclosure.mjs --check-evidence .cumpa/publication/disclosure-evidence.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --toolchain-provenance .cumpa/publication/toolchain-provenance.json --require-current</automated>
  </verify>
  <acceptance_criteria>The response names reviewer/time and every finding-specific disposition plus any requested action/order/proof/consequence, never waives incomplete collection, and explicitly grants no mutation, push, or deployment authority.</acceptance_criteria>
  <done>Plan 04 has attributable dispositions and exact requested-remediation inputs, but must authorize and recollect each consequence separately.</done>
  <resume-signal>Select `resolve-without-mutation`, `request-named-remediation`, or `block-unresolved`; provide the exact required per-finding fields and never include a raw secret.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Official release assets → local ignored tool paths | Publisher, asset, checksum, version, platform, and exact invoked bytes must all be proven before scanning. |
| Historic LFS/GitHub payloads → temporary storage | Hostile, private, binary, or oversized bytes must be bounded and completely reviewable without persistence. |
| Redacted evidence → maintainer decision | Human privacy/credential judgments bind exact findings without seeing, recording, or hashing raw secrets. |
| Maintainer decision → Plan 04 action request | Plan 03 records exact dispositions and requested remediation scope/order but grants no mutation authority. |
| Plan 03 summary commit → Plan 04 authorization loop | Any pre-summary action/ref authorization would be stale; Plan 04 recollects before each one-action or final synchronization decision. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-03-PR | Spoofing | pinned toolchain | mitigate | Official publisher/final origin, checksum manifest, binary digest/version/platform/path, owner-only ignored storage. |
| T-03-03-LF | Information Disclosure | LFS payload review | mitigate | Fetch into secure temp, pointer/OID verification, both scans per payload, redacted results, all unreviewable states block. |
| T-03-03-RP | Repudiation | finding dispositions | mitigate | Canonical blocking decision with named reviewer/time, exact finding IDs/scopes, rationales, evidence, and consequences. |
| T-03-03-CR | Elevation of Privilege | credential cleanup order | mitigate | Plan 03 executes nothing; Plan 04 proves provider rotation/revocation first and obtains a strict one-action authorization before cleanup. |
| T-03-03-NS | Information Disclosure | evidence handling | mitigate | Redacted metadata/digests only; raw downloads and credentials never persist or print. |
| T-03-03-PG | Spoofing | unavailable surface as absence | mitigate | Tool/API/status/count/LFS review failures remain incomplete and cannot receive a disposition. |
| T-03-03-SY | Tampering | premature synchronization | mitigate | No Plan 03 push; Plan 04 collects the committed summary state before exact old/new OID and deployment authorization. |
</threat_model>

<verification>Verify exact official tool provenance, then run the versioned collector/check against the private repository and make only the canonical finding/disposition decision. Do not run project-wide tests, formatters, linters, builds, provider or cleanup actions, pushes, deployments, tags, releases, package publication, or visibility changes.</verification>

<success_criteria>
- SRC-02 has executable exact scanner prerequisites and complete live coverage of intended history, GitHub surfaces, settings/credential metadata, and every historic LFS payload byte.
- Every actual credential is bound to a rotate-or-revoke-first finding-specific decision; incomplete or unreviewable evidence stays blocked.
- LIC-03 rights-review digest and exact privacy/material dispositions are present, while final refspec/OID/deployment authorization is correctly deferred until after the 03-03 summary commit.
</success_criteria>

<output>Create `.planning/phases/03-public-disclosure-and-gpl-readiness/03-03-SUMMARY.md` with official tool release/checksum/binary provenance, exact invocation paths, evidence digest and surface counts, LFS pointer/object review results, every finding disposition/requested remediation scope/order/proof/consequence, and an explicit statement that no mutation/push/deployment authority was granted and Plan 04 must recollect before each exact authorization.</output>
