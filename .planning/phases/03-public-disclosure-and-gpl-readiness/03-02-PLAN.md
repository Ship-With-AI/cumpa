---
phase: 03-public-disclosure-and-gpl-readiness
plan: 02
type: tdd
wave: 2
depends_on: ["03-01"]
files_modified:
  - scripts/verify-public-disclosure.mjs
  - tests/api/public-disclosure.test.ts
autonomous: true
requirements: [SRC-02, LIC-01, LIC-03]
must_haves:
  truths:
    - "The verifier provisions exact official Gitleaks 8.30.1 and Git LFS 3.8.0 assets into ignored owner-only paths and refuses missing or mismatched publisher/checksum/version provenance."
    - "One read-only command inventories every intended local/remote ref and reachable object plus the complete GitHub-hosted disclosure surface, historic LFS payloads, repository settings, and configured credential metadata."
    - "Every historic LFS pointer maps to a fetched, digest-verified payload scanned in secure temporary storage by both secret and project-private-data policies; missing, corrupt, binary-unreviewable, unsupported, skipped, or oversized payloads block."
    - "A strict ignored pre-push authorization is mandatory before every push and binds exact evidence/right/tool/action-ledger digests, findings/proof, reviewer/time, one main old/new OID transition, ordinary or rewritten-main command/local adoption, and all workflow/environment/deployment consequences."
    - "Every non-ref provider, hosted-data cleanup, or support action requires its own strict ignored one-action authorization and full recollection; rewritten main is instead a pre-push-authorized final transition that adopts the same new OID locally/remotely and can flow directly to attestation."
    - "A strict append-only consequential-action ledger preserves each one-action authorization's canonical bytes/digest, verified outcome/proof, following evidence digest, and previous-entry digest across repeated fixed-path authorizations."
    - "Every workflow attempt log, artifact, release asset, collaboration record, discussion/wiki record, attachment, and supported LFS payload is downloaded and scanned without persisting raw content."
    - "Missing tools or provenance, permission/pagination/download gaps, scanner warnings/skips/errors, changed snapshots, and unknown findings block rather than become absence or success."
    - "Finalization freshly re-enumerates every Git, GitHub, Actions, LFS, settings, credential-metadata, rights, and license surface; a stored digest or post-attestation change cannot pass."
    - "Final approval is derived from immutable evidence, exact pre-push authorization, post-push attestation, rights, license, and current before/after invariants rather than an editable approval flag."
  artifacts:
    - path: "scripts/verify-public-disclosure.mjs"
      provides: "Read-only collection, strict tool-provenance/evidence/authorization/attestation validation, current-surface re-enumeration, and fail-closed final gate"
      exports: ["provisionDisclosureTools", "validateToolchainProvenance", "collectDisclosureEvidence", "validateDisclosureEvidence", "checkDisclosureEvidence", "createConsequentialActionAuthorization", "verifyConsequentialActionAuthorization", "createConsequentialActionLedger", "recordConsequentialActionResult", "verifyConsequentialActionLedger", "createPrePushAuthorization", "verifyPrePushAuthorization", "createPostPushAttestation", "enumerateCurrentDisclosureSurfaces", "derivePublicationGate", "verifyPublicationGate"]
    - path: "tests/api/public-disclosure.test.ts"
      provides: "Synthetic provenance, LFS-payload, disclosure, authorization, deployment-consequence, redaction, pagination, freshness, and gate contracts"
  key_links:
    - from: "toolchain-provenance.json"
      to: "scripts/verify-public-disclosure.mjs"
      via: "exact pinned binary paths, versions, publisher release URLs, checksum-manifest digests, and binary digests"
      pattern: "gitleaks_binary_sha256|git_lfs_binary_sha256"
    - from: "historic LFS pointer manifest"
      to: "disclosure-evidence.json"
      via: "pointer-to-fetched-object digest mapping plus per-object secret/private-data scan outcome"
      pattern: "lfs_objects|payload_review"
    - from: "disclosure-evidence.json and one exact non-ref remediation decision"
      to: "consequential-action-authorization.json"
      via: "current digests, one provider/hosted/support target and command/UI operation, reviewer/time, expiry, proof, consequences, and required recollection"
      pattern: "action_kind|target|required_proof"
    - from: "consequential-action-authorization.json and following disclosure-evidence.json"
      to: "consequential-action-ledger.json"
      via: "atomic append of embedded canonical authorization, verified outcome/proof, following evidence digest, and previous-entry digest before fixed-path reuse"
      pattern: "authorization_record|result|following_evidence_sha256|previous_entry_sha256"
    - from: "current evidence, action ledger, rights, and tools"
      to: "pre-push-authorization.json"
      via: "every-push exact digests/findings/proof plus main old/new OIDs, ordinary or rewritten-main command/local adoption, and deployment consequences"
      pattern: "pre_push_evidence_sha256|action_ledger_sha256|old_oid|new_oid"
    - from: "effective pre-push authorization, final action ledger, and fresh evidence"
      to: "maintainer-attestation.json"
      via: "transition-time ledger digest plus validated final suffix, actual canonical local/remote transition, settled consequences, and exact evidence binding"
      pattern: "transition_action_ledger_sha256|final_action_ledger_sha256|post_push_evidence_sha256"
    - from: "publication-gate.json"
      to: "all current Git/GitHub/Actions/LFS/settings/credential surface manifests"
      via: "mandatory --require-current re-enumeration and exact ID/count/status/digest comparison"
      pattern: "require-current|current_surfaces_sha256"
  prohibitions:
    - "HIGH: The collector must not push, force-push, rewrite history, delete GitHub records, change settings/visibility, create tags/releases, publish packages, or rotate/revoke credentials."
    - "HIGH: Never persist or print raw credential matches, credential-bearing command lines, downloaded content, terminal-control content, or deterministic hashes of secret values."
    - "Do not add npm dependencies, reimplement Git semantics, scrape GitHub HTML, accept a 403/404/410 as an empty surface, or automate legal compatibility decisions."
---

<objective>
Build the strict, read-only evidence and publication-gate verifier for complete disclosure and GPL readiness.

Purpose: Turn SRC-02, LIC-01, and LIC-03 into deterministic fail-closed records while keeping legal/privacy decisions and consequential actions outside collection.
Output: A tested Node 24 verifier CLI/module and focused synthetic contract suite.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
@/Users/alessandro/.agents/gsd-core/references/tdd.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-CONTEXT.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-RESEARCH.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-PATTERNS.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/COVERAGE.md
@.planning/phases/03-public-disclosure-and-gpl-readiness/03-01-SUMMARY.md
@scripts/verify-supabase-support.mjs
@scripts/verify-production-artifacts.mjs
@tests/api/export-publication.test.ts
@docs/support-service-operations.md
@.github/workflows/deploy-supabase-production.yml
</context>

<interfaces>
- Provision and verify tools: `node scripts/verify-public-disclosure.mjs --provision-tools --tools-root .cumpa/tools --gitleaks-version 8.30.1 --git-lfs-version 3.8.0 --output .cumpa/publication/toolchain-provenance.json`, then `node scripts/verify-public-disclosure.mjs --check-toolchain .cumpa/publication/toolchain-provenance.json`.
- Collection and current evidence check: `node scripts/verify-public-disclosure.mjs --collect --output .cumpa/publication/disclosure-evidence.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --toolchain-provenance .cumpa/publication/toolchain-provenance.json`, then the same immutable inputs to `--check-evidence .cumpa/publication/disclosure-evidence.json --require-current`.
- Non-ref consequential-action authorization creation/check: `node scripts/verify-public-disclosure.mjs --create-action-authorization --evidence .cumpa/publication/disclosure-evidence.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --toolchain-provenance .cumpa/publication/toolchain-provenance.json --input <owner-only-temporary-json> --output .cumpa/publication/consequential-action-authorization.json`, then the same immutable inputs to `--check-action-authorization .cumpa/publication/consequential-action-authorization.json --require-current`.
- Action-ledger initialization: `node scripts/verify-public-disclosure.mjs --init-action-ledger --output .cumpa/publication/consequential-action-ledger.json`; create a canonical empty strict ledger when absent and reject resetting a nonempty ledger.
- Action-result ledger record/check: `node scripts/verify-public-disclosure.mjs --record-action-result --authorization .cumpa/publication/consequential-action-authorization.json --result <owner-only-temporary-result-json> --evidence .cumpa/publication/disclosure-evidence.json --ledger .cumpa/publication/consequential-action-ledger.json`, delete result input in `finally`, then `node scripts/verify-public-disclosure.mjs --check-action-ledger .cumpa/publication/consequential-action-ledger.json`. The record command validates following evidence at append time; the check command validates immutable internal history without requiring later current evidence to equal the last action snapshot.
- Every-push authorization creation: `node scripts/verify-public-disclosure.mjs --create-pre-push-authorization --evidence .cumpa/publication/disclosure-evidence.json --action-ledger .cumpa/publication/consequential-action-ledger.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --toolchain-provenance .cumpa/publication/toolchain-provenance.json --input <owner-only-temporary-json> --output .cumpa/publication/pre-push-authorization.json`.
- Every-push authorization check: `node scripts/verify-public-disclosure.mjs --check-pre-push-authorization .cumpa/publication/pre-push-authorization.json --evidence .cumpa/publication/disclosure-evidence.json --action-ledger .cumpa/publication/consequential-action-ledger.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --toolchain-provenance .cumpa/publication/toolchain-provenance.json --require-current`.
- Post-push attestation: `node scripts/verify-public-disclosure.mjs --create-attestation --authorization .cumpa/publication/pre-push-authorization.json --action-ledger .cumpa/publication/consequential-action-ledger.json --evidence .cumpa/publication/disclosure-evidence.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --output .cumpa/publication/maintainer-attestation.json`.
- Final derivation and check: `node scripts/verify-public-disclosure.mjs --finalize --require-current --evidence .cumpa/publication/disclosure-evidence.json --action-ledger .cumpa/publication/consequential-action-ledger.json --authorization .cumpa/publication/pre-push-authorization.json --attestation .cumpa/publication/maintainer-attestation.json --rights-review .planning/phases/03-public-disclosure-and-gpl-readiness/03-RIGHTS-REVIEW.md --output .cumpa/publication/publication-gate.json`, followed by the same inputs to `--check-final .cumpa/publication/publication-gate.json --require-current`.
- Tool provenance minimum: strict versioned kind, platform/architecture, publisher-owned release and checksum-manifest URLs, HTTPS final origins, manifest SHA-256, selected asset name/digest, exact binary path/digest/reported version for Gitleaks 8.30.1 and Git LFS 3.8.0, generated UTC time, and self-digest.
- Evidence minimum: strict versioned kind/status, repository identity/visibility/default branch, exact intended and remote ref manifests/digests, reachable objects, before/after equality, exact verified tools, per-surface authoritative IDs/counts/statuses/digests/download results, every historic LFS pointer-to-fetched-object mapping and redacted payload review, redacted findings, violations, and self-digest.
- Consequential-action authorization minimum: strict versioned kind, exact current evidence/right/tool digests, one non-ref provider/hosted/support action and finding set, reviewer/time/expiry, exact target/command/API/UI operation, required proof, mandatory recollection, consequences, and self-digest.
- Consequential-action ledger minimum: strict versioned kind; ordered append-only entries embedding canonical authorization bytes/self-digest, strict non-secret result (`authorization_sha256`, exact action kind/target, `succeeded|failed|cancelled`, provider/request/result ID, UTC completion time, proof references/digests), following evidence digest, previous-entry digest, entry digest; generated time; and ledger self-digest. Record-time validation requires the provided following evidence to be current and corroborate the outcome. Internal checks reject deletion, reorder, duplicates, unknown/raw-secret fields, authorization/action/target mismatch, absent proof, broken chain, or contradicted success; they do not compare later evidence to the final entry.
- Pre-push authorization minimum: strict versioned kind; exact evidence/right/tool and current action-ledger digests; reviewer/time/expiry; every disposition and rotate-before-cleanup proof; repository; one exact main old/new OID transition; push kind `ordinary-final` or `rewritten-main-cleanup`; exact command; workflow/digest/event, protected environment, non-secret deployment target/effects/surfaces; and self-digest. Ordinary is non-force. Rewritten-main additionally requires deterministic rewrite spec, reviewed resulting tree, clean-worktree precondition, exact canonical local-main adoption, and only the recorded force-with-lease old OID. Every other ref/force form blocks.
- Post-push attestation minimum: strict versioned kind; effective pre-push authorization/evidence/rights digests; transition-time action-ledger digest from that authorization; exact final action-ledger digest plus validated append-only suffix, if any; reviewer identity; actual canonical local/remote transition; settled triggered attempts; complete clean final surfaces; dispositions; generated UTC time; and self-digest. Later non-ref remediation may extend the ledger and recollect before attestation, but any later Git/ref mutation requires a new pre-push transition.
- Final gate minimum: strict versioned kind, exact evidence/pre-push/transition-ledger/final-ledger/attestation/rights/license/notice/current-surface digests, frozen repository/ref/object identity, derived status/violations, and self-digest. Applicable statuses are exactly `pass`, `resolved`, `accepted-public`, or `blocked`.
</interfaces>

## Artifacts this phase produces

Committed implementation artifacts from this plan:
- `scripts/verify-public-disclosure.mjs`
- `tests/api/public-disclosure.test.ts`

The verifier contract generates `.cumpa/publication/toolchain-provenance.json`, `disclosure-evidence.json`, `consequential-action-authorization.json`, `consequential-action-ledger.json`, `pre-push-authorization.json`, `maintainer-attestation.json`, and `publication-gate.json` only in later plans under the already ignored directory. Fetched LFS payloads and raw GitHub downloads exist only under owner-only OS temporary directories and are removed in `finally`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — specify complete collection, authorization, and current-surface contracts</name>
  <files>tests/api/public-disclosure.test.ts</files>
  <read_first>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-RESEARCH.md</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/COVERAGE.md</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-01-SUMMARY.md</file>
    <file>tests/api/export-publication.test.ts</file>
    <file>scripts/verify-supabase-support.mjs</file>
    <file>.github/workflows/deploy-supabase-production.yml</file>
  </read_first>
  <behavior>
    - "Strict validators reject unknown/missing keys, unsupported versions/kinds/statuses, malformed digests, editable approval booleans, non-ignored or non-owner-only authorization outputs, and every self/cross-record mismatch."
    - "Tool provenance passes only for exact official Gitleaks 8.30.1 and Git LFS 3.8.0 platform assets whose publisher release URL, HTTPS final origin, upstream checksum-manifest entry/digest, binary digest, reported version, and invoked path all agree."
    - "Every historic intended-ref LFS pointer maps to one fetched object whose bytes match its OID and receive complete Gitleaks plus project-private-data scans; missing, corrupt, unsupported, skipped, oversized, symlinked, binary-unreviewable, or otherwise incomplete payloads block with a redacted per-object result."
    - "Pagination succeeds only when all pages and unique authoritative IDs/counts reconcile; every workflow run attempt, log, artifact, release/asset, issue/PR/comment/review, commit comment, Discussion/reply, wiki item, attachment, setting, and credential-metadata scope has a complete result."
    - "Consequential-action authorization accepts exactly one non-ref provider/hosted/support action and rejects missing evidence/right/tool bindings, broad targets, multiple actions, absent proof/recollection requirements, and omitted consequences."
    - "Ledger initialization creates the bindable zero-action record when absent and rejects replacing a nonempty ledger."
    - "Recording a result requires strict owner-only temporary result input, matches its authorization/action/target, rejects raw secrets, verifies proof/outcome against current following evidence at append time, embeds the full non-secret record, and deletes input in `finally`; later evidence may differ without invalidating history."
    - "Pre-push authorization is required for every push and binds the current ledger: ordinary rejects force; rewritten-main rejects nondeterministic output, mismatched reviewed tree/local adoption/OIDs, bare force, stale lease, wildcard/mirror/tag/extra refs, incomplete remediation, or omitted deployment consequences."
    - "A main authorization identifies the exact deploy workflow digest, protected `production` environment, current non-secret Supabase target, deployment effects, and resulting Actions/log/artifact/hosted-service surfaces; an unknown consequence blocks."
    - "Post-push attestation binds the transition-time ledger from effective pre-push authorization and the exact final ledger, validates any append-only suffix, then binds fresh evidence and actual canonical local/remote transition; later Git/ref change requires a new pre-push decision."
    - "Both finalize modes require a live current-surface snapshot. Synthetic additions, deletions, status changes, or digest changes after attestation in each Git, GitHub, Actions, LFS, settings, credential-metadata, rights, license, and notice class block."
    - "Raw secrets, secret hashes, credentials in command lines, downloaded bytes, terminal controls, unsafe archive paths/symlinks, and decompression/resource-limit failures never reach persisted JSON or stdout."
  </behavior>
  <action>Create isolated temp-root Vitest cases using existing `mkdtemp`/cleanup conventions and injected fake Git/GitHub/scanner runners. Cover official tool provenance success plus wrong origin/checksum/version/path/platform cases. Model historic pointer-to-object mappings and assert complete text payload review, supported byte scanning, and fail-closed missing/corrupt/binary-unreviewable/unsupported/skipped/oversized cases. Exercise multipage and duplicate/missing-page fixtures; every workflow attempt; artifacts; releases/assets; issues/PRs; issue comments, reviews, review comments, commit comments; Discussions/comments/replies; wiki; attachments; repository/environment/organization credential and variable metadata; and permission gaps. Add exact authorization fixtures for finding dispositions, remediation order, main refspec old/new OIDs, workflow digest, `production` environment, Supabase target, and hosted consequences. For final freshness, add a table-driven synthetic post-attestation addition/change case for every named surface class and prove each requires full recollection and a new authorization/attestation. Tests use no live network or mutation.</action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/api/public-disclosure.test.ts</automated>
  </verify>
  <acceptance_criteria>The focused suite specifies official binary provenance, byte-level historic LFS review, one-action non-ref authorization, append-only independently verifiable action-result ledger, every-push authorization including canonical local adoption and reachable exact force-with-lease rewrite-as-final-transition, exact deployment consequences, ledger-bound attestation, and current re-enumeration; it is RED only for missing verifier behavior.</acceptance_criteria>
  <done>All blocker-sensitive disclosure contracts are executable before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — implement the read-only collector and fail-closed authorization/final gate</name>
  <files>scripts/verify-public-disclosure.mjs, tests/api/public-disclosure.test.ts</files>
  <read_first>
    <file>tests/api/public-disclosure.test.ts</file>
    <file>scripts/verify-supabase-support.mjs</file>
    <file>scripts/verify-production-artifacts.mjs</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-RESEARCH.md</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/COVERAGE.md</file>
    <file>.planning/phases/03-public-disclosure-and-gpl-readiness/03-01-SUMMARY.md</file>
  </read_first>
  <action>Implement one Node 24 ESM module/CLI with exported testable functions and a guarded main entrypoint. Reuse strict-key, SHA-256, immutable-input, atomic-write, temporary-directory, streamed-download, archive-boundary, and argument-array process patterns; add no dependency. Require explicit verified Gitleaks/Git-LFS paths/provenance. Enumerate every `COVERAGE.md` surface with authoritative reconciliation. Fetch/verify/scan all historic LFS payload bytes and fail closed on any gap. Implement strict provenance, evidence, non-ref authorization, append-only ledger, every-push authorization, attestation, and gate schemas. `--init-action-ledger` creates only the absent zero-action ledger. `--record-action-result` reads strict owner-only temporary result JSON containing authorization digest, exact action/target, outcome, provider/request/result ID, UTC completion time, and proof references/digests; reject raw secrets/mismatches, verify the supplied following evidence is current and corroborates outcome, atomically append the embedded authorization/result/evidence/previous digest, and delete input in `finally`. `--check-action-ledger` validates immutable internal history without comparing a later current snapshot to the last entry. Pre-push authorization binds the then-current ledger. Attestation binds both that transition-time ledger digest and the exact final ledger, accepting only a valid append-only suffix for later non-ref actions; later ref mutation requires a new pre-push transition. Support ordinary and deterministic canonical-local rewritten-main final transitions. Finalize/check-final re-enumerate every named surface class. Redact at collection, keep raw bytes temporary, and keep verifier modes free of remote mutation.</action>
  <verify>
    <automated>node node_modules/vitest/vitest.mjs run tests/api/public-disclosure.test.ts</automated>
  </verify>
  <acceptance_criteria>All synthetic contracts pass; every named CLI mode and exported validator exists; LFS payload bytes receive complete review; authorization is exact and ignored; final modes re-enumerate every surface; no remote mutation or new dependency exists.</acceptance_criteria>
  <done>The project has one tested verifier that cannot authorize a push, deployment consequence, or public gate from stale, incomplete, unreviewed, or conversation-only input.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Local process → pinned Git/GitHub/scanner binaries | External executable identity, provenance, output, and exit semantics are untrusted until exact validation. |
| GitHub/LFS payloads → temporary storage | Private, binary, archived, oversized, or hostile bytes must be bounded and completely reviewable without persistence. |
| Human decision input → pre-push authorization | Findings, remediation, refspec/OIDs, and deployment consequences require exact immutable bindings before mutation. |
| Authorized push → post-push evidence | Only the named ref transition and fully reviewed resulting workflow/hosted surfaces may be attested. |
| Evidence bundle → Phase 4 visibility action | Every authoritative surface must be re-enumerated; stored digests and stale approval cannot authorize visibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-02-PR | Spoofing | tool provenance | mitigate | Exact publisher release/final origin, checksum-manifest entry/digest, binary digest/version/path/platform binding. |
| T-03-02-LF | Information Disclosure | historic LFS payload review | mitigate | Secure temp fetch, pointer/OID verification, both policy scans per object, redacted results, and fail-closed unreviewable bytes. |
| T-03-02-PG | Tampering | API pagination/download coverage | mitigate | Authenticated pagination, unique authoritative ID/count reconciliation, every attempt/asset/attachment result, and permission gaps block. |
| T-03-02-AU | Repudiation | pre-push authorization | mitigate | Named reviewer/time, exact evidence/rights/findings/remediation/refspec/OID/deployment bindings, owner-only ignored record, self-digest. |
| T-03-02-CA | Elevation of Privilege | consequential non-ref remediation | mitigate | One-action strict authorization, exact target/operation/consequences, proof, expiry, mandatory recollection, and no ref authority. |
| T-03-02-CL | Tampering | consequential-action history | mitigate | Append-only hash-chained ledger embeds each authorization and verified result/following evidence before reuse; chain/final-current mismatch blocks. |
| T-03-02-DP | Elevation of Privilege | every main push/deployment | mitigate | Every push has exact old/new OIDs and workflow/environment/target/effects; rewrite additionally binds canonical local adoption and force-with-lease. |
| T-03-02-DR | Tampering | current snapshot | mitigate | Finalize/check-final freshly re-enumerate every Git/GitHub/Actions/LFS/settings/credential/input class and reject any delta. |
| T-03-02-AR | Denial of Service | hostile archives/downloads | mitigate | Neutral temp paths, no repository extraction, size/time/count/depth ceilings, and every warning/skip/error blocked. |
| T-03-02-RL | Information Disclosure | evidence/stdout | mitigate | Full redaction, generated temp names, metadata-only records, no raw values/secret hashes, cleanup in `finally`. |
| T-03-02-HD | Elevation of Privilege | hidden mutation | mitigate | Verifier modes are remote-read-only; provider/Git/GitHub changes require later named authorization. |
</threat_model>

<verification>The focused Vitest file passes all provenance, LFS-payload, completeness/redaction/pagination, non-ref authorization, append-only action-ledger initialization/record/replay rejection, every-push ordinary/rewrite authorization, canonical local adoption, exact force-with-lease transition, deployment consequence, ledger-bound attestation, per-surface drift, and derived-gate cases. Do not run live collection, formatters, linters, builds, project-wide suites, pushes, releases, publication, or visibility changes.</verification>

<success_criteria>
- SRC-02 has a complete fail-closed byte-review and remediation-order contract for intended Git history, historic LFS payloads, every hosted surface, settings, and credential metadata.
- No provider/cleanup/rewrite/support action, push, or deployment consequence can occur without the applicable strict ignored authorization bound to exact current evidence/right/tool digests, findings, proof, reviewer/time, targets/commands/OIDs, and named consequences.
- LIC-01 and LIC-03 remain immutable inputs to a fresh derived gate, and every final check re-enumerates all authoritative surfaces instead of trusting stored digests.
- The verifier is remote-read-only with no raw-secret persistence path, hidden cleanup, or new dependency.
</success_criteria>

<output>Create `.planning/phases/03-public-disclosure-and-gpl-readiness/03-02-SUMMARY.md` with RED/GREEN evidence, exported interfaces, exact tool/LFS contracts, one-action authorization and append-only result-ledger schemas, every-push/attestation schemas, reachable canonical rewrite tests, exhaustive current-surface matrix tests, and confirmation that no live remote mutation or collection ran.</output>
