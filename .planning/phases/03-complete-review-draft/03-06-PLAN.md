---
phase: 03-complete-review-draft
plan: 06
type: tdd
wave: 6
depends_on: [03-05]
files_modified:
  - src/contracts/api.ts
  - src/server/capabilities.ts
  - src/git/selector-drift.ts
  - src/server/routes.ts
  - src/web/api/client.ts
  - src/web/model/selector-drift-state.ts
  - src/web/components/SelectorDriftNotice.vue
  - src/web/App.vue
  - tests/git/selector-drift.test.ts
  - tests/api/selector-drift.test.ts
  - tests/integration/selector-drift-ui.spec.ts
autonomous: true
requirements: [DRFT-06]
must_haves:
  truths:
    - "DRFT-06 polling re-resolves only server-retained launch descriptors and returns each base/head source independently as unchanged, moved, or unavailable using full old/new commit OIDs."
    - "A moved/unavailable source produces a persistent notice below the pinned identity header stating the open review remains pinned; full old/new OIDs are visible/copyable and unavailable never fabricates a new identity."
    - "Drift observation never changes or rebuilds pinned base/head/merge-base OIDs, comparison key, draft, revision, blobs, inventory, Monaco models/zones, anchor fields, file selection or local attempted buffers."
    - "Polling runs on initial load, visible interval and visibility regain, coalesces overlap, announces only status/current-full-OID transitions, never steals focus, and offers only explicit Launch new comparison guidance."
    - "The browser supplies no repository, ref, worktree path, selector, OID or comparison authority; no silent refresh, rebase, source mutation, or anchor relocation route/action exists."
  artifacts:
    - path: "src/git/selector-drift.ts"
      provides: "Read-only native-Git branch/worktree re-resolution against retained launch descriptors"
    - path: "src/web/components/SelectorDriftNotice.vue"
      provides: "Pinned-identity drift/unavailable warning with full OIDs"
    - path: "tests/git/selector-drift.test.ts"
      provides: "Real-Git branch/worktree movement and immutable-comparison proof"
  key_links:
    - from: "src/server/capabilities.ts"
      to: "src/git/selector-drift.ts"
      via: "server-owned typed launch descriptors, never request-selected selectors"
    - from: "src/web/model/selector-drift-state.ts"
      to: "src/web/components/SelectorDriftNotice.vue"
      via: "read-only coalesced status projection independent of canonical draft state"
---

<objective>
Deliver selector drift as a complete read-only side channel: moving/unavailable branch or worktree selections are reported with old/new full identities while the open comparison, draft, anchors and every local attempt remain pinned and unchanged.

Purpose: Satisfy DRFT-06 without turning observation into refresh or expanding browser Git authority.
Output: Reconciled typed descriptors, native-Git re-resolution, secured no-input route, polling state, warning UI and focused real-Git/browser tests.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-complete-review-draft/03-01-SUMMARY.md
@.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/03-complete-review-draft/03-CONTEXT.md
@.planning/phases/03-complete-review-draft/03-RESEARCH.md
@.planning/phases/03-complete-review-draft/03-UI-SPEC.md

<interfaces>
Conditional source/test paths must map to the actual Phase 1 session/capability/Git helpers and Vue identity-header owners in the approved 03-01-RECONCILIATION.json ledger. Every verification executes its keyed approved executable/argument array, and 03-06-SUMMARY.md records the resolved command and actual test paths.

Settled DTO: each side is unchanged, moved { role, safe label, selector type, old full OID, new full OID }, or unavailable { role, safe label, selector type, old full OID, bounded reason }. The route has no request body/query selector authority.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED/GREEN server-owned full-OID drift observation</name>
  <files>src/contracts/api.ts, src/server/capabilities.ts, src/git/selector-drift.ts, src/server/routes.ts, tests/git/selector-drift.test.ts, tests/api/selector-drift.test.ts</files>
  <read_first>
    .planning/phases/03-complete-review-draft/03-01-SUMMARY.md
    .planning/phases/01-pinned-local-comparison/01-CONTEXT.md
    src/server/capabilities.ts
    src/git/git-runner.ts
    src/server/routes.ts
    tests/helpers/git-fixture.ts
  </read_first>
  <behavior>
    - Branch and registered worktree selectors resolve independently to unchanged/moved/unavailable; detached worktrees remain worktree selectors.
    - Equality uses complete commit OIDs; moved reports complete old/new OIDs; unavailable reports old only and bounded reason.
    - Dirty worktree bytes never affect committed HEAD observation.
    - Route rejects body/query authority and inherited unauthenticated/cross-origin calls before Git work.
    - Rechecking leaves a serialized snapshot of every pinned comparison/draft/anchor identity exactly unchanged.
  </behavior>
  <action>First add failing real-Git and Fastify tests for branch, branch/worktree, worktree/branch, worktree/worktree, detached, deleted/unregistered/unavailable and dirty-worktree fixtures. Snapshot full pinned session capability, comparison key, draft Buffer/revision, file inventory, blobs and anchor records before/after observations. Then retain typed launch descriptors server-side beside immutable pinned identities: full branch ref or exact registered worktree identity/path representation plus launch committed HEAD. Reuse only the reconciled Git runner with fixed repository closure and argument arrays—never shell interpolation. Branch resolution must invoke the semantic equivalent of argument vector `['rev-parse', '--verify', '--end-of-options', fullRef + '^{commit}']`; the browser cannot supply `fullRef` or any selector authority. Continue the reconciled NUL-safe `worktree list --porcelain -z` parsing discipline for worktrees. Register a no-body/no-selector secured status route and strict shared response schema. Map Git failures to bounded typed unavailable reasons without returning stderr, commands, absolute repository/worktree paths, tokens, or stacks. Do not accept arbitrary refs/paths/OIDs, rebuild comparison, invoke draft writer, change capability registry, refresh objects, or mutate source. Run focused RED then GREEN.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),k='03-06-task-1-git-api',c=JSON.parse(readFileSync('.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json','utf8')).commands[k]; if(!c)throw Error('unapproved command '+k); console.log('resolved command:',c.executable,...c.args); const r=spawnSync(c.executable,c.args,{stdio:'inherit'}); process.exit(r.status??1)"</automated>
  </verify>
  <acceptance_criteria>
    - All selector-kind/role cases have deterministic old/new full-OID observations.
    - Every immutable pinned/draft/anchor snapshot is byte/deep equal before and after drift checks.
    - Route security and unknown-body/query rejections precede Git invocation.
    - Runner-spy cases prove the exact option-terminated argument-array shape and zero shell interpolation; unavailable DTOs expose no stderr or absolute repository/worktree path.
    - No unavailable result fabricates a current identity.
  </acceptance_criteria>
  <done>The server reports movement without granting browser selector authority or mutating the open review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Integrate coalesced polling and pinned warning UI</name>
  <files>src/web/api/client.ts, src/web/model/selector-drift-state.ts, src/web/components/SelectorDriftNotice.vue, src/web/App.vue, tests/integration/selector-drift-ui.spec.ts</files>
  <read_first>
    .planning/phases/03-complete-review-draft/03-UI-SPEC.md#Selector-Drift-Contract
    .planning/phases/03-complete-review-draft/03-01-SUMMARY.md
    src/web/App.vue
    src/web/api/client.ts
    src/web/components/PinnedComparisonHeader.vue
    src/web/model/review-draft-state.ts
    tests/integration/selector-drift-ui.spec.ts
  </read_first>
  <behavior>
    - Initial load/visibility regain/visible fake-clock interval poll once; overlapping triggers coalesce and hidden intervals do not spawn Git calls.
    - Unchanged has no notice; moved/unavailable notice names each affected side independently and shows mandatory old/new full OIDs.
    - Only status/full-current-OID transitions announce; unchanged repeated polling does not announce or steal focus.
    - Polling while composers/conflicts/recovery/panel states exist preserves exact state and pinned Monaco/comparison identities.
  </behavior>
  <action>Add failing fake-clock/component cases, then build a read-only drift state owner separate from canonical draft state. Poll on initial load, visibility regain and a modest visible interval using one in-flight/coalesced request; stop with session teardown. Place the notice immediately below the existing pinned identity header and above workspace/recovery state. Use exact UI-SPEC copy, full untruncated OIDs with copy controls, independent Base/Head rows and bounded unavailable reason. `Launch new comparison` displays CLI relaunch instructions by default. Replace that guidance with a callable action only if the approved 03-01 ledger proves an existing pre-authorized fixed no-body capability protected by standard token/Host/Origin checks; never invent one and never accept browser refs, OIDs, selectors, or paths. Never expose Refresh/Update/Move anchors/Use commits/dismiss-forever, never modify canonical state, and preserve every local buffer, focus target, panel/recovery state and Monaco model across status transitions.</action>
  <verify>
    <automated>node -e "const{readFileSync}=require('node:fs'),{spawnSync}=require('node:child_process'),k='03-06-task-2-drift-ui',c=JSON.parse(readFileSync('.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json','utf8')).commands[k]; if(!c)throw Error('unapproved command '+k); console.log('resolved command:',c.executable,...c.args); const r=spawnSync(c.executable,c.args,{stdio:'inherit'}); process.exit(r.status??1)"</automated>
  </verify>
  <acceptance_criteria>
    - Notice shows old/new full OIDs and “open review remains pinned” while underlying diff/blob/anchor/draft snapshots remain unchanged.
    - Polling coalesces and respects visibility/focus; only meaningful transitions announce.
    - No browser authority, silent identity refresh, comparison rebuild, source mutation or relocation action is reachable.
    - All local attempted buffers survive every drift transition byte-for-byte.
  </acceptance_criteria>
  <done>The user sees exact selector movement without any change to the review they are currently maintaining.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Retained launch selector → current Git namespace | Re-resolution observes moving repository metadata but must not alter pinned authority. |
| Browser poll → Git service | Browser may request status only; it cannot supply selectors, paths, OIDs or repositories. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-23 | Tampering | selector substitution | mitigate | Server-owned typed descriptors, fixed repository closure and strict no-input route. |
| T-03-24 | Spoofing | shortened/display identity | mitigate | Compare and report complete commit OIDs; display labels never serve as authority. |
| T-03-25 | Denial of Service | polling | mitigate | Visible-only modest interval, one in-flight request and trigger coalescing. |
| T-03-26 | Elevation of Privilege | Git route | mitigate | Inherited bearer/Host/Origin checks before native Git and no browser object/path authority. |
| T-03-27 | Information Disclosure | Git errors/worktree paths | mitigate | Safe labels and bounded reasons; no absolute path, command, stderr or token exposure. |
| T-03-SC | Tampering | dependencies | mitigate | Native installed Git and existing stack only; no package. |
</threat_model>

<verification>
Run only the two approved keyed commands in 03-01-RECONCILIATION.json; their argument arrays are the sole authority for actual real-Git, Fastify, and selector-drift UI test paths. Preserve RED→GREEN→REFACTOR evidence and record each resolved command in 03-06-SUMMARY.md. Do not run formatter, linter, build, aggregate suite, packaged E2E, package manager, or project-wide command.
</verification>

<success_criteria>
- DRFT-06 visibly reports branch/worktree movement/unavailability using old/new complete OIDs.
- Pinned comparison, blobs, inventory, draft revision/bytes, anchors, Monaco models and local buffers remain unchanged.
- Polling is bounded/coalesced and has no browser selector authority.
- Only explicit new-launch guidance exists; no current-session refresh or source mutation is introduced.
</success_criteria>

## Artifacts this phase produces

- Reconciled selector descriptor, read-only native-Git status service/API and warning UI.
- Focused Git/API/UI tests and immutable-snapshot evidence in `03-06-SUMMARY.md`.

<output>Create `.planning/phases/03-complete-review-draft/03-06-SUMMARY.md` with actual owners, RED/GREEN/REFACTOR commands, selector fixture table, security denials, polling timeline, full-OID examples and immutable pinned-state comparisons.</output>
