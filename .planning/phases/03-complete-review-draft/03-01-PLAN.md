---
phase: 03-complete-review-draft
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/03-complete-review-draft/03-01-SUMMARY.md
  - .planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json
autonomous: false
requirements: [CMT-03, CMT-04, CMT-05, CMT-06, CMT-07, DRFT-04, DRFT-05, DRFT-06]
must_haves:
  truths:
    - "Before any Phase 3 source edit, the executor proves Phase 1 and Phase 2 are implemented and records the actual owners, paths, exports, schemas, routes, scripts, package versions, and test seams that Phase 3 will extend."
    - "D-01–D-17 are mapped to actual implementation seams without treating provisional Phase 1/2 plan names as source contracts."
    - "The reconciliation explicitly identifies one existing canonical draft owner, one atomic persistence boundary, one secured Fastify registry, one Vue canonical-state owner, and one long-lived Monaco navigation adapter; it forbids parallel replacements."
    - "Execution cannot proceed to Plan 03-02 until a human verifies the read-only source and pre-existing-planning-artifact handoff map, confirms both reconciliation outputs, and confirms all Phase 1/2 prerequisite summaries and source are present."
    - "The approved ledger is the sole execution-visible authority for every 03-02–03-07 test path and verification command; each keyed command records executable plus argument array and every downstream summary records the resolved command actually run."
    - "Reconciliation proves whether a secured fixed-authority no-body draft-file reveal capability already exists and whether selector drift has an existing fixed no-body new-comparison capability; absence selects the planned reveal implementation and CLI relaunch default."
    - "After substituting actual owners, the executor recounts each downstream plan's unique modified files and stops for split/replan before source edits when any plan reaches 15 or more."
  artifacts:
    - path: ".planning/phases/03-complete-review-draft/03-01-SUMMARY.md"
      provides: "Execution-time handoff map and approved reconciled interface ledger"
    - path: ".planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json"
      provides: "Human-approved machine-readable actual-path and executable/argv command ledger consumed by every downstream verification"
  key_links:
    - from: ".planning/phases/02-anchored-diff-review/02-03-SUMMARY.md"
      to: ".planning/phases/03-complete-review-draft/03-01-SUMMARY.md"
      via: "actual draft schema/store/route handoff, not provisional plan filenames"
    - from: ".planning/phases/02-anchored-diff-review/02-06-SUMMARY.md"
      to: ".planning/phases/03-complete-review-draft/03-01-SUMMARY.md"
      via: "actual canonical Vue state, comments surface, API client, and Monaco navigation handoff"
---

<objective>
## Phase Goal

**As a** developer, **I want to** finish and safely maintain a complete local review draft as comments and selected refs evolve, **so that** accepted feedback remains canonical without silent overwrite, relocation, or identity refresh.

Establish a mandatory implementation-handoff boundary before Phase 3 changes code. This plan performs inspection only: it replaces provisional Phase 1/2 names with observed interfaces and blocks execution when prerequisites are missing or ambiguous.

Purpose: Prevent duplicate infrastructure and guessed signatures while preserving D-01–D-17 and all eight Phase 3 requirements.
Output: An approved reconciliation ledger in `.planning/phases/03-complete-review-draft/03-01-SUMMARY.md` and `.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json`; creation of those exact outputs is permitted, while product/source/test/config/lockfile and pre-existing planning-artifact edits are prohibited.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/config.json
@.planning/phases/01-pinned-local-comparison/01-CONTEXT.md
@.planning/phases/02-anchored-diff-review/02-CONTEXT.md
@.planning/phases/03-complete-review-draft/03-CONTEXT.md
@.planning/phases/03-complete-review-draft/03-RESEARCH.md
@.planning/phases/03-complete-review-draft/03-UI-SPEC.md
@.planning/phases/03-complete-review-draft/03-PATTERNS.md

<interfaces>
All source names in later Phase 3 plans are conditional responsibility labels derived from research. The executor must substitute the actual interfaces recorded here. The behavioral contracts are settled; Phase 1/2 paths and symbol names are not.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile implemented Phase 1/2 handoffs without editing implementation or pre-existing planning artifacts</name>
  <files>.planning/phases/03-complete-review-draft/03-01-SUMMARY.md, .planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json</files>
  <read_first>
    .planning/phases/01-pinned-local-comparison/*-SUMMARY.md
    .planning/phases/02-anchored-diff-review/*-SUMMARY.md
    .planning/phases/02-anchored-diff-review/02-01-PLAN.md
    .planning/phases/02-anchored-diff-review/02-02-PLAN.md
    .planning/phases/02-anchored-diff-review/02-03-PLAN.md
    .planning/phases/02-anchored-diff-review/02-04-PLAN.md
    .planning/phases/02-anchored-diff-review/02-05-PLAN.md
    .planning/phases/02-anchored-diff-review/02-06-PLAN.md
    .planning/phases/02-anchored-diff-review/02-07-PLAN.md
    package.json
    package-lock.json
  </read_first>
  <action>Inspect product/source/test/config/lockfile and pre-existing planning artifacts read-only; do not edit them. Creation of exactly `.planning/phases/03-complete-review-draft/03-01-SUMMARY.md` and `.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json` is permitted. Record in those outputs: actual package/compiler/test executables and argument arrays; every 03-02–03-07 planned test responsibility mapped to its execution-visible actual path; keyed focused commands for every downstream task verification; shared Zod current-draft schema and version; comment text limits and one-per-anchor invariant; comparison-key/draft-path factory; raw loader and atomic writer; per-draft serialization mechanism; secured Fastify factory, standard token/Host/Origin hook ordering and route registry; capability/session closure; API response validation/client; Vue canonical draft owner and independent add buffer; comments rail/review surface and UI primitives; one Monaco adapter, exact anchor verifier, show-comment command, view-zone lifecycle; real-Git runner that accepts argument arrays; filesystem-fault, Fastify inject, real-Monaco, and packaged Playwright harnesses. Prove whether an existing fixed-authority `Reveal draft file` capability is launch-owned, no-body/no-path, and protected by those standard hooks; if not, mark it `MUST_IMPLEMENT_03_03`. Independently prove whether a fixed pre-authorized no-body new-comparison action exists; otherwise lock selector drift to CLI relaunch instructions. Resolve the Markdown declaration decision only after the approved registry/lock artifact and reconciled compiler/import check. Map every conditional path used by Plans 03-02–03-07 to its actual owner. Substitute those paths into each plan's declared file list conceptually, recount unique actual modified files per plan, and record the count; if any count is 15 or more, stop before Phase 3 source edits and split/narrow that plan without reducing D-01–D-17 or requirement coverage. Per D-01–D-17, identify extension points for grouping, navigation, comment lifecycle, summary, one whole-draft revision, conflict retention, corrupt/newer classification, backup-first recovery, drift, and fixed draft reveal without creating source. If Phase 1/2 are absent, incomplete, or disagree with prerequisite contracts, stop and report the exact missing handoff; do not invent signatures or recreate dependencies.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); for (const p of ['package.json','src','tests','.planning/phases/01-pinned-local-comparison','.planning/phases/02-anchored-diff-review']) if(!fs.existsSync(p)) throw new Error('missing Phase 1/2 handoff: '+p)"</automated>
  </verify>
  <acceptance_criteria>
    - The ledger names one and only one actual owner for each responsibility above and quotes its exported contract or route shape.
    - Every provisional path in later plans has an explicit actual-path substitution or an explicit prerequisite failure.
    - The JSON ledger contains one key per downstream task verification, expressed as an executable plus argument array (not a shell string), and maps every provisional test path to the approved actual test path; later summaries must echo the resolved command they ran.
    - The ledger records `revealDraftFile` as either a proven existing fixed no-body capability or `MUST_IMPLEMENT_03_03`, and records selector relaunch as either a proven existing fixed no-body capability or `CLI_RELAUNCH`.
    - The ledger records the reconciled compiler/import check that decides whether external Markdown declarations are required, without treating the research assumption as a decision.
    - An actual-path unique-file recount is recorded for 03-02–03-07; every count is below 15 or execution stops with an explicit split/replan requirement.
    - Installed versions and focused commands are observed from the repository; creating `.planning/phases/03-complete-review-draft/03-01-SUMMARY.md` and `.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json` is explicitly permitted, while product/source/test/config/lockfile, Phase 4, and pre-existing planning-artifact edits are prohibited.
    - The ledger includes D-01–D-17 and CMT-03–CMT-07/DRFT-04–DRFT-06 coverage rows.
  </acceptance_criteria>
  <done>The read-only implementation reconciliation map is complete in the two declared reconciliation outputs, grounded in implemented source and execution summaries, and contains no guessed Phase 1/2 signature.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the implementation handoff boundary</name>
  <files>.planning/phases/03-complete-review-draft/03-01-SUMMARY.md, .planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json</files>
  <read_first>.planning/phases/03-complete-review-draft/03-01-SUMMARY.md</read_first>
  <action>Present the reconciliation summary and machine-readable ledger before any Phase 3 source change. Ask the reviewer to confirm that Phase 1/2 source exists; each actual owner and test path is correctly identified; every keyed executable/argument-array command is accurate; the Markdown compiler decision procedure, reveal-capability disposition, selector-relaunch disposition, and actual-file recount are correct; and later plans will modify those owners rather than create parallel schemas, stores, apps, clients, canonical indexes, or Monaco adapters. The checkpoint performs no install or file edit and is not auto-approvable.</action>
  <verify>
    <automated>node -e "const fs=require('node:fs'); const p='.planning/phases/03-complete-review-draft/03-01-SUMMARY.md', j='.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json'; if(!fs.existsSync(p)||!fs.existsSync(j)) throw new Error('reconciliation ledger absent'); const s=fs.readFileSync(p,'utf8'), x=JSON.parse(fs.readFileSync(j,'utf8')); for(const v of ['D-01','D-17','CMT-03','DRFT-06','actual']) if(!s.includes(v)) throw new Error('ledger missing '+v); for(const v of ['paths','commands','fileCounts','revealDraftFile','selectorRelaunch','markdownDeclarations']) if(!(v in x)) throw new Error('machine ledger missing '+v)"</automated>
    <human-check>Confirm the ledger against the implemented Phase 1/2 source and type “approved”; otherwise identify the incorrect or missing seam.</human-check>
  </verify>
  <acceptance_criteria>
    - Human approval is recorded in the summary.
    - Plans 03-02–03-07 remain blocked until approval.
    - Approval covers the machine-readable path/command matrix, capability dispositions, Markdown declaration decision procedure, and below-15 actual-file counts.
    - Approval changes no source and does not settle any unobserved name.
  </acceptance_criteria>
  <done>The actual implementation handoff is approved and downstream executors have a deterministic interface map.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Provisional planning contract → implemented source | Detailed Phase 1/2 plans are not proof that an interface exists. |
| Implemented Phase 2 state → Phase 3 extension | Duplicate schema/store/app/client/editor owners would split authority and weaken safety. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-03-01 | Tampering | handoff assumptions | mitigate | Read-only implementation and pre-existing-planning-artifact inspection, creation of only the two declared reconciliation outputs, actual export ledger, and blocking human approval before downstream edits. |
| T-03-02 | Elevation of Privilege | browser/server authority seams | mitigate | Reuse the observed launch-scoped capability and earliest security hooks; never infer a broader request contract. |
| T-03-03 | Repudiation | plan-to-source mapping | mitigate | Summary records actual paths, interfaces, commands, versions, and approval. |
| T-03-SC | Tampering | package assumptions | mitigate | Record installed packages only; no install occurs in this plan. |
</threat_model>

<verification>
Only the prerequisite-presence and ledger-completeness commands above are allowed. Do not run tests, build, formatter, linter, project-wide command, package manager, or Git commit.
</verification>

<success_criteria>
- No Phase 3 code is touched before the actual Phase 1/2 implementation is reconciled.
- D-01–D-17 and all Phase 3 requirements have named actual extension seams.
- Downstream paths are explicitly reconciliation-derived, not treated as settled by planning.
- Human approval blocks continuation when prerequisite source or interfaces are missing.
</success_criteria>

## Artifacts this phase produces

- `.planning/phases/03-complete-review-draft/03-01-SUMMARY.md` — read-only implementation-interface map, prerequisite result, focused commands, and approval evidence created by the executor.
- `.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json` — machine-readable actual-path and executable/argument-array reconciliation ledger created by the executor.
- No product/source/test/config/lockfile or pre-existing planning artifact is modified by this plan.

<output>Create exactly `.planning/phases/03-complete-review-draft/03-01-SUMMARY.md` and `.planning/phases/03-complete-review-draft/03-01-RECONCILIATION.json` after read-only inspection; include the reconciliation ledger and blocking approval result without editing product/source/test/config/lockfile or pre-existing planning artifacts.</output>
