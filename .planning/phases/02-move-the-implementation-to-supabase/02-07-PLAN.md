---
phase: 02-move-the-implementation-to-supabase
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - services/support/migrations/001_init.sql
  - services/support/src/app.ts
  - services/support/src/config.ts
  - services/support/src/db.ts
  - services/support/src/email.ts
  - services/support/src/fulfillment.ts
  - services/support/src/recovery-store.ts
  - services/support/src/schema.ts
  - services/support/src/server.ts
  - render.yaml
autonomous: true
requirements: [PAY-04, REC-01, REC-02]
must_haves:
  truths:
    - "Only after approved 02-06 hosted proof, the legacy hosted application core, database migration, Render deployment surface, Resend adapter, and authority stores are deleted without fallback (D-02, D-08, D-16, D-19)."
    - "Deleting the hosted core does not touch Cumpa's local loopback Fastify server, machine-local verified support state, or the exact route/test/tooling inventory owned by 02-08 and 02-09."
    - "External provider retirement remains blocked until 02-11 production proof."
  artifacts:
    - path: "services/support/src/app.ts"
      provides: "Deleted standalone Fastify application"
    - path: "services/support/migrations/001_init.sql"
      provides: "Deleted standalone PostgreSQL schema"
    - path: "render.yaml"
      provides: "Deleted Render deployment surface"
  key_links:
    - from: ".planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md"
      to: "legacy runtime deletion"
      via: "explicit approved development gate checked before removal"
      pattern: "approved"
  prohibitions:
    - "HIGH: Never delete the legacy runtime before approved development evidence; never retain a runtime fallback afterward."
    - "HIGH: Do not remove or alter Cumpa's local loopback Fastify server."
    - "Do not confirm external provider retirement before 02-11 production proof."
---

<objective>
Delete the legacy standalone hosted runtime and deployment surface after hosted development equivalence is approved.

Purpose: Isolate irreversible runtime removal from the separate tooling/dependency cleanup so each deletion plan stays below the scope threshold.
Output: Ten exact application-core, schema, and Render paths removed without touching local Cumpa behavior.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-06-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md
@docs/support-service-operations.md
@package.json
@package-lock.json
@render.yaml
@services/support/package.json

<interfaces>
Legacy removal is partitioned across exact path inventories: 02-07 deletes ten application-core/schema/deploy files, 02-08 deletes ten route/recovery/test files, and 02-09 deletes nine service-toolchain/root-package files. Root Fastify remains because it is Cumpa's required local loopback server; only the standalone hosted Fastify service is removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete the legacy hosted application core after evidence approval</name>
  <files>services/support/migrations/001_init.sql, services/support/src/app.ts, services/support/src/config.ts, services/support/src/db.ts, services/support/src/email.ts, services/support/src/fulfillment.ts, services/support/src/recovery-store.ts, services/support/src/schema.ts, services/support/src/server.ts, render.yaml</files>
  <read_first>
    - .planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md — must contain explicit approved development gate before any deletion
    - .planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md — legacy hosted runtime inventory and unrelated local-loopback boundary
    - services/support/src/app.ts — standalone Fastify application composition
    - services/support/src/db.ts — standalone PostgreSQL boundary
    - services/support/src/email.ts — Resend adapter superseded by D-10
    - render.yaml — standalone deployment surface
  </read_first>
  <action>First assert the 02-06 evidence explicitly approves real development Supabase/GitHub/Stripe/payment-restore E2E/package behavior; stop otherwise. Delete exactly the ten listed application-core, migration, adapter, and Render files. Leave the recovery/route/test inventory for 02-08 and toolchain/root dependency cleanup for 02-09. Do not touch Cumpa's local loopback server and do not claim external Render/Postgres/Resend resources or credentials are retired; that irreversible provider action remains blocked until 02-11 per D-02.</action>
  <acceptance_criteria>
    - All ten listed legacy application-core/deployment files are absent only after evidence approval.
    - Standalone hosted application composition, database authority, Resend adapter, schema, and Render deployment definition are gone.
    - Local loopback Fastify and existing machine-local verified state remain untouched.
    - Remaining route/test/toolchain/root cleanup stays explicitly owned by 02-08 and 02-09.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e services/support/migrations/001_init.sql &amp;&amp; test ! -e services/support/src/app.ts &amp;&amp; test ! -e services/support/src/config.ts &amp;&amp; test ! -e services/support/src/db.ts &amp;&amp; test ! -e services/support/src/email.ts &amp;&amp; test ! -e services/support/src/fulfillment.ts &amp;&amp; test ! -e services/support/src/recovery-store.ts &amp;&amp; test ! -e services/support/src/schema.ts &amp;&amp; test ! -e services/support/src/server.ts &amp;&amp; test ! -e render.yaml</automated>
  </verify>
  <done>The ten-file legacy hosted application core is gone without touching local Cumpa or claiming provider retirement; commit `refactor(02-07): remove legacy hosted core`.</done>
</task>


</tasks>

## Artifacts this phase produces

- Deleted: ten exact standalone application-core, migration, adapter, schema, and Render deployment files.
- Preserved for 02-08 and 02-09: route/recovery/tests plus service toolchain and root manifest cleanup.
- No compatibility route, fallback runtime, import/reconciliation path, or external-provider retirement claim is produced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Approved hosted authority → runtime deletion | Removal must follow real development equivalence evidence and leave no runtime fallback. |
| Legacy hosted runtime → local Cumpa server | Standalone Fastify code is deleted while unrelated loopback Fastify remains intact. |
| Repository deletion → external providers | Source removal does not authorize provider/resource retirement before 02-11 production proof. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-07-PG | Tampering | HIGH | deletion precondition | mitigate | Require explicit approved 02-06 hosted evidence before any removal. |
| T-02-07-LF | Elevation/Tampering | HIGH | runtime fallback | mitigate | Delete all ten exact application-core/deployment paths and leave remaining deletion artifacts to explicit dependent plans. |
| T-02-07-SL | Information Disclosure | HIGH | legacy hosted core | mitigate | Remove standalone secret-loading, email, database, schema, and application implementation paths. |
| T-02-07-RG | Denial of Service | HIGH | local loopback | mitigate | Keep deletion scoped away from Cumpa's local Fastify server and machine-local verified state. |
| T-02-07-RP | Repudiation | MEDIUM | external retirement | mitigate | Record no provider-retirement claim; 02-11 owns dated retirement evidence after production proof. |

Execution MUST stop if any HIGH threat remains unresolved or the hosted evidence gate is not approved. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
The exact ten application-core/deployment paths are absent after the approved 02-06 gate, while local Cumpa and the explicit 02-08/02-09 deletion inventories remain intact.
</verification>

<success_criteria>
- D-02 removes the legacy runtime only after equivalent Supabase development proof.
- D-08/D-19 leave no email recovery or runtime compatibility surface.
- D-16 leaves no import, dual-write, reconciliation, or fallback runtime.
- External provider retirement remains explicitly blocked until 02-11.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-07-SUMMARY.md` with the exact runtime/deployment deletion inventory.
</output>
