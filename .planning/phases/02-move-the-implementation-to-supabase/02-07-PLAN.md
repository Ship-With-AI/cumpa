---
phase: 02-move-the-implementation-to-supabase
plan: 07
type: execute
wave: 7
depends_on: [02-06]
files_modified:
  - scripts/verify-supabase-support.mjs
  - docs/support-service-operations.md
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/support-recovery.spec.ts
  - tests/e2e/support-restore.spec.ts
  - .planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
user_setup:
  - service: supabase
    why: "One still-empty hosted project receives the one-time prelaunch acceptance deployment (D-14, D-17). The driver reads every value from the named environment variable; it never prompts for or preserves an unspecified dashboard value."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens; secret, process environment only, never recorded"
      - name: SUPABASE_PROJECT_REF
        source: "Supabase Dashboard -> Project Settings -> General -> Reference ID for the sole project; process environment only, only full-ref SHA-256 and display suffix recorded"
      - name: SUPABASE_DB_PASSWORD
        source: "Supabase Dashboard -> Project Settings -> Database; secret, process environment only, never recorded"
      - name: SUPPORT_PUBLIC_ORIGIN
        source: "Supabase Dashboard -> Project Settings -> API -> Project URL; exact canonical `https://${SUPABASE_PROJECT_REF}.supabase.co` origin with no path, query, fragment, credentials, or trailing slash; only equality status and redacted hash/suffix recorded"
      - name: PRELAUNCH_SUPABASE_SITE_URL
        source: "Desired Supabase Dashboard -> Authentication -> URL Configuration -> Site URL; must equal `${SUPPORT_PUBLIC_ORIGIN}/functions/v1/support-flow/complete`; only equality status recorded"
      - name: PRELAUNCH_SUPABASE_REDIRECT_URL
        source: "Desired Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs entry; must equal `${SUPPORT_PUBLIC_ORIGIN}/functions/v1/support-flow/callback`; only equality status recorded"
  - service: github
    why: "Temporary GitHub OAuth settings and the two real account identities are explicit prelaunch-only inputs."
    env_vars:
      - name: PRELAUNCH_GITHUB_CLIENT_ID
        source: "GitHub Settings -> Developer settings -> OAuth Apps -> temporary prelaunch app -> Client ID; process environment only, record only redacted hash/suffix and presence/mode status"
      - name: PRELAUNCH_GITHUB_CLIENT_SECRET
        source: "Same temporary GitHub OAuth app -> Client secrets; secret, process environment only, never recorded"
      - name: PRELAUNCH_GITHUB_CALLBACK_URL
        source: "Same temporary GitHub OAuth app -> Authorization callback URL; must equal `${SUPPORT_PUBLIC_ORIGIN}/auth/v1/callback`; only equality status recorded"
    dashboard_config:
      - task: "At blocking Task 2 set `paid_github_identity_ready=true` for one real paid GitHub account and `unpaid_github_identity_ready=true` for a distinct real unpaid GitHub account; these are human-action inputs, never environment credentials"
        location: "The human's isolated GitHub browser profiles; never provide credentials, account identifiers, emails, tokens, or codes in chat or evidence"
  - service: stripe
    why: "One coherent Stripe test-mode secret/Price/webhook set is configured for real signed fulfillment."
    env_vars:
      - name: PRELAUNCH_STRIPE_SECRET_KEY
        source: "Stripe Dashboard with Test mode on -> Developers -> API keys -> Secret key; secret, process environment only, never recorded; driver maps it to function secret `STRIPE_SECRET_KEY`"
      - name: PRELAUNCH_STRIPE_PRICE_ID
        source: "Stripe Dashboard with Test mode on -> Product catalog -> Price for the fixed one-time USD $49.99 product; process environment only, record only redacted hash/suffix and validated product facts; driver maps it to `STRIPE_PRICE_ID`"
      - name: PRELAUNCH_STRIPE_WEBHOOK_ENDPOINT_ID
        source: "Stripe Dashboard with Test mode on -> Developers -> Webhooks -> endpoint whose URL is `${SUPPORT_PUBLIC_ORIGIN}/functions/v1/stripe-webhook`; process environment only, record only redacted hash/suffix and endpoint status"
      - name: PRELAUNCH_STRIPE_WEBHOOK_SECRET
        source: "The signing secret revealed on that exact test webhook endpoint; secret, process environment only, never recorded; driver maps it to `STRIPE_WEBHOOK_SECRET`"
must_haves:
  truths:
    - "The exact migration and three Edge Functions deploy schema-first to exactly one still-empty project (D-14, D-15)."
    - "Before any migration, secret, Auth, GitHub, Stripe, or function mutation, one read-only preflight validates the complete named input set, canonical HTTPS origin/derived URLs, full project fingerprint, still-empty target, test-mode Stripe account/Price/endpoint, fixed one-time USD $49.99 Price facts, endpoint/signing-secret provenance, and exact GitHub callback equality; any missing, ambiguous, invalid, or test/live-mismatched value aborts all hosted changes."
    - "Evidence stores SHA-256 of the complete project ref plus a display suffix, never the complete ref; every target check hashes the supplied complete ref in memory."
    - "A paid GitHub identity and a distinct unpaid identity complete the real OAuth matrix from fresh isolated installation stores at a blocking human-action gate."
    - "The acceptance record contains redacted stable SHA-256 handles and counts for every created auth.users, support_intents, supporters, checkout_sessions, stripe_events, and installation_bindings fixture."
    - "Real test-mode OAuth, Checkout, signed webhook, restore, hostile-path, package, status, and unrestricted-review evidence receives a separate final approval (D-17)."
  artifacts:
    - path: "scripts/verify-supabase-support.mjs"
      provides: "Sole-target fingerprinting, deployment, acceptance, fixture inventory, and evidence validation"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md"
      provides: "Approved redacted prelaunch evidence and exact cleanup manifest"
  key_links:
    - from: "SUPABASE_PROJECT_REF"
      to: "02-07-PRELAUNCH-EVIDENCE.md"
      via: "SHA-256 over the complete ref in memory plus non-authoritative display suffix"
      pattern: "project_ref_sha256"
    - from: "acceptance-created hosted rows"
      to: "fixture cleanup manifest"
      via: "per-table primary-key SHA-256 handles and exact counts"
      pattern: "fixture_manifest"
    - from: "named prelaunch operator inputs"
      to: "first hosted mutation"
      via: "complete read-only preflight with no preserve-current semantics; mutation is unreachable unless every validation passes"
      pattern: "preflight.*prelaunch-test"
  prohibitions:
    - "HIGH: Never create or reference a second hosted project or write the complete project ref, credential, OAuth material, Stripe secret, endpoint secret, email, profile data, or raw provider identifier to evidence."
    - "HIGH: Stripe objects remain in Stripe test mode; never copy test objects or identifiers into live configuration, and abort all hosted changes before mutation on any missing input, failed HTTPS/fingerprint/callback/coherence check, ambiguous preserved value, or mode mismatch."
    - "Do not promote provider configuration or delete legacy source in this plan."
---

<objective>
Deploy and approve the one-time real-provider acceptance matrix against the sole still-empty project, with unique target lineage and an exact redacted cleanup manifest.

Purpose: Prove D-17 before irreversible cutover while making every acceptance fixture safely removable.
Output: Acceptance driver/contracts, runbook, fingerprinted fixture inventory, and separately approved evidence.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-06-SUMMARY.md
@supabase/config.toml
@supabase/migrations/20260814000000_support_authority.sql
@supabase/functions/support-api/index.ts
@supabase/functions/support-flow/index.ts
@supabase/functions/stripe-webhook/index.ts

<interfaces>
- `project_ref_sha256` is lowercase SHA-256 of the complete supplied ref; `project_ref_suffix` is display-only and never authorizes a target.
- `fixture_manifest` contains exact counts and non-reversible stable handles for `auth.users` plus all five `support_private` tables.
- Raw fixture keys exist only in process memory while querying, comparing, and later deleting exact matches.
- Prelaunch desired URL fields are exact: origin `https://${SUPABASE_PROJECT_REF}.supabase.co`, Supabase Site URL `${SUPPORT_PUBLIC_ORIGIN}/functions/v1/support-flow/complete`, Supabase allowed redirect `${SUPPORT_PUBLIC_ORIGIN}/functions/v1/support-flow/callback`, GitHub authorization callback `${SUPPORT_PUBLIC_ORIGIN}/auth/v1/callback`, and Stripe webhook endpoint `${SUPPORT_PUBLIC_ORIGIN}/functions/v1/stripe-webhook`.
- The driver accepts no implicit dashboard preservation: all named values must be present before its read-only preflight, secrets remain process-memory only, and evidence contains only fingerprints/suffixes, validated non-secret facts, counts, and pass/fail statuses.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy once and prepare fingerprinted acceptance evidence</name>
  <files>scripts/verify-supabase-support.mjs, docs/support-service-operations.md, tests/e2e/support-payment.spec.ts, tests/e2e/support-recovery.spec.ts, tests/e2e/support-restore.spec.ts, .planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md</files>
  <action>Implement the bounded driver with Node's `node:crypto` SHA-256 and a strict prelaunch input loader for every `user_setup` environment variable; reject absent, blank, duplicate/ambiguous, or unexpected-mode values rather than preserving current provider settings. Before any hosted mutation, perform only reads: hash the complete supplied project ref in memory; require the canonical HTTPS project origin and exact derived Supabase Site URL, allowed redirect, GitHub authorization callback, and Stripe webhook URL; verify the sole target fingerprint and still-empty state; authenticate the Stripe key as test mode; retrieve the supplied Price and require `active=true`, `livemode=false`, one-time/non-recurring, `currency=usd`, `unit_amount=4999`; retrieve the supplied test webhook endpoint and require the exact URL and required `checkout.session.completed` event; require the signing secret input to be sourced from that same endpoint via the named operator contract; and require the supplied GitHub callback value to equal the derived callback. Abort before migration, secret writes, Auth/provider updates, webhook changes, or function deployment on any failure, missing field, test/live mismatch, callback mismatch, incoherent endpoint/secret provenance, unexplained project state, or value that would otherwise be ambiguously preserved. After preflight passes, deploy migration `20260814000000` before `support-api`, `support-flow`, and `stripe-webhook` from one commit to that sole ref; set `SUPPORT_PUBLIC_ORIGIN`; configure GitHub-only Auth with the complete temporary client pair and exact Site/redirect/callback values; and atomically install the complete mapped Stripe test secret/Price/webhook set. Replace email recovery coverage with GitHub restore. Run all non-interactive hostile, privacy, package, polling, and unrestricted-review checks. Snapshot exact pre-interactive zero counts for `auth.users`, `support_intents`, `supporters`, `checkout_sessions`, `stripe_events`, and `installation_bindings`; abort if the supposedly empty authority contains unexplained rows. Prepare fresh isolated stores. Evidence records only the ref fingerprint/suffix, redacted provider identifier hashes/suffixes, non-secret validated Price/endpoint facts, counts, UTC/version lineage, and pass/fail statuses; never serialize or print a secret or raw provider value.</action>
  <verify><automated>node scripts/verify-supabase-support.mjs --mode prelaunch-test --deploy --prepare-acceptance --evidence .planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md</automated></verify>
  <done>The complete input preflight passes before mutation; the sole target is fingerprinted, schema/functions and coherent test providers are ready, zero baseline is proven, and the interactive run is prepared with redacted evidence only.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Complete paid and unpaid GitHub OAuth journeys from isolated installations</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md</files>
  <action>The executor starts the acceptance driver and pauses at each real GitHub login. The human supplies only the blocking boolean readiness fields and browser authentication for the designated paid and distinct unpaid identities; the driver records only redacted outcomes and fixture handles. Never request or accept account credentials, identifiers, emails, tokens, or codes in chat, command arguments, or evidence.</action>
  <what-built>A waiting real-provider acceptance run with separate fresh stores for paid-install-1, paid-restore-2, unlimited-restore-3, and unpaid-restore-1.</what-built>
  <how-to-verify>
    1. In the first isolated browser/store, sign in with the designated paid GitHub identity, continue to the Stripe test Checkout, use a Stripe test payment, and wait for signed-webhook polling to verify and close the dialog.
    2. Restart that isolated installation and confirm persisted suppression while all review features remain unrestricted.
    3. In a second fresh store, choose Restore, sign in with the same paid GitHub identity, and wait for automatic verification without email or device management; repeat with the third fresh store.
    4. In the unpaid fresh store, choose Restore and sign in with a distinct GitHub identity; confirm the generic completion and unverified status reveal no supporter existence.
    5. Keep all four installation stores isolated and do not reuse browser profiles between paid and unpaid identities.
    6. Let the driver finish the hostile/replay/cancellation/delay checks and inventory every acceptance-created Auth/authority row as exact per-table counts plus SHA-256 stable handles.
    7. Resume with only the driver run ID and the statement `interactive matrix complete`; do not paste account names, emails, IDs, tokens, codes, signatures, or the project ref.
  </how-to-verify>
  <verify><automated>node scripts/verify-supabase-support.mjs --mode prelaunch-test --check-interactive-evidence .planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md --require-fixture-manifest</automated><human-check>Complete steps 1–7 with the two distinct identities and fresh isolated stores.</human-check></verify>
  <done>Paid, restore, unlimited-installation, and distinct-unpaid evidence plus the complete redacted cleanup manifest are recorded.</done>
  <resume-signal>Provide the redacted driver run ID and `interactive matrix complete`, or identify the failed step.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve the complete prelaunch evidence</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md</files>
  <action>Review and approve only the redacted evidence produced by Tasks 1–2; reject approval if any required preflight status, provider-mode/URL/Price/webhook fact, fixture count/handle, lineage fact, or secret-exclusion check is missing or failed. Do not request or add any raw operator input, account identity, credential, token, secret, or provider identifier.</action>
  <what-built>One fingerprinted sole-project acceptance record covering real OAuth, Stripe test fulfillment, restore, hostile paths, package safety, and every cleanup fixture.</what-built>
  <how-to-verify>
    1. Confirm mode, UTC times, commit, migration/function versions, project-ref SHA-256, and display suffix.
    2. Confirm the paid and distinct unpaid identities used isolated stores and produced the required outcomes.
    3. Confirm every Auth/authority fixture table has exact counts and stable handles, with no raw key or PII.
    4. Confirm signed webhook is sole authority, review remains unrestricted, and hostile paths fail closed.
    5. Confirm Stripe facts are test-mode only and are not designated for copying to live mode.
    6. Record explicit dated approval; block 02-08 if any item is absent.
  </how-to-verify>
  <verify><automated>node scripts/verify-supabase-support.mjs --mode prelaunch-test --check-evidence .planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md --require-approved --require-fixture-manifest</automated><human-check>Approve only after all six checks pass.</human-check></verify>
  <done>Approved prelaunch evidence unblocks only exact fixture cleanup and in-place promotion.</done>
  <resume-signal>Type `approved` or identify the exact missing observation.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Operator → sole project | Full target identity must be checked without disclosure. |
| GitHub/Stripe → Supabase | Real identity and test payment cross authenticated/signed boundaries. |
| Hosted rows → evidence | Cleanup proof must identify every fixture without exposing raw keys. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-07-WT | Tampering | deployment target | mitigate | SHA-256 the complete ref; suffix is display-only. |
| T-02-07-AE | Spoofing | interactive acceptance | mitigate | Two distinct real identities, isolated stores, signed provider observations. |
| T-02-07-FX | Tampering | fixture manifest | mitigate | Exact per-table counts and stable primary-key handles. |
| T-02-07-SL | Information Disclosure | evidence | mitigate | No complete ref, raw fixture key, secret, token, email, or profile data. |
| T-02-07-PI | Tampering | provider input preflight | mitigate | Require the complete named environment/checkpoint contract, canonical HTTPS/derived URL equality, sole-project fingerprint, test mode, fixed Price, and endpoint/signing-secret provenance before every hosted mutation; redact all evidence. |
| T-02-07-RP | Repudiation | approval | mitigate | Immutable run ID, UTC/version lineage, separate dated approval. |
</threat_model>

<verification>The complete named operator contract and read-only preflight pass before all hosted mutation; only then may one full-ref fingerprint, real interactive evidence, complete fixture inventory, and separate approval pass, with secrets excluded from output.</verification>
<success_criteria>
- D-14/D-17 establish one hosted project and one accepted prelaunch event.
- PAY-01..04, SUP-01..05, D-10-superseded REC-01/02, and REC-03 have real-provider proof.
- Every acceptance-created Auth/authority fixture is exactly enumerable for safe cleanup.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-07-SUMMARY.md`.</output>
