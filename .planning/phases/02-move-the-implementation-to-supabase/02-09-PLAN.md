---
phase: 02-move-the-implementation-to-supabase
plan: 09
type: execute
wave: 9
depends_on: [02-08]
files_modified:
  - services/support/migrations/001_init.sql
  - services/support/src/
  - render.yaml
autonomous: true
requirements: [PAY-04, REC-01, REC-02]
must_haves:
  truths:
    - "Deletion starts only after approved 02-08 fingerprinted zero-fixture production-live evidence (D-02, D-14, D-17)."
    - "Hosted Fastify runtime/config/schema/routes, direct-Postgres migration, Resend/email recovery implementation, and Render declaration are absent; obsolete hosted-service and recovery E2E test cleanup is owned by dependent 02-11."
    - "Supabase functions/schema, local loopback Fastify, machine-local verified state, and configured Support/Restore contracts remain; no fallback or dual-write exists (D-03, D-09, D-16, D-19)."
  artifacts:
    - path: "services/support/src/"
      provides: "Deleted legacy hosted application and routes"
    - path: "render.yaml"
      provides: "Deleted Render deployment"
  key_links:
    - from: "02-08-PROMOTION-EVIDENCE.md"
      to: "legacy deletion"
      via: "approved production-live, zero-authority, matching project_ref_sha256 gate"
      pattern: "project_ref_sha256"
  prohibitions:
    - "HIGH: Do not delete before the approved 02-08 gate or remove Cumpa's local Fastify server."
    - "Do not add an alias, archived copy, fallback provider, import, reconciliation, or dual write."
---

<objective>
Remove only the legacy hosted service runtime/schema/routes and Render declaration after the approved sole-project cutover; dependent 02-11 removes the obsolete hosted-service and recovery E2E tests.

Purpose: Complete the first scope-safe clean-deletion slice without touching toolchain/package/docs work.
Output: No legacy hosted implementation or provider declaration.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-08-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md
@scripts/verify-supabase-support.mjs
</context>

<tasks>
<task type="auto">
  <name>Task 1: Enforce the approved replacement-evidence gate</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md</files>
  <action>Run the promotion evidence checker before any deletion. Require explicit approval, `production-live`, zero Auth/authority counts after exact fixture cleanup, non-destructive smoke, and the same `project_ref_sha256` as 02-07. Stop on any failure; a suffix match is insufficient.</action>
  <verify><automated>node scripts/verify-supabase-support.mjs --mode production-live --check-promotion-evidence .planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md --require-approved --require-zero-authority</automated></verify>
  <done>The approved evidence gate passes before deletion.</done>
</task>
<task type="auto">
  <name>Task 2: Delete hosted runtime, schema, routes, and Render configuration</name>
  <files>services/support/migrations/001_init.sql, services/support/src/, render.yaml</files>
  <action>After Task 1, delete the legacy direct-Postgres migration, the complete hosted application/config/database/email/fulfillment/recovery/schema/server/routes source tree, and the Render declaration. Preserve the service package/toolchain files for 02-10, and leave obsolete hosted-service tests plus the obsolete email-recovery browser spec for dependency-ordered removal and absence proof in 02-11. Preserve Supabase code, local loopback Fastify, local support store, and completed hosted action/status/browser contracts. Make no compatibility or fallback replacement.</action>
  <verify><automated>test ! -e render.yaml &amp;&amp; test ! -e services/support/src &amp;&amp; test ! -e services/support/migrations</automated></verify>
  <done>The hosted implementation slice is absent while remaining toolchain and local Cumpa boundaries are intact.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Approved evidence → deletion | Irreversible deletion requires replacement proof. |
| Hosted Fastify → local Fastify | Only the hosted service is removed. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-09-EG | Tampering | evidence gate | mitigate | Require approved fingerprinted 02-08 evidence. |
| T-02-09-LF | Elevation of Privilege | fallback surface | mitigate | Delete hosted authority and routes without aliases. |
| T-02-09-RG | Denial of Service | local server | mitigate | Preserve local loopback Fastify and support store. |
</threat_model>
<verification>The evidence gate passes first, then every declared hosted runtime/schema/config path is absent; dependent 02-11 removes the obsolete tests and proves repository/generated/package absence after both deletion slices.</verification>
<success_criteria>
- D-02/D-08/D-10/D-19 remove hosted legacy source and email recovery.
- D-03/D-09/D-12 preserve the local boundary and verified state.
- D-16 leaves no import, fallback, or dual-write path.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-09-SUMMARY.md`.</output>
