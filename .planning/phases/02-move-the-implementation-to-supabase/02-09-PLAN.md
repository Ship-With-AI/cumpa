---
phase: 02-move-the-implementation-to-supabase
plan: 09
type: execute
wave: 9
depends_on: [02-08]
files_modified:
  - services/support/package-lock.json
  - services/support/package.json
  - services/support/scripts/migrate.mjs
  - services/support/scripts/test-postgres.mjs
  - services/support/tsconfig.json
  - services/support/vitest.config.ts
  - scripts/test-support-e2e-postgres.mjs
  - package.json
  - package-lock.json
autonomous: true
requirements: [PAY-04, REC-01, REC-02]
must_haves:
  truths:
    - "The final standalone-service configuration, migration/test tooling, workspace entry, root script, and service-only lock dependencies are removed after 02-07/02-08 delete implementation and tests."
    - "The published package contains no legacy service implementation, Stripe/webhook secret path, OAuth/profile/email content, or obsolete support endpoint while retaining Cumpa's local loopback Fastify dependencies (PAY-04)."
    - "The repository clean cutover is complete with no shim, alias, fallback, import, dual write, or reconciliation machinery (D-02, D-08, D-16, D-19)."
    - "External Render/Postgres/Resend resources and credentials remain unretired until 02-11 production proof."
  artifacts:
    - path: "services/support/package.json"
      provides: "Deleted standalone service package boundary"
    - path: "services/support/scripts/migrate.mjs"
      provides: "Deleted standalone database migration tooling"
    - path: "scripts/test-support-e2e-postgres.mjs"
      provides: "Deleted root standalone-Postgres test wrapper"
    - path: "package.json"
      provides: "Root scripts/workspaces/dependencies without legacy service callers"
    - path: "package-lock.json"
      provides: "Lock graph without service-only dependencies"
  key_links:
    - from: ".planning/phases/02-move-the-implementation-to-supabase/02-08-SUMMARY.md"
      to: "root manifest and lock cleanup"
      via: "confirms all service source/tests are already absent before package graph removal"
      pattern: "deletion inventory"
    - from: "package.json"
      to: "package-lock.json"
      via: "root dependency/workspace removal is reflected in the lock graph"
      pattern: "services/support"
  prohibitions:
    - "HIGH: Do not remove Fastify or dependencies used by Cumpa's local loopback server."
    - "No compatibility script, workspace alias, import/reconciliation job, or stale service-only lock dependency."
    - "Do not claim external provider retirement before 02-11 production proof."
---

<objective>
Complete repository clean cutover by deleting the standalone service toolchain and removing its root package graph.

Purpose: Finish the final nine-path deletion slice only after implementation, routes, and tests are gone, while preserving unrelated local Cumpa dependencies.
Output: Empty legacy service tree, clean root manifest/lock graph, and packed-package absence proof.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md
@.planning/phases/02-move-the-implementation-to-supabase/02-07-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-08-SUMMARY.md
@package.json
@package-lock.json
@tests/e2e/package-assets.spec.ts

<interfaces>
Repository cutover invariant after 02-08:
- Every tracked legacy service source, route, migration, and test file is absent.
- Root Fastify dependencies remain required by Cumpa's loopback server and must not be removed by name alone.
- Existing machine-local verified state remains accepted; no hosted import/reconciliation path exists.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete standalone service configuration and tooling</name>
  <files>services/support/package-lock.json, services/support/package.json, services/support/scripts/migrate.mjs, services/support/scripts/test-postgres.mjs, services/support/tsconfig.json, services/support/vitest.config.ts</files>
  <read_first>
    - .planning/phases/02-move-the-implementation-to-supabase/02-08-SUMMARY.md — confirms all legacy implementation/routes/tests are already absent
    - services/support/package.json — standalone package scripts and dependency boundary to remove
    - services/support/scripts/migrate.mjs — obsolete direct-Postgres migration entry point
    - services/support/vitest.config.ts — obsolete standalone test configuration
  </read_first>
  <action>Delete exactly the six service package/config/tooling files after 02-08 confirms no service implementation or tests remain. Remove the now-empty `services/support` directories as a consequence. Do not create replacement aliases, wrapper scripts, archived configs, or compatibility packages.</action>
  <acceptance_criteria>
    - All six listed service package/config/tooling files are absent.
    - No tracked file remains under `services/support`.
    - No archived or renamed compatibility package preserves the standalone service.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e services/support/package-lock.json &amp;&amp; test ! -e services/support/package.json &amp;&amp; test ! -e services/support/scripts/migrate.mjs &amp;&amp; test ! -e services/support/scripts/test-postgres.mjs &amp;&amp; test ! -e services/support/tsconfig.json &amp;&amp; test ! -e services/support/vitest.config.ts &amp;&amp; test ! -e services/support</automated>
  </verify>
  <done>The standalone service package/config/tooling boundary is gone; commit `refactor(02-09): remove legacy service toolchain`.</done>
</task>

<task type="auto">
  <name>Task 2: Remove root callers and service-only lock dependencies</name>
  <files>scripts/test-support-e2e-postgres.mjs, package.json, package-lock.json</files>
  <read_first>
    - scripts/test-support-e2e-postgres.mjs — obsolete standalone Postgres wrapper
    - package.json — root workspace, scripts, and dependencies requiring clean cutover
    - package-lock.json — lock graph to regenerate without service-only packages
    - tests/e2e/package-assets.spec.ts — packed-package absence contract retained from hosted acceptance
  </read_first>
  <action>Delete the standalone Postgres wrapper. Remove root workspace/script entries and dependencies reachable only from the deleted service, then refresh the root lock graph. Preserve Fastify and every dependency still used by Cumpa's local loopback server. Remove stale package/generated-asset references without adding compatibility commands, dual-write tooling, import/reconciliation jobs, or a second support client. Do not edit the operations runbook or claim external resource retirement; 02-10 owns workflow/runbook policy and 02-11 owns the live retirement observation.</action>
  <acceptance_criteria>
    - The root wrapper, workspace, scripts, and service-only lock dependencies are absent.
    - Cumpa's local loopback Fastify dependencies and behavior remain represented in the manifest and lockfile.
    - Packed assets contain no legacy service, provider secret, OAuth/profile/email, recovery-token, or obsolete endpoint content.
    - No provider/resource retirement is claimed.
  </acceptance_criteria>
  <verify>
    <automated>test ! -e scripts/test-support-e2e-postgres.mjs &amp;&amp; test ! -e services/support &amp;&amp; npx playwright test --config=tests tests/e2e/package-assets.spec.ts</automated>
  </verify>
  <done>The root package graph and packed application are free of the legacy service while local Fastify remains; commit `refactor(02-09): finish repository support cutover`.</done>
</task>

</tasks>

## Artifacts this phase produces

- Deleted: six exact standalone-service package/config/tooling files.
- Deleted/updated: root standalone-Postgres wrapper, manifest, and lock graph.
- Verified: packed package has no legacy service/provider-secret/profile/email/recovery-token surface.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Deleted service tree → root package graph | Service-only workspaces, scripts, and dependencies disappear without harming local Cumpa. |
| Root package graph → published package | Packed assets must exclude all legacy hosted and secret-bearing surfaces. |
| Repository cutover → external providers | Clean source does not authorize provider retirement before actual production proof. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-09-LF | Elevation/Tampering | HIGH | package graph | mitigate | Remove service-only workspace/scripts/dependencies and prohibit compatibility aliases or wrappers. |
| T-02-09-SL | Information Disclosure | HIGH | published package | mitigate | Execute packed-assets acceptance against legacy code, secrets, profile/email, and endpoint surfaces. |
| T-02-09-RG | Denial of Service | HIGH | local loopback server | mitigate | Preserve Fastify and dependencies with live Cumpa callers rather than deleting by package name. |
| T-02-09-RP | Repudiation | MEDIUM | external retirement | mitigate | Record no provider-retirement claim; 02-11 owns dated retirement evidence after production proof. |

Execution MUST stop if any HIGH threat remains unresolved or predecessor deletion summaries are missing. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
The service tree and root wrapper are absent, root package/lock state has no service-only graph, and the retained package-assets acceptance proves no legacy or secret-bearing content ships.
</verification>

<success_criteria>
- D-02/D-19 complete repository cutover without fallback or compatibility surfaces.
- D-16 leaves no import, reconciliation, or dual-write machinery.
- PAY-04 preserves published-package secret safety.
- External provider retirement remains blocked until 02-11.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-09-SUMMARY.md` with the service-toolchain/root-package deletion inventory and packed-assets result.
</output>
