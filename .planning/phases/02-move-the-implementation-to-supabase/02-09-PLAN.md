---
phase: 02-move-the-implementation-to-supabase
plan: 09
type: execute
wave: 9
depends_on: [02-08]
files_modified:
  - scripts/verify-supabase-support.mjs
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/support-restore.spec.ts
  - .planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md
  - .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-MARKER.json
  - .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
user_setup:
  - service: github
    why: "The one-time D-17 acceptance requires a designated paid GitHub identity and a distinct unpaid identity in separate browser profiles."
    dashboard_config:
      - task: "Use separate browser profiles for the paid and unpaid identities; supply only browser authentication, never credentials, identifiers, email, tokens, codes, or profile data to the executor, chat, or evidence."
        location: "Human-controlled isolated browser profiles"
  - service: stripe
    why: "The paid path uses Stripe's test Checkout after GitHub authentication."
    dashboard_config:
      - task: "Complete the one-time test-mode Checkout in the browser with Stripe test data; do not disclose it or use a live payment."
        location: "Stripe-hosted test Checkout opened by the accepted Support flow"
must_haves:
  truths:
    - "Plan 02-08 completes the corrective source/test/doc cutover and protected setup without a push; this plan's first task owns the first ordinary main push and proves its exact immutable canonical-origin deployment before browser journeys begin."
    - "Only an isolated acceptance runtime receives the canonical public origin; ordinary local development remains hosted-free and no deployment/provider credential or bare management ref enters the local environment (D-13, D-18, D-20)."
    - "Acceptance preserves hosted-only OAuth, unrestricted review, GitHub-only identity, authentication before Checkout, automatic paid restore, no email fallback, privacy-safe local persistence, OAuth requirement cutover, unlimited installation restore, and unchanged PAY/SUP behavior (D-03, D-04, D-05, D-06, D-07, D-08, D-09, D-10, D-11, D-12)."
    - "Browser-only journeys prove GitHub OAuth before test Checkout, signed-webhook polling close, restart persistence, repeated paid restore, distinct unpaid generic completion, cancellation/delay, and unrestricted review."
    - "Every hostile, expired/reused-intent, replay, and concurrency case runs only in the next automatic protected-main job; no human or local provider control is introduced."
    - "The automatic driver full-ref-guards every case-owned hosted mutation and emits immutable results plus one exact manifest owning every browser- and driver-created row in Auth and all five authority tables."
    - "Acceptance evidence contains the exact canonical origin only in typed origin/route fields and rejects bare ref leakage, another Supabase host, credentials, OAuth/PII, provider secrets, and raw fixture identifiers."
  artifacts:
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md"
      provides: "Immutable first corrected prelaunch deployment, canonical route signatures, and zero baseline"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-MARKER.json"
      provides: "Versioned, redacted interactive-complete trigger at the exact path consumed by the protected workflow"
    - path: "scripts/verify-supabase-support.mjs"
      provides: "Real nine-case protected hostile executor and post-execution six-table manifest capture"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md"
      provides: "Approved one-time real-provider matrix, canonical-origin lineage, and exact redacted fixture manifest"
  key_links:
    - from: "02-08-TEST-DEPLOYMENT-EVIDENCE.md"
      to: "human acceptance matrix"
      via: "immutable successful prelaunch-test run, exact canonical public origin, and supplemental internally derived fingerprint correlation"
      pattern: "prelaunch-test"
    - from: "acceptance-complete main push"
      to: "hostile matrix and fixture manifest"
      via: "automatic protected-environment driver executes every case, then snapshots all browser/driver rows"
      pattern: "hostile_matrix"
    - from: "signed Stripe webhook"
      to: "local verified state"
      via: "hosted authority then existing status polling and monotonic local promotion"
      pattern: "signed_webhook"
  prohibitions:
    - "HIGH: No acceptance before 02-08 success, live Stripe payment, local privileged query, local deploy, manual workflow dispatch, or raw fixture/provider value."
    - "HIGH: Do not reuse paid/unpaid browser profiles or infer supporter ownership from email."
    - "Do not clean fixtures, promote providers, delete legacy source, repeat this matrix after live promotion, or treat the canonical public origin as a credential."
---

<objective>
Perform the first corrected automatic prelaunch deployment, run and approve the one-time OAuth/payment/restore acceptance matrix, then capture the automatic hostile matrix and exact cleanup manifest.

Purpose: Prove the corrected D-15/D-20 deployment and every PAY/SUP plus D-10 OAuth REC behavior against the still-empty sole project while preserving D-18 lineage and redaction.
Output: Immutable first-deployment evidence, human observations, exact workflow marker, and immutable redacted hostile/fixture evidence.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-08-SUMMARY.md
@scripts/verify-supabase-support.mjs
@tests/integration/support-dialog.spec.ts
</context>

<interfaces>
- Deployment gate: `node scripts/verify-supabase-support.mjs --check-run-evidence .planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md --expected-mode prelaunch-test --require-immutable-run --require-zero-authority`.
- Acceptance validator: `node scripts/verify-supabase-support.mjs --check-acceptance-evidence PATH --deployment DEPLOYMENT --require-hostile-matrix --require-fixture-manifest --require-immutable-run`.
- Evidence uses `public_origin: https://<project-ref>.supabase.co`; route fields may use only the Auth and three function paths defined in 02-08. The ref is forbidden as a standalone evidence value or prose token.
- Marker path and schema: `.planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-MARKER.json` contains version `1`, kind `acceptance-marker`, `acceptance_marker.status: interactive-matrix-complete`, the exact canonical origin, immutable GitHub run/commit bindings, an optional internally derived correlation fingerprint, and digests binding the 02-08 run plus redacted browser observations. The existing protected workflow consumes this exact path.
- Marker validator: `node scripts/verify-supabase-support.mjs --check-acceptance-marker .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-MARKER.json --deployment .planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md --expected-mode prelaunch-test` is read-only and must pass before the marker is committed.
- Machine merge: `node scripts/verify-supabase-support.mjs --merge-acceptance-evidence OBSERVATIONS --deployment-run RUN --marker .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-MARKER.json --output .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md` is the only acceptance writer after the workflow run; it validates all bindings and atomically merges `hostile_matrix` plus post-run `fixture_manifest`.
</interfaces>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Push the first corrected deployment, prove zero, and complete the browser matrix</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md, .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-08-SUMMARY.md`
    - `.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md` D-03 through D-20
    - `.github/workflows/deploy-supabase-production.yml`
    - `scripts/verify-supabase-support.mjs`
    - `tests/integration/support-dialog.spec.ts`
  </read_first>
  <action>Confirm 02-08 completed both corrective tasks and protected setup without committing or pushing. Commit that correction and push current protected main normally; do not dispatch, deploy locally, or alter provider inputs during the run. Bind `02-08-TEST-DEPLOYMENT-EVIDENCE.md` to the pushed commit and exact automatic prelaunch-test artifact. Require repository gates before environment access; an immediate canonical 20-character complete-ref shape guard before every mutation; schema before all functions; exact derived canonical Auth/function/webhook routes; fixed text/plain completion/invalid/unavailable signatures; and zero counts for `auth.users` plus all five authority tables before and after non-mutating probes. Run the deployment gate in `<interfaces>` and stop unless the record is immutable, successful, zero-baseline, canonical-route-complete, and exact-origin-safe. Then launch Cumpa with fresh isolated stores and only the record's canonical `public_origin` as `CUMPA_SUPPORT_SERVICE_URL`; deployment/provider credentials and bare management ref stay absent. Keep paid and unpaid profiles separate. The human performs browser journeys only: Support with the paid GitHub identity and test Checkout; observe signed-webhook-only polling close and restart suppression; Restore two additional installations with the paid identity; Restore with a distinct unpaid identity; and exercise cancellation/delay while review remains unrestricted. Record only redacted observations in `02-09-ACCEPTANCE-EVIDENCE.md`; any derived ref fingerprint is supplemental correlation, never target authority.</action>
  <what-built>The first corrected automatic deployment is immutable, schema-first, target-guarded, zero-authority, and reachable at the canonical origin; the redacted acceptance observation record is ready for human verification.</what-built>
  <how-to-verify>
    1. Paid fresh install: GitHub first, test Checkout second, signed webhook and polling only, automatic dialog close.
    2. Restart: persisted verified status suppresses the prompt while every review feature remains available.
    3. Two further fresh installs: Restore with the same paid identity verifies both without email or device management.
    4. Distinct unpaid install/profile: fixed generic completion text, unverified status, and no supporter enumeration.
    5. Cancellation/delay never verifies or gates review.
    6. Evidence includes only the canonical public origin/derived routes and contains no bare ref, other Supabase host, credential, identity, email, token, code, provider secret, payment data, or raw fixture key.
  </how-to-verify>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --check-run-evidence .planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md --expected-mode prelaunch-test --require-immutable-run --require-zero-authority</automated>
    <human-check>Complete steps 1–6 with isolated identities and stores.</human-check>
  </verify>
  <done>The first corrected deployment and one-time human matrix are complete; the redacted observation record is ready for the automatic hostile run.</done>
  <resume-signal>Type `interactive matrix complete` without account/provider detail, or identify the failed numbered check.</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the real protected hostile run, push its marker, and merge the complete manifest</name>
  <files>scripts/verify-supabase-support.mjs, tests/e2e/support-payment.spec.ts, tests/e2e/support-restore.spec.ts, .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-MARKER.json, .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md`
    - `.planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md`
    - `.github/workflows/deploy-supabase-production.yml`
    - `scripts/verify-supabase-support.mjs` `HOSTILE_CASES`, `runHostileExecutor`, `snapshotAuthority`, and acceptance validation
    - `tests/e2e/support-payment.spec.ts`
    - `tests/e2e/support-restore.spec.ts`
  </read_first>
  <behavior>
    - The exact marker path and schema in `<interfaces>` bind only the approved 02-08 deployment and Task 1 observations; malformed, moved, mutable, cross-origin, cross-run/commit, inconsistent derived-correlation, or raw-value markers fail before any hosted action.
    - The exact read-only `--check-acceptance-marker` command in `<interfaces>` passes locally before the marker is committed; the workflow consumes no unchecked marker.
    - The protected executor runs exactly `wrong-signature`, `wrong-product`, `wrong-amount`, `wrong-currency`, `wrong-binding`, `expired-intent`, `reused-intent`, `sequential-replay`, and `concurrent-replay-settlement`; no placeholder/hash-only substitute passes.
    - Each case creates distinct provider-valid fixtures as needed, verifies its named rejection/replay/concurrency invariant against before/after authority, and immediately full-ref-guards every case-owned hosted mutation.
    - After all browser and automatic cases, the protected executor inventories `auth.users` plus all five authority tables, hashes raw keys only in memory, and emits one sorted exact six-table manifest.
    - The downloaded immutable workflow result is machine-merged with the redacted browser observations into `02-09-ACCEPTANCE-EVIDENCE.md`; missing cases, partial manifests, duplicate handles, or cross-run lineage fail.
  </behavior>
  <action>Write focused failing verifier tests first in the existing payment and restore suites, including the exact machine-merge CLI from `<interfaces>`, then replace the current synthetic hostile executor with real protected test-provider interactions and authority checks. Preserve the existing `HOSTILE_CASES` order exactly: `wrong-signature`, `wrong-product`, `wrong-amount`, `wrong-currency`, `wrong-binding`, `expired-intent`, `reused-intent`, `sequential-replay`, and `concurrent-replay-settlement`. Each case must use distinct fixtures, exercise the real signed-webhook/intent/RPC boundary appropriate to the ID, compare exact before/after authority, and validate the complete ref's canonical 20-character shape through `guardTarget(inputs)` immediately before every hosted mutation. Move final `snapshotAuthority` after the hostile executor and merge its case-owned handles with every browser-created row so the immutable deployment artifact contains the authoritative six-table `fixture_manifest`; no Date/hash-only fabricated case result is acceptable. After Task 1, write the exact marker file from `<interfaces>` with only redacted bindings, validate it locally, commit the verifier/tests/marker/observation record, and push main normally. The existing workflow must consume that exact marker path and run the protected executor automatically. Download the immutable workflow artifact and invoke the implemented machine-merge CLI; it must verify exact canonical origin, immutable GitHub run/commit, routes, and marker lineage against 02-08, treating any internally derived fingerprint consistency as supplemental correlation; reject mutable/partial/duplicate inputs; and atomically write `02-09-ACCEPTANCE-EVIDENCE.md` with the automatic `hostile_matrix` plus post-run `fixture_manifest`. Raw keys stay runner-memory-only and no deployed acceptance backdoor remains.</action>
  <verify>
    <automated>npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/support-restore.spec.ts &amp;&amp; node scripts/verify-supabase-support.mjs --check-acceptance-marker .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-MARKER.json --deployment .planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md --expected-mode prelaunch-test &amp;&amp; node scripts/verify-supabase-support.mjs --check-acceptance-evidence .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md --deployment .planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md --require-hostile-matrix --require-fixture-manifest --require-immutable-run</automated>
  </verify>
  <done>The exact workflow marker triggers nine real protected cases, and immutable acceptance evidence owns the automatically merged complete browser-plus-driver cleanup manifest.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve acceptance, canonical lineage, and cleanup manifest</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md</files>
  <what-built>One browser acceptance record, one immutable CI hostile matrix, and one exact hashed cleanup manifest tied to 02-08.</what-built>
  <how-to-verify>
    1. Confirm the immutable 02-08 deployment preceded every browser journey and uses the exact canonical public origin and immutable GitHub run/commit; any fingerprint is internally derived supplemental correlation only.
    2. Confirm paid, restore, unpaid, persistence, polling, cancellation, fixed text/plain completion, and unrestricted-review outcomes.
    3. Confirm all nine hostile/replay/concurrency cases ran in the automatic protected job and passed exact invariants.
    4. Confirm all six tables have counts and hashed handles covering browser plus driver fixtures with no raw values.
    5. Confirm evidence permits the exact canonical origin/derived routes but excludes the bare ref, other Supabase hosts, credentials, OAuth/PII, provider secrets, and raw IDs.
    6. Confirm approval grants only exact in-workflow cleanup, not manual/local deployment or broad deletion.
  </how-to-verify>
  <action>Review only the redacted facts from checks 1–6. Reject any missing/failed fact, cross-record canonical-origin or immutable run/commit mismatch, inconsistent derived correlation fingerprint, noncanonical route, non-text/plain browser signature, raw value, or waived case. A dated approval tied to immutable run/artifact IDs authorizes only Plan 02-10's exact manifest cleanup and in-place promotion of the same project.</action>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --check-acceptance-evidence .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md --deployment .planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md --require-approved --require-hostile-matrix --require-fixture-manifest --require-immutable-run</automated>
    <human-check>Approve only after checks 1–6 pass.</human-check>
  </verify>
  <done>Approved acceptance evidence safely unblocks exact fixture cleanup and same-project live promotion.</done>
  <resume-signal>Type `approved` or identify the exact missing redacted fact.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Immutable deployment → acceptance | Human testing must use the exact canonical origin and immutable GitHub run/commit deployed in 02-08; a derived fingerprint is supplemental correlation only. |
| Human identities/providers → local runtime | Only browser interaction and the public capability origin may cross locally. |
| Hosted fixtures → workflow evidence | Raw authority keys must become non-reversible handles before leaving the protected runner. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-09-AE | Spoofing | acceptance identities | mitigate | Distinct real profiles and isolated stores; no email ownership. |
| T-02-09-PA | Elevation of Privilege | payment authority | mitigate | Signed webhook only; redirects and local claims never verify. |
| T-02-09-FX | Tampering | fixture manifest | mitigate | Protected runner inventories all six tables and emits sorted SHA-256 handles/counts. |
| T-02-09-OR | Spoofing | public route lineage | mitigate | Exact 02-08 `public_origin` and fixed path comparison; other hosts fail. |
| T-02-09-SL | Information Disclosure | local/evidence | mitigate | Exact-origin allowance with bare-ref, secret, OAuth/PII, provider, and raw-ID denial. |
| T-02-09-HM | Tampering | CI hostile driver | mitigate | Protected prelaunch-only execution, per-mutation guards, exact case IDs, and immutable results. |
</threat_model>

<verification>The immutable canonical deployment validates before acceptance; the human matrix passes; the next automatic main-push run emits the exact redacted fixture manifest; separate approval passes.</verification>
<success_criteria>
- PAY-01..04, SUP-01..05, D-10 REC-01/02, and REC-03 have real test-mode proof.
- D-13 keeps ordinary local development hosted-free; D-14/D-17 use the sole project exactly once for destructive acceptance.
- D-18/D-20 permit only canonical public routing and exclude identity, credentials, provider material, bare refs, and raw fixtures.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-09-SUMMARY.md` when complete.</output>
