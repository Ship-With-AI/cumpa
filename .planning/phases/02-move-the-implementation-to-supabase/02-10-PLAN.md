---
phase: 02-move-the-implementation-to-supabase
plan: 10
type: execute
wave: 10
depends_on: [02-09]
files_modified:
  - .github/workflows/deploy-supabase-production.yml
  - docs/support-service-operations.md
  - scripts/verify-supabase-support.mjs
  - .planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md
autonomous: true
requirements: [PAY-03, PAY-04, REC-01, REC-02, REC-03]
user_setup:
  - service: supabase
    why: "Production is the second and only other hosted Supabase project (D-14)."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Protected GitHub production environment secret"
      - name: SUPABASE_PRODUCTION_PROJECT_REF
        source: "Production Supabase project reference; protected GitHub environment secret/variable"
      - name: SUPABASE_PRODUCTION_DB_PASSWORD
        source: "Production database password; protected GitHub environment secret"
    dashboard_config:
      - task: "Enable GitHub only, disable email/magic-link, and configure exact production Auth/site/callback URLs"
        location: "Production Supabase → Authentication"
  - service: github
    why: "Distinct production OAuth and protected CI environment are required for production callbacks/deployment."
    env_vars:
      - name: GITHUB_OAUTH_CLIENT_ID
        source: "Production GitHub OAuth App; configure only in production Supabase Auth"
      - name: GITHUB_OAUTH_CLIENT_SECRET
        source: "Production GitHub OAuth App; configure only in production Supabase Auth"
    dashboard_config:
      - task: "Create/confirm production OAuth App with exact production Supabase Auth callback and no repository scopes"
        location: "GitHub Developer settings → OAuth Apps"
      - task: "Protect the production GitHub environment and restrict deploy workflow secrets to main"
        location: "GitHub repository Settings → Environments"
  - service: stripe
    why: "Production Checkout and webhook authority require live server-only configuration."
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe live secret key; production Supabase function secret only"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Production Supabase webhook endpoint live signing secret"
      - name: STRIPE_PRICE_ID
        source: "One live one-time USD $49.99 Price"
    dashboard_config:
      - task: "Create/confirm the fixed live Price and production webhook endpoint/events; Plan 02-11 records availability"
        location: "Stripe live mode"
must_haves:
  truths:
    - "Every push/merge to main automatically applies production migrations before deploying all Edge Functions from the same commit through the protected production environment (D-15)."
    - "Wrong target, missing secret, schema failure, Auth drift, or smoke failure stops deployment before later authority steps."
    - "The workflow and runbook never expose privileged Supabase, Stripe, GitHub, OAuth, installation, or payment values (PAY-04)."
    - "Production configuration declares GitHub-only support OAuth and server-owned signed-webhook authority, with no email recovery or manual promotion path (D-05, D-08, D-15, PAY-03, REC-01, REC-02)."
    - "The evidence template requires migration/function commit identity and unlimited-restore production observations without adding device management (REC-03)."
  artifacts:
    - path: ".github/workflows/deploy-supabase-production.yml"
      provides: "Protected main-triggered schema-before-functions production deployment"
    - path: "docs/support-service-operations.md"
      provides: "Final Supabase-only production, drift, incident, retention, and forward-fix runbook"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md"
      provides: "Redacted production CI/schema/function/Auth/smoke/package evidence template"
  key_links:
    - from: ".github/workflows/deploy-supabase-production.yml"
      to: "supabase/migrations and supabase/functions"
      via: "main push, protected production environment, db push before function deploy"
      pattern: "branches:.*main|db push|functions deploy"
    - from: "supabase/config.toml"
      to: "production Auth/function configuration evidence"
      via: "versioned supported settings plus explicit dashboard drift comparison"
      pattern: "verify_jwt|GitHub|callback"
    - from: "production workflow"
      to: ".planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md"
      via: "run URL, commit, migration/function/smoke fields consumed by 02-11"
      pattern: "20260814000000"
  prohibitions:
    - "HIGH: Do not deploy functions before migrations, continue after schema failure, target development, or print protected values."
    - "HIGH: Do not represent dashboard configuration as complete without redacted expected-versus-observed drift evidence."
    - "No destructive rollback, data import, provider dual-run, manual promotion as normal path, Supabase Git integration, or email/operator recovery."
---

<objective>
Install the protected main-triggered production deployment contract and its evidence/runbook policy after the repository becomes Supabase-only.

Purpose: Isolate the second irreversible cutover—production automation—from legacy deletion and live human approval.
Output: One production workflow, finalized operations policy, and a redacted evidence template ready for the actual main run.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-09-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md
@supabase/config.toml
@supabase/migrations/20260814000000_support_authority.sql
@docs/support-service-operations.md

<interfaces>
Production workflow contract:
- Trigger `push` to `main` for `supabase/**`, workflow, or relevant lock/tooling changes; optional dispatch may rerun the exact main commit but is not manual promotion.
- Permissions `contents: read`; protected `production` environment; serialized production deploys without cancelling an in-flight migration.
- Exact lockfile-pinned Supabase CLI; production ref is non-empty and unequal to the documented development ref.
- Ordered link → database push → migration/grant/RLS check → deploy `support-api` → `support-flow` → `stripe-webhook` → function list/non-mutating smoke.
- Schema/config/smoke failure blocks subsequent steps; recovery uses a forward-fix migration.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install the fail-closed production workflow</name>
  <files>.github/workflows/deploy-supabase-production.yml, scripts/verify-supabase-support.mjs</files>
  <read_first>
    - supabase/config.toml — function names/JWT declarations and versioned settings
    - supabase/migrations/20260814000000_support_authority.sql — schema that must precede functions
    - package.json — exact pinned Supabase CLI command
    - scripts/verify-supabase-support.mjs — existing hosted verifier to extend with the workflow-validation mode used below
    - .planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md — development refs/versions baseline
  </read_first>
  <action>Create the workflow exactly as `<interfaces>` specifies with protected environment secrets and minimal permissions. In `scripts/verify-supabase-support.mjs`, explicitly add the `--verify-production-workflow &lt;path&gt;` mode consumed by this task: parse the named workflow and fail unless the main trigger, protected production environment, serialized job, pinned CLI, target guard, schema push plus migration/RLS/grant inspection, same-commit function deploys, and smoke/package evidence steps occur in fail-closed order. Reject missing, reordered, development-targeted, manually substituted, or secret-printing steps. Mask provider values and record only safe ref suffixes, commit, migration/function versions, and run URL. Keep database push and inspection as blocking predecessors to function deployment; use no Supabase Git integration or dashboard schema edit path.</action>
  <acceptance_criteria>
    - Main changes automatically invoke one serialized protected production job using pinned tooling.
    - Schema apply/inspection completes before any function deploy and every failure halts later steps.
    - Target confusion and credential/log leakage have explicit fail-closed controls.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --verify-production-workflow .github/workflows/deploy-supabase-production.yml</automated>
  </verify>
  <done>The production workflow and its explicitly implemented structural verifier are reproducible, protected, schema-first, and fail-closed; commit `ci(02-10): deploy Supabase authority from main`.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize production policy and evidence contract</name>
  <files>docs/support-service-operations.md, .planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md</files>
  <read_first>
    - docs/support-service-operations.md — Supabase-only development and retirement baseline
    - .github/workflows/deploy-supabase-production.yml — exact trigger/order/evidence producer
    - .planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md — resolved Auth drift and Stripe identifier-retention ownership
  </read_first>
  <action>Finalize the runbook with workflow trigger/protection, schema-first failure behavior, live GitHub/Stripe configuration, secret rotation, provider outage, incident correlation, forward-fix recovery, and D-18 identifier retention/deletion review. For Supabase Auth, list every supported versioned non-secret setting and every unavoidable dashboard-only GitHub setting with expected value and redacted drift-check evidence for development and production. Create the production evidence template requiring main run URL/commit, safe project ref suffix, migration/RLS/grant observations, function versions, Auth expected-versus-observed comparison, anonymous smoke, package scan, live Price/account/webhook provisioning, OAuth/Checkout/restore observations, provider retirement, and dated approval. Never include credentials, PII, full installation/intent tokens, or webhook signatures.</action>
  <acceptance_criteria>
    - Runbook resolves Auth settings ownership and D-18 identifier retention policy explicitly.
    - Evidence template makes live Stripe provisioning and Auth drift observable gates, not assumptions.
    - Production and development remain the only hosted projects and no manual promotion path is documented.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --verify-production-workflow .github/workflows/deploy-supabase-production.yml</automated>
  </verify>
  <done>Production operations and evidence requirements are executable and ready for the actual main-triggered run; commit `docs(02-10): finalize Supabase production operations`.</done>
</task>

</tasks>

## Artifacts this phase produces

- `.github/workflows/deploy-supabase-production.yml`: protected main production workflow with schema-before-functions ordering.
- `docs/support-service-operations.md`: production deployment, Auth drift, retention, incident, and forward-fix policy.
- `.planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md`: live-run evidence template consumed by 02-11.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub main/protected environment → production Supabase | CI commit, credentials, target, and ordering control production authority. |
| Migration → deployed functions | Functions must never address absent schema/RPCs. |
| Dashboard provider configuration → versioned expectation | Untracked Auth/Stripe drift can subvert locked behavior. |
| CI logs/evidence → repository | Production proof must remain useful without exposing secrets or PII. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-10-CI | Tampering/Elevation | HIGH | production workflow | mitigate | Main-only protected environment, minimal permission, pinned CLI, serialized target-guarded steps. |
| T-02-10-TG | Spoofing | HIGH | production target | mitigate | Compare protected production ref with development ref and capture safe target/run identity. |
| T-02-10-SD | Tampering | HIGH | schema/function order | mitigate | Blocking database apply plus migration/RLS/grant inspection precede unreachable-on-failure function deploys. |
| T-02-10-DR | Tampering | HIGH | Auth/Stripe dashboard drift | mitigate | Version supported config and require expected-versus-observed redacted evidence for dashboard-only settings. |
| T-02-10-SL | Information Disclosure | HIGH | workflow/logs/evidence | mitigate | Protected secrets, masking, redacted identifiers, package scan, no auth/payment/PII values. |
| T-02-10-DO | Denial of Service | MEDIUM | migration/deploy | mitigate | Serialized non-cancelling runs, fail-fast schema gate, forward-only recovery; local review remains available. |

Execution MUST stop if any HIGH threat remains unresolved. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
The workflow validator proves the source contract only. Plan 02-11 must observe the actual main-triggered run; this plan makes no live-deployment claim.
</verification>

<success_criteria>
- D-14/D-15 define exactly two projects and automatic protected main deployment.
- PAY-03 production webhook authority deploys only after its database authority.
- PAY-04 secrets remain protected from workflow output and package assets.
- REC-01/REC-02 GitHub-only configuration is versioned or drift-evidenced; REC-03 acceptance remains explicit.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-10-SUMMARY.md` with workflow validation and finalized evidence/runbook policy.
</output>
