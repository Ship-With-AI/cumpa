---
phase: 02-move-the-implementation-to-supabase
plan: 10
type: execute
wave: 10
depends_on: [02-09]
files_modified:
  - services/support/package-lock.json
  - services/support/package.json
  - services/support/scripts/migrate.mjs
  - services/support/scripts/test-postgres.mjs
  - services/support/tsconfig.json
  - services/support/vitest.config.ts
  - scripts/test-support-e2e-postgres.mjs
  - package.json
  - package-lock.json
autonomous: true
requirements: [PAY-04, REC-01, REC-02]
must_haves:
  truths:
    - "02-09 evidence-gated hosted-source deletion is complete before toolchain removal."
    - "The legacy service package, migration/test scripts, TypeScript/Vitest config, root wrapper, workspace/scripts, and now-unused hosted dependencies are absent."
    - "Fastify and every dependency still used by Cumpa's 127.0.0.1 local server remain; Supabase tooling used by the active implementation remains."
  artifacts:
    - path: "services/support/"
      provides: "Fully deleted legacy service workspace"
    - path: "package.json"
      provides: "Root scripts/workspace/dependencies without legacy hosted service"
    - path: "package-lock.json"
      provides: "Regenerated active-only dependency graph"
  key_links:
    - from: "02-09-SUMMARY.md"
      to: "toolchain deletion"
      via: "prior sequential deletion proof"
      pattern: "02-09"
  prohibitions:
    - "HIGH: Do not remove Fastify or dependencies with active local-Cumpa callers."
    - "Do not retain disabled scripts/workspaces as compatibility aliases or add fallback/dual-write tooling."
---

<objective>
Remove the legacy service toolchain/workspace and root dependency graph entries after its source is gone.

Purpose: Complete the second scope-safe deletion slice without mixing documentation, packaging, or production deployment.
Output: No service workspace/toolchain and an active-only root package graph.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-09-SUMMARY.md
@package.json
@package-lock.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Delete the standalone service toolchain</name>
  <files>services/support/package-lock.json, services/support/package.json, services/support/scripts/migrate.mjs, services/support/scripts/test-postgres.mjs, services/support/tsconfig.json, services/support/vitest.config.ts, scripts/test-support-e2e-postgres.mjs</files>
  <action>Require the 02-09 summary and absence checks, then delete the remaining service manifest/lock, migration and PostgreSQL test runners, service TypeScript/Vitest configuration, and root standalone-Postgres E2E wrapper. Remove the empty service directory. Do not touch active Supabase migrations/functions/tooling or local Cumpa server code.</action>
  <verify><automated>test ! -e services/support &amp;&amp; test ! -e scripts/test-support-e2e-postgres.mjs</automated></verify>
  <done>No standalone service workspace, script, or toolchain file remains.</done>
</task>
<task type="auto">
  <name>Task 2: Remove root workspace, scripts, and unused dependencies</name>
  <files>package.json, package-lock.json</files>
  <action>Remove only root workspace entries, npm scripts, and dependencies whose callers were deleted in 02-09/Task 1, then regenerate the npm lockfile using the existing package-manager workflow. Check live callers before removing each shared dependency; retain Fastify for the local loopback server and retain the approved Supabase CLI/tooling used by migrations and deployment. Add no replacement dependency or compatibility script.</action>
  <verify><automated>npm install --package-lock-only --ignore-scripts &amp;&amp; npm ls --all</automated></verify>
  <done>The root manifest and lock describe only active Cumpa/Supabase code and contain no legacy service workspace/script/dependency.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Deleted service → root graph | Removed callers must not leave runnable tooling or dependencies. |
| Hosted Fastify → local Fastify | Shared package removal must preserve the local server. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-10-LF | Elevation of Privilege | legacy tooling | mitigate | Delete workspace/scripts rather than disabling aliases. |
| T-02-10-RG | Denial of Service | root dependencies | mitigate | Keep packages with active local callers. |
| T-02-10-SC | Tampering | lockfile | mitigate | Regenerate with existing npm and run `npm ls`. |
</threat_model>
<verification>The legacy workspace is absent and the regenerated root graph retains all active local/Supabase dependencies.</verification>
<success_criteria>
- D-02/D-08/D-10/D-19 remove legacy recovery/service tooling cleanly.
- D-03/D-13 preserve ordinary local review and loopback Fastify.
- PAY-04 removes obsolete secret-bearing service dependencies and scripts.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-10-SUMMARY.md`.</output>
