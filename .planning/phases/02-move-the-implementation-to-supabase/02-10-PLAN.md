---
phase: 02-move-the-implementation-to-supabase
plan: 10
type: execute
wave: 10
depends_on: [02-09]
files_modified:
  - .github/workflows/deploy-supabase-production.yml
  - scripts/verify-supabase-support.mjs
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/support-restore.spec.ts
  - .planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md
autonomous: false
requirements: [PAY-01, PAY-03, PAY-04, REC-01, REC-02, REC-03]
user_setup:
  - service: github
    why: "Only after automatic exact cleanup and zero proof, replace the same protected production environment's temporary provider set with the complete live set per D-14/D-15."
    dashboard_config:
      - task: "Replace, do not add alongside, SUPABASE_GITHUB_CLIENT_ID, SUPABASE_GITHUB_CLIENT_SECRET, STRIPE_SECRET_KEY, STRIPE_PRICE_ID, STRIPE_WEBHOOK_ENDPOINT_ID, and STRIPE_WEBHOOK_SECRET; retain the same protected SUPABASE_PROJECT_REF variable and deployment credentials, and set SUPPORT_PROVIDER_MODE to production-live."
        location: "GitHub repository Settings -> Environments -> production"
  - service: github-oauth
    why: "The release OAuth app uses the same sole-project Supabase Auth callback as prelaunch."
    dashboard_config:
      - task: "Configure exactly https://<project-ref>.supabase.co/auth/v1/callback with no repository scopes; store only the replacement client ID and secret in production."
        location: "GitHub Settings -> Developer settings -> OAuth Apps"
  - service: stripe
    why: "The complete Stripe test set is replaced by one coherent live key, USD $49.99 Price, endpoint, and signing secret."
    dashboard_config:
      - task: "With test mode off, configure one endpoint at https://<project-ref>.supabase.co/functions/v1/stripe-webhook subscribed only to checkout.session.completed and checkout.session.async_payment_succeeded; replace all four protected Stripe inputs and retain no test identifier or value."
        location: "Stripe Dashboard live mode -> Product catalog and Developers -> Webhooks"
must_haves:
  truths:
    - "02-09 is explicitly approved with a complete immutable six-table fixture manifest and canonical-origin lineage before cleanup."
    - "A first ordinary main push, still in prelaunch-test mode, full-ref-guards and exactly deletes only rows matching the approved manifest, then proves Auth and all authority tables are zero."
    - "Only after immutable cleanup/zero proof does the human replace provider inputs in the same production environment; protected-environment target ownership, canonical public origin, callback routes, and management ref do not change (D-14, D-20)."
    - "A second ordinary main push is the sole live-promotion trigger; it verifies zero, applies coherent live provider configuration, deploys schema before functions, and runs only non-destructive smoke (D-15, D-17)."
    - "Immutable redacted evidence binds both automatic runs, exact cleanup, zero state, exact canonical origin, immutable GitHub run/commit, provider coherence, and unchanged post-promotion counts; a derived fingerprint is supplemental correlation only."
  artifacts:
    - path: ".github/workflows/deploy-supabase-production.yml"
      provides: "Automatic prelaunch exact-cleanup branch and production-live zero/coherence/smoke branch"
    - path: "scripts/verify-supabase-support.mjs"
      provides: "Exact manifest cleanup executor, live-zero/provider coherence gates, and promotion record validation"
    - path: "tests/e2e/support-restore.spec.ts"
      provides: "Manifest mismatch, exact deletion, zero, live-coherence, and non-destructive smoke regressions"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md"
      provides: "Immutable prelaunch cleanup/zero run plus same-project live-promotion run evidence"
  key_links:
    - from: "approved 02-09 fixture_manifest"
      to: "first prelaunch-test main-push run"
      via: "current raw keys hashed in protected runner memory; exact count/handle multiset equality gates exact deletion"
      pattern: "fixture_manifest"
    - from: "immutable cleanup zero proof"
      to: "production environment live replacement"
      via: "blocking browser checkpoint occurs only after first automatic run validates"
      pattern: "require-zero-authority"
    - from: "production-live environment"
      to: "same canonical public_origin and immutable GitHub lineage"
      via: "second normal main push derives all routes again and applies provider configuration"
      pattern: "production-live"
  prohibitions:
    - "HIGH: No broad delete, cleanup on manifest mismatch, second project/environment, mixed test/live set, or provider replacement before zero proof."
    - "HIGH: No local secret/ref injection, local/manual deployment, workflow dispatch, or destructive live matrix."
    - "Do not add stored URL variables, change the canonical origin/routes, or delete legacy source before both immutable automatic runs validate."
---

<objective>
Delete exactly the approved prelaunch fixtures, prove zero, replace provider inputs in place, and promote the same canonical-origin project live through a second automatic main-push run.

Purpose: Complete D-14/D-17 in the required cleanup → zero → provider replacement → automatic live promotion order without changing D-15/D-20 lineage.
Output: One redacted evidence record binding immutable cleanup and live-promotion runs.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-09-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md
@.github/workflows/deploy-supabase-production.yml
@scripts/verify-supabase-support.mjs
</context>

<interfaces>
- Cleanup validation: `node scripts/verify-supabase-support.mjs --check-run-evidence PATH --expected-mode prelaunch-test --acceptance ACCEPTANCE --require-exact-cleanup --require-zero-authority --require-immutable-run`.
- Promotion validation: `node scripts/verify-supabase-support.mjs --check-promotion-evidence PATH --acceptance ACCEPTANCE --require-cleanup-run --require-live-run --require-one-fingerprint --require-zero-authority --non-destructive --require-immutable-runs`.
- Both run records use the same exact `public_origin: https://<project-ref>.supabase.co`, production environment, immutable GitHub run/commit lineage, and enumerated Auth/function routes. The ref is never stored separately in evidence; any fingerprint is internally derived correlation only.
- Live replacement changes only the six GitHub/Stripe provider inputs listed in `user_setup` plus `SUPPORT_PROVIDER_MODE`; the management target and derived routing are invariant.
- Before either push, the workflow/verifier implementation must support two fail-closed automatic branches: prelaunch-test plus approved acceptance performs exact manifest cleanup and repeated zero proof; production-live requires zero, verifies the complete live provider set, applies guarded configuration/deployment, and performs only non-destructive smoke.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement automatic exact cleanup and live-promotion executor branches, then capture cleanup zero proof</name>
  <files>.github/workflows/deploy-supabase-production.yml, scripts/verify-supabase-support.mjs, tests/e2e/support-payment.spec.ts, tests/e2e/support-restore.spec.ts, .planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md`
    - `.planning/phases/02-move-the-implementation-to-supabase/02-08-TEST-DEPLOYMENT-EVIDENCE.md`
    - `.github/workflows/deploy-supabase-production.yml`
    - `scripts/verify-supabase-support.mjs` deployment, authority snapshot, cleanup, promotion, and workflow-validation paths
    - `tests/e2e/support-payment.spec.ts`
    - `tests/e2e/support-restore.spec.ts`
  </read_first>
  <behavior>
    - The workflow selects exact cleanup only when mode is `prelaunch-test` and the approved immutable 02-09 acceptance path exists; mode/path/approval/canonical-origin/run/commit mismatch aborts before deletion, while derived fingerprint inconsistency is a supplemental lineage failure.
    - Before deletion, current raw keys are hashed in protected memory and each of all six table count/handle multisets must exactly equal the approved manifest; an extra, missing, duplicate, or unknown row aborts with zero mutations.
    - Deletion is dependency-safe and key-scoped, uses raw keys only in runner memory, and immediately validates the complete protected ref's canonical 20-character shape before every delete mutation; broad delete/truncate is impossible.
    - Cleanup evidence captures exact deleted hashed handles plus repeated post-delete six-table zero snapshots and immutable run/artifact lineage.
    - The already-implemented production-live branch rejects nonzero authority and incomplete/mixed provider state before mutation, verifies the protected Stripe key owns the active USD 4999 Price and exact canonical webhook endpoint/event, applies the guarded coherent GitHub Auth/function-secret configuration, then allows only configuration/version reads and one random generic-unverified status probe with unchanged zero snapshots.
  </behavior>
  <action>Write failing workflow/verifier tests first for exact branch selection, approval and lineage mismatch, six-table equality, unknown/missing/duplicate handles, mutation-free aborts, dependency-safe key-scoped delete order, an immediate `guardTarget(inputs)` canonical 20-character shape validation before every deletion, repeated zero proof, nonzero live abort, incomplete/mixed provider rejection, exact Stripe Price/webhook coherence, and unchanged counts around the live smoke allowlist. Update the existing workflow so a normal main push in prelaunch-test mode passes the committed approved 02-09 acceptance path into an explicit exact-cleanup executor branch; a normal main push in production-live mode selects the separate zero/coherence/non-destructive branch. Implement both branches in `scripts/verify-supabase-support.mjs` before any provider replacement occurs. For cleanup, fetch raw keys only inside the protected runner, compare their table-qualified SHA-256 handle multisets exactly to `fixture_manifest`, abort without mutation on any mismatch, then delete only matched keys in dependency-safe order with fresh complete-ref shape validation immediately before every delete and prove all six tables zero twice. For the live branch, require zero before mutation; use the protected Stripe key to fetch and validate the retained Price and webhook endpoint IDs as active USD 4999 and the exact canonical endpoint with only `checkout.session.completed`; require the complete GitHub client pair and Stripe secret/Price/endpoint/webhook-secret set; apply the exact guarded Auth/provider and function-secret payloads established in 02-08; deploy schema before functions; and restrict smoke to configuration/version reads plus one random generic-unverified status request with zero before/after. Record only redacted booleans, counts, handles, canonical origin/routes, immutable GitHub run/commit lineage, a supplemental internally derived fingerprint, and mutation order. Commit the implementation/tests plus approved manifest, push main normally while still prelaunch-test, download the exact automatic cleanup artifact, and write its cleanup/zero section into 02-10 evidence.</action>
  <verify>
    <automated>npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/support-restore.spec.ts &amp;&amp; node scripts/verify-supabase-support.mjs --check-run-evidence .planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md --expected-mode prelaunch-test --acceptance .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md --require-exact-cleanup --require-zero-authority --require-immutable-run</automated>
  </verify>
  <done>The tested automatic executor removes only the accepted fixtures, proves zero, and has a fail-closed live branch ready before protected provider replacement.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Replace temporary protected provider inputs only after zero proof</name>
  <what-built>The accepted manifest has been exactly removed by the automatic workflow and immutable evidence proves zero authority at the unchanged canonical origin.</what-built>
  <how-to-verify>
    1. Confirm the cleanup run is immutable, exact, and zero.
    2. Confirm production retains the same protected `SUPABASE_PROJECT_REF` variable and canonical `https://&lt;project-ref&gt;.supabase.co` origin under the same protected-environment ownership.
    3. Confirm the release GitHub app callback and Stripe live webhook use the exact canonical routes in `user_setup`.
    4. Confirm mode is production-live and the six replacement provider inputs form one complete live set.
    5. Confirm no temporary provider value, duplicate URL input, provider/deployment mutation, or disclosure occurred.
  </how-to-verify>
  <action>Stop unless Task 1 validates exact deletion and zero authority. During a no-public-traffic window, configure the release GitHub OAuth app and Stripe live Price/webhook in browser dashboards, then replace exactly the six provider inputs named in `user_setup` and set `SUPPORT_PROVIDER_MODE=production-live`. Retain the same protected `SUPABASE_PROJECT_REF` variable, deployment credentials, environment, and canonical routes. Do not add a hash, callback, origin, or webhook variable; retain aliases; mutate Supabase; deploy; push; or disclose a value.</action>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --check-run-evidence .planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md --expected-mode prelaunch-test --acceptance .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md --require-exact-cleanup --require-zero-authority --require-immutable-run</automated>
    <human-check>Complete checks 1–5 without disclosing or applying protected values outside the dashboards.</human-check>
  </verify>
  <done>The unchanged production environment is ready for the automatic live-promotion run.</done>
  <resume-signal>Type `live environment ready` without any value, or identify the missing numbered setting.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Push main again and capture automatic same-project live promotion</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md`
    - `.planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md`
    - `.github/workflows/deploy-supabase-production.yml`
    - `scripts/verify-supabase-support.mjs`
  </read_first>
  <action>Commit the immutable cleanup evidence and push main through the ordinary protected path. Do not run a provider/deployment command. Bind the result to the exact automatic production-live run. Require repository gates before environment access; one coherent live provider set; the same exact canonical `public_origin` and immutable GitHub run/commit lineage; zero Auth/authority counts before provider mutation; immediate canonical 20-character complete-ref shape validation before every hosted mutation; derived Auth/function/webhook routes; schema before all functions; and only configuration/version reads plus one random generic-unverified status read afterward with unchanged zero counts. Merge the immutable run/artifact lineage into 02-10 evidence; any internally derived fingerprint is supplemental correlation only. Reject mixed mode, a second origin, nonzero authority, protected/raw output, destructive behavior, or target change.</action>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --check-promotion-evidence .planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md --acceptance .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md --require-cleanup-run --require-live-run --require-one-fingerprint --require-zero-authority --non-destructive --require-immutable-runs</automated>
  </verify>
  <done>The same clean canonical-origin project is live-configured through a second automatic run; validated evidence unblocks legacy retirement.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Accepted manifest → prelaunch cleanup | Only exact acceptance rows may be removed. |
| Cleanup zero proof → protected provider replacement | Live inputs cannot replace test inputs while fixtures remain. |
| Protected live inputs → same project | Provider mode changes without changing target, environment, or public routing. |
| Promoted production → smoke | Live verification must not create authority. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-10-BD | Tampering | fixture cleanup | mitigate | Exact count/handle multiset match, dependency-safe exact deletion, and zero gate. |
| T-02-10-OR | Tampering | promotion order | mitigate | Immutable cleanup validates before input replacement and second push. |
| T-02-10-WT | Tampering | deployment target | mitigate | Unchanged protected-environment target ownership and immediate canonical complete-ref shape validation in both runs. |
| T-02-10-RO | Spoofing | public routing | mitigate | Exact cross-record canonical origin and enumerated routes; no stored duplicate URLs. |
| T-02-10-MM | Elevation of Privilege | provider mode | mitigate | Complete live-set replacement and explicit test/live coherence checks. |
| T-02-10-LD | Tampering | live authority | mitigate | Read-only/config/status allowlist and before/after zero counts. |
</threat_model>

<verification>An automatic prelaunch run performs exact cleanup and zero proof before provider replacement; a second automatic run promotes the same canonical-origin project live and performs only non-destructive smoke.</verification>
<success_criteria>
- D-14/D-16 leave one empty project promoted in place without import, dual-write, or reconciliation.
- D-15 keeps cleanup and promotion as automatic unfiltered main-push runs with immediate mutation guards.
- D-17 permits no destructive live matrix; D-18/D-20 preserve minimal redacted data and exact public-route lineage.
- PAY-01/PAY-03/PAY-04 and D-10 REC semantics remain configured under webhook authority.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-10-SUMMARY.md` when complete.</output>
