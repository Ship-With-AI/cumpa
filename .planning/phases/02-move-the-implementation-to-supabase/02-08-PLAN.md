---
phase: 02-move-the-implementation-to-supabase
plan: 08
type: execute
wave: 8
depends_on: [02-07]
files_modified:
  - scripts/verify-supabase-support.mjs
  - docs/support-service-operations.md
  - .planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md
autonomous: false
requirements: [PAY-01, PAY-03, PAY-04, REC-01, REC-02]
user_setup:
  - service: supabase
    why: "The driver cleans and promotes the same sole project in place (D-14); every value is a named environment input and no unspecified dashboard value may be preserved."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard -> Account -> Access Tokens; secret, process environment only, never recorded"
      - name: SUPABASE_PROJECT_REF
        source: "Supabase Dashboard -> Project Settings -> General -> Reference ID for the same sole project accepted in 02-07; process environment only, only full-ref SHA-256 and display suffix recorded"
      - name: SUPABASE_DB_PASSWORD
        source: "Supabase Dashboard -> Project Settings -> Database; secret, process environment only, never recorded"
      - name: RELEASE_SUPPORT_PUBLIC_ORIGIN
        source: "Release Supabase Project URL from Project Settings -> API; exact canonical `https://${SUPABASE_PROJECT_REF}.supabase.co` origin with no path, query, fragment, credentials, or trailing slash; maps to function secret `SUPPORT_PUBLIC_ORIGIN`; only equality status and redacted hash/suffix recorded"
      - name: RELEASE_SUPABASE_SITE_URL
        source: "Desired Supabase Dashboard -> Authentication -> URL Configuration -> Site URL; must equal `${RELEASE_SUPPORT_PUBLIC_ORIGIN}/functions/v1/support-flow/complete`; only equality status recorded"
      - name: RELEASE_SUPABASE_REDIRECT_URL
        source: "Desired Supabase Dashboard -> Authentication -> URL Configuration -> Redirect URLs entry; must equal `${RELEASE_SUPPORT_PUBLIC_ORIGIN}/functions/v1/support-flow/callback`; only equality status recorded"
  - service: github
    why: "The complete temporary OAuth pair is replaced by the complete release pair after exact cleanup."
    env_vars:
      - name: RELEASE_GITHUB_CLIENT_ID
        source: "GitHub Settings -> Developer settings -> OAuth Apps -> release app -> Client ID; process environment only, record only redacted hash/suffix and presence status"
      - name: RELEASE_GITHUB_CLIENT_SECRET
        source: "Same release GitHub OAuth app -> Client secrets; secret, process environment only, never recorded"
      - name: RELEASE_GITHUB_CALLBACK_URL
        source: "Same release GitHub OAuth app -> Authorization callback URL; must equal `${RELEASE_SUPPORT_PUBLIC_ORIGIN}/auth/v1/callback`; only equality status recorded"
  - service: stripe
    why: "The complete test set is replaced by one coherent live secret/Price/webhook set; test objects stay isolated in test mode."
    env_vars:
      - name: STRIPE_LIVE_SECRET_KEY
        source: "Stripe Dashboard with Test mode off -> Developers -> API keys -> Secret key; secret, process environment only, never recorded; driver maps it to function secret `STRIPE_SECRET_KEY`"
      - name: STRIPE_LIVE_PRICE_ID
        source: "Stripe Dashboard with Test mode off -> Product catalog -> Price for the fixed one-time USD $49.99 product; process environment only, record only redacted hash/suffix and validated product facts; driver maps it to `STRIPE_PRICE_ID`"
      - name: STRIPE_LIVE_WEBHOOK_ENDPOINT_ID
        source: "Stripe Dashboard with Test mode off -> Developers -> Webhooks -> endpoint whose URL is `${RELEASE_SUPPORT_PUBLIC_ORIGIN}/functions/v1/stripe-webhook`; process environment only, record only redacted hash/suffix and endpoint status"
      - name: STRIPE_LIVE_WEBHOOK_SECRET
        source: "The signing secret revealed on that exact live webhook endpoint; secret, process environment only, never recorded; driver maps it to `STRIPE_WEBHOOK_SECRET`"
must_haves:
  truths:
    - "Before every hosted query, deletion, secret update, or provider mutation, SHA-256 of the complete supplied ref matches 02-07 `project_ref_sha256`; suffix never authorizes a target."
    - "Before fixture deletion, secret update, Auth/GitHub/Stripe change, or any other hosted mutation, one read-only preflight validates every named release input, canonical HTTPS origin/derived URLs, full 02-07 project fingerprint, live Stripe account/Price/endpoint, fixed one-time USD $49.99 Price facts, endpoint/signing-secret provenance, and exact GitHub callback equality; any missing, ambiguous, invalid, test-mode, mixed-mode, or mismatched value aborts all hosted changes."
    - "Only fixtures whose stable handles appear in the approved 02-07 manifest are deleted, in dependency-safe order, before provider promotion."
    - "Any missing handle, extra row, changed count, or unexplained Auth/authority row aborts; after cleanup `auth.users` and all five authority tables contain zero rows."
    - "Stripe test objects remain in Stripe test mode and are neither copied nor reused in live configuration."
    - "The same clean project receives release GitHub and complete live Stripe configuration in place, followed only by non-destructive observation (D-14, D-17)."
  artifacts:
    - path: "scripts/verify-supabase-support.mjs"
      provides: "Fingerprint-guarded fixture cleanup, atomic provider promotion, and non-destructive checks"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md"
      provides: "Zero-fixture gate and approved production-live configuration evidence"
  key_links:
    - from: "02-07 fixture_manifest"
      to: "hosted cleanup"
      via: "exact SHA-256 handle multiset comparison before deletion"
      pattern: "fixture_manifest"
    - from: "02-07 project_ref_sha256"
      to: "every 02-08 hosted operation"
      via: "hash complete supplied ref in memory immediately before operation"
      pattern: "project_ref_sha256"
    - from: "named release operator inputs"
      to: "fixture cleanup and provider promotion"
      via: "one complete read-only production-live preflight before Task 1 permits any deletion or configuration write"
      pattern: "preflight.*production-live"
  prohibitions:
    - "HIGH: No suffix-only target check, broad delete, cleanup on input/preflight mismatch, second project, data move, mixed test/live configuration, test-object copying, or implicit preservation of a dashboard value."
    - "HIGH: Abort before every hosted change when any named input is absent/blank/ambiguous, origin is not canonical HTTPS, the full fingerprint differs, Stripe is not coherently live with the fixed one-time USD $49.99 Price and matching endpoint/signing-secret provenance, or the GitHub callback differs from the derived exact value."
    - "HIGH: No OAuth, Checkout, webhook, restore, replay, hostile, or payment matrix after live promotion."
    - "Do not delete legacy repository source or enable public clients in this plan."
---

<objective>
Delete exactly the approved acceptance fixtures, prove a zero-authority launch state, and promote that same project in place to coherent live provider configuration.

Purpose: Prevent test identities/payment authority from becoming production data while preserving the one-project model.
Output: Fingerprint-guarded cleanup/promotion automation, runbook, zero-count evidence, and approval.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-07-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md
@supabase/migrations/20260814000000_support_authority.sql
@scripts/verify-supabase-support.mjs

<interfaces>
- Authorization for every hosted operation is equality with `project_ref_sha256`, calculated over the complete supplied ref in memory.
- Cleanup compares exact table counts and handle multisets before deleting only matching raw keys held in memory.
- Cleanup order is `stripe_events`, `installation_bindings`, `supporters`, `checkout_sessions`, `support_intents`, then acceptance `auth.users`; a transaction protects authority-table deletion.
- Promotion begins only after repeated zero counts for Auth and every authority table.
- Release desired URL fields are exact: origin `https://${SUPABASE_PROJECT_REF}.supabase.co`, Supabase Site URL `${RELEASE_SUPPORT_PUBLIC_ORIGIN}/functions/v1/support-flow/complete`, Supabase allowed redirect `${RELEASE_SUPPORT_PUBLIC_ORIGIN}/functions/v1/support-flow/callback`, GitHub authorization callback `${RELEASE_SUPPORT_PUBLIC_ORIGIN}/auth/v1/callback`, and Stripe webhook endpoint `${RELEASE_SUPPORT_PUBLIC_ORIGIN}/functions/v1/stripe-webhook`.
- The driver accepts no preserve-current semantics: all release values must be present before its read-only preflight, secrets remain process-memory only, and evidence contains only fingerprints/suffixes, validated non-secret facts, counts, timestamps, and pass/fail statuses.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove only inventoried acceptance fixtures and prove zero authority</name>
  <files>scripts/verify-supabase-support.mjs, docs/support-service-operations.md, .planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md</files>
  <action>Before any hosted read that can lead to mutation or any fixture deletion, load every named release environment variable as one complete production-live input set; reject absent, blank, duplicate/ambiguous, or unmapped values. Perform a read-only preflight: hash the full `SUPABASE_PROJECT_REF` in memory and require the approved 02-07 fingerprint; require canonical HTTPS release origin plus exact derived Supabase Site/redirect, GitHub callback, and Stripe webhook URLs; authenticate `STRIPE_LIVE_SECRET_KEY` as live mode; retrieve `STRIPE_LIVE_PRICE_ID` and require `active=true`, `livemode=true`, one-time/non-recurring, `currency=usd`, `unit_amount=4999`; retrieve `STRIPE_LIVE_WEBHOOK_ENDPOINT_ID` and require `livemode=true`, exact URL, and `checkout.session.completed`; require the signing-secret input provenance to be that same endpoint; and require the supplied release GitHub callback to equal the derived callback. Abort before cleanup, secret writes, Auth/provider changes, webhook changes, or any other hosted mutation on a missing input, validation/mode/coherence/callback/fingerprint failure, or value that would otherwise be preserved ambiguously. After the complete preflight passes, require explicit 02-07 approval and fixture manifest. Before each hosted read or delete, recheck the full-ref fingerprint; query `auth.users` and every `support_private` table, derive the same stable handles, and require exact per-table count/multiset equality. Delete only matched authority rows in foreign-key-safe order inside one transaction, then matched Auth users through the supported admin boundary. Requery and require zero rows in all six locations. Evidence includes only redacted input identifiers/statuses, fingerprint/suffix, exact counts, stable handles, and UTC outcomes; never a secret or raw provider value.</action>
  <verify><automated>node scripts/verify-supabase-support.mjs --mode production-live --cleanup-acceptance-fixtures --prelaunch .planning/phases/02-move-the-implementation-to-supabase/02-07-PRELAUNCH-EVIDENCE.md --evidence .planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md --require-zero-authority</automated></verify>
  <done>Only recorded fixtures are gone and Auth plus all authority tables are proven empty with no unexplained row.</done>
</task>

<task type="auto">
  <name>Task 2: Promote the clean sole project to production-live configuration</name>
  <files>scripts/verify-supabase-support.mjs, docs/support-service-operations.md, .planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md</files>
  <action>Recheck the already-complete input preflight, full-ref fingerprint, and zero-authority gate immediately before each provider/configuration mutation; never infer that a missing release field means preserve the temporary value. In a no-public-traffic window, replace the complete Stripe test set atomically with the mapped live secret, fixed one-time USD $49.99 Price, and exact live webhook endpoint/signing-secret set; replace the temporary GitHub client pair and Supabase Site/redirect settings with the complete release pair and exact derived URLs; set the release `SUPPORT_PUBLIC_ORIGIN`; keep GitHub-only Auth and email/magic-link disabled. Assert the Stripe key, Price, and endpoint are all live, mutually coherent, and distinct from every recorded test handle; assert the GitHub dashboard callback equals the derived value; assert no test value remains and no live value equals a recorded test handle. Abort before the first or any subsequent provider write on failure rather than leaving an unvalidated or mixed set. Record separate `production-live` timestamps, the same fingerprint/suffix, redacted provider identifier hashes/suffixes, validated non-secret Price/endpoint/callback statuses, commit/migration/function versions, and zero counts; never record secrets or raw identifiers. Run only configuration reads and one fresh random-installation status read; require generic unverified and unchanged authority counts.</action>
  <verify><automated>node scripts/verify-supabase-support.mjs --mode production-live --promote --non-destructive --require-zero-authority --evidence .planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md</automated></verify>
  <done>The same clean project has a fully supplied, preflight-validated coherent live GitHub/Stripe/Supabase set and only non-destructive, count-preserving redacted smoke evidence.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve cleanup and in-place production configuration</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md, docs/support-service-operations.md</files>
  <action>Review and approve only the redacted cleanup/promotion evidence; reject approval if the complete input preflight, exact cleanup/zero counts, canonical URL/callback equality, coherent live fixed-Price/webhook facts, lineage, non-destructive observation, or secret-exclusion status is missing or failed. Do not request or add any raw provider input, credential, secret, or identifier.</action>
  <what-built>Exact acceptance-fixture cleanup followed by same-project live provider promotion.</what-built>
  <how-to-verify>
    1. Confirm 02-07 and 02-08 use the identical full-ref SHA-256 and only a display suffix.
    2. Confirm pre-delete counts/handles exactly matched the approved manifest and all post-delete Auth/authority counts are zero.
    3. Confirm no unexplained row was ignored and only recorded fixtures were deleted.
    4. Confirm Stripe test objects stayed in test mode and no test identifier/value was copied into live configuration.
    5. Confirm every named release input passed before cleanup, all URLs are canonical HTTPS/exact derived values, one coherent live Stripe key/fixed one-time USD $49.99 Price/webhook set replaced the test set, the release GitHub callback equals the derived Supabase callback contract, and GitHub-only Auth keeps email/magic links disabled.
    6. Confirm post-promotion activity was only count-preserving generic status/configuration observation and evidence contains no secret or raw provider value.
    7. Record explicit dated approval; keep deletion and public release blocked on failure.
  </how-to-verify>
  <verify><automated>node scripts/verify-supabase-support.mjs --mode production-live --check-promotion-evidence .planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md --require-approved --require-zero-authority</automated><human-check>Approve only after steps 1–7 pass.</human-check></verify>
  <done>Approved zero-fixture same-project promotion unblocks sequential legacy deletion.</done>
  <resume-signal>Type `approved` or identify the exact mismatch.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Evidence → deletion | Redacted handles must authorize only exact acceptance fixtures. |
| Operator → project | Every mutation must target the full-ref fingerprint. |
| Test providers → live providers | Test facts must never cross into live mode. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-08-WT | Tampering | hosted target | mitigate | Full-ref SHA-256 equality before every operation. |
| T-02-08-BD | Tampering | fixture cleanup | mitigate | Exact handle multiset, FK-safe exact deletion, abort on mismatch. |
| T-02-08-MM | Elevation of Privilege | provider mode | mitigate | Complete-set replacement and test/live isolation checks. |
| T-02-08-LD | Tampering | live authority | mitigate | Zero gate and count-preserving status-only smoke. |
| T-02-08-SL | Information Disclosure | evidence | mitigate | Fingerprints, suffix, counts, and redacted handles only. |
| T-02-08-PI | Tampering | release input preflight | mitigate | Complete named inputs, canonical HTTPS/derived URL equality, full fingerprint, live fixed Price, endpoint/signing-secret provenance, and GitHub callback equality gate cleanup and promotion; evidence is redacted. |
</threat_model>

<verification>Promotion passes only after the complete named release-input preflight, exact cleanup, zero Auth/authority counts, full-ref guards, canonical URLs, coherent live fixed-Price/webhook/GitHub configuration, redacted evidence, and non-destructive smoke.</verification>
<success_criteria>
- D-14/D-16 promote one empty project in place without test authority, migration, or parallel service.
- D-15/D-17 prohibit destructive live acceptance.
- PAY-01/PAY-03 live configuration remains webhook-authoritative without exposing PAY-04 data.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-08-SUMMARY.md`.</output>
