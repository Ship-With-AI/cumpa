---
phase: 02-move-the-implementation-to-supabase
plan: 11
type: execute
wave: 11
depends_on: [02-10]
files_modified:
  - render.yaml
  - services/support/migrations/001_init.sql
  - services/support/src/config.ts
  - services/support/src/db.ts
  - services/support/src/email.ts
  - services/support/src/fulfillment.ts
  - services/support/src/schema.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
must_haves:
  truths:
    - "No deletion begins until immutable 02-10 evidence proves exact fixture cleanup, zero Auth/authority counts, same-project live promotion, and non-destructive smoke (D-14, D-17)."
    - "The obsolete Render descriptor, standalone PostgreSQL migration, database/config adapters, Resend email module, legacy fulfillment service, and legacy schema are absent (D-02, D-08, D-19)."
    - "Supabase migrations, service-role RPC authority, Edge Functions, and protected-main deployment remain untouched."
  artifacts: []
  key_links:
    - from: ".planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md"
      to: "render.yaml and legacy schema/provider deletion"
      via: "offline immutable promotion-evidence gate immediately before the first deletion"
      pattern: "require-zero-after-cleanup"
  prohibitions:
    - "HIGH: Do not delete any file until the exact 02-10 promotion evidence validates."
    - "HIGH: Do not remove Supabase migration/function files, active local Cumpa Fastify dependencies, or any completed Phase 02 artifact."
    - "Do not retain a fallback, compatibility provider, archived copy, or generated legacy runtime."
---

<objective>
Retire the legacy Render, schema, database, and provider implementation in the first bounded deletion slice.

Purpose: Begin the irreversible D-19 clean cutover only after approved sole-project promotion proof, while keeping each retirement plan below the standard file-scope warning threshold.
Output: Seven obsolete provider/schema files removed with evidence-gated absence proof.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-10-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md
@scripts/verify-supabase-support.mjs
@render.yaml
@services/support/migrations/001_init.sql
@services/support/src/config.ts
@services/support/src/db.ts
@services/support/src/email.ts
@services/support/src/fulfillment.ts
@services/support/src/schema.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate retirement and delete Render plus standalone database surfaces</name>
  <files>render.yaml, services/support/migrations/001_init.sql, services/support/src/config.ts, services/support/src/db.ts</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md`
    - `scripts/verify-supabase-support.mjs`
    - `render.yaml`
    - `services/support/migrations/001_init.sql`
    - `services/support/src/config.ts`
    - `services/support/src/db.ts`
  </read_first>
  <action>Run the already-tested offline 02-10 promotion validator before the first deletion and stop unless it proves the approved 02-09 manifest was exactly removed, all six Auth/authority tables are zero, the exact canonical `public_origin` and immutable GitHub run/commit lineage were promoted in a separate automatic production-live run, live smoke was non-destructive, and both runs are immutable; any fingerprint consistency is supplemental derived correlation only. Then delete exactly the Render descriptor, standalone PostgreSQL migration, legacy service configuration, and direct database adapter. Do not edit or delete Supabase migrations/configuration, the sole workflow, root Cumpa Fastify runtime dependencies, or evidence. Do not archive or rename these files.</action>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --check-promotion-evidence .planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md --acceptance .planning/phases/02-move-the-implementation-to-supabase/02-09-ACCEPTANCE-EVIDENCE.md --require-approved --require-cleanup-run --require-live-run --require-one-fingerprint --require-exact-cleanup --require-zero-after-cleanup --require-live-smoke --non-destructive --require-immutable-runs &amp;&amp; test ! -e render.yaml &amp;&amp; test ! -e services/support/migrations/001_init.sql &amp;&amp; test ! -e services/support/src/config.ts &amp;&amp; test ! -e services/support/src/db.ts</automated>
  </verify>
  <done>Immutable promotion proof gated deletion and the four legacy deployment/database files are absent.</done>
</task>

<task type="auto">
  <name>Task 2: Delete provider, fulfillment, and legacy schema modules</name>
  <files>services/support/src/email.ts, services/support/src/fulfillment.ts, services/support/src/schema.ts</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-10-LIVE-PROMOTION-EVIDENCE.md`
    - `services/support/src/email.ts`
    - `services/support/src/fulfillment.ts`
    - `services/support/src/schema.ts`
    - `supabase/functions/_shared/fulfillment.ts`
    - `supabase/functions/_shared/validation.ts`
  </read_first>
  <action>After Task 1's evidence gate and absence checks pass, delete the obsolete Resend email module, Node fulfillment service, and legacy request/schema module. Preserve the active Supabase fulfillment classifier, strict function validation, webhook function, and database RPCs; those are the replacement authority already proved by 02-10. Leave no re-export, stub, compatibility wrapper, fallback, or copied source.</action>
  <verify>
    <automated>test ! -e services/support/src/email.ts &amp;&amp; test ! -e services/support/src/fulfillment.ts &amp;&amp; test ! -e services/support/src/schema.ts &amp;&amp; test -e supabase/functions/_shared/fulfillment.ts &amp;&amp; test -e supabase/functions/_shared/validation.ts</automated>
  </verify>
  <done>All seven files in the first retirement slice are absent and the Supabase authority remains.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Immutable promotion evidence → deletion | Untrusted or incomplete evidence must never authorize irreversible retirement. |
| Legacy provider/schema → Supabase authority | Deletion must remove only superseded surfaces and preserve the proven replacement. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-11-01 | Tampering | retirement gate | mitigate | Validate exact immutable cleanup/live lineage immediately before first deletion. |
| T-02-11-02 | Denial of Service | replacement authority | mitigate | Exact seven-file deletion list plus positive Supabase artifact checks. |
| T-02-11-03 | Information Disclosure | provider remnants | mitigate | Delete Resend/direct database/provider modules without archival copies. |
</threat_model>

<verification>The exact 02-10 immutable evidence gate passes first; all seven named legacy files are absent; active Supabase replacement artifacts remain.</verification>
<success_criteria>
- D-14/D-17 promotion evidence precedes irreversible deletion.
- D-02/D-08/D-19 remove Render, direct PostgreSQL, Resend, legacy fulfillment, and schema surfaces without fallback.
- The plan modifies seven files, below the standard ten-file warning threshold.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-11-SUMMARY.md` when complete.</output>
