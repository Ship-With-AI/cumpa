---
phase: 02-move-the-implementation-to-supabase
plan: 11
type: execute
wave: 11
depends_on: [02-10]
files_modified:
  - docs/support-service-operations.md
  - scripts/verify-supabase-support.mjs
  - tests/e2e/package-assets.spec.ts
  - services/support/tests/
  - tests/e2e/support-recovery.spec.ts
  - .planning/phases/02-move-the-implementation-to-supabase/02-11-RETIREMENT-EVIDENCE.md
autonomous: true
requirements: [PAY-04, REC-01, REC-02]
must_haves:
  truths:
    - "02-09/02-10 deletion summaries and approved 02-08 evidence gate precede absence proof."
    - "Operations documentation describes only ordinary-local disablement, one-project prelaunch/promotion, protected direct production deployment, and no fallback."
    - "After 02-09 runtime/schema/config deletion and 02-10 toolchain deletion, obsolete hosted-service tests and the email-recovery E2E spec are removed; tracked, generated, built, and packed outputs contain no Render service, hosted direct-Postgres runtime, Resend/email recovery, deleted routes/schemas/tests, workspace, or privileged provider value."
    - "The ordinary package built without the 02-12 release input contains no configured hosted endpoint or privileged provider value and its installed ordinary launch performs no outbound support request (D-13, PAY-04, D-18)."
    - "The packed package keeps the public-capability release seam for 02-12 but contains no secret, full project ref, PII, OAuth token, or provider credential."
  artifacts:
    - path: "docs/support-service-operations.md"
      provides: "Supabase-only one-project operations without legacy guidance"
    - path: "scripts/verify-supabase-support.mjs"
      provides: "Reusable repository/build/package absence verifier"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-11-RETIREMENT-EVIDENCE.md"
      provides: "Hash-bound generated and packed absence proof"
  key_links:
    - from: "repository and npm pack output"
      to: "verify-supabase-support.mjs"
      via: "tracked/generated/archive inspection"
      pattern: "verify-legacy-absence"
    - from: "02-09-SUMMARY.md and 02-10-SUMMARY.md"
      to: "obsolete hosted test removal and package/remnant proof"
      via: "02-11 depends on 02-10, which depends on 02-09; moved cleanup and assertions execute only after both deletion slices"
      pattern: "02-09.*02-10"
  prohibitions:
    - "Do not add production deployment/release configuration here; 02-12 owns it."
    - "Do not allow legacy text merely because it is documentation, generated, ignored, or packed."
---

<objective>
Finish retirement documentation and prove legacy/provider-sensitive material is absent from repository, generated output, and package.

Purpose: Isolate the third deletion slice from protected deployment and final verification.
Output: Supabase-only runbook, reusable verifier, package contract, and retirement evidence.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-10-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md
@package.json
@tests/e2e/package-assets.spec.ts
</context>

<tasks>
<task type="auto">
  <name>Task 1: Replace legacy operations guidance and extend the absence verifier</name>
  <files>docs/support-service-operations.md, scripts/verify-supabase-support.mjs, services/support/tests/, tests/e2e/support-recovery.spec.ts</files>
  <action>Require the 02-09 and 02-10 deletion summaries plus approved 02-08 evidence before changing remnants. Remove the obsolete hosted-service test tree and obsolete email-recovery E2E spec moved from 02-09; do not recreate or replace them. Rewrite operations around D-13 ordinary-local complete disablement, D-14 one project used once for prelaunch test acceptance then cleaned/promoted in place, D-15 protected-main direct deployment, additive forward fixes, non-destructive live checks, rotation/incident handling, and no fallback/import/dual-write. Extend the verifier to reject legacy source/routes/schema/tests/workspace/scripts/dependencies, Render/Resend/email-recovery in tracked or generated content, and obsolete package entries.</action>
  <verify><automated>test ! -e services/support/tests &amp;&amp; test ! -e tests/e2e/support-recovery.spec.ts &amp;&amp; node scripts/verify-supabase-support.mjs --verify-legacy-absence --repository --docs</automated></verify>
  <done>Documentation and verifier describe/enforce only the one-project Supabase cutover.</done>
</task>
<task type="auto">
  <name>Task 2: Rebuild, pack, and record absence evidence</name>
  <files>tests/e2e/package-assets.spec.ts, .planning/phases/02-move-the-implementation-to-supabase/02-11-RETIREMENT-EVIDENCE.md</files>
  <action>Move the ordinary-package D-13 assertion intact from 02-06 into the existing package-assets contract: with the release input absent, a fresh generated package contains no configured hosted endpoint or privileged provider value and an installed ordinary launch emits no outbound support request. Also inspect fresh generated output and `npm pack --json` archive contents for the complete legacy/secret/PII deny-list, including the test/remnant paths removed after 02-09/02-10. Run the verifier after a clean build and pack, recording commit, manifest/lock hashes, tarball hash, allowed public material, denied-category counts, and command outcomes. Do not store the complete Supabase ref, raw provider identifier, or credential. This proves ordinary-package disablement and absence; configured production package availability remains owned by 02-12.</action>
  <verify><automated>npx playwright test --config=tests tests/e2e/package-assets.spec.ts &amp;&amp; node scripts/verify-supabase-support.mjs --verify-legacy-absence --generated --package --evidence .planning/phases/02-move-the-implementation-to-supabase/02-11-RETIREMENT-EVIDENCE.md</automated></verify>
  <done>Tracked/generated/packed legacy and privileged material is absent with reproducible hash-bound evidence.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Repository → generated/package | Deleted content can survive in ignored build output or archives. |
| Operations docs → operator | Stale instructions can recreate forbidden topology. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-11-SL | Information Disclosure | package/generated output | mitigate | Deny-list scan and tarball hash evidence. |
| T-02-11-LF | Elevation of Privilege | docs/fallback | mitigate | Supabase-only one-project runbook and legacy-text rejection. |
| T-02-11-RP | Repudiation | absence proof | mitigate | Commit/manifest/lock/tarball hashes and commands. |
</threat_model>
<verification>After both deletion summaries, obsolete hosted tests are removed and repository/docs/generated/package checks prove legacy, secret, PII, and configured ordinary-package endpoint/network absence.</verification>
<success_criteria>
- D-02/D-08/D-10/D-19 retirement has no source, docs, generated, or package remnant.
- D-13/D-14/D-15 operations match the locked local and sole-project model.
- PAY-04/D-18 package evidence contains no privileged or personal material.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-11-SUMMARY.md`.</output>
