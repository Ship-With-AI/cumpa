---
phase: 02-move-the-implementation-to-supabase
plan: 11
type: execute
wave: 11
depends_on: [02-10]
files_modified:
  - .planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md
  - docs/support-service-operations.md
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
must_haves:
  truths:
    - "An actual reviewed commit on main triggers the protected production workflow, applies migration `20260814000000`, inspects RLS/grants, then deploys all three functions from that same commit (D-15)."
    - "Production has an active live Stripe account, exactly one live one-time USD $49.99 Price, and the signed webhook endpoint; only the webhook confers authority (PAY-01, PAY-03)."
    - "Production GitHub-only OAuth and dashboard settings match versioned/runbook expectations with no email recovery, token leakage, or unauthenticated supporter enumeration (D-05, D-08, REC-01, REC-02)."
    - "The live application remains anonymous/unrestricted, shows the optional dialog until verified, dismisses for unpaid users, auto-closes after verified polling, and suppresses later launches after persistence (PAY-02, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05)."
    - "The same supporter restores unlimited fresh installations with no device-management or transfer flow (D-11, REC-03)."
    - "Packed assets and evidence contain no Stripe/Supabase/GitHub secret, OAuth/profile/email content, or legacy provider surface; external Render/Postgres/Resend resources and credentials are retired (PAY-04, D-02)."
  artifacts:
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md"
      provides: "Approved actual production CI/schema/function/Auth/Stripe/smoke/package/retirement evidence"
    - path: "docs/support-service-operations.md"
      provides: "Dated production activation and external provider retirement record"
  key_links:
    - from: ".github/workflows/deploy-supabase-production.yml"
      to: "production Supabase migration and functions"
      via: "successful protected main workflow URL and same-commit evidence"
      pattern: "20260814000000"
    - from: "production Stripe webhook"
      to: "installation status polling"
      via: "signed event → fulfillment RPC → status → machine-wide promotion"
      pattern: "checkout.session.completed"
    - from: "production Auth/dashboard settings"
      to: "docs/support-service-operations.md"
      via: "redacted expected-versus-observed drift comparison"
      pattern: "GitHub|email|callback"
  prohibitions:
    - "HIGH: Do not approve source inspection, build success, manual promotion, feature-branch deployment, or development-project evidence as production proof."
    - "HIGH: Do not complete an unintended live charge or record secrets, PII, full tokens, auth codes, webhook signatures, or provider credentials."
    - "Do not retire external legacy resources before production schema/function/smoke/package checks pass."
---

<objective>
Approve the actual main-triggered Supabase production deployment, live provider configuration, canonical PAY/SUP/REC behavior, and final external-provider retirement.

Purpose: Close Phase 02 with observed production truth after repository deletion and workflow installation, without combining either irreversible implementation cutover into this checkpoint.
Output: Dated redacted production evidence and confirmed Supabase-only operations state.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-10-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md
@.github/workflows/deploy-supabase-production.yml
@docs/support-service-operations.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Approve actual production deployment and final retirement</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md, docs/support-service-operations.md</files>
  <read_first>
    - .github/workflows/deploy-supabase-production.yml — exact protected deploy contract
    - .planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md — required live evidence fields
    - docs/support-service-operations.md — production smoke, Auth drift, Price activation, retention, and retirement checklist
    - .planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md — approved development baseline for comparison only
  </read_first>
  <action>Inspect the exact protected workflow run and provider-retirement sequence below. Record only redacted deployment identifiers, expected-versus-observed config comparisons, and dated approval/failure. Do not bypass CI with manual schema/function promotion. The checkpoint explicitly resolves production Stripe activation/Price availability and production Auth dashboard drift before approval.</action>
  <what-built>Supabase-only repository, protected main workflow, production Postgres/Auth/Edge Functions, fixed live Stripe Price/webhook configuration, canonical support/restore behavior, and redacted production evidence.</what-built>
  <how-to-verify>
    1. Merge/push the exact reviewed commit to `main`; confirm the automatic protected `production` workflow, not a manual substitute, runs.
    2. Confirm the job targets the recorded production ref, applies migration `20260814000000`, inspects RLS/grants, then deploys `support-api`, `support-flow`, and `stripe-webhook` from the same commit.
    3. Compare every GitHub Auth provider/callback/site setting with the versioned/runbook expectation; record redacted equality and confirm email/magic-link sign-in remains disabled.
    4. Confirm the production Stripe account is live-enabled, the configured server-only Price is one-time USD $49.99, and the webhook endpoint/events/signing secret are active; use a provider test delivery where supported and do not complete an unintended charge.
    5. Confirm production anonymous start/status and invalid-input smoke expose only allowed generic shapes; ordinary review launches and remains fully usable without authentication or hosted availability.
    6. Exercise the dialog matrix safely: optional fixed-price Support handoff, Not now/unpaid unrestricted use, verified polling automatic close, persisted next-launch suppression, GitHub Restore on repeated fresh installations, and generic unpaid restore.
    7. Confirm packed Cumpa contains no provider secret/auth/profile/email/legacy service content and existing verified local state remains accepted.
    8. Only after steps 1–7 pass, confirm external Render service/database and Resend/recovery credentials are deleted/revoked; record workflow URL, commit, redacted project/migration/function/Price/event observations, config comparisons, retirement confirmations, date/operator.
  </how-to-verify>
  <acceptance_criteria>
    - Actual main-triggered production CI proves schema application/inspection before same-commit functions; source/build success is insufficient.
    - Live Price/account/webhook availability and Auth expected-versus-observed settings are recorded and approved.
    - Canonical PAY-01 through PAY-04, SUP-01 through SUP-05, and D-10-superseded REC-01/REC-02 plus unchanged REC-03 are observed without feature gating or secret/PII leakage.
    - No legacy provider, service source, route, dependency, secret, or recovery-email path remains live or tracked.
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow deploy-supabase-production.yml --branch main --limit 1</automated>
    <human-check>Open the successful run and production provider dashboards, complete steps 2–8, and type `approved` only when the redacted evidence file is complete.</human-check>
  </verify>
  <done>Actual production Supabase deployment, canonical support behavior, live provider configuration, and complete legacy retirement are approved; Phase 02 may close.</done>
  <resume-signal>Type `approved` or identify the exact failed deployment/configuration/behavior/retirement observation.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `.planning/phases/02-move-the-implementation-to-supabase/02-10-PRODUCTION-EVIDENCE.md`: actual production run/config/behavior/package/retirement evidence approval.
- `docs/support-service-operations.md`: dated live activation and external provider retirement record.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Main/protected CI → production Supabase | Only the reviewed commit and protected target may establish production state. |
| GitHub/Stripe dashboards → recorded configuration | Human-visible settings and live availability must match locked/versioned expectations. |
| Production responses/package → anonymous user | Public surfaces expose only generic capability data and no secret/session/profile information. |
| New production authority → external legacy retirement | Retirement follows complete production proof and leaves no fallback. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-11-CI | Tampering/Elevation | HIGH | production run | mitigate | Inspect actual protected main run, target, same commit, schema/RLS/grants before functions. |
| T-02-11-PV | Spoofing | HIGH | live provider configuration | mitigate | Record redacted Auth expected-versus-observed settings and live Stripe account/Price/webhook observations. |
| T-02-11-AU | Elevation | HIGH | payment/restore authority | mitigate | Signed webhook/status evidence only; redirect/test handoff cannot grant verification; unlimited restore uses authenticated binding. |
| T-02-11-SL | Information Disclosure | HIGH | dashboards/evidence/package | mitigate | No credentials/PII/full tokens/signatures; redacted comparisons and packed content scan. |
| T-02-11-LF | Tampering | HIGH | provider retirement | mitigate | Retire external legacy resources only after production proof; confirm repository and provider absence. |
| T-02-11-RP | Repudiation | MEDIUM | final approval | mitigate | Preserve run URL, commit, safe version/event correlations, dated operator approval and retirement record. |

Execution MUST stop if any HIGH threat remains unresolved, production deployment lacks actual schema evidence, or provider retirement is incomplete. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
The only passing proof is an actual protected main workflow plus redacted production Auth/Stripe/schema/function/smoke/package/behavior/retirement observations. No local, development, source, or build substitute passes.
</verification>

<success_criteria>
- D-01/D-14/D-15/D-17 leave Supabase as the sole production hosted authority deployed from main.
- D-02/D-08/D-16/D-19 leave no legacy provider, email recovery, fallback, or migration machinery.
- PAY-01 through PAY-04 and SUP-01 through SUP-05 retain their exact canonical meanings in production.
- REC-01/REC-02 use only D-10's GitHub OAuth/intention supersession; REC-03 remains unlimited.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-11-SUMMARY.md` with approved production and retirement evidence.
</output>
