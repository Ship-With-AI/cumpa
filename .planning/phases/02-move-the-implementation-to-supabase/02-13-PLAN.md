---
phase: 02-move-the-implementation-to-supabase
plan: 13
type: execute
wave: 13
depends_on:
  - 02-12
files_modified:
  - tests/e2e/support-recovery.spec.ts
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/support-restore.spec.ts
  - services/support/tests/deployment.test.ts
  - services/support/tests/fulfillment.test.ts
  - services/support/tests/installations.test.ts
  - services/support/tests/postgres-payment.integration.test.ts
  - services/support/tests/postgres-recovery.integration.test.ts
  - services/support/tests/recovery.test.ts
autonomous: true
requirements:
  - PAY-01
  - PAY-02
  - PAY-03
  - PAY-04
  - SUP-01
  - SUP-02
  - SUP-03
  - SUP-04
  - SUP-05
  - REC-01
  - REC-02
  - REC-03
must_haves:
  truths:
    - "Every active assertion in support-recovery.spec.ts survives in the current payment or restore contract before the stale-named file is deleted."
    - "The migrated destination suites pass before and after deletion, with a one-to-one assertion inventory recorded in the 02-13 summary."
    - "Only tests for the already-retired legacy service disappear; active Supabase/workflow/package/restore coverage remains."
  artifacts:
    - path: "tests/e2e/support-payment.spec.ts"
      provides: "Workflow, canonical default-origin routing, mutation-guard, deployment-evidence, and payment contract coverage"
    - path: "tests/e2e/support-restore.spec.ts"
      provides: "OAuth, fixture-manifest, approval, promotion, restore, and non-enumeration coverage"
  key_links:
    - from: "tests/e2e/support-recovery.spec.ts"
      to: "tests/e2e/support-payment.spec.ts and tests/e2e/support-restore.spec.ts"
      via: "one-to-one title/assertion inventory followed by passing destination runs"
      pattern: "support-(payment|restore)"
  prohibitions:
    - "HIGH: Do not delete support-recovery.spec.ts until every active assertion is mapped and both destination files pass."
    - "Do not weaken, collapse, or replace assertions with source-text checks."
    - "Do not touch runtime/toolchain/package/docs/hosted state in this plan."
---

<objective>
Preserve every active recovery/workflow assertion in the durable payment/restore suites, then remove stale recovery and legacy-service tests.

Purpose: Complete the test-side D-19 cutover without discarding acceptance protection.
Output: One-to-one migrated E2E assertions and no obsolete service-test tree.
</objective>

<execution_context>
@GSD core/workflows/execute-plan.md
@GSD core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-12-SUMMARY.md
@tests/e2e/support-recovery.spec.ts
@tests/e2e/support-payment.spec.ts
@tests/e2e/support-restore.spec.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Inventory and migrate every active recovery assertion before deletion</name>
  <files>tests/e2e/support-recovery.spec.ts, tests/e2e/support-payment.spec.ts, tests/e2e/support-restore.spec.ts</files>
  <read_first>
    - `tests/e2e/support-recovery.spec.ts` as it exists after 02-07
    - `tests/e2e/support-payment.spec.ts`
    - `tests/e2e/support-restore.spec.ts`
    - `.planning/phases/02-move-the-implementation-to-supabase/02-08-SUMMARY.md`
  </read_first>
  <action>At execution time inventory every test title and substantive assertion in the post-02-08 `support-recovery.spec.ts`; do not assume its current pre-plan contents. Map workflow trigger/job boundary, canonical-origin derivation and exact-origin-aware evidence rules, text/plain route probes, hostile-executor ownership, mutation-guard ordering, deployment/evidence schema, and redaction assertions into `support-payment.spec.ts`. Map OAuth, exact fixture-manifest, separate approval, promotion, cleanup, restore, and non-enumeration assertions into `support-restore.spec.ts`. Reuse current helpers and keep behavioral assertions; do not replace them with grep/source-text-only checks. Record a one-to-one source-title/assertion to destination-title/assertion mapping in the 02-13 summary. Run both destination files and require all mapped assertions to pass while the source still exists. Only then delete `support-recovery.spec.ts`, run both destinations again, and prove no assertion/title in the inventory is unmatched.</action>
  <verify>
    <automated>npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/support-restore.spec.ts</automated>
  </verify>
  <done>Every active source assertion has one equivalent passing destination assertion before and after the stale file is removed.</done>
</task>

<task type="auto">
  <name>Task 2: Delete tests for the retired standalone service</name>
  <files>services/support/tests/deployment.test.ts, services/support/tests/fulfillment.test.ts, services/support/tests/installations.test.ts, services/support/tests/postgres-payment.integration.test.ts, services/support/tests/postgres-recovery.integration.test.ts, services/support/tests/recovery.test.ts</files>
  <action>After Task 1 passes, delete exactly the six legacy service test files. They test the runtime removed across 02-11 and 02-12 and must not be rewritten against Supabase. The two named `postgres-*.integration.test.ts` files are part of this obsolete service workspace; preserve all active root integration, Deno, payment, restore, workflow, and package tests outside the exact six-file list. Assert the six named files are absent and rerun the two migrated E2E destinations so deletion cannot mask an import or coverage dependency.</action>
  <verify>
    <automated>test ! -e services/support/tests/deployment.test.ts &amp;&amp; test ! -e services/support/tests/fulfillment.test.ts &amp;&amp; test ! -e services/support/tests/installations.test.ts &amp;&amp; test ! -e services/support/tests/postgres-payment.integration.test.ts &amp;&amp; test ! -e services/support/tests/postgres-recovery.integration.test.ts &amp;&amp; test ! -e services/support/tests/recovery.test.ts &amp;&amp; npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/support-restore.spec.ts</automated>
  </verify>
  <done>Only obsolete service tests are gone; the migrated active contract remains executable.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Stale test file → active suites | Assertion migration must preserve observable behavior before deletion. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-13-01 | Repudiation | assertion migration | mitigate | One-to-one title/assertion inventory plus passing destination runs before and after deletion. |
| T-02-13-02 | Tampering | test retirement | mitigate | Exact six-file legacy allowlist; active Supabase/workflow/package tests excluded. |
</threat_model>

<verification>Both destination suites pass before and after source deletion, every inventoried assertion maps one-to-one, and exactly six legacy service tests are absent.</verification>
<success_criteria>
- Active workflow/domain/evidence and OAuth/fixture/approval/restore assertions are preserved.
- D-19 removes stale recovery and legacy-service test surfaces without reducing behavior coverage.
- The plan modifies nine files, below the standard ten-file warning threshold.
</success_criteria>

<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-13-SUMMARY.md` when complete.</output>
