---
phase: 02-move-the-implementation-to-supabase
plan: 04
type: tdd
wave: 4
depends_on: [02-03]
files_modified:
  - src/contracts/api.ts
  - src/server/support-client.ts
  - src/server/capabilities.ts
  - src/server/routes.ts
  - src/web/api/client.ts
  - src/web/App.vue
  - src/web/components/SupportDialog.vue
  - tests/api/support.test.ts
  - tests/integration/support-dialog.spec.ts
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/support-recovery.spec.ts
  - tests/e2e/support-restore.spec.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
must_haves:
  truths:
    - "Every review launch and review capability remains anonymous, local-first, and unrestricted; authentication starts only after Support or Restore (D-04, PAY-02)."
    - "Support and Restore both open the returned hosted GitHub OAuth flow in a new tab while the workspace stays usable and the dialog waits/dismisses normally (D-03, D-05, D-07, D-12)."
    - "Only background hosted status observation monotonically promotes the existing machine-wide local state; redirects, tab closure, and unverified/network responses never verify or demote it (PAY-03, PAY-04, SUP-03)."
    - "The local/browser contract contains no email, magic-link, challenge, recovery token, OAuth session, GitHub profile, payer email, or legacy recovery route (D-08, D-09, D-19)."
    - "Previously persisted verified `SupportStateV1` files remain valid and suppress the prompt after upgrade (D-19, SUP-03)."
  artifacts:
    - path: "src/contracts/api.ts"
      provides: "Strict action-start and status schemas with recovery schemas removed"
      exports: ["SupportActionSchema", "SupportStartRequestSchema", "SupportStartResultSchema", "SupportStatusSchema"]
    - path: "src/server/support-client.ts"
      provides: "HTTPS-only bounded hosted start/status client"
      exports: ["HostedSupportClient", "createHostedSupportClient"]
    - path: "src/server/capabilities.ts"
      provides: "Local start(action)/refresh monotonic support capability"
      exports: ["SupportCapability", "createSupportCapability"]
    - path: "src/web/components/SupportDialog.vue"
      provides: "Optional hosted Support/Restore/waiting/verified dialog without email recovery state"
    - path: "tests/e2e/support-restore.spec.ts"
      provides: "OAuth restoration and local-store compatibility acceptance contract"
  key_links:
    - from: "src/web/components/SupportDialog.vue"
      to: "src/web/App.vue"
      via: "support and restore action events"
      pattern: "support|restore"
    - from: "src/web/App.vue"
      to: "src/web/api/client.ts"
      via: "startSupportAction then existing polling/visibility refresh"
      pattern: "startSupportAction|refreshSupportStatus"
    - from: "src/server/capabilities.ts"
      to: "src/server/support-store.ts"
      via: "verified-only monotonic promote"
      pattern: "promote.*verified"
    - from: "src/server/support-client.ts"
      to: "supabase/functions/support-api/index.ts"
      via: "POST start and GET status over configured HTTPS origin"
      pattern: "support-api/(?:start|status)"
  prohibitions:
    - "HIGH: No Supabase session/token/code, provider profile/email, service-role key, Stripe secret, or payment authority enters browser/local state/package."
    - "HIGH: No redirect, local browser claim, unverified response, or client action directly writes verified local state."
    - "No recovery alias, email fallback, dual API surface, feature gate, startup network dependency, or change to `SupportStateV1Schema` (D-02, D-04, D-08, D-19)."
---

<objective>
Cut Cumpa atomically from Phase 01 Checkout/email-recovery contracts to one hosted Supabase support-action capability while preserving the local-first support experience.

Purpose: Make the shipped package use the new hosted OAuth surface without hosting a Supabase session and without regressing machine-wide status, accessibility, or unrestricted review.
Output: RED/GREEN/REFACTOR tests, strict shared/local/browser contracts, server wiring, UI action flow, and renamed OAuth restore acceptance coverage.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-04-SUMMARY.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-05-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-03-SUMMARY.md
@src/server/support-store.ts
@src/server/support-client.ts
@src/server/capabilities.ts
@src/server/routes.ts
@src/contracts/api.ts
@src/web/api/client.ts
@src/web/App.vue
@src/web/components/SupportDialog.vue

<interfaces>
Clean local contract:
- `SupportActionSchema = z.enum(['support', 'restore'])`.
- `SupportStartRequestSchema = z.strictObject({ action: SupportActionSchema }).readonly()`.
- `SupportStartResultSchema` is a readonly discriminated union `{ kind: 'ready', flowUrl: z.string().url() } | { kind: 'unavailable' }`.
- `HostedSupportClient.start(action, installationId)` → `{ flowUrl } | undefined`; `status(installationId)` and `close()` retain the HTTPS/5-second/8-KiB behavior.
- `SupportCapability.start(action)` injects the store-owned installation ID; `refresh()` alone calls monotonic promotion.
- Loopback `POST /api/support/start` accepts only `{action}`; `GET /api/support/status` and `POST /api/support/refresh` remain bearer/Host/Origin protected.
- `SessionClient.startSupportAction(action)` parses strict request/response contracts.

Delete, do not alias: `SupportCheckoutResultSchema`, `SupportRecoveryRequestSchema`, `SupportRecoveryResultSchema`, `SupportRecoveryStatusSchema`; `checkout`, `requestRecovery`, `recoveryStatus`; browser `openSupportCheckout`, `requestSupportRecovery`, `getSupportRecoveryStatus`; loopback `/api/support/checkout`, `/api/support/recovery`, `/api/support/recovery-status`; `CUMPA_SUPPORT_PAYMENT_URL`. Keep `CUMPA_SUPPORT_SERVICE_URL` as the sole public HTTPS capability origin.
</interfaces>
</context>

<feature>
  <name>Anonymous local review with explicit hosted Support/Restore action handoff</name>
  <files>src/contracts/api.ts, src/server/support-client.ts, src/server/capabilities.ts, src/server/routes.ts, src/web/api/client.ts, src/web/App.vue, src/web/components/SupportDialog.vue, focused tests</files>
  <behavior>
    - Existing verified store → prompt suppressed without hosted call; schema unchanged.
    - Unverified ready workspace → optional dialog; Not now dismisses current session only; every review feature remains usable.
    - Support/Restore → strict loopback action → HTTPS hosted flow URL → new browser tab; workspace remains open.
    - Hosted unavailable/malformed/oversized/slow response → generic unavailable behavior; no local state change.
    - Waiting/visibility polling verified → store promotes once, dialog shows brief thank-you/closes; unverified/network result never demotes.
    - No email UI/recovery method/route/schema/token state remains; no Supabase auth material reaches loopback/browser/store.
  </behavior>
  <implementation>Drive the cutover from failing API/UI/e2e contracts, implement every caller in one clean change, then remove dead recovery/checkout state and rename the restoration acceptance file. Keep `support-store.ts` byte-for-byte unchanged unless a test proves an actual compatibility defect; do not migrate valid local state.</implementation>
</feature>

<tasks>

<task type="auto">
  <name>Task 1: RED — replace payment/recovery tests with hosted action contracts</name>
  <files>tests/api/support.test.ts, tests/integration/support-dialog.spec.ts, tests/e2e/support-payment.spec.ts, tests/e2e/support-recovery.spec.ts, tests/e2e/support-restore.spec.ts</files>
  <read_first>
    - tests/api/support.test.ts — current hosted client/capability/route/store behavior
    - tests/integration/support-dialog.spec.ts — real browser dialog/polling/accessibility oracle
    - tests/e2e/support-payment.spec.ts — optional payment/no-gating/package flow oracle
    - tests/e2e/support-recovery.spec.ts — restoration oracle whose email mechanism is superseded
    - src/server/support-store.ts — compatibility invariant that tests must preserve
  </read_first>
  <action>First rename the e2e restoration contract to `support-restore.spec.ts` and delete the old recovery test path in the same change. Write failing tests for every `<feature><behavior>` case: strict action input, injected installation ID, one HTTPS origin, response/time/size bounds, abort, no direct verification, monotonic refresh, already-verified startup without network, Support/Restore new-tab handoff, waiting/dismissal/visibility refresh, generic unavailable behavior, accessibility/focus/inert preservation, second-installation restore, and absence of email/recovery controls. Add a packed-contract assertion that no provider secret/auth token or old recovery API string ships. Run focused tests and require RED against the existing Checkout/email interfaces.</action>
  <acceptance_criteria>
    - Tests fail for the intended obsolete contracts and missing action-start contract.
    - Test behavior covers every caller layer and existing verified-store compatibility.
    - No test treats browser redirect, tab closure, or local action as payment authority.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/api/support.test.ts &amp;&amp; npx playwright test --config=tests tests/integration/support-dialog.spec.ts</automated>
  </verify>
  <done>New action/OAuth behavior is specified and demonstrably RED; commit `test(02-04): specify hosted support action cutover`.</done>
</task>

<task type="auto">
  <name>Task 2: GREEN — cut shared contracts, hosted client, capability, and loopback routes</name>
  <files>src/contracts/api.ts, src/server/support-client.ts, src/server/capabilities.ts, src/server/routes.ts, tests/api/support.test.ts</files>
  <read_first>
    - tests/api/support.test.ts — RED transport/capability/route contract
    - src/contracts/api.ts — readonly strict Zod conventions and current support schemas
    - src/server/support-client.ts — HTTPS validation, AbortController, timeout, 8 KiB response parser
    - src/server/capabilities.ts — `SupportCapability`, `createSupportCapability`, monotonic `promote`
    - src/server/routes.ts — bearer-protected support block and generic malformed request handling
  </read_first>
  <action>Implement the exact `<interfaces>` contract. Replace rather than extend old support schemas and methods. In `createHostedSupportClient`, validate one HTTPS service origin, POST strict action+installation to fixed `/functions/v1/support-api/start`, GET fixed status with encoded installation ID, keep AbortController/five-second timeout/8-KiB ceiling, and accept only same configured HTTPS origin for returned `flowUrl` (or an explicitly locked allowlisted Stripe URL only if the hosted API contract requires it; do not accept arbitrary redirects). In `createSupportCapability`, remove transient recovery state and expose only `status`, `start`, `refresh`, `close`; inject the store installation ID server-side and keep `promote` verified-only. Replace the three obsolete routes and Checkout route with one strict `/api/support/start`; retain status/refresh and all existing loopback Host/Origin/bearer/body guards. Remove `CUMPA_SUPPORT_PAYMENT_URL`. Do not touch store schema/persistence or ordinary capability authorization.</action>
  <acceptance_criteria>
    - Focused API tests pass for start/status/refresh/abort/bounds/monotonicity and already-verified compatibility.
    - Old schemas, methods, route registrations, recovery state, and payment URL environment lookup no longer exist in shared/server production code.
    - Loopback accepts only `support|restore`; hosted request owns installation ID and local browser never supplies it.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/api/support.test.ts tests/api/support-store.test.ts tests/api/security.test.ts</automated>
  </verify>
  <done>The server-side clean cutover is green with store/security regressions intact; commit `feat(02-04): cut support capability to Supabase actions`.</done>
</task>

<task type="auto">
  <name>Task 3: GREEN/REFACTOR — migrate browser callers and delete email state</name>
  <files>src/web/api/client.ts, src/web/App.vue, src/web/components/SupportDialog.vue, tests/integration/support-dialog.spec.ts, tests/e2e/support-payment.spec.ts, tests/e2e/support-restore.spec.ts</files>
  <read_first>
    - tests/integration/support-dialog.spec.ts — RED rendered behavior/accessibility contract
    - tests/e2e/support-payment.spec.ts — RED payment workspace contract
    - tests/e2e/support-restore.spec.ts — RED OAuth restoration contract
    - src/web/api/client.ts — closure-held loopback bearer and current support methods
    - src/web/App.vue — support refs/handlers and visibility polling lifecycle
    - src/web/components/SupportDialog.vue — focus trap, modes, busy/waiting/verified UI
  </read_first>
  <action>Replace browser client support methods with `startSupportAction(action)` using the strict shared schemas. In `App.vue`, route both Support and Restore through that method, open the returned URL in a new tab, keep the dialog in cancellable waiting state, reuse existing 2/3/5/8/10 then 15-second and visibility refresh behavior, and allow only refresh to transition verified/thank-you. Remove all email, recovery-pending, challenge, submission, and recovery-status refs/handlers/timers. In `SupportDialog.vue`, preserve primary Support $49.99 hierarchy, Restore/Not now secondary actions, compact no-gating copy, focus restoration, Tab containment, Escape/dismiss, inert background, busy gating, waiting, verified, and thank-you behavior; Restore emits the same typed hosted action rather than displaying an email form. Remove unused imports/callers, do not keep aliases. Turn the full focused test set green, then delete weightless duplicate UI state without changing behavior.</action>
  <acceptance_criteria>
    - Both actions open hosted flow URLs in a new tab and never navigate/close the local workspace.
    - UI remains dismissible/accessible and review features remain usable in initial, waiting, unavailable, and delayed states.
    - Verified polling closes the dialog and persists through unchanged store authority; no browser action directly verifies.
    - No email/recovery state or old API caller survives in `src/**`; existing recovery-named draft concepts unrelated to support are not touched.
  </acceptance_criteria>
  <verify>
    <automated>npx vitest run tests/api/support.test.ts tests/api/support-store.test.ts &amp;&amp; npx playwright test --config=tests tests/integration/support-dialog.spec.ts</automated>
  </verify>
  <done>All Cumpa callers use the new hosted action contract, focused tests pass, and old support recovery UI/API state is deleted; commit `feat(02-04): move support and restore to hosted OAuth` plus optional minimal refactor commit.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/contracts/api.ts`: `SupportActionSchema`, `SupportStartRequestSchema`, `SupportStartResultSchema`, retained `SupportStatusSchema`; obsolete support recovery/checkout exports removed.
- `src/server/support-client.ts`: `HostedSupportClient.start/status/close` and `createHostedSupportClient` using one HTTPS capability origin.
- `src/server/capabilities.ts`: `SupportCapability.start/status/refresh/close`, verified-only `createSupportCapability` promotion.
- `src/server/routes.ts`: `/api/support/start`, `/api/support/status`, `/api/support/refresh` only.
- `src/web/api/client.ts`: `SessionClient.startSupportAction` and retained status/refresh methods.
- `src/web/App.vue` / `SupportDialog.vue`: hosted Support/Restore action flow without email recovery.
- `tests/e2e/support-restore.spec.ts`: renamed OAuth restore contract; old `support-recovery.spec.ts` removed.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → authenticated loopback API | Browser input is untrusted but protected by existing Host/Origin/bearer boundary. |
| Loopback server → hosted Supabase capability | Public HTTPS responses are untrusted until bounded and schema/origin validated. |
| Hosted boolean status → machine-wide JSON | Only verified status may monotonically promote durable local state. |
| Hosted browser tab → Cumpa workspace | OAuth/Stripe redirects must never return auth/payment authority to loopback. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-04-LA | Elevation of Privilege | HIGH | `/api/support/start` | mitigate | Existing bearer/Host/Origin guards plus strict action-only body; installation identity injected server-side. |
| T-02-04-HR | Spoofing/Tampering | HIGH | hosted responses | mitigate | HTTPS-only fixed origin, five-second abort, 8 KiB ceiling, strict schemas, flow URL origin allowlist, generic unavailable result. |
| T-02-04-LS | Elevation of Privilege | HIGH | local support store | mitigate | Only refresh + hosted `verified` calls unchanged monotonic `promote`; redirect/action/unverified/network cannot write/demote. |
| T-02-04-AU | Information Disclosure | HIGH | browser/store/package | mitigate | No OAuth/token/code/profile/email/secret fields in contracts or state; packed artifact assertion. |
| T-02-04-FG | Elevation of Privilege | HIGH | review capabilities | mitigate | Support status controls dialog visibility only; anonymous review APIs and UI remain constructed identically. |
| T-02-04-DOS | Denial of Service | MEDIUM | polling/hosted calls | mitigate | Existing bounded cadence, visibility coalescing, timeout, close abort; failures leave workspace usable. |

Execution MUST stop if any HIGH threat remains unresolved or uncovered by focused tests. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
Run only the focused API/store/security and rendered dialog contracts during execution. Hosted OAuth/Stripe truth is not claimed here; Plan 02-05 must prove it against the development project before legacy deletion.
</verification>

<success_criteria>
- D-03/D-04 keep the loopback anonymous and token-free.
- D-07–D-12 replace email recovery with hosted GitHub actions while retaining unlimited restore and all PAY/SUP behavior.
- D-19 cleanly migrates every Cumpa caller with no alias/fallback.
- Existing verified machine-wide state survives without migration.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-04-SUMMARY.md` with RED/GREEN/REFACTOR evidence and an explicit caller-removal inventory.
</output>
