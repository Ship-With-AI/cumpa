---
phase: 02-move-the-implementation-to-supabase
plan: 15
type: execute
wave: 15
depends_on: [02-14]
files_modified:
  - scripts/verify-supabase-support.mjs
  - scripts/verify-production-artifacts.mjs
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/package-assets.spec.ts
  - docs/support-service-operations.md
  - .planning/phases/02-move-the-implementation-to-supabase/02-15-RETIREMENT-EVIDENCE.md
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
must_haves:
  truths:
    - "A deterministic retirement scan rejects legacy runtime/provider names in ship-relevant tracked sources while immutable planning/history/evidence and named scanner-contract tests may describe denied fixtures (D-19)."
    - "The scanner contract can allow one caller-supplied canonical public origin only in its typed field, enumerated route URLs, or configured-launcher assignment; 02-15 exercises this with table-driven positive/negative fixtures but does not require a configured release before 02-16 (D-20)."
    - "Retirement scans tracked files, configured-absent rebuilt dist, tarball inventory, and extracted package; because no release origin is embedded yet, any Supabase host, bare project ref, credential, OAuth/PII, provider secret, or raw protected value fails."
    - "Focused scanner fixtures prove the exact supplied canonical-origin content passes and arbitrary host, bare-ref, duplicate/missing launcher, runtime, workflow, dist, inventory, extracted-package, and protected-value leaks fail."
    - "Final review digest-binds six distinct inputs including the 02-17 local/package/security record; only final-review may write 02-17-FINAL-EVIDENCE.md."
  artifacts:
    - path: "scripts/verify-supabase-support.mjs"
      provides: "Scope-aware retirement scanner, exact-origin-aware evidence policy, and six-input final verifier"
    - path: "scripts/verify-production-artifacts.mjs"
      provides: "Exact-origin-aware release scanner capability and configured-absent retirement mode; actual configured launcher proof is deferred to 02-16"
    - path: "tests/e2e/package-assets.spec.ts"
      provides: "Self-scan and strict dist/package canonical-origin and protected-value regressions"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-15-RETIREMENT-EVIDENCE.md"
      provides: "Redacted immutable retirement scope, package, and scan evidence"
  key_links:
    - from: "caller-supplied canonical origin fixture"
      to: "typed evidence/route fields and configured-launcher fixture"
      via: "one exact allowlist value plus enumerated URL paths; all remaining Supabase hosts and bare refs fail"
      pattern: "https://.*\\.supabase\\.co"
    - from: "scripts/verify-supabase-support.mjs --retirement-review"
      to: "tracked sources and configured-absent dist/tarball/extracted package"
      via: "versioned scope classifier plus strict no-host packaged-output scans; configured positive scan waits for 02-16"
      pattern: "retirement"
    - from: "six immutable evidence records"
      to: "02-17-FINAL-EVIDENCE.md"
      via: "path/kind/version/digest plus canonical origin and immutable GitHub run/commit lineage validation; derived fingerprint consistency is supplemental"
      pattern: "local-package-security"
  prohibitions:
    - "HIGH: Do not reinstate a blanket denial of the canonical public origin or allow arbitrary `.supabase.co` content."
    - "HIGH: Do not allow a project ref outside an exact canonical origin/derived route occurrence, even in evidence prose."
    - "Do not add a compatibility scanner, path-wide protected-value exception, fifth-input final mode, or second final-evidence writer."
---

<objective>
Prove clean retirement with strict exact-origin-aware repository/package/evidence scans and implement the six-input final-evidence contract.

Purpose: Make D-19/D-20 auditable without rejecting the required public routing identifier or allowing a real runtime, package, credential, or bare-ref leak.
Output: Tested scanner/verifier modes, retirement evidence, operations policy, and final-review interface ready for 02-17.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-14-SUMMARY.md
@scripts/verify-supabase-support.mjs
@scripts/verify-production-artifacts.mjs
@tests/e2e/support-payment.spec.ts
@tests/e2e/package-assets.spec.ts
@docs/support-service-operations.md
</context>

<interfaces>
- `node scripts/verify-supabase-support.mjs --retirement-review --output PATH` writes only `02-15-RETIREMENT-EVIDENCE.md` after source, value, configured-absent dist, inventory, and extracted-package checks pass.
- `node scripts/verify-production-artifacts.mjs` scans ordinary configured-absent output and rejects every Supabase origin/ref. `--expected-support-origin ORIGIN --require-configured-launcher PATH` enables exact-origin configured-release mode; 02-15 implements and fixture-tests that interface, while 02-16 is the first plan allowed to invoke it against a real generated launcher.
- `node scripts/verify-supabase-support.mjs --local-package-security-review --output PATH` writes only `02-17-LOCAL-PACKAGE-SECURITY-EVIDENCE.md` from the credential-free 02-17 suite.
- `node scripts/verify-supabase-support.mjs --final-review --test-deployment PATH --acceptance PATH --promotion PATH --retirement PATH --release PATH --local-package-security PATH --output PATH` requires six distinct versioned records and alone writes `02-17-FINAL-EVIDENCE.md`.
- `node scripts/verify-supabase-support.mjs --check-final PATH --test-deployment PATH --acceptance PATH --promotion PATH --retirement PATH --release PATH --local-package-security PATH` is read-only and recomputes all bindings.
- Exact-origin scan algorithm: when a caller supplies `public_origin`, validate it equals `https://<project-ref>.supabase.co`; allow that complete string and URLs formed by appending only documented Auth/function paths in typed fields/content; remove those complete occurrences before denying the extracted ref, any remaining `.supabase.co` hostname, credentials, OAuth/PII, provider secrets, and raw identifiers. With no supplied origin, allow no Supabase hostname or ref.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement scoped retirement and exact canonical-origin package/evidence scans</name>
  <files>scripts/verify-supabase-support.mjs, scripts/verify-production-artifacts.mjs, tests/e2e/support-payment.spec.ts, tests/e2e/package-assets.spec.ts, .planning/phases/02-move-the-implementation-to-supabase/02-15-RETIREMENT-EVIDENCE.md</files>
  <read_first>
    - `.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md` Evidence Package Scanner Contract and Concrete File Symbol Impacts
    - `scripts/verify-supabase-support.mjs`
    - `scripts/verify-production-artifacts.mjs`
    - `tests/e2e/support-payment.spec.ts`
    - `tests/e2e/package-assets.spec.ts`
  </read_first>
  <behavior>
    - A denied legacy runtime/provider name in ship-relevant source, workflow, non-scanner script, operations docs, or package manifest fails with classified path and rule ID.
    - Immutable planning/history/evidence and the exact scanner/test files may describe denied name fixtures, but never receive a protected-value exception.
    - With an expected origin, the exact canonical origin passes in `public_origin`, documented route fields, and a configured-launcher fixture; a missing/duplicate/different launcher, arbitrary Supabase host, bare ref outside an allowed occurrence, credential, OAuth/PII, provider secret, or raw ID fails.
    - Configured-absent rebuilt dist, tarball inventory, and extracted package contain no support-origin launcher assignment and no Supabase host/ref; actual positive configured launcher proof belongs to 02-16.
    - Retirement evidence records commit, policy/scope version and digests, scanned path counts, exact purpose exclusions, configured-absent status, build/package digests, and zero violations.
  </behavior>
  <action>Write table-driven failing fixtures first. Keep the existing Git NUL-delimited tracked-file inventory and deterministic classifier; exclude only immutable `.planning/**` descriptions and the exact scanner/contract files from denied-name checks. Apply protected-value and legacy scans to every tracked file, Phase 02 evidence record, configured-absent rebuilt `dist/**`, `npm pack --dry-run --json` inventory, and extracted package. Update `scripts/verify-production-artifacts.mjs` with two explicit modes: no expected origin requires configured absence and rejects every Supabase host/ref; caller-supplied `--expected-support-origin` plus `--require-configured-launcher PATH` validates canonical shape, requires exactly one launcher assignment, removes only complete allowed origin/route occurrences, then rejects the bare ref elsewhere and every other protected/legacy class. Fixture-test both modes, including an allowed origin containing the public ref plus the same ref standalone, a second host, duplicate/missing launcher, credentials, OAuth/PII, provider IDs/secrets, raw fixture IDs, and misleading names. Do not scan a real configured launcher or require origin embedding in 02-15; `scripts/build-bin.mjs` does not add it until 02-16. Run retirement in configured-absent mode and emit 02-15 evidence with no standalone ref.</action>
  <verify>
    <automated>npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/package-assets.spec.ts &amp;&amp; node scripts/verify-supabase-support.mjs --retirement-review --output .planning/phases/02-move-the-implementation-to-supabase/02-15-RETIREMENT-EVIDENCE.md</automated>
  </verify>
  <done>Retirement passes in configured-absent mode, scanner capability accepts only an explicitly supplied canonical origin in fixture-tested locations, and actual configured launcher proof is ready for 02-16.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Require six immutable inputs and canonical lineage for final evidence</name>
  <files>scripts/verify-supabase-support.mjs, tests/e2e/support-payment.spec.ts, docs/support-service-operations.md</files>
  <behavior>
    - Final review rejects any missing, duplicate, unknown, wrong-kind, wrong-version, mutable, or digest-mismatched input among test deployment, acceptance, promotion, retirement, release, and local/package/security evidence.
    - Cross-record validation requires one exact canonical `public_origin`, only its enumerated derived routes, one immutable GitHub run/commit promotion lineage, zero post-cleanup/live authority, and a valid release approval attestation; any fingerprint must be internally derived and is supplemental correlation only.
    - Final output stores six path/digest bindings and redacted conclusions; canonical origin/route strings are allowed, but a bare ref, other Supabase host, credential, OAuth/PII, provider secret, fixture handle, or raw identifier fails.
    - `--local-package-security-review` runs the configured-absent local suite, two-cycle database proof, Deno functions, retirement/package scanner, and package smoke without accepting hosted/provider/deployment environment values.
    - `--final-review` alone writes final evidence; `--check-final` is read-only; the former five-input invocation is rejected.
  </behavior>
  <action>Add focused parser, schema, digest, exact-origin, and lineage tests first, including the former five-input invocation as a required failure. Implement the three interfaces exactly as named above. The local collector must require output basename `02-17-LOCAL-PACKAGE-SECURITY-EVIDENCE.md`, run and summarize the repository's exact Vitest and Playwright suites, two clean Supabase reset/test/list/lint cycles after `db start`, Deno function tests, build/package smoke, retirement review, configured-absent support behavior, and protected-value checks. Record only command identities, statuses, counts, commit/build/package/scope digests, and zero-violation conclusions. Final review must validate the immutable 02-16 release approval against release-record SHA-256, run ID, and package digest and must apply the same exact-origin scan to all six inputs and its output. Update operations documentation to state that 02-15 retirement proves configured absence and scanner capability, while only 02-16 may perform the first actual configured-launcher scan.</action>
  <verify>
    <automated>npx playwright test --config=tests tests/e2e/support-payment.spec.ts</automated>
  </verify>
  <done>The final verifier rejects five records, digest-binds the sixth, enforces one canonical public-origin lineage, and has one final-evidence writer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Tracked descriptions/tests → shipped runtime | Purpose text may name denied fixtures but cannot mask an implementation. |
| Canonical public routing → scans | One exact public origin is allowed; every other host/ref occurrence remains untrusted. |
| Repository → dist/tarball/extracted package | Packaging can reintroduce deleted code or protected values independently. |
| Six immutable records → final evidence | Omission, substitution, mutation, or path aliasing must fail closed. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-15-SC | Tampering | retirement scope | mitigate | Versioned classifier and exclusion digest with unknown-entry rejection. |
| T-02-15-OR | Spoofing | public origin | mitigate | Caller-supplied exact canonical origin required across records and package. |
| T-02-15-ID | Information Disclosure | scans/evidence | mitigate | Strip only full allowed URL occurrences before bare-ref and protected-value denial. |
| T-02-15-PK | Tampering | package | mitigate | Rebuild, inventory, extract, and require configured absence in retirement; fixture-test exact configured mode for 02-16. |
| T-02-15-FI | Repudiation | final evidence | mitigate | Six distinct immutable path/digest bindings and read-only recomputation. |
</threat_model>

<verification>Focused scanner/verifier tests pass; retirement evidence is immutable; canonical origin is required and narrowly allowed; arbitrary hosts, bare refs, and protected values fail; six-input final interfaces exist before 02-17.</verification>
<success_criteria>
- D-19 clean retirement is proven without a fallback or archive.
- D-20 exact canonical routing is permitted in package/evidence while all other ref/host exposure fails.
- PAY-04/D-18 credential, provider, OAuth/PII, and protected-value exclusions remain strict.
- Final verification cannot omit the local/package/security record or bypass immutable lineage.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-15-SUMMARY.md` when complete.</output>
