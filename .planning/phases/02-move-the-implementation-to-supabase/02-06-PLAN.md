---
phase: 02-move-the-implementation-to-supabase
plan: 06
type: execute
wave: 6
depends_on: [02-05]
files_modified:
  - services/support/package.json
  - services/support/package-lock.json
  - services/support/tsconfig.json
  - services/support/vitest.config.ts
  - services/support/scripts/migrate.mjs
  - services/support/scripts/test-postgres.mjs
  - services/support/migrations/001_init.sql
  - services/support/src/app.ts
  - services/support/src/config.ts
  - services/support/src/db.ts
  - services/support/src/email.ts
  - services/support/src/fulfillment.ts
  - services/support/src/recovery.ts
  - services/support/src/recovery-store.ts
  - services/support/src/schema.ts
  - services/support/src/server.ts
  - services/support/src/routes/installations.ts
  - services/support/src/routes/recovery.ts
  - services/support/src/routes/stripe-webhook.ts
  - services/support/tests/deployment.test.ts
  - services/support/tests/fulfillment.test.ts
  - services/support/tests/installations.test.ts
  - services/support/tests/postgres-payment.integration.test.ts
  - services/support/tests/postgres-recovery.integration.test.ts
  - services/support/tests/recovery.test.ts
  - render.yaml
  - scripts/test-support-e2e-postgres.mjs
  - package.json
  - package-lock.json
  - .github/workflows/deploy-supabase-production.yml
  - docs/support-service-operations.md
  - .planning/phases/02-move-the-implementation-to-supabase/02-06-PRODUCTION-EVIDENCE.md
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
user_setup:
  - service: supabase
    why: "Production is the second and only other hosted Supabase project (D-14)."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Protected GitHub production environment secret"
      - name: SUPABASE_PRODUCTION_PROJECT_REF
        source: "Production Supabase project reference; protected GitHub environment secret/variable"
      - name: SUPABASE_PRODUCTION_DB_PASSWORD
        source: "Production database password; protected GitHub environment secret"
    dashboard_config:
      - task: "Enable GitHub only, disable email/magic-link, and configure exact production Auth/site/callback URLs"
        location: "Production Supabase → Authentication"
  - service: github
    why: "A distinct production OAuth App is required by the production callback URL."
    env_vars:
      - name: GITHUB_OAUTH_CLIENT_ID
        source: "Production GitHub OAuth App; configure only in production Supabase Auth"
      - name: GITHUB_OAUTH_CLIENT_SECRET
        source: "Production GitHub OAuth App; configure only in production Supabase Auth"
    dashboard_config:
      - task: "Create/confirm production OAuth App with exact production Supabase Auth callback and no repository scopes"
        location: "GitHub Developer settings → OAuth Apps"
      - task: "Protect the `production` GitHub environment and restrict deploy workflow secrets to main"
        location: "GitHub repository Settings → Environments"
  - service: stripe
    why: "Production Checkout and webhook authority require live server-only configuration."
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe live secret key; production Supabase function secret only"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Production Supabase webhook endpoint live signing secret"
      - name: STRIPE_PRICE_ID
        source: "One live one-time USD $49.99 Price"
    dashboard_config:
      - task: "Create/confirm fixed live Price and production webhook endpoint/events"
        location: "Stripe live mode"
must_haves:
  truths:
    - "Only after approved 02-05 hosted proof, every standalone Render/Fastify/direct-pg/Resend/email-recovery artifact and caller is deleted, with no shim, alias, fallback, import, dual write, or reconciliation path (D-02, D-08, D-16, D-19)."
    - "Every push/merge to `main` automatically applies production migrations before deploying all Edge Functions from the same commit through a protected GitHub environment (D-15)."
    - "The actual production project records the migration and function commit/version, and the public smoke path proves anonymous status/start plus fail-closed behavior without exposing secrets."
    - "The final packed Cumpa contains only the public hosted capability URL/configuration and no service implementation, OAuth session/profile/email, Stripe/Supabase/GitHub secret, or obsolete endpoint string (PAY-04, SUP-05)."
    - "Existing machine-local verified files remain accepted after the final package cutover; production deployment introduces no hosted legacy-data migration (D-16, D-19)."
  artifacts:
    - path: ".github/workflows/deploy-supabase-production.yml"
      provides: "Protected main-triggered schema-before-functions production deployment"
    - path: "docs/support-service-operations.md"
      provides: "Final Supabase-only operations/retirement/rollback-by-forward-fix runbook"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-06-PRODUCTION-EVIDENCE.md"
      provides: "Redacted production CI/schema/function/smoke/package/legacy-retirement evidence"
  key_links:
    - from: ".github/workflows/deploy-supabase-production.yml"
      to: "supabase/migrations and supabase/functions"
      via: "main push, protected production environment, db push before function deploy"
      pattern: "branches:.*main|db push|functions deploy"
    - from: "02-05-HOSTED-EVIDENCE.md"
      to: "legacy deletion"
      via: "hard precondition checked before removal"
      pattern: "approved"
    - from: "production workflow run"
      to: "02-06-PRODUCTION-EVIDENCE.md"
      via: "run URL/commit/migration/function/smoke observations"
      pattern: "20260814000000"
  prohibitions:
    - "HIGH: Never delete legacy artifacts before the approved development evidence gate; never retain them after deletion as fallback or compatibility code."
    - "HIGH: Production workflow must not deploy from feature branches, deploy functions before migrations, echo protected values, use development project refs, or continue after schema failure."
    - "No destructive database rollback, data import, provider dual-run, manual promotion as the normal production path, Supabase Git integration, or email/operator recovery path."
---

<objective>
Delete the complete legacy backend and finish the Supabase-only production cutover through protected main-triggered CI with actual deployment evidence.

Purpose: Fulfill the clean-cutover contract only after equivalent hosted behavior is proven, leaving one production authority and one reproducible deployment path.
Output: Removed legacy service/deployment/dependencies/scripts, protected production workflow, final Supabase operations runbook, and approved production evidence.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-05-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-05-HOSTED-EVIDENCE.md
@docs/support-service-operations.md
@package.json
@render.yaml
@services/support/package.json

<interfaces>
Production workflow contract (`.github/workflows/deploy-supabase-production.yml`):
- Trigger: `push` to `main` only for `supabase/**`, workflow, or relevant lock/tooling changes; optional `workflow_dispatch` may rerun the exact main commit but is not manual promotion.
- Permissions: `contents: read`; environment: protected `production`; concurrency serializes production deploys without cancelling an in-flight migration.
- Exact tooling: `npm ci`, lockfile-pinned `npx supabase`; no floating install.
- Target guard: production project ref must be non-empty and unequal to documented development ref.
- Ordered steps: link production → `supabase db push` → migration list/check → deploy `support-api` → `support-flow` → `stripe-webhook` → function list/non-mutating smoke.
- Failure: schema failure stops all function deployment; forward-fix migration is recovery, not destructive rollback.

Legacy removal includes the entire `services/support/**` tree, `render.yaml`, `scripts/test-support-e2e-postgres.mjs`, service workspace references/scripts/dependencies in root manifests, Render/Postgres/Resend/Payment-Link environment instructions, and obsolete generated-package assertions. Root Fastify remains because it is the required local loopback server; only the standalone hosted Fastify service is removed.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Delete every legacy backend surface after evidence approval</name>
  <files>services/support/**, render.yaml, scripts/test-support-e2e-postgres.mjs, package.json, package-lock.json, docs/support-service-operations.md</files>
  <read_first>
    - .planning/phases/02-move-the-implementation-to-supabase/02-05-HOSTED-EVIDENCE.md — must contain explicit approved development gate before any deletion
    - .planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md — complete legacy removal inventory
    - package.json — root scripts/workspace/package boundary
    - package-lock.json — service-only dependency entries to remove without removing local Fastify
    - render.yaml — Render resources/env map to retire
    - services/support/package.json — standalone Fastify/pg/Stripe/Resend-era dependency boundary
    - docs/support-service-operations.md — final retirement and secret rotation checklist
  </read_first>
  <action>First assert the 02-05 evidence contains explicit approval for real development Supabase/GitHub/Stripe/package behavior; stop otherwise. Remove the entire tracked `services/support` tree, `render.yaml`, and PostgreSQL wrapper. Remove root scripts/workspace/lock entries that existed only for that service while retaining Fastify and other dependencies required by Cumpa's local loopback server. Remove old generated-artifact references and all support-specific callers/config for Render, direct `pg`, Resend, email recovery, HMAC recovery keys, static Payment Link, old route names, and old service migrations. Update the operations runbook to Supabase-only steady state and include confirmed retirement/secret revocation steps for any externally created Render/Postgres/Resend resources without presenting them as fallback. Search source, tests, scripts, manifests, workflows, docs, and packed inventory for obsolete support surfaces; distinguish unrelated Cumpa draft recovery symbols from support recovery and do not delete those. Do not add compatibility aliases, migration/import jobs, dual writes, or dead shims.</action>
  <acceptance_criteria>
    - Every listed legacy file is deleted only after evidence approval.
    - No standalone hosted Fastify, `pg`, Render, Resend, support-email/magic-link/token, static Payment Link, or old support route/caller remains.
    - Local loopback Fastify and unrelated draft-recovery features remain intact.
    - Root lockfile contains no dependency reachable only from the removed service; Supabase CLI tooling remains pinned for CI.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --environment development --verify-clean-cutover --evidence .planning/phases/02-move-the-implementation-to-supabase/02-05-HOSTED-EVIDENCE.md</automated>
  </verify>
  <done>Legacy hosted implementation and every caller/config/test/dependency are gone with no fallback surface; commit `refactor(02-06): remove legacy support backend`.</done>
</task>

<task type="auto">
  <name>Task 2: Install protected main-triggered production deployment</name>
  <files>.github/workflows/deploy-supabase-production.yml, docs/support-service-operations.md, .planning/phases/02-move-the-implementation-to-supabase/02-06-PRODUCTION-EVIDENCE.md</files>
  <read_first>
    - supabase/config.toml — function names/JWT declarations
    - supabase/migrations/20260814000000_support_authority.sql — schema that must precede functions
    - package.json — exact pinned Supabase CLI command
    - .planning/phases/02-move-the-implementation-to-supabase/02-05-HOSTED-EVIDENCE.md — development refs/versions and negative-case baseline
    - docs/support-service-operations.md — production secrets/callback/webhook/forward-fix/evidence instructions
  </read_first>
  <action>Create the workflow exactly as `<interfaces>` specifies using protected environment secrets and minimal permissions. Never print secret-bearing commands/URLs; mask provider values and record only ref suffixes, commit, migration/function versions, and run URL. Production database push must be an explicit blocking step and function deploy steps must be unreachable if it fails. Add a non-mutating post-deploy check for migration `20260814000000`, all three function versions, anonymous status/start response shape, generic invalid request, and no privileged fields. Initialize the production evidence template with required fields and update the runbook with workflow trigger, environment protection, live GitHub/Stripe configuration, secret rotation, incident correlation, forward-fix, and legacy provider deletion confirmation. Do not use Supabase Git integration or dashboard schema edits.</action>
  <acceptance_criteria>
    - Pushes to main automatically invoke a protected production job using the pinned CLI.
    - Workflow serializes deploys and always applies/validates schema before functions.
    - Development/production target confusion, missing secret, schema failure, and smoke failure stop the job.
    - Workflow output/evidence contains no credentials, auth/payment tokens, PII, or full opaque installation/intent values.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --verify-production-workflow .github/workflows/deploy-supabase-production.yml</automated>
  </verify>
  <done>Production deployment is reproducible from protected main with schema-first fail-closed ordering; commit `ci(02-06): deploy Supabase support authority from main`.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve actual production deployment and final retirement</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-06-PRODUCTION-EVIDENCE.md, docs/support-service-operations.md</files>
  <read_first>
    - .github/workflows/deploy-supabase-production.yml — exact protected deploy contract
    - .planning/phases/02-move-the-implementation-to-supabase/02-06-PRODUCTION-EVIDENCE.md — production run evidence awaiting approval
    - docs/support-service-operations.md — production smoke and provider retirement checklist
  </read_first>
  <action>Trigger and inspect the exact protected main workflow and provider retirement sequence below. Record only redacted deployment identifiers and the dated approval or exact failure; do not bypass CI with manual schema/function promotion.</action>
  <what-built>Supabase-only repository, protected main deployment workflow, production Postgres/Auth/Edge Functions configuration, final package/caller cleanup, and redacted production evidence contract.</what-built>
  <how-to-verify>
    1. Merge/push the exact reviewed commit to `main` so the automatic protected `production` workflow runs; do not manually reproduce its deployment steps.
    2. Confirm the job targeted the recorded production ref, applied migration `20260814000000`, then deployed `support-api`, `support-flow`, and `stripe-webhook` from the same commit.
    3. Confirm the post-deploy smoke returned only allowed anonymous start/status shapes and invalid input failed generically.
    4. Perform one production GitHub OAuth → live exact USD $49.99 Checkout handoff without completing an unintended charge; validate configured live webhook endpoint/secret with provider test delivery where Stripe permits.
    5. Confirm packed Cumpa/local verified-state compatibility and absence of legacy/service/secret/auth/profile/email content.
    6. Confirm external Render service/database and Resend/recovery credentials are deleted/revoked only after the production checks pass.
    7. Record workflow URL, commit, redacted project/function/migration observations, smoke results, retirement confirmations, date/operator in production evidence.
  </how-to-verify>
  <acceptance_criteria>
    - Actual main-triggered production CI proves schema application before functions; file inspection/build success is insufficient.
    - Production OAuth/Checkout/webhook configuration matches locked GitHub-only and $49.99 authority behavior.
    - No legacy provider, service source, route, dependency, secret, or recovery-email path remains live or tracked.
    - Evidence is approved and contains no sensitive value or PII.
  </acceptance_criteria>
  <verify>
    <automated>gh run list --workflow deploy-supabase-production.yml --branch main --limit 1</automated>
    <human-check>Open the successful run and production dashboards, complete steps 2–7, then type `approved` only when the evidence file is complete.</human-check>
  </verify>
  <done>Production Supabase deployment and complete legacy retirement are approved; Phase 02 may close.</done>
  <resume-signal>Type `approved` or identify the failed deployment/retirement check.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- Deleted: complete `services/support/**`, `render.yaml`, `scripts/test-support-e2e-postgres.mjs`, and all root lock/script references reachable only from them.
- `.github/workflows/deploy-supabase-production.yml`: protected `main` production workflow with schema-before-functions order.
- `docs/support-service-operations.md`: final Supabase-only runbook and confirmed legacy retirement checklist.
- `.planning/phases/02-move-the-implementation-to-supabase/02-06-PRODUCTION-EVIDENCE.md`: actual production CI/schema/function/smoke/package/retirement evidence and approval.
- No compatibility symbol, alias, import/reconciliation job, fallback route, or dual-write artifact is produced.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| GitHub main/protected environment → production Supabase | CI commit, credentials, target, and ordering control production authority. |
| Migration → deployed functions | Functions must never address schema/RPCs absent from production. |
| New production authority → legacy provider retirement | Deletion must follow proof and leave no fallback/secret exposure. |
| Production response/package → anonymous user | Public surfaces must expose only capability URLs and boolean/generic results. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-06-CI | Tampering/Elevation | HIGH | production workflow | mitigate | Main-only protected environment, contents-read permission, exact lockfile CLI, serialized runs, target guard, schema-first fail-fast steps. |
| T-02-06-TG | Spoofing | HIGH | Supabase project target | mitigate | Compare protected production ref against recorded development ref and capture redacted target/run evidence. |
| T-02-06-SD | Tampering | HIGH | schema/function order | mitigate | Blocking `db push` and migration check precede unreachable-on-failure function deploys; same commit/version evidence. |
| T-02-06-SL | Information Disclosure | HIGH | CI logs/package/evidence | mitigate | Protected secrets, masking, redacted evidence, post-pack content scan, no auth/profile/email/service implementation. |
| T-02-06-LF | Elevation/Tampering | HIGH | legacy fallback | mitigate | Approved dev proof precondition, complete tracked/caller/dependency deletion, external resource/secret retirement after production proof. |
| T-02-06-RP | Repudiation | MEDIUM | deploy/retirement | mitigate | Preserve workflow URL, commit, migration/function versions, smoke results, dated approval and provider retirement record. |
| T-02-06-DO | Denial of Service | MEDIUM | migration/deploy | mitigate | Serialized non-cancelling runs, fail-fast schema gate, forward-only corrective migration runbook, anonymous local review unaffected by outage. |

Execution MUST stop if any HIGH threat remains unresolved, production deployment lacks actual schema evidence, or legacy retirement is incomplete. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
Final proof is one successful protected main workflow that applies the production schema, deploys all functions from the same commit, passes non-mutating smoke/package/compatibility checks, and records complete legacy retirement. A source/build-only result does not pass.
</verification>

<success_criteria>
- D-02 cleanly removes Render/Fastify-hosted/pg/Resend/email recovery after equivalence proof.
- D-14 leaves exactly development and production Supabase projects.
- D-15 production deploys automatically from main through CI, schema first.
- D-16 leaves no migration/dual-write/reconciliation machinery.
- D-17 Supabase is the only live acceptance/production target.
- D-19 migrates every caller with no compatibility surface while existing verified local state remains valid.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-06-SUMMARY.md` with deletion inventory, production workflow evidence, and approval status.
</output>
