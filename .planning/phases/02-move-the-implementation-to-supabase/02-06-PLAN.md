---
phase: 02-move-the-implementation-to-supabase
plan: 06
type: execute
wave: 6
depends_on: [02-03, 02-05]
files_modified:
  - scripts/verify-supabase-support.mjs
  - docs/support-service-operations.md
  - tests/e2e/support-payment.spec.ts
  - tests/e2e/support-recovery.spec.ts
  - tests/e2e/support-restore.spec.ts
  - .planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
user_setup:
  - service: supabase
    why: "Exactly two hosted projects provide development and production Postgres/Auth/Edge Functions (D-14)."
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Supabase Dashboard → Account → Access Tokens; development deployment environment only for this plan"
      - name: SUPABASE_DEVELOPMENT_PROJECT_REF
        source: "Supabase development project → Settings → General → Reference ID"
      - name: SUPABASE_DEVELOPMENT_DB_PASSWORD
        source: "Supabase development project database password; shell/secret store only"
    dashboard_config:
      - task: "Create/identify exactly one development and one production project; record non-secret refs/owners without creating a third project"
        location: "Supabase Dashboard"
      - task: "Enable GitHub as the only social provider for development; disable email/magic-link sign-in and configure exact Supabase Auth callback"
        location: "Development project → Authentication → Providers / URL Configuration"
  - service: github
    why: "Development GitHub OAuth proves support-scoped identity without local Cumpa authentication."
    env_vars:
      - name: GITHUB_OAUTH_CLIENT_ID
        source: "Development GitHub OAuth App; configure in Supabase Auth only, not Cumpa or evidence"
      - name: GITHUB_OAUTH_CLIENT_SECRET
        source: "Development GitHub OAuth App; configure in Supabase Auth secret field only"
    dashboard_config:
      - task: "Create the development OAuth App with exact Supabase `/auth/v1/callback` URL and no repository scopes"
        location: "GitHub Settings → Developer settings → OAuth Apps"
  - service: stripe
    why: "Real test-mode Checkout and signed webhook delivery are required hosted acceptance evidence."
    env_vars:
      - name: STRIPE_SECRET_KEY
        source: "Stripe test-mode secret key; set only as development Supabase function secret"
      - name: STRIPE_WEBHOOK_SECRET
        source: "Signing secret for development Supabase stripe-webhook endpoint"
      - name: STRIPE_PRICE_ID
        source: "One fixed test-mode USD $49.99 Price"
    dashboard_config:
      - task: "Create/confirm one-time USD $49.99 test Price and webhook endpoint for completed and async-success events"
        location: "Stripe Dashboard test mode → Product catalog / Developers → Webhooks"
must_haves:
  truths:
    - "The development Supabase project has the exact Git migration and functions deployed in schema-before-function order; local compilation is not accepted as deployment proof (D-14, D-17)."
    - "A real GitHub OAuth support action reaches real Stripe test Checkout, a signed test webhook verifies the first installation, and Cumpa polling promotes/closes the dialog (D-05, D-06, D-12)."
    - "The same GitHub account restores a second fresh installation automatically; an unpaid account receives the same generic completion and remains unverified (D-07, D-10, D-11)."
    - "Actual packed Cumpa assets contain no hosted secrets, Supabase privileged client/session, OAuth token/profile/email, or standalone service implementation (PAY-04, SUP-05)."
    - "Failure cases—invalid signature/product/amount/binding, replay, cancellation, delayed webhook, expired/reused intent—fail closed without gating the workspace."
  artifacts:
    - path: "scripts/verify-supabase-support.mjs"
      provides: "Bounded redacted hosted-development acceptance driver for start/status, hostile cases, and package inventory"
    - path: "docs/support-service-operations.md"
      provides: "Supabase dev/prod configuration, deploy, rotation, logs, incident, retention, and evidence runbook"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md"
      provides: "Approved redacted development schema/function/OAuth/Stripe/restore/package evidence"
  key_links:
    - from: "supabase/migrations/20260814000000_support_authority.sql"
      to: "development Supabase migration history"
      via: "`supabase db push` before function deploy"
      pattern: "20260814000000"
    - from: "src/server/support-client.ts"
      to: "development support-api"
      via: "packed Cumpa `CUMPA_SUPPORT_SERVICE_URL` HTTPS capability"
      pattern: "CUMPA_SUPPORT_SERVICE_URL"
    - from: "development Stripe endpoint"
      to: "stripe-webhook → fulfillment RPC → support-api status → Cumpa store"
      via: "real signed test-mode delivery and polling"
      pattern: "checkout.session.completed"
  prohibitions:
    - "HIGH: Do not accept mocks, local-only functions, TypeScript/build success, dashboard screenshots without migration history, or browser redirects as hosted authority evidence (D-17)."
    - "HIGH: Do not record secrets, OAuth codes/tokens, GitHub profile/email, payer email, database passwords, raw intent tokens, or webhook signatures in evidence/logs."
    - "Do not delete or deploy the legacy backend in this plan; 02-07 owns deletion only after this checkpoint is approved (D-02)."
---

<objective>
Deploy and prove the complete Supabase migration against the sole hosted non-production acceptance target before any legacy backend deletion.

Purpose: Turn the blocked Phase 01 provider checkpoint into real development-project evidence for GitHub OAuth, Stripe Checkout/webhooks, automatic restore, polling, privacy, and package safety.
Output: Deployment/acceptance driver, Supabase operations runbook, redacted hosted evidence, and blocking human approval.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-03-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-04-SUMMARY.md
@.planning/phases/01-add-voluntary-stripe-support-payment-and-email-recovery/01-05-SUMMARY.md
@docs/support-service-operations.md
@tests/e2e/support-payment.spec.ts
@tests/e2e/package-assets.spec.ts

<interfaces>
Development deployment order:
1. Verify `SUPABASE_DEVELOPMENT_PROJECT_REF` is not the recorded production ref.
2. Link the checkout to the development project non-interactively.
3. Set server-only Edge secrets: `SUPPORT_PUBLIC_ORIGIN`, `STRIPE_SECRET_KEY`, `STRIPE_WEBHOOK_SECRET`, `STRIPE_PRICE_ID`; use platform-provided Supabase server variables.
4. `supabase db push` and query migration history/RLS/RPC presence.
5. Deploy `support-api`, `support-flow`, then `stripe-webhook` from the same Git commit.
6. Configure development GitHub provider and Stripe test webhook exact URLs.
7. Run real hosted journeys and capture redacted identifiers/statuses/timestamps only.

Evidence must identify project ref suffix/environment, Git commit, migration version, deployed function versions, Stripe test event/session IDs in redacted form, two fresh installation ID digests/redacted suffixes, status transitions, package tar inventory scan, and human approval. It must never contain a credential or full capability token.
</interfaces>
</context>

<tasks>

<task type="auto" gate="blocking">
  <name>Task 1: Apply schema and functions to the hosted development target</name>
  <files>docs/support-service-operations.md, .planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md</files>
  <read_first>
    - supabase/config.toml — exact function JWT settings
    - supabase/migrations/20260814000000_support_authority.sql — schema that must deploy first
    - .planning/phases/02-move-the-implementation-to-supabase/02-02-SCHEMA-EVIDENCE.md — local application baseline
    - supabase/functions/support-api/index.ts — start/status endpoint and required secrets
    - supabase/functions/support-flow/index.ts — Auth callback/origin/secrets
    - supabase/functions/stripe-webhook/index.ts — endpoint/events/signing secret
    - docs/support-service-operations.md — legacy runbook to rewrite, not append with dual-provider instructions
  </read_first>
  <action>Validate that exactly two project refs are documented and the chosen target is development. Configure the development GitHub provider and function secrets through provider CLI/dashboard without writing values to Git. Rewrite `docs/support-service-operations.md` around Supabase ownership, environment separation, exact callback/webhook URLs, schema-before-function deployment, secret rotation, provider outage behavior, log correlation, D-18 identifier retention/deletion review, forward-fix migration recovery, and evidence collection; version every supported non-secret Auth setting and enumerate any unavoidable dashboard-only setting with expected value and drift evidence. Label legacy retirement pending Plan 02-07 rather than presenting Render as fallback. Link to development, push the migration, inspect migration history/RLS/RPC grants on that actual hosted database, deploy all three functions from the current commit, and record redacted commands/results/versions plus Auth dashboard/config comparison in `02-06-HOSTED-EVIDENCE.md`. This blocking schema-application task cannot pass on local evidence, source inspection, function deployment before schema, or build/type success.</action>
  <acceptance_criteria>
    - Hosted development migration history contains `20260814000000` and direct public role access remains denied.
    - All three functions are deployed from the same recorded commit only after schema application.
    - Development Auth has GitHub only, exact callbacks/origins, and no email/magic-link provider path.
    - Evidence and runbook contain names/locations but no secret values or auth/payment/capability material.
  </acceptance_criteria>
  <verify>
    <automated>npx supabase@2.114.0 migration list --linked &amp;&amp; npx supabase@2.114.0 functions list --project-ref "$SUPABASE_DEVELOPMENT_PROJECT_REF"</automated>
  </verify>
  <done>The actual hosted development database and functions match the Git artifacts with redacted deployment/schema evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Exercise real Stripe, status, restore, hostile, and package paths</name>
  <files>scripts/verify-supabase-support.mjs, tests/e2e/support-payment.spec.ts, tests/e2e/support-recovery.spec.ts, tests/e2e/support-restore.spec.ts, .planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md</files>
  <read_first>
    - tests/e2e/support-payment.spec.ts — Phase 1 journey to rewrite for the real hosted Support action
    - tests/e2e/support-recovery.spec.ts — email restoration oracle to rename and supersede completely
    - tests/e2e/package-assets.spec.ts — tar inventory/content secret scan pattern
    - scripts/verify-production-artifacts.mjs — generated package safety pattern
    - .planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md — Hosted acceptance matrix and negative cases
  </read_first>
  <action>Own the payment/restore Playwright cutover wholly in this hosted acceptance plan: rewrite `support-payment.spec.ts`, rename `support-recovery.spec.ts` to `support-restore.spec.ts`, remove the old path in the same change, and provide deterministic hosted-development seams for exact test account/installation inputs. Do not claim a RED cycle here; Plans 02-04/02-05 did not modify or execute these E2E files. Create one bounded Node 24 acceptance driver that accepts required URLs/opaque test inputs through environment, never prints secrets, times out network/subprocess actions, and records redacted results. Execute both rewritten E2E files plus the driver to prove anonymous start/status, GitHub-authenticated fixed-price Checkout, cancellation/delay usability, signed-webhook-only installation verification, replay idempotency, second-installation restore, generic unpaid restore, hostile signature/product/amount/currency/quantity/user/installation and expired/reused-intent failures, persisted suppression, and packed-artifact secret safety. Append exact observed statuses, timestamps, redacted IDs, command exits, and artifact scan results without synthesizing interactive success.</action>
  <acceptance_criteria>
    - Real Stripe test Checkout and signed webhook—not mocks or redirect—produce verified status and local monotonic promotion.
    - Same-account second-installation restore and non-supporter generic result are observed against development Supabase.
    - Replay and all listed hostile cases fail closed with one settlement/binding authority result.
    - Packed Cumpa assets pass content/inventory scans and ordinary review remains usable during provider delay/outage.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --environment development --evidence .planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md &amp;&amp; npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/support-restore.spec.ts</automated>
  </verify>
  <done>All automatable hosted and package checks are recorded honestly; interactive OAuth observations are ready for the approval gate.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Approve hosted GitHub OAuth support and restore evidence</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md</files>
  <read_first>
    - .planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md — automated development evidence awaiting approval
    - docs/support-service-operations.md — exact expected journey and safe evidence fields
  </read_first>
  <action>Review the automated evidence, then perform only the interactive hosted OAuth observations listed below. Append the dated approval or exact failure to the evidence artifact without credentials or PII; approval is the hard dependency for legacy deletion.</action>
  <what-built>Development Supabase schema/functions, GitHub-only hosted OAuth, server-created Stripe test Checkout, signed webhook settlement, two-installation restore, hostile-path checks, Cumpa polling/UI behavior, and packed-package safety evidence.</what-built>
  <how-to-verify>
    1. Launch packed Cumpa with only the development public capability URL and a fresh support state.
    2. Confirm the ready review workspace appears before auth and every review control remains usable.
    3. Choose Support; confirm a new hosted tab requests GitHub login before exact USD $49.99 Stripe test Checkout and no auth material appears in the loopback URL.
    4. Complete test payment; confirm only the signed webhook/status poll produces thank-you and machine-wide prompt suppression.
    5. On a second fresh installation choose Restore, authenticate the same GitHub account, and confirm automatic verification without email/device management.
    6. Use an unpaid GitHub account; confirm the hosted completion is generic and installation remains unverified.
    7. Review evidence for real migration/function versions, signed Stripe delivery/replay, negative cases, package scan, and absence of secrets/PII.
  </how-to-verify>
  <acceptance_criteria>
    - Human approves all seven observations against the development project.
    - Any false authority, auth leakage, profile/email persistence, feature gating, missing hosted schema proof, mocked provider evidence, or failed payment/restore E2E blocks 02-07.
    - Approval is appended with date/operator and no credential/PII to the evidence artifact.
  </acceptance_criteria>
  <verify>
    <automated>node scripts/verify-supabase-support.mjs --environment development --evidence .planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md &amp;&amp; npx playwright test --config=tests tests/e2e/support-payment.spec.ts tests/e2e/support-restore.spec.ts</automated>
    <human-check>Complete the two-account/two-installation hosted browser matrix above and type `approved` only when evidence matches.</human-check>
  </verify>
  <done>Real development Supabase/GitHub/Stripe/package behavior is approved; legacy deletion may begin.</done>
  <resume-signal>Type `approved` or describe the exact failed observation.</resume-signal>
</task>

</tasks>

## Artifacts this phase produces

- `scripts/verify-supabase-support.mjs`: bounded, secret-redacting hosted acceptance driver.
- `docs/support-service-operations.md`: Supabase dev/prod operations and evidence runbook replacing Render/Resend operating instructions.
- `.planning/phases/02-move-the-implementation-to-supabase/02-06-HOSTED-EVIDENCE.md`: actual hosted development schema/function/OAuth/Stripe/restore/negative/package proof plus approval.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Operator/CI → development project | Wrong-project commands or leaked credentials can mutate production or expose secrets. |
| GitHub/Stripe hosted systems → evidence | Evidence must prove real provider behavior without recording identity/credentials. |
| Development service → packed Cumpa | Public capability URL may ship; privileged configuration must not. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-06-WT | Tampering | HIGH | deployment target | mitigate | Assert development project ref differs from production, record ref suffix/commit, schema before functions, inspect migration/function lists. |
| T-02-06-AE | Spoofing | HIGH | acceptance evidence | mitigate | Real GitHub OAuth, Stripe test Checkout, signed webhook delivery IDs, database/status observations, payment/restore E2E, and human browser approval; mocks rejected. |
| T-02-06-SL | Information Disclosure | HIGH | scripts/docs/evidence/logs | mitigate | Environment-only secrets, structured redaction, no full capability/auth/payment/PII values, packed content scan. |
| T-02-06-FA | Elevation of Privilege | HIGH | hostile-path matrix | mitigate | Wrong signatures/payment facts/intents/accounts are exercised against hosted target and must leave authority unchanged. |
| T-02-06-DOS | Denial of Service | MEDIUM | provider outage/delay | mitigate | Bounded driver, Cumpa timeouts/poll cadence/dismissal, ordinary review availability observed. |
| T-02-06-RP | Repudiation | MEDIUM | deployment/fulfillment | mitigate | Record commit, migration/function versions, redacted event/session correlations, timestamps, and human approval. |

Execution MUST stop if any HIGH threat remains unresolved or hosted evidence is incomplete. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
Hosted development proof is the deliverable: actual schema history, deployed function versions, real GitHub OAuth, real Stripe test Checkout/signed webhook, two installations/two account classes, hostile cases, local polling, and package safety. No local-only substitute passes.
</verification>

<success_criteria>
- D-14 exactly-two-project environment model is recorded.
- D-17 Supabase is the only hosted acceptance target and real provider evidence is approved.
- REC-01/REC-02 now have GitHub OAuth/account+installation proof; REC-03 remains unlimited.
- All PAY/SUP behavior remains unchanged apart from locked authentication/restore handoff.
- 02-07 is unblocked only by explicit approved evidence.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-06-SUMMARY.md` with development deployment identifiers, E2E/evidence results, and approval status.
</output>
