---
phase: 02-move-the-implementation-to-supabase
plan: 05
type: tdd
wave: 5
depends_on: [02-04]
files_modified:
  - src/web/api/client.ts
  - src/web/App.vue
  - src/web/components/SupportDialog.vue
  - tests/integration/support-dialog.spec.ts
autonomous: true
requirements: [PAY-01, PAY-02, PAY-03, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
must_haves:
  truths:
    - "Every review launch and feature remains anonymous and unrestricted; GitHub authentication starts only after Support or Restore (D-04, PAY-02)."
    - "The dialog presents one optional USD $49.99 Support action, Restore, and Not now; either hosted action opens a new tab while the workspace remains usable (D-05, D-07, D-12, PAY-01, SUP-02, SUP-03)."
    - "The dialog appears until machine-wide verification, then polling closes it automatically and persisted verification suppresses later launches (SUP-01, SUP-04, SUP-05)."
    - "Only status refresh can promote verification; browser actions, redirects, tab closure, delay, cancellation, and unavailable responses never do (PAY-03)."
    - "No email, magic-link, recovery-token, Supabase session, OAuth code, GitHub profile, or payer email state/caller remains in the browser (D-08, D-09, D-19, REC-01, REC-02)."
    - "Restore remains available for repeated fresh installations without device-management UI (D-07, D-11, REC-03)."
  artifacts:
    - path: "src/web/api/client.ts"
      provides: "Strict Support/Restore action caller and status refresh"
      exports: ["SessionClient"]
    - path: "src/web/App.vue"
      provides: "New-tab action handoff and polling-driven state promotion"
    - path: "src/web/components/SupportDialog.vue"
      provides: "Accessible optional Support/Restore/waiting/verified dialog without email recovery"
    - path: "tests/integration/support-dialog.spec.ts"
      provides: "Rendered RED/GREEN contract for every browser caller and SUP behavior"
  key_links:
    - from: "src/web/components/SupportDialog.vue"
      to: "src/web/App.vue"
      via: "support and restore action events"
      pattern: "support|restore"
    - from: "src/web/App.vue"
      to: "src/web/api/client.ts"
      via: "startSupportAction and refreshSupportStatus"
      pattern: "startSupportAction|refreshSupportStatus"
    - from: "src/web/App.vue"
      to: "src/server/capabilities.ts"
      via: "loopback action/status APIs established by 02-04"
      pattern: "support/(?:start|status|refresh)"
  prohibitions:
    - "HIGH: No browser action, redirect, or local timer may directly mark support verified."
    - "HIGH: No OAuth/session/code/profile/email/secret material may enter browser state, loopback requests, or logs."
    - "No email fallback, recovery alias, feature gate, startup hosted request, or second browser support contract (D-04, D-08, D-19)."
---

<objective>
Migrate every browser caller to the hosted Support/Restore action contract and preserve the complete optional support-dialog experience.

Purpose: Complete the clean local cutover after the shared/server interface is green, without mixing hosted-provider acceptance or Playwright payment/restore rewrites into this plan.
Output: One RED/GREEN rendered integration contract and three browser production files with all obsolete email-recovery callers removed.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-PATTERNS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-04-SUMMARY.md
@src/web/api/client.ts
@src/web/App.vue
@src/web/components/SupportDialog.vue
@tests/integration/support-dialog.spec.ts

<interfaces>
- `SessionClient.startSupportAction('support' | 'restore')` parses `SupportStartResultSchema` from the bearer-protected loopback API.
- `App.vue` opens a returned ready `flowUrl` in a new tab; unavailable leaves the local workspace and dialog usable.
- Only `refreshSupportStatus()` observes hosted verification and drives the existing verified/thank-you/close transition.
- The dialog retains Support at USD $49.99, Restore, Not now, focus trap/restoration, inert background, Escape/dismiss, waiting, unavailable, and verified states.
</interfaces>
</context>

<feature>
  <name>Optional hosted Support/Restore browser experience</name>
  <files>src/web/api/client.ts, src/web/App.vue, src/web/components/SupportDialog.vue, tests/integration/support-dialog.spec.ts</files>
  <behavior>
    - Unverified ready workspace shows the optional support dialog; Not now dismisses only the current session and all review controls remain usable.
    - Existing persisted verification suppresses the dialog at launch without a hosted startup dependency.
    - Support and Restore call the strict loopback action and open only its returned hosted URL in a new tab.
    - Waiting remains dismissible; delayed, cancelled, unavailable, and visibility-change paths do not claim payment failure or verification.
    - Verified polling shows the brief thank-you and closes automatically; the next launch remains suppressed.
    - No email form, challenge, recovery status, magic link, or OAuth material appears in UI or browser state.
  </behavior>
  <implementation>Use the existing rendered integration harness as the single deterministic RED/GREEN seam. Hosted payment/restore Playwright files are untouched here and belong wholly to Plan 02-06.</implementation>
</feature>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: RED — specify the complete browser action and dialog contract</name>
  <files>tests/integration/support-dialog.spec.ts</files>
  <behavior>
    - Support/Restore new-tab handoff and unavailable behavior.
    - Unpaid dismissal and unrestricted review behavior.
    - Polling-driven automatic close and persisted launch suppression.
    - Accessibility, focus, inert background, waiting, cancellation, and delayed status behavior.
    - Absence of every email-recovery control and browser state seam.
  </behavior>
  <read_first>
    - tests/integration/support-dialog.spec.ts — current real-browser support dialog oracle
    - src/web/App.vue — current Checkout/recovery handlers and polling lifecycle
    - src/web/components/SupportDialog.vue — current focus/accessibility/dialog modes
    - .planning/phases/02-move-the-implementation-to-supabase/02-04-SUMMARY.md — exact green loopback action/status interface
  </read_first>
  <action>Rewrite the rendered integration contract for every `<feature><behavior>` case. Exercise both Support and Restore events through the real browser client seam; assert new-tab behavior, unchanged local workspace, Not now, waiting/delay/cancellation/unavailable states, visibility polling, verified thank-you/automatic close, next-launch suppression, focus restoration, Tab containment, Escape/dismiss, inert background, and absence of email/recovery UI/state. Require the focused file to fail against the old browser callers; if it passes, correct the test.</action>
  <acceptance_criteria>
    - The single integration file covers every browser production caller and all formal SUP-01 through SUP-05 outcomes.
    - RED failures identify obsolete Checkout/email-recovery callers or missing action/polling behavior.
    - The test never mocks local verification from a redirect or tab result.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --config=tests tests/integration/support-dialog.spec.ts</automated>
  </verify>
  <done>The rendered browser cutover contract is demonstrably RED; commit `test(02-05): specify hosted support browser flow`.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: GREEN — migrate all browser callers and remove email state</name>
  <files>src/web/api/client.ts, src/web/App.vue, src/web/components/SupportDialog.vue</files>
  <behavior>
    - Every RED browser behavior passes through the 02-04 loopback interface.
    - No obsolete browser method, state, alias, timer, or recovery control survives.
  </behavior>
  <read_first>
    - tests/integration/support-dialog.spec.ts — RED rendered behavior/accessibility contract
    - src/web/api/client.ts — closure-held loopback bearer and current support methods
    - src/web/App.vue — support refs/handlers and visibility polling lifecycle
    - src/web/components/SupportDialog.vue — focus trap, modes, busy/waiting/verified UI
  </read_first>
  <action>Replace browser client support methods with `startSupportAction(action)` using the strict shared schemas. Route Support and Restore through it, open the returned URL in a new tab, keep the dialog in cancellable waiting state, and reuse the existing bounded polling plus visibility refresh. Allow only refresh to transition verified/thank-you. Remove every email, recovery-pending, challenge, submission, recovery-status ref/handler/timer and old caller; do not alias them. Preserve the fixed USD $49.99 optional-support copy, Restore/Not now hierarchy, unrestricted workspace, focus restoration, Tab containment, Escape/dismiss, inert background, busy gating, waiting, verified, automatic close, and launch suppression per D-07 through D-12.</action>
  <acceptance_criteria>
    - The focused rendered suite is green for all PAY/SUP browser outcomes and OAuth restoration handoff.
    - Both actions open a hosted flow in a new tab and never navigate or close the workspace.
    - Polling alone closes and persistently suppresses the dialog after verification.
    - No obsolete email/recovery browser caller or alias remains; unrelated draft-recovery concepts are untouched.
  </acceptance_criteria>
  <verify>
    <automated>npx playwright test --config=tests tests/integration/support-dialog.spec.ts</automated>
  </verify>
  <done>Every browser caller is green on the hosted action contract with full clean-cutover inventory; commit `feat(02-05): move support dialog to hosted OAuth actions`.</done>
</task>

</tasks>

## Artifacts this phase produces

- `src/web/api/client.ts`: strict `startSupportAction` plus retained status/refresh methods.
- `src/web/App.vue`: hosted Support/Restore handoff and polling-only verification promotion.
- `src/web/components/SupportDialog.vue`: accessible optional support experience without email recovery.
- `tests/integration/support-dialog.spec.ts`: deterministic rendered RED/GREEN proof for every browser caller.

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Browser → authenticated loopback API | Browser input is untrusted but protected by existing Host/Origin/bearer boundary. |
| Hosted browser tab → local workspace | OAuth/Stripe results must never confer local authority. |
| Hosted boolean status → browser dialog | Only the loopback's persisted verified status may close/suppress the dialog. |

## STRIDE Threat Register — ASVS L1

| Threat ID | Category | Severity | Component | Disposition | Mitigation Plan |
|---|---|---:|---|---|---|
| T-02-05-LA | Elevation of Privilege | HIGH | browser action API | mitigate | Strict action enum, existing bearer/Host/Origin boundary, installation ID remains server-owned. |
| T-02-05-AU | Information Disclosure | HIGH | browser state/logs | mitigate | No OAuth/code/token/profile/email/secret fields; hosted flow remains in separate tab. |
| T-02-05-LS | Elevation of Privilege | HIGH | verified UI state | mitigate | Only parsed loopback verified status transitions state; action/redirect/unverified/failure cannot promote. |
| T-02-05-FG | Elevation of Privilege | HIGH | review experience | mitigate | Support controls dialog visibility only; rendered integration proves all review controls remain usable. |
| T-02-05-DOS | Denial of Service | MEDIUM | polling/action failure | mitigate | Existing bounded cadence, timeout from server client, visibility coalescing, dismissible failure/waiting states. |

Execution MUST stop if any HIGH threat remains unresolved or uncovered by the rendered contract. No HIGH threat may be accepted or transferred.
</threat_model>

<verification>
Complete one deterministic RED → GREEN cycle with `tests/integration/support-dialog.spec.ts`. Plan 02-06 alone owns payment/restore Playwright rewrites and hosted truth.
</verification>

<success_criteria>
- D-04 keeps all ordinary review work anonymous and unrestricted.
- D-05 through D-12 preserve support/restore behavior with GitHub OAuth replacing email recovery.
- PAY-01/PAY-02/PAY-03 and SUP-01 through SUP-05 have rendered browser coverage with exact canonical meanings.
- D-19 migrates every browser caller with no compatibility alias.
</success_criteria>

<output>
Create `.planning/phases/02-move-the-implementation-to-supabase/02-05-SUMMARY.md` with RED/GREEN evidence and a complete browser caller-removal inventory.
</output>
