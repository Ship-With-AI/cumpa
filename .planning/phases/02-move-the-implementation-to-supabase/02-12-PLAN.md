---
phase: 02-move-the-implementation-to-supabase
plan: 12
type: execute
wave: 12
depends_on: [02-11]
files_modified:
  - .github/workflows/deploy-supabase-production.yml
  - scripts/build-bin.mjs
  - scripts/verify-supabase-support.mjs
  - tests/e2e/package-assets.spec.ts
  - docs/support-service-operations.md
  - .planning/phases/02-move-the-implementation-to-supabase/02-12-PRODUCTION-EVIDENCE.md
autonomous: false
requirements: [PAY-01, PAY-02, PAY-03, PAY-04, SUP-01, SUP-02, SUP-03, SUP-04, SUP-05, REC-01, REC-02, REC-03]
user_setup:
  - service: github
    why: "Protected main and the production environment must land and run the sole deployment/release workflow (D-15)."
    dashboard_config:
      - task: "Protect main, require review/status checks, and restrict the production environment to protected main"
        location: "GitHub branch and environment protection settings"
    env_vars:
      - name: SUPABASE_ACCESS_TOKEN
        source: "Protected production environment secret"
      - name: SUPABASE_PROJECT_REF
        source: "Same complete sole-project ref; protected secret"
      - name: SUPABASE_DB_PASSWORD
        source: "Protected production environment secret"
      - name: CUMPA_RELEASE_SUPPORT_SERVICE_URL
        source: "Protected production environment variable containing the public HTTPS capability URL"
must_haves:
  truths:
    - "Protected main runs local gates, compares SHA-256 of the complete supplied ref with approved lineage before every hosted mutation, then deploys migrations before all three functions to the sole project (D-14, D-15)."
    - "A release-only build input causes `scripts/build-bin.mjs` to embed the public HTTPS capability URL in the generated package entrypoint before dynamically importing the CLI; ordinary source/local builds without that input remain completely support-disabled (D-13)."
    - "Installed production users need no environment variable: the protected workflow's packed artifact advertises Support/Restore while user `CUMPA_SUPPORT_SERVICE_URL` is absent."
    - "A blocking checkpoint configures protection, lands/triggers the actual workflow, waits, and records immutable run ID/URL plus redacted outputs before validation."
    - "Production smoke is limited to deployment/config/version reads and one fresh random unverified status observation with unchanged authority counts; no OAuth/payment/restore mutation runs (D-17)."
  artifacts:
    - path: ".github/workflows/deploy-supabase-production.yml"
      provides: "Protected schema-first sole-project deployment and configured package release"
    - path: "scripts/build-bin.mjs"
      provides: "Release-only public capability embedding with ordinary-build disablement"
    - path: ".planning/phases/02-move-the-implementation-to-supabase/02-12-PRODUCTION-EVIDENCE.md"
      provides: "Immutable protected run, packaged capability, and non-destructive smoke evidence"
  key_links:
    - from: "CUMPA_RELEASE_SUPPORT_SERVICE_URL"
      to: "dist/bin/cumpa.mjs"
      via: "validated HTTPS literal assigned before dynamic CLI import only in release build"
      pattern: "CUMPA_RELEASE_SUPPORT_SERVICE_URL"
    - from: "02-08 project_ref_sha256"
      to: "every protected hosted command"
      via: "hash complete secret ref in memory and compare before db/function operations"
      pattern: "project_ref_sha256"
  prohibitions:
    - "HIGH: No suffix-only guard, second target, function-before-schema deployment, unprotected release, fabricated run evidence, or destructive live matrix."
    - "HIGH: Do not place project credentials, complete ref, OAuth/Stripe secrets, or user-required configuration in the package."
    - "Do not make ordinary source/local builds inherit the release URL."
---

<objective>
Land and execute the protected sole-production deployment while releasing a package that enables the public support capability without end-user environment setup.

Purpose: Complete the missing client-side production enablement and prove it through an immutable real workflow run.
Output: Protected workflow, release-only entrypoint configuration, runbook/tests, actual run evidence, and non-destructive smoke.
</objective>

<execution_context>
@/Users/alessandro/.agents/gsd-core/workflows/execute-plan.md
@/Users/alessandro/.agents/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-move-the-implementation-to-supabase/02-CONTEXT.md
@.planning/phases/02-move-the-implementation-to-supabase/02-RESEARCH.md
@.planning/phases/02-move-the-implementation-to-supabase/02-11-SUMMARY.md
@.planning/phases/02-move-the-implementation-to-supabase/02-08-PROMOTION-EVIDENCE.md
@scripts/build-bin.mjs
@package.json
@supabase/config.toml
</context>

<interfaces>
- Ordinary build: release input absent, generated bin dynamically imports `run` without setting the support URL; D-13 remains disabled.
- Protected release build: validate one public HTTPS URL, JSON-escape it into the generated bin, set `process.env.CUMPA_SUPPORT_SERVICE_URL` before `await import('../cli/run.js')`, then call `run`; installed users set nothing.
- The public URL is non-secret. Project ref and management/provider credentials remain protected and absent from package/evidence.
- Every hosted mutation has an immediately preceding full-ref fingerprint equality check.
</interfaces>
</context>

<tasks>
<task type="auto">
  <name>Task 1: Implement protected deployment and release-only package configuration</name>
  <files>.github/workflows/deploy-supabase-production.yml, scripts/build-bin.mjs, scripts/verify-supabase-support.mjs, tests/e2e/package-assets.spec.ts, docs/support-service-operations.md</files>
  <action>Create one serialized protected-`main` workflow with minimal permissions. Run local application, Deno, database/RLS/grant, browser-disabled, security, legacy-absence, and package gates before hosted credentials. In the protected job, hash the complete ref and compare with approved `project_ref_sha256` immediately before link/database inspection, migration push, and each function deployment; migrate/inspect before deploying `support-api`, `support-flow`, and `stripe-webhook`. Add the smallest production-client mechanism supported by the existing generated bin: `scripts/build-bin.mjs` accepts `CUMPA_RELEASE_SUPPORT_SERVICE_URL` only during the protected release build, validates HTTPS, emits a generated wrapper that sets the runtime support URL before a dynamic CLI import, and otherwise emits a support-unconfigured wrapper. Build/pack the release artifact in that job. Extend tests/verifier to prove ordinary builds remain disabled and release-built package exposes capability metadata and Support/Restore controls with the user's runtime variable absent. Do not click Support/Restore in production.</action>
  <verify><automated>node scripts/verify-supabase-support.mjs --verify-production-workflow .github/workflows/deploy-supabase-production.yml --verify-release-build scripts/build-bin.mjs --expected-mode production-live</automated></verify>
  <done>The workflow structurally enforces protected sole-target deployment and release-only public capability embedding without changing ordinary local mode.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Protect main, land the workflow, and collect its immutable run</name>
  <files>.planning/phases/02-move-the-implementation-to-supabase/02-12-PRODUCTION-EVIDENCE.md</files>
  <what-built>A production workflow ready to deploy the same project and build the configured package from protected main.</what-built>
  <how-to-verify>
    1. Configure main branch protection and the production environment so required review/status gates pass before the environment releases secrets/variables only to protected main.
    2. Set the four protected inputs, with `CUMPA_RELEASE_SUPPORT_SERVICE_URL` equal to the public HTTPS capability origin; do not expose the complete project ref.
    3. Land the workflow commit through the protected-main path and trigger the named production workflow from that exact commit.
    4. Wait for the workflow to finish; do not resume while queued or running.
    5. Record immutable GitHub run ID and run URL, commit SHA, artifact digest, migration/function versions, project-ref fingerprint/suffix, gate results, and redacted non-destructive smoke outputs.
    6. Confirm logs/artifacts contain no complete project ref, database password, access token, provider secret, OAuth material, PII, or signature.
    7. Resume with the immutable run ID/URL and `protected run complete`; do not paste any secret.
  </how-to-verify>
  <verify><automated>node scripts/verify-supabase-support.mjs --check-run-evidence .planning/phases/02-move-the-implementation-to-supabase/02-12-PRODUCTION-EVIDENCE.md --require-immutable-run</automated><human-check>Complete steps 1–7 against the real protected-main workflow.</human-check></verify>
  <done>The actual protected run has finished and immutable redacted evidence exists.</done>
  <resume-signal>Provide the run ID/URL and `protected run complete`, or identify the failed step.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Validate the run, configured package, and non-destructive live smoke</name>
  <files>scripts/verify-supabase-support.mjs, tests/e2e/package-assets.spec.ts, docs/support-service-operations.md, .planning/phases/02-move-the-implementation-to-supabase/02-12-PRODUCTION-EVIDENCE.md</files>
  <action>Only after Task 2, fetch/validate the immutable run facts and require the same `project_ref_sha256` as 02-07/02-08, schema-first ordering, exact commit, and artifact digest. Install the protected run's tarball in isolation with user `CUMPA_SUPPORT_SERVICE_URL` removed; launch it and observe session capability plus visible Support/Restore controls. Make no action/OAuth/Checkout/restore request. Issue one fresh random installation status observation through the packaged client and require generic unverified plus unchanged Auth/authority counts before/after. Validate package secret/full-ref/PII absence and record only URL origin, hashes, counts, versions, and outcomes. Finalize forward-fix guidance: additive migrations; compatible function redeploy only; no reset/down/fallback.</action>
  <verify><automated>node scripts/verify-supabase-support.mjs --mode production-live --check-deployment-evidence .planning/phases/02-move-the-implementation-to-supabase/02-12-PRODUCTION-EVIDENCE.md --verify-configured-package --non-destructive --require-immutable-run</automated></verify>
  <done>The real protected run and its package deliver Support/Restore without user configuration while live observations remain non-destructive.</done>
</task>
</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Protected CI → sole project | Secrets select and mutate the only hosted authority. |
| Release build → installed package | Public capability must be enabled without leaking privileged inputs. |
| Production → smoke/evidence | Observation must not create identity/payment authority. |
## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|---|---|---|---|---|
| T-02-12-WT | Tampering | deployment target | mitigate | Full-ref fingerprint before every hosted mutation. |
| T-02-12-RP | Repudiation | actual run | mitigate | Blocking landing/run checkpoint with immutable ID/URL and artifact digest. |
| T-02-12-SL | Information Disclosure | package/logs | mitigate | Public URL only; scan for full ref, credentials, provider secrets, PII. |
| T-02-12-LD | Tampering | production smoke | mitigate | Capability/status observation with before/after zero counts. |
| T-02-12-DO | Denial of Service | deployment order | mitigate | Gates, migration/inspection, then functions; additive forward-fix. |
</threat_model>
<verification>An actual protected run targets the full-ref fingerprint, deploys schema first, builds the configured package, and records only non-destructive live observations.</verification>
<success_criteria>
- D-13 leaves ordinary source/local builds fully disabled while production package users need no environment variable.
- D-14/D-15 establish one protected direct production path and D-17 limits live checks.
- PAY/SUP/REC production capability is reachable from the installed package with webhook authority unchanged.
</success_criteria>
<output>Create `.planning/phases/02-move-the-implementation-to-supabase/02-12-SUMMARY.md`.</output>
