---
phase: 12-behavior-continuity
plan: 03
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - .planning/phases/12-behavior-continuity/12-UI-REVIEW.md
  - .planning/phases/12-behavior-continuity/12-CONTINUITY.md
autonomous: true
requirements: [CON-01, CON-02, CON-03]

must_haves:
  truths:
    - "A human reviewer completes the ordered eight-step journey — launch, select file, line comment, resolve, summary, export, recover, attached Finish — through the restyled packaged surface, and each step's observable result is recorded."
    - "Seven captures at the exact UI-SPEC filenames and viewports exist in one retained directory, taken from a real packaged CLI served by an ephemeral 127.0.0.1 Fastify, not Vite and not a fixed development port."
    - "Every visible difference from the approved references is itemized as an authorized production-data difference, an authorized inherited departure, or an owning-phase finding; nothing is left unclassified."
    - "The CON-02 command ledger records every suite with its command, exit status, and passed/failed/skipped counts, including the two documented external prerequisites."
    - "The CON-03 evidence names the role/name/focus assertions that already carry the matrix and the Tab-order coverage plan 02 added, without claiming the deferred DEFER-04 recertifications."
  artifacts:
    - path: ".planning/phases/12-behavior-continuity/12-UI-REVIEW.md"
      provides: "CON-01 human journey dossier, CON-03 structural evidence, and the success-criterion-4 visual equivalence record"
      contains: "## Live evidence"
      min_lines: 80
    - path: ".planning/phases/12-behavior-continuity/12-CONTINUITY.md"
      provides: "Completed CON-02 command ledger across every suite, appended to the plan 01 sections"
      contains: "## Full suite ledger"
  key_links:
    - from: ".planning/phases/12-behavior-continuity/12-UI-REVIEW.md"
      to: ".planning/ui-reviews/12-live-20260914/"
      via: "capture manifest names each retained screenshot by the exact UI-SPEC filename and viewport"
      pattern: "12-live-"
    - from: ".planning/phases/12-behavior-continuity/12-UI-REVIEW.md"
      to: ".planning/phases/12-behavior-continuity/12-02-SUMMARY.md"
      via: "the Phase 11 owning-phase finding recorded by plan 02 is transcribed into the dossier's findings table"
      pattern: "Owning-phase finding"
    - from: ".planning/phases/12-behavior-continuity/12-CONTINUITY.md"
      to: "package.json"
      via: "every ledger row names the project script it ran"
      pattern: "npm run test:"
---

<objective>
Produce the evidence half of Phase 12: the CON-01 human journey dossier, the success-criterion-4 visual equivalence record, the CON-03 structural accessibility evidence, and the completed CON-02 command ledger.

Purpose: success criterion 1 asks for a *human reviewer* completing the journey. All eight steps are already covered by robots — `pinned-session.spec.ts:395`, `file-tree.spec.ts:363`, `complete-review-draft.spec.ts:314/390/670/706`, `anchored-review.spec.ts:216`, `agent-ready-export.spec.ts:380/581` — so the missing deliverable is the live evidence, not a seventh multi-minute packaged mega-spec. Success criterion 4 asks for captures visually equivalent to the approved contract, which is the same live-capture evidence shape Phases 09 through 11 produced, now for the integrated result.

This plan runs last because its ledger must reflect the repaired `agent-ready-export.spec.ts` from plan 01 and the new Tab-order step from plan 02. Running it earlier would record a stale result.

Output: `12-UI-REVIEW.md`, the completed `12-CONTINUITY.md`, and seven retained captures.
</objective>

<execution_context>
@gsd-core/workflows/execute-plan.md
@gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/12-behavior-continuity/12-UI-SPEC.md
@.planning/phases/12-behavior-continuity/12-PATTERNS.md
@.planning/phases/12-behavior-continuity/12-01-SUMMARY.md
@.planning/phases/12-behavior-continuity/12-02-SUMMARY.md
@.planning/phases/11-workspace-shell-review-surfaces/11-UI-REVIEW.md
@.planning/phases/11-workspace-shell-review-surfaces/11-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the seven live states from one real packaged CLI session</name>
  <files>.planning/ui-reviews/12-live-20260914/desktop.png, .planning/ui-reviews/12-live-20260914/full-desktop.png, .planning/ui-reviews/12-live-20260914/mobile.png, .planning/ui-reviews/12-live-20260914/mobile-files.png, .planning/ui-reviews/12-live-20260914/details.png, .planning/ui-reviews/12-live-20260914/review-notes.png, .planning/ui-reviews/12-live-20260914/warning-stack.png</files>
  <read_first>
    - `.planning/phases/12-behavior-continuity/12-UI-SPEC.md:128-141` — the Exact Live Evidence Contract. Seven captures, exact filenames, exact viewports: `desktop.png` at `1440×1000`, `full-desktop.png` at `1650×900`, `mobile.png` at `420×900`, `mobile-files.png` at `420×900`, `details.png` at `1440×900`, `review-notes.png` at `1440×900`, `warning-stack.png` at `1440×900`. Each row states the required state and what it is compared against. Note `desktop.png` is `1440×1000`, not `1440×900` — the raster authority `mockups/01b-desktop.png` is genuinely `1440×1000` and must not be rescaled or relabeled.
    - `.planning/phases/12-behavior-continuity/12-UI-SPEC.md:130` — the evidence origin rule: all captures come from one real packaged CLI fixture served by an ephemeral `127.0.0.1` Fastify, never Vite, a static mockup, a component harness, or a fixed development port. Retain them in one `.planning/ui-reviews/12-live-<date>/` directory.
    - `.planning/phases/12-behavior-continuity/12-UI-SPEC.md:144-150` — what counts as visual equivalence, including the three required `document.documentElement.clientWidth === document.documentElement.scrollWidth` measurements and the rule that at `420×900` horizontal overflow belongs only to `.diff-workspace__viewport`.
    - `.planning/ui-reviews/.gitignore:1-8` — rasters are ignored (`*.png`, `*.webp`, `*.jpg`, `*.jpeg`, `*.gif`, `*.bmp`, `*.tiff`). The captures are retained on disk for human comparison and are never committed. Existing sibling directories: `09-live-20260913`, `10-20260913-live`, `11-live-20260914`, `11-live-20260914-r2`.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-REVIEW.md:17-34` — the live-evidence provenance block and the numeric overflow table Phase 11 produced. Reproduce this shape, not a new one.
    - `tests/e2e/agent-ready-export.spec.ts:120-142` — `startGeneratedCli`, the only in-repo pattern that drives an *installed packaged* binary over ephemeral loopback. Note it spawns `acceptance.launch.command` with `acceptance.launch.args`, not `process.execPath`, and passes `CUMPA_LAUNCH_OPTIONS` with ordered base/head selections plus a `CUMPA_BROWSER_OPEN_MARKER` and a fake `BROWSER`. This is the launch idiom the throwaway capture script must copy, because the UI-SPEC evidence origin requires a real packaged CLI.
    - `tests/e2e/agent-ready-export.spec.ts:182-186` — `waitForAttachedLoopbackUrl`, which harvests the ephemeral URL by polling the child's stderr file.
    - `tests/e2e/complete-review-draft.spec.ts:107-130` — the contrasting `spawn(process.execPath, [executablePath], …)` shape. That launches the built dist entrypoint, NOT a packed tarball. Do not use it for evidence origin.
    - `.planning/phases/12-behavior-continuity/12-01-SUMMARY.md` — the custody recipe plan 01 recorded. Reuse the same packed artifact rather than packing a second one; Phase 12 changes zero files under `src/`, so one artifact is valid for the whole phase.
    - `src/web/components/ChangedFilesDialog.vue:23-48` — the narrow `Changed files` dialog, needed for `mobile-files.png` with `Filter files` focused.
    - `src/web/components/DetailsDialog.vue:33-54` — the `Details` dialog contents for `details.png`.
    - `src/web/components/ReviewNotesDialog.vue:108-114,151,165-170,189-196` — the `Review notes` dialog: summary section, comment counts, export readiness and receipt, and the `Finish attached review` region. `review-notes.png` must show an accepted summary, the counts, export readiness, and the current paired-export receipt.
  </read_first>
  <action>
    Write a throwaway capture script under a gitignored scratch path — not under `tests/` and not committed. Launch the packaged CLI from the artifact plan 01 packed, using the `startGeneratedCli` idiom at `agent-ready-export.spec.ts:120-142`: spawn `acceptance.launch.command` with its args, supply ordered base/head selections through `CUMPA_LAUNCH_OPTIONS`, redirect the browser open to a marker file, and harvest the ephemeral loopback URL from stderr the way `waitForAttachedLoopbackUrl` does. Do not use the dist-entrypoint spawn shape; it does not satisfy the evidence-origin rule.

    Drive one session to the state each capture requires, then take the seven screenshots at the exact viewports and exact filenames from the UI-SPEC manifest, into `.planning/ui-reviews/12-live-20260914/`. Use one fixture and one selection throughout so `full-desktop.png` is genuinely the same state at a wider treatment and `mobile-files.png` shows the same selected/tree state as `desktop.png`.

    For `review-notes.png` the dialog must show a real accepted summary, real comment counts, real export readiness, and a real current paired-export receipt — so save a summary and complete one export before capturing. For `warning-stack.png` induce real selector drift and stale/orphan history with the dialog layer closed. Do not stage a fake warning.

    While the session is live, take the numeric measurements the UI-SPEC requires via `page.evaluate`: `document.documentElement.clientWidth` and `scrollWidth` at `1440×1000`, `1650×900`, and `420×900`; and at `420×900` confirm horizontal overflow is confined to `.diff-workspace__viewport` with Phase 10's inherited `640px` side-by-side canvas intact. Also collect the computed-style values the UI-SPEC names for the toolbar, footer, body/control type, dialog boundary, selected tree row, and Monaco token consumers, in the shape `11-UI-REVIEW.md:29-34` used. Record the raw numbers now; Task 2 tabulates them.

    Record the session provenance while you have it: the artifact identity or package version, fixture paths, pinned commit IDs or exact-patch digest, capture time, and viewport per capture. Record the repository root as a placeholder, never as an absolute home path.

    Delete the throwaway script when the captures and measurements are in hand. The rasters stay on disk and are ignored by `.planning/ui-reviews/.gitignore`; they are never committed.
  </action>
  <acceptance_criteria>
    - All seven files exist with the exact UI-SPEC names: `ls .planning/ui-reviews/12-live-20260914/ | sort` lists exactly `desktop.png`, `details.png`, `full-desktop.png`, `mobile-files.png`, `mobile.png`, `review-notes.png`, `warning-stack.png`.
    - Real raster metadata matches the contract viewports: `desktop.png` is `1440×1000`; `full-desktop.png` is `1650×900`; `mobile.png` and `mobile-files.png` are `420×900`; `details.png`, `review-notes.png`, and `warning-stack.png` are `1440×900`. Verify with an image-metadata reader, not by assumption.
    - `git status --porcelain .planning/ui-reviews/` is empty — the ignore policy held and no raster is staged.
    - `git status --porcelain` shows no leftover throwaway capture script anywhere in the tree.
    - The `clientWidth === scrollWidth` measurement was taken at all three required viewports and the three pairs are recorded for Task 2.
    - `git diff --stat -- src/ tests/ package.json` is empty — capturing evidence changes no code.
  </acceptance_criteria>
  <verify>
    <automated>test "$(ls .planning/ui-reviews/12-live-20260914/ | sort | tr '\n' ' ')" = "desktop.png details.png full-desktop.png mobile-files.png mobile.png review-notes.png warning-stack.png " &amp;&amp; test -z "$(git status --porcelain .planning/ui-reviews/)" &amp;&amp; git diff --quiet -- src tests package.json</automated>
  </verify>
  <done>Seven correctly-sized captures from one real packaged CLI session exist in the retained directory, the numeric measurements are in hand, and nothing was committed or left behind.</done>
</task>

<task type="auto">
  <name>Task 2: Write the CON-01 journey dossier and the visual equivalence record</name>
  <files>.planning/phases/12-behavior-continuity/12-UI-REVIEW.md</files>
  <read_first>
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-REVIEW.md:1-11` — the front matter and verdict shape: `phase`, `status`, `audit`, `score`, `reviewed`, then `# Phase N UI Review` and `## Verdict`. Reproduce this structure.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-REVIEW.md:17-34` — the live-evidence provenance block and the numeric overflow table (`Viewport | clientWidth | scrollWidth | Result`). Fill the same table with the Task 1 measurements.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-REVIEW.md:60-71` — the evidence-per-check table, one row per check, each citing a capture or a `file:line`.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-UI-REVIEW.md:73-92` — pillar scores, top-priority fixes, and the mockup comparison conclusion.
    - `.planning/phases/12-behavior-continuity/12-UI-SPEC.md` — the End-to-End Human Flow Contract (CON-01). Eight ordered steps with, for each, the reviewer action and the observable proof that behaviour is unchanged. The dossier records the observable result of every step. The contract is explicit that a network response or a screenshot alone does not replace a user-visible result, and a UI message alone does not replace persisted or exported bytes.
    - `.planning/phases/12-behavior-continuity/12-UI-SPEC.md:144-150` — the visual equivalence rules, the three classification buckets, and the statement that an unclassified visible difference fails acceptance.
    - `.planning/phases/12-behavior-continuity/12-UI-SPEC.md` — the Mechanically Checkable Accessibility Contract (CON-03) matrix rows for tree rows, tree filter, active-file toolbar, changed-files dialog, Details dialog, Review notes dialog, and the warning stack.
    - `.planning/phases/12-behavior-continuity/12-02-SUMMARY.md` — the Phase 11 owning-phase finding for the four unroled `ReviewToolbar.vue` containers. Transcribe it into the dossier's findings section; do not restate it from memory.
    - `.planning/phases/12-behavior-continuity/12-01-SUMMARY.md` — the `agent-ready-export.spec.ts` re-authoring result, needed for the CON-01 step 8 row (attached Finish) and the step 5/6 rows (summary and export).
    - `.planning/REQUIREMENTS.md:60-77` — the deferred items. `DEFER-04` defers broader composited contrast, `320px`, and true 400% zoom recertification; those must not be falsely claimed as passing here. The authorized inherited departures — Monaco side-by-side at the local `640px` minimum on mobile, the comments rail as a production overlay, the truthful production footer, and the absence of viewed tracking, `All files`/`Unviewed`, hunk jump, wrap-lines, and unified-diff controls shown in the prototypes — are departures, not production-data excuses.
    - `tests/e2e/pinned-session.spec.ts:395`, `tests/e2e/file-tree.spec.ts:363`, `tests/e2e/complete-review-draft.spec.ts:314,390,670,706`, `tests/e2e/anchored-review.spec.ts:216`, `tests/e2e/agent-ready-export.spec.ts:380,581` — the automated coverage for the eight journey steps. Cite these alongside the human observations so the dossier shows both.
  </read_first>
  <action>
    Create `.planning/phases/12-behavior-continuity/12-UI-REVIEW.md` following the `11-UI-REVIEW.md` structure exactly — front matter, verdict, live evidence, then the tables. Do not invent a new document shape.

    Live evidence section: the provenance block naming the packaged artifact identity, fixture paths, pinned commit IDs or exact-patch digest, capture time, the retained directory `.planning/ui-reviews/12-live-20260914/`, and the explicit statement that the origin was an ephemeral `127.0.0.1` Fastify from a real packaged CLI, not Vite and not a fixed development port. List all seven captures with their viewports and what each shows. Then the numeric overflow table with the Task 1 `clientWidth`/`scrollWidth` pairs at `1440×1000`, `1650×900`, and `420×900`, plus the `420×900` note that horizontal overflow is confined to `.diff-workspace__viewport`.

    CON-01 section: one row per journey step, eight rows, in the contract's order — launch, select file, line comment, resolve, summary, export, recover, attached Finish. Each row carries the reviewer action, the observable result actually seen, and the automated spec that also covers it with its `file:line`. Where the contract demands bytes, cite bytes: for export, that exactly `review.json` and `review.md` exist at the receipt paths and their hashes and byte counts match disk; for attached Finish, that the CLI emitted exactly one canonical document, emitted nothing before Finish, and exited successfully. Record the recovery step's two halves separately: reload-before-Finish restoring the accepted draft, and the corrupt-draft recovery path where a verified byte-for-byte backup precedes replacement.

    CON-03 section: state the mechanically checkable evidence rather than re-deriving the matrix. Give the counts that carry it — 290 `getByRole` calls and 37 `toBeFocused` assertions across the six ROADMAP-named specs — broken down per spec, and name the specific contract properties already asserted: aria-level, aria-selected, aria-expanded, roving tabindex, treeitem/tree/navigation roles, aria-modal, aria-haspopup, and the named dialog controls. Then cite the Tab-order traversal plan 02 added as the one gap that was genuinely missing and is now closed. State plainly that `DEFER-04` recertifications are not claimed.

    Visual equivalence section: exact-viewport comparison of `desktop.png` at `1440×1000` against `mockups/01b-desktop.png`, and `mobile.png` at `420×900` against `mockups/01b-mobile.png`. Assess `full-desktop.png` at `1650×900` solely against the Phase 11 wide contract and its retained live evidence — never against a rescaled `mockups/01b-desktop-full.png`. Itemize every visible difference into exactly one of `Authorized production-data difference`, `Authorized inherited departure`, or `Owning-phase finding`. Leave none unclassified; an unclassified difference fails acceptance.

    Findings section: transcribe the Phase 11 owning-phase finding for the four unroled `ReviewToolbar.vue` containers from `12-02-SUMMARY.md`, keeping its line citations and its git evidence. Add any further owning-phase finding the captures surface, each naming the owning phase and the contract line it departs from. Do not propose a Phase 12 fix for any of them.

    Close with the verdict, the pillar scores, and the mockup comparison conclusion per viewport, matching `11-UI-REVIEW.md:73-92`.

    Every claim carries a command, a count, a capture filename, or a `file:line`. A claim without evidence does not belong in this document.
  </action>
  <acceptance_criteria>
    - `.planning/phases/12-behavior-continuity/12-UI-REVIEW.md` exists with front matter containing `phase: 12`: `grep -c '^phase: 12' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 1.
    - `grep -c '## Live evidence' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 1.
    - All seven capture filenames are cited: `grep -c 'desktop.png\|full-desktop.png\|mobile.png\|mobile-files.png\|details.png\|review-notes.png\|warning-stack.png' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 7.
    - The three required viewports appear: `grep -c '1440×1000\|1650×900\|420×900' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 3.
    - `grep -c '1440×900' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 1 — the three dialog captures are recorded at their own viewport and were not silently merged with `desktop.png`.
    - The CON-01 table has eight journey rows: `grep -c 'Launch\|Select file\|Line comment\|Resolve\|Summary\|Export\|Recover\|Finish attached' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 8.
    - All three classification buckets are used or explicitly declared empty: `grep -c 'Authorized production-data difference' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 1, `grep -c 'Authorized inherited departure'` is at least 1, and `grep -c 'Owning-phase finding'` is at least 1.
    - `grep -c 'ReviewToolbar.vue:25' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 1 — the plan 02 finding was transcribed with its citations.
    - `grep -c 'DEFER-04' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 1 — the deferred recertifications are explicitly not claimed.
    - `grep -c 'clientWidth' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is at least 1 — the numeric overflow table carries real measurements.
    - No absolute home path appears: `grep -c '/Users/' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md` is `0`.
    - `git diff --stat -- src/ tests/ package.json` is empty.
  </acceptance_criteria>
  <verify>
    <automated>grep -q '## Live evidence' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md &amp;&amp; grep -q 'DEFER-04' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md &amp;&amp; ! grep -q '/Users/' .planning/phases/12-behavior-continuity/12-UI-REVIEW.md &amp;&amp; git diff --quiet -- src tests package.json</automated>
    <human-check>Open the live pinned comparison at 1440×1000, 1650×900, and 420×900 beside `mockups/01b-desktop.png` and `mockups/01b-mobile.png`, then walk the eight-step journey end to end in the restyled workspace. Confirm each dossier row matches what you actually see, and that every visible difference is already itemized in one of the three buckets. Success criterion 1 asks for a human completing the journey; this is that step.</human-check>
  </verify>
  <done>The dossier records the eight-step journey with observable results and automated cross-references, the seven captures with provenance and numeric measurements, the CON-03 evidence with its real counts, and every visible difference classified into exactly one bucket.</done>
</task>

<task type="auto">
  <name>Task 3: Complete the CON-02 full-suite command ledger</name>
  <files>.planning/phases/12-behavior-continuity/12-CONTINUITY.md</files>
  <read_first>
    - `.planning/phases/12-behavior-continuity/12-CONTINUITY.md` — the sections plan 01 wrote: `## RED baseline`, `## Re-authoring ledger`, `## GREEN result`, `## Runner reachability`, `## Documented external prerequisites`. Append to them; do not rewrite them.
    - `.planning/phases/11-workspace-shell-review-surfaces/11-VERIFICATION.md:95-99` — the command-ledger row shape with an explicit scope disclaimer, and the two-run order-regression evidence Phase 11 recorded.
    - `.planning/phases/08-semantic-visual-foundation/08-VERIFICATION.md:55-60` — the simpler command-ledger table shape.
    - `package.json:36-49` — the project scripts the ledger must name: `verify:semantic-css` (`:36`), `test:unit` (`:37`), `test:git` (`:38`), `test:api` (`:39`), `test:package` (`:40`), `test:browser` (`:41`), plus `test:runtime-artifact` added by plan 01.
    - `.planning/phases/12-behavior-continuity/12-CONTEXT.md` — the Phase 11 end state to compare against: browser suite 99 passed with two environment-marker failures; `test:unit` 186, `test:git` 69, `test:api` 142 all passing; `verify:semantic-css`, `typecheck:web`, and `build` passing. A count that moves without an explanation is a finding.
    - `.planning/phases/12-behavior-continuity/12-UI-SPEC.md` — the Test Reconciliation Rule's recording requirement: command, exit status, passed/failed/skipped counts, and any prerequisite-gated file. Missing environment markers are not presentation failures and must not be converted into skips.
    - `.planning/phases/12-behavior-continuity/12-01-SUMMARY.md` and `.planning/phases/12-behavior-continuity/12-02-SUMMARY.md` — the two spec changes whose effect this ledger must reflect.
  </read_first>
  <action>
    Run every suite and record the result. Append a `## Full suite ledger` section to `12-CONTINUITY.md` with one row per command: the command as written in `package.json`, the exit status, and the passed/failed/skipped counts. Cover `npm run test:unit`, `npm run test:git`, `npm run test:api`, `npm run test:browser`, `npm run verify:semantic-css`, `npm run typecheck:web`, `npm run build`, and `npm run test:runtime-artifact` with the custody environment from plan 01 exported.

    Compare each count against the Phase 11 end state recorded in `12-CONTEXT.md`. Where a count moved, say why in the same row. The browser suite gains the new Tab-order step from plan 02, so its passing count should rise by the step's test — not by a new test file, since plan 02 adds a step inside an existing test. If a count moves for a reason you cannot name, stop and record it as a finding rather than rounding it into the ledger.

    Add a `## Six named specs` subsection confirming each of the six presentation-sensitive specs the ROADMAP names is executing and passing: `file-tree.spec.ts`, `pinned-session.spec.ts`, `anchored-review.spec.ts`, `complete-review-draft.spec.ts`, `agent-ready-export.spec.ts`, and `responsive-session.spec.ts`. For each, give the runner that executes it, since `agent-ready-export.spec.ts` runs under a different config from the other five. This subsection is the direct answer to success criterion 2.

    Extend the existing `## Documented external prerequisites` section with the observed result from this phase's browser run, so the record shows the same two specs failing for the same two reasons at the same two current line citations. Do not add a marker, do not publish a package, do not stand up a hosted origin, and do not convert either into a skip.

    Add a `## CON-02 verdict` line stating plainly whether the criterion is met: every covered flow still executes, every re-authored assertion satisfies the five Test Reconciliation conditions, no flow was skipped, and the only outstanding failures are the two documented external prerequisites.

    Record no absolute home paths and no real operator support origin.
  </action>
  <acceptance_criteria>
    - `grep -c '## Full suite ledger' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1.
    - Every project script is named in the ledger: `grep -c 'npm run test:unit' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1, and the same holds for `test:git`, `test:api`, `test:browser`, `verify:semantic-css`, `typecheck:web`, and `test:runtime-artifact`.
    - `grep -c '## Six named specs' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1, and all six spec filenames appear: `grep -c 'file-tree.spec.ts\|pinned-session.spec.ts\|anchored-review.spec.ts\|complete-review-draft.spec.ts\|agent-ready-export.spec.ts\|responsive-session.spec.ts' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 6.
    - `grep -c '## CON-02 verdict' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 1.
    - The five plan 01 sections survive: `grep -c '## RED baseline\|## Re-authoring ledger\|## GREEN result\|## Runner reachability\|## Documented external prerequisites' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is at least 5.
    - No absolute home path appears: `grep -c '/Users/' .planning/phases/12-behavior-continuity/12-CONTINUITY.md` is `0`.
    - `npm run test:unit`, `npm run test:git`, `npm run test:api`, `npm run verify:semantic-css`, `npm run typecheck:web`, and `npm run build` all exit 0.
    - `npm run test:browser` fails only on `tests/e2e/marketplace-review.spec.ts` and `tests/e2e/public-support-states.spec.ts`, both for missing external prerequisites.
    - `git diff --stat -- src/` is empty.
  </acceptance_criteria>
  <verify>
    <automated>npm run test:unit &amp;&amp; npm run test:git &amp;&amp; npm run test:api &amp;&amp; npm run verify:semantic-css &amp;&amp; npm run typecheck:web &amp;&amp; grep -q '## CON-02 verdict' .planning/phases/12-behavior-continuity/12-CONTINUITY.md</automated>
  </verify>
  <done>Every suite has a ledger row with command, exit status, and counts; the six named specs are confirmed executing and passing with their runners named; the two external prerequisites are recorded unchanged; and the CON-02 verdict is stated.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| packaged CLI → capture session | Evidence is only worth what its origin is worth. A capture from Vite, a component harness, or a fixed development port proves nothing about the shipped artifact. |
| live session data → committed documents | The session renders a real repository identity, real paths, and a real filesystem layout into every capture and every dossier row. |
| capture rasters → git | Binary screenshots must not enter the repository; the retained directory exists for human comparison only. |
| evidence documents → release decision | This dossier and ledger are what a future maintainer will trust when deciding whether the restyle was behaviour-preserving. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-12-03-01 | Spoofing | Capture provenance | mitigate | A capture taken from a dev server or a mockup would spoof the shipped surface. Task 1 launches the packaged CLI with the `startGeneratedCli` idiom over an ephemeral `127.0.0.1` Fastify, and the dossier's provenance block states the artifact identity, fixture paths, pinned commit IDs, and capture time explicitly. The contrasting dist-entrypoint spawn shape is named in `read_first` specifically so it is not used by accident. |
| T-12-03-02 | Information disclosure | `12-UI-REVIEW.md`, `12-CONTINUITY.md`, capture rasters | mitigate | Absolute home paths, repository roots, and a real operator support origin must not be committed. Both document tasks carry a `grep -c '/Users/'` is `0` criterion, repository roots are recorded as placeholders, and `.planning/ui-reviews/.gitignore:1-8` keeps every raster out of git — asserted by `git status --porcelain .planning/ui-reviews/` being empty. |
| T-12-03-03 | Repudiation | Visual equivalence classification | mitigate | An unclassified visible difference lets a real regression pass as "just production data". The UI-SPEC makes an unclassified difference an acceptance failure; Task 2 requires every difference to land in exactly one of the three buckets and asserts all three bucket labels are present. |
| T-12-03-04 | Tampering | CON-02 full-suite ledger | mitigate | A ledger that rounds an unexplained count movement hides a regression. Task 3 requires every moved count to carry a named reason in its own row and to stop on any movement that cannot be named, and it re-records the two external prerequisites rather than converting them to skips. |
| T-12-03-05 | Elevation of privilege | Throwaway capture script | mitigate | A scratch script that spawns a packaged binary with a fabricated environment is exactly the shape of a persistent backdoor if it is left behind. Task 1 requires deletion and asserts a clean `git status --porcelain`; the script never lives under `tests/`. |
| T-12-03-SC | Tampering | npm/pip/cargo installs | accept | No package-manager install occurs. The capture session reuses the artifact plan 01 already packed, and no new dependency is added. Verified by `git diff --stat package.json package-lock.json` being empty; no Package Legitimacy Audit is required because there are no install tasks. |
</threat_model>

<verification>
- Seven correctly-sized captures exist in `.planning/ui-reviews/12-live-20260914/` and `git status --porcelain .planning/ui-reviews/` is empty.
- `.planning/phases/12-behavior-continuity/12-UI-REVIEW.md` carries the eight-step CON-01 journey, the seven-capture manifest with provenance, the numeric overflow table at all three required viewports, the CON-03 evidence, and every visible difference classified.
- `.planning/phases/12-behavior-continuity/12-CONTINUITY.md` carries the five plan 01 sections plus the full suite ledger, the six-named-specs confirmation, and the CON-02 verdict.
- `npm run test:unit`, `npm run test:git`, `npm run test:api`, `npm run verify:semantic-css`, `npm run typecheck:web`, and `npm run build` exit 0.
- `npm run test:browser` fails only on the two documented external prerequisites.
- `git diff --stat -- src/` is empty across the whole phase.
- Neither document contains an absolute home path or a real operator support origin.

**Do not claim what is deferred:** `DEFER-04` defers broader composited contrast, `320px`, and true 400% zoom recertification. The dossier must say so rather than implying the captures cover them.

**If a capture reveals a visual difference that fits none of the three buckets:** that is an acceptance failure, not a fourth bucket. Record it as an owning-phase finding against the phase whose contract it departs from and say plainly that visual acceptance is blocked until that owner resolves it. Phase 12 does not redesign around it.
</verification>

<success_criteria>
- A human completes the ordered eight-step journey through the restyled packaged surface and every step's observable result is recorded with the automated spec that also covers it.
- Seven captures at the exact UI-SPEC filenames and viewports exist in one retained, gitignored directory, from a real packaged CLI over ephemeral loopback.
- `clientWidth === scrollWidth` holds at `1440×1000`, `1650×900`, and `420×900`, with `420×900` overflow confined to `.diff-workspace__viewport`.
- Every visible difference is classified as an authorized production-data difference, an authorized inherited departure, or an owning-phase finding — none left unclassified.
- The CON-03 evidence names the real assertion counts and the Tab-order coverage plan 02 added, and explicitly does not claim the `DEFER-04` recertifications.
- The CON-02 ledger records every suite's command, exit status, and counts; confirms all six ROADMAP-named specs execute and pass with their runners named; and re-records the two external prerequisites without converting either into a skip.
- Zero files under `src/` changed across the phase.
</success_criteria>

## Artifacts this phase produces

| Artifact | Kind | Produced by |
|---|---|---|
| Seven live captures in `.planning/ui-reviews/12-live-20260914/` (gitignored, retained on disk) | evidence (raster) | Task 1 |
| Numeric viewport and computed-style measurements from the live session | evidence (data) | Task 1 |
| `.planning/phases/12-behavior-continuity/12-UI-REVIEW.md` — CON-01 journey dossier, CON-03 structural evidence, success-criterion-4 visual equivalence record, owning-phase findings | doc (evidence dossier) | Task 2 |
| `## Full suite ledger`, `## Six named specs`, and `## CON-02 verdict` sections of `.planning/phases/12-behavior-continuity/12-CONTINUITY.md` | doc (evidence) | Task 3 |

<output>
Create `.planning/phases/12-behavior-continuity/12-03-SUMMARY.md` when done.
</output>
